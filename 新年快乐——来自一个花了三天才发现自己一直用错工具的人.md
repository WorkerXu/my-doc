# 新年快乐——来自一个花了三天才发现自己一直用错工具的人：实现细节与技术原理分析

## 1. 调研对象

- 原文：Happy New Year from a Guy Who Just Spent Three Days Learning He Was Using the Wrong Tool
- 作者：Mustapha Liaichi
- 地址：https://medium.com/@mustaphaliaichi/happy-new-year-from-a-guy-who-just-spent-three-days-learning-he-was-using-the-wrong-tool-20bf0450e58c
- 发布时间：2026-01-02
- 主题：Crawl4AI 与直接使用 Playwright 在复杂、长时间运行的生产级网页交互任务中的边界

本文最有价值的地方并不是“Playwright 比 Crawl4AI 好”，而是给出了一个非常典型的工程边界：**面向内容抓取的高层抽象，与面向有状态浏览器交互的低层控制，应该是两类不同执行路径。** 对 1000+ 技术博客知识库来说，这一边界直接决定动态归档、筛选器、Load More、无限滚动、分页组件等站点是否能长期稳定运行。

---

## 2. 原文案例复盘

原文任务是从 IMDb 抓取约 30,000 部电影的国家维度评分分布。单个电影页面并不是“打开页面后抽一次 DOM”即可完成，而是需要：

1. 打开评分页；
2. 找出所有国家 Tab；
3. 点击一个国家 Tab；
4. 等待图表更新；
5. 抽取该 Tab 对应的数据；
6. 切换下一个 Tab；
7. 重复 3～6；
8. 再进入下一部电影。

每部电影通常有 5～7 次 Tab 切换，理论上是约 150,000～210,000 次交互。作者最终报告实际完成约 167,000 次国家 Tab 点击，任务连续运行约 5 天。

作者最初使用 Crawl4AI，将复杂交互写成 Python 字符串中的 JavaScript，再交给浏览器上下文执行。这样在简单的“滚动一次”“点一次 Load More”场景中很方便，但当流程变成循环、每一步都要等待、抽取、判断和错误恢复时，出现了几个问题：

- Python 控制流与 JavaScript 控制流被拆成两层；
- selector、等待、异常发生在 JavaScript blob 内，Python 侧只能看到较粗粒度结果；
- 数据需要在浏览器上下文中序列化，再返回 Python；
- 调试时堆栈跨越 Python -> Crawl4AI -> Playwright -> Browser JS；
- 固定 `setTimeout` 容易产生时序不稳定；
- 复杂步骤难以做细粒度 checkpoint 和恢复。

作者改用 Playwright Python 直接操作 `page/locator` 后，交互步骤全部回到 Python 的显式控制流中：点击、等待、抽取、异常捕获、checkpoint 都可以在同一执行模型下完成。作者称重写约 4 小时，并最终稳定完成整个批量任务。

这里的结论应理解为：**当抓取任务的核心是“页面内容”，使用 Crawl4AI 的抽象收益很高；当任务的核心是“有状态的交互过程”，直接 Playwright 的显式控制更合适。**

---

## 3. Crawl4AI 并非不能做多步交互，真正的问题是抽象边界

需要补充原文的一个技术细节：当前 Crawl4AI 本身支持较完整的页面交互能力，包括：

- `js_code`；
- `js_code_before_wait`；
- `wait_for`；
- `session_id`；
- `js_only`；
- `c4a_script`；
- 页面滚动、iframe、Shadow DOM 等能力。

官方文档：

- https://docs.crawl4ai.com/core/page-interaction/
- https://docs.crawl4ai.com/core/browser-crawler-config/

所以问题并不是“Crawl4AI 做不到复杂交互”，而是：**一旦业务需要对每一步交互的状态、异常、输出、重试、审计和 checkpoint 进行精确控制，继续把整个交互过程封装成浏览器脚本，就会损失生产系统需要的可观察性与可恢复性。**

这可以用“抽象泄漏”解释：

```text
简单场景
URL -> Crawl4AI -> Rendered DOM/Markdown

复杂场景
URL
 -> action 1
 -> wait 1
 -> extract 1
 -> assert 1
 -> action 2
 -> wait 2
 -> extract 2
 -> branch
 -> checkpoint
 -> action N
```

后一个流程的核心产物已经不只是最终 DOM，而是**交互轨迹本身**。此时浏览器不再只是“渲染器”，而是工作流执行器。

---

## 4. 技术原理一：内容抓取和交互自动化是两种不同计算模型

### 4.1 内容抓取模型

典型的博客正文抓取近似一个函数：

```text
fetch(url, config) -> snapshot
extract(snapshot, recipe) -> candidate
quality(candidate) -> canonical document
```

它的主要输入是 URL 和配置，主要输出是页面快照/正文。中间状态较少，天然适合 Crawl4AI、Trafilatura、lxml 等高层工具。

### 4.2 有状态交互模型

复杂动态页面更接近有限状态机：

```text
S0: NAVIGATE
 -> S1: WAIT_INDEX_READY
 -> S2: ENUMERATE_ITEMS
 -> S3: SELECT_ITEM(i)
 -> S4: WAIT_ITEM_STABLE
 -> S5: EXTRACT_ITEM(i)
 -> S6: ASSERT_ITEM(i)
 -> i++
 -> S3 ...
 -> DONE
```

状态不只存在于 DOM 中，还包括：

- 当前循环索引；
- 已处理的 tab/page/cursor；
- 当前页面的 DOM/数据签名；
- session/cookie/localStorage；
- 上一步动作是否成功；
- 本步骤重试次数；
- 已生成的部分输出；
- 当前 lease/fencing token。

一旦有这些状态，就应该把它们提升为一等业务对象，而不是藏在一段 JavaScript 中。

---

## 5. 技术原理二：等待条件必须是语义条件，而不是时间猜测

原文早期实现使用固定 `setTimeout(2000)`，这类做法在大批量运行中非常脆弱：

- 2 秒在低负载时过长，浪费吞吐；
- 2 秒在高延迟时又可能不够；
- 页面可能只更新局部区域，网络仍有长连接；
- AJAX 完成不等于目标组件完成渲染；
- A/B 页面或不同地理位置会改变加载时间。

Playwright 的 Locator 具备 auto-wait/retry 能力，但这不意味着任何等待都自动可靠。官方文档也特别指出，动态列表如果直接调用 `locator.all()`，在列表尚未稳定时可能得到不可预测结果，因此需要先等待列表达到预期状态。

推荐等待优先级：

```text
1. 目标业务数据签名变化
2. 目标元素进入目标状态
3. 指定 selector/locator 可操作
4. DOM/text quiet window
5. 关键 API response 完成
6. navigation state
7. 固定 sleep 仅作为极小的最后安全边界
```

例如切换归档年份后，不要只等待 2 秒，而应等待：

```text
before_hash = hash(article_links)
click(year_tab)
wait until hash(article_links) != before_hash
wait until article_links count stable for 500ms
```

对 1000 个博客的动态列表抓取，这种“状态变化 + 稳定窗口”比统一 `networkidle` 更可靠。

---

## 6. 技术原理三：直接 Playwright 的价值在于可观察的步骤边界

Playwright Python 的优势不只是“用 Python 写，不用 JS 字符串”，而是可以把每一个动作转化为有明确输入、输出和错误类型的步骤：

```text
step_id
step_type
selector/locator
input
expected_condition
started_at
finished_at
status
attempt
error_type
before_artifact
after_artifact
output_ref
```

这样可以得到：

- 哪个 selector 失效；
- 是点击失败还是等待失败；
- 页面切换后目标内容是否真的变化；
- 某个站点最近在哪一步开始大量失败；
- 某一步平均耗时是否突然升高；
- 重试究竟恢复了多少；
- 是否应自动熔断这一条 Recipe。

这正是生产级抓取与一次性脚本的差异。

---

## 7. 技术原理四：Browser Process、Context、Page 必须分层管理

原文强调任务需要连续运行数天。这样的任务不能“每个 URL 起一个新浏览器进程”。合理生命周期应为：

```text
Browser Worker Process
  -> long-lived Browser Process
      -> bounded BrowserContext Pool
          -> Page / Interaction Run
```

Playwright BrowserContext 类似隔离的浏览器会话，cookie、localStorage、sessionStorage 等状态隔离，创建成本比启动完整 Chromium 低。官方文档也把 BrowserContext 作为可重复、互不污染的隔离单元。

对博客知识库平台建议：

- Browser Process：长寿命，按请求数、RSS、崩溃率或运行时长回收；
- Context：默认 source/run 级隔离，避免 cookie 污染；
- Page：task 或 interaction run 级；
- 登录态不是默认能力，公开博客不应依赖持久登录；
- Context Pool 必须有硬上限；
- Browser Worker 与 HTTP Worker 完全分池；
- Browser 崩溃只重启对应 Worker/Context，不影响 URL Truth 和已提交 checkpoint。

---

## 8. 技术原理五：复杂交互要有 Step Checkpoint，而不只是 URL Checkpoint

普通抓取用下面的幂等键基本足够：

```text
FETCH:{url_id}:{fetch_policy}:{validator_state}
```

但若一个 URL 内包含 40 个分页、20 个 filter 或 N 个 Load More 批次，仅记录“这个 URL 抓过/没抓过”远远不够。

建议增加：

```text
INTERACT:{url_id}:{recipe_release}:{run_key}
INTERACT_STEP:{interaction_run_id}:{step_id}:{loop_key}
```

以及：

```text
interaction_checkpoint
- interaction_run_id
- recipe_release_id
- state_name
- loop_cursor
- logical_item_key
- partial_output_ref
- page_signature
- committed_at
```

恢复时无需重新执行全部历史动作，可以依据 Recipe 的恢复策略：

1. 重新打开入口 URL；
2. 恢复到稳定状态；
3. 跳过已提交的 logical item；
4. 从第一个未提交 item 继续。

注意不能序列化并盲目恢复任意 DOM 对象或 Locator；应保存**逻辑状态**，例如 `year=2022,page=17`、`tab=DE`、`load_more_batch=38`，然后通过确定性动作重新到达该状态。

---

## 9. 对博客知识库最关键的优化：Interaction Complexity Router

现有平台不应该把所有 JS 页面统一送进 Crawl4AI，也不应该把所有动态页面都改成 Playwright。应该增加一个基于复杂度的路由器。

### 9.1 建议分级

```text
L0 STATIC
- HTTP 即完整正文

L1 RENDER_ONLY
- 需要浏览器渲染，但无需交互
- 优先 Crawl4AI Browser Fetch

L2 SIMPLE_INTERACTION
- 单次滚动、单次 Load More、简单点击后抓最终 DOM
- 优先 Crawl4AI js_code/c4a_script + wait_for

L3 STATEFUL_INTERACTION
- 多步 click/wait/extract
- foreach tab/filter
- 分页后每页都要提交结果
- 需要条件分支、step retry、checkpoint
- 直接 Playwright Interaction Engine

L4 UNSUPPORTED/POLICY_BLOCKED
- 登录墙、CAPTCHA、需要绕过访问控制等
- 进入 Known Gap/人工处理，不自动规避
```

### 9.2 路由特征

Probe 阶段采集：

- 仅渲染后正文是否完整；
- 是否出现 Load More/infinite scroll；
- 是否存在 tab/filter/carousel/pagination；
- 动作次数估计；
- 是否每次动作都产生独立需要保存的数据；
- 是否需要循环；
- 是否存在分支；
- 是否需要会话状态；
- Crawl4AI 简单交互样本成功率；
- Playwright 样本的恢复收益；
- 历史 selector drift；
- 每文档 Browser 成本。

可以计算一个可解释分数，但最终路由必须保存“证据”，不能只保存分数。

---

## 10. 不要回到“1000 个站点 = 1000 个 Playwright 脚本”：需要 Browser Interaction Recipe DSL

直接 Playwright 的风险是团队很容易为每个网站写一个 Python 文件，最终再次失去可扩展性。因此本项目应该把直接 Playwright 作为**执行引擎**，而不是让站点逻辑散落在代码里。

建议定义版本化的 Interaction Recipe：

```yaml
kind: browser_interaction
version: 7
entry:
  goto: "{{url}}"
  wait:
    locator: "article, main"

steps:
  - id: year_tabs
    foreach:
      locator: "[data-year]"
      key: "attr:data-year"
      body:
        - click: "{{item}}"
        - wait_change:
            target: ".post-list a"
            fingerprint: href_set
        - wait_stable:
            target: ".post-list a"
            quiet_ms: 500
        - extract_links:
            locator: ".post-list a"
        - assert:
            min_items: 1
        - checkpoint:
            key: "{{item.key}}"

limits:
  max_actions: 200
  max_wall_time_s: 180
  max_retries_per_step: 2
```

执行器将这些 typed action 映射到直接 Playwright API，而不是生成任意 JavaScript 字符串。

建议内置动作集：

```text
GOTO
WAIT_LOCATOR
WAIT_CHANGE
WAIT_STABLE
CLICK
SCROLL
SCROLL_UNTIL_NO_NEW
FOREACH
PAGINATE
EXTRACT_LINKS
EXTRACT_HTML
EXTRACT_TEXT
ASSERT
BRANCH
CHECKPOINT
SCREENSHOT_ON_FAIL
```

只有 DSL 无法表达、且确实有长期价值的极少数站点，才允许 `CUSTOM_PLAYWRIGHT_PLUGIN`。插件必须版本化、受测试、受预算、可回放，不能直接写进 Source 表的任意代码字段。

---

## 11. Step 级错误模型

复杂交互不能只记录 `BROWSER_FAILED`。建议至少拆分：

```text
NAVIGATION_TIMEOUT
LOCATOR_NOT_FOUND
LOCATOR_NOT_ACTIONABLE
ACTION_TIMEOUT
EXPECTED_CHANGE_NOT_OBSERVED
STATE_NOT_STABLE
OUTPUT_EMPTY
OUTPUT_DUPLICATE
ASSERTION_FAILED
PAGE_CRASHED
CONTEXT_CRASHED
BROWSER_CRASHED
ACCESS_DENIED
POLICY_BLOCKED
RECIPE_LIMIT_EXCEEDED
CHECKPOINT_CONFLICT
```

其中：

- `LOCATOR_NOT_FOUND` 高比例上升通常意味着 selector drift；
- `EXPECTED_CHANGE_NOT_OBSERVED` 可能是点击没有生效或页面行为改变；
- `OUTPUT_DUPLICATE` 连续出现可用于停止分页/Load More；
- `RECIPE_LIMIT_EXCEEDED` 必须进入显式 DEGRADED/QUARANTINE，而不是假装抓完。

---

## 12. Selector/Locator 稳定性策略

直接 Playwright 并不自动消除 selector drift。建议选择器优先级：

```text
稳定语义属性 / role / label
> 明确 data-* 标识
> 稳定 id
> 结构化关系定位
> class
> 深层 CSS/XPath
```

同时对 Recipe 做：

- Golden Page Replay；
- selector candidate 数量监控；
- action 前后页面签名；
- 失败截图/MHTML/DOM Snapshot；
- source-scoped drift alert；
- Candidate Recipe -> Canary -> ACTIVE 发布链。

不要让 LLM 在生产抓取中实时看到页面后自由决定“下一步点哪里”。LLM 可以在离线诊断中提出新 Locator/Recipe 候选，但上线前要通过静态校验、Golden Replay、Canary 和人工审批。

---

## 13. 并发与吞吐：不要把“异步”误认为“无限并行”

30,000 页、数十万次交互的核心瓶颈不是 Python `await` 语法，而是：

- Chromium 内存；
- CPU；
- Context 数；
- 目标域名可接受 QPS；
- 页面脚本负载；
- 网络；
- 单次交互平均耗时。

应按资源建模：

```text
Global Browser Slots
 -> per-worker context slots
 -> per-source concurrency
 -> per-domain QPS/token bucket
 -> per-recipe action budget
```

例如：

```text
browser_global_slots = 40
worker_context_slots = 4
source_max_concurrency = 2
domain_qps = 0.5
```

具体值由压测决定，不应硬编码到方案。

Backfill 的 Stateful Browser 任务必须与 Incremental HTTP 任务分队列、分 Worker Pool、分预算，避免一个复杂动态归档站拖垮 999 个正常博客的增量同步。

---

## 14. Artifact 与回放设计

对复杂交互，仅保存最终 HTML 不够。建议保存：

```text
interaction/{run_id}/manifest.json
interaction/{run_id}/step/{step_no}/before.mhtml
interaction/{run_id}/step/{step_no}/after.mhtml
interaction/{run_id}/step/{step_no}/output.json
interaction/{run_id}/failure/{step_no}.png
```

正常成功路径无需每一步都保存完整 MHTML，可以配置采样或只存关键 checkpoint；失败路径应强制保存足够诊断证据。

Manifest 至少包含：

```text
recipe_release
runtime_release
chromium_version
source_release
url_id
step sequence
logical cursors
attempts
timing
outputs
stop_reason
```

这样 Selector 漂移后，可以对旧 Artifact 做离线诊断，而不是立刻反复请求源站。

---

## 15. Web 管理后台应增加 Interaction 面板

Source 页面新增：

```text
Interaction Routing
Interaction Recipes
Interaction Runs
Step Timeline
Checkpoints
Locator Drift
Browser Cost
Failure Evidence
Replay
```

关键视图：

- HTTP / Crawl4AI Render / Crawl4AI Simple Interaction / Direct Playwright 比例；
- L0-L4 路由原因；
- 每个 Recipe 的 p50/p95 动作数和耗时；
- Step failure heatmap；
- selector/locator 失败趋势；
- checkpoint resume 成功率；
- Browser recovery rate；
- 单恢复文档 Browser 成本；
- 最近的 DOM/MHTML/截图证据；
- Recipe Release diff + Canary 结果。

这比只有“Browser 成功/失败”更能支撑 1000 站长期维护。

---

## 16. 对现有博客知识库技术方案的具体修改建议

当前方案已经正确提出：

- HTTP First, Browser Last；
- Crawl4AI 作为 Browser/Fetch Adapter；
- Playwright 用于确定性滚动、点击、分页 Recipe；
- Browser Worker 使用 long-lived browser + bounded Context Pool；
- 固定 sleep/networkidle 不作为统一 readiness；
- Recipe、Release、Trace/Replay、Retry、DLQ、Circuit Breaker。

原文带来的主要增量不是推翻这些设计，而是把“Playwright Recipe”从一句组件定位提升为完整的一等执行模型。

建议新增以下能力：

### 16.1 新增 Interaction Complexity Router

明确 L0 STATIC、L1 RENDER_ONLY、L2 SIMPLE_INTERACTION、L3 STATEFUL_INTERACTION、L4 BLOCKED 五档，防止 Crawl4AI/Playwright 二选一写死。

### 16.2 新增 Browser Interaction Recipe Release

复杂交互通过 typed DSL 编排并由直接 Playwright 执行，避免 Python 内嵌大段 JS，也避免每个站点各写一个爬虫。

### 16.3 新增 Interaction Run / Step Run / Checkpoint

让每个动作可跟踪、可重试、可恢复、可审计。

### 16.4 新增 Step 级 Trace 与错误类型

生产问题能定位到“哪个动作、哪个 Locator、哪个逻辑 Item、哪个等待条件”。

### 16.5 新增状态变化等待原语

`WAIT_CHANGE`、`WAIT_STABLE`、`SCROLL_UNTIL_NO_NEW` 应成为平台能力，禁止站点 Recipe 普遍使用固定 sleep。

### 16.6 Browser Fallback 进一步细化

```text
HTTP
 -> Crawl4AI Render
 -> Crawl4AI Simple Interaction
 -> Direct Playwright Stateful Interaction
 -> QUARANTINE / Known Gap
```

不能把所有 Browser 都视为同一种成本和可靠性等级。

---

## 17. 推荐的最终执行路径

对博客/技术文档页面：

```text
Probe
 -> Interaction Complexity Classification

L0 STATIC
 -> HTTP

L1 RENDER_ONLY
 -> Crawl4AI Browser Render

L2 SIMPLE_INTERACTION
 -> Crawl4AI js_code/c4a_script + semantic wait

L3 STATEFUL_INTERACTION
 -> Direct Playwright Interaction Recipe Engine
      -> Step Run
      -> Semantic Wait
      -> Extract Partial Output
      -> Assert
      -> Checkpoint
      -> Resume on Failure

All paths
 -> Immutable Artifact
 -> Extraction Portfolio
 -> Quality Gate
 -> Canonical IR
 -> Markdown
```

历史 URL Discovery 同样使用该路由。特别是年份 Tab、Category Filter、Load More 和无限滚动页面，应先识别它们属于“发现交互”还是“正文交互”，并把产出的 URL/正文重新汇入统一 URL Inventory 和 Artifact 流程，而不是建立第二套数据真相。

---

## 18. 结论

这篇文章对本项目最重要的启示可以概括成四句话：

1. **高层抓取框架适合把页面变成内容，不应强行承担所有复杂浏览器工作流。**
2. **直接 Playwright 的优势是显式、有类型、有步骤边界的控制，而不是“更底层所以更强”。**
3. **复杂交互一旦进入生产规模，必须拥有 Step 状态、Checkpoint、错误分类、Trace、资源预算和 Replay。**
4. **为了保持 1000+ 站点可扩展性，直接 Playwright 应被封装成版本化 Interaction Recipe Engine，而不是演化成 1000 个手写爬虫。**

因此，博客知识库平台最合理的设计不是 Crawl4AI 与 Playwright 二选一，而是建立一个**交互复杂度路由层**：静态内容走 HTTP，渲染和简单交互优先 Crawl4AI，复杂有状态交互升级为直接 Playwright typed workflow；所有执行路径最终仍汇入同一 URL Identity、Immutable Artifact、Quality Gate、Canonical IR 和 Markdown 主链。

这样既保留 Crawl4AI 在高频内容抽取上的生产效率，也能在少量复杂站点上获得 Playwright 的精确控制、可调试性和长任务恢复能力。
