# 多来源商品数据合并系统设计

## 1. 背景与目标

系统汇总雷小安、腕表之家、奢当家等来源的商品数据。

各来源提供品牌、分类和商品标题，但标题格式并不统一。型号、规格、系列、材质等属性通常藏在标题里，因此不能为每个来源维护一套固定解析规则。

目标是识别不同来源中指向同一官方商品变体的记录，并将其归入同一实体组。

## 2. 系统架构

整体分为四层：

| 层 | 主要职责 | 主要输出 |
| --- | --- | --- |
| 数据源层 | 提供商品记录 | 来源原始记录 |
| 接入与存储层 | 保存原始数据，统一记录格式 | `raw_records` |
| 计算层 | 抽取、规范化、候选生成、匹配判定和聚类 | 属性、候选对、匹配边、实体组 |
| 输出与审核层 | 提供查询、导出和人工复核 | JSON、结构化表、审核结果 |

```mermaid
flowchart TB
    subgraph SRC["数据源层"]
        A[雷小安]
        B[腕表之家]
        C[奢当家]
    end

    subgraph INGEST["接入与存储层"]
        R[(raw_records / Iceberg)]
    end

    subgraph COMPUTE["计算层"]
        E[属性抽取]
        N[型号规范 + MVP-RAG 属性标准化]
        B1[候选生成]
        M[匹配判定（Selecting）]
        C1[传递一致性清洗]
        G[实体组生成]
    end

    subgraph OUTPUT["输出与审核层"]
        O[(实体组与证据)]
        J[JSON 导出]
        H[人工复核]
    end

    A --> R
    B --> R
    C --> R
    R --> E --> N --> B1 --> M --> C1 --> G --> O
    O --> J
    O --> H
    H -. 黄金标签 .-> M
    H -. 阈值调优 .-> B1
```

**图 1：系统四层架构与数据流，人工复核结果以黄金标签回流到匹配判定与阈值调优。**

## 3. 批处理时序

每个批次按以下顺序处理：

```mermaid
sequenceDiagram
    participant SRC as 数据源
    participant ING as 接入存储
    participant EXT as 属性抽取
    participant NORM as MVP-RAG 规范化
    participant BLK as 候选生成
    participant MAT as 匹配判定(Selecting)
    participant CLN as 传递一致性清洗
    participant GRP as 实体组生成
    participant OUT as 输出与审核

    SRC->>ING: 写入来源记录
    ING->>ING: 统一记录格式
    ING->>EXT: 提供标准化记录
    EXT->>NORM: 输出属性和置信度
    NORM->>BLK: 输出规范化属性
    BLK->>MAT: 输出候选对和召回证据
    MAT->>CLN: 输出匹配判定
    CLN->>GRP: 输出清洗后的边
    GRP->>OUT: 输出实体组和冲突标记
    OUT-->>MAT: 黄金标签
    OUT-->>BLK: 阈值调优
```

**图 2：一次批处理的跨组件时序，审核结果以黄金标签回流到匹配判定。**

## 4. 输入标准化

来源字段原样保留，同时映射到统一字段。

| 字段 | 说明 |
| --- | --- |
| `source_id` | 来源标识 |
| `product_id` | 来源内商品 ID |
| `brand` | 来源品牌字段，可以为空 |
| `category` | 来源分类字段，可以为空 |
| `title` | 原始商品标题，不改写 |
| `official_code` | 来源提供的官方 SKU 或 variant 编码，可以为空 |
| `raw` | 来源原始字段快照 |
| `raw_hash` | 原始记录内容的哈希值 |
| `ingested_at` | 接入时间 |

`source_id` 与 `product_id` 共同构成来源记录的唯一键，单独的 `product_id` 不能作为全局键。

## 5. 属性抽取

### 5.1 GLiNER2 Schema

GLiNER2 按动态 Schema 抽取属性，无需为每个来源维护固定解析规则。

默认 Schema 如下：

| 类型 | 含义 |
| --- | --- |
| `brand` | 品牌 |
| `model` | 型号或 Reference |
| `series` | 系列或子系列 |
| `material` | 材质、表壳材质或表链材质 |
| `size` | 表径或官方尺寸 |
| `color` | 颜色或表盘颜色 |
| `hardware` | 箱包五金 |
| `movement` | 腕表机芯 |
| `version` | 版本或特殊款式 |
| `official_code` | 官方 SKU 或 variant 编码 |

每种类型都有一段描述，其中列出常见叫法。

抽取结果至少保存以下字段：

- 来源记录键。
- 实体类型。
- 原始实体文本。
- 规范化实体值。
- 置信度。
- 模型版本。
- 抽取时间。

### 5.2 低置信处理

关键属性的抽取置信度低于配置阈值时，记录进入低置信队列。

低置信记录可以生成候选对，但不能自动合并，结果必须进入人工复核。

第一阶段只验证中文标题的抽取效果。GLiNER2 base 模型主要面向英文，只有小样本验证不足时，才评估 adapter 或模型微调。

## 6. 型号和属性规范化

原始值与规范化值同时保存，规范化不会覆盖原始值。

### 6.1 型号规范化

型号规范化按以下规则处理：

- 统一全角和半角字符。
- 统一大小写。
- 统一空格、连字符和斜杠。
- 去除 `ref`、`reference no`、`model no` 等前缀噪声。
- 保留型号后缀，因为后缀可能区分官方变体。

无法确认属于噪声的字母或数字一律保留。

### 6.2 MVP-RAG 属性标准化

系统不维护静态属性规范化词表，而是检索同品类、同品牌的相似商品，再由生成模型完成属性标准化。

#### 6.2.1 相似商品检索

每条记录都由 `BAAI/bge-m3` 生成稠密向量，并做 L2 归一化。

FAISS 索引按「分类 + 品牌」建立。检索前先映射品牌别名，例如将 `Rolex` 与 `劳力士` 归为同一规范品牌，避免同一品牌落入多个桶。

检索流程：

- 对目标记录，确定其分类和规范品牌。
- 在对应「分类 + 品牌」索引中取 Top-K 相似商品（K 建议 4 至 5），剔除目标自身。
- 品牌缺失时回退到「仅分类」索引；分类缺失时回退到「仅品牌」索引；两者都缺失时跳过检索。
- 相似商品及其相似度写入候选证据，供生成模型参考。

#### 6.2.2 Prompt 构造

Prompt 由以下四部分组成：

- 任务说明。
- 注意事项。
- 相似商品（同品类同品牌，仅参考）。
- 目标商品。

生成模型根据目标商品信息输出标准化属性。结果必须是字段顺序固定的 JSON，无法判断的属性填 `null`。

#### 6.2.3 生成与后处理

`Qwen3-4B-Instruct` 负责生成标准化属性。输出解析失败时重试一次，仍失败则标记为 `unknown`。

低置信或无法判断的属性进入人工复核，不参与自动合并。

规范化值用于候选 blocking、结构化文本和结果输出，但它本身既不产生匹配结果，也不会单独拒绝候选。

```mermaid
flowchart TD
    A[输入记录] --> B[规范化品牌和分类]
    B --> C{品牌和分类均存在?}
    C -->|是| D[使用 分类加品牌 FAISS 索引]
    C -->|否| E{仅品牌缺失?}
    E -->|是| F[使用仅分类索引]
    E -->|否| G{仅分类缺失?}
    G -->|是| H[使用仅品牌索引]
    G -->|否| I[跳过检索]

    D --> J[取 Top-K 相似商品并剔除自身]
    F --> J
    H --> J
    J --> K[写入相似商品和相似度证据]
    K --> L[拼接任务、注意事项、相似商品和目标商品]
    L --> M[Qwen3-4B-Instruct 输出严格 JSON]
    M --> N{解析成功?}
    N -->|是| O[写入标准化属性]
    N -->|否| P[重试一次]
    P --> Q{解析成功?}
    Q -->|是| O
    Q -->|否| R[标记 unknown]
    O --> S{低置信或无法判断?}
    R --> S
    S -->|是| T[进入人工复核，不自动合并]
    S -->|否| U[用于 blocking、结构化文本和结果输出]
```

**图 3：属性标准化按品牌与分类选择检索索引，将相似商品作为证据生成严格 JSON，并处理解析失败与低置信结果。**

## 7. 候选对生成

百万级数据无法全量两两比较。候选生成以 `bge-m3 + FAISS Top-K + 相似度阈值截断` 为主路径，`exact_model` 为补充路径。两条路径的结果合并后，按左右来源记录键去重，再进入后续流程。

不同缺失字段场景对应的候选路径与自动合并限制如下：

| 品牌 | 分类 | 型号状态 | 生成路径 | 是否可自动合并 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 存在 | 存在 | 规范化型号存在且相同 | `exact_model` | 是（进入匹配判定） | 补充路径；只缩小候选范围，不代表同一商品 |
| 存在 | 存在 | 缺失或置信度低 | `semantic_top_k` | 是（进入匹配判定） | `bge-m3` + FAISS Top-K |
| 缺失 | 存在 | 任意 | 生成候选 | 否 | 候选只能进入人工复核 |
| 存在 | 缺失 | 规范化型号存在 | 使用品牌和规范化型号生成候选 | 由匹配判定决定 | 不使用分类桶 |
| 缺失 | 缺失 | 任意 | 不生成自动合并候选 | 否 | 放入人工复核队列 |
| 已有人工确认映射 | 任意 | 任意 | 直接复用映射 | 是 | 保存映射版本和来源，不重复生成相同候选 |

### 7.1 精确候选路径

这条路径用于补充语义召回。

品牌、分类和规范化型号都存在且相同时，生成候选对。

它只负责缩小候选范围，不表示两条记录就是同一商品。

### 7.2 语义候选路径

语义路径是候选生成的主路径。型号缺失或置信度低时，在品牌和分类桶内使用 `BAAI/bge-m3` 向量与 FAISS 做 Top-K 语义召回。`K` 为配置项，默认 5 至 10，在抽样阶段调优。

品牌缺失时仍可生成候选，但这类候选只能进入人工复核，不能自动合并。

分类缺失时，可用品牌和规范化型号生成候选。

品牌和分类都缺失时，不执行自动合并，记录直接进入人工复核队列。

每个候选对都保存生成路径，例如 `exact_model` 或 `semantic_top_k`。

两个来源已有人工确认的商品映射时，直接复用该映射，并保存映射版本和来源，避免重复生成相同候选。

### 7.3 候选合并与去重

7.1 和 7.2 的候选对合并后，按左右来源记录键去重。去重结果进入 7.4 的候选证据与阈值截断。

### 7.4 候选证据

每条记录生成一段结构化文本，字段顺序固定：

`品牌 | 型号 | 系列 | 材质 | 尺寸 | 颜色 | 五金 | 机芯 | 版本 | 官方编码`

字段标签固定，例如：

`品牌=Rolex|型号=126610LN|系列=Submariner|材质=钢|尺寸=41mm`

缺失字段直接省略，不同时使用“留空”和“省略”两种规则。

原始标题和结构化文本都由 `BAAI/bge-m3` 编码。每个候选对计算以下分数：

- 原始标题的稠密向量分数。
- 结构化文本的稠密向量分数。
- 结构化文本的稀疏词项分数。

先合并两类稠密分数：

`dense_score = w_title × title_dense + w_structured × structured_dense`

`w_title` 和 `w_structured` 的和为 1。

再合并稠密分数与稀疏分数：

`score = w_dense × dense_score + w_sparse × sparse_score`

`w_dense` 与 `w_sparse` 的和为 1。候选记录保存各项分数、权重和总分；未启用稀疏分数时，将 `w_sparse` 设为 0。

三个分数的输入与权重如下：

| 分数项 | 输入文本 | 向量类型 | 权重 | 合并位置 |
| --- | --- | --- | --- | --- |
| `title_dense` | 原始标题 | bge-m3 稠密 | `w_title` | `dense_score` |
| `structured_dense` | 结构化文本 | bge-m3 稠密 | `w_structured` | `dense_score` |
| `sparse` | 结构化文本 | 稀疏词项 | `w_sparse` | 最终 `score`（可设 0 关闭） |

相似度和阈值只用于候选生成。`T_review` 与 `T_auto` 划分候选截断和人工复核：低于 `T_review` 的候选对不进入后续流程；不低于 `T_review` 且低于 `T_auto` 的进入人工复核；不低于 `T_auto` 的交给匹配判定。分数也作为审核证据，但不承担最终匹配判定。

阈值由黄金标签集调整。候选记录同时保存阈值、权重、模型版本和规则版本。

```mermaid
flowchart TD
    A[标准化记录与属性置信度] --> B{品牌存在?}
    B -->|是| C{分类存在?}
    B -->|否| D{分类存在?}

    C -->|是| E{规范化型号存在且高置信?}
    E -->|是| F[exact_model：同品牌、分类和型号生成候选]
    E -->|否| G[semantic_top_k：品牌分类桶内 Top-K 召回]
    C -->|否| H[使用品牌和规范化型号生成候选]

    D -->|是| I[生成候选，仅进入人工复核]
    D -->|否| J[不执行自动合并，进入人工复核]

    F --> K[合并候选并按左右来源记录键去重]
    G --> K
    H --> K
    M[复用已确认的跨来源映射并保存版本和来源] --> K

    K --> L[计算标题稠密、结构化稠密和稀疏分数]
    L --> N[合并分数并保存权重、模型与规则版本]
    N --> O{总分与阈值比较}
    O -->|低于 T_review| P[不进入后续流程]
    O -->|介于 T_review 和 T_auto| Q[人工复核]
    O -->|不低于 T_auto| R[进入 Selecting 判定]
    I --> Q
    J --> Q
```

**图 4：候选对按字段可用性选择生成路径，合并去重后经阈值截断，再进入人工复核或 Selecting 判定。**

## 8. 匹配判定（Selecting）

匹配判定从每条记录的候选中选出真正匹配的记录。

### 8.1 Selecting 判定

每条不重复的锚点记录都与候选一起组成选择式 Prompt：

- 任务说明。
- 锚点记录。
- 候选记录列表，编号为 `[1]` 至 `[K]`。

调用 `DeepSeek-V4-Flash` 时，温度设为 0，输出只包含一个编号：`[k]` 表示锚点与第 k 个候选匹配，`[0]` 表示没有匹配候选。

解析失败或低置信的结果进入人工复核，不参与自动合并。

`candidate_pairs` 保存 `selecting_decision` 与 `selecting_model_version`。匹配判定结果和候选生成分数一并保留，供审核与传递一致性清洗使用。

## 9. 实体组生成

### 9.1 传递一致性清洗

图中只加入 `auto_accepted` 的 `SAME_AS` 边，`review` 和 `rejected` 结果不加入图。

生产环境用 Spark 计算连通分量，NetworkX 仅用于抽样和调试。

每个至少包含三个节点的连通分量都检查长度为 2 的路径。例如 A 与 B、B 与 C 匹配时，使用已存的 `BAAI/bge-m3` 向量计算 A 与 C 的隐含分数。

A 与 C 的分数低于 `T_auto` 时，按以下规则处理：

1. 不将 A-C 写入匹配边。
2. 在已接受的 A-B 和 B-C 边中删除分数较低的一条。
3. 两条边分数相同时，不自动选择，将该分量标记为 `conflict` 并进入人工复核。
4. 重新计算受影响的连通分量。

重复上述步骤，直到没有矛盾或达到配置的迭代上限。A-C 是隐含比较结果，不是被删除的已接受边。

删除的边写入 `removed_edges`，至少记录两个来源记录键、边分数、删除原因和清洗任务 ID，不会被静默丢弃。

达到迭代上限后仍有矛盾时，将相关分量标记为 `conflict` 并进入人工复核。

```mermaid
flowchart TD
    A[构建图：仅加入 auto_accepted 的 SAME_AS 边] --> B[计算连通分量]
    B --> C{"分量节点数 >= 3?"}
    C -- 否 --> P[输出当前分量]
    C -- 是 --> D[枚举长度 2 路径 A-B-C]
    D --> E[用 bge-m3 向量计算隐含边 A-C 分数]
    E --> F{"A-C 分数 >= T_auto?"}
    F -- 是 --> G[保留 A-B 与 B-C 边]
    F -- 否 --> I{"A-B 与 B-C 分数相等?"}
    I -- 是 --> J[标记分量 conflict 并写入人工复核队列]
    I -- 否 --> K[删除分数较低边并写入 removed_edges]
    G --> L{是否还有未检查路径?}
    L -- 是 --> D
    L -- 否 --> P
    K --> M[重新计算受影响的连通分量]
    M --> N{达到迭代上限且仍有矛盾?}
    N -- 是 --> O[标记相关分量 conflict，进入人工复核]
    N -- 否 --> B
    P --> Q[输出清洗后的 SAME_AS 边]
```

**图 5：传递一致性清洗算法流程：对长度 2 路径做隐含比较，删除低分边或标记冲突并迭代重算。**

### 9.2 实体组生成

使用清洗后的 `SAME_AS` 边生成实体组，只对产品节点计算连通分量。

品牌、分类和颜色等属性作为实体字段保存，不直接参与连通分量计算。

`VARIANT_OF` 表示不同官方变体之间的关系。该边不参与实体组聚类。

传递一致性清洗发现的矛盾组标记为 `conflict`，交由人工复核。

## 10. 输出和审核

每个实体组输出以下字段：

| 字段 | 说明 |
| --- | --- |
| `entity_id` | 稳定的统一实体 ID |
| `canonical_record_id` | canonical 记录的来源记录键 |
| `canonical` | 代表记录及字段来源 |
| `members` | 实体组中的来源记录 |
| `evidence` | 已接受匹配边的分数和理由 |
| `status` | `auto_accepted`、`review` 或 `rejected` |
| `conflict_flag` | 是否存在冲突 |
| `rule_version` | 匹配规则版本 |
| `model_version` | 抽取和向量模型版本 |

`candidate_pairs` 保存全部候选对。`entity_groups.evidence` 只保留进入实体组的匹配证据，避免重复保存候选信息。

人工审核至少支持：

- 接受合并。
- 拒绝合并。
- 拆分实体组。
- 将两个实体组合并。
- 标记错误属性。

审核结果回流为黄金标签，并记录审核人、审核时间和审核理由。

```mermaid
stateDiagram-v2
    [*] --> rejected : Selecting 输出 [0]
    [*] --> review : Selecting 输出解析失败或低置信
    [*] --> auto_accepted : Selecting 输出匹配候选

    auto_accepted --> review : 人工审核拒绝合并
    review --> auto_accepted : 人工审核接受合并
    review --> rejected : 人工审核拒绝合并
    review --> review : 拆分实体组 / 合并实体组 / 标记错误属性
```

**图 6：判定与审核状态机：Selecting 输出决定初始状态，人工审核操作触发后续流转；conflict_flag 是实体组属性，不是图中状态。**

## 11. 数据模型

| 表名 | 用途 | 关键字段 |
| --- | --- | --- |
| `raw_records` | 保存来源原始记录 | `source_id`、`product_id`、`brand`、`category`、`title`、`official_code`、`raw_hash` |
| `extracted_attributes` | 保存抽取属性 | 来源记录键、类型、原始值、规范化值、置信度、模型版本 |
| `candidate_pairs` | 保存候选对、生成证据与匹配判定 | 两个来源记录键、生成路径、各项分数、总分、selecting_decision、selecting_model_version、理由 |
| `entity_groups` | 保存实体组 | `entity_id`、canonical 记录键、状态、冲突标记、版本 |
| `entity_members` | 保存实体组成员 | `entity_id`、`source_id`、`product_id`、成员状态 |
| `removed_edges` | 保存传递一致性清洗删除的边 | 两个来源记录键、相似度、删除原因、清洗任务 |
| `review_events` | 保存人工审核历史 | 候选对或实体组、操作、理由、审核人、审核时间 |

```mermaid
erDiagram
    RAW_RECORDS ||--o{ EXTRACTED_ATTRIBUTES : "抽取"
    RAW_RECORDS ||--o{ CANDIDATE_PAIRS : "候选成员（两个记录键）"
    RAW_RECORDS ||--o{ ENTITY_MEMBERS : "成员来源"
    ENTITY_GROUPS ||--o{ ENTITY_MEMBERS : "包含"
    CANDIDATE_PAIRS ||--o{ ENTITY_GROUPS : "auto_accepted 边聚合"
    RAW_RECORDS ||--o{ REMOVED_EDGES : "被删除边端点"
    REVIEW_EVENTS }o--o| CANDIDATE_PAIRS : "审核候选对"
    REVIEW_EVENTS }o--o| ENTITY_GROUPS : "审核实体组"

    RAW_RECORDS {
        string source_id PK "来源标识"
        string product_id PK "来源内商品ID"
        string brand "品牌，可为空"
        string category "分类，可为空"
        string title "原始标题"
        string official_code "官方编码，可为空"
        string raw "原始快照"
        string raw_hash "内容哈希"
        datetime ingested_at "接入时间"
    }
    EXTRACTED_ATTRIBUTES {
        string source_id PK
        string product_id PK
        string entity_type
        string raw_value
        string normalized_value
        float confidence
        string model_version
    }
    CANDIDATE_PAIRS {
        string left_source_id PK
        string left_product_id PK
        string right_source_id PK
        string right_product_id PK
        string generation_path
        float title_dense
        float structured_dense
        float sparse
        float score
        string selecting_decision
        string selecting_model_version
        string reason
    }
    ENTITY_GROUPS {
        string entity_id PK
        string canonical_record_id
        string status
        bool conflict_flag
        string rule_version
        string model_version
    }
    ENTITY_MEMBERS {
        string entity_id PK
        string source_id PK
        string product_id PK
        string member_status
    }
    REMOVED_EDGES {
        string left_record_key
        string right_record_key
        float score
        string reason
        string cleaning_task_id
    }
    REVIEW_EVENTS {
        string review_id PK
        string target_type
        string operation
        string reason
        string reviewer
        datetime reviewed_at
    }
```

**图 7：核心数据表逻辑关系图：展示候选对、实体组、成员、被删边与审核事件之间的逻辑关联与基数。**

批处理由 Spark 执行，中间结果和最终结果写入 Iceberg。

原始数据和中间结果可按来源或分类分区。实体组可能包含多个来源，不能只按来源分区。

## 12. 增量处理

增量任务只处理新增记录、发生变化的记录及其候选邻域。

处理步骤如下：

1. 根据 `raw_hash` 判断记录是否变化。
2. 对新增或变化记录重新抽取并规范化。
3. 在相关 blocking 桶内重新生成候选。
4. 对新增候选重新执行 Selecting 匹配判定。
5. 更新匹配边和实体组。
6. 对受影响的连通分量重新执行传递一致性清洗。
7. 保持未变化实体的 `entity_id` 不变。
8. 为拆分、合并和失效映射记录审核事件。

重复处理同一批增量数据时，不能生成重复实体组。

## 13. 部署与资源

- 第一阶段使用 200M 至 1B 参数量级的 GLiNER2 模型，通过抽样测试确定具体版本。
- `BAAI/bge-m3` 使用量化版本，以降低 CPU 推理成本。
- `Qwen3-4B-Instruct` 用于 MVP-RAG 属性标准化，需要一张 8GB 以上显存的 GPU；通过 vLLM 或 Ollama 提供接口，并开启批量推理。
- `DeepSeek-V4-Flash` 用于匹配判定（Selecting），云端 API 按 token 计费，上线前确认具体型号与接口命名。
- 开发阶段用 Spark local 或 DuckDB 跑通抽样数据。
- 生产阶段根据数据量选择 EMR、自建集群或托管 Spark。
- FAISS 只用于稠密向量召回，稀疏词项分数由单独的检索或计算过程生成。
- 生产环境记录抽取吞吐、候选数量、自动合并率、人工复核率和错误合并率。

## 14. 范围外

- 商品图片识别和图片比对。
- MICE 图像描述多模态属性抽取（本期不做）。
- 电商平台发布和库存回写。
- GLiNER2 大模型微调。
- 第一阶段的全语种支持。
- 实时在线匹配 API。

## 15. 参考资料

- [ecom-product-knowledge-graph](https://github.com/adityashah841/ecom-product-knowledge-graph)
- [GLiNER2](https://github.com/fastino-ai/GLiNER2)
- [GLiNER2 电商 Schema 教程](https://github.com/fastino-ai/GLiNER2/blob/main/tutorial/4-combined.md)
- [Amazon ESCI 数据](https://github.com/amazon-science/esci-data)
- [MVP-RAG: Multi-Value-Product Retrieval-Augmented Generation](https://aclanthology.org/2025.emnlp-industry.147/)
- [MICE: Mixture of Image Captioning Experts for e-Commerce Attribute Value Extraction](https://aclanthology.org/2025.acl-industry.80/)
- [ComEM: Match, Compare, or Select?](https://github.com/tshu-w/ComEM)
- [product-matching-rag](https://github.com/Abhisheksasidharann/product-matching-rag)
- [Splink](https://github.com/moj-analytical-services/splink)
- [dedupe](https://github.com/dedupeio/dedupe)
