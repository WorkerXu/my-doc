# AI 如何帮助我的团队每周节省 40 小时：自动化活动信息发现实践

## 基本信息

- 编号：29
- 原文：How AI helped my team save 40 man-hours of work a week by automating event discovery!
- 作者：Kuldeep Paul
- 来源：Medium
- 地址：https://medium.com/@kuldeep.paul08/how-ai-helped-my-team-save-40-man-hours-of-work-a-week-by-automating-event-discovery-b71435ab1716
- 发布时间：2025-06-01
- 与本项目相关性：高。文章给出了一条“搜索引擎发现候选 URL → URL 去重 → Crawl4AI 转 Markdown → Gemini 结构化抽取 → JSON/CSV/Google Sheets → n8n Agent → Slack 查询”的完整信息发现流水线。对博客知识库最有价值的不是固定工具组合，而是把 Discovery、Content Normalization、Semantic ETL、Projection/Delivery 分层，并通过自动化消除重复人工研究。

---

## 1. 原方案的实际执行链

文章方案可以抽象为：

```text
自然语言搜索条件
 -> Playwright 打开 DuckDuckGo
 -> 等待搜索结果 DOM
 -> 抽取 URL
 -> 无限滚动 / 点击 More Results
 -> Link Bank(JSON)
 -> Python set 精确字符串去重
 -> Crawl4AI AsyncWebCrawler
 -> Markdown
 -> Gemini 结构化抽取
 -> JSON
 -> CSV
 -> Google Sheets
 -> n8n AI Agent
 -> Slack 自然语言查询
```

其本质是一个“发现 → 内容标准化 → 语义结构化 → 业务消费”的数据产品流水线。文章声称该系统把原先大量人工搜索、打开网页、判断、抄字段、整理表格和回答团队问题的工作自动化，从而每周节省约 40 小时。

对 1000+ 博客、全历史和长期增量场景而言，这条链路的思想值得保留，但示例中的本地 JSON、精确字符串去重、逐 URL Crawler、Prompt-only JSON 和表格真相都需要平台化重构。

---

## 2. Playwright + DuckDuckGo：搜索发现的技术本质

### 2.1 动态搜索页其实是一个状态机

文章用 Playwright 搜索类似：

```text
AI Events in the USA in May - Oct 2025
```

基本动作：

1. 打开 DuckDuckGo；
2. 等待 `article h2 a` 等结果元素；
3. 抽取页面中的外部 HTTP URL；
4. 过滤搜索引擎自身链接；
5. 记录新 URL；
6. 滚动到底部；
7. 点击 `#more-results`；
8. 等待新结果后继续；
9. 按钮消失时结束。

这不是普通“一次 HTTP GET 得到所有结果”的页面，而是**动作驱动分页**。页面状态由滚动、点击和异步请求推进，因此生产系统应该把它建模成：

```text
Query
 -> Search UI State Machine
 -> Page Observation
 -> Candidate URL Observation
```

而不是把 Playwright 看成正文抓取器。

### 2.2 Search Provider 的正确边界

搜索引擎只回答“还有哪些候选页面可能被权威 Provider 漏掉”。它不能证明：

- 某站全历史共有多少文章；
- 搜索结果为空就代表页面不存在；
- 搜索结果翻到底就代表历史完整；
- 搜索结果数量等于站点文章总量。

对博客知识库应定位为：

```text
Authoritative Discovery
  CMS/API / Sitemap / RSS / Archive / Pagination
          |
          +-- Coverage Gap
                  |
             External Search
                  |
             Candidate URLs
```

所以 Search 是 Gap Provider，不是 Coverage Truth。

### 2.3 原代码缺少的生产能力

文章里的循环存在这些问题：

- selector 漂移时可能误判“无结果”；
- More Results 消失可能是异常，不一定是真穷尽；
- 页面重复加载时可能形成低价值循环；
- 没有记录“第几页发现了哪些 URL”；
- 没有 page fingerprint；
- 没有连续 N 页无新增停止；
- 没有页数、运行时长、成本和 Browser 预算；
- 失败后无法从某个 cursor/page 状态恢复。

生产系统应持久化：

```text
search_query
search_page_observation
search_observation
```

每页至少保存：page/cursor、结果数、新 URL 数、标准化 URL 集合 fingerprint、Evidence Artifact、stop reason。

### 2.4 饱和停止条件

建议明确区分：

```text
PROVIDER_EXHAUSTED
PROVIDER_RESULT_CAP
MAX_PAGES
MAX_WALL_TIME
COST_BUDGET_EXHAUSTED
N_PAGES_NO_NEW
REPEATED_PAGE_FINGERPRINT
PROVIDER_DEGRADED
POLICY_BLOCKED
CANCELLED
```

这样“搜索结束”成为可解释状态，而不是 `while True` + 按钮消失。

---

## 3. 从文章的“按地域/月份搜索”进一步推导：Search Gap 自适应分区

这是本次在现有方案基础上新增的最重要优化。

文章人工构造了带地域和时间窗口的查询。这个做法背后的真实原因是：**一个宽泛搜索查询通常拿不到完整结果，必须把搜索空间拆小。** 对博客历史 URL 补洞同样成立。

例如直接执行：

```text
site:example.com
```

如果 Provider 有结果数量上限，那么即使最后一页仍不断出现新 URL，也可能因为结果上限而停止。此时把 stop reason 记为 exhausted 是错误的。

### 3.1 生产化模型

新增：

```text
SearchQueryTemplate
 -> SearchPartition
 -> Query Instance
 -> Page Observation
```

`search_partition` 建议保存：

```text
partition_id
source_id
parent_partition_id
dimension_type       # TIME / PATH / TOPIC / CUSTOM
dimension_value
depth
state
split_reason
query_budget
result_count
new_url_count
marginal_new_rate
child_count
```

### 3.2 何时判定需要拆分

出现任一信号即可认为宽分区可能被截断：

- Provider 明确暴露 result cap；
- 到达 `MAX_PAGES` 时末页仍持续有新 URL；
- 连续多页 `new_url_rate` 仍很高；
- 搜索结果时间分布明显偏向最近一段；
- 某一个路径前缀占据结果绝大多数；
- 多次查询结果集合高度重叠但仍发现少量长尾 URL。

### 3.3 优先拆分顺序

为了保持确定性和成本可控，优先级建议：

```text
YEAR
 -> MONTH
 -> PATH_PREFIX
 -> 受控 TOPIC/CUSTOM
```

时间和路径是天然、可解释的维度，应优先于 LLM 自由生成关键词。

例如：

```text
site:example.com 2024
 -> site:example.com 2024 January
 -> site:example.com 2024 February
 ...
```

或：

```text
site:example.com inurl:/blog/
site:example.com inurl:/engineering/
site:example.com inurl:/news/
```

### 3.4 不能变成“无限搜索”

必须设置：

```text
max_depth
max_partitions_per_source
max_queries_per_partition
max_search_cost_per_source
min_marginal_new_rate
```

当继续拆分带来的新 URL 边际收益低于阈值时，停止细化。

这使 External Search 形成：

```text
Broad Query
 -> Saturation Detection
 -> Controlled Split
 -> Child Queries
 -> Marginal Gain Measurement
 -> Stop
```

分区树本身是 Gap Evidence，但仍不能升级为“全历史完整证明”。

---

## 4. Link Bank 与 URL 去重：为什么 `set(raw_url)` 不够

文章把搜索结果写入 JSON，并用 Python `set` 做精确字符串去重。这对 PoC 有效，但对长期知识库远远不够。

同一文章可能有：

```text
https://site.com/post
https://site.com/post/
https://site.com/post?utm_source=x
https://site.com/post#section
https://www.site.com/post
301 -> https://site.com/new-post
<link rel="canonical" href="...">
```

因此至少需要五层身份处理：

1. raw URL 观察事实；
2. normalized URL；
3. canonical/redirect identity；
4. Artifact sha256；
5. Canonical semantic hash；
6. 可选近似正文指纹识别镜像/转载。

关键原则：**URL Observation 和 URL Identity 分离。** 原始 URL 永远保留用于审计，规范化只是 Identity Candidate，不直接覆盖事实。

JSON 也不能当真相库，因为它缺少并发事务、唯一约束、状态机、游标恢复和审计。

---

## 5. Crawl4AI 转 Markdown：正确价值与错误边界

### 5.1 文章为什么先转 Markdown

文章使用 Crawl4AI 把网页转成 Markdown，再交给 Gemini。这个方向本身合理：

- 去掉 script/style/大部分 DOM 噪声；
- 保留标题、段落、列表、链接等语义结构；
- 降低 LLM token；
- 比直接把原 HTML 交给模型更稳定。

这体现了一个正确原则：

```text
Deterministic Compression
 -> Expensive Semantic Processing
```

### 5.2 为什么不能直接把 Crawl4AI Markdown 当最终真相

Markdown 结果依赖：

- 浏览器渲染状态；
- DOM；
- Crawl4AI 版本；
- Markdown Generator；
- Filter / extraction 参数。

若只保存最终 Markdown，未来抽取器升级时只能重新访问源站；而源站可能已经修改或删除。

生产链应为：

```text
Raw HTML / Browser Render Artifact
 -> Extraction Candidate(s)
 -> Independent Quality Gate
 -> Canonical IR
 -> Versioned Markdown Renderer
```

Crawl4AI Markdown 适合作为 Diagnostic Candidate，不适合作为唯一 Canonical Truth。

### 5.3 并发与生命周期

文章示例逐 URL 处理，在百万 URL 场景存在：

- 每次创建 Crawler/Browser 的固定成本；
- 串行慢；
- 无连接池和 Browser Context Pool；
- 无 domain QPS；
- 无 backpressure；
- 无公平调度。

建议：

```text
HTTP Worker
 -> async connection pool
 -> bounded concurrency

Browser Worker
 -> long-lived browser/crawler
 -> bounded context pool
 -> per-page lifecycle
 -> recycle by request_count/memory/age
```

HTTP First，只有真实 JS 依赖才升级 Browser。

---

## 6. Gemini 结构化抽取：Semantic ETL 的正确边界

文章把每个 Markdown 文件整体送入 Gemini，让模型抽取：event name、location、date、URL 等字段，再通过 `json.loads` 追加到 JSON。

这类能力适合抽象成：

```text
Canonical Content
 -> Semantic Interpretation
 -> Schema-constrained Record
 -> Structured Knowledge
```

它适合处理不同页面对同类概念的自然语言差异，但不适合接管 URL 身份、Fetch 状态、任务调度和正文版本这些确定性职责。

### 6.1 Prompt-only JSON 不够

`json.loads` 成功只能证明语法合法，不能证明：

- 字段类型正确；
- 日期格式统一；
- URL 合法；
- 枚举值受控；
- 字段确实来自原文；
- 没有遗漏；
- 没有幻觉；
- 空数组不是模型失败；
- 结果没有被截断。

生产系统至少要：

```text
Deterministic Chunk
 -> Schema/Tool Calling Extractor
 -> JSON Schema / Pydantic Validate
 -> Evidence Bind
 -> Merge / Dedup
 -> Enrichment Version
```

### 6.2 状态必须区分

```text
SUCCESS
VALID_EMPTY
SCHEMA_ERROR
MODEL_ERROR
TRUNCATED
BUDGET_EXHAUSTED
EVIDENCE_INSUFFICIENT
```

不能把模型调用失败返回的 `[]` 当成“网页确实没有结果”。

### 6.3 字段级证据

结构化字段应绑定原文：

```json
{
  "technology": "FastAPI",
  "evidence": {
    "document_version_id": "...",
    "block_id": "b-42",
    "quote_hash": "..."
  }
}
```

这样检索/Agent 输出结构化事实时能回到 Canonical Block，而不是只能相信模型。

### 6.4 长文分块与幂等

技术博客可能很长，应按 Canonical IR 的 heading/paragraph/code block 边界确定性分块，再 merge/dedup。

幂等键：

```text
ENRICH:{document_version_id}:{schema_release}:{extractor_release}:{chunker_release}
```

### 6.5 文章中的“最多 30 条”不能照搬

Top-N 上限会静默丢数据。知识库若有输出上限，必须：

- 分块继续抽取；或
- 分页/多轮；或
- 标记 `TRUNCATED`；
- 生成后续任务。

预算导致的能力下降必须显式可见。

---

## 7. 跨文档实体身份：URL 去重之外的第二层去重

活动信息场景中，同一活动可能被多个页面重复报道；博客知识库中同一 GitHub 项目、组织、产品、技术版本也会重复出现。

因此需要独立实体层：

```text
field_evidence
 -> enrichment_record
 -> entity_identity_candidate
 -> deterministic key / alias / canonical URL
 -> optional semantic match
 -> entity_identity
```

原则：

- 原始文档事实永远保留；
- Entity Merge 不修改 Canonical 文档；
- GitHub repo URL、官网、标准 ID 等确定性键优先；
- 语义匹配只产生 Candidate；
- 不确定时不强行合并；
- 错误合并可拆分并重建 Projection。

---

## 8. JSON → CSV → Google Sheets → n8n/Slack：交付层的意义

文章最终把 JSON 转成 CSV、上传 Google Sheets，再由 n8n AI Agent 通过 Slack 供团队查询。

真正值得吸收的是：

```text
Content Truth
 -> Knowledge Enrichment
 -> Projection
 -> Delivery Adapter
```

CSV、Sheets、Slack、n8n 都是消费方式，不应成为唯一业务真相。

博客知识库应让 PostgreSQL + Object Storage 保存 Source、URL、Artifact、Document Version、Enrichment Version；Sheets/Slack 只消费可重建 Projection。

### 8.1 Agent 不能绕过统一 Query Service

推荐：

```text
User Query
 -> auth/source scope
 -> metadata filter
 -> BM25 + vector
 -> fusion/rerank
 -> evidence context
 -> optional answer generation
 -> citation response
```

Slack/n8n Adapter 不直接读数据库私表，也不自由访问外网，从而保持权限、版本和证据边界。

---

## 9. Agent Evaluation：文章对最终知识访问的启示

文章还提到使用模拟/评估工具验证 Agent 行为。这一点非常重要：**抓取质量和最终知识访问质量是两个不同 SLO。**

即使 Markdown 抓对，仍可能发生：

- 正确文章召回不到；
- reranker 排序错误；
- metadata filter 错误；
- Agent 引用旧版本；
- 回答缺证据；
- Prompt/Model 更新导致回归。

因此需要独立 Evaluation Plane：

```text
Golden Query Dataset
 -> Retrieval Eval
 -> Answer/Citation Eval
 -> Baseline Compare
 -> Canary
 -> Release Guard
```

关键指标：Recall@K、MRR、nDCG、filter accuracy、stale-version rate、citation coverage、evidence precision、groundedness、abstention correctness。

---

## 10. 对现有博客知识库方案的具体优化结论

现有方案已经覆盖：

- External Search Gap；
- Page-level Observation；
- 饱和停止状态机；
- Stable URL Identity；
- HTTP First / Browser Last；
- Immutable Artifact；
- Extraction Portfolio + Quality Gate；
- Canonical IR + Markdown Renderer；
- Structured Enrichment + Evidence；
- Entity Identity；
- Query Service；
- Retrieval/Agent Evaluation。

本次进一步新增：

### 10.1 Search Gap 自适应分区规划

这是基于文章“按地域/时间拆查询”进一步抽象出来的生产能力。

新增 `search_partition`、父子 lineage、饱和检测、按时间/路径细化、边际新 URL 收益、`max_depth/max_partitions/cost_budget` 等机制。

价值是：

1. 避免宽查询命中 Provider 结果上限后误判 exhausted；
2. 在 1000 个站点上自动把高价值 Gap 继续细化；
3. 不依赖人工逐年逐月写 Query；
4. 仍保持预算上限，防止 Search 无限膨胀；
5. Web 后台可解释“为什么拆分、拆分后新增多少 URL”。

### 10.2 Search Partition 仍不是 Coverage Truth

即使所有搜索分区都低收益或 exhausted，也只能说明 Search Gap 补洞趋于饱和；全历史是否完整仍要看 CMS/API/Sitemap/Archive 等 Authoritative Provider 的证据。

---

## 11. 不建议直接照搬的做法

- 不使用搜索引擎结果数量证明全历史完整；
- 不用本地 JSON 作为 URL 或任务真相；
- 不只用 `set(raw_url)` 去重；
- 不把无限滚动写成无 fingerprint、无新增阈值、无预算的循环；
- 不把“按钮消失”唯一解释为 Provider exhausted；
- 不为每个 URL 重启完整 Browser/Crawler；
- 不串行处理百万 URL；
- 不只保存 Markdown 而丢弃 Raw Artifact；
- 不用清洗后的 URL 长字符串当稳定主键/文件名；
- 不把 Crawl4AI Markdown 直接发布为 Canonical；
- 不把 Prompt-only JSON 解析成功等同数据质量成功；
- 不整体无界送入模型；
- 不静默丢弃超过 Top-N 的结果；
- 不把 VALID_EMPTY 与模型/Schema 错误混为一类；
- 不把 CSV、Google Sheets、n8n、Slack 当业务真相；
- 不让 Agent 绕过统一检索/证据策略；
- 不只测试抓取质量而不测试 Retrieval/Answer 回归；
- 不让自适应 Search Partition 无深度、数量和成本上限。

---

## 12. 推荐落地形态

```text
1. Discovery Truth
   CMS/API/Sitemap/RSS/Archive/Dynamic
   + Search Gap
   + Page Observation
   + Saturation Detection
   + Adaptive Search Partition
          |
2. Content Truth
   Immutable Artifact
   -> Extraction Portfolio
   -> Quality Gate
   -> Canonical IR
   -> Document Version
          |
3. Knowledge Enrichment
   Deterministic Chunk
   -> Schema/LLM Extract
   -> Evidence Bind
   -> Entity Identity / Merge
   -> Enrichment Version
          |
4. Projection & Delivery
   Markdown / BM25 / Vector / JSONL / CSV / API
   -> n8n / Slack / Chat(optional)
          |
5. Knowledge Access Evaluation
   Golden Queries
   -> Retrieval Eval
   -> Answer/Citation Eval
   -> Release / Canary / Regression Guard
```

前两层决定“有没有发现、原文到底是什么”，必须确定性、可追溯、可回放；第三层允许 LLM 参与，但必须有 Schema、Evidence、Version、Budget；第四层全部可重建；第五层保证最终用户得到可验证的知识。

## 13. 结论

这篇文章最值得吸收的不是 Playwright + Crawl4AI + Gemini + n8n 的固定技术栈，而是把信息获取工作拆成“候选发现、内容标准化、语义结构化、业务消费”的流水线，并证明了自动化研究循环可以显著降低人工成本。

对 1000+ 技术博客知识库，应把文章中的脚本式实现升级为：

- Authoritative Discovery + Search Gap；
- Page-level Search Observation；
- 饱和状态机 + 自适应 Search Partition；
- Stable URL Identity；
- 长生命周期 HTTP/Browser Worker；
- Immutable Artifact；
- 多 Candidate Extraction + 独立 Quality Gate；
- Canonical IR + Document Version；
- Schema/Evidence-bound Enrichment；
- 跨文档 Entity Identity；
- 可重建 Projection/Delivery；
- Retrieval/Agent Golden Evaluation。

这样既保留文章展示的自动化效率，又能满足全历史回填、长期增量、持续扩站、可审计、可重建和成本治理要求。