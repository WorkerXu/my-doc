# AI 如何帮助我的团队每周节省 40 小时：自动化活动信息发现实践

## 基本信息

- 编号：29
- 原文：How AI helped my team save 40 man-hours of work a week by automating event discovery!
- 作者：Kuldeep Paul
- 来源：Medium
- 地址：https://medium.com/@kuldeep.paul08/how-ai-helped-my-team-save-40-man-hours-of-work-a-week-by-automating-event-discovery-b71435ab1716
- 发布时间：2025-06-01
- 与本项目相关性：高。文章给出了一条从“搜索引擎发现候选 URL → URL 去重 → Crawl4AI 转 Markdown → Gemini 结构化抽取 → JSON/CSV/Google Sheets → n8n Agent → Slack 查询”的端到端流水线。它对本项目最有价值的不是具体工具组合，而是把 Discovery、Fetch、Content Normalization、Semantic Enrichment、Projection/Delivery 分层的思路，以及搜索补洞、可选 LLM 结构化增强和面向业务用户消费的实践。

## 1. 原方案的实际执行链

文章中的实现可以还原为：

```text
自然语言搜索条件
  -> Playwright 打开 DuckDuckGo
  -> 等待 article h2 a
  -> 抽取结果 URL
  -> 向下滚动 / 点击 #more-results
  -> Link Bank(JSON)
  -> Python set 精确字符串去重
  -> Crawl4AI AsyncWebCrawler
  -> DefaultMarkdownGenerator
  -> 每 URL 一个 Markdown 文件
  -> Gemini 1.5 Flash + Prompt
  -> JSON 文本清理 + json.loads
  -> event_data.json
  -> CSV
  -> Google Sheets
  -> n8n AI Agent
  -> Slack 自然语言查询
```

本质上这是一个“发现 → 标准化 → 语义 ETL → 交付”的数据产品流水线。它之所以能显著降低人工工作量，是因为人工原本需要反复搜索、打开页面、判断内容、抄字段、整理表格、回答团队问题，而方案把这些动作拆成可自动执行的阶段。

但博客知识库要求 1000+ 站点、全历史、长期增量、可回放和可治理，因此不能直接照搬文章的本地脚本形态，需要把其中有价值的思想平台化。

---

## 2. Playwright 搜索发现：实现细节与技术原理

### 2.1 页面驱动发现机制

文章使用 Playwright 驱动 DuckDuckGo 搜索结果页，典型查询类似：

```text
AI Events in the USA in May - Oct 2025
```

流程大致是：

1. 打开搜索页；
2. 等待 `article h2 a` 出现，确认首屏结果已经可用；
3. 从页面提取所有候选 `href`；
4. 过滤非 HTTP 链接以及搜索引擎自身链接；
5. 将新 URL 放入进程内集合并追加到 JSON；
6. 滚动到底部；
7. 若存在 `#more-results`，点击继续加载；
8. 没有更多结果则退出。

这类页面不是传统的“第 1 页、第 2 页”静态分页，而是**动作驱动分页**：滚动和点击会触发异步请求，页面状态随动作推进，因此只做一次 HTTP GET 不能拿到完整结果。

### 2.2 技术本质：Search Provider 不是 Fetcher

这里 Playwright 的职责不是抓正文，而是把搜索引擎转换成一个候选 URL Provider：

```text
Query
 -> Search UI State Machine
 -> Result Observation
 -> Candidate URL
```

这是很重要的边界。对博客知识库而言，搜索引擎只负责“我还能在哪里发现可能遗漏的 URL”，真正的正文 Fetch、身份合并、版本判断仍进入统一数据面。

### 2.3 原实现的主要风险

文章代码适合 PoC，但不适合全历史知识库：

- Link Bank 是本地 JSON，无法承担事务、并发、审计、游标恢复和多 Worker 真相；
- `set(raw_url)` 只能做精确字符串去重，无法合并 tracking query、fragment、canonical、redirect、尾斜杠等同一页面变体；
- 搜索引擎索引天然不完备，结果数量不能证明历史覆盖完整；
- DOM selector、滚动逻辑、More Results 按钮会漂移；
- 没有持久化每一次结果页状态，无法解释“为什么结束”“某个 URL 在第几页何时被发现”；
- 如果页面出现重复结果、循环加载或按钮失灵，简单循环缺乏严格的饱和停止条件；
- 浏览器成本远高于 API/HTTP，需要独立并发、QPS、页数和费用预算。

### 2.4 生产化设计：Search Query Bank + Page Observation

建议将文章的 Link Bank 升级为数据库化的搜索发现模型：

```text
SearchQueryTemplate
 -> SearchQueryInstance
 -> SearchPageObservation
 -> SearchObservation
 -> URL Normalize
 -> URL Identity Merge
 -> URL Inventory
```

核心实体建议：

```text
search_query
- query_id
- source_id
- provider
- query_text
- template_release
- partition_key
- cursor_state
- page_count
- consecutive_no_new
- stop_reason
- result_count
- new_url_count
- started_at
- finished_at

search_page_observation
- page_observation_id
- query_id
- page_no / cursor
- page_fingerprint
- result_count
- new_url_count
- evidence_artifact_id
- observed_at

search_observation
- query_id
- page_observation_id
- rank
- raw_url
- normalized_url
- title
- snippet
- observed_at
```

其中 `search_page_observation` 是文章方案里缺失但生产系统非常重要的一层。它让动态滚动/点击不再是黑盒循环，而成为可恢复的状态机。

### 2.5 明确的饱和停止条件

搜索补洞任务不能只依赖“按钮消失”。建议同时支持：

```text
PROVIDER_EXHAUSTED       # 明确无更多结果
MAX_PAGES                # 页数预算到达
MAX_WALL_TIME            # 运行时限到达
COST_BUDGET_EXHAUSTED    # 成本预算到达
N_PAGES_NO_NEW           # 连续 N 页没有新 URL
REPEATED_PAGE_FINGERPRINT# 页面内容/结果集合循环
PROVIDER_DEGRADED        # selector/API/交互失败
CANCELLED
```

`page_fingerprint` 可由当前页标准化结果 URL 列表排序后哈希得到。如果连续加载得到相同 fingerprint，即使按钮仍存在也应该终止或进入 DEGRADED，防止无限循环。

### 2.6 对本项目的正确定位

External Search 只能用于：

- Authoritative Provider 间出现时间线缺口时补洞；
- 新 Source Probe 时寻找未知 Archive/历史页面；
- Sitemap/RSS/Archive 结果交并差异常时验证遗漏；
- 低频 Coverage Verify。

不能用于宣称“已经抓全”。全历史完整度必须由 CMS/API、Sitemap、Archive/Pagination 等权威发现源的游标、时间范围、穷尽证据和 Known Gap 共同解释。

---

## 3. Crawl4AI 转 Markdown：实现细节与伸缩性分析

### 3.1 原方案为什么先转 Markdown

文章使用：

```text
AsyncWebCrawler
 + CrawlerRunConfig(markdown_generator=DefaultMarkdownGenerator())
 -> result.markdown
```

随后把结果写成 Markdown 文件。这样做的收益是：

- 去掉大量 script/style/DOM 属性；
- 将标题、段落、列表、链接等转成更紧凑的线性语义结构；
- 降低后续 LLM token 数；
- 相比直接给模型喂 HTML，更容易保持字段定位稳定。

其技术原则是**先用确定性转换压缩表示，再做昂贵语义处理**，方向是正确的。

### 3.2 原实现为何无法直接扩到百万文章

文章在 URL 循环中逐个执行 Crawl4AI，而且每 URL 的调用生命周期较重。对少量活动页面没问题，但 1000 个博客的全历史可能是百万级 URL，会产生：

- 每 URL 初始化/销毁浏览器或 Crawler 的固定成本；
- 串行执行导致一个慢站点阻塞整个列表；
- 无连接池、无长生命周期 Worker；
- 无 Browser Context Pool；
- 无 bounded concurrency 和 backpressure；
- 无域名级 QPS、crawl delay、公平调度；
- 无 Retry/DLQ/lease/fencing；
- 只留下 Markdown，丢失 Raw HTML/DOM 后无法离线升级抽取器；
- URL 清洗后直接当文件名容易碰撞、超长和无法表达版本。

### 3.3 生产环境中的 Crawl4AI 定位

Crawl4AI 更适合作为：

```text
Browser/Fetch Adapter
 + Dynamic Discovery Adapter
 + Diagnostic Markdown Candidate
```

而不是整个知识库的“真相引擎”。推荐 Worker 生命周期：

```text
Browser Worker Process
 -> long-lived Browser/Crawler
 -> bounded Context Pool
 -> per-page lifecycle
 -> recycle by request_count / memory / age
```

普通技术博客优先 HTTP Fetch；只有确定存在 JS shell、动态正文、Load More、Browser 对质量有真实恢复收益时才升级 Browser。

### 3.4 为什么不能直接把 Crawl4AI Markdown 当 Canonical

Markdown Generator 的结果依赖：

- 页面渲染状态；
- DOM 结构；
- 抽取算法版本；
- Markdown renderer 行为；
- 过滤规则。

如果直接把它作为最终知识库真相，一旦算法升级就很难重放。更可靠的链路是：

```text
Raw Artifact
 -> Extraction Candidate(s)
 -> Independent Quality Gate
 -> Canonical IR
 -> Versioned Markdown Renderer
```

Crawl4AI Markdown 可以是 Candidate 或诊断参考，但必须与 Trafilatura、CSS/XPath Recipe、Structured Metadata 等候选一起进入质量判断。

---

## 4. Gemini 结构化抽取：语义 ETL 的正确边界

### 4.1 文章中的实现

文章把每个 Markdown 文件整体送给 Gemini，Prompt 约束：

- 目标对象为活动；
- 地域/时间条件；
- `event_name`、`location`、`dates`、`url` 等字段；
- 缺字段时跳过；
- 不要幻觉；
- 最多返回 30 条并优先重要活动。

模型返回文本后，用正则去掉 Markdown code fence，再执行 `json.loads`，结果追加到 `event_data.json`。

### 4.2 技术本质：Semantic ETL

这不是正文抓取，而是：

```text
Canonical / Clean Content
 -> Semantic Interpretation
 -> Schema-constrained Record
 -> Structured Knowledge
```

LLM 适合解决“不同网页用不同自然语言描述同一类字段”的问题，例如技术栈、产品、组织、人物、版本、项目等；它不适合接管 HTTP、版本、任务状态和 URL 身份这类确定性职责。

### 4.3 Prompt-only JSON 为什么不够

`json.loads` 成功仅代表语法合法，不代表：

- 字段类型正确；
- 日期格式统一；
- URL 合法；
- 枚举值受控；
- 字段真的有原文证据；
- 没有遗漏；
- 没有截断；
- 空数组是真实无结果而不是模型失败。

生产系统至少应有：

```text
Deterministic Chunk
 -> Schema/Tool Calling Extractor
 -> JSON Schema / Pydantic Validate
 -> Evidence Bind
 -> Merge / Dedup
 -> Enrichment Version
```

状态明确区分：

```text
SUCCESS
VALID_EMPTY
SCHEMA_ERROR
MODEL_ERROR
TRUNCATED
BUDGET_EXHAUSTED
EVIDENCE_INSUFFICIENT
```

### 4.4 证据绑定

结构化字段应保留 `block_id`/span 证据。例如：

```json
{
  "technology": "FastAPI",
  "evidence": {
    "document_version_id": "...",
    "block_id": "b-42",
    "quote_hash": "..."
  }
}
```

这样后续用户查“某文章用了什么技术”时，可以回到原文，而不是只能相信模型输出。

### 4.5 长文分块与合并

文章整体送入模型对短页面可行，技术博客则可能很长。应按 Canonical IR 的 heading/paragraph/code block 边界做确定性 chunk，避免语义切断；每个 chunk 独立抽取后进行 merge/dedup。

同一文档、同一 `schema_release + extractor_release + chunker_release` 必须幂等：

```text
ENRICH:{document_version_id}:{schema_release}:{extractor_release}:{chunker_release}
```

### 4.6 禁止静默 Top-N 截断

文章“最多 30 条并丢弃剩余”适合业务演示，但知识库不能这么做。若有结果上限，必须：

- 分块继续抽；或
- 分页/多轮；或
- 显式标记 `TRUNCATED`；
- 生成补处理任务。

任何预算导致的能力降级都必须可见，而不是把不完整结果写成 SUCCESS。

---

## 5. 结构化记录的跨文档去重与身份问题

文章处理活动数据时，同一活动可能被多个站点、多个页面重复报道。URL 去重并不能解决实体级重复。

对博客知识库的 Structured Enrichment 也一样：同一个 GitHub 项目、组织、产品或技术版本可能从几十篇文章中重复抽取。建议把“文档事实”和“实体身份”分离：

```text
field_evidence
 -> enrichment_record
 -> entity_identity_candidate
 -> deterministic key / alias / canonical URL
 -> optional semantic match
 -> entity_identity
```

原则：

- 原始文档证据永远保留；
- 实体合并是 Projection/Knowledge Layer，不覆盖原文；
- 不确定合并保留候选关系，不强行合并；
- entity merge 规则/模型版本化；
- 支持拆分错误合并并重建图谱/索引。

这能避免未来做 Graph/RAG 时出现“一个项目几十个重复实体”或错误跨站合并。

---

## 6. JSON → CSV → Google Sheets → n8n/Slack：交付层的意义

文章把结构化 JSON 转为 CSV，再放到 Google Sheets，最后由 n8n AI Agent 通过 Slack 让团队自然语言查询。

这里最值得吸收的是**交付层与真相层分离**：

```text
Content Truth
 -> Knowledge Enrichment
 -> Projection
 -> Delivery Adapter
```

CSV、Sheets、Slack、n8n 都是消费方式，不应成为唯一业务状态。博客知识库的 PostgreSQL + Object Storage 仍保存 Source、URL、Artifact、Document Version、Enrichment Version；Sheets/Slack 只消费可重建投影。

### 6.1 Agent 不能绕过 Retrieval Policy

Slack/n8n Agent 查询时应经过统一检索服务：

```text
User Query
 -> auth / source scope / metadata filter
 -> BM25 + vector / structured filter
 -> fusion/rerank
 -> evidence context
 -> answer generation(optional)
 -> citation/evidence response
```

不能让 Agent 直接自由浏览对象存储、数据库或外部网站，否则会失去版本、权限和证据边界。

---

## 7. 文章中的 Agent Evaluation 对本项目的新增启示

文章不仅做了 n8n/Slack 查询入口，还使用模拟/评估工具验证 Agent 行为。这一部分对当前博客知识库方案是可进一步补强的点：**抓取质量和检索/问答质量是两套不同的 SLO，后者也要发布、回归和评估。**

### 7.1 为什么抓对 Markdown 仍不代表知识库好用

即使采集链已经达到高 Coverage、高 Extraction Correctness，用户仍可能遇到：

- 搜索召回不到正确文章；
- reranker 把次要结果排在前面；
- metadata filter 错误；
- Agent 引用了旧版本；
- 回答缺少证据；
- 结构化字段检索与全文检索冲突；
- prompt/model 更新让回答质量回退。

因此需要独立的 Knowledge Access Evaluation Plane。

### 7.2 推荐评估模型

持久化：

```text
query_eval_case
- case_id
- query
- source_scope
- expected_document_ids
- expected_filters
- expected_facts/evidence_refs
- tags
- dataset_release

retrieval_eval_run
- run_id
- retrieval_release
- dataset_release
- recall_at_k
- mrr
- ndcg_at_k
- filter_accuracy
- stale_version_rate
- latency_p95
- cost

answer_eval_result
- case_id
- answer_release
- citation_coverage
- evidence_precision
- groundedness
- contradiction_count
- abstention_correctness
- latency
- cost
```

### 7.3 评估必须版本化

以下组件变化都可能引入回归：

```text
BM25 analyzer
embedding model
chunker
reranker
fusion weight
metadata schema
prompt
LLM model
answer policy
```

所以检索/Agent 发布应有：

```text
DRAFT
 -> Offline Eval
 -> Golden Query Replay
 -> Canary
 -> Compare Baseline
 -> Activate
```

不能只评估抓取 Worker 而忽略知识访问层。

### 7.4 Web 管理后台应增加 Evaluation 视图

展示：

- Golden Query Set；
- Recall@K / MRR / nDCG；
- metadata filter accuracy；
- citation coverage；
- groundedness；
- stale-version rate；
- latency/cost；
- release-to-release diff；
- 失败 case 的 evidence trace。

这样 Slack/n8n/Chat API 只是不同入口，底层访问质量可以统一治理。

---

## 8. 对当前博客知识库技术方案的具体优化结论

当前方案已经吸收了文章的大部分核心价值，包括 External Search Gap、Canonical 后 Enrichment、版本化 Schema/Prompt/Model、Web 管理、Projection/Delivery 分层等。基于本次更深入分析，仍建议补强以下三点：

### 8.1 Search Gap 增加页级 Observation 和饱和停止状态机

不要只记录 Query 总结果，而要记录每次动态分页/滚动的 page/cursor/fingerprint/new URL 数，支持 `N_PAGES_NO_NEW`、`REPEATED_PAGE_FINGERPRINT`、`PROVIDER_DEGRADED` 等停止原因。

这把文章中的 Playwright 无限滚动从脚本循环升级成可审计、可续跑、可防死循环的 Discovery Provider。

### 8.2 Enrichment 增加跨文档实体身份层

URL/Document 去重不等于结构化实体去重。技术栈、项目、组织、产品等需要独立 `entity_identity`，所有合并都保留原文证据并可拆分重放。

### 8.3 增加 Retrieval/Agent Evaluation Plane

文章展示了最终业务价值来自“团队能可靠地问到正确结果”，因此应把 RAG/Search/Agent 也纳入 Release + Golden Eval + Canary 治理，而不仅仅是把 `/rag/query` 当一个 API。

---

## 9. 不建议直接照搬的做法

- 不使用搜索引擎结果数量证明全历史完整；
- 不用本地 JSON 作为 URL 或任务真相；
- 不只用 `set(raw_url)` 去重；
- 不把无限滚动写成没有 fingerprint/无新增阈值/预算的无限循环；
- 不为每个 URL 重启 Crawler/Browser；
- 不串行处理百万 URL；
- 不只保存 Markdown 而丢弃 Raw Artifact；
- 不用清洗后的 URL 长字符串当稳定主键/文件名；
- 不把任意 Crawl4AI Markdown 直接发布为 Canonical；
- 不把 Prompt-only JSON 解析成功等同数据质量成功；
- 不整体无界送入模型；
- 不静默丢弃超过 Top-N 的结果；
- 不把 VALID_EMPTY 与模型/Schema 错误混为一类；
- 不把 CSV、Google Sheets、n8n、Slack 当业务真相；
- 不让 Agent 绕过统一检索/证据策略；
- 不只测试抓取质量而不测试 Retrieval/Answer 回归；
- 不硬编码 API Key 或把 Secret 写日志。

---

## 10. 推荐落地形态

结合本文，最终平台可抽象为五层：

```text
1. Discovery Truth
   CMS/API/Sitemap/RSS/Archive/Dynamic/Search Gap
   Search Page Observation + Saturation State
          |
2. Content Truth
   Immutable Artifact
   -> Candidate Portfolio
   -> Quality Gate
   -> Canonical IR
   -> Document Version
          |
3. Knowledge Enrichment
   Deterministic Chunk
   -> Schema/LLM Extract
   -> Evidence Bind
   -> Entity Identity / Merge
   -> Enrichment Version
          |
4. Projection & Delivery
   Markdown / BM25 / Vector / JSONL / CSV / API
   -> n8n / Slack / Chat(optional)
          |
5. Knowledge Access Evaluation
   Golden Queries
   -> Retrieval Eval
   -> Answer/Citation Eval
   -> Release / Canary / Regression Guard
```

前两层决定“有没有发现、原文到底是什么”，必须确定性、可追溯、可回放；第三层允许 LLM 参与，但必须有 Schema、Evidence、Version、Budget；第四层全部可重建；第五层保证最终用户真正得到可验证的知识，而不是只保证爬虫成功。

## 11. 结论

这篇文章最值得吸收的不是 Playwright + Crawl4AI + Gemini + n8n 的固定技术栈，而是把信息获取工作拆成“候选发现、内容标准化、语义结构化、业务消费”的流水线，并通过自动化把重复人工动作消除。

面向 1000+ 技术博客知识库，应该保留这种分层思路，但把示例中的本地 JSON、字符串去重、逐 URL Crawler、无界动态分页、Prompt-only JSON 和表格真相升级为：

- Authoritative Discovery + Search Gap；
- Page-level Search Observation + 可证明的停止状态机；
- Stable URL Identity；
- 长生命周期 HTTP/Browser Worker；
- Immutable Artifact；
- 多 Candidate Extraction + 独立 Quality Gate；
- Canonical IR + Document Version；
- Schema/Evidence-bound Enrichment；
- 跨文档 Entity Identity；
- 可重建 Projection/Delivery；
- Retrieval/Agent Golden Evaluation。

这样既能获得文章展示的业务效率，又能满足全历史回填、长期增量、可扩展、可审计、可重建和最终知识访问质量治理。