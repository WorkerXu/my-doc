# AI 如何帮助我的团队每周节省 40 小时：自动化活动信息发现实践

## 基本信息

- 编号：29
- 原文：How AI helped my team save 40 man-hours of work a week by automating event discovery!
- 作者：Kuldeep Paul
- 来源：Medium
- 地址：https://medium.com/@kuldeep.paul08/how-ai-helped-my-team-save-40-man-hours-of-work-a-week-by-automating-event-discovery-b71435ab1716
- 发布时间：2025-06-01
- 与本项目相关性：高。文章给出了一个从“搜索发现 URL → 去重 → Crawl4AI 转 Markdown → LLM 结构化抽取 → CSV/Google Sheets → n8n Agent/Slack 查询”的完整流水线，尤其适合作为博客知识库中“外部搜索补洞、Markdown 后处理、结构化知识增强、面向非技术用户消费”的参考案例。

## 1. 文章方案的实际流水线

文章解决的是跨大量异构网页自动发现活动信息的问题。其处理链可抽象为：

```text
搜索意图/查询条件
  -> Playwright 驱动 DuckDuckGo
  -> 无限滚动/More Results
  -> URL Link Bank(JSON)
  -> 精确字符串去重
  -> Crawl4AI AsyncWebCrawler
  -> DefaultMarkdownGenerator
  -> 每个 URL 一个 Markdown 文本文件
  -> Gemini 按 Prompt 提取结构化事件
  -> event_data.json
  -> CSV
  -> Google Sheets
  -> n8n AI Agent
  -> Slack 查询入口
```

这套方案的关键价值不在某个单独组件，而在于把“发现、抓取、清洗、语义抽取、结构化、交付”拆成可以独立替换的阶段。

## 2. URL 发现实现与原理

### 2.1 Playwright 搜索结果采集

文章使用 Playwright 打开 DuckDuckGo，根据诸如 `AI Events in the USA in May - Oct 2025` 的查询获取搜索结果。程序等待 `article h2 a` 出现，然后循环执行：

1. 从当前页面提取所有 `a.href`；
2. 过滤非 HTTP URL 和 DuckDuckGo 自身链接；
3. 与进程内 `set` 比较，得到新 URL；
4. 追加到 JSON；
5. 滚动到底部；
6. 点击 `#more-results`；
7. 若没有更多结果则结束。

技术原理是把搜索引擎当作一个“候选 URL Discovery Provider”。浏览器并不负责正文抓取，只负责获取搜索结果页动态加载后的 URL。无限滚动的本质是“动作驱动分页”：页面状态由滚动、按钮点击和异步请求推进，因此不能只依赖静态 HTTP 返回。

### 2.2 这个实现的优点

- 对搜索结果页的 JS 动态加载兼容性强；
- 不要求目标站点结构一致；
- 能迅速得到跨站 URL 候选池；
- 查询词本身就是可解释的发现证据。

### 2.3 生产化时的缺陷

文章代码适合快速验证，不适合作为 1000+ 站点知识库的长期真相层：

- JSON 文件同时承担状态和数据存储，无法支持并发 Worker、事务、审计、游标恢复和多实例运行；
- 去重仅是 URL 字符串级 `set`，没有处理 fragment、tracking query、canonical、301/308、尾斜杠、大小写和镜像 URL；
- 搜索引擎结果不能证明历史覆盖完整，即使 `site:domain` 也只能作为补洞证据；
- selector、分页按钮和搜索引擎页面结构容易漂移；
- 浏览器搜索成本高，必须有并发、QPS、页数和查询预算；
- 没有保存 query、结果排名、抓取时间、终止原因和结果页快照，因此无法回答“某 URL 为什么进入系统”。

### 2.4 对博客知识库的可复用设计

应把它改造成可插拔的 `SEARCH_GAP` / `EXTERNAL_SEARCH` Discovery Provider：

```text
SearchQueryTemplate
  -> query instance(year/month/category/site)
  -> Search Provider Adapter
  -> Search Observation
  -> URL Normalize
  -> URL Identity Merge
  -> Candidate URL Inventory
```

建议持久化：

```text
search_query
- query_id
- source_id
- provider
- query_text
- template_release
- partition_key
- started_at
- finished_at
- stop_reason
- result_count
- new_url_count

search_observation
- query_id
- rank
- raw_url
- normalized_url
- title/snippet
- observed_at
- evidence_artifact_id
```

它只能用于：

- Sitemap/Archive/RSS 与已知 URL 时间线出现异常时补洞；
- 新增站点时寻找历史文章入口；
- 验证是否存在未进入 URL Inventory 的公开页面。

不能把搜索结果数量当成 Coverage 完整性的依据。

## 3. Crawl4AI 转 Markdown 实现与原理

文章对每个 URL 创建 `AsyncWebCrawler`，使用 `CrawlerRunConfig(markdown_generator=DefaultMarkdownGenerator())`，得到 `result.markdown` 后保存本地文件。

### 3.1 为什么先转 Markdown 有效

原始 HTML 同时包含导航、脚本、样式、广告、页脚、DOM 属性等大量与语义抽取无关的信息。Markdown 把主要文本压缩成标题、段落、列表、链接等线性结构，可以：

- 降低后续 LLM token 数；
- 减少 DOM 噪声；
- 保留基本文档层次；
- 让模型更稳定地定位字段。

这说明“先做确定性/规则化清洗，再做昂贵语义抽取”是正确方向。

### 3.2 文章代码的伸缩性问题

文章在 `convert_html_to_markdown` 中对每个 URL 都进入一次 `async with AsyncWebCrawler()`，外层又用 `for url in url_list` 串行执行。对数十 URL 可以工作，但对百万级文章会产生明显问题：

- Browser/Crawler 初始化被放大到每 URL；
- 没有连接池、Browser Context Pool 和 Worker 生命周期复用；
- 没有 bounded concurrency；
- 单个慢站点会阻塞后续 URL；
- 没有域名级限流、公平调度、Retry/DLQ；
- 抓取结果只留下 Markdown，没有保留原始不可变 HTML/DOM，后续算法升级无法离线重建；
- 使用 URL 清洗后作为文件名可能截断、冲突，也无法表达 Document/Version 身份。

因此生产方案应保留“Crawl4AI 是 Fetch/Browser Adapter”的思路，但改为长生命周期 Worker + 有界 Context Pool + Artifact/Document ID 存储。

## 4. Gemini 结构化抽取实现与原理

文章把每个 Markdown 文件整体送给 Gemini，Prompt 指定：

- 目标对象类型；
- 地理位置和日期约束；
- 必填字段；
- JSON key；
- 不允许幻觉；
- 缺字段则跳过；
- 最多返回 30 条。

得到文本响应后，用正则去掉 Markdown code fence，再 `json.loads`，最后追加到 `event_data.json`。

### 4.1 核心原理

这是典型的“语义 ETL”：

```text
非结构化内容
 -> 文档压缩/清洗
 -> LLM 语义理解
 -> 受约束结构
 -> 下游数据库/表格
```

LLM 的正确位置是处理“仅靠 DOM/XPath 难以统一表达的语义字段”，而不是承担 HTTP、分页、重试、数据版本等确定性职责。

### 4.2 文章实现值得借鉴的部分

- 明确要求只抽取源文中存在的信息；
- 明确缺字段策略；
- 有有限重试；
- 把结构化结果与原始 Markdown 分开；
- 最终结果进入独立数据集，而不是覆盖原文。

### 4.3 生产化必须修正的部分

1. **不能只靠 Prompt 保证 Schema**：应使用 JSON Schema/Pydantic 校验，字段类型、枚举、日期格式、URL 格式都要机器验证。
2. **不能丢失证据**：每个结构化字段应带 `evidence_span` 或 block ref，能回到 Canonical IR/Artifact。
3. **不能整体无界送入模型**：长文应按 heading/paragraph block 做确定性 chunk；抽取后再 merge/dedup。
4. **不能用“最多 30 条并丢弃剩余”作为生产逻辑**：这会产生不可见的数据丢失。应分页/分块抽取，或记录 truncation 并进入补处理任务。
5. **不能把模型返回的空数组等同于真实无结果**：应区分 `VALID_EMPTY`、`MODEL_ERROR`、`SCHEMA_ERROR`、`TRUNCATED`。
6. **模型/Prompt 要版本化**：否则同一个 Artifact 重放得到不同结构无法审计。
7. **API Key 不能硬编码**：通过 Secret Manager/环境注入，日志不得输出。
8. **阻塞 `time.sleep` 不适合异步 Worker**：生产调度应使用队列级 rate limit/backoff。
9. **结果追加文件不是事务**：并发执行可能覆盖或损坏 JSON，应写数据库或 append-only object + manifest。

## 5. JSON → CSV → Agent/Slack 的意义

文章把 JSON 转 CSV，再放入 Google Sheets，最后让 n8n AI Agent 作为自然语言访问层，并通过 Slack 暴露给团队。

这说明知识库的价值不应止于“生成 Markdown 文件”，还需要明确区分：

- Canonical 内容层；
- 结构化增强层；
- 搜索/向量 Projection；
- 导出层；
- 面向业务用户的查询/交付 Adapter。

CSV、Google Sheets、Slack 都不应成为真相库，只是 Projection/Delivery Adapter。源内容、版本和结构化数据仍应由平台保存并可重建。

## 6. 对当前博客知识库技术方案的直接优化

### 6.1 新增 External Search Gap Provider

当前方案已有 CMS/API、Sitemap、RSS、Archive、Pagination、Bounded Deep Crawl。建议增加低优先级外部搜索补洞：

```text
CMS/API
 -> Sitemap
 -> RSS/Atom
 -> Archive/Pagination
 -> Dynamic Listing
 -> Bounded Deep Crawl
 -> External Search Gap Fill
 -> Optional Web Archive/Common Crawl Gap Fill
```

外部搜索绝不能参与“Authoritative Coverage 已完整”的判定，只作为 Gap Evidence。

### 6.2 新增 Search Query Bank，但数据库化

借鉴文章的 link bank 思路，但将本地 JSON 改为：

- Query Template；
- Query Instance；
- Search Observation；
- URL Inventory；
- Query/URL Provenance；
- Query stop reason；
- Search provider release。

可按站点、年份、月份和栏目分区查询，并允许 Web 管理后台查看“搜索补洞发现了哪些 URL”。

### 6.3 新增可选 Structured Enrichment Plane

当前平台重点是抓全和生成高质量 Markdown。文章说明在 Canonical 内容稳定以后，还可以追加一个独立的结构化增强阶段：

```text
PASS/WARN Canonical IR
 -> Enrichment Job
 -> deterministic chunk
 -> optional LLM/schema extractor
 -> Schema Validate
 -> Evidence Bind
 -> Enrichment Version
 -> Search/CSV/JSONL/API Projection
```

Enrichment 不得改变 Canonical 原文。适合后续生成 topic、技术栈、作者、组织、实体、项目名、版本号、发布日期等结构化字段。

### 6.4 Enrichment 也要有版本和幂等键

建议增加：

```text
enrichment_schema
enrichment_release
enrichment_run
enrichment_result
field_evidence
```

幂等键：

```text
ENRICH:{document_version_id}:{schema_release}:{extractor_release}
```

相同 `document_version_id + release` 不重复调用模型。

### 6.5 Web 后台增加两个视图

1. **Discovery / External Search**：Query、Provider、结果数、新 URL 数、重复 URL、Gap、查询快照、停止原因。
2. **Enrichment**：Schema、Prompt/Model release、成功率、字段缺失率、Schema 错误、证据覆盖率、成本、重放。

## 7. 不建议直接照搬的部分

- 不使用搜索引擎抓取结果作为历史完整性证明；
- 不用本地 JSON 文件当 URL 真相库；
- 不对 URL 只做字符串级去重；
- 不为每个 URL 重启 Crawler/Browser；
- 不串行跑整个 URL 列表；
- 不只保存 Markdown 而丢弃 Raw Artifact；
- 不以 URL 清洗后的长字符串做 Document 主键/文件名；
- 不把任意 Crawl4AI Markdown 直接视为 Canonical；
- 不把 LLM 文本 JSON 解析成功视为数据质量通过；
- 不设置“最多 N 条然后丢弃”的静默截断；
- 不把 Google Sheets、CSV、Slack Bot 作为业务真相；
- 不硬编码 API Key。

## 8. 推荐落地形态

结合本文经验，博客知识库应形成四层：

```text
1. Discovery Truth
   CMS/API/Sitemap/RSS/Archive/Dynamic/Search Gap
          |
2. Content Truth
   Immutable Artifact -> Candidate -> Quality -> Canonical IR -> Version
          |
3. Knowledge Enrichment
   Optional Schema/LLM Extraction -> Evidence-bound Structured Data
          |
4. Projection & Delivery
   Markdown / OpenSearch / Vector / JSONL / CSV / API / Optional Chat Adapter
```

其中 1、2 层决定“有没有抓全、原文是什么”，必须确定性、可追溯、可回放；3 层可以使用 LLM，但必须有 Schema、证据、版本和预算；4 层全部可重建。

## 9. 结论

这篇文章最值得吸收的不是“用 Playwright + Crawl4AI + Gemini”这个技术组合本身，而是从检索候选到可消费知识的分阶段思路。对于 1000+ 技术博客知识库，应保留其“搜索发现 → 内容标准化 → 语义提取 → 多终端消费”的结构，同时把示例中的本地文件、字符串去重、逐 URL Crawler、Prompt-only JSON、无证据 LLM 输出升级为数据库真相、Stable Identity、长生命周期 Worker、Canonical IR、Schema Validation、Evidence Binding 和版本化 Enrichment。

最终建议：把外部搜索定位为低优先级 Gap Provider，把 LLM 定位为 Canonical 之后的可选 Knowledge Enrichment，而不是让两者进入全量历史抓取的核心真相链。这样既能获得文章展示的业务效率，又不会牺牲本项目要求的全历史覆盖、增量同步、可扩展性、可审计性和长期可维护性。
