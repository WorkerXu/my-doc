# 博客知识库技术方案

## 1. 建设目标与规模

建设一个长期运行、可横向扩展、可审计、可回放的技术博客知识库平台。首期接入约 1000 个技术博客、工程博客、技术文档站与公开 Newsletter，尽可能抓取完整历史文章并清洗为稳定 Markdown；后续能够持续增加网站，并以低成本完成增量同步、版本管理、搜索/RAG、Web 管理和离线重处理。

目标规模：

- 1000+ Source，架构可扩展到数千站点；
- 百万级 Document / Document Version；
- 数百万到千万级 URL Observation；
- 千万级 Chunk / Embedding；
- HTML、PDF、Markdown、TXT、JSON/API、RSS/Atom/JSON Feed；
- 中英日韩等多语言；
- HTTP 为默认抓取路线，Browser 为受控 fallback；
- CMS API、Sitemap、Feed、Archive、Category/Tag、Docs TOC、Common Crawl、受控 Deep Crawl 等多入口历史发现；
- PostgreSQL + Object Storage 保存事实；Markdown、全文索引、向量索引、Graph、AI Insight、Export、Dashboard Read Model 均作为可重建 Projection；
- Web UI、n8n、MCP、CI、Agent、外部系统只能通过受控 API / Event 边界驱动平台。

平台必须能回答：

1. 一个站点理论上有哪些历史文章，为什么认为历史已经基本覆盖；
2. 一个 URL 从哪里被发现、如何规范化、为何被过滤或放行、经过哪些跳转、最终抓到了什么；
3. 为什么某 URL 走 HTTP、平台 API、文件流、Crawl4AI Browser 或 Playwright；
4. 一个 Markdown 来自哪个 Snapshot、Extractor、Release 和 Document Version；
5. 源站变化后哪些内容真正变化，哪些派生物需要重建；
6. Worker 崩溃、批任务部分失败或外部自动化重启后如何恢复且不丢成功项；
7. 新增第 1001 个站点时，能否主要依靠配置、Probe 与审核接入，而不是增加站点专用代码；
8. 全量历史抓取是否有 durable checkpoint，能否从断点继续；
9. 百万 URL 批处理是否保持有界内存，并支持逐 URL 提交；
10. Browser 是否被复用且安全隔离，何时回收，是否因资源压力自动降并发；
11. Web 管理中的暂停、重跑、重新抽取、切 Release、导出等动作是否经过 RBAC、审计与幂等控制；
12. Markdown 导出到本地/S3/Git 后，是否仍能稳定映射回平台内部 Document Version。

---

## 2. 核心设计原则

1. **Config First**：普通站点靠 Source / Site Profile / Release 接入，不修改核心代码。
2. **Coverage First**：历史完整性由 Provider Evidence、cursor、exhaustion reason 和 known gap 证明，队列为空不等于完整。
3. **Authoritative First**：CMS/API、Sitemap、Feed、Archive 优先，Common Crawl/Deep Crawl 用于补洞和交叉验证。
4. **Discovery Before Fetch**：先发现、Observation、规范化、过滤、分类、去重，再抓正文。
5. **Durable Streaming Frontier**：大站 URL 发现必须流式、小批持久化，禁止把全站 URL 一次性物化在内存。
6. **Filter With Evidence**：Filter 可 HARD/SOFT/DEPRIORITIZE，但任何决定都保存规则、原因和证据。
7. **URL Is Locator**：资源类型由响应 MIME、签名、跳转和站点规则判断，不能只看扩展名。
8. **HTTP First, Browser Last**：静态 HTML、Feed、API、Sitemap 优先 HTTP；Browser 只处理动态正文、交互和质量 fallback。
9. **Reuse Browser, Isolate Session**：复用 Browser/Crawler 进程降低成本，但 Cookie、LocalStorage、认证态和代理身份按 Source/Profile/安全域隔离。
10. **Platform Permit Before Local Dispatcher**：平台全局/domain/source 配额是并发上限；Crawl4AI Dispatcher/Semaphore 只负责 Worker 内二级自适应控制。
11. **Bounded Window + Commit Per URL**：Worker 只 lease 有界窗口，流式返回结果；每个 URL 成功立即落库，不等待整批完成。
12. **Snapshot First**：先保存不可变网络事实，再做抽取、清洗、Markdown、Chunk、Graph、AI。
13. **IR First, Markdown as Projection**：Canonical IR / Document Version 是内容事实层，Markdown 可确定性重建。
14. **Full Canonical Before Fit Markdown**：Pruning/BM25/LLM 等有损过滤只能产生 FILTERED/ENHANCED Projection，绝不能覆盖 Canonical Markdown。
15. **Recipe, Not Site Code**：交互、路由、Filter、Schema、Selector、Extractor 均声明式、版本化、可回放。
16. **Stable Identity**：URL Alias、Document、Version、Chunk、Embedding、Graph、Analysis、Export 身份分离且稳定。
17. **Release Everything**：影响结果的 Profile、Provider、Filter、Recipe、Extractor、Markdown、Chunk、Embedding、Graph、Model、Prompt、Metric、Webhook、Export、Runtime 全部版本化。
18. **Idempotent + Recoverable**：任务有幂等键、lease、fencing token、checkpoint、retry、partial success。
19. **One Durable Task System**：PostgreSQL durable state + Transactional Outbox + Redis Streams 是唯一业务任务体系。
20. **External Automation Is Adapter, Not Truth**：n8n/MCP/CI/Agent 不能成为 Coverage、Task、Snapshot、Version 或 retry 状态真相。
21. **Projection Is Rebuildable**：全文、向量、Graph、AI、Export、Dashboard 都可从事实层离线重建。
22. **Generation Instead of Destructive Rebuild**：索引/图谱新版本后台构建、验证、原子切换。
23. **Security on Every Hop**：起始 URL、Redirect、Browser navigation、Webhook endpoint 每跳重新做 SSRF/Egress/Scope 校验。
24. **Source Friendly**：吞吐最大化不是唯一目标，robots、站点策略、分层限流和退避优先于速度。
25. **Benchmark Before Switch**：Filter、Extractor、Markdown、Embedding、Graph、Retrieval、Metric 升级先固定回归，再激活。

---

## 3. 总体架构

```text
                 ┌────────────────────────────────────────────┐
                 │ Web / REST / n8n / MCP / CI / Agent      │
                 └──────────────────┬─────────────────────────┘
                                    │ Command / Query
                 ┌──────────────────▼─────────────────────────┐
                 │ API + RBAC + Idempotency + Policy        │
                 └──────────────────┬─────────────────────────┘
                                    │
                 ┌──────────────────▼─────────────────────────┐
                 │ PostgreSQL Truth Store                    │
                 │ Source/Profile/Run/Task/Observation       │
                 │ Coverage/Snapshot/Document/Version        │
                 │ Release/Audit/Outbox/Event/Export         │
                 └────────────┬──────────────────┬────────────┘
                              │                  │
                           Outbox              Query
                              │                  │
                    ┌─────────▼────────┐   ┌─────▼─────────────┐
                    │ Redis Streams    │   │ Read Models       │
                    │ Task Transport   │   │ Search/Dashboard  │
                    └──────┬─────┬─────┘   └───────────────────┘
                           │     │
                ┌──────────┘     └─────────────┐
                ▼                              ▼
       Discovery Workers                Fetch Workers
 API/Sitemap/Feed/Archive/        HTTP Pool / Browser Pool
 CC/Deep Crawl/Surface            Crawl4AI / Playwright
                │                              │
                ▼                              ▼
       URL Observation ──> Durable Frontier ──> Snapshot
                                                  │
                                                  ▼
                                      Extract / Canonical IR
                                                  │
                         ┌────────────────────────┼─────────────────────┐
                         ▼                        ▼                     ▼
                  Canonical Markdown       Structured/AI       Chunk/Index/Graph
                         │
                         ▼
                  Export Sink / Git / S3
```

### 3.1 真相边界

```text
PostgreSQL + Object Storage = Truth
Redis Streams              = Transport
Crawler internal frontier  = Execution state only
Crawl4AI dispatcher        = Worker-local execution control only
n8n execution              = External automation state only
OpenSearch / Vector Store  = Rebuildable Projection
Graph Store                = Rebuildable Projection
Canonical Markdown         = Rebuildable Projection from IR
Filtered/Enhanced Markdown = Rebuildable Derived Projection
Dashboard Read Model       = Rebuildable Projection
SSE/WebSocket/Webhook      = Notification/Delivery only
Browser/session cache      = Ephemeral execution state
```

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST/Command API、Schema 校验、Webhook Receiver、内部服务接口；
- **PostgreSQL**：Source、Profile、Run、Task、Coverage、Observation、Snapshot metadata、Document、Version、Release、Audit、Outbox、Event、Webhook Delivery、Export metadata；
- **Transactional Outbox**：保证业务数据库事务与异步消息发布一致；
- **Redis Streams**：consumer group、retry transport、backpressure，只做任务运输；
- **React/Vue + TypeScript**：Web 管理台；
- **Docker/Kubernetes**：按 Discovery / HTTP Fetch / Browser / Extract / Projection Worker 类型独立扩缩容。

### 4.2 抓取与解析

- **HTTPX/aiohttp**：静态 HTML、Feed、Sitemap、API、Conditional GET、Range GET；
- **Crawl4AI**：URL Seeder、`arun_many()` 流式多 URL 抓取、Browser Interaction、Markdown/Structured Extraction Adapter、Worker 内 Dispatcher；
- **Playwright**：复杂 JS 与特殊交互 fallback；
- **lxml/BeautifulSoup/CSS/XPath/Regex**：确定性抽取；
- **Trafilatura/Readability**：通用正文候选；
- **PDF parser + OCR fallback**；
- **S3/MinIO**：raw response、HTML、Rendered DOM、PDF、WARC、Canonical IR、Markdown、Structured JSON、Graph/AI artifact、fixture。

### 4.3 检索与派生

- **OpenSearch**：BM25、过滤、聚合、Alias/Generation；
- **pgvector/OpenSearch kNN**：首期向量；规模增长后可切 Qdrant/Milvus；
- **RRF/Controlled Fusion**：BM25 + Vector 基线；
- **Graph**：首期 PostgreSQL 表验证价值，需要高阶图查询再增加 Neo4j/Memgraph Adapter；
- **Cross Encoder Reranker**：可选。

### 4.4 可观测与运营

- Prometheus + Grafana + OpenTelemetry；
- PostgreSQL incremental/materialized read model；
- 数据规模进一步增长后可增加 ClickHouse；
- SSE/WebSocket：Web UI 实时状态；
- Signed Webhook：外部系统事件交付。

---

## 5. Release 与 Generation 治理

影响结果的策略禁止原地覆盖，至少版本化：

```text
site_profile_release
provider_release
url_normalization_release
url_filter_release
candidate_classifier_release
resolver_release
resource_probe_release
fetch_route_release
browser_pool_policy_release
interaction_recipe_release
extractor_release
quality_release
identity_release
markdown_release
filtered_markdown_release
structured_schema_release
structured_extraction_release
chunk_release
embedding_release
vector_index_release
graph_ontology_release
graph_projection_release
retrieval_release
analysis_release
model_release
prompt_release
webhook_policy_release
export_sink_release
ops_metric_release
runtime_artifact_release
```

Runtime Artifact 固定 Crawl4AI、Playwright、Browser Revision、Python、解析器、模型 digest、系统依赖和镜像 digest。

全文、向量、Graph 使用 Generation：

```text
BUILDING -> VALIDATING -> READY -> ACTIVE -> RETIRED
```

新 Generation 先离线构建、固定 judgment/fixture 验证、canary，再通过 Alias/逻辑指针原子切换。

---

## 6. Source、Site Profile 与新站 PROBE

### 6.1 Source

```text
source
- id
- tenant_id
- name
- entry_url
- registrable_domain
- allowed_origins
- source_type
- language_hint
- status
- robots_policy
- schedule_policy_id
- active_profile_release_id
```

### 6.2 Site Profile Release

```text
site_profile_release
- source_id
- version
- scope_rules
- provider_config
- url_normalization_release_id
- url_filter_release_id
- resolver_policy
- resource_probe_policy
- fetch_route_policy
- browser_pool_policy_id
- interaction_recipe_release_ids
- extractor_policy
- quality_policy
- markdown_policy
- chunk_policy_id
- schedule_policy
- rate_limit_policy
```

### 6.3 新站 PROBE

新增站点先低成本探测：

1. URL/domain/origin、DNS/IP、robots、SSRF/egress；
2. Sitemap Index / Sitemap；
3. RSS/Atom/JSON Feed；
4. WordPress/Ghost/Substack 等平台 API；
5. Archive、Category/Tag/Author、Blog Index、Docs TOC、pagination；
6. Common Crawl URL Index；
7. URL path/template 聚类；
8. HTTP link sample；
9. 小预算 Best-First/BFS；
10. HTTP 与 Browser 正文质量抽样；
11. wrapper/short-link/meta refresh/JS redirect；
12. HTML/PDF/MD/JSON Resource Probe；
13. 动态页面生成 Interaction Recipe；
14. 重复 DOM 生成 Selector/Schema 候选；
15. 自动生成 Profile Draft，Web 审核后启用。

新增第 1001 个站点的理想路径是“配置 + Probe + fixture 验证”，而不是新增 crawler 代码。

---

## 7. 全量历史发现与 Coverage

### 7.1 Provider Contract

```text
probe(source, profile) -> capability
discover(cursor, budget) -> candidates + evidence + next_cursor
checkpoint() -> durable_cursor
exhaustion() -> explicit_reason
```

Provider：

```text
CMS_API
SITEMAP
FEED
ARCHIVE
CATEGORY_TAG_AUTHOR
DOCS_TOC
DISCOVERY_SURFACE
BROWSER_DISCOVERY_SURFACE
COMMON_CRAWL_URL_INDEX
SITE_SEARCH
DEEP_CRAWL_BFS
DEEP_CRAWL_DFS
DEEP_CRAWL_BEST_FIRST
```

### 7.2 历史发现优先级

```text
CMS/API
 -> Sitemap/Sitemap Index
 -> Historical Feed
 -> Archive year/month
 -> Category/Tag/Author
 -> Blog Index / Docs TOC
 -> Common Crawl
 -> HTTP/Browser Discovery Surface
 -> Best-First/BFS gap filling
 -> Site Search supplement
```

Deep Crawl 是 gap discovery，不是 Coverage 真相。

### 7.3 Coverage 事实

```text
coverage_provider_run
- run_id
- source_id
- provider_type
- provider_release_id
- cursor_before
- cursor_after
- status
- exhaustion_reason
- candidate_count
- committed_observation_count
- completed_count
- failed_count
- known_gap
- blocker
- evidence_ref
```

预算耗尽只能标记：

```text
known_gap = true
exhaustion_reason = BUDGET_EXCEEDED
```

只有主要权威 Provider exhausted，或剩余缺口均成为可解释、可枚举的 known gap，`FULL_BACKFILL` 才能结束。

### 7.4 Sitemap Index 分片与 checkpoint

大站 Sitemap Index 必须把每个 child sitemap 作为独立分片：

```text
sitemap_shard_state
- source_id
- sitemap_url
- parent_sitemap_url nullable
- etag
- last_modified
- content_hash
- parse_cursor
- observation_commit_cursor
- state
- last_success_at
- provider_release_id
```

处理流程：

```text
conditional fetch child sitemap
 -> stream parse URLs
 -> observation mini-batch commit
 -> outbox/task publish
 -> advance durable observation cursor
 -> finish shard
```

Provider checkpoint 只能在 Observation 已持久化后推进，避免“发现游标前进但 URL 未落库”。

---

## 8. Durable Streaming Frontier

这是大规模全量历史抓取的核心。

### 8.1 不一次性物化全站 URL

禁止：

```text
all_urls = load_everything_from_sitemap()
await crawl(all_urls)
```

推荐：

```text
Provider page/shard
 -> 100~1000 URL mini-batch
 -> Observation commit
 -> Normalize/Filter/Classify
 -> Durable Task
 -> next Provider page/shard
```

单个站点即使有数十万 URL，也只保留一个有界工作窗口。

### 8.2 Frontier 优先级

```text
P0 new feed/API item
P1 manual/targeted fetch
P2 incremental high-yield URL
P3 historical authoritative provider
P4 historical gap/deep crawl
P5 reprocess/reindex/AI optional work
```

Source Sync 与新内容优先于 Graph/AI/Reembed 等可延迟 Projection。

### 8.3 水位控制

每 Source 和全局维护：

```text
frontier_low_watermark
frontier_high_watermark
max_pending_per_source
max_pending_per_domain
max_observation_commit_lag
```

当 Fetch/Extract 下游积压超过高水位，Discovery 暂停推进非紧急 Provider；降到低水位后恢复。

---

## 9. URL Observation、Normalization、Filter 与 Resolution

### 9.1 Observation First

```text
url_observation
- source_id
- observed_url
- provider_type
- provider_run_id
- parent_url nullable
- anchor_text nullable
- depth nullable
- observed_at
- evidence_ref
```

Filter 不能删除“曾经发现过这个 URL”的事实。

### 9.2 Normalization

全局安全规则：host lowercase、default port removal、fragment removal、path slash、安全 percent-encoding、query key ordering、tracking param removal。

站点规则：semantic query allowlist/denylist。

禁止全局删除 query；`?id=123`、`?p=456`、`?page=7` 可能有业务语义。

### 9.3 URL Filter Chain

```text
HARD_REJECT
SOFT_REJECT
DEPRIORITIZE
ACCEPT
```

HARD 仅用于明确禁止域、robots/policy、后台/登录、静态资产等；路径猜测、相关性、category/tag 默认 SOFT/DEPRIORITIZE。

每次决策保存：

```text
url_filter_decision
- url_id
- filter_release_id
- rule_id
- action
- reason_code
- score nullable
- evidence_ref
- decided_at
```

新 Filter Release 上线前对已知文章 URL fixture 做 recall 回归。

### 9.4 Classification / Resolution / Resource Probe

Candidate：

```text
ARTICLE_CANDIDATE
DISCOVERY_SURFACE
ASSET
NAVIGATION
EXTERNAL_REFERENCE
IGNORE_BY_POLICY
UNKNOWN
```

Resolution：

```text
Observed URL
 -> Normalized Candidate
 -> HTTP Redirect
 -> Wrapper Param / HTML Target
 -> Meta Refresh
 -> Browser Navigation Fallback
 -> Resolved Target
```

每一跳重新执行 DNS/IP/SSRF/origin scope 校验。`canonical`/`og:url` 是 Identity Evidence，不等同于 redirect。

Resource Probe 综合 Content-Type、Content-Disposition、magic bytes、Content-Length、HTML 壳、Range GET、redirect 和 Site Profile，产出：

```text
HTML_PAGE
PDF_DOCUMENT
MARKDOWN_TEXT
PLAIN_TEXT
JSON_API
XML_FEED
IMAGE
ARCHIVE
UNKNOWN_BINARY
```

---

## 10. Scheduler、Task、幂等与可靠性

Run Type：

```text
PROBE
FULL_BACKFILL
INCREMENTAL
RECONCILE
MANUAL_INGEST
TARGETED_FETCH
REPROCESS
RECHUNK
REEMBED
REINDEX
REGRAPH
RETRIEVAL_EVAL
RECIPE_VALIDATE
FILTER_VALIDATE
EXPORT_REBUILD
OPS_METRIC_REBUILD
```

Task State：

```text
PENDING
LEASED
RUNNING
SUCCEEDED
RETRY_WAIT
FAILED_TERMINAL
CANCELLED
```

Task 包含：`lease_owner`、`lease_until`、`fencing_token`、`attempt`、`checkpoint`、`deadline`、`budget_reservation`。

关键幂等键：

```text
DISCOVER:       source + provider_release + cursor/shard
FILTER:         url + filter_release
RESOLVE:        input_url + resolver_release
RESOURCE_PROBE: resolved_url + probe_release + epoch
FETCH:          url + route_release + fetch_epoch
EXTRACT:        snapshot + extractor_release
IDENTITY:       canonical_ir + identity_release
MARKDOWN:       document_version + markdown_release
FILTERED_MD:    document_version + filtered_markdown_release + input_hash
CHUNK:          boundary_id + chunk_release
EMBED:          chunk + embedding_release
INDEX:          projection + generation
GRAPH:          boundary_id + graph_projection_release + input_hash
ANALYZE:        document_version + analysis_release + manifest_hash
EXPORT:         sink + document_version + markdown_release + export_release
WEBHOOK:        subscription + event_id
```

批任务必须 partial success：成功事实立即持久化，失败项独立 retry。

Redis/Crawl4AI/n8n 中的 job id 只能映射到平台 Task：

```text
platform_task_id <-> adapter_job_id
```

Adapter 丢失、重启或升级不能导致平台丢失任务事实。

---

## 11. Fetch Plane：路由、流式批处理与 Browser Pool

### 11.1 Fetch Route

```text
HTTP_STATIC
 -> HTTP_ALT_HEADERS
 -> HTTP_PLATFORM_API
 -> FILE_STREAM_FETCH
 -> BROWSER_CRAWL4AI_RECIPE
 -> BROWSER_PLAYWRIGHT_CUSTOM
 -> SITE_CUSTOM_EXTRACTOR
```

站点内部不同 URL 可按 matcher 使用不同 route/extractor：

```text
/blog/*      -> HTML article
/docs/*      -> docs profile
*.pdf        -> PDF parser
/api/*       -> JSON
/search/*    -> discovery only
/account/*   -> hard reject
```

### 11.2 Bounded Window Streaming

Worker 不接收“整个站点 URL 数组”，而是从 Durable Task Store lease 一个窗口：

```text
lease N tasks
 -> group by route/profile/domain
 -> acquire platform permits
 -> HTTP batch or Crawl4AI arun_many(stream=True)
 -> each result completes independently
 -> persist Snapshot/Attempt/links
 -> ACK current URL
 -> refill below low watermark
```

N 是几十/数百量级的执行窗口，不是全量历史 URL 数。

### 11.3 Browser Pool

Browser Worker 启动时建立长期存活池：

```text
Browser Worker
  └─ pool(runtime_release, profile/security_scope, proxy_scope)
       ├─ browser/crawler instance
       │    ├─ context/session
       │    └─ context/session
       └─ browser/crawler instance
```

Browser Process 可以复用；认证态不跨安全边界共享。

回收条件：

- `max_pages_per_browser`；
- `max_browser_age`；
- worker/browser RSS 超阈值；
- crash/page error 超阈值；
- Runtime Release 切换；
- drain/部署；
- context 泄漏或清理失败。

池策略：

```text
browser_pool_policy
- min_instances
- max_instances
- max_contexts_per_instance
- max_pages_per_browser
- max_browser_age_seconds
- rss_soft_limit_mb
- rss_hard_limit_mb
- recycle_error_rate
- session_isolation_mode
- affinity_policy
```

### 11.4 两层并发控制

第一层是平台 permit：

```text
Global
 -> route
 -> registrable-domain
 -> source
 -> host
```

第二层是 Worker 本地：

- Crawl4AI `MemoryAdaptiveDispatcher`；
- 或固定 Semaphore；
- `RateLimiter`；
- 本地 browser page/context ceiling。

本地 Dispatcher 可以因为内存/错误率而主动降低并发，但不能突破平台 permit 上限。

### 11.5 页面新链接不能直接递归抓

Fetch Result 的 links 必须回到：

```text
URL Observation
 -> Normalize
 -> Scope
 -> Filter
 -> Classify
 -> Dedupe
 -> Budget/Priority
 -> Durable Task
```

禁止 Fetch Worker 自己维护无限递归 frontier。

---

## 12. Rate Limit、Retry 与端到端 Backpressure

### 12.1 分层配额

每个 Source/Profile 可配置：

```text
max_rps
max_concurrency
burst
max_browser_seconds_per_hour
max_bytes_per_hour
max_pages_per_run
retry_budget
```

同 registrable domain 的多个 Source 共享上层域配额。

### 12.2 Retry

- 429/503：解析 `Retry-After`，退避并降低 host/domain concurrency；
- timeout/connection reset：exponential backoff + jitter；
- 401/403/404/410：按原因分类，不盲目重试；
- robots/policy：终止；
- Browser crash：换实例后有限重试；
- HTTP 正文质量不足：可触发 Browser fallback；
- Extractor 失败：优先从已保存 Snapshot 离线重试，不重新访问源站。

### 12.3 端到端背压信号

不仅看 crawler 内存，还看：

```text
fetch_queue_lag
extract_queue_lag
snapshot_upload_lag
DB_commit_p95
object_store_p95
worker_RSS
browser_RSS
browser_page_count
429_503_rate
fetch_error_rate
```

控制逻辑：

```text
memory high OR downstream lag high
 -> shrink worker-local concurrency
429/503 high
 -> shrink host/domain permits + backoff
healthy AND queue lag high
 -> grow local concurrency, never above platform permits
```

这样把“Memory Adaptive”从浏览器局部能力升级为整条流水线的闭环背压。

---

## 13. Snapshot、Extraction、Canonical IR 与 Markdown

### 13.1 Snapshot First

```text
snapshot
- snapshot_id
- fetch_attempt_id
- source_id
- final_url
- snapshot_origin
- status_code
- content_type
- body_hash
- object_ref
- headers_hash
- rendered_dom_ref nullable
- captured_at
```

Extractor/Filter/Markdown/Graph/AI 升级优先对旧 Snapshot/Document Version replay，不重新访问源站。

### 13.2 Extraction Candidate

同一 Snapshot 可产生：

- CMS/API body；
- Site Selector；
- CSS/XPath/Regex；
- JSON-LD；
- Trafilatura/Readability；
- Crawl4AI cleaned HTML/raw Markdown；
- Rendered DOM；
- Feed Content；
- PDF Text/OCR；
- Markdown/TXT native parse。

### 13.3 Canonical IR

```text
DocumentIR
  metadata:
    title/authors/published_at/updated_at/language/canonical_url/tags
  blocks:
    heading/paragraph/list/quote/code/table/image/figure/formula
  assets[]
  outbound_links[]
  source_evidence[]
```

HTML/PDF/MD/TXT 最终统一进入 IR。

### 13.4 Quality

至少检查：

- 正文长度；
- 标题一致性；
- link density；
- boilerplate；
- code/table 保留；
- language；
- published_at confidence；
- truncation；
- selector match；
- required field；
- DOM drift；
- HTML 与 Browser 候选差异。

低质量候选进入 quarantine，Web 可比较不同 Extractor。

### 13.5 Canonical Markdown

从 IR 确定性生成：

- 去导航、广告、推荐、评论等 boilerplate；
- 保留标题层级、代码、列表、引用、表格、图片 alt/caption、重要链接；
- 解析相对链接；
- 保留文章完整主体，不做 query-dependent 删减；
- Front Matter 记录 document/version/source/canonical URL/content hash/release。

### 13.6 Filtered / Fit Markdown 是有损 Projection

Crawl4AI `PruningContentFilter`、BM25 filter、LLM filter 等产出的 `fit_markdown` 只能存为：

```text
FILTERED_MARKDOWN
ENHANCED_MARKDOWN
```

它们适合检索/RAG，但不能覆盖：

```text
Snapshot
Canonical IR
Canonical Markdown
```

这样过滤阈值或模型升级后可以从事实层离线重建，而不是重新抓源站。

---

## 14. Document Identity、版本与增量同步

### 14.1 Identity

综合：

- platform stable ID；
- canonical/og:url；
- redirect evidence；
- Feed GUID；
- CMS/article ID；
- normalized URL；
- exact/near duplicate；
- title + published time。

URL Alias、Document Identity、Document Version 分离。

Document Version 只在 Canonical IR 或关键元数据变化时创建。304、重复抓取、Markdown/Chunk/Embedding/Graph/AI/Export 策略升级不创建正文版本。

### 14.2 增量信号

```text
Feed/API cursor
 -> Sitemap conditional fetch + lastmod
 -> High-yield Surface
 -> Conditional GET
 -> Metadata/Resource Probe
 -> Full HTTP
 -> Browser fallback
```

保存 ETag、Last-Modified、previous_body_hash、content_length、content_kind。

304：不重复保存 body、不重新抽取、不新建 Version、不重做 Projection。

### 14.3 调度分层

```text
Tier 0 Feed/API            分钟级
Tier 1 Sitemap/Blog Index  10~60 分钟
Tier 2 Category/Archive    1~6 小时
Tier 3 Deep Reconcile      每周/每月
Tier 4 Common Crawl        新 collection/按月
```

Feed/API 使用 overlap window；周期 Reconcile 发现迟到、Feed 窗口外遗漏、URL 迁移、删除和结构漂移。

### 14.4 删除与 Tombstone

源站 404/410 不直接物理删除历史 Version。创建 source availability/tombstone 状态，并保留已归档事实；是否继续导出由 Policy 决定。

---

## 15. Markdown Export Projection

知识库需要文件形态消费，因此提供独立 Export Plane。

Sink：

```text
LOCAL_FS
S3_COMPATIBLE
GIT_REPOSITORY
ZIP_EXPORT
WEB_DOWNLOAD
```

```text
markdown_export
- export_id
- sink_id
- document_id
- document_version_id
- markdown_release_id
- export_sink_release_id
- path_key
- content_hash
- manifest_hash
- state
- exported_at
```

稳定路径建议：

```text
{source_slug}/{published_year}/{document_id}-{safe_slug}.md
```

Front Matter 至少：

```yaml
document_id: ...
document_version_id: ...
source_id: ...
canonical_url: ...
published_at: ...
content_hash: ...
markdown_release: ...
```

原子写：

```text
render
 -> temp file/object
 -> hash verify
 -> atomic rename/object commit
 -> manifest update
```

Git Sink 按 Source/时间窗口聚合 commit，避免百万文档每篇一个 commit。

---

## 16. Structured Extraction 与 AI Analysis

结构化抽取与 canonical 内容分离。

```text
structured_schema_release
- schema_json
- required_fields
- validation_policy
- fixture_refs

structured_extraction_release
- engine: API / JSON_LD / CSS / XPATH / REGEX / LLM
- schema_release_id
- selector_or_instruction
- model_release_id nullable
- prompt_release_id nullable
- quality_policy
```

优先级：

```text
platform API / JSON-LD / CSS / XPath / Regex
 -> deterministic extractors
 -> LLM only for complex/unstructured fields
```

LLM 使用 JSON Schema/Pydantic 约束，记录 token、latency、cost、validation error。

AI Analysis 与 Source Sync 解耦：SUMMARY、TOPIC、ENTITY、SENTIMENT、FAQ、ENHANCED_MARKDOWN 等均是可重放 Projection，失败不阻塞文档 READY。

---

## 17. Chunk、Embedding、全文与 Graph

### 17.1 Chunk

Structure-aware：Heading/Section 优先；段落、列表、引用尽量完整；code/table 默认不拆；保存 heading path/page/block range。

硬规则：

```text
Chunk NEVER crosses Document Version boundary
Chunk NEVER crosses Structured Record boundary
```

```text
chunk_id = hash(boundary_id + chunk_release_id + block_range + normalized_text_hash)
```

### 17.2 Embedding

```text
embedding_id = hash(chunk_id + embedding_release_id)
```

Worker 支持 batch token ceiling、OOM 自动缩 batch、lease/retry/partial success/backpressure。

### 17.3 Retrieval

基线：

```text
BM25 + Vector
 -> hard SOURCE/DOCUMENT/VERSION/ACL scope
 -> RRF/controlled fusion
 -> optional reranker
 -> context assembly
```

Graph 是可选第三召回通道，不替代原文/BM25/Vector。只有固定 judgment set 证明增量收益后才默认开启。

---

## 18. Event、Webhook 与外部自动化

事件：

```text
command.queued
command.started
command.succeeded
command.failed
run.progress
run.completed
source.backfill.completed
document.version.created
markdown.ready
markdown.export.ready
quality.quarantined
```

事件由业务事务通过 Outbox 产生。

Webhook：

- at-least-once；
- `event_id` 稳定；
- HMAC-SHA256；
- timestamp + delivery id 防重放；
- 2xx 才 ACK；
- exponential backoff + jitter；
- 超过最大次数进入 DLQ；
- Web UI 支持 replay；
- endpoint 做 SSRF/egress policy；
- payload 默认发 metadata/resource URL，大正文按权限 GET。

n8n 适合运营编排，不适合成为 durable state。MCP Agent 只能调用 Platform API，不直接操作 Redis、Coverage、Task 或对象存储内部状态。

---

## 19. Web 管理功能

### 19.1 Fleet Overview

展示：

- Source 状态；
- FULL_BACKFILL completion/known gap；
- freshness SLA；
- pending/leased/running/retry queue；
- changed documents；
- HTTP/Browser 比例；
- quality quarantine；
- Embedding/Graph/AI/Export backlog；
- index lag；
- watermark/coverage。

### 19.2 Source / Profile

支持：

- 新增/停用 Source；
- PROBE；
- Profile Draft/Review/Activate；
- Feed/Sitemap/API/Common Crawl 配置；
- Route/Filter/Extractor diff；
- 调度、限流、预算；
- Profile Release 回滚。

### 19.3 Coverage

展示：

- Provider cursor；
- Sitemap child shard checkpoint；
- exhaustion reason；
- candidate/Observation/unique URL 数；
- known gap；
- 历史时间覆盖；
- Common-Crawl-only URL；
- Browser-only discovery；
- Provider overlap matrix。

### 19.4 Durable Frontier / Streaming 视图

展示：

- discovery emitted URL/s；
- Observation commit/s；
- frontier pending/oldest age；
- current worker lease window；
- fetch/extract lag；
- Provider checkpoint；
- Source/domain watermarks；
- backpressure 原因。

### 19.5 Browser Pool 视图

展示：

- Browser Worker 数；
- browser instances；
- active contexts/pages；
- worker/browser RSS；
- current local adaptive concurrency；
- platform permit ceiling；
- restart/crash/recycle rate；
- pages per browser lifetime；
- HTTP -> Browser fallback ratio；
- Browser seconds/Source；
- recycle reason。

### 19.6 URL Filter 调试台

输入 URL/URL 集，显示命中规则、HARD/SOFT、decision、reason、Release；支持新旧 Release diff 和已知文章 recall fixture。

### 19.7 单 URL Pipeline Trace

```text
Observe -> Normalize -> Filter -> Resolve -> Probe -> Route -> Fetch
 -> Snapshot -> Extract -> Quality -> Identity -> Version
 -> Canonical Markdown -> Filtered Markdown -> Chunk -> Index -> Export
```

每步可查看 input/output hash、release、耗时、错误、evidence、artifact。

### 19.8 内容比较

同一 Document Version 可比较：

- raw HTML；
- cleaned HTML；
- Canonical IR；
- Canonical Markdown；
- Filtered/Fit Markdown；
- Browser 与 HTTP 抽取候选；
- 不同 Extractor Release diff。

### 19.9 操作审计

暂停、恢复、重跑、REPROCESS、REINDEX、REGRAPH、EXPORT、切 Release 全部走 Command API，生成 command id、actor、reason、idempotency key 和审计记录。

---

## 20. 安全、访问策略与合规边界

### 20.1 SSRF / Egress

每跳：

1. 解析 URL；
2. 验证 scheme；
3. DNS resolve；
4. 拒绝 private/link-local/loopback/metadata IP；
5. 验证 allowed origin/domain；
6. Redirect 后重新验证；
7. Browser navigation/request interception 同样执行策略。

### 20.2 Browser Session 隔离

- tenant/source/profile 安全域隔离；
- 不跨域共享 Cookie/LocalStorage；
- Secret 只经 Secret Manager 注入；
- worker 日志禁止输出 Cookie、Authorization、API key；
- 截图/HTML 可能含敏感信息时按 Object Policy 管理。

### 20.3 抓取策略

- robots 与站点 policy 可配置且默认尊重；
- 不默认绕过认证、付费墙或访问控制；
- per-domain rate limit；
- 保存来源 URL、抓取时间和 provenance；
- 对需要长期完整转载的第三方内容，应另外确认版权/许可证与内部使用范围。

---

## 21. 可观测性、SLO 与质量指标

### 21.1 Source Sync SLO

至少跟踪：

```text
freshness_lag_seconds
provider_checkpoint_lag
coverage_known_gap_count
fetch_success_rate
http_to_browser_fallback_rate
content_change_rate
quality_quarantine_rate
retry_rate
```

### 21.2 Streaming / Worker 指标

```text
frontier_depth
frontier_oldest_age
observation_commit_rate
fetch_completion_rate
extract_queue_lag
snapshot_upload_p95
DB_commit_p95
worker_RSS
browser_RSS
browser_active_pages
adaptive_concurrency_current
platform_permit_ceiling
429_503_rate
browser_recycle_rate
```

### 21.3 内容质量指标

- title match rate；
- empty/too-short rate；
- truncation rate；
- code/table preservation fixture pass rate；
- published_at confidence；
- canonical identity conflict rate；
- Markdown deterministic rebuild hash match。

### 21.4 告警

- Source freshness 超 SLA；
- Provider cursor 长时间不前进；
- frontier oldest age 超阈值；
- 429/503 突增；
- Browser crash/recycle 异常；
- Snapshot 写入失败；
- Outbox backlog；
- DB/对象存储延迟异常；
- quarantine 激增；
- Release canary 回归失败。

---

## 22. 数据分区、存储与保留

### 22.1 PostgreSQL

大表按时间/tenant/source hash 分区：

- url_observation；
- task/task_attempt；
- snapshot metadata；
- event/outbox；
- webhook delivery；
- retrieval trace。

常用索引围绕 `source_id + state + scheduled_at`、`normalized_url_hash`、`document_id + version`、`task idempotency key`。

### 22.2 Object Storage

对象路径不依赖标题：

```text
snapshots/{source_id}/{yyyy}/{mm}/{snapshot_id}
ir/{document_id}/{version_id}.json
markdown/{document_id}/{version_id}/{release}.md
projection/{type}/{generation}/...
```

对象 immutable；元数据引用 hash。大正文不写 PostgreSQL 行内。

### 22.3 Retention

- Snapshot/Canonical IR：按知识库合规策略长期保留；
- Rendered DOM/screenshot：可按成本策略缩短；
- Worker debug log：短期；
- Derived Projection：随时可重建，可按 generation 淘汰；
- Export：依据 Sink Policy 保留。

### 22.4 备份

- PostgreSQL PITR；
- Object Storage versioning/replication；
- Release/Profile/Schema 进入 Git/数据库双重审计；
- 定期做 restore drill。

---

## 23. 关键 API

### 23.1 Source

```text
POST /sources
POST /sources/{id}/probe
POST /sources/{id}/backfill
POST /sources/{id}/sync
POST /sources/{id}/pause
POST /sources/{id}/resume
GET  /sources/{id}/coverage
GET  /sources/{id}/frontier
GET  /sources/{id}/browser-stats
```

### 23.2 URL / Document

```text
POST /ingest/url
GET  /urls/{id}/trace
POST /urls/{id}/retry
GET  /documents/{id}
GET  /documents/{id}/versions
GET  /documents/{id}/artifacts
POST /documents/{id}/reprocess
```

### 23.3 Release

```text
POST /profiles/{id}/validate
POST /profiles/{id}/activate
POST /filters/{id}/validate
POST /extractors/{id}/validate
POST /generations/{id}/activate
```

长任务统一返回 `202 Accepted + command_id`，由 Event/SSE/Webhook 通知完成。

---

## 24. 关键数据表最小集合

```text
source
site_profile_release
provider_state
coverage_provider_run
sitemap_shard_state
url_observation
normalized_url
url_filter_decision
url_alias
run
task
task_attempt
fetch_attempt
snapshot
document
document_version
canonical_ir_artifact
projection_artifact
markdown_export
release_registry
outbox_event
event
webhook_subscription
webhook_delivery
audit_log
```

Redis、crawler frontier、Browser Pool 内存状态都不是这些表的替代品。

---

## 25. 初始容量与部署建议

首期不要直接按“1000 站同时 Browser”设计，建议分类：

```text
静态/Feed/API 站点         -> HTTP Worker 为主
轻度 JS                    -> HTTP + Browser fallback
重度 JS/交互               -> Browser Worker 专属队列
PDF/OCR                     -> 独立 CPU/OCR Worker
Embedding/Graph/AI          -> 独立低优先级 Projection Worker
```

起步值仅作为默认模板，最终由压测调整：

- 每 domain 并发 1~4；
- 每 Source 并发 1~8；
- Browser Worker 每实例 context/page 上限小于 HTTP 并发一个数量级；
- Browser local adaptive ceiling 根据 RSS 自动收缩；
- Discovery mini-batch 100~1000 URL；
- Fetch lease window 20~200 URL，按 route 和内存调整；
- Redis Stream backlog 只做运输观测，真正任务状态以 PostgreSQL 为准。

---

## 26. 实施阶段

### Phase 1：可用的全量历史库

- Source/Profile；
- CMS/Sitemap/Feed/Archive Provider；
- URL Observation + Durable Frontier；
- HTTP Fetch；
- Snapshot；
- Extract + Canonical IR + Canonical Markdown；
- Document Identity/Version；
- Local/S3/Git Export；
- Web Source/Coverage/URL Trace；
- 基础增量同步。

验收：选 30~50 个结构差异大的站点，证明可恢复全量、Coverage 可解释、Markdown 可重建。

### Phase 2：动态站与规模化执行

- Crawl4AI Browser Adapter；
- `arun_many(stream=True)` bounded window；
- Browser Pool；
- MemoryAdaptiveDispatcher/Semaphore 二级并发；
- Domain/Source permit；
- Interaction Recipe；
- Browser Pool / Streaming Web 视图；
- 1000 Source 分层调度。

验收：长时间运行无明显内存爬升；worker 重启后 Task 可恢复；高峰 backlog 可通过背压稳定消化。

### Phase 3：质量与检索

- Extractor fixture；
- Quality quarantine；
- Filtered/Fit Markdown Projection；
- Chunk/Embedding/OpenSearch；
- Hybrid Retrieval；
- Generation 切换。

验收：Canonical Markdown 完整性与检索 Projection 解耦；Filter/Embedding 升级不重新抓源站。

### Phase 4：高级能力

- Graph；
- AI Analysis；
- n8n/MCP；
- Signed Webhook；
- 高级运营分析/ClickHouse（确有需要再引入）。

高级能力不得反向阻塞 Source Sync 主链路。

---

## 27. 最终推荐运行链路

### 全量历史

```text
Add Source
 -> PROBE
 -> Profile Review/Activate
 -> authoritative Providers
 -> stream URL Observations
 -> Durable Frontier
 -> bounded Fetch lease window
 -> platform permits
 -> HTTP or Browser Pool
 -> per-URL Snapshot commit
 -> Extract / Quality / Canonical IR
 -> Identity / Document Version
 -> Canonical Markdown
 -> Export
 -> async Chunk/Index/Graph/AI
 -> Coverage reconcile
```

### 增量同步

```text
Feed/API cursor + overlap
 -> Sitemap Conditional GET
 -> high-yield surfaces
 -> Conditional GET / 304
 -> changed Snapshot only
 -> new Document Version only when canonical content changes
 -> rebuild affected Projections
 -> periodic Reconcile for late/missing/moved URLs
```

### 动态页面

```text
HTTP quality insufficient
 -> Browser route
 -> acquire domain/source permit
 -> lease/reuse Browser Pool instance
 -> worker-local adaptive dispatcher
 -> interaction recipe with hard budget
 -> Snapshot
 -> release context/page
 -> recycle browser by policy
```

这套方案把 Crawl4AI 的 URL Seeder、流式多 URL 抓取、Dispatcher、Browser 复用和 Markdown 能力放在正确的执行层位置，同时把 Coverage、持久化 frontier、幂等、增量、版本、回放、质量和 Web 管理留在平台事实层。这样既适合首期 1000 个技术博客，也能在后续新增网站和提升抓取规模时保持可扩展、可恢复、可审计。