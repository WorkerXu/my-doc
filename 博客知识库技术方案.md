# 博客知识库技术方案

适用范围：从约 1000 个技术博客、工程博客、技术文档站、Newsletter、CMS 与公开文档站抓取尽可能完整的历史文章，清洗为稳定 Markdown 建设长期知识库；后续持续增加网站，并支持全量回灌、增量同步、Web 管理、版本留存、离线重处理、全文/向量检索、质量审计、图片附件归档，以及 Dify/n8n/Agent/SEO 分析/Newsletter/TTS 等下游消费。整体按百万级文档、数百万到千万级 URL、站点高度异构和多年持续运行设计。

## 1. 建设目标

1. 支持站点根地址、单 URL、批量 URL、RSS/Atom/JSON Feed、Sitemap/Sitemap Index、OPML、CSV、JSON、Markdown/TXT 等来源导入。
2. 自动探测 Sitemap、Feed、公开 API、Archive、Category/Tag/Author、Docs Navigation、普通内链、`llms.txt`/`llms-full.txt`、平台 API 等发现入口。
3. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`PROBE`、`METADATA_PROBE`、`FOCUSED_DISCOVERY`、`REPROCESS`、`RECHUNK`、`REEMBED`、`REINDEX`、`ASSET_REPAIR`、`DERIVED_BUILD`、`CONTEXT_REFRESH`、`DELIVERY_RETRY` 等运行类型。
4. 历史完整性由 Provider 级 Coverage Evidence 证明，不能以“队列空了”“达到 max_depth/max_pages/max_urls”代替完整性证明。
5. HTTP-first；只有动态渲染、正文缺失、挑战页、选择器失效或质量失败有证据时才升级 Browser/Crawl4AI/Playwright。
6. 支持站点能力学习，用正文接受率、质量、延迟、成本和 blocker 优化抓取 route，但 Capability 不替代 Coverage。
7. Source/Profile/Discovery/Priority/Budget/Fetch/Extractor/PostProcess/Quality/Identity/Asset/Projection/Search/Embedding/Context/AI/TTS/Workflow/Delivery 全部采用不可变 Release。
8. 使用 Sitemap `lastmod`、Feed GUID/updated、API cursor、ETag、Last-Modified、body hash、Canonical IR hash、重叠窗口与周期 Reconcile 驱动增量同步。
9. 最终知识对象保存标题、作者、发布时间、更新时间、标签、canonical、正文块、代码块、表格、图片、附件、链接关系、Snapshot、字段 provenance 和完整 lineage。
10. 支持 durable frontier、断点续跑、幂等、lease、heartbeat、重试、限流、熔断、公平调度、backpressure 和 dead-letter。
11. 保存不可变 Network Snapshot、Rendered DOM、Extraction Candidate、Canonical IR、Accepted Document Version 和质量结果；规则升级优先离线 replay，不重复访问源站。
12. Markdown、Chunk、全文索引、Embedding、摘要、标签、Topic、FAQ、Digest、Newsletter、SEO/内容分析报告、TTS 均为可重建 Projection/Derived Artifact，失败不能阻塞 Source Sync。
13. Web 管理端覆盖 Source Intake、Site Profile、Provider Coverage、Focused Discovery、Capability & Route、Run/Task、Budget、Targeted Fetch、Extraction Preview、Snapshot Replay、Version Diff、Quality/Drift、Assets、Chunk/Embedding、Search/RAG、Context Snapshot、Analysis Manifest、AI Artifact、Integration、Workflow/Delivery、成本和审计。
14. Canonical IR hash 在 Chunk/Embedding/AI/TTS 等昂贵下游之前计算；正文未变化只写 Freshness Observation。
15. Change-Signal Plane 优先通过 Feed/Sitemap/API/HTTP 元数据判断是否可能变化，再决定是否抓正文。
16. 日报/Newsletter/批量问答/SEO 报告必须先冻结 Selection/Input/Analysis Manifest，再执行摘要、RAG、TTS 或外部 Workflow，保证补跑和重放结果可解释。
17. Dify/n8n/Agent 只能通过版本化 Integration Job Contract 使用抓取和派生能力，不得依赖内部 crawler task id、hostname 或返回结构。
18. 所有网络、模型、TTS、外部分析 Provider 和 Delivery 副作用在 dispatch 前必须取得持久 Budget Reservation。
19. Discovery 中的主题相关度、URL pattern、SEO/head metadata、链接位置、人工主题和第三方 score 只能生成 Priority Hint；在 `FULL_BACKFILL`/`RECONCILE` 中不得因低分删除合法 Coverage Candidate。
20. 候选页 `<head>`/有限字节探测必须建模为显式 `METADATA_PROBE`，受缓存、QPS、字节、deadline、SSRF、robots 和总预算约束。
21. Crawl4AI 等第三方 runtime 的版本、配置、返回字段和 score 通过 Adapter 隔离；业务层不直接绑定 `markdown_v2`、内部 queue score 等易变化 API。
22. Chunk ID、Embedding Projection 和 RAG Citation 必须稳定可重放；禁止用随机 UUID 作为唯一业务身份后直接覆盖向量库。
23. 向量库、全文索引和 Markdown 文件不是正文真相源；PostgreSQL + 对象存储中的不可变事实可以重建所有 Projection。

## 2. 非目标与边界

- 不绕过登录、付费墙、验证码、WAF 或明确访问控制。
- 不承诺未知站点仅靠自动探测达到 100% 历史完整；必须展示 coverage confidence、known gap 与 blocker。
- 不用 LLM、Reranker、SEO 分数、URL 关键词、SERP 排名或抓取成功率决定 FULL_BACKFILL 中合法历史文章是否存在。
- Markdown 不是唯一真相；Canonical IR 是内部内容语义真相，Markdown 是确定性投影。
- 不锁死单一爬虫库、浏览器库、向量库、搜索引擎、消息队列、工作流引擎、SERP/GSC Provider 或模型供应商。
- HTTP 200、Browser success、协程正常返回不等于业务内容成功。
- 不把多个 Document Version 的全文拼成一个无结构大字符串作为默认 AI/TTS 输入。
- 不在 Web/Dify/n8n 请求线程同步等待长期 Browser、LLM、OCR、Embedding 或 TTS 全链路完成。
- 不让 n8n/Dify Loop/Batch 节点成为 1000 站点全局抓取调度器。
- 不把 Google Sheets/Drive/Notion 等协作工具作为主内容真相源或唯一运行状态库。
- 不把 SERP Top N、搜索结果 Top K 或人工竞品 URL 当成历史 Coverage 证明；它们属于业务分析样本。
- 不允许 Workflow Code/Plugin/HTTP Request/Browser/Tool/自定义节点绕过统一 egress、Admission、SSRF、预算和审计策略。
- 不相信第三方 `BestFirst`、`score`、`priority` 等命名天然满足平台排序语义；必须做归一化和实际 runtime contract test。
- 不把“最多返回 N 个成功结果”解释成“最多发出 N 次源站请求”；redirect、retry、prefetch、batch 都纳入 dispatch budget。
- 不允许 fixed interval refresh 每次无差别删除全部向量再重建；增量更新必须基于内容版本和 Chunk diff。
- 不允许只保存 source URL 作为 RAG 引用；引用必须能固定到 Document Version 和 Chunk。

## 3. 总体架构

```text
                        ┌──────────────────────────┐
                        │         Web / API        │
                        │ Source / Run / Search UI │
                        └────────────┬─────────────┘
                                     │ Command
                           ┌─────────▼─────────┐
                           │ PostgreSQL Truth │
                           │ State + Metadata │
                           └──────┬──────┬─────┘
                                  │Outbox │Projection
                                  │       │
                          ┌───────▼───┐   │
                          │Redis Stream│   │
                          │ Task Bus   │   │
                          └───────┬────┘   │
                                  │        │
              ┌───────────────────┼────────────────────┐
              │                   │                    │
      ┌───────▼────────┐  ┌──────▼───────┐   ┌────────▼────────┐
      │Discovery/Probe │  │HTTP/Browser   │   │Context/Workflow │
      │Coverage Worker │  │Fetch Worker   │   │AI/TTS/Delivery  │
      └───────┬────────┘  └──────┬───────┘   └────────┬────────┘
              │                  │                    │
              └──────────────┬───┴──────────────┬─────┘
                             │                  │
                      ┌──────▼───────┐   ┌──────▼────────┐
                      │ S3 / MinIO   │   │ Search/Vector │
                      │ Immutable    │   │ Rebuildable   │
                      │ Snapshots    │   │ Projection    │
                      └──────────────┘   └───────────────┘
```

核心事实边界：

- **Coverage**：应该有哪些历史文章。
- **Candidate/Evidence**：观察到了哪些 URL、从哪里发现。
- **Priority Hint**：预算内先处理谁。
- **Capability/Route**：怎样抓更可靠、更便宜。
- **Budget Reservation**：真实副作用是否允许派发。
- **Snapshot**：源站当时实际返回了什么。
- **Canonical IR / Document Version**：平台接受的正文事实是什么。
- **Markdown / Chunk / Search / Embedding**：从某一 Accepted Version 可重建的投影。
- **Context Snapshot**：GSC/SERP/PageSpeed/BigQuery 等外部观察事实。
- **Manifest**：某次批量消费到底使用了哪些不可变输入。
- **Derived Artifact**：摘要、报告、Newsletter、TTS 等结果。
- **Integration/Delivery Job**：外部系统消费与交付边界。

## 4. 技术选型

### 4.1 控制面

- API：FastAPI/Python，业务 Contract 使用 Pydantic/JSON Schema；也可用 Go 实现高 QPS API，但不影响整体 Contract。
- PostgreSQL：业务状态、索引元数据、任务真相、版本、Manifest、Audit。
- Redis Streams：任务运输和短期信号，不保存决定性业务状态。
- Transactional Outbox：所有需要发消息的状态变化先和业务数据同事务写 PG，再异步 relay 到 Redis Streams。
- 前端：React/Vue + TypeScript，所有高风险 Command 使用强类型 API。

### 4.2 数据面

- S3/MinIO：原始响应、HTML、Rendered DOM、MHTML、截图、PDF、Canonical IR 大对象、Markdown、附件、派生 Artifact。
- OpenSearch：全文搜索、过滤、聚合，可删除重建。
- pgvector/Qdrant/Milvus/OpenSearch kNN：向量 Projection；起步优先 pgvector 或 OpenSearch kNN，规模和延迟要求上升后再拆专用 Vector DB。
- Crawl4AI/Playwright：Browser/动态页 Adapter，不承担平台调度真相。
- Trafilatura/Readability/CSS/XPath：确定性正文 Extractor。

## 5. 核心数据模型

### 5.1 Source 与配置

`source`

```text
id
name
entry_url
registrable_domain
allowed_origins
source_type
status
policy_class
robots_policy
schedule_policy_id
active_profile_release_id
created_at / updated_at
```

`site_profile_release`

```text
id
source_id
version
scope_rules
provider_config
url_normalization_rules
fetch_route_policy
browser_recipe
extractor_policy
quality_policy
asset_policy
chunk_policy_id
created_by
created_at
```

Release 一旦被 Run 引用即不可修改，只能创建新 Release。

### 5.2 Run / Task / Budget

`run`

```text
id
source_id
run_type
requested_config
effective_config
profile_release_id
status
started_at
finished_at
coverage_summary
cost_summary
```

`task`

```text
id
run_id
stage
task_type
url_id
operation_type
priority
state
attempt
lease_owner
lease_until
deadline_at
budget_reservation_id
parent_task_id
created_at / updated_at
```

`budget_reservation`

```text
id
scope_type / scope_id
resource_type
reserved_amount
consumed_amount
expires_at
state
idempotency_key
```

资源类型至少包括：request、response bytes、browser seconds、OCR pages、embedding tokens/chunks、LLM tokens、TTS chars、Context Provider calls、Delivery calls。

### 5.3 Discovery / Coverage

`coverage_provider_run`

```text
id
run_id
provider_type
provider_release_id
cursor_before
cursor_after
status
exhaustion_reason
candidate_count
known_gap
blocker
```

`url_candidate`

```text
id
source_id
normalized_url
url_hash
first_seen_at
last_seen_at
state
```

`url_evidence`

```text
id
candidate_id
provider_run_id
parent_url
relation_type
observed_at
raw_href
provider_cursor
metadata_snapshot_id
```

同一 URL 可以有多条 Evidence，不因去重丢失来源证明。

### 5.4 Fetch / Snapshot / Extraction

`fetch_observation`

```text
id
source_id
url_id
run_id
task_id
route_release_id
requested_url
final_url
status_code
response_headers_hash
etag
last_modified
content_type
body_hash
started_at / finished_at
outcome_code
snapshot_id
```

`network_snapshot`

```text
id
content_hash
object_key
content_type
bytes
capture_type
created_at
```

相同内容可 content-addressed 去重，但 Observation 不能去重，因为时间和响应头是事实。

`extraction_candidate`

```text
id
snapshot_id
extractor_release_id
candidate_type
canonical_ir_ref
quality_score
quality_breakdown
status
```

### 5.5 Document / Version / Projection

`document`

```text
id
source_id
canonical_identity_key
current_version_id
created_at
```

`document_version`

```text
id
document_id
source_url_id
snapshot_id
canonical_ir_hash
metadata_hash
version_no
published_at
updated_at
accepted_at
extractor_release_id
quality_release_id
status
```

`freshness_observation`

```text
id
document_id
url_id
observed_at
etag
last_modified
body_hash
canonical_ir_hash
result
```

`markdown_projection`

```text
id
document_version_id
projection_release_id
object_key
content_hash
state
```

### 5.6 Chunk / Embedding

`chunk_projection`

```text
id
document_version_id
chunk_policy_release_id
state
chunk_count
projection_hash
created_at
```

`chunk`

```text
id
chunk_projection_id
ordinal
structural_key
heading_path
block_start / block_end
char_start / char_end
token_start / token_end
content_hash
content_ref
language
split_reason
```

`embedding_projection`

```text
id
chunk_projection_id
embedding_model_release_id
index_namespace
state
ready_at
```

`embedding_cache`

```text
chunk_content_hash
embedding_model_release_id
vector_ref
dimension
created_at
```

Chunk 业务身份必须确定性生成，禁止只依赖随机 UUID。

## 6. Source Intake 与站点边界

新增站点进入 `PROBE`：

1. URL 标准化；
2. 使用 Public Suffix List 解析 registrable domain；
3. DNS/SSRF 检查；
4. 获取 robots、主页头信息、Sitemap/Feed 候选；
5. 探测 CMS/平台特征；
6. 生成 Site Profile Draft；
7. 抓 3~10 个 Golden URL 做 Extraction Preview；
8. 人工或自动批准后发布 Profile Release；
9. 才允许 FULL_BACKFILL。

域名判断不能只使用字符串后缀或 `netloc == base_domain`。必须区分：

- registrable domain；
- origin；
- allowed subdomain；
- CDN/asset domain；
- canonical redirect domain；
- 外链。

所有 redirect 后再次执行 Admission/SSRF/domain scope 检查。

## 7. 历史 Coverage 与 Discovery

### 7.1 Provider 优先级

历史回灌按“能证明边界的 Provider 优先”：

1. 官方 API / export；
2. Sitemap Index / Sitemap；
3. RSS/Atom/JSON Feed；
4. Archive/分页列表/年份月份归档；
5. Tag/Category/Author；
6. Docs Navigation；
7. `llms.txt` / `llms-full.txt`；
8. 普通内链图；
9. Search/SERP/人工补充仅作为 gap discovery，不作为 Coverage 证明。

### 7.2 Coverage 完成条件

每个 Provider 必须给出可审计的 completion evidence，例如：

- Sitemap Index 所有子 Sitemap 处理完成；
- API cursor 已 exhaustion；
- Archive pagination 达到稳定终点；
- Feed 已覆盖到已知历史边界，但 Feed 通常不能单独证明全部历史；
- Link crawler 达到无新增 candidate 仅说明该图遍历收敛，不自动等于站点历史完整。

Run 结束输出：

```text
provider coverage
candidate count
accepted document count
known gap
blocked paths
robots/policy blocker
unreachable URLs
coverage confidence
```

### 7.3 Prefetch 与 Metadata Probe

Crawl4AI `prefetch`、纯 HTTP link extraction、HEAD/有限字节 GET 可作为 Fast Discovery Route；Crawl4AI `link_preview_config` 或自建 head parser 可作为 `METADATA_PROBE` Adapter。

原则：

- Prefetch 只产生 Candidate/Evidence；
- Metadata Probe 只产生 metadata observation / Priority Hint；
- 它们都不直接生成 Accepted Document Version；
- 任何需要联网的 scorer/filter 都必须变成持久 Task，不能偷偷在进程内访问网络；
- Probe Snapshot 可被不同 Priority Release 离线重放。

## 8. Priority 与 Capability

### 8.1 Priority Hint

Priority 可使用：

- Provider 类型；
- 发布日期；
- URL path pattern；
- title/head metadata；
- 主题关键词；
- parent link context；
- 人工优先级；
- 第三方 raw score。

平台统一定义：`normalized_priority` 越大越先处理。第三方 queue/scorer 由 Adapter 映射，不直接复用 raw score。

FULL_BACKFILL/RECONCILE 中 Priority 只改变顺序，不能删除 Coverage Candidate。

### 8.2 Capability Projection

按 `source + operation_type + route` 聚合：

```text
success rate
accepted rate
quality score
latency p50/p95
bytes
browser seconds
blocker rate
cost
last observed at
sample count
```

Route Decision 根据 Capability、Profile 和 Policy 选择抓取方式。Capability 是可重建 Projection，不是 Redis 中的临时计数。

## 9. Durable Frontier、调度与公平性

PostgreSQL 保存 Task 真相，Redis Streams 只运输。标准状态：

```text
PENDING
READY
LEASED
RUNNING
SUCCEEDED
FAILED_RETRYABLE
FAILED_FINAL
CANCELLED
EXPIRED
```

Worker：

1. 获取短 lease；
2. dispatch 前申请 Budget Reservation；
3. heartbeat 延长 lease；
4. 完成后同事务写 outcome + outbox；
5. 崩溃后 lease 到期重新调度。

调度必须同时限制：

- global concurrency；
- per registrable domain；
- per origin；
- source weight；
- route/browser pool；
- bytes/sec；
- Browser/GPU/OCR/Model quota。

单站点 429、超时或 WAF 不能拖死其他 Source。采用 weighted fair scheduling + per-origin token bucket + exponential backoff + circuit breaker。

进程内 `asyncio.Semaphore` 只做 Worker 本地资源保护，不承担平台全局真相。

## 10. Fetch Route 与 Crawl4AI Runtime Adapter

### 10.1 Route 顺序

默认：

```text
Platform API / Feed Fast Path
  -> Plain HTTP
  -> HTTP + deterministic extractor
  -> Crawl4AI / Browser
  -> Site-specific Browser Recipe
  -> OCR/PDF/LLM structured extraction fallback
```

### 10.2 统一 Typed Fetch Result

业务层只读取：

```text
requested_url
final_url
status_code
headers
content_type
body_ref
rendered_dom_ref
links
media
route_release_id
runtime_release_id
duration
bytes
outcome_code
```

不允许业务层直接依赖第三方库私有字段。

### 10.3 Crawl4AI 兼容层

建立 `crawler_runtime_release`：

```text
library = crawl4ai
package_version
browser_revision
config_schema_version
adapter_version
default_browser_config
default_run_config
```

当前 Crawl4AI 已使用 `result.markdown.raw_markdown`；旧示例中的 `markdown_v2` 已移除。平台 Adapter 必须：

- 通过 `CrawlerRunConfig` 明确传运行参数；
- 对 `result.markdown`、links、metadata、status/error 做归一化；
- 对 package 升级做 Contract Test；
- 保存 runtime release，保证历史结果可解释。

Crawl4AI deep crawl 的 `resume_state/on_state_change` 可用于一个执行单元内的 crash recovery，但平台 Candidate、Task、visited/evidence 最终仍必须落 PG；第三方 resume state 只是执行优化。

## 11. Snapshot、Extraction 与 Canonical IR

每次网络获取先保存不可变 Snapshot，再做内容解释：

```text
Network Response
  -> raw body snapshot
  -> optional rendered DOM/MHTML/screenshot
  -> extractor candidates
  -> quality scoring
  -> selected Canonical IR
```

推荐并行候选：

- Site-specific CSS/XPath；
- Trafilatura；
- Readability；
- Crawl4AI cleaned content；
- JSON-LD/article structured data；
- LLM structured extraction 仅在必要时 fallback。

Canonical IR 使用结构化块：

```text
DocumentIR
  metadata:
    title
    author[]
    published_at
    updated_at
    canonical_url
    tags[]
    language
  blocks[]:
    heading(level,text)
    paragraph(text)
    code(language,text)
    list(items)
    quote(text)
    table(rows)
    image(asset_ref,alt,caption)
    formula(text)
```

不要在 IR 阶段把所有内容压成纯文本。

## 12. Quality Gate

成功必须是业务成功，不是 HTTP success。Quality 至少检查：

- title 是否存在且合理；
- 正文字符/词数；
- boilerplate/link density；
- 代码、表格、图片结构保留；
- canonical 合理性；
- 发布时间可信度；
- 页面是否 challenge/login/404 shell；
- 与 Golden Template 相似度；
- extraction candidate 之间一致性；
- 重复/镜像检测；
- language/encoding。

Quality Result 绑定 `quality_policy_release_id`，可在旧 Snapshot 上离线重跑。

低质量时按 Profile 决定 route escalation，不允许无限 Browser 重试。

## 13. URL、Document 与 Version Identity

必须分离：

- **URL Identity**：某个归一化 URL；
- **URL Alias**：redirect/canonical/历史地址关系；
- **Document Identity**：逻辑文章；
- **Document Version**：某次 Accepted 内容版本；
- **Freshness Observation**：确认内容仍新鲜但正文未变化。

URL canonicalization 处理：scheme/host case、默认端口、fragment、tracking query、排序规则、尾斜杠策略；但不得在未证实前删除业务有意义 query。

Document Identity 可综合 canonical、平台 article id、Feed GUID、URL alias、内容 fingerprint；任何自动 merge 必须保留证据并可撤销。

## 14. 增量同步

### 14.1 Change-Signal Plane

优先级：

1. Feed GUID/updated；
2. Sitemap lastmod；
3. 平台 API cursor；
4. ETag / If-None-Match；
5. Last-Modified / If-Modified-Since；
6. metadata probe；
7. 周期 Reconcile。

Feed/API 增量必须使用 overlap window，不能只记“最后一个时间戳”后严格大于，否则时钟漂移、迟到数据和排序变化会漏项。

### 14.2 内容变化判定

```text
Signal says maybe changed
  -> Fetch
  -> body hash
  -> extract Canonical IR
  -> canonical_ir_hash
```

- HTTP 304：写 Freshness Observation；
- 200 但 body hash 相同：写 Observation；
- body 变化但 Canonical IR 未变：写 Observation，可记录 presentation change；
- Canonical IR 变化：创建新 Document Version。

不因每次定时同步而创建空版本。

### 14.3 删除与下线

单次 404/410/Provider 消失不立即删除知识：

- 记录 availability observation；
- 多次确认 + Reconcile；
- 标记 source availability/tombstone；
- 历史 Version 仍按保留策略存在；
- Search current projection 再决定是否隐藏。

## 15. Markdown Projection

Markdown 从 Accepted Document Version 确定性生成：

- Front Matter 可包含 document/version/source/canonical/published/updated/tags；
- 标题层级、代码 fence、表格、列表、引用、图片 alt/caption 必须稳定；
- 资源 URL 可映射到归档 asset ref；
- 同 `Canonical IR + Markdown Release` 必须产生相同 hash；
- Markdown 规则升级走 `REPROCESS`/Projection rebuild，不重新访问源站。

文件路径只用于人类浏览，不承担 Document Identity。

## 16. Chunk Projection

Chunk 是检索 Projection，不是正文真相。

### 16.1 Chunk Policy

优先按语义结构切分：

1. heading section；
2. paragraph/list/table/code block 原子边界；
3. 超长 section 再按 tokenizer-aware recursive split；
4. overlap 仅在必要边界使用；
5. 中英文/代码密集页面可使用不同 release。

固定“800 字符 + 80 overlap”只能作为初始参数，不作为长期全局事实。

### 16.2 稳定 Chunk Identity

每个 Chunk 保存完整 `heading_path`，例如：

```text
Asyncio > Task Management > Error Handling
```

确定性主键可使用：

```text
chunk_id = hash(
  document_version_id,
  chunk_policy_release_id,
  ordinal,
  structural_key,
  content_hash
)
```

同一 Version + 同一 Chunk Release 重跑必须生成同一 chunk 集和 hash。

### 16.3 Chunk Diff

新 Version 生成后和上一 Accepted Version 比较：

- exact `content_hash` 相同：unchanged；
- structural_key 相同但 hash 变：changed；
- 新 key：added；
- 旧 key 消失：removed。

Diff 只用于复用和 UI，不改变 Version append-only 事实。

## 17. Embedding 增量投影

### 17.1 Model Release

记录：

```text
provider
model
model_revision
tokenizer_revision
dimension
normalization
pooling
distance_metric
batch_config
```

不同 dimension/model 不混写同一 namespace。模型升级新建 index/namespace，完成 backfill 后切 alias。

### 17.2 Embedding Cache

```text
embedding_cache_key = hash(
  chunk_content_hash,
  embedding_model_release_id
)
```

Chunk 内容相同即可复用向量，即使来自不同 Version/Document。

### 17.3 Delta Pipeline

```text
Accepted Document Version
  -> Chunk Projection
  -> Chunk Diff
  -> unchanged: reuse embedding
  -> added/changed: embed
  -> removed: tombstone in new projection
  -> all ready
  -> atomic current pointer / index alias switch
  -> async retire old projection
```

禁止“先 delete 旧 URL 全部向量，再重新 add”。若新 Embedding 失败，旧 current projection 必须继续服务。

`RECHUNK` 和 `REEMBED` 都从 Accepted Version 离线执行，不重新抓源站。

## 18. 搜索与 RAG

### 18.1 Hybrid Search

默认：

```text
BM25/full-text
  + vector search
  + metadata filters
  + optional reranker
```

过滤维度：source、domain、language、published range、tag、document status、version policy。

### 18.2 Citation Contract

Retrieval Result 必须携带：

```text
source_id
document_id
document_version_id
canonical_url
chunk_id
heading_path
chunk_content_hash
chunk_projection_release_id
embedding_model_release_id
retrieval_method
retrieval_score
```

LLM 答案引用固定 `document_version_id + chunk_id`，URL 只是展示字段。这样相同 Analysis Manifest 可以在未来重放当时使用的文本。

### 18.3 Current 与 Historical

- 普通问答默认检索 current accepted version；
- 审计/研究可指定 as-of/version；
- 删除 current projection 不删除历史 Version；
- Search/Vector 故障不影响 Source Sync。

## 19. Asset Pipeline

图片、PDF、附件独立处理：

```text
asset_url
  -> Admission/SSRF
  -> conditional fetch
  -> content hash
  -> immutable object
  -> metadata
  -> optional OCR/thumbnail
```

Markdown 中引用稳定 Asset ID/Object Ref。重复图片按 hash 去重，URL Observation 仍保留。

## 20. Web 管理功能

### 20.1 Source Intake

- 新增/批量导入 Source；
- 自动探测 Provider；
- robots/domain scope 预览；
- Golden URL 测试；
- Profile Draft/Release；
- Pause/Resume/Archive。

### 20.2 Coverage

- Provider 运行状态；
- candidate/evidence 数量；
- provider exhaustion；
- known gap/blocker；
- coverage confidence；
- 手工补充 URL/Provider。

### 20.3 Run / Task / Budget

- FULL_BACKFILL/INCREMENTAL/RECONCILE 触发；
- Stage backlog；
- lease/retry/dead-letter；
- per-source/per-origin QPS；
- request/bytes/browser/model/TTS budget；
- cancel 和 deadline。

### 20.4 Capability / Route

- HTTP/Browser/API route 成功率、Accepted 率、成本、延迟；
- 当前 Route Decision 原因；
- Profile Override；
- shadow policy 对比。

### 20.5 Extraction / Replay

- Snapshot 原文预览；
- 多 Extractor Candidate 对比；
- Canonical IR；
- Quality breakdown；
- 新 Release 离线 Replay；
- Version Diff。

### 20.6 Chunk / Embedding

- heading path 与 chunk 边界；
- 新旧 Version chunk diff；
- Chunk Policy Release；
- Embedding Model Release；
- cache reuse 命中率；
- stale/building/ready/failed；
- `RECHUNK` / `REEMBED` / `REINDEX` 操作；
- RAG citation lineage。

### 20.7 Search / RAG

- hybrid search debug；
- BM25/vector/reranker 分数；
- source/version filters；
- 具体命中的 Chunk 和引用。

所有管理动作写 Audit Log。

## 21. Context Snapshot 与外部分析数据

GSC、BigQuery、SERP、PageSpeed、Index 状态等不是正文，必须进入独立 Context Truth：

`context_snapshot`

```text
provider
request_normalized
as_of
evaluation_time
timezone
window_start/window_end
response_hash
object_ref
provider_release_id
expires_at
```

不能把外部分析字段混入 Document Version 后覆盖正文事实。

跨数据源报告创建 `analysis_manifest`，明确：

```text
ordered document_version_ids
context_snapshot_ids
evaluation_time
timezone
selection_policy_release_id
recipe_release_id
model_release_id
parameters
manifest_hash
```

重试使用同 Manifest，不重新读取新的墙钟时间或实时 Provider。

## 22. AI / Digest / TTS Derived Artifact

所有派生能力都从 Accepted Version/Manifest 输入：

```text
Summary
Tag/Topic/FAQ
Digest/Newsletter
SEO/SERP analysis
TTS
```

`derived_artifact`

```text
id
artifact_type
input_manifest_id
recipe_release_id
model/runtime_release_id
content_hash
object_ref
status
cost
created_at
```

多文档 Digest 使用 map/reduce 或层级摘要，不把全部全文一次塞入 LLM。TTS 默认消费已经冻结的 Digest/Script Artifact。

AI/TTS 失败只影响对应 Artifact，不反向标记 Document Version 失败。

## 23. Integration、Dify 与 n8n

### 23.1 稳定 Integration Contract

外部提交：

```text
POST /integration/jobs
idempotency_key
operation
authorization scope
input items / manifest
completion policy
deadline
callback
```

服务端返回 job id，随后 fan-out。外部不直接控制内部 Worker concurrency。

Completion Policy：

```text
ALL
BEST_EFFORT
MIN_SUCCESS
DEADLINE
```

轮询支持 `Retry-After`、ETag、长轮询；callback 使用 HMAC、replay window、SSRF 检查、幂等和 dead-letter。

### 23.2 Workflow 治理

导入 n8n/Dify Blueprint 时：

- 剥离 credential instance id、webhook id、pinned execution data、样例 PII、内部 hostname；
- 按真实 `type/typeVersion/parameters` 解析，不相信节点显示名称；
- 物化引擎默认值形成 Effective Config；
- 校验 dependency/model/tool/plugin/variable selector；
- `$now`、`CURRENT_DATE` 等可变时间先冻结进 Manifest；
- `continueOnFail/onError` 只允许图继续，不能把失败 Node/Item 统计为成功。

### 23.3 Egress 与 Sandbox

HTTP Request、Browser、Code、Plugin、Tool、自定义网络节点一律视为独立 egress principal：

- 源站抓取只能调用 Integration Gateway / `TARGETED_FETCH`；
- Context 数据只能通过 Context Adapter；
- 外部交付只能通过 Destination Adapter；
- Sandbox 默认无 PG/S3/Redis/Kubernetes/metadata 凭据；
- DNS rebinding、redirect 到私网、metadata IP 均阻断。

## 24. 安全、合规与抓取政策

### 24.1 SSRF

每次请求和 redirect：

1. URL parse/normalize；
2. 禁止非 http/https；
3. DNS resolve；
4. 拒绝 loopback、link-local、private、multicast、metadata、保留网段；
5. 建立连接前再校验实际 IP；
6. Browser 使用独立网络策略和 egress firewall。

### 24.2 Robots / Policy

- 读取并缓存 robots；
- Source 可配置更严格 policy；
- `ROBOTS_BLOCKED`/`POLICY_BLOCKED` 不自动升级到 Browser 绕过；
- WAF/验证码记 blocker，不做规避性绕过。

### 24.3 Secret

数据库只保存 Secret Manager 引用。Secret 不进入：日志、trace、Snapshot、前端 config、Workflow template、普通 cookie/session。

## 25. Typed Outcome 与错误分类

统一结果：

```text
SUCCESS
UNCHANGED
NOT_MODIFIED
CONNECT_TIMEOUT
READ_TIMEOUT
DEADLINE_EXCEEDED
RESPONSE_TOO_LARGE
HTTP_4XX
HTTP_5XX
RATE_LIMITED
ROBOTS_BLOCKED
POLICY_BLOCKED
SSRF_BLOCKED
WAF_CHALLENGE
JS_SHELL
EMPTY_CONTENT
SELECTOR_MISS
CONTENT_QUALITY_LOW
UNSUPPORTED_TYPE
BROWSER_CRASH
PARSER_ERROR
STORAGE_ERROR
SEARCH_ERROR
EMBEDDING_ERROR
MODEL_ERROR
TTS_ERROR
CONTEXT_PROVIDER_ERROR
CONTEXT_QUOTA_EXHAUSTED
INVALID_MANIFEST
PRIORITY_CONTRACT_VIOLATION
BUDGET_EXHAUSTED
RESERVATION_CONFLICT
DISPATCH_OVERRUN
WORKFLOW_PREFLIGHT_FAILED
WORKFLOW_SANDBOX_VIOLATION
WORKFLOW_EGRESS_DENIED
INTEGRATION_UNAUTHORIZED
CALLBACK_ERROR
DELIVERY_ERROR
CANCELLED
EXPIRED
```

只有适合升级的错误才进入下一 route：例如 `JS_SHELL` 可 HTTP → Browser；`ROBOTS_BLOCKED/POLICY_BLOCKED` 不升级；`RATE_LIMITED` 延迟重试；Search/Embedding/AI/TTS/Workflow/Delivery 错误不反向污染已有 Accepted Version。

## 26. 可观测性与 SLO

指标按 Source/Provider/Route/Stage/Release 分桶：

- candidate discovered/sec；
- fetch dispatch/success/accepted；
- 429/5xx/blocker；
- response bytes；
- queue lag / lease expiry；
- Browser ratio / browser seconds；
- extraction quality；
- unchanged ratio；
- Version creation rate；
- chunk count/diff；
- embedding cache hit；
- projection build latency；
- search latency/recall benchmark；
- cost per source / accepted document；
- coverage gap；
- integration/workflow/delivery success。

关键 SLO：

- 新文章发现延迟；
- 增量同步完成延迟；
- FULL_BACKFILL Provider 可解释完成率；
- Source isolation；
- RAG current projection freshness；
- callback/delivery deadline。

## 27. 数据一致性与幂等

幂等键至少覆盖：

- Run Command；
- Task dispatch；
- Fetch Observation；
- Snapshot content hash；
- Document Version semantic hash；
- Markdown Projection；
- Chunk Projection；
- Embedding cache；
- Integration Job；
- Derived Artifact；
- Delivery Job。

任何消息重复投递都不能产生重复逻辑 Version 或重复外部副作用。

Current Pointer 只在新 Projection 完整 ready 后切换；不允许半成品 Search/Vector 覆盖当前可服务版本。

## 28. 核心算法

### 28.1 FULL_BACKFILL

```text
Create Run
  -> enumerate Coverage Providers
  -> persist Candidate + Evidence
  -> Priority Hint
  -> Budget Admission
  -> Fetch
  -> Snapshot
  -> Extract candidates
  -> Quality
  -> Identity
  -> Accepted Version
  -> Markdown / Asset / Chunk
  -> Embedding / Search
  -> Provider exhaustion report
  -> Coverage report
```

### 28.2 INCREMENTAL

```text
Read Change Signals
  -> overlap window
  -> candidate upsert
  -> conditional fetch
  -> unchanged => Freshness Observation
  -> changed => new Accepted Version
  -> Chunk Diff
  -> Embedding Delta
  -> Search/Vector pointer switch
```

### 28.3 RECONCILE

周期重新运行关键 Coverage Provider，发现：

- Feed 漏项；
- Sitemap 延迟更新；
- 旧文章补发；
- canonical/redirect 变化；
- 已删除页面；
- 站点结构变化。

### 28.4 REPROCESS / RECHUNK / REEMBED

```text
REPROCESS: Snapshot/Accepted Version -> 新 Extractor/Markdown/Quality Projection
RECHUNK: Accepted Version -> 新 Chunk Policy
REEMBED: Chunk Projection -> 新 Embedding Model
REINDEX: Accepted Projection -> Search/Vector index rebuild
```

全部不访问源站，除非显式创建新的 Fetch Run。

## 29. 测试、Replay 与发布门禁

1. **Adapter Contract Test**：Crawl4AI/HTTP/Browser/Provider 输入输出 schema、error、timeout、字段兼容；特别验证 Crawl4AI `result.markdown` 等版本变化。
2. **Golden Extraction Test**：静态博客、SPA、WordPress、Ghost、Substack、Docs、代码、表格、图片、PDF、多语言。
3. **Snapshot Replay Test**：新 Extractor/PostProcess/Markdown Release 对旧 Snapshot 批量回放。
4. **Coverage Test**：已知小站点验证 Sitemap/Feed/API/Archive 枚举边界和 gap。
5. **Priority Contract Test**：不同 Priority Release 不能改变 FULL_BACKFILL Candidate 集，只改变顺序。
6. **Metadata Probe Test**：联网 scorer 必须有 Task、Budget、cache、SSRF 和 audit。
7. **Budget Concurrency Test**：并发/retry/cancel 下 dispatch 不越 reservation。
8. **Idempotency Test**：重复 Command/message/callback 不重复创建 Version/Artifact/Delivery。
9. **Crash Recovery Test**：Worker 在 fetch/extract/publish/chunk/embed 各阶段退出后可恢复。
10. **Projection Determinism Test**：同 IR + 同 Markdown/Chunk Release 产生相同 hash。
11. **Chunk Stability Test**：同 Version 重跑 chunk_id/heading_path/ordinal 完全一致。
12. **Embedding Delta Test**：unchanged chunk 不重新调用 embedding；新增/变化 chunk 才调用。
13. **Projection Switch Test**：新向量构建失败时旧 current index 仍可查询。
14. **RAG Citation Replay Test**：同 Manifest 能重新定位相同 document_version_id/chunk_id。
15. **PSL Domain Scope Test**：`co.uk`、IDN、多级子域、跨 origin redirect。
16. **Workflow Preflight/Sandbox/Egress Test**：内部 host、metadata、缺依赖、动态时间、silent failure 必须被发现或阻断。
17. **Load Test**：按真实站点分布模拟 1000 Source，验证 fairness、PG/S3/Redis、Browser、Embedding 和 Context quota。

发布：shadow → 少量 canary source → 分批 source → 全量；Quality/Coverage/Budget/Projection/Integration Contract 回归时自动停止扩大范围。

## 30. 部署与横向扩展

推荐 Kubernetes/等价编排，Worker 独立 Deployment：

- API/Web；
- Scheduler/Dispatcher；
- Outbox Relay；
- Discovery Worker；
- Metadata Probe Worker；
- HTTP Fetch Worker；
- Browser/Crawl4AI Worker；
- Extraction/Quality Worker；
- Asset Worker；
- Markdown/Chunk Worker；
- Search/Embedding Worker；
- Context Adapter Worker；
- AI/TTS Worker；
- Workflow Preflight/Sandbox Worker；
- Integration/Callback Worker；
- Delivery Worker。

Browser、OCR、Embedding GPU、LLM/TTS、Workflow Sandbox 不与轻量 HTTP Worker 混部。

PostgreSQL 大表按实际查询模式进行时间/source/hash 分区；S3 使用 content-addressed immutable key；Search/Vector 可删除重建。

## 31. 容量与成本

主要成本不是“Python 能开多少协程”，而是：

- 源站礼貌限流；
- Browser 比例；
- 历史 URL 数量；
- Snapshot/Asset bytes；
- Chunk 数；
- Embedding/LLM/TTS；
- Reconcile 频率。

降本顺序：

1. Provider 直接枚举优先于盲目深爬；
2. Change Signal 先行；
3. HTTP-first；
4. Prefetch/Metadata Probe 与完整正文抓取分离；
5. Snapshot replay 避免重抓；
6. Canonical IR hash 早判断 unchanged；
7. Chunk diff；
8. Embedding cache；
9. Search/Vector delta；
10. Derived Artifact cache；
11. Browser/GPU/TTS 独立预算；
12. S3 lifecycle 热冷分层。

任何文章中给出的单机页/秒、向量查询毫秒等示例数字都不直接作为容量承诺，必须使用真实 1000 Source 分布做 benchmark。

## 32. 数据生命周期

- Snapshot、Document Version、Manifest、Audit 按合规要求长期保留；
- 大 screenshot/MHTML/debug payload 可转冷存或按 policy 到期；
- Context Snapshot 按 Provider 许可设置 retention；
- Search/Embedding/cache 可重建，不是归档唯一副本；
- Workflow 原始模板、净化报告、Preflight Report 按合规保留；
- 删除/合规请求必须传播到 Markdown/Search/Vector/Derived/Delivery Projection，并保留受限删除审计。

## 33. 分阶段实施

### Phase 1：历史抓取主链路

Source/Profile、PSL scope、Sitemap/Feed/Archive Provider、Candidate/Evidence、PostgreSQL + Redis Streams + Outbox、HTTP Fetch、Snapshot、Trafilatura/Readability/CSS、Canonical IR、Quality、Document/Version、Markdown、基础 Web。

### Phase 2：1000 站点扩展与增量

Change-Signal、Conditional GET、Budget Reservation、per-origin fairness、Capability/Route、Browser/Crawl4AI Adapter、Reconcile/Coverage、Metadata Probe、Profile Studio、Golden Replay、Drift/告警。

### Phase 3：Chunk、Embedding 与 RAG

结构化 Chunk Projection、稳定 Chunk ID、Chunk Diff、Embedding Model Registry、Embedding Cache、Delta Projection、OpenSearch/Vector、Hybrid Search、RAG Citation、Chunk/Embedding Web 页面。

### Phase 4：Context 与 Derived Artifact

Context Provider Adapter、Context Snapshot、Selection/Input/Analysis Manifest、evaluation_time/timezone freeze、Summary/Tag/Topic/FAQ、Digest、SEO/SERP Analysis、AI/TTS Recipe/Model Registry。

### Phase 5：Integration / Workflow / Delivery

Integration Job、server-side fan-out/barrier、polling/callback、Workflow Blueprint Sanitizer、Effective Config、Preflight、Sandbox、Egress、n8n/Dify Adapter、Destination Adapter、Integration Web Console。

### Phase 6：高级治理

多租户/RBAC、自动 drift 建议、模型评估、完整成本治理、数据生命周期、灾备和跨区域对象存储策略。

## 34. 验收标准

### 34.1 历史完整性

- 每个 Source 可查看 Provider Coverage；
- 已知小站点可以证明候选枚举边界；
- FULL_BACKFILL 报告包含 exhaustion/gap/blocker；
- Priority/Best-First/SEO score 不改变 Coverage Candidate 集。

### 34.2 增量

- 新文章在目标 SLA 内被发现；
- unchanged 不创建新 Document Version；
- unchanged 不重新 Chunk/Embedding/AI；
- Feed/API/Sitemap 使用 overlap window；
- 周期 Reconcile 能发现增量通道漏项。

### 34.3 数据质量

- Golden 集 title/body/code/table/image 结构达到上线阈值；
- 同 Snapshot/Version + 同 Release 重放 hash 稳定；
- 每个 Markdown 可追溯到 Version/IR/Snapshot；
- 低质量页面不会因 HTTP 200 静默成功。

### 34.4 Chunk / Embedding / RAG

- 同 Version + 同 Chunk Release 的 chunk_id、边界、heading_path 稳定；
- 新 Version 只有 added/changed Chunk 触发 embedding；
- Embedding Model 升级不重新抓源站；
- 新 Projection 构建失败不会删除旧 current projection；
- 每个 RAG 引用可定位 document_version_id + chunk_id + heading_path；
- Vector DB 删除后可从事实层完整重建。

### 34.5 可靠性

- Worker crash 后任务恢复；
- 重复消息不重复发布 Version；
- 并发 dispatch 不突破 Budget Reservation；
- 单 Source 故障不阻塞其他 Source；
- Browser/Search/Embedding/AI/TTS 故障隔离。

### 34.6 Web 管理

- 新增 Source 不修改核心代码即可接入常见站点；
- 可暂停、重跑、Reconcile、Reprocess、Rechunk、Reembed；
- 可查看 Coverage、Route、Budget、Snapshot、Extraction、Version Diff、Chunk Diff、Embedding 状态、RAG Citation 和 Audit。

### 34.7 Integration / Workflow

- Dify/n8n 不知道内部 crawler hostname/task id；
- 同 idempotency key 不创建重复 Job；
- 大 batch 由平台 fan-out，遵守 global/per-host budget；
- callback 有签名、重试、幂等和 dead-letter；
- Workflow Template 不携带 credential id/webhook id/pinned PII；
- 所有网络节点统一 egress；
- 动态时间先冻结，跨日重试不漂移；
- `continueOnFail` 不把失败统计成成功。

## 35. 最终架构决策

这个系统不是“一台 Crawl4AI 把 1000 个站点爬下来”，也不是“每次 RAG 查询时临时抓网页”，而是长期运行的数据同步、版本和派生平台。

最终坚持以下边界：

- Coverage Provider 决定历史全集的证据；
- Candidate/Evidence 保存观察事实；
- Priority 只改变处理顺序；
- Capability 只决定 route；
- 所有副作用先做持久 Budget Reservation；
- Snapshot 与 Accepted Document Version 是正文事实；
- Markdown、Chunk、Search、Embedding 是可重建 Projection；
- Chunk 使用稳定结构身份，Embedding 只对变化内容增量计算；
- 新 Search/Vector Projection ready 后才切 current pointer；
- RAG Citation 固定到 Document Version 和 Chunk，而不是只引用可变 URL；
- Crawl4AI/Playwright/向量库/LLM 都通过版本化 Adapter/Release 隔离，可以替换而不污染业务真相；
- Context Snapshot 与 Document Version 分离；
- 批量分析先冻结 Manifest 和 evaluation_time/timezone；
- Dify/n8n/Agent 是受控消费者与编排器，不是抓取调度器、正文真相源或任意出网代理；
- Web 管理展示每个重要决策的 evidence、release、outcome、cost 和 lineage；
- 所有配置、规则、模型、Crawler Runtime、Chunk Policy、Embedding Model、Workflow 都必须可版本化、可审计、可重放、可回滚。
