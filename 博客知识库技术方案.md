# 博客知识库技术方案

适用范围：从约 1000 个技术博客、工程博客、技术文档站、Newsletter、CMS 与公开文档站抓取尽可能完整的历史文章，清洗为稳定 Markdown 建设长期知识库；后续持续增加网站，并支持全量回灌、增量同步、Web 管理、版本留存、离线重处理、全文/向量检索、质量审计、图片附件归档，以及 Dify/n8n/Agent/SEO 分析/Newsletter/TTS 等下游工作流消费。整体按百万级文档、数百万到千万级 URL、站点高度异构和多年持续运行设计。

## 1. 建设目标

1. 支持站点根地址、单 URL、批量 URL、RSS/Atom/JSON Feed、Sitemap/Sitemap Index、OPML、CSV、JSON、Markdown/TXT 等来源导入。
2. 自动探测 Sitemap、Feed、公开 API、Archive、Category/Tag/Author、Docs Navigation、普通内链、`llms.txt`/`llms-full.txt`、平台 API 等发现入口。
3. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`PROBE`、`METADATA_PROBE`、`FOCUSED_DISCOVERY`、`REPROCESS`、`ASSET_REPAIR`、`REINDEX`、`DERIVED_BUILD`、`CONTEXT_REFRESH`、`DELIVERY_RETRY` 等运行类型。
4. 历史完整性由 Provider 级 Coverage Evidence 证明，不能以“队列空了”“达到 max_depth/max_pages/max_urls”代替完整性证明。
5. HTTP-first；只有动态渲染、正文缺失、挑战页、选择器失效或质量失败有证据时才升级 Browser/Crawl4AI/Playwright。
6. 支持站点能力学习，用正文接受率、质量、延迟、成本和 blocker 优化抓取 route，但 Capability 不替代 Coverage。
7. Source/Profile/Discovery/Priority/Budget/Fetch/Extractor/PostProcess/Quality/Identity/Asset/Projection/Search/Embedding/Context/AI/TTS/Workflow/Delivery 全部采用不可变 Release。
8. 使用 Sitemap `lastmod`、Feed GUID/updated、API cursor、ETag、Last-Modified、body hash、Canonical IR hash、重叠窗口与周期 Reconcile 驱动增量同步。
9. 最终知识对象保存标题、作者、发布时间、更新时间、标签、canonical、正文块、代码块、表格、图片、附件、链接关系、Snapshot、字段 provenance 和完整 lineage。
10. 支持 durable frontier、断点续跑、幂等、lease、heartbeat、重试、限流、熔断、公平调度、backpressure 和 dead-letter。
11. 保存不可变 Network Snapshot、Rendered DOM、Extraction Candidate、Canonical IR、Markdown Projection 和质量结果；规则升级优先离线 replay，不重复访问源站。
12. Web 管理端覆盖 Source Intake、Site Profile、Provider Coverage、Focused Discovery、Capability & Route、Run/Task、Budget、Targeted Fetch、Extraction Preview、Snapshot Replay、Version Diff、Quality/Drift、Assets、Search/RAG、Context Snapshot、Analysis Manifest、AI Artifact、Integration、Workflow/Delivery、成本和审计。
13. Markdown、全文索引、Embedding、摘要、标签、Topic、FAQ、Digest、Newsletter、SEO/内容分析报告、TTS 等均是 Accepted Document Version 或显式 Context Snapshot 的异步 Projection/Derived Artifact，失败不能阻塞 Source Sync。
14. append-only 历史真相层之上提供可重建 Current Projection。
15. Canonical IR hash 在 Chunk/Embedding/AI/TTS 等昂贵下游之前计算；正文未变化只写 Freshness Observation。
16. Change-Signal Plane 优先通过 Feed/Sitemap/API 元数据判断是否可能变化，再决定是否抓正文。
17. 日报/Newsletter/批量问答/SEO 报告必须先冻结 Selection/Input/Analysis Manifest，再执行摘要、RAG、TTS 或外部 Workflow，保证补跑和重放结果可解释。
18. Dify/n8n 的 Code/Plugin/Tool 节点视为独立不可信执行边界；任意 URL 抓取必须通过 Integration Gateway/`TARGETED_FETCH`，不得直接访问内部 crawler。
19. Discovery 中的主题相关度、URL pattern、SEO/head metadata、链接位置和人工主题只生成 Priority Hint；在 `FULL_BACKFILL`/`RECONCILE` 中不得因低分而删除合法 Coverage Candidate。
20. 候选页 `<head>`/有限字节探测必须建模为显式 `METADATA_PROBE` 任务，受缓存、QPS、字节、deadline 和总预算约束。
21. 第三方 crawler/scorer/priority queue 的原生 `score`/`priority` 不能直接成为业务调度优先级；必须通过版本化 Priority Adapter 归一化并做实际 runtime 顺序契约测试。
22. Web 控件、API Command 与持久 Run Type 必须强类型一一映射；保存 requested/effective run type/config 和解析 provenance，禁止隐藏字符串布尔值或前端默认值静默改变运行模式。
23. 所有网络、模型、TTS、外部交付副作用在 **dispatch 前** 强制预留预算；候选数、派发数、成功数、Accepted 数、字节、时间和成本预算分开建模。
24. 外部 Dify/n8n/Agent 只能通过版本化 Integration Job Contract 使用抓取和派生能力；支持幂等提交、不可变 Input Manifest、服务端 fan-out/barrier、`Retry-After`/长轮询、签名 callback 和 Artifact Ref，禁止依赖 Crawl4AI `task_id`、内部 hostname、内部 status/result shape。
25. Google Search Console、BigQuery、Serper/SerpAPI、PageSpeed、Index 状态等非文章数据必须进入独立 Context Snapshot/Analytics Artifact，不得混入 Document Version 或 Coverage Truth。
26. 多数据源分析必须冻结异构 Typed Analysis Input Manifest，明确“哪一版文章 + 哪一批外部数据 + 哪些参数 + 哪个 Recipe/Model”共同产生结果。
27. Workflow 中所有具备网络能力的节点（HTTP Request、Browser、Code、Plugin、Tool、自定义节点）都视为独立 egress principal；用户派生 URL、源站 URL 和动态 URL 必须经 Integration Gateway、Context Adapter 或预注册 Destination Adapter，不能因节点类型“看起来安全”而绕过 Admission、SSRF、预算和审计。
28. Workflow 的时间语义必须确定化：`evaluation_time`、timezone、窗口边界和 `as_of` 必须在 Manifest/Job 创建时冻结；`$now`、`CURRENT_DATE`、随机数或其他运行时隐式变量若会影响结果，必须先物化为显式输入，重试不得悄悄读取新的墙钟时间。
29. 导入的 n8n/Dify Blueprint 必须经过模板净化和有效参数审计：节点显示名称不作为语义，实际 `type/typeVersion/parameters` 与引擎默认值才是 Contract；credential instance id、webhook id、pinned execution data、示例 PII、内部 hostname 必须剥离；`continueOnFail/onError` 等失败策略必须映射为显式 Typed Outcome，不能把失败静默包装成成功。

## 2. 非目标与边界

- 不绕过登录、付费墙、验证码、WAF 或明确访问控制。
- 不承诺未知站点仅靠自动探测达到 100% 历史完整；必须展示 coverage confidence、known gap 与 blocker。
- 不用 LLM、Reranker、SEO 分数、URL 关键词、SERP 排名或抓取成功率决定 FULL_BACKFILL 中合法历史文章是否存在。
- Markdown 不是唯一真相；Canonical IR 是内部内容真相，Markdown 是确定性投影。
- 不锁死单一爬虫库、浏览器库、向量库、搜索引擎、消息队列、工作流引擎、SERP/GSC Provider 或模型供应商。
- HTTP 200、Browser success、协程正常返回不等于业务内容成功。
- 不把全局 reload/scroll/wait 脚本作为所有站点默认策略；浏览器动作必须进入 Site Profile Recipe。
- 不把多个 Document Version 的全文拼成一个无结构大字符串作为默认 AI/TTS 输入。
- 不在 Web/Dify/n8n 请求线程同步等待长期 Browser、LLM、OCR 或 TTS 全链路完成。
- 不允许 Workflow Code/Plugin 节点拥有不受控的互联网/内网访问、数据库/对象存储真相层凭据或云 metadata 权限。
- 不相信第三方 API 名称暗示的排序方向；`BestFirst`、`score`、`priority` 等必须通过运行时契约验证。
- 不在父页面链接已经可观察后，因为剩余抓取预算而按 DOM 顺序提前丢弃未评分候选。
- 不把模型/API Secret 放进客户端 cookie/session、普通日志、trace 或前端可回显配置。
- 不把“只返回 N 个成功结果”解释成“最多发出 N 个源站请求”；并发 batch、prefetch、redirect、retry 都纳入 dispatch budget。
- 不让 n8n/Dify 的 Loop/Batch 节点成为 1000 站点全局抓取调度器。
- 不把 Google Sheets/Drive/Notion 等协作工具作为主内容真相源或唯一运行状态库。
- 不把 SERP Top N、搜索结果 Top K 或人工竞品 URL 当成历史 Coverage 证明；它们属于业务分析样本。
- 不用 URL slug、Google Drive 文件夹名或 Sheet 名替代稳定 Document Identity、Version ID 或幂等键。
- 不允许 n8n `HTTP Request`、Browser、Code、Plugin、Tool 或自定义网络节点直接访问任意用户派生 URL；“不是 Code 节点”不代表可以绕过 egress policy。
- 不把节点名称（例如名为 `5_sec` 的 Wait）当成真实执行语义；必须读取有效参数和目标引擎默认值。
- 不允许 `continueOnFail/onError=continue` 把抓取、Context、Delivery 或解析失败计入成功结果；失败必须保留 Typed Outcome 与原因。
- 不允许依赖 `$now`、`CURRENT_DATE` 等隐式当前时间产生可重放结果；可变时间必须先进入 Manifest。
- 不把模板里的 credential id、webhook id、pinned data、样例个人数据或内部地址直接带入生产 Blueprint。

## 3. 核心原则

1. **PostgreSQL 是业务状态真相源；S3/MinIO 是不可变大对象真相源。**
2. Redis Streams 只做任务运输；决定性状态先写 PostgreSQL，再由 Transactional Outbox 投递。
3. Source Intake、Change Signal、Discovery、Admission、Priority、Budget Admission、Route Decision、Fetch、Extraction、PostProcess、Quality、Identity、Publish、Asset、Projection、Search、Context、AI、TTS、Integration、Delivery 分层。
4. Discovery 只产生 URL Candidate 与 Evidence；列表页、Feed 摘要、搜索结果不能直接成为最终正文。
5. URL Identity、Document Identity、Content Version、Freshness 与 Business Correlation Key 分离。
6. Discovery Fetch Route 与 Article Fetch Route 分离，能力统计按 operation type 单独维护。
7. HTTP、Browser、PDF/OCR、Platform API、Fast Path 通过 Adapter 隔离，第三方原始返回 shape 不泄漏到业务真相层。
8. 所有 Stage 使用固定 Typed Outcome Contract。
9. `document_version` append-only；Current/Markdown/Search/Embedding/AI/TTS/Delivery Projection 可重建。
10. `max_depth/max_pages/max_runtime/max_urls/top_k` 是预算或停止条件，不是 Coverage Evidence。
11. 进程内 semaphore/`asyncio.gather` 只做 Worker 本地资源保护，不承担持久调度或全局预算真相。
12. Config 使用不可变 Release；Secret 只保存 Secret Manager 引用。
13. 代码缩进、表格、列表层级、引用、公式和换行都是内容数据，不能在通用 cleaner 中被压平。
14. 多个 Extractor/Strategy 和 Link Discovery 优先消费同一 Snapshot；网络访问与内容解释解耦。
15. Coverage Candidate 与 Priority Hint 是两个事实：前者决定“应该枚举与审计哪些 URL”，后者只决定“预算内先处理谁”。
16. Discovery Filter 必须声明 `LOCAL_PREDICATE` 或 `NETWORK_PROBE`；会联网的 Filter/Scorer 转成持久 Probe Task。
17. 第三方 `raw_score` 与平台 `normalized_priority` 分离；平台统一定义 **normalized_priority 越大越先处理**，Scheduler Adapter 再转换到底层 queue key。
18. 已抓父页面产生的 observable links 先形成 Candidate/Evidence，再做评分和 fetch budget admission。
19. UI 请求参数、Command、Run 的 requested/effective config 都是可审计事实。
20. **预算以副作用边界为准。** 每次真实访问源站、模型、TTS、外部 Destination 或外部分析 Provider 前必须取得持久 Budget Reservation。
21. 外部工作流只表达业务意图、输入集合、完成策略和所需 Artifact，不拥有内部抓取调度权；raw priority、循环并发、超时均由平台 Policy 归一化。
22. 外部接口稳定性由 Integration API Release 保证；内部 crawler、queue、Browser Runtime 可以替换而不要求所有 n8n/Dify 工作流同步修改。
23. 外部分析数据是独立 Context Truth：它可以参与 Derived Artifact，但不能改变 Document Source Truth。
24. 所有跨数据源分析先冻结 Typed Manifest；“实时重新查询后继续跑”必须被视为新 Manifest，而不是同一次重试。
25. Workflow 的安全边界按“是否产生副作用”而不是按“节点名称/类型标签”划分；所有网络能力节点统一经过 egress admission、Destination policy、预算与审计。
26. Workflow 的可重放输入必须包含时间：同一 Manifest 的 `evaluation_time/timezone/as_of` 不变，同一重试不得因为墙钟推进改变 BigQuery/GSC 窗口、文件名、查询范围或模型上下文。
27. Workflow 的有效语义来自导出 DSL 中的真实 `type/typeVersion/parameters` 加目标引擎默认值，不来自节点显示名称；Preflight 必须把 effective parameters 物化并存证。
28. Workflow 的“继续执行”与“成功”分离；任何 `continueOnFail/onError` 只决定图是否继续，不改变失败事实，Node/Item/Job 三层 Outcome 都必须可追踪。
29. Source/Host identity 使用标准化 host 与 Public Suffix List 计算 `registrable_domain`；eTLD+1、origin、subdomain scope 分离，不能用简单 `split('.')` 猜业务域名。

## 4. 规模假设与容量边界

初始按以下量级设计，不把估算写死成限制：

- Source：1000 个起步，可扩展到数千。
- 文档：百万级 Accepted Document；历史版本可达数百万。
- URL Candidate/Evidence：数百万到千万级。
- 日常增量：几千到数十万 URL/日弹性变化。
- Snapshot/DOM/附件/Context Dataset：主要容量进入对象存储；PG 只放索引、状态、hash、引用和必要 metadata。
- Browser 是昂贵稀缺资源，HTTP Worker 与 Browser Worker 独立池化和配额。
- Embedding/LLM/TTS/SEO 分析是异步派生资源，不能反压 Source Sync 主链路。
- 外部 Context Provider 可能有严格按日/按月调用额度，必须独立做 Provider Budget 和缓存。

容量扩展首先通过 Worker 横向扩容、队列分片、对象存储扩容和 PostgreSQL 分区实现；只有实际指标证明单库/单索引成为瓶颈时再拆服务或迁移专用向量库。

## 5. 推荐技术栈

### 5.1 控制面

- Web：Next.js + TypeScript。
- API：FastAPI + Pydantic，Command/API 使用 Enum、Discriminated Union 和显式版本。
- PostgreSQL：Source、Profile、Provider、Candidate、Evidence、Run、Task、Budget、Reservation、Document、Version、Coverage、Capability、RouteDecision、PriorityEvidence、Manifest、ContextSnapshot、IntegrationJob、DerivedArtifact、WorkflowExecution、DeliveryJob、Audit。
- Redis Streams：任务运输和 Consumer Group。
- Transactional Outbox：保证数据库状态与消息投递一致。
- Redis/Valkey：robots、Capability Projection、短期 probe、UI 热数据和非关键缓存，不作为唯一真相源。
- Secret Manager：Vault、云 Secret Manager 或 Kubernetes Secret 引用。
- Integration Gateway：面向 Dify/n8n/Agent/Webhook 提供稳定 Integration Job/Artifact/Search/Context API。
- Workflow Preflight：DSL Parser/Linter + target-engine import test + dependency resolver + effective-default materializer + template sanitizer。
- Workflow Sandbox：Kubernetes Job/隔离 Worker + seccomp/AppArmor/NetworkPolicy/egress proxy。
- Public Suffix List Resolver：标准化 `registrable_domain`、subdomain scope、origin grouping，并定期版本化更新。

### 5.2 数据面

- HTTP：`httpx.AsyncClient` 长生命周期连接池。
- HTML：selectolax/lxml/BeautifulSoup。
- 主体抽取：Trafilatura/Readability/Crawl4AI/CSS/XPath/JSON-LD/OpenGraph 组合 Adapter。
- Browser：Playwright/Crawl4AI Browser Runtime Pool。
- PDF：PyMuPDF/pypdf + OCR fallback。
- 对象存储：S3/MinIO。
- 全文检索：OpenSearch。
- 向量：pgvector 起步，规模扩大后可替换专用 Vector Store。
- Context Adapter：GSC API/BigQuery/Serper/SerpAPI/PageSpeed/Index Provider 等统一适配。
- AI/TTS：独立 Adapter + Worker Pool。
- 可观测：OpenTelemetry + Prometheus/Grafana + 日志聚合。

## 6. 总体架构

```text
Source Registry
  -> Coverage Providers
  -> Durable URL Candidate / Evidence
  -> Change-Signal Plane
  -> Admission & Security
  -> Metadata Probe / Priority Hint(optional)
  -> Priority Adapter
  -> Budget Admission / Dispatch Reservation
  -> Durable Frontier
  -> Route Decision
  -> HTTP / Browser / Platform API / PDF
  -> Immutable Snapshot
  -> Multi Extractor Candidates
  -> Canonical IR + Quality
  -> Identity + Append-only Document Version
  -> Current Projection
  -> Markdown / Assets / Search / Embedding

External Analytics Providers
  -> Context Adapter
  -> Context Budget / Snapshot
  -> Context Snapshot / Analytics Artifact

Document Version + Context Snapshot
  -> Selection/Input/Analysis Manifest
  -> Derived Job / AI / TTS
  -> Integration Job / Workflow Execution
  -> Delivery Job
  -> Dify / n8n / Agent / Sheets / Drive / Newsletter / Webhook

Workflow Blueprint
  -> Template Sanitizer
  -> Effective Parameter Materialization
  -> Time/Input Freeze
  -> Egress/Secret/Error Policy Preflight
  -> Sandbox Runtime
  -> Integration Gateway / Destination Adapter

Capability Observation
  -> Route Capability Projection
  -> Route Decision
```

控制面创建和管理持久事实，不直接执行长期 I/O/GPU 任务。第三方 crawler 内部 frontier、batch、prefetch 只能作为单 Task/Worker 的实现细节，不能成为平台唯一 Candidate、Coverage、Priority 或 Budget 真相。工作流引擎的 HTTP Request、Browser、Code、Plugin、Tool 也只能作为受治理的业务编排节点，不能成为绕过平台抓取和 egress 控制的第二数据面。

## 7. Release Registry 与可重复运行

所有会改变结果的行为都必须版本化：

```text
command_schema_release
config_resolver_release
site_profile_release
domain_scope_policy_release
discovery_provider_release
discovery_priority_policy_release
priority_adapter_release
budget_policy_release
metadata_probe_policy_release
fetch_engine_release
representation_provider_release
route_policy_release
extractor_release
postprocess_release
quality_policy_release
identity_policy_release
asset_policy_release
markdown_projection_release
chunker_release
search_provider_release
reranker_release
embedding_release
context_provider_release
context_schema_release
context_refresh_policy_release
selection_policy_release
analysis_manifest_schema_release
ai_model_release
ai_recipe_release
tts_model_release
tts_recipe_release
workflow_recipe_release
workflow_blueprint_release
workflow_template_sanitizer_release
workflow_time_policy_release
workflow_sandbox_policy_release
integration_api_release
integration_callback_policy_release
delivery_recipe_release
destination_adapter_release
```

每个 Run、Task、Snapshot、Version、Context Snapshot、Manifest、Artifact、Integration Job 都记录实际使用 Release Ref。Release 只追加不原地修改；升级走 draft → test/replay → shadow/canary → publish → rollback。

每个 Release 至少记录 source commit、config hash、dependency lock hash、container image digest、SBOM、作者/时间、兼容 Contract、Golden/shadow 测试和 rollback target。

## 8. 核心数据模型

### 8.1 Source / Profile / Provider

`source` 保存站点身份和归属；`site_profile_release` 保存 normalized host、`registrable_domain`、origin/subdomain scope、canonical 规则、允许路径、排除路径、Provider 配置、Selector、Browser Recipe、限流、语言、默认 route 等。`registrable_domain` 必须由版本化 Public Suffix List 解析，不能通过字符串截断推断。

`coverage_provider` 表示独立历史枚举来源：Sitemap、Feed、CMS API、Archive/Category/Tag/Author、Docs Navigation、`llms.txt`、普通内部链接、人工 seed/import。

Provider 记录枚举边界、cursor、oldest/newest、expected/discovered、gap、blocker、confidence 和 evidence。

### 8.2 Candidate / Evidence

```text
url_candidate
- id / source_id
- normalized_url
- url_fingerprint
- origin / registrable_domain
- first_seen_at / last_seen_at
- current_admission_state

discovery_evidence
- candidate_id
- provider_id
- parent_candidate_id(optional)
- observed_at
- depth / anchor / rel / source_position
- provider_payload_ref
- evidence_hash
```

同一 URL 可有多个 Evidence，不能因为任务层去重而丢失“从哪里被发现”。

### 8.3 Snapshot / IR / Document / Version

```text
network_snapshot
- request_url / final_url / redirect_chain
- request/response metadata
- headers/status/mime
- body_object_key / body_hash
- rendered_dom_ref(optional)
- fetch_engine_release
- runtime_attestation

canonical_ir
- structured blocks
- metadata + provenance
- canonical_ir_hash
- extractor/postprocess releases

document
- stable logical identity
- source/canonical identity

document_version
- document_id
- canonical_ir_ref/hash
- observed_at / accepted_at
- quality_outcome
- predecessor_version_id
- append-only
```

Current Projection 只指向当前 Accepted Version，可随时从版本历史重建。

### 8.4 Run / Task / Reservation

Run 保存 requested/effective config、Release、预算、状态、统计和 lineage。Task 使用 lease/heartbeat；Worker 崩溃后任务可安全重新领取。

```text
budget_reservation
- id
- run_id / task_id / integration_job_id / context_job_id
- dimension: REQUEST|BYTE|TIME|COST|BROWSER_SLOT|MODEL_TOKEN|CONTEXT_CALL|DELIVERY
- amount
- status: RESERVED|DISPATCHED|SETTLED|RELEASED|EXPIRED
- idempotency_key
- lease_until
- actual_usage
```

### 8.5 Context Snapshot

```text
context_snapshot
- id
- type: GSC_PERFORMANCE|BIGQUERY_RESULT|SERP_RESULT|PAGE_SPEED|INDEX_STATUS|ANALYTICS
- provider_id / provider_release_id
- request_hash / query_hash
- window_start / window_end / as_of
- country / device / language(optional)
- schema_release_id
- object_ref / content_hash
- observed_at
- retention_class
- provenance
```

Context Snapshot 是不可变外部观察事实，不是 Document Version，也不生成 Coverage 结论。

### 8.6 Typed Manifest

```text
analysis_input_manifest
- id
- purpose
- ordered_typed_refs[]
- selection/query evidence
- caller_item_key(optional)
- business_object_key(optional)
- evaluation_time
- timezone
- requested/effective parameters
- schema_release_id
- manifest_hash
- created_at
```

允许的 typed ref 包括：

```text
DOCUMENT_VERSION_REF
MARKDOWN_PROJECTION_REF
CONTEXT_SNAPSHOT_REF
SEARCH_RESULT_SNAPSHOT_REF
SELECTION_MANIFEST_REF
DERIVED_ARTIFACT_REF
```

### 8.7 外部消费者与业务关联键

```text
external_consumer
- id / tenant_id / name
- auth_client_ref
- scopes
- quota_policy_release
- allowed_operations
- allowed_destination_ids
- enabled

integration_job_item
- caller_item_key
- business_object_key(optional)
- normalized_url or input_ref
- dedupe_group_key(optional)
- status / typed outcome
- document_version_ref
- artifact_refs
- error_class
```

`caller_item_key/business_object_key/display_slug` 只用于调用方映射和展示，不能替代 Document Identity。多个 caller item（例如 mobile/desktop SERP 同一 URL）可以映射到同一个内部逻辑抓取项，但各自 rank/device/query evidence 必须保留。

## 9. Source Intake 与站点 Onboarding

新增 Source 时执行：

```text
Normalize Host + PSL registrable domain + origin scope
 -> robots/policy check
 -> DNS/IP/SSRF admission
 -> HEAD/GET lightweight probe
 -> detect Feed/Sitemap/API/llms.txt
 -> infer platform/CMS
 -> discover archive/category/docs navigation
 -> sample representative URLs
 -> HTTP extraction test
 -> conditional Browser test
 -> build Site Profile Draft
 -> Golden URL validation
 -> publish Profile
 -> create FULL_BACKFILL
```

自动推断只能生成 Draft。任何高风险 Browser Action、自定义 JS、宽松 allow pattern 必须人工审阅或经过 Golden Replay 门禁。对 `blog.example.co.uk`、`docs.example.co.uk` 等 host 必须明确 Source scope 是单 origin、指定 subdomain 集还是整个 registrable domain，不能靠字符串第一段或最后两段猜测。

## 10. 历史全量发现与 Coverage

### 10.1 Provider 优先级

推荐发现顺序：

1. Sitemap Index/Sitemap；
2. 平台/CMS API；
3. RSS/Atom/JSON Feed；
4. Archive/Category/Author/Docs Navigation；
5. `llms.txt`/公开文档 manifest；
6. 普通站内链接 crawl；
7. 外部搜索只用于补洞和提示，不作为唯一 Coverage 来源。

### 10.2 Coverage 不是抓取成功率

每个 Provider 保存：枚举方法与边界、cursor/page/date range、oldest/newest、expected/discovered、duplicate/filtered/admission denied、known gaps/blockers、confidence、最近 reconcile 时间。

`FULL_BACKFILL` 结束条件必须基于 Provider exhaustion/evidence，而不是只看队列清空。

### 10.3 Reconcile

周期运行 `RECONCILE`：重新枚举关键 Provider，比较新旧 candidate set、日期边界和 manifest hash，发现遗漏、删除、迁移、canonical 改变或 Provider 漂移。

### 10.4 业务样本与 Coverage 分离

SERP Top N、搜索 Top K、竞品列表、人工指定 URL 只创建 `TARGETED_FETCH`/Analysis Sample，不修改 FULL_BACKFILL Coverage Candidate。

样本选择保存：

```text
sample_selection_evidence
- source: SERP|USER|SEARCH|RANKER
- rank
- query/context_snapshot_id
- selection_policy_release_id
- selected_at
```

## 11. Change-Signal Plane 与增量同步

增量同步先判断“值得抓正文吗”，再派发正文 Fetch：

1. Feed GUID/updated/new item；
2. Sitemap `lastmod`；
3. API updated_at/cursor；
4. Conditional GET：ETag/Last-Modified；
5. 轻量 metadata probe；
6. 正文抓取后 body hash；
7. 提取后 Canonical IR hash。

每个 Source 维护动态调度间隔：活跃站点缩短，长期无变化站点延长，但保留最大静默检查周期。

使用重叠窗口避免 Feed/API 时间边界漏数据；重复项依靠 URL/content identity 幂等去重。若 body 变化但 Canonical IR 不变，只记录 Freshness Observation，不触发 Chunk/Embedding/AI/TTS 重算。

## 12. Admission、安全与访问策略

所有 URL 在入队和 redirect 后重新检查：

- scheme 仅允许 http/https；
- IDNA/host normalize；
- 用版本化 Public Suffix List 计算 `registrable_domain`，origin、host、eTLD+1 分开记录；
- DNS 解析和 private/link-local/loopback/cloud metadata 阻断；
- redirect hop/目标限制，每一跳重新 Admission；
- robots 和站点访问政策；
- MIME/Content-Length/实际字节上限；
- 压缩炸弹防护；
- connect/read/total deadline；
- 禁止泄漏内部 header/credential。

Browser Worker 使用隔离容器、最小文件权限、NetworkPolicy 和独立 egress；不复用管理员登录浏览器 Session。

External callback/webhook URL 同样属于 SSRF 面，必须通过预注册 Destination、DNS/IP 校验和 allow policy，不能在每次 Job 中接受任意内网 URL。

Context Provider 的 API Secret 只在 Context Adapter Worker 使用，不透传给 n8n/Dify/前端。

Workflow Sandbox 的所有网络能力节点默认 deny；只有声明为 Integration Gateway、Context Provider、模型 Provider 或预注册 Destination 的目标才可放行。URL 来自表单、邮箱域名、LLM 输出、Sheet 单元格或上游 JSON 时，必须重新走 URL Admission，不能直接拼接后发起请求。

## 13. Priority 与 Metadata Probe

Priority 只决定“先处理谁”，不决定“历史全集里有没有它”。可用信号：Provider 类型/可靠度、URL/path pattern、depth/anchor/link location、日期、主题相关度、head title/description/canonical、人工业务优先级。

任何需要访问候选 URL 才能得到的信号都必须走 `METADATA_PROBE`：

```text
Candidate
 -> probe budget reservation
 -> limited-byte HEAD/GET
 -> immutable Probe Snapshot/cache
 -> Priority Evidence
 -> Priority Adapter
 -> normalized_priority
```

禁止第三方 `score`、n8n `priority: 10` 或底层 queue key 直接穿透到平台调度。

## 14. Durable Frontier、预算与公平调度

调度层同时考虑：normalized priority、per-source/per-origin/per-registrable-domain token bucket、tenant quota、HTTP/Browser worker capacity、retry/backoff、run fairness、global dispatch budget、bytes/time/cost budget。

派发流程：

```text
Task READY
 -> acquire host/global concurrency token
 -> reserve request/byte/time/cost budget
 -> mark reservation DISPATCHED
 -> worker starts side effect
 -> settle actual usage/outcome
 -> release token
```

Reservation 持久化并有 TTL/lease；重复投递由稳定 idempotency key 保证同一逻辑 attempt 不重复扣减。

`queued / reserved / dispatched / started / completed / transport_success / accepted` 分开统计。取消 Run 后，未 dispatch 的 Reservation 释放；已经 in-flight 的请求记为 wasted in-flight，不伪装成“从未发出”。

## 15. Route Decision 与抓取策略

### 15.1 HTTP-first

默认 HTTP 获取原始响应并存 Snapshot。成功条件不是 HTTP 200，而是后续抽取和 Quality 通过。

### 15.2 Browser 升级

仅在 JS shell、正文依赖客户端渲染、需要有限交互才能暴露公开内容、HTTP Extractor 连续质量失败且 Browser Golden 样本更好时升级。

Browser Recipe 版本化保存 wait selector、scroll 次数、click selector、viewport、locale、timeout 等；禁止站点无差别无限滚动。

### 15.3 Platform API / Fast Path

对 WordPress/Ghost/Substack/Docs 等，若公开 API 或结构化端点更完整、更稳定，可优先使用，但最终仍转成统一 Snapshot/IR/Version Contract。

### 15.4 PDF/OCR

PDF 先保留原文件 Snapshot，再提取文本和结构；只有文本层不足时使用 OCR。OCR 结果带 runtime/model/version 和页级 provenance。

## 16. Snapshot、Extraction、Canonical IR 与 Markdown

### 16.1 Snapshot 是重处理基础

至少保存 request URL/final URL/redirect chain、status/headers/MIME、原始 body、可选 rendered DOM、fetch 时间、route、runtime release、body hash。规则升级时优先从 Snapshot 离线 replay。

### 16.2 多 Extractor Candidate

同一 Snapshot 可生成 JSON-LD/OpenGraph metadata、CSS/XPath site-specific、Trafilatura、Readability、Crawl4AI extraction、平台结构化 payload 等 Candidate。Quality Policy 对 Candidate 评分并选 Accepted Candidate，保留候选和决策证据。

### 16.3 Canonical IR

```text
DocumentIR
- title/author/published_at/updated_at/tags/canonical
- language
- blocks[]
  - heading(level,text)
  - paragraph
  - code(language,text)
  - list(items,nesting)
  - quote
  - table(rows/cells)
  - image(asset_ref,alt,caption)
  - formula
- outbound_links[]
- metadata_provenance{}
```

### 16.4 Markdown Projection

Markdown 由 `markdown_projection_release` 从 Accepted IR 确定性生成：保留代码 fence、列表层级、表格、图片资源引用和固定 Front Matter，不做会破坏代码/公式/表格的通用 whitespace collapse。

禁止某个 n8n 工作流自行 `cleaned_html -> Markdown`，另一个直接使用 crawler `result.markdown`，导致同一 Version 出现两个知识真相。外部只能获取平台生成的 `MarkdownArtifactRef`。

## 17. Identity、版本与删除

URL normalization 处理 scheme/host case、default port、fragment、追踪参数、规则化 query、已知 canonical 规则，原始 URL 始终保留。host identity 同时记录 origin、normalized host、PSL `registrable_domain` 和 Source scope，避免 `co.uk`、多级子域、IDN 场景误归属。

Document Identity 综合 canonical URL、platform post ID/GUID、stable slug/path、title/date/source 和相似性证据。自动 merge 只在高置信度执行；不确定时生成人工候选。

Canonical IR hash 变化才创建新 Document Version。源站 404/410/Feed 移除不能立即物理删除历史版本；记录 tombstone。只有合规或人工删除请求走受审计删除流程。

## 18. Asset 归档

图片、附件和可下载资源独立 Asset Pipeline：

```text
Asset Candidate
 -> admission
 -> download budget
 -> immutable object
 -> hash/mime/dimension
 -> optional metadata cleanup
 -> asset version/ref
 -> Markdown rewrite
```

防止 data URI、超大图片、跨站跟踪像素、恶意文件。资源失败不让正文 Version 丢失，但 Quality 显示 missing asset，并支持 `ASSET_REPAIR`。

## 19. Quality、Drift 与人工治理

Quality 至少检查 title/正文、字数/段落、boilerplate、导航污染、code/table/list 保真、发布时间、canonical、重复正文、语言、资源缺失和 Snapshot/IR 一致性。

状态建议：`ACCEPTED / REVIEW / REJECTED / BLOCKED`。

每个 Source 保存 Golden URL 集，Profile/Extractor/Projection 升级先 Snapshot Replay，对比 title、正文 hash、结构块数量、代码/表格、metadata 和 Markdown diff。

Drift 指标包括 accepted rate、selector miss、正文长度分布、browser escalation、canonical 变化、provider discovered 数异常、同步延迟。异常只生成 Profile Draft/建议，不能自动发布高风险规则。

## 20. Search、RAG 与知识消费

Accepted Version 异步生成 Markdown、chunk、全文索引、embedding、topic/tag/summary 等 Artifact。

Chunk 以 IR block 为基础，按标题层级和语义边界切分；代码块、表格尽量整体保留。Chunk 记录 `document_version_id + block range + chunker_release`。

检索采用 Hybrid Search：BM25 + Vector + metadata filter + optional reranker。技术博客对代码 token、错误码、版本号、命令、配置键和文件路径做专门 lexical 分析。所有命中回到不可变 Version/Chunk Ref。

Embedding 模型升级创建新 `embedding_release`，后台重建新 projection；切换 alias 后旧索引再回收。

## 21. Context Snapshot / External Analytics Plane

### 21.1 目的

Google Search Console、BigQuery、SERP、PageSpeed、Index 状态、第三方行业数据不是文章正文，但常用于 SEO、内容分析和业务报告。它们必须成为显式、不可变、可追踪的 Context Snapshot，而不是临时 n8n 节点变量。

### 21.2 获取流程

```text
Context Request
 -> Consumer/Policy Admission
 -> Context Budget Reservation
 -> Provider Adapter
 -> schema normalize
 -> immutable raw payload/object
 -> Context Snapshot
 -> optional normalized Analytics Projection
```

### 21.3 时间和查询语义

每个 Snapshot 必须保存 query/request hash、时间窗口、`as_of`、国家/设备/语言等关键维度。相同请求可按 Provider TTL/cache policy 复用；超出 freshness 要求时创建新 Snapshot，不原地覆盖旧快照。

Context/Analysis Job 创建时必须先冻结 `evaluation_time + timezone`，然后由它派生“最近 30 天/前 30 天/今日”等窗口。SQL 中的 `CURRENT_DATE`、n8n 的 `$now`、模板生成的日期文件名必须使用该冻结值或等价绑定参数；同一个 Job 重试时禁止重新读取墙钟时间，否则同一 Manifest 会得到不同输入。

### 21.4 与 Source Truth 的边界

- Context Snapshot 不创建/删除 Document Version。
- SERP rank 不代表文章历史完整性。
- GSC/BigQuery 指标变化不触发文章重新抓取，除非独立 Policy 显式生成新的 Targeted Fetch 请求。
- Context Provider 故障只影响对应 Analysis/Derived Job，不污染 Source Sync。

## 22. Manifest、Derived Artifact 与多数据源分析

### 22.1 Selection Manifest

日报/Newsletter/批量问答先冻结有序 Document Version 集合、窗口、时区、Selection Policy、ranking evidence 和 manifest hash。

### 22.2 Analysis Input Manifest

跨数据源报告先冻结异构输入：

```text
Document Version
+ Context Snapshot(s)
+ Search/SERP Selection Evidence
+ Business Parameters
+ evaluation_time/timezone
+ Recipe/Model/Workflow Release
 -> Analysis Input Manifest
 -> Derived Build
```

重试必须复用同一 Manifest；若 GSC/SERP 数据更新后希望重新分析，则新建 Manifest。任何影响结果的 `$now/CURRENT_DATE/random/runtime default` 都必须先物化进 Manifest 或 effective config。

### 22.3 Derived Artifact

```text
derived_artifact
- type: SUMMARY|TAG|TOPIC|FAQ|DIGEST|SEO_REPORT|SERP_ANALYSIS|REPORT|TTS|...
- input_manifest_id
- input_version_refs
- input_context_refs
- recipe_release
- model_release
- workflow_recipe_release(optional)
- output object ref/hash
- token/cost/latency
- quality/outcome
```

派生缓存键：

```text
hash(
  input_manifest_hash
  + recipe_release
  + model_release
  + projection_release
  + normalized_args
)
```

相同输入/Release 可复用已有 Artifact。

## 23. Workflow / Dify / n8n / Agent Integration

### 23.1 集成边界

长期 Source Sync 与外部业务工作流彻底分离：

```text
Source Sync -> Accepted Version -> Artifact/Manifest
Context Plane -------------------> Context Snapshot
                                      |
                                      v
                              Integration Gateway
                                      |
                             n8n / Dify / Agent
                                      |
                              Business Workflow
                                      |
                         Delivery / Sheets / Drive / Email
```

外部工作流可以请求 Targeted Fetch、Search、Artifact、Context Refresh、Derived Build，但不能：

- 直接调用内部 Crawl4AI/Playwright hostname；
- 持有内部 crawler task id；
- 直接写 PostgreSQL/S3 真相层；
- 用自身 Loop 并发绕过 per-site/global budget；
- 直接指定底层 queue priority；
- 自己生成“官方 Markdown 真相”；
- 用 n8n HTTP Request、Browser、Code、Plugin、Tool 或自定义节点直接访问任意源站/用户派生 URL；
- 依赖节点显示名称、未显式默认值或 `continueOnFail` 把失败隐藏为成功。

### 23.2 Integration Job Contract

```text
integration_job
- id / tenant_id / consumer_id
- operation
- idempotency_key
- input_manifest_id / manifest_hash
- requested_artifacts
- requested/effective config
- evaluation_time / timezone
- completion_policy
- budget_class
- status
- deadline_at
- submitted/started/terminal timestamps
```

外部 API 只暴露稳定平台状态，内部 crawler 可自由替换。

### 23.3 批量 fan-out、Canonical Dedupe 与 Completion Barrier

外部提交一个 batch 后，平台先冻结原始 caller items 和 evidence，再按 normalized URL/canonical fingerprint 建 dedupe group，然后 durable scheduler fan-out。多个 caller item 可共享一个内部抓取结果，但 rank、device、query、原始 item key 等业务证据不得丢失。外部 workflow 不承担抓取调度。

Completion Policy：`ALL`、`BEST_EFFORT`、`MIN_SUCCESS(n)`、`DEADLINE`。

Job 状态：`QUEUED | RUNNING | PARTIAL | SUCCEEDED | FAILED | CANCELLED | EXPIRED | BLOCKED`。Item 保存更细 Typed Outcome；底层 Node 即使配置 continue-on-error，也不能把 Item Outcome 改写成成功。

### 23.4 Polling 协议

不采用固定 5 秒无限轮询。Status API 支持：

- `Retry-After`；
- ETag/If-None-Match；
- 可选长轮询；
- 客户端指数退避 + jitter；
- absolute deadline；
- terminal state；
- cancel API；
- 429/503 明确 retry semantics；
- consumer polling QPS quota。

### 23.5 Callback/Webhook

终态或重要事件落为持久 Delivery。要求 HMAC 签名、timestamp、replay window、幂等 event id、connect/read deadline、response byte limit、指数退避+jitter、最大重试、dead-letter。Destination 预注册并做 SSRF/egress policy。

Callback 默认只带 job id、状态、counts、manifest hash 和轻量结果引用，不推送大段文章正文。

### 23.6 稳定结果与 Artifact API

Integration Item 默认返回：`document_version_ref`、`markdown_artifact_ref`、Context/Derived Artifact Ref、canonical/title/published_at 等轻量 metadata、Quality Outcome、lineage summary、error class。

原始 HTML/Rendered DOM 只通过受权限控制的诊断 Snapshot API 获取。

## 24. 官方 Workflow Blueprint

平台提供版本化、可测试的“薄客户端”参考工作流，而不是在模板中写死 crawler 地址。

### 24.1 `CONTENT_OPTIMIZATION`

```text
article URL or Document Ref
 -> Targeted Fetch / Artifact API
 -> GSC/BigQuery Context Snapshot
 -> Analysis Input Manifest
 -> AI Derived Build
 -> SEO Report Artifact
 -> Drive/Sheets Delivery
```

### 24.2 `SERP_RESEARCH`

```text
keyword/country/device
 -> SERP Context Snapshot
 -> Top N + rank evidence
 -> Targeted Fetch batch
 -> canonical dedupe while preserving rank/device evidence
 -> Analysis Manifest
 -> LLM/Rule Analysis
 -> Report Artifact
```

Top N 只代表研究样本，不影响 Coverage。

### 24.3 `DIGEST_NEWSLETTER`

只消费 Selection Manifest + Derived Artifact，不临时抓网页。

### 24.4 `REPORT_EXPORT`

`Artifact -> HTML/PDF -> Drive/Email/Webhook Delivery`。

### 24.5 Blueprint Release

```text
workflow_blueprint_release
- engine_type: N8N|DIFY|INTERNAL
- business_workflow_type
- input/output schema
- required_scopes
- required_artifact_types
- required_context_types
- compatible_engine_version
- node_contract_manifest
- effective_default_manifest
- egress_contract_manifest
- error_policy_manifest
- test_fixture_refs
- sanitization_report_ref
- source_commit / hash
```

生产实例化 Blueprint 后仍生成独立 Workflow Recipe Release，并走 Sanitizer → Preflight → Sandbox。模板中 credential instance id、webhook id、pinned execution data、内部 host、示例 PII 不属于可部署 Blueprint 内容。

## 25. Workflow Recipe、Preflight 与沙箱

Workflow DSL/Graph 改动生成新 Workflow Recipe Release。发布前执行：

1. 目标引擎 parse/import/export round-trip，并固定兼容 engine version；
2. graph edge/node/variable/input-output schema 完整性；
3. 解析每个节点真实 `type/typeVersion/parameters`，把目标引擎默认值物化为 `effective_default_manifest`，禁止用节点显示名称推断行为；
4. Code 节点语法、直接依赖、启动与最小 contract test；
5. 扫描 HTTP Request、Browser、Code、Plugin、Tool、自定义节点等所有网络能力，生成 egress contract；任何动态 URL 必须映射到 Integration Gateway/Context Adapter/预注册 Destination；
6. 检测硬编码 localhost、私网、cloud metadata 和未经批准外部 URL；
7. 模板净化：移除 credential instance id、webhook id、pinned execution data、示例执行结果/PII、内部 hostname；只保留逻辑 credential slot/schema；
8. 固定 Model/Provider/Tool/Plugin 依赖；
9. Secret scope 最小权限；
10. 节点输入/输出字节、token、文件和 deadline 上限；
11. 扫描 `$now`、`CURRENT_DATE`、随机数、隐式 locale/timezone 等非确定性输入；影响结果者必须声明并冻结 `evaluation_time/timezone` 或显式 seed；
12. 审计 `continueOnFail/onError/retryOnFail` 等错误策略：允许图继续，但必须生成显式 Node Outcome，且不能被默认 success count 吞掉；
13. Sandbox Policy、egress allowlist、CPU/Memory/PID/临时盘配额；
14. Golden Input 结构化输出测试；
15. 生成不可变 Preflight Report，包含 effective params、sanitization、egress、time-input、error-policy 结果。

生产执行保存 WorkflowExecution/NodeExecution：实际 `type/typeVersion/effective parameters`、Code/Tool/Plugin/Model、输入输出 Artifact hash、evaluation_time/timezone、Secret scope、network policy decision、error policy decision、latency/cost、Outcome、trace。

所有网络能力节点默认拒绝任意源站出网；抓网页必须走 Integration Gateway，外部分析必须走 Context Adapter，交付必须走预注册 Destination Adapter。`continueOnFail` 只允许后续节点继续，不允许把失败节点或失败 Item 计作成功。

## 26. TTS 与 Delivery

TTS 默认消费 Summary/Digest，不消费大量完整正文；限制输入 token/字符、目标时长、deadline、模型/音色 Release、GPU 配额、失败重试和成本预算。

Audio Artifact 保存对象 key/hash、duration、mime、model/recipe/runtime attestation。

通用 Delivery Job：

```text
delivery_job
- trigger
- input artifact / manifest
- destination adapter release
- recipe release
- idempotency_key
- business_object_key(optional)
- destination_object_id(optional)
- status / attempt / next_retry
- response fingerprint
- error class
```

### 26.1 Google Sheets / Drive Adapter

支持 caller item key 回填、business correlation key、artifact hash 去重、明确 `CREATE|UPDATE|APPEND` operation、持久 destination object id、retry/dead-letter。

Drive/Sheets 文件是否存在不能作为抓取平台唯一状态；它们只是 Delivery Projection。禁止“按 URL slug 搜索文件 → 不存在就创建”作为唯一幂等机制；需用稳定 delivery idempotency key + 持久 destination object id，避免并发 search-create 竞态与同名碰撞。

## 27. Web 管理端

### 27.1 Source Intake

新增单站点/批量/OPML/CSV；显示自动探测、robots/policy、Provider 建议、Golden URL，并创建 FULL_BACKFILL。

### 27.2 Typed Run Console

每个按钮明确映射 Run Type。提交后展示 requested/effective config、默认值/Profile/Policy/Override 来源、resolver release、Run ID、validation warning。

### 27.3 Site Profile Studio

编辑 URL/Provider/Selector/metadata/remove rule、HTTP/Browser route constraint、Golden URL；同一 Snapshot 多策略预览 Candidate/IR/Markdown；支持 Draft、Replay Test、Shadow、Publish、Rollback。

### 27.4 Provider Coverage

显示 Provider、枚举边界、oldest/newest、cursor、expected/discovered、known gap、blocker、confidence，并可触发 Reconcile。

### 27.5 Focused Discovery / Onboarding Explorer

输入 root URL + 可选主题 + 页面/时间/dispatch 预算，只创建持久 `FOCUSED_DISCOVERY` Run。Web 渐进展示 observed、scored、probe reserved/dispatched、fetch reserved/dispatched/started/completed、accepted 数量及候选证据，并明确“Focused 结果不是 Coverage 完整率”。

### 27.6 Run / Task / Replay / Quality

展示 Route Decision、Capability、requested/effective config、Budget/Reservation、lease/retry/dead-letter、Snapshot Replay、Priority Replay、Quality/Drift、Search/RAG/AI lineage、token/cost/truncation、Workflow Node trace 和 Delivery。

### 27.7 Context / Analysis Console

展示 Context Provider、query hash、时间窗、country/device、freshness、cache hit、budget、raw payload ref、schema release；可查看 Analysis Input Manifest 中每个 typed ref、`evaluation_time/timezone` 和最终 Derived Artifact lineage。

### 27.8 Integration Runs / Consumers

展示 consumer/scope/quota、Integration Job/Input Manifest、Job/Item/caller key、dedupe group、requested/effective config、evaluation_time/timezone、budget reserved/dispatched/settled、polling rate/Retry-After、callback attempt/dead-letter、Artifact Ref、idempotency 命中、cancellation/deadline/partial success、Workflow/Blueprint Release lineage，以及每个 Workflow Node 的 effective params、egress decision、error policy 和 Typed Outcome。

## 28. Observability、Audit、Health 与缓存

主链路 Trace：

```text
Command
 -> Run
 -> Candidate/Evidence
 -> PriorityEvidence
 -> BudgetReservation
 -> Task
 -> RouteDecision
 -> Fetch
 -> Snapshot
 -> Extraction
 -> Quality
 -> Identity
 -> Version
 -> Projection
 -> ContextSnapshot(optional)
 -> Manifest/IntegrationJob
 -> DerivedJob/WorkflowExecution
 -> Artifact
 -> Delivery
```

技术指标：queue lag、latency、timeout、retry、browser crash、DB/Redis/S3 error、response-too-large、cancellation、pool utilization、lease expiration。

业务指标：discovered/candidate、budget admitted/denied、reserved/dispatched/started/completed、transport success、accepted、new version/unchanged、coverage gap、sync lag、asset missing。

Context：provider call、cache hit、bytes、latency、quota exhausted、snapshot freshness、schema error。

Integration：job/Item 状态、poll QPS、Retry-After adherence、callback success/retry/dead-letter、manifest size、dedupe ratio、idempotency hit、quota exhausted、deadline/cancel ratio。

Workflow：node effective-param drift、implicit-default usage、template sanitization failure、egress allow/deny、dynamic-URL admission、continue-on-error count、node failure vs execution success、evaluation-time mismatch、credential/pinned-data residue。

预算指标至少区分 limit、reserved、settled、expired、overrun detected、wasted in-flight。

所有 Profile/Release/Override/Manual Merge/Targeted Fetch/Focused Discovery/Metadata Probe/Replay/Quality Override/Context Refresh/Selection Build/Analysis Manifest/Derived Build/Workflow Template Sanitize/Workflow Preflight/Sandbox Override/Integration Submit/Callback Replay/Delivery Retry/Budget Override 全部写 Audit。

Health 区分 liveness、readiness、dependency_degraded、critical_dependency_unavailable。搜索/Context Provider/AI/TTS/Destination 故障可降级；PostgreSQL/S3 等任务所需真相源不可用时 fail-closed。

缓存可覆盖 robots、Provider manifest、Capability Projection、Metadata Probe、conditional metadata、短期 Fetch dedupe、Search result、Context Snapshot lookup、Derived Artifact lookup、Integration status 热读。真相永远可从 PG/S3 恢复。

## 29. 失败分类与升级

统一错误至少覆盖：

```text
DNS_ERROR
CONNECT_TIMEOUT
READ_TIMEOUT
DEADLINE_EXCEEDED
RESPONSE_TOO_LARGE
HTTP_4XX
HTTP_5XX
RATE_LIMITED
ROBOTS_BLOCKED
POLICY_BLOCKED
SSRF_BLOCKED
WAF_CHALLENGE
JS_SHELL
EMPTY_CONTENT
SELECTOR_MISS
CONTENT_QUALITY_LOW
UNSUPPORTED_TYPE
BROWSER_CRASH
PARSER_ERROR
STORAGE_ERROR
SEARCH_ERROR
MODEL_ERROR
TTS_ERROR
CONTEXT_PROVIDER_ERROR
CONTEXT_QUOTA_EXHAUSTED
CONTEXT_SCHEMA_ERROR
INVALID_MANIFEST
COMMAND_CONFIG_VALIDATION_ERROR
PRIORITY_CONTRACT_VIOLATION
BUDGET_EXHAUSTED
RESERVATION_CONFLICT
DISPATCH_OVERRUN
DOMAIN_SCOPE_INVALID
WORKFLOW_ERROR
WORKFLOW_PREFLIGHT_FAILED
WORKFLOW_SANDBOX_VIOLATION
WORKFLOW_EGRESS_DENIED
WORKFLOW_TEMPLATE_SANITIZATION_FAILED
WORKFLOW_EFFECTIVE_CONFIG_INVALID
WORKFLOW_NONDETERMINISTIC_INPUT
WORKFLOW_SILENT_FAILURE_POLICY
INTEGRATION_UNAUTHORIZED
INTEGRATION_QUOTA_EXHAUSTED
CALLBACK_ERROR
CALLBACK_SIGNATURE_ERROR
CALLBACK_DESTINATION_BLOCKED
DELIVERY_ERROR
CANCELLED
EXPIRED
```

只有适合升级的错误才进入下一 route：`JS_SHELL` 可 HTTP → Browser；`ROBOTS_BLOCKED/POLICY_BLOCKED` 不升级；`RATE_LIMITED` 延迟重试；Context/AI/TTS/Workflow/Delivery 错误不反向标记 Source Sync 失败。Workflow “图继续运行”不等于 Node/Item 成功，错误状态必须保留到 Integration/Derived/Delivery 的最终统计中。

## 30. 测试、Replay 与发布门禁

1. **Contract Test**：Adapter schema、错误分类、timeout、取消传播、字节边界、provenance。
2. **Golden Extraction Test**：典型文章、代码、表格、图片、分页、动态页。
3. **Snapshot Replay Test**：新 Extractor/PostProcess/Markdown Release 在旧 Snapshot 上批量回放。
4. **Coverage Test**：已知小站点验证 Sitemap/Feed/Archive 枚举边界和 gap。
5. **Priority Contract Test**：pinned 第三方 runtime 验证 raw score 到 normalized priority 的实际顺序。
6. **Budget Concurrency Test**：并发/batch/retry/cancel 下 dispatched 永不越过 reservation policy。
7. **Idempotency Test**：重复 Command、队列消息、Integration Submit、Delivery 不产生重复逻辑版本/扣费/外部副作用。
8. **Crash Recovery Test**：Worker 在 fetch/extract/publish 各阶段退出后可恢复。
9. **Browser Pool Test**：崩溃、泄漏、超时、Session 隔离和资源上限。
10. **Projection Determinism Test**：同 IR + 同 Release 生成相同 Markdown hash。
11. **Context Snapshot Test**：相同 request/query/window 缓存语义稳定；新 freshness 创建新 Snapshot，不覆盖旧数据。
12. **Analysis Manifest Replay Test**：同一 Document Version + Context Snapshot + evaluation_time/timezone + Recipe 重跑输入完全一致。
13. **SERP Sample Boundary Test**：Top N 分析样本绝不修改 FULL_BACKFILL Coverage Candidate。
14. **Integration API Contract Test**：外部协议不暴露 crawler task/status shape；API Release 升级兼容。
15. **Integration Dedupe Test**：mobile/desktop/重复 caller URL 归并为逻辑抓取项时，所有 caller key/rank/device evidence 均可回放。
16. **Polling Test**：Retry-After、ETag、长轮询、deadline、429 下不会形成忙轮询。
17. **Callback Test**：HMAC、replay window、SSRF、timeout、重试、幂等、dead-letter。
18. **Barrier Test**：ALL/BEST_EFFORT/MIN_SUCCESS/DEADLINE 的 partial success 语义。
19. **Workflow Effective Config Test**：节点名称与真实参数不一致、参数缺失依赖引擎默认值时，Preflight 可物化并验证 effective config。
20. **Workflow Template Sanitization Test**：credential id、webhook id、pinned data、样例 PII、内部 host 残留时阻断发布。
21. **Workflow Egress Test**：HTTP Request、Browser、Code、Plugin、Tool、自定义网络节点直接访问源站/私网/metadata 时均阻断；仅允许 Gateway/Context/Destination 合同目标。
22. **Workflow Time Freeze Test**：`$now/CURRENT_DATE` 依赖被物化；同 Job 跨日重试仍使用同 evaluation_time/window。
23. **Workflow Error Policy Test**：`continueOnFail/onError` 允许图继续但 Node/Item 保持失败，success/partial 统计正确。
24. **Destination Adapter Test**：Sheets/Drive create/update/append、artifact hash 去重、destination object id、并发 search-create 和 retry 幂等。
25. **PSL Domain Scope Test**：`co.uk`、多级子域、IDN、跨 origin redirect 的 Source 归属、限流与 Admission 正确。
26. **Load Test**：按真实站点分布模拟 1000 Source，验证 host fairness、queue lag、PG/S3/Redis、Browser 配额和 Context Provider quota。

发布策略：shadow → canary source/tenant → 分批 → 全量；任何 Quality/Coverage/Budget/Integration/Context/Workflow Contract 回归自动停止扩大范围。

## 31. 部署、资源隔离与数据生命周期

推荐 Kubernetes/等价编排：

- API/Web；
- Scheduler/Dispatcher；
- Outbox Relay；
- Discovery Worker；
- Metadata Probe Worker；
- HTTP Fetch Worker；
- Browser Worker；
- Extraction/Projection Worker；
- Asset Worker；
- Search/Embedding Worker；
- Context Adapter Worker；
- AI/TTS Worker；
- Workflow Template Sanitizer/Preflight/Sandbox Worker；
- Integration/Callback Worker；
- Delivery Worker。

不同 Worker 使用独立 Deployment/Queue/Resource Limit。Browser、OCR、Embedding、LLM/TTS、Workflow Sandbox 不与轻量 HTTP Worker 混部。

数据库大表按 source/time/hash 等实际查询模式分区；对象存储使用内容 hash 和 immutable key；OpenSearch/Vector 仅保存 Projection，可删除重建。

数据生命周期分层：

- Snapshot/Version/Manifest/Audit 按合规要求长期保留；
- Rendered screenshot、大调试 payload 可转冷存或按 policy 到期；
- Context Snapshot 依据 Provider 许可和业务保留期限设置 retention class；
- Search/Embedding/缓存可重建，不作为归档唯一副本；
- Workflow 原始模板/净化报告/Preflight Report 依据合规保留，pinned execution data 和样例 PII 不进入生产 Blueprint；
- 删除/合规请求必须传播到相关 Projection/Artifact，并保留受限删除审计。

## 32. 分阶段实施

### Phase 1：可用的历史抓取主链路

Source/Profile、PSL domain scope、Sitemap/Feed/Archive Provider、URL Candidate/Evidence、PostgreSQL + Redis Streams + Outbox、HTTP Fetch + Snapshot、Trafilatura/Readability/CSS Extractor、Canonical IR、Quality、Document/Version、Markdown Projection、基础 Web Source/Run 页面。

### Phase 2：1000 站点扩展与增量

Change-Signal Plane、Conditional GET、Durable Budget Reservation、per-origin/per-registrable-domain fairness、Capability/Route Decision、Browser Pool、Reconcile/Coverage、Site Profile Studio/Golden Replay、Drift/告警。

### Phase 3：资产与检索

Asset Pipeline、OpenSearch、pgvector/Embedding、Hybrid Search、Chunk lineage、Reindex/Re-embedding、Search/RAG Web 页面。

### Phase 4：Context 与 Derived Artifact

Context Provider Adapter、Context Snapshot、Context Budget/Cache、Selection/Input/Analysis Manifest、evaluation_time/timezone freeze、Summary/Tag/Topic/FAQ、Digest Map/Reduce、SEO/SERP Analysis Artifact、Derived cache、Token/cost budget、AI Recipe/Model Registry。

### Phase 5：Integration / Workflow / Delivery

External Consumer/scopes/quota、Integration Job/Item、Canonical Input Dedupe、Targeted Fetch/Search/Artifact/Context API、Polling Retry-After/ETag/long-poll、signed callback + dead-letter、Completion Barrier、Workflow Blueprint Sanitizer/effective-default materialization/time freeze/egress-error policy Preflight/Sandbox、n8n/Dify Adapter、Integration Web Console、Google Sheets/Drive Delivery Adapter。

### Phase 6：TTS 与高级治理

TTS Worker、Typed Audio Artifact、Recipe/Selection/Analysis/Delivery Studio、多租户/RBAC 强化、自动 Drift 建议、大规模成本优化与数据生命周期策略。

## 33. 验收标准

### 33.1 历史完整性

- 每个 Source 有可查看的 Provider Coverage 状态；
- 已知小样本站点可证明候选枚举边界；
- FULL_BACKFILL 结束报告包含 provider exhaustion/gap/blocker；
- SERP/Search Top N 不得被显示成 Coverage。

### 33.2 增量

- 新文章在目标 SLA 内被发现；
- unchanged 内容不重复创建 Version/Embedding/AI；
- Feed/API/Sitemap 边界通过重叠窗口不漏项；
- 周期 Reconcile 能发现增量通道漏掉的候选。

### 33.3 数据质量

- Golden 集 title/body/code/table/image 结构通过率达到上线阈值；
- 同 Snapshot + 同 Release 重放输出 hash 稳定；
- 每个 Markdown 可追溯到 Document Version、Canonical IR 和 Snapshot；
- 所有外部消费者获取同一 Version 时得到同一官方 Markdown Projection。

### 33.4 可靠性

- Worker crash 后任务可恢复；
- 重复消息不重复发布逻辑 Version；
- 并发 dispatch 不突破 Budget Reservation；
- 单站点故障不会阻塞其他站点；
- Browser/Context/AI/TTS 故障可隔离降级。

### 33.5 Context / Analysis

- 任意 SEO/SERP/业务报告可回答使用的 Document Version、Context Snapshot、时间窗、Provider、Manifest、evaluation_time/timezone、Recipe、Model 和 Workflow Release；
- 同一 Analysis Manifest 重试不重新查询实时 GSC/SERP，也不因跨日改变时间窗口；
- 需要刷新 Context 时创建新 Snapshot/Manifest；
- Context Provider 故障不影响 Source Sync。

### 33.6 Integration / Workflow

- n8n/Dify 不需要知道内部 crawler hostname/task id；
- 重复相同 idempotency key 不创建重复 Job；
- 大 batch 由平台 fan-out，并遵守 per-host/global budget；重复 canonical URL 只做逻辑一次抓取但 caller evidence 完整；
- polling 遵守 Retry-After/deadline；
- callback 支持签名、重试、幂等和 dead-letter；
- ALL/BEST_EFFORT/MIN_SUCCESS/DEADLINE 行为可测试；
- caller item key/business object key 可稳定映射外部行或业务对象，但不充当 Document Identity；
- Sheets/Drive 只作为派生交付目标，不影响 Source Truth；
- 官方 n8n Blueprint 不包含内部 crawler hostname/task id，也不自行生成官方 Markdown；
- Blueprint 不携带 credential id、webhook id、pinned data、样例 PII，并有 Sanitization Report；
- HTTP Request/Browser/Code/Plugin/Tool/自定义网络节点的 egress 都受统一 Policy 管控；
- 节点名称与参数不一致时，以物化后的 effective config 为准并可审计；
- `$now/CURRENT_DATE` 等动态时间被 Manifest 冻结，同一 Job 重试输出输入不漂移；
- `continueOnFail/onError` 不会把失败 Node/Item 统计为成功。

## 34. 最终架构决策

这个系统不是“一台 Crawl4AI 把 1000 个站点爬下来”，也不是“Dify/n8n 每次需要内容时临时抓网页”，而是长期运行的数据同步、版本、外部上下文和派生平台。

核心事实边界如下：

- **Coverage** 回答应该有哪些历史文章；
- **Candidate/Evidence** 回答实际观察到了哪些 URL、从哪里发现；
- **Priority Hint** 只回答预算内先处理谁；
- **Budget Reservation** 决定真实副作用是否允许派发；
- **Capability/Route** 决定怎样抓更可靠、更便宜；
- **Snapshot/Canonical IR/Version** 回答当时抓到了什么、如何解释、内容是否变化；
- **Markdown/Search/Embedding** 是可重建 Projection；
- **Context Snapshot** 回答当时外部 GSC/BigQuery/SERP/PageSpeed 等数据是什么；
- **Selection/Input/Analysis Manifest** 固化每次批量或跨数据源消费的不可变事实输入和 `evaluation_time/timezone`；
- **Derived Artifact** 保存摘要、SEO 报告、SERP 分析、Digest、音频等派生结果；
- **Integration Job** 是 Dify/n8n/Agent 使用平台能力的稳定异步边界；
- **Workflow Blueprint/Template Sanitizer/Effective Config/Preflight/Sandbox** 约束低代码与 Agent 编排；
- **Delivery Job** 负责 Google Sheets/Drive/Webhook/邮件等可靠交付和重试。

对 1000 站点长期知识库，始终坚持：**Coverage Candidate 决定历史全集而 Priority 只能改变处理顺序；已经观察到的链接先成为可审计 Evidence，再应用评分和 fetch budget；所有真实网络/模型/外部 Provider/交付副作用在 dispatch 前取得持久 Reservation；Accepted Document Version 是正文事实输入，Context Snapshot 是独立外部观察事实，Markdown/搜索/AI/TTS/SEO 报告是可重建派生；任何批量或跨数据源分析先冻结 Typed Manifest 与 evaluation_time/timezone；Dify/n8n/Agent 是受控消费者与业务编排器，不是抓取调度器、内容真相源、官方 Markdown 生成器或任意出网代理；外部只依赖 Integration Job、Manifest、Typed Status 和 Artifact Ref，不依赖 Crawl4AI task_id/内部 host/result shape；Workflow 的真实语义以 type/typeVersion/effective parameters 为准，所有网络节点统一 egress 治理，continue-on-error 不改变失败事实；Source domain scope 使用 PSL registrable domain/origin 模型而不是字符串猜测；所有配置、规则、模型、Context Provider 和 Workflow 都必须可版本化、可审计、可重放、可回滚。**