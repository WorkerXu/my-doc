# 博客知识库技术方案

适用范围：从约 1000 个技术博客、工程博客、技术文档站、Newsletter、CMS 与公开文档站抓取尽可能完整的历史文章，清洗为稳定 Markdown 建设长期知识库；后续持续增加网站，并支持全量回灌、增量同步、Web 管理、版本留存、离线重处理、全文/向量检索、质量审计、图片附件归档，以及 Dify/n8n/Agent/Newsletter/TTS 等下游工作流消费。整体按百万级文档、数百万到千万级 URL、站点高度异构和多年持续运行设计。

## 1. 建设目标

1. 支持站点根地址、单 URL、批量 URL、RSS/Atom/JSON Feed、Sitemap/Sitemap Index、OPML、CSV、JSON、Markdown/TXT 等来源导入。
2. 自动探测 Sitemap、Feed、公开 API、Archive、Category/Tag/Author、Docs Navigation、普通内链、`llms.txt`/`llms-full.txt`、平台 API 等发现入口。
3. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`PROBE`、`METADATA_PROBE`、`FOCUSED_DISCOVERY`、`REPROCESS`、`ASSET_REPAIR`、`REINDEX`、`DERIVED_BUILD`、`DELIVERY_RETRY` 等运行类型。
4. 历史完整性由 Provider 级 Coverage Evidence 证明，不能以“队列空了”“达到 max_depth/max_pages/max_urls”代替完整性证明。
5. HTTP-first；只有动态渲染、正文缺失、挑战页、选择器失效或质量失败有证据时才升级 Browser/Crawl4AI/Playwright。
6. 支持站点能力学习，用正文接受率、质量、延迟、成本、blocker 优化抓取 route，但 Capability 不替代 Coverage。
7. Source/Profile/Discovery/Fetch/Extractor/PostProcess/Quality/Asset/Chunker/Search/Reranker/Embedding/AI/TTS/Workflow/Delivery 全部采用不可变 Release。
8. 使用 Sitemap `lastmod`、Feed GUID/updated、API cursor、ETag、Last-Modified、body hash、Canonical IR hash、重叠窗口与周期 Reconcile 驱动增量同步。
9. 最终知识对象保存标题、作者、发布时间、更新时间、标签、canonical、正文块、代码块、表格、图片、附件、链接关系、Snapshot、字段 provenance 和完整 lineage。
10. 支持 durable frontier、断点续跑、幂等、lease、heartbeat、重试、限流、熔断、公平调度、backpressure 和 dead-letter。
11. 保存不可变 Network Snapshot、Rendered DOM、Extraction Candidate、Canonical IR、Markdown Projection 和质量结果；规则升级优先离线 replay，不重复访问源站。
12. Web 管理端覆盖 Source Intake、Site Profile、Provider Coverage、Focused Discovery、Capability & Route、Run/Task、Targeted Fetch、Extraction Preview、Snapshot Replay、Version Diff、Quality/Drift、Assets、Search/RAG、AI Artifact、Integration、Workflow/Delivery、成本和审计。
13. Markdown、全文索引、Embedding、摘要、标签、Topic、FAQ、Digest、Newsletter、TTS 和报告均是 Accepted Document Version 的异步 Projection/Derived Artifact，失败不能阻塞 Source Sync。
14. append-only 历史真相层之上提供可重建 Current Projection。
15. Canonical IR hash 在 Chunk/Embedding/AI/TTS 等昂贵下游之前计算；正文未变化只写 Freshness Observation。
16. Change-Signal Plane 优先通过 Feed/Sitemap/API 元数据判断是否可能变化，再决定是否抓正文。
17. 日报/Newsletter/批量问答必须先冻结 Selection/Input Manifest，再执行摘要、RAG、TTS 或外部 Workflow，保证补跑和重放结果可解释。
18. Dify/n8n 的 Code/Plugin/Tool 节点视为独立不可信执行边界；任意 URL 抓取必须通过 Integration Gateway/`TARGETED_FETCH`，不得直接访问内部 crawler。
19. Discovery 中的主题相关度、URL pattern、SEO/head metadata、链接位置和人工主题只生成 Priority Hint；在 `FULL_BACKFILL`/`RECONCILE` 中不得因低分而删除合法 Coverage Candidate。
20. 候选页 `<head>`/有限字节探测必须建模为显式 `METADATA_PROBE` 任务，受缓存、QPS、字节、deadline 和总预算约束。
21. 第三方 crawler/scorer/priority queue 的原生 `score`/`priority` 不能直接成为业务调度优先级；必须通过版本化 Priority Adapter 归一化并做实际 runtime 顺序契约测试。
22. Web 控件、API Command 与持久 Run Type 必须强类型一一映射；保存 requested/effective run type/config 和解析 provenance，禁止隐藏字符串布尔值或前端默认值静默改变运行模式。
23. 所有网络、模型、TTS、外部交付副作用在 **dispatch 前** 强制预留预算；候选数、派发数、成功数、Accepted 数、字节、时间和成本预算分开建模。
24. 外部 Dify/n8n/Agent 只能通过版本化 Integration Job Contract 使用抓取和派生能力；支持幂等提交、不可变 Input Manifest、服务端 fan-out/barrier、`Retry-After`/长轮询、签名 callback 和 Artifact Ref，禁止依赖 Crawl4AI `task_id`、内部 hostname、内部 status/result shape。

## 2. 非目标与边界

- 不绕过登录、付费墙、验证码、WAF 或明确访问控制。
- 不承诺未知站点仅靠自动探测达到 100% 历史完整；必须展示 coverage confidence、known gap 与 blocker。
- 不用 LLM、Reranker、SEO 分数、URL 关键词或抓取成功率决定 FULL_BACKFILL 中合法历史文章是否存在。
- Markdown 不是唯一真相；Canonical IR 是内部内容真相，Markdown 是确定性投影。
- 不锁死单一爬虫库、浏览器库、向量库、搜索引擎、消息队列、工作流引擎或模型供应商。
- HTTP 200、Browser success、协程正常返回不等于业务内容成功。
- 不把全局 reload/scroll/wait 脚本作为所有站点默认策略；浏览器动作必须进入 Site Profile Recipe。
- 不把多个 Document Version 的全文拼成一个无结构大字符串作为默认 AI/TTS 输入。
- 不在 Web/Dify/n8n 请求线程同步等待长期 Browser、LLM、OCR 或 TTS 全链路完成。
- 不允许 Workflow Code/Plugin 节点拥有不受控的互联网/内网访问、数据库/对象存储真相层凭据或云 metadata 权限。
- 不相信第三方 API 名称暗示的排序方向；`BestFirst`、`score`、`priority` 等必须通过运行时契约验证。
- 不在父页面链接已经可观察后，因为剩余抓取预算而按 DOM 顺序提前丢弃未评分候选。
- 不把模型/API Secret 放进客户端 cookie/session、普通日志、trace 或前端可回显配置。
- 不把“只返回 N 个成功结果”解释成“最多发出 N 个源站请求”；并发 batch、prefetch、redirect、retry 都纳入 dispatch budget。
- 不让 n8n/Dify 的 Loop/Batch 节点成为 1000 站点全局抓取调度器。
- 不把 Google Sheets/Drive/Notion 等协作工具作为主内容真相源。

## 3. 核心原则

1. **PostgreSQL 是业务状态真相源；S3/MinIO 是不可变大对象真相源。**
2. Redis Streams 只做任务运输；决定性状态先写 PostgreSQL，再由 Transactional Outbox 投递。
3. Source Intake、Change Signal、Discovery、Admission、Priority、Budget Admission、Route Decision、Fetch、Extraction、PostProcess、Quality、Identity、Publish、Asset、Search、AI、TTS、Integration、Delivery 分层。
4. Discovery 只产生 URL Candidate 与 Evidence；列表页、Feed 摘要、搜索结果不能直接成为最终正文。
5. URL Identity、Document Identity、Content Version 与 Freshness 分离。
6. Discovery Fetch Route 与 Article Fetch Route 分离，能力统计按 operation type 单独维护。
7. HTTP、Browser、PDF/OCR、Platform API、Fast Path 通过 Adapter 隔离，第三方原始返回 shape 不泄漏到业务真相层。
8. 所有 Stage 使用固定 Typed Outcome Contract。
9. `document_version` append-only；Current/Markdown/Search/Embedding/AI/TTS/Delivery Projection 可重建。
10. `max_depth/max_pages/max_runtime/max_urls/top_k` 是预算或停止条件，不是 Coverage Evidence。
11. 进程内 semaphore/`asyncio.gather` 只做 Worker 本地资源保护，不承担持久调度或全局预算真相。
12. Config 使用不可变 Release；Secret 只保存 Secret Manager 引用。
13. 代码缩进、表格、列表层级、引用、公式和换行都是内容数据，不能在通用 cleaner 中被压平。
14. 多个 Extractor/Strategy 和 Link Discovery 优先消费同一 Snapshot；网络访问与内容解释解耦。
15. Coverage Candidate 与 Priority Hint 是两个事实：前者决定“应该被枚举与审计哪些 URL”，后者只决定“预算内先处理谁”。
16. Discovery Filter 必须声明 `LOCAL_PREDICATE` 或 `NETWORK_PROBE`；会联网的 Filter/Scorer 转成持久 Probe Task。
17. 第三方 `raw_score` 与平台 `normalized_priority` 分离；平台统一定义 **normalized_priority 越大越先处理**，Scheduler Adapter 再转换到底层 queue key。
18. 已抓父页面产生的 observable links 先形成 Candidate/Evidence，再做评分和 fetch budget admission。
19. UI 请求参数、Command、Run 的 requested/effective config 都是可审计事实。
20. **预算以副作用边界为准。** 每次真实访问源站、模型、TTS、外部 Destination 前必须取得持久 Budget Reservation。
21. 外部工作流只表达业务意图、输入集合、完成策略和所需 Artifact，不拥有内部抓取调度权；raw priority、循环并发、超时均由平台 Policy 归一化。
22. 外部接口稳定性由 Integration API Release 保证；内部 crawler、queue、Browser Runtime 可以替换而不要求所有 n8n/Dify 工作流同步修改。

## 4. 规模假设与容量边界

初始按以下量级设计，不把估算写死成限制：

- Source：1000 个起步，可扩展到数千。
- 文档：百万级 Accepted Document；历史版本可达数百万。
- URL Candidate/Evidence：数百万到千万级。
- 日常增量：取决于站点更新频率，调度必须允许从几千到数十万 URL/日弹性变化。
- Snapshot/DOM/附件：主要容量进入对象存储；PG 只放索引、状态、hash、引用和必要 metadata。
- Browser 是昂贵稀缺资源，HTTP Worker 与 Browser Worker 独立池化和配额。
- Embedding/LLM/TTS 是异步派生资源，不能反压 Source Sync 主链路。

容量扩展首先通过 Worker 横向扩容、队列分片、对象存储扩容和 PostgreSQL 分区实现；只有实际指标证明单库/单索引成为瓶颈时再拆服务或迁移专用向量库。

## 5. 推荐技术栈

### 5.1 控制面

- Web：Next.js + TypeScript。
- API：FastAPI + Pydantic，Command/API 使用 Enum、Discriminated Union 和显式版本。
- PostgreSQL：Source、Profile、Provider、Candidate、Evidence、Run、Task、Budget、Reservation、Document、Version、Coverage、Capability、RouteDecision、PriorityEvidence、Manifest、IntegrationJob、DerivedArtifact、WorkflowExecution、DeliveryJob、Audit。
- Redis Streams：任务运输和 Consumer Group。
- Transactional Outbox：保证数据库状态与消息投递一致。
- Redis/Valkey：robots、Capability Projection、短期 probe、UI 热数据和非关键缓存，不作为唯一真相源。
- Secret Manager：Vault、云 Secret Manager 或 Kubernetes Secret 引用。
- Integration Gateway：面向 Dify/n8n/Agent/Webhook 提供稳定 Integration Job/Artifact/Search API。
- Workflow Preflight：DSL Parser/Linter + target-engine import test + dependency resolver。
- Workflow Sandbox：Kubernetes Job/隔离 Worker + seccomp/AppArmor/NetworkPolicy/egress proxy。

### 5.2 数据面

- HTTP：`httpx.AsyncClient` 长生命周期连接池。
- HTML：selectolax/lxml/BeautifulSoup。
- 主体抽取：Trafilatura/Readability/Crawl4AI/CSS/XPath/JSON-LD/OpenGraph 组合 Adapter。
- Browser：Playwright/Crawl4AI Browser Runtime Pool。
- PDF：PyMuPDF/pypdf + OCR fallback。
- 对象存储：S3/MinIO。
- 全文检索：OpenSearch。
- 向量：pgvector 起步，规模扩大后可替换专用 Vector Store。
- AI/TTS：独立 Adapter + Worker Pool。
- 可观测：OpenTelemetry + Prometheus/Grafana + 日志聚合。

## 6. 总体架构

```text
Source Registry
  -> Coverage Providers
  -> Durable URL Candidate / Evidence
  -> Change-Signal Plane
  -> Admission & Security
  -> Metadata Probe / Priority Hint(optional)
  -> Priority Adapter
  -> Budget Admission / Dispatch Reservation
  -> Durable Frontier
  -> Route Decision
  -> HTTP / Browser / Platform API / PDF
  -> Immutable Snapshot
  -> Multi Extractor Candidates
  -> Canonical IR + Quality
  -> Identity + Append-only Document Version
  -> Current Projection
  -> Markdown / Assets / Search / Embedding
  -> Selection/Input Manifest
  -> Derived Job / AI / TTS
  -> Integration Job / Workflow Execution
  -> Delivery Job
  -> Dify / n8n / Agent / Newsletter / Webhook

                   ^
                   |
         Capability Observation
                   |
         Route Capability Projection
```

控制面创建和管理持久事实，不直接执行长期 I/O/GPU 任务。第三方 crawler 内部 frontier、batch、prefetch 只能作为单 Task/Worker 的实现细节，不能成为平台唯一 Candidate、Coverage、Priority 或 Budget 真相。

## 7. Release Registry 与可重复运行

所有会改变结果的行为都必须版本化：

```text
command_schema_release
config_resolver_release
site_profile_release
discovery_provider_release
discovery_priority_policy_release
priority_adapter_release
budget_policy_release
metadata_probe_policy_release
fetch_engine_release
representation_provider_release
route_policy_release
extractor_release
postprocess_release
quality_policy_release
identity_policy_release
asset_policy_release
markdown_projection_release
chunker_release
search_provider_release
reranker_release
embedding_release
ai_model_release
ai_recipe_release
tts_model_release
tts_recipe_release
workflow_recipe_release
workflow_sandbox_policy_release
integration_api_release
integration_callback_policy_release
delivery_recipe_release
```

每个 Run、Task、Snapshot、Version、Artifact、Integration Job 都记录实际使用 Release Ref。Release 只追加不原地修改；升级走 draft → test/replay → shadow/canary → publish → rollback。

## 8. 核心数据模型

### 8.1 Source / Profile / Provider

`source` 保存站点身份和归属；`site_profile_release` 保存域名、canonical 规则、允许路径、排除路径、Provider 配置、Selector、Browser Recipe、限流、语言、默认 route 等。

`coverage_provider` 表示独立的历史枚举来源，例如：

- Sitemap / Sitemap Index
- RSS/Atom/JSON Feed
- CMS/平台 API
- Archive/Category/Tag/Author pagination
- Docs navigation
- `llms.txt`
- 普通内部链接
- 人工 seed/import

Provider 记录枚举边界、cursor、oldest/newest、expected/discovered、gap、blocker、confidence 和 evidence。

### 8.2 Candidate / Evidence

```text
url_candidate
- id / source_id
- normalized_url
- url_fingerprint
- first_seen_at / last_seen_at
- current_admission_state

discovery_evidence
- candidate_id
- provider_id
- parent_candidate_id(optional)
- observed_at
- depth / anchor / rel / source_position
- provider_payload_ref
- evidence_hash
```

同一 URL 可有多个 Evidence，不能因为去重而丢失“它从哪里被发现”。

### 8.3 Snapshot / IR / Document / Version

```text
network_snapshot
- request/response metadata
- final_url / redirect_chain
- headers/status/mime
- body_object_key / body_hash
- rendered_dom_ref(optional)
- fetch_engine_release

canonical_ir
- structured blocks
- metadata + provenance
- canonical_ir_hash
- extractor/postprocess releases

document
- stable logical identity
- source/canonical identity

document_version
- document_id
- canonical_ir_ref/hash
- valid_from / observed_at
- quality_outcome
- append-only
```

Current Projection 只指向当前 Accepted Version，可随时从版本历史重建。

### 8.4 Run / Task / Reservation

Run 保存 requested/effective config、Release、预算、状态、统计和 lineage。Task 使用 lease/heartbeat；Worker 崩溃后任务可被安全重新领取。

Budget Reservation：

```text
budget_reservation
- id
- run_id / task_id / integration_job_id
- dimension: REQUEST|BYTE|TIME|COST|BROWSER_SLOT|MODEL_TOKEN|DELIVERY
- amount
- status: RESERVED|DISPATCHED|SETTLED|RELEASED|EXPIRED
- idempotency_key
- lease_until
- actual_usage
```

## 9. Source Intake 与站点 Onboarding

新增 Source 时执行：

```text
Normalize Domain
 -> robots/policy check
 -> DNS/IP/SSRF admission
 -> HEAD/GET lightweight probe
 -> detect Feed/Sitemap/API/llms.txt
 -> infer platform/CMS
 -> discover archive/category/docs navigation
 -> sample representative URLs
 -> HTTP extraction test
 -> conditional Browser test
 -> build Site Profile Draft
 -> Golden URL validation
 -> publish Profile
 -> create FULL_BACKFILL
```

自动推断只能生成 Draft。任何高风险 Browser Action、自定义 JS、宽松 allow pattern 必须人工审阅或经过 Golden Replay 门禁。

## 10. 历史全量发现与 Coverage

### 10.1 Provider 优先级

推荐发现顺序：

1. Sitemap Index/Sitemap；
2. 平台/CMS API；
3. RSS/Atom/JSON Feed；
4. Archive/Category/Author/Docs Navigation；
5. `llms.txt`/公开文档 manifest；
6. 普通站内链接 crawl；
7. 外部搜索仅用于补洞和提示，不作为唯一 Coverage 来源。

### 10.2 Coverage 不是抓取成功率

对每个 Provider 保存：

- 枚举方法与边界；
- cursor/page range/date range；
- oldest/newest observed；
- expected/discovered count；
- duplicate/filtered/admission denied；
- known gaps/blockers；
- confidence；
- 最近 reconcile 时间。

`FULL_BACKFILL` 结束条件必须基于 Provider exhaustion/evidence，而不是只看队列清空。

### 10.3 Reconcile

周期运行 `RECONCILE`：重新枚举关键 Provider，比较新旧 candidate set、日期边界和 manifest hash，发现遗漏、删除、迁移、canonical 改变或 Provider 漂移。

## 11. Change-Signal Plane 与增量同步

增量同步先判断“值得抓正文吗”，再派发正文 Fetch：

1. Feed GUID/updated/new item；
2. Sitemap `lastmod`；
3. API updated_at/cursor；
4. Conditional GET：ETag/Last-Modified；
5. 轻量 metadata probe；
6. 正文抓取后 body hash；
7. 提取后 Canonical IR hash。

每个 Source 维护动态调度间隔：活跃站点缩短，长期无变化站点延长，但必须保留最大静默检查周期。

使用重叠窗口避免 Feed/API 时间边界漏数据；重复项依靠 URL/content identity 幂等去重。

若 body 变化但 Canonical IR 不变，只记录 Freshness Observation，不触发 Chunk/Embedding/AI/TTS 重算。

## 12. Admission、安全与访问策略

所有 URL 在入队和 redirect 后都重新检查：

- scheme 仅允许 http/https；
- IDNA/host normalize；
- DNS 解析和 private/link-local/loopback/cloud metadata 阻断；
- redirect hop/目标限制；
- robots 和站点访问政策；
- MIME/Content-Length/实际字节上限；
- 压缩炸弹防护；
- 下载 deadline；
- 禁止泄漏内部 header/credential。

Browser Worker 使用隔离容器、最小文件权限、NetworkPolicy 和独立 egress；不复用管理员登录浏览器 Session。

External callback/webhook URL 同样属于 SSRF 面，必须通过预注册 Destination、DNS/IP 校验和 allow policy，不能在每次 Job 中接受任意内网 URL。

## 13. Priority 与 Metadata Probe

Priority 只决定“先处理谁”，不决定“历史全集里有没有它”。

可用信号：

- Provider 类型和可靠度；
- URL/path pattern；
- depth/anchor/link location；
- 日期/更新时间；
- 主题相关度；
- head title/description/canonical；
- 人工业务优先级。

任何需要访问候选 URL 才能得到的信号都必须走 `METADATA_PROBE`：

```text
Candidate
 -> probe budget reservation
 -> limited-byte HEAD/GET
 -> immutable Probe Snapshot/cache
 -> Priority Evidence
 -> Priority Adapter
 -> normalized_priority
```

禁止第三方 `score`、n8n `priority: 10` 或底层 queue key 直接穿透到平台调度。

## 14. Durable Frontier、预算与公平调度

调度层同时考虑：

- normalized priority；
- per-source/per-host token bucket；
- tenant quota；
- HTTP/Browser worker capacity；
- retry/backoff；
- run fairness；
- global dispatch budget；
- bytes/time/cost budget。

派发流程：

```text
Task READY
 -> acquire host/global concurrency token
 -> reserve request/byte/time/cost budget
 -> mark reservation DISPATCHED
 -> worker starts side effect
 -> settle actual usage/outcome
 -> release token
```

Reservation 持久化并有 TTL/lease；重复投递由稳定 idempotency key 保证同一逻辑 attempt 不重复扣减。

`queued / reserved / dispatched / started / completed / transport_success / accepted` 分开统计。取消 Run 后，未 dispatch 的 Reservation 释放；已经 in-flight 的请求记为 wasted in-flight，不伪装成“从未发出”。

## 15. Route Decision 与抓取策略

### 15.1 HTTP-first

默认 HTTP 获取原始响应并存 Snapshot。成功条件不是 HTTP 200，而是后续抽取和 Quality 通过。

### 15.2 Browser 升级

仅当出现以下证据时升级：

- JS shell/正文为空；
- 正文依赖客户端渲染；
- 需要有限交互才能暴露公开内容；
- HTTP Extractor 连续质量失败且 Browser Golden 样本更好。

Browser Recipe 版本化保存：wait selector、scroll 次数、click selector、viewport、locale、timeout 等；禁止站点无差别无限滚动。

### 15.3 Platform API / Fast Path

对 WordPress/Ghost/Substack/Docs 平台等，若公开 API 或结构化端点能更完整、更稳定获取数据，可优先使用，但最终仍转成统一 Snapshot/IR/Version Contract。

### 15.4 PDF/OCR

PDF 先保留原文件 Snapshot，再提取文本和结构；只有文本层不足时使用 OCR。OCR 结果带 runtime/model/version 和页级 provenance。

## 16. Snapshot、Extraction、Canonical IR 与 Markdown

### 16.1 Snapshot 是重处理基础

至少保存：

- request URL/final URL/redirect chain；
- status/headers/MIME；
- 原始 body；
- 可选 rendered DOM；
- fetch 时间、route、runtime release；
- body hash。

规则升级时优先从 Snapshot 离线 replay，避免重新抓站点。

### 16.2 多 Extractor Candidate

同一 Snapshot 可并行/串行生成：

- JSON-LD/OpenGraph metadata；
- CSS/XPath site-specific；
- Trafilatura；
- Readability；
- Crawl4AI extraction；
- 平台结构化 payload。

Quality Policy 对 Candidate 评分并选 Accepted Candidate，保留候选和决策证据。

### 16.3 Canonical IR

IR 不以 Markdown 字符串为唯一结构，推荐：

```text
DocumentIR
- title/author/published_at/updated_at/tags/canonical
- language
- blocks[]
  - heading(level,text)
  - paragraph
  - code(language,text)
  - list(items,nesting)
  - quote
  - table(rows/cells)
  - image(asset_ref,alt,caption)
  - formula
- outbound_links[]
- metadata_provenance{}
```

### 16.4 Markdown Projection

Markdown 由 `markdown_projection_release` 从 Accepted IR 确定性生成：

- 保留代码 fence 和语言；
- 保留列表层级；
- 表格按统一规则输出；
- 图片改写到归档 Asset URL/相对路径；
- Front Matter 使用固定 schema；
- 不做会破坏代码/公式/表格的通用 whitespace collapse。

禁止某个 n8n 工作流自行 `cleaned_html -> Markdown`，另一个直接用 crawler `result.markdown`，导致同一版本产生两个知识真相。外部只能获取平台生成的 Markdown Artifact Ref。

## 17. Identity、版本与删除

### 17.1 URL Identity

URL normalization 处理：scheme/host case、default port、fragment、追踪参数、规则化 query、已知 canonical 规则。原始 URL 始终保留。

### 17.2 Document Identity

综合：

- canonical URL；
- platform post ID/GUID；
- stable slug/path；
- title/date/source；
- content similarity。

自动 merge 只在高置信度执行；不确定时生成候选供人工确认。

### 17.3 Version

Canonical IR hash 变化才创建新 Document Version。字段变化也要保留 provenance。Current Projection 指向最新 Accepted Version。

源站 404/410/Feed 移除不能立即物理删除历史版本；记录 source state/tombstone。只有合规或人工删除请求才走受审计删除流程。

## 18. Asset 归档

图片、附件和可下载资源独立 Asset Pipeline：

```text
Asset Candidate
 -> admission
 -> download budget
 -> immutable object
 -> hash/mime/dimension
 -> optional metadata cleanup
 -> asset version/ref
 -> Markdown rewrite
```

防止 data URI、超大图片、跨站跟踪像素、恶意文件。资源失败不应让正文 Version 丢失，但 Quality 显示 missing asset，并支持 `ASSET_REPAIR`。

## 19. Quality、Drift 与人工治理

Quality 至少检查：

- title/正文是否存在；
- 字数/段落数；
- boilerplate 比例；
- 导航/页脚污染；
- code/table/list 保真；
- published_at 合理性；
- canonical/domain 合理性；
- 重复正文；
- 语言；
- 资源缺失；
- Snapshot 与 IR 一致性。

状态建议：`ACCEPTED / REVIEW / REJECTED / BLOCKED`。

每个 Source 保存 Golden URL 集，Profile/Extractor/Projection 升级先 Snapshot Replay，对比 title、正文 hash、结构块数量、代码/表格、metadata 和 Markdown diff。

Drift 指标包括 accepted rate、selector miss、正文长度分布、browser escalation、canonical 变化、provider discovered 数异常、同步延迟。异常只生成 Profile Draft/建议，不能自动发布高风险规则。

## 20. Search、RAG 与知识消费

Accepted Version 异步生成：

- Markdown；
- chunk；
- 全文索引；
- embedding；
- topic/tag/summary 等 Derived Artifact。

Chunk 以 IR block 为基础，优先按标题层级和语义边界切分；代码块、表格尽量整体保留。Chunk 记录 `document_version_id + block range + chunker_release`。

检索推荐 Hybrid Search：BM25 + Vector + metadata filter + optional reranker。所有命中回到不可变 Version/Chunk Ref，RAG 答案可追溯到具体文档版本。

Embedding 模型升级创建新 `embedding_release`，后台重建新 projection；切换 alias 后旧索引再回收。

## 21. Derived Artifact、AI、Digest 与 TTS

AI 不修改 Source Truth，只生成 Derived Artifact：

```text
derived_artifact
- type: SUMMARY|TAG|TOPIC|FAQ|DIGEST|REPORT|...
- input_manifest_id / input_version_refs
- recipe_release
- model_release
- prompt/template hash
- output object ref/hash
- token/cost/latency
- quality/outcome
```

批量消费必须先冻结 Manifest：

```text
Selection Policy
 -> Selection Manifest(version refs + order + hash)
 -> per-document Summary(optional/reusable)
 -> Reduce/Digest
 -> TTS(optional)
 -> Delivery
```

失败重试复用同一 Manifest；若重新选文则显式生成新 Manifest。

派生缓存键：

```text
hash(
  input_version_refs
  + manifest_hash
  + recipe_release
  + model_release
  + projection_release
  + normalized_args
)
```

相同输入/Release 可复用已有 Artifact，替代外部工作流中的临时 pin data。

## 22. Workflow / Dify / n8n / Agent Integration

### 22.1 集成边界

长期 Source Sync 与外部业务工作流彻底分离：

```text
Source Sync -> Accepted Version -> Artifact/Manifest
                                 -> Integration Gateway
                                 -> n8n/Dify/Agent
                                 -> Business Workflow
                                 -> Delivery/Sheets/Drive/Email
```

外部工作流可以请求 Targeted Fetch、Search、Artifact、Derived Build，但不能：

- 直接调用 `http://crawl4ai:...`；
- 持有内部 crawler task id；
- 直接写 PostgreSQL/S3 真相层；
- 用自身 Loop 并发绕过 per-site/global budget；
- 直接指定底层 queue priority；
- 自己生成“官方 Markdown 真相”。

### 22.2 External Consumer

```text
external_consumer
- id / tenant_id / name
- auth_client_ref
- scopes
- quota_policy_release
- allowed_operations
- allowed_destination_ids
- enabled
```

典型 scope：`integration:submit`、`integration:read`、`artifact:read`、`search:read`。n8n 可以继续持有 Google Sheets/Drive 等业务系统自己的凭据，但不获得知识库真相层凭据。

### 22.3 Integration Job Contract

```text
integration_job
- id / tenant_id / consumer_id
- operation
- idempotency_key
- input_manifest_id / manifest_hash
- requested_artifacts
- requested/effective config
- completion_policy
- budget_class
- status
- deadline_at
- submitted/started/terminal timestamps

integration_job_item
- id / job_id / caller_item_key
- normalized_url or input_version_ref
- run_id / task_id(internal only)
- status / typed outcome
- document_version_id
- artifact_refs
- error_class
```

示例：

```json
POST /v1/integration-jobs
{
  "operation": "TARGETED_FETCH",
  "idempotency_key": "consumer-run-2026-08-15-001",
  "input_manifest": {
    "items": [
      {"key": "row-1", "url": "https://example.com/a"},
      {"key": "row-2", "url": "https://example.com/b"}
    ]
  },
  "requested_artifacts": ["DOCUMENT_VERSION", "MARKDOWN"],
  "completion_policy": {
    "mode": "BEST_EFFORT",
    "deadline_seconds": 900
  },
  "callback_destination_id": "dest_123"
}
```

响应：

```json
{
  "integration_job_id": "ij_123",
  "status": "QUEUED",
  "status_url": "/v1/integration-jobs/ij_123"
}
```

外部 API 只暴露稳定平台状态；Crawl4AI/Playwright/HTTPX 可在内部自由替换。

### 22.4 Input Manifest 与批量去重

提交后冻结不可变 Input Manifest：

- 保留 caller item key 方便 n8n 回填原行；
- URL normalize/canonicalize 后去重；
- 同一 Job 内相同逻辑 URL 只产生一个抓取 item，多 caller key 可引用同一结果；
- manifest hash 参与幂等、缓存和审计；
- `TARGETED_FETCH` URL 输入与 `DERIVED_BUILD` Version Ref 输入使用不同 typed schema；
- 大批次 Item 结果分页读取，避免单个 status 响应过大。

### 22.5 服务端 fan-out 与 Completion Barrier

外部提交一个 batch 后，平台内部通过 durable scheduler fan-out；外部 workflow 不承担抓取调度。

Completion Policy：

- `ALL`：全部 item 到终态；
- `BEST_EFFORT`：deadline 到达后返回成功/失败明细；
- `MIN_SUCCESS(n)`：达到最低成功数量即可触发下游；
- `DEADLINE`：时间预算优先，剩余 item 进入取消/后续处理。

Job 状态：`QUEUED | RUNNING | PARTIAL | SUCCEEDED | FAILED | CANCELLED | EXPIRED | BLOCKED`。Item 保存更细 Typed Outcome，不能只用一个 `completed` 布尔值吞掉失败原因。

### 22.6 Polling 协议

不采用固定 5 秒无限轮询。Status API 支持：

- `Retry-After`；
- ETag/If-None-Match；
- 可选长轮询；
- 客户端指数退避 + jitter；
- absolute deadline；
- terminal state；
- cancel API；
- 429/503 明确 retry semantics。

轮询本身有 consumer QPS quota，防止大量 n8n execution 形成 polling storm。

### 22.7 Callback/Webhook

终态或重要事件可异步 callback。Callback 不直接执行临时 HTTP，而落为持久 Delivery：

```text
callback_delivery
- integration_job_id
- destination_id
- event_id / event_type
- payload_hash
- attempt / next_retry
- status
- response_fingerprint
```

要求：HMAC 签名、timestamp、replay window、幂等 event id、connect/read deadline、response byte limit、指数退避+jitter、最大重试、dead-letter。Destination 预注册并做 SSRF/egress policy。

Callback 默认只带 job id、状态、counts、manifest hash 和轻量结果引用，不推送大段文章正文；消费者再通过 scoped Artifact API 获取正文。

### 22.8 稳定结果与 Artifact API

Integration Item 默认返回：

- `document_version_ref`；
- `markdown_artifact_ref`；
- canonical/title/published_at 等轻量 metadata；
- Quality Outcome；
- lineage summary；
- error class。

原始 HTML/Rendered DOM 只通过受权限控制的诊断 Snapshot API 获取。这样底层 crawler 的 `cleaned_html`、`markdown`、metadata shape 不会泄漏成公共协议。

### 22.9 Workflow Recipe 与沙箱

外部/托管 Workflow 的 DSL/Graph 改动生成新 Workflow Recipe Release。生产执行保存 WorkflowExecution/NodeExecution：实际 Code/Tool/Plugin/Model、输入输出 Artifact hash、Secret scope、network policy decision、latency/cost、Outcome、trace。

Code/Plugin 节点使用默认拒绝网络、最小文件系统、CPU/memory/time limit；需要互联网访问时走明确 egress policy，抓网页仍必须走 Integration Gateway，而不是给代码节点通用公网权限。

## 23. TTS 与 Delivery

TTS 默认消费 Summary/Digest，不消费大量完整正文；限制输入 token/字符、目标时长、deadline、模型/音色 Release、GPU 配额、失败重试和成本预算。

Audio Artifact 保存对象 key/hash、duration、mime、model/recipe/runtime attestation。

通用 Delivery Job：

```text
delivery_job
- trigger
- input artifact / manifest
- destination adapter release
- recipe release
- idempotency_key
- status / attempt / next_retry
- response fingerprint
- error class
```

Webhook、邮件、对象存储、外部 API 都使用统一 retry/dead-letter/audit 模型。

## 24. Web 管理端

### 24.1 Source Intake

- 新增单站点/批量/OPML/CSV；
- 自动探测结果；
- robots/policy 状态；
- Provider 建议；
- Golden URL；
- 创建 FULL_BACKFILL。

### 24.2 Typed Run Console

每个按钮明确映射 Run Type，例如 Full Backfill、Incremental、Reconcile、Targeted Fetch、Replay、Reindex。提交后展示 requested/effective config、默认值/Profile/Policy/Override 来源、resolver release、Run ID、validation warning。

### 24.3 Site Profile Studio

编辑 URL/Provider/Selector/metadata/remove rule、HTTP/Browser route constraint、Golden URL；同一 Snapshot 多策略预览 Candidate/IR/Markdown；支持 Draft、Replay Test、Shadow、Publish、Rollback。

### 24.4 Provider Coverage

显示 Provider、枚举边界、oldest/newest、cursor、expected/discovered、known gap、blocker、confidence，并可触发 Reconcile。

### 24.5 Focused Discovery / Onboarding Explorer

输入 root URL + 可选主题 + 页面/时间/dispatch 预算，只创建持久 `FOCUSED_DISCOVERY` Run。Web 用 SSE/WebSocket/轮询展示 observed、scored、probe reserved/dispatched、budget admitted、fetch reserved/dispatched/started/completed、accepted 数量及候选证据。

Focused Discovery 只能帮助快速找 Golden URL/文章 pattern/Archive/Feed/Sitemap/Browser Recipe，UI 必须明确“Focused 结果不是 Coverage 完整率”。

### 24.6 Run / Task / Replay / Quality

展示 Route Decision、Capability、requested/effective config、Budget/Reservation、lease/retry/dead-letter、Snapshot Replay、Priority Replay、Quality/Drift、Search/RAG/AI lineage、token/cost/truncation、Workflow Node trace 和 Delivery。

### 24.7 Integration Runs / Consumers

展示：

- external consumer、scope、quota；
- Integration Job/Input Manifest；
- Job/Item 状态与 caller key；
- requested/effective config；
- budget reserved/dispatched/settled；
- polling rate/Retry-After；
- callback attempt/dead-letter；
- Artifact Ref；
- idempotency 命中；
- cancellation/deadline/partial success；
- 外部工作流版本与平台 Release lineage。

管理员可重放 callback，但不能通过 UI 绕过原 Job 权限扩大 Artifact scope。

## 25. Observability、Audit 与 Health

主链路 Trace：

```text
Command
 -> Run
 -> Candidate/Evidence
 -> PriorityEvidence
 -> BudgetReservation
 -> Task
 -> RouteDecision
 -> Fetch
 -> Snapshot
 -> Extraction
 -> Quality
 -> Identity
 -> Version
 -> Projection
 -> Manifest/IntegrationJob
 -> DerivedJob/WorkflowExecution
 -> Artifact
 -> Delivery
```

技术指标：queue lag、latency、timeout、retry、browser crash、DB/Redis/S3 error、response-too-large、cancellation、pool utilization、lease expiration。

业务指标：discovered/candidate、budget admitted/denied、reserved/dispatched/started/completed、transport success、accepted、new version/unchanged、coverage gap、sync lag、asset missing。

Focused Discovery：first-accepted latency、candidate-to-dispatch ratio、raw/normalized priority、Coverage starvation。

Metadata Probe：request/cache/bytes/p95/budget exhausted。

Integration：job submitted/running/partial/terminal、item success/error、poll QPS、Retry-After adherence、callback success/retry/dead-letter、manifest size、idempotency hit、consumer quota exhausted、deadline/cancel ratio。

预算指标至少区分 limit、reserved、settled、expired、overrun detected、wasted in-flight。任何“成功计数未超限但 dispatched 超限”必须告警。

Profile/Release/Override/Manual Merge/Targeted Fetch/Focused Discovery/Metadata Probe/Replay/Quality Override/Selection Build/Derived Build/Workflow Preflight/Sandbox Override/Integration Submit/Callback Replay/Delivery Retry/Budget Override 全部写 Audit。

Health 区分 liveness、readiness、dependency_degraded、critical_dependency_unavailable。搜索/AI/TTS/Destination 故障可降级；PostgreSQL/S3 等任务所需真相源不可用时 fail-closed。

## 26. 缓存策略

可缓存：robots、Provider manifest、Capability Projection、Metadata Probe、conditional metadata、短期 Fetch dedupe、Search result、Derived Artifact lookup、Integration status 热读。

缓存原则：

- 真相永远可从 PG/S3 恢复；
- timeout 严格、重试有限；
- cache key 带必要 Release/Input Version/Manifest；
- Priority 依赖的 Probe Snapshot 可缓存事实，但 normalized priority 必须带 Priority Policy/Adapter Release；
- Integration Job 状态缓存失效时回源 PG，不允许 Redis 成为 job 唯一状态。

## 27. 失败分类与升级

统一错误至少覆盖：

- DNS/连接/读取超时；
- deadline；
- response too large；
- HTTP 4xx/5xx；
- rate limit；
- robots/policy/SSRF block；
- WAF challenge；
- JS shell；
- empty content；
- selector miss；
- quality low；
- unsupported type；
- browser crash；
- parser/storage/search/model/TTS error；
- workflow/sandbox/egress violation；
- delivery/callback error；
- cancelled/expired；
- command/config validation；
- priority contract violation；
- budget exhausted/reservation conflict/dispatch overrun；
- integration consumer unauthorized/quota exhausted；
- invalid manifest；
- callback replay/signature/destination block。

只有适合升级的错误才进入下一 route：`JS_SHELL` 可 HTTP → Browser；`ROBOTS_BLOCKED/POLICY_BLOCKED` 不升级；`RATE_LIMITED` 延迟重试；AI/TTS/Workflow/Delivery 错误不反向标记 Source Sync 失败。

## 28. 测试、Replay 与发布门禁

1. **Contract Test**：Adapter 输入输出 schema、错误分类、timeout、取消传播、字节边界、provenance。
2. **Golden Extraction Test**：典型文章、代码、表格、图片、分页、动态页。
3. **Snapshot Replay Test**：新 Extractor/PostProcess/Markdown Release 在旧 Snapshot 上批量回放。
4. **Coverage Test**：对已知小站点验证 Sitemap/Feed/Archive 枚举边界和 gap 计算。
5. **Priority Contract Test**：用 pinned 第三方 runtime 验证 raw score 到 normalized priority 的实际顺序。
6. **Budget Concurrency Test**：并发/batch/retry/cancel 下 dispatched 永不越过 reservation policy。
7. **Idempotency Test**：重复 Command、重复队列消息、重复 Integration Submit 不产生重复逻辑版本/扣费。
8. **Crash Recovery Test**：Worker 在 fetch/extract/publish 各阶段退出，lease 回收后继续。
9. **Browser Pool Test**：崩溃、泄漏、超时、Session 隔离和资源上限。
10. **Integration API Contract Test**：外部协议不暴露 crawler task/status shape；API Release 升级兼容。
11. **Polling Test**：Retry-After、ETag、长轮询、deadline、429 下不会形成忙轮询。
12. **Callback Test**：HMAC、replay window、SSRF、timeout、重试、幂等、dead-letter。
13. **Barrier Test**：ALL/BEST_EFFORT/MIN_SUCCESS/DEADLINE 的 partial success 语义。
14. **Projection Determinism Test**：同 IR + 同 Release 必须生成相同 Markdown hash。
15. **Load Test**：按真实站点分布模拟 1000 Source，验证 host fairness、queue lag、PG/S3/Redis、Browser 配额。

发布策略：shadow → canary source → 分批 source → 全量；任何 Quality/Coverage/Budget/Integration 契约回归自动停止扩大范围。

## 29. 部署与资源隔离

推荐 Kubernetes/等价编排：

- API/Web；
- Scheduler/Dispatcher；
- Discovery Worker；
- Metadata Probe Worker；
- HTTP Fetch Worker；
- Browser Worker；
- Extraction/Projection Worker；
- Asset Worker；
- Search/Embedding Worker；
- AI/TTS Worker；
- Integration/Callback Worker；
- Delivery Worker。

不同 Worker 使用独立 Deployment/Queue/Resource Limit。Browser、OCR、Embedding、LLM/TTS 不与轻量 HTTP Worker 混部，避免重任务拖垮核心抓取。

数据库大表按 source/time/hash 等实际查询模式分区；对象存储使用内容 hash 和 immutable key；OpenSearch/Vector 仅保存 Projection，可删除重建。

## 30. 分阶段实施

### Phase 1：可用的历史抓取主链路

- Source/Profile；
- Sitemap/Feed/Archive Provider；
- URL Candidate/Evidence；
- PostgreSQL + Redis Streams + Outbox；
- HTTP Fetch + Snapshot；
- Trafilatura/Readability/CSS Extractor；
- Canonical IR；
- Quality；
- Document/Version；
- Markdown Projection；
- 基础 Web Source/Run 页面。

目标：先稳定抓一批代表性站点并证明可恢复、可重放、Markdown 可重复。

### Phase 2：1000 站点扩展与增量

- Change-Signal Plane；
- Conditional GET；
- Durable Budget Reservation；
- per-host fairness；
- Capability/Route Decision；
- Browser Pool；
- Reconcile/Coverage；
- Site Profile Studio/Golden Replay；
- Drift/告警。

### Phase 3：资产与检索

- Asset Pipeline；
- OpenSearch；
- pgvector/Embedding；
- Hybrid Search；
- Chunk lineage；
- Reindex/Re-embedding；
- Search/RAG Web 页面。

### Phase 4：Derived Artifact 与 AI

- Selection/Input Manifest；
- Summary/Tag/Topic/FAQ；
- Digest Map/Reduce；
- Derived cache；
- Token/cost budget；
- AI Recipe/Model Registry。

### Phase 5：Integration / Workflow / Delivery

- External Consumer/scopes/quota；
- Integration Job/Item；
- Input Manifest；
- Targeted Fetch/Search/Artifact API；
- Polling Retry-After/ETag/long-poll；
- signed callback + dead-letter；
- Completion Barrier；
- Workflow Recipe/Preflight/Sandbox；
- Dify/n8n Adapter；
- Integration Web Console；
- Delivery Job。

### Phase 6：TTS 与高级治理

- TTS Worker；
- Typed Audio Artifact；
- Recipe/Selection/Delivery Studio；
- 多租户/RBAC 强化；
- 自动 Drift 建议；
- 大规模成本优化与数据生命周期策略。

## 31. 验收标准

### 31.1 历史完整性

- 每个 Source 至少有可查看的 Provider Coverage 状态；
- 已知小样本站点可证明候选枚举边界；
- FULL_BACKFILL 结束报告包含 provider exhaustion/gap/blocker，不只给成功数量。

### 31.2 增量

- 新文章能在目标 SLA 内被发现；
- unchanged 内容不重复创建 Version/Embedding/AI；
- Feed/API/Sitemap 边界通过重叠窗口不漏项；
- 周期 Reconcile 能发现增量通道漏掉的候选。

### 31.3 数据质量

- Golden 集 title/body/code/table/image 结构通过率达到上线阈值；
- 同 Snapshot + 同 Release 重放输出 hash 稳定；
- 每个 Markdown 可追溯到 Document Version、Canonical IR 和 Snapshot。

### 31.4 可靠性

- Worker crash 后任务可恢复；
- 重复消息不重复发布逻辑 Version；
- 并发 dispatch 不突破 Budget Reservation；
- 单站点故障不会阻塞其他站点；
- Browser/AI/TTS 故障可隔离降级。

### 31.5 Integration

- n8n/Dify 不需要知道内部 crawler hostname/task id；
- 重复相同 idempotency key 不创建重复 Job；
- 大 batch 由平台 fan-out，并遵守 per-host/global budget；
- polling 遵守 Retry-After/deadline；
- callback 支持签名、重试、幂等和 dead-letter；
- ALL/BEST_EFFORT/MIN_SUCCESS/DEADLINE 行为可测试；
- 同一 Accepted Version 的 Markdown 对所有外部消费者一致；
- Sheets/Drive 等只作为派生交付目标，不影响 Source Truth。

## 32. 最终架构决策

这个系统不是“一台 Crawl4AI 把 1000 个站点爬下来”，也不是“Dify/n8n 每次需要内容时临时抓网页”，而是长期运行的数据同步、版本和派生平台。

核心事实边界如下：

- **Coverage** 回答应该有哪些历史文章；
- **Candidate/Evidence** 回答实际观察到了哪些 URL、从哪里发现；
- **Priority Hint** 只回答预算内先处理谁；
- **Budget Reservation** 决定真实副作用是否允许派发；
- **Capability/Route** 决定怎样抓更可靠、更便宜；
- **Snapshot/Canonical IR/Version** 回答当时抓到了什么、如何解释、内容是否变化；
- **Markdown/Search/Embedding** 是可重建 Projection；
- **Selection/Input Manifest** 固化每次批量消费的不可变事实输入；
- **Derived Artifact** 保存摘要、报告、音频等派生结果；
- **Integration Job** 是 Dify/n8n/Agent 使用平台能力的稳定异步边界；
- **Workflow Recipe/Preflight/Sandbox** 约束外部或托管编排逻辑；
- **Delivery Job** 负责可靠交付和重试。

对 1000 站点长期知识库，始终坚持：**Coverage Candidate 决定历史全集而 Priority 只能改变处理顺序；已经观察到的链接先成为可审计 Evidence，再应用评分和 fetch budget；第三方 score 经过版本化 Priority Adapter；任何联网评分进入受预算约束的 Metadata Probe；所有真实网络/模型/交付副作用在 dispatch 前取得持久 Reservation；Accepted Document Version 是事实输入，Markdown/搜索/AI/TTS 是可重建派生；Dify/n8n/Agent 是受控消费者与业务编排器，不是抓取调度器和内容真相源；外部只依赖 Integration Job、Manifest、Typed Status 和 Artifact Ref，不依赖 Crawl4AI task_id/内部 host/result shape；所有配置、规则、模型和 Workflow 都必须可版本化、可审计、可重放、可回滚。**
