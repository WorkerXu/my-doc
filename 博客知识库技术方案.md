# 博客知识库技术方案

## 1. 建设目标与规模

建设一个长期运行、可横向扩展、可审计、可回放的技术博客知识库平台。首期接入约 1000 个技术博客、工程博客、技术文档站与公开 Newsletter，尽可能抓取完整历史文章并清洗为稳定 Markdown；后续持续增加网站，并低成本完成增量同步、版本管理、搜索/RAG、知识派生、Web 管理和离线重处理。

目标规模：

- 1000+ Source，可扩展到数千站点；
- 百万级 Document / Document Version；
- 数百万到千万级 URL Observation；
- 千万级 Chunk / Embedding；
- HTML、PDF、Markdown、TXT、JSON/API、RSS/Atom/JSON Feed；
- 中英日韩等多语言；
- HTTP 为默认抓取路线，Browser 为受控 fallback；
- CMS API、Sitemap、Feed、Archive、Category/Tag、Docs TOC、Common Crawl、受控 Deep Crawl 等多入口历史发现；
- PostgreSQL + Object Storage 保存事实；Markdown、全文索引、向量索引、Graph、AI Insight、Export、Dashboard Read Model 均为可重建 Projection；
- Web UI、n8n、MCP、CI、Agent、外部系统只能通过受控 API / Event 边界驱动平台。

平台必须能回答：

1. 一个站点理论上有哪些历史文章，为什么认为历史已经基本覆盖；
2. 一个 URL 从哪里被发现、如何规范化、为何被过滤或放行、经过哪些跳转、最终抓到了什么；
3. 为什么某 URL 走 HTTP、平台 API、文件流、Crawl4AI Browser 或 Playwright；
4. 一个 Markdown 来自哪个 Snapshot、Extractor、Release 和 Document Version；
5. 源站变化后哪些内容真正变化，哪些派生物需要重建；
6. Worker 崩溃、批任务部分失败或外部自动化重启后如何恢复且不丢成功项；
7. 新增第 1001 个站点时，能否主要依靠配置、Probe 与审核接入，而不是增加站点专用代码；
8. 全量历史抓取是否有 durable checkpoint，能否从断点继续；
9. 百万 URL 批处理是否保持有界内存，并支持逐 URL 提交；
10. Browser 是否被复用且安全隔离，何时回收，是否因资源压力自动降并发；
11. 第三方 crawler API 升级后，是否只修改 Adapter 而不污染平台数据契约；
12. Markdown 导出到本地/S3/Git 后，是否仍能稳定映射回平台内部 Document Version；
13. 一条 AI Insight 到底来自哪个原文版本、哪些 block、哪个模型和 Prompt；
14. 两篇内容被判断为“同文转载”或“相关主题”时，依据是什么，是否可逆；
15. 自动研究发现的新站点为什么进入知识库、消耗多少预算、是否会影响受管理站点 Coverage。

---

## 2. 核心设计原则

1. **Config First**：普通站点靠 Source / Site Profile / Release 接入，不修改核心代码。
2. **Coverage First**：历史完整性由 Provider Evidence、cursor、exhaustion reason 和 known gap 证明，队列为空不等于完整。
3. **Authoritative First**：CMS/API、Sitemap、Feed、Archive 优先，Common Crawl/Deep Crawl 用于补洞和交叉验证。
4. **Discovery Before Fetch**：先发现、Observation、规范化、过滤、分类、去重，再抓正文。
5. **Durable Streaming Frontier**：大站 URL 发现必须流式、小批持久化，禁止把全站 URL 一次性物化在内存。
6. **Filter With Evidence**：Filter 可 HARD/SOFT/DEPRIORITIZE，但任何决定都保存规则、原因和证据。
7. **URL Is Locator**：资源类型由响应 MIME、签名、跳转和站点规则判断，不能只看扩展名。
8. **HTTP First, Browser Last**：静态 HTML、Feed、API、Sitemap 优先 HTTP；Browser 只处理动态正文、交互和质量 fallback。
9. **Reuse Browser, Isolate Session**：复用 Browser/Crawler 进程降低成本，但 Cookie、LocalStorage、认证态和代理身份按 Source/Profile/安全域隔离。
10. **Adapter Is Boundary**：Crawl4AI/Playwright/HTTP 客户端只输出平台内部稳定契约，第三方 DTO、字段名和版本变化不得泄漏到事实层。
11. **Platform Permit Before Local Dispatcher**：平台全局/domain/source 配额是并发上限；Crawl4AI Dispatcher/Semaphore 只负责 Worker 内二级自适应控制。
12. **Bounded Window + Commit Per URL**：Worker 只 lease 有界窗口，流式返回结果；每个 URL 成功立即落库，不等待整批完成。
13. **Snapshot First**：先保存不可变网络事实，再做抽取、清洗、Markdown、Chunk、Graph、AI。
14. **IR First, Markdown as Projection**：Canonical IR / Document Version 是内容事实层，Markdown 可确定性重建。
15. **Full Canonical Before Fit Markdown**：Pruning/BM25/LLM 等有损过滤只能产生 FILTERED/ENHANCED Projection，绝不能覆盖 Canonical Markdown。
16. **Recipe, Not Site Code**：交互、路由、Filter、Schema、Selector、Extractor 均声明式、版本化、可回放。
17. **Stable Identity**：URL Alias、Document、Version、Chunk、Embedding、Relation、Knowledge Note、Export 身份分离且稳定。
18. **Release Everything**：影响结果的 Profile、Provider、Filter、Adapter、Recipe、Extractor、Markdown、Chunk、Embedding、Graph、Relation、Model、Prompt、Research、Metric、Webhook、Export、Runtime 全部版本化。
19. **Idempotent + Recoverable**：任务有幂等键、lease、fencing token、checkpoint、retry、partial success。
20. **One Durable Task System**：PostgreSQL durable state + Transactional Outbox + Redis Streams 是唯一业务任务体系。
21. **External Automation Is Adapter, Not Truth**：n8n/MCP/CI/Agent 不能成为 Coverage、Task、Snapshot、Version 或 retry 状态真相。
22. **Projection Is Rebuildable**：全文、向量、Graph、AI、Relation、Export、Dashboard 都可从事实层离线重建。
23. **Generation Instead of Destructive Rebuild**：索引/图谱/语义关系/AI 派生新版本后台构建、验证、原子切换。
24. **Security on Every Hop**：起始 URL、Redirect、Browser navigation、Webhook endpoint 每跳重新做 SSRF/Egress/Scope 校验。
25. **Source Friendly**：吞吐最大化不是唯一目标，robots、站点策略、分层限流和退避优先于速度。
26. **Benchmark Before Switch**：Adapter、Filter、Extractor、Markdown、Embedding、Graph、Retrieval、Metric 升级先固定回归，再激活。
27. **Source Truth != AI Derivation**：摘要、Insight、FAQ、实体、研究问题不能写回 Canonical IR 或 Document Version。
28. **User Note != AI Insight**：用户自己的判断/批注与模型生成内容必须有不同 origin、权限和审计。
29. **Similarity Is Evidence, Not Identity**：向量、标题距离和 LLM 判定只产生关系证据，不能单独触发不可逆 Document 合并。
30. **Collapse Is Projection, Not Destructive Merge**：同文转载可在 UI 中折叠，但事实记录和来源引用保持可恢复。
31. **Research Expansion Is Bounded**：自动研究必须有 max depth/fanout/URL/domain/cost budget 和明确 stop reason。
32. **External Research != Managed Source**：搜索发现的外部 URL 不计入正式 Source Coverage，只有显式 Promote 后才走 PROBE/Backfill。
33. **Hybrid Retrieval First**：代码符号、错误信息、版本号和专有名词必须依靠 BM25/字段检索与 Vector 互补，不能只靠 Embedding。
34. **Provenance to Block Level**：AI Insight、检索片段和知识关系都应尽可能回溯到 Document Version + IR block。

---

## 3. 总体架构与真相边界

```text
                    ┌────────────────────────────────────────────┐
                    │ Web / REST / n8n / MCP / CI / Agent      │
                    └───────────────────┬────────────────────────┘
                                        │ Command / Query
                    ┌───────────────────▼────────────────────────┐
                    │ API + RBAC + Idempotency + Policy         │
                    └───────────────────┬────────────────────────┘
                                        │
                    ┌───────────────────▼────────────────────────┐
                    │ PostgreSQL Truth Store                     │
                    │ Source/Profile/Run/Task/Observation        │
                    │ Coverage/Snapshot/Document/Version         │
                    │ Release/Audit/Outbox/Research Metadata     │
                    └──────────────┬────────────────┬─────────────┘
                                   │                │
                                Outbox            Query
                                   │                │
                        ┌──────────▼────────┐  ┌────▼─────────────┐
                        │ Redis Streams     │  │ Read Models      │
                        │ Task Transport    │  │ Search/Dashboard │
                        └───────┬─────┬─────┘  └──────────────────┘
                                │     │
                     ┌──────────┘     └──────────────┐
                     ▼                               ▼
             Discovery Workers                Fetch Workers
      API/Sitemap/Feed/Archive/          HTTP Pool / Browser Pool
      CC/Deep Crawl/Surface              Crawl4AI / Playwright
                     │                               │
                     ▼                               ▼
            URL Observation ──> Durable Frontier ──> Snapshot
                                                       │
                                                       ▼
                                           Extract / Canonical IR
                                                       │
                  ┌──────────────────┬─────────────────┼──────────────────┐
                  ▼                  ▼                 ▼                  ▼
          Canonical Markdown   Chunk/Search       Structured/Graph   Export Sink
                                                       │
                                                       ▼
                                           Derived Knowledge Plane
                                     Summary/Insight/User Note/Relation
                                                       │
                                                       ▼
                                           Research Expansion Plane
                                     Search Candidate / External Evidence
                                                       │
                                              explicit Promote only
                                                       ▼
                                                Managed Source
```

真相边界：

```text
PostgreSQL + Object Storage = Truth
Redis Streams              = Transport
Crawler internal frontier  = Execution state only
Crawl4AI dispatcher        = Worker-local execution control only
n8n execution              = External automation state only
OpenSearch / Vector Store  = Rebuildable Projection
Graph Store                = Rebuildable Projection
Canonical Markdown         = Rebuildable Projection from IR
Filtered/Enhanced Markdown = Rebuildable Derived Projection
AI Insight / Relation      = Rebuildable Derived Knowledge
Dashboard Read Model       = Rebuildable Projection
SSE/WebSocket/Webhook      = Notification/Delivery only
Browser/session cache      = Ephemeral execution state
```

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST/Command API、Schema 校验、Webhook Receiver、内部服务接口；
- **PostgreSQL**：Source、Profile、Run、Task、Coverage、Observation、Snapshot metadata、Document、Version、Release、Audit、Outbox、Knowledge/Research metadata；
- **Transactional Outbox**：保证业务数据库事务与异步消息发布一致；
- **Redis Streams**：consumer group、retry transport、backpressure，只做任务运输；
- **React/Vue + TypeScript**：Web 管理台；
- **Docker/Kubernetes**：按 Discovery / HTTP Fetch / Browser / Extract / Projection / Research Worker 独立扩缩容。

### 4.2 抓取与解析

- **HTTPX/aiohttp**：静态 HTML、Feed、Sitemap、API、Conditional GET、Range GET；
- **Crawl4AI**：URL Seeder、`arun_many()` 流式多 URL 抓取、Browser Interaction、Markdown/Structured Extraction Adapter、Worker 内 Dispatcher；
- **Playwright**：复杂 JS 与特殊交互 fallback；
- **lxml/BeautifulSoup/CSS/XPath/Regex**：确定性抽取；
- **Trafilatura/Readability**：通用正文候选；
- **PDF parser + OCR fallback**；
- **S3/MinIO**：raw response、HTML、Rendered DOM、PDF、WARC、Canonical IR、Markdown、Structured JSON、Graph/AI artifact、fixture。

### 4.3 检索、知识派生与研究扩展

- **OpenSearch**：BM25、字段过滤、聚合、Alias/Generation；
- **pgvector/OpenSearch kNN**：首期向量；规模增长后可切 Qdrant/Milvus；
- **RRF/Controlled Fusion**：BM25 + Vector 基线；
- **Cross Encoder Reranker**：可选；
- **Graph**：首期 PostgreSQL 关系表验证价值，高阶图查询再增加 Neo4j/Memgraph Adapter；
- **LLM Gateway**：统一 Provider、模型、结构化输出、token/cost/latency 计量；
- **Search Provider Adapter**：Exa/Brave/SearXNG/其他 Web Search 都通过稳定 Adapter 接入研究扩展层。

### 4.4 可观测与运营

- Prometheus + Grafana + OpenTelemetry；
- PostgreSQL incremental/materialized read model；
- 数据规模进一步增长后可增加 ClickHouse；
- SSE/WebSocket：Web UI 实时状态；
- Signed Webhook：外部系统事件交付。

---

## 5. Release、Adapter 与 Generation 治理

至少版本化：

```text
site_profile_release
provider_release
url_normalization_release
url_filter_release
candidate_classifier_release
resolver_release
resource_probe_release
fetch_route_release
fetch_adapter_release
browser_pool_policy_release
interaction_recipe_release
extractor_release
quality_release
identity_release
markdown_release
filtered_markdown_release
structured_schema_release
structured_extraction_release
chunk_release
embedding_release
vector_index_release
graph_ontology_release
graph_projection_release
relation_release
retrieval_release
analysis_release
knowledge_derivation_release
model_release
prompt_release
research_query_release
research_admission_release
search_provider_release
webhook_policy_release
export_sink_release
ops_metric_release
runtime_artifact_release
```

`runtime_artifact_release` 固定 Crawl4AI、Playwright、Browser Revision、Python、解析器、模型 digest、系统依赖和镜像 digest；`fetch_adapter_release` 固定第三方 DTO 到平台内部 `FetchAdapterResult` 的转换语义。

第三方抓取库升级：

```text
new runtime/adapter release
 -> fixture regression
 -> old/new normalization diff
 -> raw/fit markdown hash diff
 -> canary
 -> activate
```

全文、向量、Graph、Relation、AI Derived Knowledge 均使用 Generation：

```text
BUILDING -> VALIDATING -> READY -> ACTIVE -> RETIRED
```

新 Generation 后台构建、固定 judgment/fixture 验证、canary，再通过 Alias/逻辑指针原子切换。切换 Projection 不修改 Document Version。

---

## 6. Source、Site Profile 与新站 PROBE

### 6.1 Source

```text
source
- id
- tenant_id
- name
- entry_url
- registrable_domain
- allowed_origins
- source_type
- language_hint
- status
- robots_policy
- schedule_policy_id
- active_profile_release_id
- origin: MANAGED | PROMOTED_RESEARCH
```

### 6.2 Site Profile Release

```text
site_profile_release
- source_id
- version
- scope_rules
- provider_config
- url_normalization_release_id
- url_filter_release_id
- resolver_policy
- resource_probe_policy
- fetch_route_policy
- fetch_adapter_policy
- browser_pool_policy_id
- interaction_recipe_release_ids
- extractor_policy
- quality_policy
- markdown_policy
- chunk_policy_id
- schedule_policy
- rate_limit_policy
```

### 6.3 新站 PROBE

新增站点先低成本探测：

1. URL/domain/origin、DNS/IP、robots、SSRF/egress；
2. Sitemap Index / Sitemap；
3. RSS/Atom/JSON Feed；
4. WordPress/Ghost/Substack 等平台 API；
5. Archive、Category/Tag/Author、Blog Index、Docs TOC、pagination；
6. Common Crawl URL Index；
7. URL path/template 聚类；
8. HTTP link sample；
9. 小预算 Best-First/BFS；
10. HTTP 与 Browser 正文质量抽样；
11. wrapper/short-link/meta refresh/JS redirect；
12. HTML/PDF/MD/JSON Resource Probe；
13. 动态页面生成 Interaction Recipe；
14. 重复 DOM 生成 Selector/Schema 候选；
15. 自动生成 Profile Draft，Web 审核后启用。

新增站点的理想路径是“配置 + Probe + fixture 验证”，而不是新增 crawler 代码。

---

## 7. 全量历史发现、Coverage 与 Durable Frontier

### 7.1 Provider Contract

```text
probe(source, profile) -> capability
discover(cursor, budget) -> candidates + evidence + next_cursor
checkpoint() -> durable_cursor
exhaustion() -> explicit_reason
```

Provider：

```text
CMS_API
SITEMAP
FEED
ARCHIVE
CATEGORY_TAG_AUTHOR
DOCS_TOC
DISCOVERY_SURFACE
BROWSER_DISCOVERY_SURFACE
COMMON_CRAWL_URL_INDEX
SITE_SEARCH
DEEP_CRAWL_BFS
DEEP_CRAWL_DFS
DEEP_CRAWL_BEST_FIRST
```

历史发现优先级：

```text
CMS/API
 -> Sitemap/Sitemap Index
 -> Historical Feed
 -> Archive year/month
 -> Category/Tag/Author
 -> Blog Index / Docs TOC
 -> Common Crawl
 -> HTTP/Browser Discovery Surface
 -> Best-First/BFS gap filling
 -> Site Search supplement
```

Deep Crawl 是 gap discovery，不是 Coverage 真相。

### 7.2 Coverage 事实

```text
coverage_provider_run
- run_id
- source_id
- provider_type
- provider_release_id
- cursor_before
- cursor_after
- status
- exhaustion_reason
- candidate_count
- committed_observation_count
- completed_count
- failed_count
- known_gap
- blocker
- evidence_ref
```

预算耗尽只能标记：

```text
known_gap = true
exhaustion_reason = BUDGET_EXCEEDED
```

只有主要权威 Provider exhausted，或剩余缺口均成为可解释、可枚举的 known gap，`FULL_BACKFILL` 才能结束。

### 7.3 Sitemap Index 分片与 checkpoint

```text
sitemap_shard_state
- source_id
- sitemap_url
- parent_sitemap_url nullable
- etag
- last_modified
- content_hash
- parse_cursor
- observation_commit_cursor
- state
- last_success_at
- provider_release_id
```

处理：

```text
conditional fetch child sitemap
 -> stream parse URLs
 -> observation mini-batch commit
 -> outbox/task publish
 -> advance durable observation cursor
 -> finish shard
```

Provider checkpoint 只能在 Observation 已持久化后推进。

### 7.4 Durable Streaming Frontier

禁止全站 URL 一次性进内存。推荐：

```text
Provider page/shard
 -> 100~1000 URL mini-batch
 -> Observation commit
 -> Normalize/Filter/Classify
 -> Durable Task
 -> next page/shard
```

优先级：

```text
P0 new feed/API item
P1 manual/targeted fetch
P2 incremental high-yield URL
P3 historical authoritative provider
P4 historical gap/deep crawl
P5 reprocess/reindex/AI/research optional work
```

维护 `frontier_low_watermark`、`frontier_high_watermark`、`max_pending_per_source`、`max_pending_per_domain`、`max_observation_commit_lag`；积压超过高水位时暂停非紧急 Discovery。

---

## 8. URL Observation、Normalization、Filter 与 Resolution

### 8.1 Observation First

```text
url_observation
- source_id nullable
- observation_origin: MANAGED_DISCOVERY | EXTERNAL_RESEARCH
- observed_url
- provider_type
- provider_run_id nullable
- research_run_id nullable
- parent_url nullable
- anchor_text nullable
- depth nullable
- observed_at
- evidence_ref
```

Filter 不能删除“曾经发现过这个 URL”的事实。

### 8.2 Normalization

全局安全规则：host lowercase、default port removal、fragment removal、path slash、安全 percent-encoding、query key ordering、tracking param removal。

站点规则：semantic query allowlist/denylist。禁止全局删除 query；`?id=123`、`?p=456`、`?page=7` 可能有业务语义。

### 8.3 URL Filter Chain

```text
HARD_REJECT
SOFT_REJECT
DEPRIORITIZE
ACCEPT
```

每次决策保存 `filter_release_id`、`rule_id`、`action`、`reason_code`、`score`、`evidence_ref`、`decided_at`。新 Filter Release 上线前对已知文章 URL fixture 做 recall 回归。

### 8.4 Classification / Resolution / Resource Probe

Candidate：

```text
ARTICLE_CANDIDATE
DISCOVERY_SURFACE
ASSET
NAVIGATION
EXTERNAL_REFERENCE
IGNORE_BY_POLICY
UNKNOWN
```

Resolution：

```text
Observed URL
 -> Normalized Candidate
 -> HTTP Redirect
 -> Wrapper Param / HTML Target
 -> Meta Refresh
 -> Browser Navigation Fallback
 -> Resolved Target
```

每一跳重新执行 DNS/IP/SSRF/origin scope 校验。`canonical`/`og:url` 是 Identity Evidence，不等同于 redirect。

Resource Probe 综合 Content-Type、Content-Disposition、magic bytes、Content-Length、HTML 壳、Range GET、redirect 和 Site Profile，产出 HTML/PDF/Markdown/Text/JSON/XML/Image/Archive/Unknown Binary 类型。

---

## 9. Scheduler、Task、幂等与可靠性

Run Type：

```text
PROBE
FULL_BACKFILL
INCREMENTAL
RECONCILE
MANUAL_INGEST
TARGETED_FETCH
REPROCESS
RECHUNK
REEMBED
REINDEX
REGRAPH
RERELATE
REDERIVE_KNOWLEDGE
RESEARCH_EXPANSION
RETRIEVAL_EVAL
RECIPE_VALIDATE
FILTER_VALIDATE
ADAPTER_VALIDATE
EXPORT_REBUILD
OPS_METRIC_REBUILD
```

Task State：

```text
PENDING
LEASED
RUNNING
SUCCEEDED
RETRY_WAIT
FAILED_TERMINAL
CANCELLED
```

Task 包含 `lease_owner`、`lease_until`、`fencing_token`、`attempt`、`checkpoint`、`deadline`、`budget_reservation`。

关键幂等键：

```text
DISCOVER:       source + provider_release + cursor/shard
FILTER:         url + filter_release
RESOLVE:        input_url + resolver_release
RESOURCE_PROBE: resolved_url + probe_release + epoch
FETCH:          url + route_release + adapter_release + runtime_release + fetch_epoch
EXTRACT:        snapshot + extractor_release
IDENTITY:       canonical_ir + identity_release
MARKDOWN:       document_version + markdown_release
FILTERED_MD:    document_version + filtered_markdown_release + input_hash
CHUNK:          boundary_id + chunk_release
EMBED:          chunk/note + embedding_release
INDEX:          projection + generation
GRAPH:          boundary_id + graph_projection_release + input_hash
RELATE:         subject + object + relation_release + evidence_manifest_hash
DERIVE:         document_version + derivation_type + knowledge_derivation_release + input_manifest_hash
RESEARCH:       seed + research_query_release + admission_release + budget_hash
EXPORT:         sink + document_version + markdown_release + export_release
WEBHOOK:        subscription + event_id
```

批任务必须 partial success：成功事实立即持久化，失败项独立 retry。外部执行器 job id 只能映射到平台 Task，不能替代 Task 真相。

---

## 10. Fetch Plane：路由、稳定 Adapter、流式批处理与 Browser Pool

### 10.1 Fetch Route

```text
HTTP_STATIC
 -> HTTP_ALT_HEADERS
 -> HTTP_PLATFORM_API
 -> FILE_STREAM_FETCH
 -> BROWSER_CRAWL4AI_RECIPE
 -> BROWSER_PLAYWRIGHT_CUSTOM
 -> SITE_CUSTOM_EXTRACTOR
```

站内 URL 可按 matcher 使用不同 route/extractor。

### 10.2 Stable Adapter Contract

```text
FetchAdapterResult
- requested_url
- final_url
- redirect_chain[]
- success
- status_code
- response_headers
- content_type
- body_ref/raw_html_ref nullable
- cleaned_html nullable
- raw_markdown nullable
- fit_markdown nullable
- markdown_with_citations nullable
- references_markdown nullable
- metadata nullable
- links[]
- media[]
- extracted_content nullable
- error_class nullable
- error_message nullable
- dispatch_metrics nullable
- runtime_release_id
- adapter_release_id
```

原则：

- 版本字段差异由 Adapter 消化；
- requested URL、final URL、redirect chain 分开保存；
- title 优先结构化 metadata/H1/JSON-LD/CMS 字段并保存 confidence；
- 第三方 `fit_markdown` 只能作为候选派生数据；
- `links` 必须回流 Observation，不由 Fetch Worker 无界递归；
- 缺字段、未知字段、类型漂移产生显式 normalization error。

### 10.3 Bounded Window Streaming

```text
lease N tasks
 -> group by route/profile/domain
 -> acquire platform permits
 -> HTTP batch or Crawl4AI arun_many(stream=True)
 -> normalize each result
 -> each result completes independently
 -> persist Snapshot/Attempt/links
 -> ACK current URL
 -> refill below low watermark
```

N 是几十/数百量级执行窗口，不是全量历史 URL 数。

### 10.4 Browser Pool

长期存活池按 `runtime_release + profile/security_scope + proxy_scope` 分组。Browser Process 可复用，认证态不跨安全边界共享。

回收条件：`max_pages_per_browser`、`max_browser_age`、RSS 超阈值、crash/page error 超阈值、Runtime Release 切换、drain/部署、context 泄漏或清理失败。

### 10.5 两层并发控制

平台 permit：

```text
Global -> route -> registrable-domain -> source -> host
```

Worker 本地：MemoryAdaptiveDispatcher / Semaphore / RateLimiter / browser page ceiling。本地控制只能收缩，不能突破平台上限。

### 10.6 新链接不能直接递归抓

```text
URL Observation
 -> Normalize
 -> Scope
 -> Filter
 -> Classify
 -> Dedupe
 -> Budget/Priority
 -> Durable Task
```

---

## 11. Rate Limit、Retry 与端到端 Backpressure

每 Source/Profile 配置 `max_rps`、`max_concurrency`、`burst`、`max_browser_seconds_per_hour`、`max_bytes_per_hour`、`max_pages_per_run`、`retry_budget`，同 registrable domain 的多个 Source 共享域配额。

Retry：

- 429/503：解析 `Retry-After`，退避并降低并发；
- timeout/connection reset：exponential backoff + jitter；
- 401/403/404/410：按原因分类，不盲目重试；
- robots/policy：终止；
- Browser crash：换实例后有限重试；
- HTTP 正文质量不足：可触发 Browser fallback；
- Extractor 失败：优先从 Snapshot 离线重试。

背压指标：`fetch_queue_lag`、`extract_queue_lag`、`snapshot_upload_lag`、`DB_commit_p95`、`object_store_p95`、`worker_RSS`、`browser_RSS`、`429_503_rate`、`fetch_error_rate`。

---

## 12. Snapshot、Extraction、Canonical IR 与 Markdown

### 12.1 Snapshot First

```text
snapshot
- snapshot_id
- fetch_attempt_id
- source_id nullable
- requested_url
- final_url
- snapshot_origin
- status_code
- content_type
- body_hash
- object_ref
- headers_hash
- rendered_dom_ref nullable
- runtime_release_id
- adapter_release_id
- captured_at
```

Extractor/Filter/Markdown/Graph/AI 升级优先对旧 Snapshot/Document Version replay，不重新访问源站。

### 12.2 Extraction Candidate

同一 Snapshot 可产生：CMS/API body、Site Selector、CSS/XPath/Regex、JSON-LD、Trafilatura/Readability、Crawl4AI cleaned HTML/raw Markdown、Rendered DOM、Feed Content、PDF Text/OCR、Markdown/TXT native parse。

### 12.3 Canonical IR

```text
DocumentIR
  metadata:
    title/authors/published_at/updated_at/language/canonical_url/tags
    field_confidence/source_evidence
  blocks:
    block_id
    heading/paragraph/list/quote/code/table/image/figure/formula
  assets[]
  outbound_links[]
  source_evidence[]
```

`block_id` 在同一 Document Version 内稳定，用于检索命中、AI Insight provenance、Graph/Relation evidence。

### 12.4 Quality

检查正文长度、标题一致性、link density、boilerplate、code/table 保留、language、published_at confidence、truncation、selector match、required field、DOM drift、HTTP 与 Browser 候选差异。低质量进入 quarantine。

### 12.5 Canonical Markdown

从 IR 确定性生成，保留完整主体、标题层级、代码、列表、引用、表格、图片 alt/caption、重要链接；解析相对链接；Front Matter 记录 document/version/source/canonical URL/content hash/release。

### 12.6 Filtered / Fit Markdown

Pruning/BM25/LLM filter 只能产生：

```text
FILTERED_MARKDOWN
ENHANCED_MARKDOWN
```

不得覆盖 Snapshot、Canonical IR、Canonical Markdown。

---

## 13. Document Identity、版本与增量同步

Identity 综合 platform stable ID、canonical/og:url、redirect evidence、Feed GUID、CMS/article ID、normalized URL、exact/near duplicate、title + published time。URL Alias、Document Identity、Document Version 分离。

Document Version 只在 Canonical IR 或关键元数据变化时创建。304、重复抓取、Markdown/Chunk/Embedding/Graph/AI/Export 策略升级不创建正文版本。

增量信号：

```text
Feed/API cursor
 -> Sitemap conditional fetch + lastmod
 -> High-yield Surface
 -> Conditional GET
 -> Metadata/Resource Probe
 -> Full HTTP
 -> Browser fallback
```

保存 ETag、Last-Modified、previous_body_hash、content_length、content_kind。304 不重复保存 body、不重新抽取、不新建 Version、不重做 Projection。

调度：

```text
Tier 0 Feed/API            分钟级
Tier 1 Sitemap/Blog Index  10~60 分钟
Tier 2 Category/Archive    1~6 小时
Tier 3 Deep Reconcile      每周/每月
Tier 4 Common Crawl        新 collection/按月
```

404/410 不物理删除历史 Version，而创建 availability/tombstone 状态。

---

## 14. Markdown Export Projection

Sink：LOCAL_FS、S3_COMPATIBLE、GIT_REPOSITORY、ZIP_EXPORT、WEB_DOWNLOAD。

```text
markdown_export
- export_id
- sink_id
- document_id
- document_version_id
- markdown_release_id
- export_sink_release_id
- path_key
- content_hash
- manifest_hash
- state
- exported_at
```

路径：

```text
{source_slug}/{published_year}/{document_id}-{safe_slug}.md
```

文件名只是 Projection 路径，真实身份始终由 `document_id/document_version_id` 决定。

Front Matter 至少：

```yaml
document_id: ...
document_version_id: ...
source_id: ...
canonical_url: ...
published_at: ...
content_hash: ...
markdown_release: ...
```

Git Sink 按 Source/时间窗口聚合 commit，避免百万文档每篇一个 commit。

---

## 15. Derived Knowledge Plane：事实、AI 派生与用户笔记分离

### 15.1 三类知识对象

```text
SOURCE_FACT
  Snapshot / Canonical IR / Document Version

AI_DERIVATION
  Summary / Insight / FAQ / Entity / Topic / Research Question

USER_NOTE
  Comment / Counterpoint / Hypothesis / Manual Zettelkasten Note
```

AI 结果永远不能覆盖 Source Fact；用户笔记也不能被模型重生成覆盖。

### 15.2 Derivation Run

```text
derivation_run
- id
- input_document_version_id
- derivation_type
- knowledge_derivation_release_id
- model_release_id
- prompt_release_id
- input_manifest_hash
- status
- token_input
- token_output
- cost
- latency_ms
- tool_trace_ref nullable
- output_manifest_hash
- started_at
- completed_at
```

### 15.3 Knowledge Note

```text
knowledge_note
- id
- origin: AI | USER
- note_type: SUMMARY | INSIGHT | QUESTION | COMMENT | COUNTERPOINT | HYPOTHESIS
- title
- body
- source_document_version_id nullable
- source_block_refs[]
- derivation_run_id nullable
- author_user_id nullable
- state: GENERATED | REVIEWED | SUPERSEDED | REJECTED
- content_hash
- created_at
```

AI Insight 必须尽量引用精确 block，而不是只保存模型重写的 snippet。原文新版本产生后，旧 Note 不静默覆盖：保留旧对象并进入 `SUPERSEDED` 或重新评估队列。

### 15.4 生命周期

```text
Document Version READY
 -> async derivation tasks
 -> Summary/Insight/Question
 -> validate structured output
 -> store provenance + cost
 -> optional review
 -> active derived generation
```

派生失败不影响正文 READY 状态。

---

## 16. Semantic Relation：可逆关系层，不做破坏性语义合并

### 16.1 Relation Types

```text
SAME_WORK
REPUBLISHED
TRANSLATION_OF
RELATED
CONTRASTS
SUPPORTS
DERIVED_FROM
```

### 16.2 数据结构

```text
semantic_relation
- id
- subject_type/id
- object_type/id
- relation_type
- confidence
- state: CANDIDATE | CONFIRMED | REJECTED | SUPERSEDED
- relation_release_id
- generation_id
- created_at

semantic_relation_evidence
- relation_id
- evidence_type: EXACT_HASH | NEAR_HASH | EMBEDDING | TITLE | CANONICAL | REDIRECT | CMS_ID | LLM
- value
- score
- evidence_ref
- release_id
```

### 16.3 判定策略

Embedding 只做 candidate generation；Title distance、LLM 只做补充证据。只有可验证、高稳定性的 identity evidence 才能驱动 Document Identity 决策。

推荐两级策略：

```text
Level A identity evidence
  canonical + redirect + CMS/Feed stable id + exact hash + high-confidence near hash
  -> may merge/alias under identity policy

Level B semantic evidence
  embedding + title similarity + LLM
  -> relation candidate only
```

### 16.4 UI Collapse

同文转载可在检索/UI 中折叠成一个 cluster view，但底层 Document、Source、Version、引用保持独立。折叠规则属于 Projection，可回放、可关闭、可升级。

---

## 17. Research Expansion Plane：有界主动研究

### 17.1 目的

允许从 Document/Insight/User Note 生成研究问题，通过 Web Search 发现相关来源，但严格与 Managed Source Sync 分离。

### 17.2 Research Run

```text
research_expansion_run
- id
- seed_type/id
- query_generation_release_id
- search_provider_release_id
- admission_policy_release_id
- max_depth
- max_fanout
- max_searches
- max_new_urls
- max_new_domains
- max_cost
- spent_cost
- status
- stop_reason
- created_at
```

### 17.3 Candidate

```text
research_candidate
- run_id
- url
- domain
- search_rank
- selected_by_model
- novelty_score
- source_diversity_score
- near_duplicate_of nullable
- admission_state: NEW | REJECTED | EXTERNAL_EVIDENCE | PROMOTED
- promoted_source_id nullable
- evidence_ref
```

### 17.4 强制边界

- max depth；
- max fanout；
- max search count；
- max new URLs/domains；
- token/search/fetch cost budget；
- 同域候选占比上限；
- near-duplicate rejection；
- allowed/disallowed domain policy；
- robots/SSRF/egress 与正常抓取相同；
- External Candidate 不计入 Managed Source Coverage；
- 只有显式 `Promote to Source` 才执行 PROBE/Profile/Backfill。

### 17.5 防止 Echo Chamber

候选选择不能只按 embedding similarity 最大化。排序应综合：

```text
relevance
+ novelty
+ domain diversity
+ source authority/evidence
- near duplicate penalty
- same-domain concentration penalty
```

研究扩展只是一种“发现外部证据”的派生能力，不得反向改变原文事实。

---

## 18. Structured Extraction、Chunk、Embedding、Search 与 Graph

结构化抽取优先级：

```text
platform API / JSON-LD / CSS / XPath / Regex
 -> deterministic extractors
 -> LLM only for complex/unstructured fields
```

LLM 使用 JSON Schema/Pydantic，记录 token、latency、cost、validation error。

Chunk 使用 Structure-aware 规则：Heading/Section 优先，段落、列表、引用尽量完整，code/table 默认不拆；Chunk 不跨 Document Version boundary，并保存 heading path/page/block range。

```text
chunk_id = hash(boundary_id + chunk_release_id + block_range + normalized_text_hash)
embedding_id = hash(chunk_or_note_id + embedding_release_id)
```

检索基线：

```text
BM25 + Vector
 -> hard SOURCE/DOCUMENT/VERSION/ACL scope
 -> metadata filters
 -> RRF/controlled fusion
 -> optional relation/graph boost
 -> optional reranker
 -> context assembly with block provenance
```

代码符号、函数名、错误信息、版本号、RFC/CVE 等必须保留 lexical route。Vector 不能成为唯一召回通道。

Graph 是可选第三召回通道，不替代原文/BM25/Vector。只有固定 judgment set 证明增量收益后才默认开启。

---

## 19. AI 审计、成本与可重放

所有 AI/Tool 派生必须能够回答：

- 输入哪个 Document Version/Note；
- 输入哪些 IR block；
- 使用哪个 Model Release；
- 使用哪个 Prompt/Schema Release；
- 调用了哪些工具/Web Search；
- token、latency、费用；
- 输出 hash；
- 是否人工 review/override；
- 模型升级后是否需要重建。

建议统一 `ai_execution_ledger`：

```text
ai_execution_ledger
- derivation_run_id
- provider
- model_release_id
- prompt_release_id
- input_tokens
- output_tokens
- cached_tokens
- tool_calls
- latency_ms
- cost
- error_code
- created_at
```

预算优先级：Source Sync 主链路 > Extract/Quality > Search Index > AI Derivation > Research Expansion。AI/Research 预算耗尽不得阻塞正文同步。

---

## 20. Event、Webhook 与外部自动化

事件：

```text
command.queued
command.started
command.succeeded
command.failed
run.progress
run.completed
source.backfill.completed
document.version.created
markdown.ready
knowledge.derivation.ready
relation.candidate.created
research.run.completed
research.candidate.promoted
markdown.export.ready
quality.quarantined
adapter.normalization.failed
```

事件由业务事务通过 Outbox 产生。

Webhook：at-least-once、`event_id` 稳定、HMAC-SHA256、timestamp + delivery id 防重放、2xx 才 ACK、exponential backoff + jitter、超限 DLQ、支持 replay；endpoint 做 SSRF/egress policy。

n8n 适合运营编排，不适合成为 durable state。MCP Agent 只能调用 Platform API，不直接操作 Redis、Coverage、Task 或对象存储内部状态。

---

## 21. Web 管理功能

### 21.1 Fleet Overview

展示 Source 状态、FULL_BACKFILL completion/known gap、freshness SLA、pending/leased/running/retry queue、changed documents、HTTP/Browser 比例、quality quarantine、Embedding/Graph/AI/Research/Export backlog、index lag、watermark/coverage。

### 21.2 Source / Profile

支持新增/停用 Source、PROBE、Profile Draft/Review/Activate、Feed/Sitemap/API/Common Crawl 配置、Route/Filter/Extractor/Adapter diff、调度/限流/预算、Profile Release 回滚。

### 21.3 Coverage / Frontier

展示 Provider cursor、Sitemap shard checkpoint、exhaustion reason、candidate/Observation/unique URL 数、known gap、历史时间覆盖、Provider overlap matrix、frontier pending/oldest age、worker lease window、fetch/extract lag、backpressure 原因。

### 21.4 Browser Pool

展示 Browser Worker、instances、active contexts/pages、RSS、local adaptive concurrency、platform permit ceiling、restart/crash/recycle rate、HTTP -> Browser fallback ratio、Browser seconds/Source、recycle reason。

### 21.5 Adapter / Runtime

展示 active adapter/runtime release、Crawl4AI/Playwright/Browser revision、normalization error、unknown/missing field rate、fixture pass rate、old/new canary diff、raw/fit Markdown diff。

### 21.6 URL Filter 与单 URL Trace

```text
Observe -> Normalize -> Filter -> Resolve -> Probe -> Route -> Adapter -> Fetch
 -> Snapshot -> Extract -> Quality -> Identity -> Version
 -> Canonical Markdown -> Chunk -> Index -> Derived Knowledge -> Relation -> Export
```

每步查看 input/output hash、release、耗时、错误、evidence、artifact。

### 21.7 内容比较

同一 Document Version 可比较 raw HTML、cleaned HTML、Canonical IR、Canonical Markdown、Filtered/Fit Markdown、Browser 与 HTTP 候选、不同 Extractor/Adapter Release diff。

### 21.8 Derived Knowledge

按 Document 查看 Summary/Insight/Question/User Note，展示 Model/Prompt Release、原文 block 引用、review 状态、成本；支持 Reject/Supersede/Rebuild。

### 21.9 Relation Review

展示 SAME_WORK/REPUBLISHED/RELATED/CONTRASTS 等候选及 exact hash、canonical、embedding、title、LLM 等证据；支持 Confirm/Reject。默认不 destructive merge。

### 21.10 Research Expansion

展示 seed、generated query、depth/fanout、候选 URL、domain diversity、novelty、near duplicate、预算消耗、stop reason；支持 Promote to Source，Promote 后走正常 PROBE。

### 21.11 AI Cost & Audit

按 Source、Document、Model、Prompt、任务类型查看 token/cost/error/replay；从 Insight 一键回到 Document Version/block/Snapshot。

### 21.12 操作审计

暂停、恢复、重跑、REPROCESS、REINDEX、RERELATE、REDERIVE、RESEARCH、EXPORT、切 Release 全部走 Command API，生成 command id、actor、reason、idempotency key 和 audit log。

---

## 22. 安全、合规、可观测与存储

### 22.1 SSRF / Egress

每跳解析 URL、验证 scheme、DNS resolve、拒绝 private/link-local/loopback/metadata IP、验证 allowed origin/domain；Redirect 与 Browser navigation/request interception 重新验证。

### 22.2 Browser Session 隔离

- tenant/source/profile 安全域隔离；
- 不跨域共享 Cookie/LocalStorage；
- Secret 只经 Secret Manager 注入；
- 日志禁止输出 Cookie、Authorization、API key；
- screenshot/HTML 按 Object Policy 管理。

### 22.3 抓取策略与版权

- robots 与站点 policy 可配置且默认尊重；
- 不默认绕过认证、付费墙或访问控制；
- per-domain rate limit；
- 保存来源 URL、抓取时间和 provenance；
- 对需要长期完整转载的第三方内容，确认版权/许可证与内部使用范围；
- Research Expansion 与 Managed Source 均使用相同合规策略。

### 22.4 核心指标

```text
freshness_lag_seconds
provider_checkpoint_lag
coverage_known_gap_count
frontier_depth
frontier_oldest_age
observation_commit_rate
fetch_success_rate
http_to_browser_fallback_rate
extract_queue_lag
snapshot_upload_p95
DB_commit_p95
worker_RSS
browser_RSS
adaptive_concurrency_current
platform_permit_ceiling
429_503_rate
browser_recycle_rate
adapter_normalization_error_rate
quality_quarantine_rate
derivation_success_rate
derivation_cost_per_document
relation_candidate_accept_rate
research_cost_per_run
research_new_domain_count
research_duplicate_rate
```

质量指标包括 title match、empty/too-short、truncation、code/table preservation、published_at confidence、identity conflict、Markdown deterministic rebuild hash match、Insight block provenance completeness。

### 22.5 数据存储与保留

PostgreSQL 大表按时间/tenant/source hash 分区；常用索引围绕 `source_id + state + scheduled_at`、`normalized_url_hash`、`document_id + version`、`task idempotency key`、`derivation_run_id`、`research_run_id`。

Object Storage：

```text
snapshots/{source_id_or_external}/{yyyy}/{mm}/{snapshot_id}
ir/{document_id}/{version_id}.json
markdown/{document_id}/{version_id}/{release}.md
projection/{type}/{generation}/...
derived/{document_id}/{version_id}/{generation}/...
research/{run_id}/...
```

对象 immutable；Snapshot/Canonical IR 按策略长期保留；Derived Projection 可重建并按 generation 淘汰。

备份：PostgreSQL PITR、Object Storage versioning/replication、Release/Profile/Schema 双重审计，并定期 restore drill。

---

## 23. 关键 API 与数据表

关键 API：

```text
POST /sources
POST /sources/{id}/probe
POST /sources/{id}/backfill
POST /sources/{id}/sync
POST /sources/{id}/pause
POST /sources/{id}/resume
GET  /sources/{id}/coverage
GET  /sources/{id}/frontier
GET  /sources/{id}/browser-stats
GET  /sources/{id}/adapter-stats
POST /ingest/url
GET  /urls/{id}/trace
POST /urls/{id}/retry
GET  /documents/{id}
GET  /documents/{id}/versions
GET  /documents/{id}/artifacts
POST /documents/{id}/reprocess
GET  /documents/{id}/knowledge-notes
POST /documents/{id}/derive
GET  /relations
POST /relations/{id}/confirm
POST /relations/{id}/reject
POST /research-runs
GET  /research-runs/{id}
POST /research-candidates/{id}/promote
POST /profiles/{id}/validate
POST /profiles/{id}/activate
POST /filters/{id}/validate
POST /adapters/{id}/validate
POST /extractors/{id}/validate
POST /generations/{id}/activate
```

长任务统一返回 `202 Accepted + command_id`，由 Event/SSE/Webhook 通知完成。

关键数据表最小集合：

```text
source
site_profile_release
provider_state
coverage_provider_run
sitemap_shard_state
url_observation
normalized_url
url_filter_decision
url_alias
run
task
task_attempt
fetch_attempt
snapshot
document
document_version
canonical_ir_artifact
projection_artifact
markdown_export
derivation_run
knowledge_note
semantic_relation
semantic_relation_evidence
research_expansion_run
research_candidate
ai_execution_ledger
release_registry
outbox_event
event
webhook_subscription
webhook_delivery
audit_log
```

Redis、crawler frontier、Browser Pool 内存状态都不是这些表的替代品。

---

## 24. 初始容量与部署

按站点类型拆 Worker：

```text
静态/Feed/API 站点         -> HTTP Worker
轻度 JS                    -> HTTP + Browser fallback
重度 JS/交互               -> Browser Worker 专属队列
PDF/OCR                     -> CPU/OCR Worker
Embedding/Graph/AI          -> 低优先级 Projection Worker
Research Expansion          -> 独立预算/并发 Worker
```

起步模板，最终由压测调整：

- 每 domain 并发 1~4；
- 每 Source 并发 1~8；
- Browser Worker context/page 上限远低于 HTTP 并发；
- Browser adaptive ceiling 根据 RSS 自动收缩；
- Discovery mini-batch 100~1000 URL；
- Fetch lease window 20~200 URL；
- Research Worker 默认低并发，强制 run budget；
- Redis Stream backlog 只做运输观测，任务状态以 PostgreSQL 为准。

---

## 25. 实施阶段

### Phase 1：可用的全量历史库

Source/Profile、CMS/Sitemap/Feed/Archive Provider、URL Observation + Durable Frontier、HTTP Fetch、Snapshot、Extract + Canonical IR + Canonical Markdown、Document Identity/Version、Local/S3/Git Export、Web Source/Coverage/URL Trace、基础增量同步。

验收：30~50 个结构差异大的站点可恢复全量，Coverage 可解释，Markdown 可重建。

### Phase 2：动态站与规模化执行

Crawl4AI Browser Adapter、稳定 `FetchAdapterResult`、`arun_many(stream=True)` bounded window、Browser Pool、MemoryAdaptiveDispatcher/Semaphore 二级并发、Domain/Source permit、Interaction Recipe、Adapter fixture/canary、1000 Source 分层调度。

验收：长时间运行无明显内存爬升；Worker 重启可恢复；crawler 升级只改 Adapter；高峰 backlog 可通过背压消化。

### Phase 3：质量与检索

Extractor fixture、Quality quarantine、Filtered/Fit Markdown、Chunk/Embedding/OpenSearch、Hybrid Retrieval、Generation 切换。

验收：Canonical Markdown 完整性与检索 Projection 解耦；Filter/Embedding 升级不重新抓源站。

### Phase 4：Derived Knowledge 与 Relation

Derivation Run、AI/User Knowledge Note、block-level provenance、Semantic Relation Candidate、Relation Review、AI Cost/Audit。

验收：AI 输出不可修改 Source Fact；模型升级可并行重建；相似关系误判不会导致不可逆文档合并。

### Phase 5：Bounded Research Expansion

Research Query、Search Provider Adapter、Candidate Admission、Novelty/Diversity、Budget/Depth/Fanout、Promote to Source。

验收：外部研究 URL 不污染正式 Coverage；预算和深度可证明终止；Promote 后统一进入 PROBE/Backfill。

### Phase 6：高级能力

Graph、n8n/MCP、Signed Webhook、高级运营分析/ClickHouse。高级能力不得反向阻塞 Source Sync 主链路。

---

## 26. 关键验收测试

### 26.1 全量历史与恢复

- Sitemap Index 中途 Worker 崩溃，checkpoint 从已提交 Observation 继续；
- 百万 URL 发现保持有界内存；
- Redis 丢消息可由 PostgreSQL Task/Outbox 恢复；
- Browser Worker 重启不丢平台任务。

### 26.2 增量同步

- ETag/Last-Modified 304 不创建新 Version；
- Feed overlap 捕获迟到文章；
- Sitemap lastmod 错误时 periodic reconcile 仍可发现真实变化；
- 404/410 保留历史版本。

### 26.3 Adapter 与 Markdown

- crawler runtime 升级固定 fixture old/new diff；
- 第三方字段漂移不会改变事实表 schema；
- Canonical Markdown 可从 IR 确定性重建；
- fit markdown 不能覆盖 canonical。

### 26.4 Insight Provenance

- 每个 AI Insight 关联有效 Document Version；
- `source_block_refs` 可定位 Canonical IR；
- 新 Document Version 不静默覆盖旧 Insight；
- AI 失败不影响 Document READY。

### 26.5 Relation Safety

构造同文转载不同标题、同标题不同正文、同主题不同观点、多语言翻译等 fixture。验证 embedding/title/LLM 只产生 relation candidate；只有稳定 Identity Evidence 才可影响身份。

### 26.6 Research Budget

构造可递归研究问题，验证达到 max depth、max URLs、max domains、max cost 后明确停止；同域过多被 diversity policy 抑制；External Candidate 不计入 Managed Source Coverage。

### 26.7 AI Release Replay

升级 Prompt/Model 后旧 derivation 保留，新 generation 并行计算，差异可审计，切 Active Generation 不修改 Document Version。

---

## 27. 最终推荐运行链路

### 27.1 全量历史

```text
Add Source
 -> PROBE
 -> Profile Review/Activate
 -> authoritative Providers
 -> stream URL Observations
 -> Durable Frontier
 -> bounded Fetch lease window
 -> platform permits
 -> HTTP or Browser Pool
 -> stable Fetch Adapter normalization
 -> per-URL Snapshot commit
 -> Extract / Quality / Canonical IR
 -> Identity / Document Version
 -> Canonical Markdown
 -> Export
 -> async Chunk/Index
 -> async Derived Knowledge/Relation
 -> Coverage reconcile
```

### 27.2 增量同步

```text
Feed/API cursor + overlap
 -> Sitemap Conditional GET
 -> high-yield surfaces
 -> Conditional GET / 304
 -> changed Snapshot only
 -> new Document Version only when canonical content changes
 -> rebuild affected Projections
 -> re-evaluate affected Derived Knowledge/Relations
 -> periodic Reconcile
```

### 27.3 动态页面

```text
HTTP quality insufficient
 -> Browser route
 -> acquire domain/source permit
 -> lease/reuse Browser Pool instance
 -> worker-local adaptive dispatcher
 -> interaction recipe with hard budget
 -> normalize Crawl4AI/Playwright result through Adapter
 -> Snapshot
 -> release context/page
 -> recycle browser by policy
```

### 27.4 知识派生

```text
Document Version READY
 -> Derivation Run
 -> Summary / Insight / Question
 -> block-level provenance
 -> AI cost/audit
 -> semantic relation candidates
 -> optional review
```

### 27.5 主动研究

```text
Document / Insight / User Note
 -> Research Question
 -> bounded Research Expansion Run
 -> Search Provider Adapter
 -> relevance + novelty + diversity ranking
 -> External Research Candidates
 -> evidence ingest under research budget
 -> explicit Promote to Source when valuable
 -> normal PROBE/Profile/Backfill
```

最终架构把“受管理站点同步”“原文事实”“检索投影”“AI 派生知识”“语义关系”“主动研究扩展”明确分层：Coverage、Snapshot、Canonical IR、Document Version 始终是可审计事实；Markdown、Search、Embedding、Graph、AI Insight 和 Relation 都可重建；自动研究有预算、有终止条件、有来源多样性，并且必须显式 Promote 才能进入正式 Source 治理。这样既能支撑首期 1000 个技术博客的全量历史与增量同步，也能在后续不断增加网站、模型和研究能力时保持可扩展、可恢复、可解释。