# 博客知识库技术方案

适用范围：从约 1000 个技术博客、工程博客、技术文档站、Newsletter、CMS 与公开文档站抓取尽可能完整的历史文章，清洗为稳定 Markdown 建设长期知识库；后续持续增加网站，并支持全量回灌、增量同步、Web 管理、版本留存、离线重处理、全文/向量检索、质量审计、图片附件归档，以及 Dify/n8n/Agent/SEO 分析/Newsletter/TTS 等下游消费。整体按百万级文档、数百万到千万级 URL、站点高度异构和多年持续运行设计。

## 1. 建设目标

1. 支持站点根地址、单 URL、批量 URL、RSS/Atom/JSON Feed、Sitemap/Sitemap Index、OPML、CSV、JSON、Markdown/TXT 等来源导入。
2. 自动探测 Sitemap、Feed、公开 API、Archive、Category/Tag/Author、Docs Navigation、普通内链、`llms.txt`/`llms-full.txt`、平台 API 等发现入口。
3. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`PROBE`、`METADATA_PROBE`、`FOCUSED_DISCOVERY`、`REPROCESS`、`RECHUNK`、`REEMBED`、`REINDEX`、`ASSET_REPAIR`、`DERIVED_BUILD`、`CONTEXT_REFRESH`、`DELIVERY_RETRY` 等运行类型。
4. 历史完整性必须由 Provider 级 Coverage Evidence 证明，不能以“队列空了”“达到 max_depth/max_pages/max_urls”代替完整性证明。
5. HTTP-first；只有动态渲染、正文缺失、挑战页、选择器失效或质量失败有证据时才升级 Browser/Crawl4AI/Playwright。
6. Feed/API 中已经提供的正文作为低成本 `ContentCandidate` 使用；质量足够时可以跳过 Browser，但 Feed 不能替代历史 Coverage 证明。
7. 支持站点能力学习，用正文接受率、质量、延迟、成本和 blocker 优化抓取 route，但 Capability 不替代 Coverage。
8. Source/Profile/Discovery/Priority/Budget/Fetch/Extractor/PostProcess/Quality/Identity/Asset/Projection/Search/Embedding/Context/AI/TTS/Workflow/Delivery 全部采用不可变 Release。
9. 使用 Sitemap `lastmod`、Feed GUID/updated、API cursor、ETag、Last-Modified、body hash、Canonical IR hash、重叠窗口与周期 Reconcile 驱动增量同步。
10. 最终知识对象保存标题、作者、发布时间、更新时间、标签、canonical、正文块、代码块、表格、图片、附件、链接关系、Snapshot、字段 provenance 和完整 lineage。
11. 支持 durable frontier、断点续跑、幂等、lease、heartbeat、重试、限流、熔断、公平调度、backpressure、partial success 和 dead-letter。
12. 所有并发 fan-out 必须保留已经成功完成的部分结果；禁止因为批次总超时而把已经完成的站点结果整体丢弃。
13. 保存不可变 Network Snapshot、Rendered DOM、Feed Item Observation、Extraction Candidate、Canonical IR、Accepted Document Version 和质量结果；规则升级优先离线 replay，不重复访问源站。
14. Markdown、Chunk、全文索引、Embedding、摘要、标签、Topic、FAQ、Digest、Newsletter、SEO/内容分析报告、TTS 均为可重建 Projection/Derived Artifact，失败不能阻塞 Source Sync。
15. Web 管理端覆盖 Source Intake、Site Profile、Provider Coverage、Focused Discovery、Capability & Route、Run/Task、Budget、Targeted Fetch、Extraction Preview、Snapshot Replay、Version Diff、Quality/Drift、Assets、Chunk/Embedding、Search/RAG、Context Snapshot、Analysis Manifest、AI Artifact、Integration、Workflow/Delivery、成本和审计。
16. Canonical IR hash 在 Chunk/Embedding/AI/TTS 等昂贵下游之前计算；正文未变化只写 Freshness Observation。
17. Change-Signal Plane 优先通过 Feed/Sitemap/API/HTTP 元数据判断是否可能变化，再决定是否抓正文。
18. 日报/Newsletter/批量问答/SEO 报告必须先冻结 Selection/Input/Analysis Manifest，再执行摘要、RAG、TTS 或外部 Workflow，保证补跑和重放结果可解释。
19. Dify/n8n/Agent 只能通过版本化 Integration Job Contract 使用抓取和派生能力，不得依赖内部 crawler task id、hostname 或第三方 crawler 的返回结构。
20. 所有网络、模型、TTS、外部分析 Provider 和 Delivery 副作用在 dispatch 前必须取得持久 Budget Reservation。
21. Discovery 中的主题相关度、URL pattern、SEO/head metadata、链接位置、人工主题和第三方 score 只能生成 Priority Hint；在 `FULL_BACKFILL`/`RECONCILE` 中不得因低分删除合法 Coverage Candidate。
22. 候选页 `<head>`/有限字节探测必须建模为显式 `METADATA_PROBE`，受缓存、QPS、字节、deadline、SSRF、robots 和总预算约束。
23. Crawl4AI 等第三方 runtime 的版本、配置、返回字段和 score 通过 Adapter 隔离；业务层不直接绑定 `markdown_v2`、内部 queue score 等易变化 API。
24. Chunk ID、Embedding Projection 和 RAG Citation 必须稳定可重放；禁止用随机 UUID 作为唯一业务身份后直接覆盖向量库。
25. 向量库、全文索引和 Markdown 文件不是正文真相源；PostgreSQL + 对象存储中的不可变事实可以重建所有 Projection。

## 2. 非目标与边界

- 不绕过登录、付费墙、验证码、WAF 或明确访问控制。
- 不承诺未知站点仅靠自动探测达到 100% 历史完整；必须展示 coverage confidence、known gap 与 blocker。
- 不用 LLM、Reranker、SEO 分数、URL 关键词、SERP 排名或抓取成功率决定 FULL_BACKFILL 中合法历史文章是否存在。
- Markdown 不是唯一真相；Canonical IR 是内部内容语义真相，Markdown 是确定性投影。
- 不锁死单一爬虫库、浏览器库、向量库、搜索引擎、消息队列、工作流引擎或模型供应商。
- HTTP 200、Browser success、协程正常返回不等于业务内容成功。
- 不把多个 Document Version 的全文拼成一个无结构大字符串作为默认 AI/TTS 输入。
- 不在 Web/Dify/n8n 请求线程同步等待长期 Browser、LLM、OCR、Embedding 或 TTS 全链路完成。
- 不让 n8n/Dify Loop/Batch 节点成为 1000 站点全局抓取调度器。
- 不把 Google Sheets/Drive/Notion 等协作工具作为主内容真相源或唯一运行状态库。
- 不把 SERP Top N、搜索结果 Top K 或人工竞品 URL 当成历史 Coverage 证明；它们属于业务分析样本。
- 不允许 Workflow Code/Plugin/HTTP Request/Browser/Tool/自定义节点绕过统一 egress、Admission、SSRF、预算和审计策略。
- 不把“最多返回 N 个成功结果”解释成“最多发出 N 次源站请求”；redirect、retry、prefetch、batch 都纳入 dispatch budget。
- 不允许 fixed interval refresh 每次无差别删除全部向量再重建；增量更新必须基于内容版本和 Chunk diff。
- 不允许只保存 source URL 作为 RAG 引用；引用必须能固定到 Document Version 和 Chunk。
- 不把“标题相同”直接当作同一篇文章；标题相似度只能作为 Duplicate Hint。

## 3. 总体架构

```text
                               ┌─────────────────────────────┐
                               │          Web / API          │
                               │ Source / Run / Search / Ops │
                               └──────────────┬──────────────┘
                                              │ Command
                                   ┌──────────▼──────────┐
                                   │ PostgreSQL Truth DB │
                                   │ State / Metadata    │
                                   │ Outbox / Audit      │
                                   └───────┬───────┬─────┘
                                           │       │
                               Outbox Relay│       │Projection Event
                                           │       │
                                 ┌─────────▼───┐   │
                                 │ Redis Stream │   │
                                 │ Task Bus     │   │
                                 └──────┬───────┘   │
                                        │           │
                 ┌──────────────────────┼─────────────────────────────┐
                 │                      │                             │
        ┌────────▼────────┐   ┌────────▼────────┐          ┌────────▼────────┐
        │Discovery/Probe  │   │Fetch Route      │          │Projection/AI    │
        │Coverage Worker  │   │HTTP/Browser     │          │Search/Delivery  │
        └────────┬────────┘   └────────┬────────┘          └────────┬────────┘
                 │                      │                            │
                 │              ┌───────▼────────┐                   │
                 │              │Extraction /    │                   │
                 │              │Quality / ID    │                   │
                 │              └───────┬────────┘                   │
                 │                      │                            │
                 └──────────────────────┼────────────────────────────┘
                                        │
                           ┌────────────▼────────────┐
                           │      S3 / MinIO        │
                           │ Immutable Snapshots    │
                           │ Canonical IR / Assets  │
                           │ Derived Artifacts      │
                           └────────────┬────────────┘
                                        │
                              ┌─────────▼──────────┐
                              │ Search / Vector   │
                              │ Rebuildable Index │
                              └────────────────────┘
```

核心事实边界：

- **Coverage**：应该有哪些历史文章。
- **Candidate/Evidence**：观察到了哪些 URL、从哪里发现。
- **Feed Item Observation**：Feed/API 当时给出的条目、正文片段和变化信号。
- **Priority Hint**：预算内先处理谁。
- **Capability/Route**：怎样抓更可靠、更便宜。
- **Budget Reservation**：真实副作用是否允许派发。
- **Fetch Route Attempt**：某次 HTTP/Browser/备用 route 的执行事实。
- **Snapshot**：源站当时实际返回了什么。
- **Extraction Candidate**：不同 extractor / feed / browser 产生的正文候选。
- **Canonical IR / Document Version**：平台接受的正文事实是什么。
- **Markdown / Chunk / Search / Embedding**：从某一 Accepted Version 可重建的投影。
- **Manifest**：某次批量消费到底使用了哪些不可变输入。
- **Derived Artifact**：摘要、报告、Newsletter、TTS 等结果。
- **Integration/Delivery Job**：外部系统消费与交付边界。

## 4. 技术选型

### 4.1 控制面

- API：FastAPI + Python；Contract 使用 Pydantic/JSON Schema。
- PostgreSQL：业务状态、任务真相、Coverage、版本、Manifest、Audit。
- Redis Streams：任务运输、短期事件和 backpressure，不保存决定性业务状态。
- Transactional Outbox：所有需要发消息的状态变化先和业务数据同事务写 PG，再异步 relay 到 Redis Streams。
- 前端：React/Vue + TypeScript；高风险 Command 使用强类型 API。

### 4.2 数据面

- S3/MinIO：原始响应、HTML、Rendered DOM、MHTML、截图、PDF、Feed payload、Canonical IR 大对象、Markdown、附件、派生 Artifact。
- OpenSearch：全文搜索、过滤、聚合，可删除重建。
- pgvector/Qdrant/Milvus/OpenSearch kNN：向量 Projection；起步优先 pgvector 或 OpenSearch kNN，规模和延迟要求上升后再拆专用 Vector DB。
- HTTPX/aiohttp：静态 HTTP、Feed、Sitemap、API 抓取。
- Crawl4AI/Playwright：Browser/动态页 Adapter，不承担平台调度真相。
- Trafilatura/Readability/lxml/BeautifulSoup/CSS/XPath：确定性正文 Extractor。
- Prometheus + Grafana + OpenTelemetry：指标、日志、trace。

### 4.3 部署形态

生产环境拆分 Worker Pool：

```text
api
scheduler
outbox-relay
discovery-worker
http-fetch-worker
browser-worker
extract-worker
quality-worker
projection-worker
asset-worker
embedding-worker
workflow-worker
```

Browser Worker 独立进程/容器，生产优先 Linux。开发环境可以 Docker Compose 一体化部署。

## 5. 核心数据模型

### 5.1 Source 与站点配置

`source`

```text
id
name
entry_url
registrable_domain
allowed_origins
source_type
status
policy_class
robots_policy
schedule_policy_id
active_profile_release_id
created_at / updated_at
```

`site_profile_release`

```text
id
source_id
version
scope_rules
provider_config
url_normalization_rules
fetch_route_policy
browser_recipe
extractor_policy
quality_policy
asset_policy
chunk_policy_id
egress_policy_id
created_by
created_at
```

Release 一旦被 Run 引用即不可修改，只能创建新 Release。站点专用 selector、页尾裁剪规则、Browser wait 条件、Feed 入口、API cursor 规则都放在 Profile Release 中，不硬编码到全局业务代码。

### 5.2 Run / Task / Budget

`run`

```text
id
source_id
run_type
requested_config
effective_config
profile_release_id
status
started_at
finished_at
coverage_summary
success_summary
cost_summary
partial_failure_count
```

`task`

```text
id
run_id
stage
task_type
url_id
operation_type
priority
state
attempt
lease_owner
lease_until
deadline_at
budget_reservation_id
parent_task_id
idempotency_key
created_at / updated_at
```

`budget_reservation`

```text
id
scope_type / scope_id
resource_type
reserved_amount
consumed_amount
expires_at
state
idempotency_key
```

资源类型至少包括：request、response bytes、browser seconds、OCR pages、embedding tokens/chunks、LLM tokens、TTS chars、Context Provider calls、Delivery calls。

### 5.3 Discovery / Coverage

`coverage_provider_run`

```text
id
run_id
provider_type
provider_release_id
cursor_before
cursor_after
status
exhaustion_reason
candidate_count
completed_count
failed_count
known_gap
blocker
started_at / finished_at
```

`url_candidate`

```text
id
source_id
normalized_url
url_hash
first_seen_at
last_seen_at
state
```

`url_evidence`

```text
id
candidate_id
provider_run_id
parent_url
relation_type
observed_at
raw_href
provider_cursor
metadata_snapshot_id
```

同一 URL 可以有多条 Evidence，不因去重丢失来源证明。

### 5.4 Feed / Change Signal

`feed_item_observation`

```text
id
source_id
provider_run_id
feed_url
guid
entry_url
normalized_entry_url
title
published_at
updated_at
author
categories
summary_hash
content_hash
payload_snapshot_id
observed_at
```

Feed 中的 `description`、`content:encoded`、Atom content 或 JSON Feed content 可以生成 `content_candidate`，并保存 `candidate_source=FEED_CONTENT/FEED_SUMMARY`。这类候选用于低成本增量正文，不代表 Coverage 已完整。

`change_signal_observation`

```text
id
source_id
url_id
signal_type
signal_value
observed_at
changed
provider_run_id
```

signal_type 可为：`SITEMAP_LASTMOD`、`FEED_GUID`、`FEED_UPDATED`、`API_CURSOR`、`ETAG`、`LAST_MODIFIED`、`HEAD_HASH`、`CONTENT_LENGTH_HINT`。

### 5.5 Fetch / Route / Snapshot

`fetch_route_attempt`

```text
id
task_id
source_id
url_id
route_type
route_release_id
attempt_no
requested_url
started_at
finished_at
state
outcome_code
request_count
response_bytes
browser_seconds
snapshot_id
next_route_hint
```

route_type 示例：

```text
HTTP_STATIC
HTTP_ALT_HEADERS
HTTP_PLATFORM_API
BROWSER_CRAWL4AI
BROWSER_PLAYWRIGHT_RECIPE
SITE_CUSTOM_EXTRACTOR
```

每个 fallback route 都是显式持久记录，不由一个进程内 `except` 隐式完成。

`fetch_observation`

```text
id
source_id
url_id
run_id
task_id
route_attempt_id
requested_url
final_url
redirect_chain
status_code
response_headers_hash
etag
last_modified
content_type
body_hash
started_at / finished_at
outcome_code
snapshot_id
```

任务关联永远使用 `task_id/observation_id`。`final_url` 只作为 redirect/canonical evidence，不能用 final URL 反查原 task。

`network_snapshot`

```text
id
content_hash
object_key
content_type
bytes
capture_type
created_at
```

相同内容可 content-addressed 去重，但 Observation 不能去重，因为时间、路由、响应头、redirect 都是事实。

### 5.6 Extraction / Content Candidate

`content_candidate`

```text
id
source_id
url_id
snapshot_id
feed_item_observation_id
candidate_source
extractor_release_id
canonical_ir_ref
content_hash
quality_score
quality_breakdown
body_chars
status
created_at
```

candidate_source 至少包括：

```text
FEED_CONTENT
FEED_SUMMARY
JSON_LD
HTTP_TRAFILATURA
HTTP_READABILITY
HTTP_SITE_SELECTOR
BROWSER_CRAWL4AI_FIT_MD
BROWSER_CRAWL4AI_RAW_MD
BROWSER_DOM
PLATFORM_API
PDF_TEXT
```

不同 Candidate 不互相覆盖。Quality Worker 决定哪个进入 Accepted Version。

### 5.7 Document / Identity / Version

`document`

```text
id
source_id
canonical_identity_key
current_version_id
created_at
```

`document_alias`

```text
id
document_id
url_id
alias_type
valid_from
valid_to
evidence_id
```

alias_type 可为：`CANONICAL`、`REDIRECT`、`MIGRATED_URL`、`AMP`、`PRINT`、`MIRROR`。

`document_version`

```text
id
document_id
source_url_id
snapshot_id
content_candidate_id
canonical_ir_hash
metadata_hash
version_no
published_at
updated_at
accepted_at
extractor_release_id
quality_release_id
version_reason
status
```

version_reason 示例：`NEW_DOCUMENT`、`SOURCE_UPDATE`、`EXTRACTION_UPGRADE`、`METADATA_FIX`、`REPROCESS`。

`freshness_observation`

```text
id
document_id
url_id
observed_at
etag
last_modified
body_hash
canonical_ir_hash
result
```

`duplicate_hint`

```text
id
source_id
left_document_or_candidate_id
right_document_or_candidate_id
hint_type
score
evidence
created_at
```

Normalized title、SimHash、MinHash、canonical、redirect、发布日期接近都可以生成 hint，但只有 Identity Resolver 可以真正合并 Document。

### 5.8 Projection

`markdown_projection`

```text
id
document_version_id
projection_release_id
object_key
content_hash
created_at
```

`chunk_projection`

```text
id
document_version_id
chunk_release_id
chunk_key
ordinal
heading_path
text_hash
content_ref
created_at
```

`embedding_projection`

```text
id
chunk_projection_id
embedding_release_id
vector_store_key
vector_hash
created_at
```

Chunk key 必须稳定，例如：

```text
sha256(document_id + document_version_id + heading_path + ordinal + text_hash)
```

## 6. Source Intake 与站点探测

新增站点时先执行 `PROBE`，不要直接进入全量 Browser Crawl。

探测顺序：

1. 解析输入 URL，确定 registrable domain、allowed origins。
2. 获取并记录 robots.txt。
3. 探测：
   - `/sitemap.xml`
   - sitemap index 引用
   - robots 中 Sitemap 声明
   - RSS/Atom/JSON Feed `<link rel="alternate">`
   - 常见 `/feed`、`/rss`、`/atom.xml`
   - `llms.txt` / `llms-full.txt`
   - CMS/API 指纹：WordPress REST、Ghost Content API、Substack、Dev.to、Medium、GitHub Pages、Docusaurus、MkDocs、Hugo、Jekyll 等。
4. 抽样 5~20 个页面，比较 HTTP 与 Browser 抽取质量。
5. 识别文章 URL pattern、分页、Archive、Category、Tag、Author、Docs Navigation。
6. 形成 Site Profile Release，并记录 Confidence 和需要人工确认的 blocker。

Web 管理端允许人工修正 Profile，但修改只能创建新 Release。

## 7. 全量历史发现与 Coverage

### 7.1 Provider 优先级

推荐 Provider 顺序：

1. 官方 API / CMS API。
2. Sitemap Index / Sitemap。
3. Feed / 历史 Feed。
4. Archive / 年月归档。
5. Category / Tag / Author 分页。
6. Docs Navigation / TOC。
7. 站内分页、Next/Prev。
8. 普通内链图。
9. 搜索页或站内搜索，仅作为补充证据。

Provider 之间并行发现，但各自独立记录 Coverage。

### 7.2 Coverage Exhaustion

每个 Provider 必须给出明确 exhaustion：

```text
SITEMAP_INDEX_EXHAUSTED
API_CURSOR_END
PAGINATION_END
ARCHIVE_YEAR_RANGE_EXHAUSTED
FEED_WINDOW_ONLY
ROBOTS_BLOCKED
AUTH_REQUIRED
RATE_LIMIT_BLOCKED
UNSUPPORTED
MANUAL_STOP
```

不能因为 `max_pages=1000` 就标记完整。达到预算上限只能标记 `BUDGET_STOPPED` + known gap。

### 7.3 Coverage Confidence

Source 页面展示：

```text
官方 API：complete
Sitemap：complete
Archive：complete
Category：partial
普通内链：best-effort
Known gaps：2
```

历史完整性由 Evidence 组合判断，不由抓取成功率判断。

## 8. 增量同步

### 8.1 Change-Signal Plane

按便宜到昂贵检查：

```text
Feed/API cursor
  ↓
Sitemap lastmod
  ↓
Conditional HTTP: If-None-Match / If-Modified-Since
  ↓
METADATA_PROBE / HEAD or small range
  ↓
Full HTTP body
  ↓
Browser
```

### 8.2 Feed-first 增量

Feed 条目到达后：

1. 先保存 Feed payload Snapshot 与 Feed Item Observation。
2. GUID/link/updated 与历史比较。
3. Feed 自带全文时生成 Content Candidate。
4. 经过 Quality Gate：
   - 足够好：可直接接受为低成本正文候选；
   - 只是 summary 或质量不足：创建页面 Fetch Task；
   - 页面仍不足：升级 Browser。
5. 即使 Feed 正文被接受，也保留 URL 作为后续 Reconcile 对象。

这能显著减少高频增量同步中的 Browser 成本。

### 8.3 重叠窗口

Feed/API 增量不要只依赖“上次 cursor 之后”，默认保留时间重叠窗口，例如最近 N 小时/最近 N 条；依靠 GUID/URL/IR hash 幂等去重，防止源站修订、乱序或时钟漂移。

### 8.4 周期 Reconcile

Feed 可能漏历史、Sitemap lastmod 可能不可信，因此每周/月执行低频 `RECONCILE`：

- 重扫 Sitemap/Archive；
- 对近期文章抽样 conditional fetch；
- 检查已知 URL 是否删除、迁移或 canonical 改变；
- 发现 gap 时补发任务。

## 9. 抓取路由与升级策略

### 9.1 HTTP-first

默认 route：

```text
HTTP_STATIC
  ├─ 成功 + Quality Accept → 完成
  ├─ HTTP 304 → Freshness Observation
  ├─ 正文不足 → HTTP_ALT / Site Extractor
  ├─ 动态壳页 → Browser
  └─ Challenge / Block → 按 policy 停止或人工处理
```

### 9.2 Quality-Gated Enrichment

Browser 升级不能只看字符数。Quality Policy 至少使用：

- 正文字符数/词数；
- 标题与正文相关度；
- 主体 DOM 文本密度；
- 导航/推荐/页脚占比；
- 错误页 marker；
- 代码块、表格、图片是否异常缺失；
- 发布时间/作者/canonical 是否合理；
- Feed summary 与页面正文相似度；
- 页面类型分类；
- extractor 历史接受率。

决策输出：

```text
ACCEPT
RETRY_HTTP_ALT
ESCALATE_BROWSER
SITE_RECIPE
MANUAL_REVIEW
REJECT_NON_ARTICLE
```

### 9.3 Browser Worker Pool

- 同一 Browser process 复用多个 URL，避免每 URL 启停 Playwright。
- 按 worker 设置最大 context/page 数。
- 设置 max_tasks_per_browser、max_rss_memory、max_lifetime 后 recycle。
- Source 可配置独立 context，防止 cookie/session 污染。
- Browser Pool 不和 API 进程共享事件循环。
- `Crawl4AI` 与 `Playwright recipe` 都通过 Adapter 暴露统一 `FetchResult`。

### 9.4 并发控制

至少四层限流：

```text
Global worker capacity
Source concurrency
Registrable-domain concurrency / QPS
Route-specific capacity（Browser 比 HTTP 更紧）
```

再叠加 robots crawl-delay、429 Retry-After、动态熔断和 Budget。

### 9.5 Deadline

不要把 correctness 依赖在 `asyncio.wait_for(gather(...))`。

持久化：

```text
Task deadline
Route attempt deadline
Run deadline
Budget reservation expiry
```

批量 worker 可以用“波次估算”计算软 deadline，但每个 URL 的成功结果一完成就落库。

### 9.6 Partial-result Preservation

例如 100 个 Feed 并发时，90 个已成功、10 个卡住：

- 90 个结果立即提交；
- stage deadline 到达后只取消剩余 10 个；
- Run = `PARTIAL_SUCCESS`；
- 10 个进入 retry/dead-letter；
- 绝不返回空列表覆盖 90 个成功事实。

### 9.7 Fallback 是持久 Route Attempt

错误的实现：

```text
try browser batch
except timeout:
    aiohttp fetch all targets again
```

正确实现：

```text
RouteAttempt A: HTTP_STATIC
  ↓ quality fail
RouteAttempt B: BROWSER_CRAWL4AI
  ↓ runtime fail
RouteAttempt C: HTTP_ALT_EXTRACTOR
  ↓
RouteAttempt D: SITE_RECIPE / MANUAL
```

每次 attempt 都有独立 id、Snapshot、预算、状态和错误，避免重复流量和重启后丢状态。

## 10. 抽取、清洗与 Canonical IR

### 10.1 多候选 Extractor

同一 Snapshot 可以并行/串行生成多个候选：

- JSON-LD articleBody。
- `<article>` / `main` selector。
- Trafilatura。
- Readability。
- Site CSS/XPath。
- Feed content。
- Crawl4AI fit markdown / raw markdown。
- Browser Rendered DOM。
- 平台 API。

不让某个第三方库直接决定最终正文。

### 10.2 Canonical IR

内部采用结构化 IR，而不是直接把 Markdown 当真相：

```text
DocumentIR
  title
  subtitle
  author[]
  published_at
  updated_at
  canonical_url
  tags[]
  blocks[]
    heading(level,text)
    paragraph(text)
    code(language,text)
    list(items)
    quote(text)
    table(cells)
    image(src,alt,caption,asset_id)
    embed(type,url)
  outbound_links[]
  metadata
  provenance
```

### 10.3 Boilerplate 清理

通用规则：

- nav/header/footer/aside/form/script/style。
- cookie/consent overlay。
- share toolbar。
- newsletter signup。
- recommendation rail。
- repetitive related posts。

站点专用尾部 marker、selector、市场 ticker 等规则必须进入 `SiteProfileRelease`，不能写死在通用 schema mapper。

### 10.4 Prompt Text 与 Markdown 分离

长期 Markdown 保留：

- 标题层级；
- 链接；
- 图片；
- code fence；
- 表格；
- blockquote；
- 列表；
- 必要 metadata。

另建 `prompt_text_projection` 用于 LLM，可删除图片和链接 URL、压缩空白。不要用“为 prompt 降噪”的 cleaner 破坏知识库 Markdown。

### 10.5 Content Upgrade

同一 URL 首次只得到 Feed summary，之后页面抓到完整正文：

1. 新建 Content Candidate；
2. 重新过 Quality；
3. 生成新的 Canonical IR hash；
4. 若确实更完整，创建 `EXTRACTION_UPGRADE` Document Version；
5. 不覆盖原 Snapshot/Candidate/Version；
6. 只增量重建受影响的 Markdown/Chunk/Embedding。

## 11. URL、Redirect、Canonical 与身份

### 11.1 URL Normalization

分层处理：

1. 语法规范化：scheme/host case、默认端口、fragment、tracking 参数。
2. Source Profile 规则：AMP、打印页、移动版、分页参数。
3. 网络证据：301/308 redirect chain。
4. 页面证据：`rel=canonical`、OpenGraph URL。
5. 内容证据：文本相似度、发布日期、标题。

### 11.2 Requested URL 与 Final URL

一次抓取的主键是 task/observation，不是 URL 字符串。

```text
requested_url = 原任务 URL
redirect_chain = 每一跳
final_url = 最终响应 URL
canonical_url = 页面声明/身份解析结果
```

即使 final URL 改变，也不会造成抓取结果无法关联回原任务。

### 11.3 Document Identity

不使用“URL hash = Document ID”作为唯一身份。

- URL Candidate 有稳定 URL ID。
- Document 是逻辑文章身份。
- Document Alias 支持多个 URL。
- Document Version 保存内容历史。

### 11.4 Duplicate Hint

可以使用：

- normalized title；
- SimHash/MinHash；
- Canonical；
- redirect；
- published_at；
- author；
- 文本相似度。

但 normalized title 只能产生 hint，不能直接删除，因为“Weekly Update”“Release Notes”等标题会合法重复。

## 12. Markdown 规范

建议输出：

```markdown
---
title: ...
author:
  - ...
published_at: ...
updated_at: ...
canonical_url: ...
source_url: ...
document_id: ...
document_version_id: ...
tags:
  - ...
---

# 标题

正文……
```

要求：

- UTF-8。
- LF 换行。
- 标题层级稳定。
- code fence 保留语言。
- 图片改写到可管理的 asset URL 或相对路径。
- 外链保留原 URL。
- 表格尽可能保留 Markdown；复杂表格可附 HTML block。
- 每个文件可追溯 Document Version。
- Projection Release 改变时可全量离线重建，不抓源站。

## 13. 图片与附件

`asset`

```text
id
document_version_id
source_url
normalized_url
content_hash
mime_type
bytes
object_key
status
alt_text
caption
width / height
```

策略：

1. 正文 Accept 后异步下载 Assets，不阻塞 Source Sync。
2. 去重基于 content hash。
3. HTML 中 data URI、小图标、tracking pixel 过滤。
4. 图片下载同样经过 SSRF/Egress Policy。
5. Markdown Projection 使用归档 URL；保留原始 source URL metadata。
6. PDF/ZIP 等附件按 policy 决定是否归档和抽取。

## 14. Chunk、全文检索与向量

### 14.1 Chunk

优先按语义结构切：

```text
Heading path
→ block group
→ token ceiling
→ overlap only when necessary
```

保留：

- document_id；
- document_version_id；
- heading_path；
- ordinal；
- source URL；
- published_at；
- tags；
- text hash。

### 14.2 增量 Embedding

1. 比较旧新 Chunk key/text hash。
2. 未变化 chunk 复用向量。
3. 新增/变化 chunk 才 embedding。
4. 删除 chunk 标记 tombstone 后从向量索引移除。
5. Embedding Model/参数进入 immutable release。

### 14.3 Citation

RAG 返回必须引用：

```text
document_id
document_version_id
chunk_id
source_url
```

不能只引用当前 URL，否则页面后续修改后引用无法复现。

## 15. Web 管理端

### 15.1 Source 列表

显示：

- 状态；
- Entry URL；
- 最近同步时间；
- 下次同步；
- 历史文章数；
- Coverage confidence；
- Known gaps；
- 成功率；
- Browser 使用率；
- 最近错误；
- 成本。

### 15.2 Source 详情

Tab：

```text
Overview
Profile Releases
Discovery Providers
Coverage
Feed / Change Signals
Runs
Tasks
Fetch Routes
Extraction Candidates
Quality
Documents / Versions
Assets
Search / Embedding
Audit
```

### 15.3 Run 详情

展示 stage DAG：

```text
Discovery → Change Signal → Fetch → Extract → Quality → Identity → Projection
```

每个 stage 显示：

- pending/running/success/failed/dead-letter；
- partial success；
- QPS；
- bytes；
- Browser seconds；
- P50/P95 latency；
- 失败分布。

### 15.4 Extraction Preview

同一 URL 可并排查看：

- Raw HTML；
- Rendered DOM；
- Feed body；
- Trafilatura；
- Readability；
- Crawl4AI fit/raw；
- 最终 Canonical IR；
- Markdown Preview；
- Quality breakdown。

支持创建新的 Site Profile Release 并离线 Replay。

### 15.5 运维操作

- Pause/Resume Source。
- Run Full Backfill。
- Run Incremental。
- Reconcile。
- Targeted Fetch。
- Reprocess Snapshot。
- Rebuild Markdown。
- Rechunk/Reembed。
- Retry failed only。
- Mark known gap。
- Pin canonical/identity。

所有操作写 Audit Log。

## 16. 调度、队列与容错

### 16.1 Durable Frontier

Task 真相在 PostgreSQL。Redis Streams 只是运输。

Worker 流程：

```text
claim task lease
→ check budget/admission
→ execute
→ persist result + outbox in transaction
→ ack stream
```

### 16.2 Lease / Heartbeat

- 长任务定期 heartbeat。
- Worker 崩溃后 lease 到期自动重新入队。
- Task 必须幂等。
- 同一 URL/Run/Operation 使用 idempotency key 去重。

### 16.3 Retry

按错误类型：

```text
DNS_TEMPORARY        exponential backoff
HTTP_429             Retry-After + domain throttle
HTTP_5XX             limited retry
BROWSER_CRASH        fresh browser worker retry
QUALITY_FAIL         route escalation, not blind same-route retry
ROBOTS_BLOCKED       no retry
AUTH_REQUIRED        no retry / manual
PERMANENT_404        record observation, no hot-loop
```

### 16.4 Circuit Breaker

按 domain + route 统计：

- 连续 429/503；
- Browser crash rate；
- extraction zero-body rate；
- TLS/DNS error。

触发后降速或暂停 source，避免雪崩。

### 16.5 公平调度

不能让一个百万页站点占满所有 worker。使用 weighted fair queue：

- source weight；
- run priority；
- age；
- route cost；
- domain token bucket。

## 17. 安全与合规

### 17.1 SSRF / Egress

所有 Feed、HTTP、Browser、Asset、Webhook、Context Provider 请求统一走 Egress Policy：

1. 只允许 http/https。
2. DNS 解析 A/AAAA 后拒绝 private、loopback、link-local、multicast、metadata ranges。
3. 每次 redirect 重新校验。
4. 防 DNS rebinding：连接 IP 必须来自已校验解析结果，并控制 TTL/缓存策略。
5. 限制端口。
6. 可配置 source allowlist origin。
7. Browser 请求同样经过 network interception policy。
8. 禁止 `file://`、本地文件、未授权内部网络。

### 17.2 Robots 与访问策略

- robots 结果作为 Snapshot/Policy Evidence 保存。
- 每个 Source 可设置 legal/policy class。
- 不绕过付费墙、登录、验证码或明确 block。
- User-Agent、联系信息、速率可配置。

### 17.3 Web 管理端

- RBAC：viewer/operator/admin。
- SSO/OIDC。
- 所有高风险操作审计。
- Secrets 放 Vault/KMS/Kubernetes Secret，不进 Site Profile 明文。

## 18. 可观测性

核心指标：

### Coverage

```text
coverage_candidates_total
provider_exhausted_total
known_gap_total
coverage_confidence
```

### Fetch

```text
fetch_requests_total
fetch_success_ratio
http_304_ratio
response_bytes_total
route_attempts_total
browser_seconds_total
redirect_count
```

### Quality

```text
candidate_accept_ratio
zero_body_ratio
boilerplate_ratio
browser_escalation_ratio
content_upgrade_total
extractor_accept_ratio{extractor}
```

### 调度

```text
task_queue_depth
lease_expired_total
retry_total
dead_letter_total
partial_success_runs_total
source_lag_seconds
```

### 成本

```text
cost_per_source
cost_per_accepted_document
browser_seconds_per_document
embedding_tokens
llm_tokens
asset_bytes
```

Alert：

- 某 source 连续增量无新内容异常。
- Sitemap candidate 数突然下降。
- Browser escalation 突增。
- Extractor accept rate 突降。
- Feed fetch 全失败。
- known gap 增加。
- dead-letter 堆积。

## 19. Snapshot Replay 与规则升级

Extractor/Quality/Markdown 规则改变时：

```text
existing Snapshot
  ↓
new Extractor Release
  ↓
new Content Candidate
  ↓
new Quality Release
  ↓
new/unchanged Canonical IR
  ↓
new Projection
```

优先离线 replay，不重复访问源站。

如果 Canonical IR 未变化，只更新处理 provenance，不重新 embedding。

## 20. Integration Contract

外部系统只调用稳定 API：

```text
POST /sources
POST /sources/{id}/runs
GET  /runs/{id}
GET  /documents
GET  /documents/{id}/versions
GET  /search
POST /derived-jobs
POST /integration-jobs
```

`integration_job`

```text
id
consumer_type
contract_version
input_manifest_id
operation
params
status
result_artifact_id
created_at / finished_at
```

Dify/n8n 不直接访问 Redis task、不直接操作 Browser Worker、不依赖 Crawl4AI 返回对象。

## 21. 数据保留

建议分层：

- PG metadata：长期保留。
- Accepted Document Version：长期保留。
- Raw HTML/Feed payload：长期或按合规策略压缩归档。
- Browser screenshots/MHTML：按失败、质量审计和抽样策略保留。
- 临时中间文件：TTL。
- Search/Vector：可删除重建。

对象存储使用 content-addressed key：

```text
sha256/ab/cd/<hash>
```

减少重复 HTML、图片、附件存储。

## 22. 数据库与规模化策略

### PostgreSQL

- `task`、`fetch_observation`、`feed_item_observation` 按时间或 source hash 分区。
- `url_candidate` 唯一索引：`(source_id, url_hash)`。
- `document_alias` 索引 normalized URL。
- 大 payload 不放 PG，放对象存储。

### Redis Streams

按 stage 分 stream，例如：

```text
tasks.discovery
tasks.fetch.http
tasks.fetch.browser
tasks.extract
tasks.quality
tasks.projection
tasks.asset
tasks.embedding
```

### Browser

独立节点池，可自动扩缩；不跟 HTTP Worker 混部，避免 Chromium 内存波动影响轻量任务。

## 23. 测试策略

### 单元测试

- URL normalization。
- Feed/RSS/Atom/JSON Feed parsing。
- Sitemap parsing。
- Canonical/redirect identity。
- Quality rule。
- Markdown deterministic projection。
- SSRF resolver。

### Contract Test

对 Crawl4AI/Playwright/Trafilatura 等第三方 Adapter 固定一组 fixture，验证版本升级不会改变业务 Contract。

### Golden Corpus

维护 50~200 个代表站点页面 Snapshot：

- 静态博客；
- SPA；
- WordPress；
- Substack；
- Medium；
- Docs；
- 含大量代码；
- 含表格；
- 含广告/导航；
- redirect；
- AMP；
- 404/challenge。

每次 Extractor/Quality Release 跑离线 golden diff。

### 故障测试

必须覆盖：

- 单个 Feed 超时但其他结果保留。
- 批次 deadline 到期时 partial success 不丢。
- Worker 中途崩溃 lease 恢复。
- Browser crash 后只重试受影响 task。
- Redirect 后仍能关联原 requested URL。
- Crawl4AI 成功但正文质量失败时正确升级/回退。
- DNS rebinding/redirect SSRF 被拒绝。
- 同标题不同文章不被错误合并。
- Feed summary 后续升级完整正文形成新 Version。

## 24. 推荐实施阶段

### Phase 1：可用的历史知识库

实现：

- Source/Web 管理。
- Sitemap + Feed + 普通内链 Discovery。
- PostgreSQL + Redis Streams + MinIO。
- HTTP Fetch。
- Trafilatura/Readability + Site Selector。
- Canonical IR + Markdown。
- Document Version。
- Run/Task Web 页面。
- ETag/Last-Modified 增量。

### Phase 2：动态站点与质量体系

增加：

- Browser Worker Pool。
- Crawl4AI Adapter。
- Quality-Gated Enrichment。
- Feed Body Candidate。
- Partial-result-preserving fanout。
- Durable Route Fallback。
- Extraction Preview / Snapshot Replay。
- Assets。
- Coverage UI / Known Gap。

### Phase 3：搜索、RAG 与智能运维

增加：

- OpenSearch。
- Chunk/Embedding。
- RAG citation。
- Duplicate Hint / Identity Resolver。
- Capability learning。
- Route cost optimization。
- Drift detection。
- Dify/n8n/Agent Integration Contract。
- Derived Artifact / Manifest。

## 25. 验收标准

系统达到以下条件后才算满足核心需求：

1. 新增普通博客站点不需要改代码，只需 Web 配置/Profile。
2. 每个 Source 都能看到历史 Coverage 状态、Evidence、Known Gap，而不是只有“抓取完成”。
3. 服务重启、Worker 崩溃后任务可以从 lease/frontier 恢复，不重复大规模抓取。
4. 1000 站并发同步时，某些站点超时不会导致已完成站点结果丢失。
5. Feed/API 已带完整正文时可直接进入 Quality，避免无意义 Browser 请求。
6. Feed 正文不足时能自动升级 HTTP/Browser；Browser 失败后 fallback 由持久 Route Attempt 驱动。
7. Redirect 后结果仍通过 task/observation 正确关联原请求。
8. 同标题不同文章不会仅因标题相同被合并。
9. 同一文章后续获得更完整正文时生成可追溯 `EXTRACTION_UPGRADE` Version，而不是覆盖历史内容。
10. Crawl4AI/Playwright/Extractor 升级后可以对历史 Snapshot 离线重放。
11. Markdown、Chunk、Embedding、Search 都能从 PostgreSQL + 对象存储重建。
12. RAG 引用可以稳定定位到 Document Version + Chunk。
13. 所有抓取出口经过统一 SSRF/Egress、robots、限流和审计策略。
14. Web 管理端可以定位一次失败到底发生在 Coverage、Fetch、Extraction、Quality、Identity 还是 Projection。
15. Source Sync 与 AI/Embedding/TTS/Delivery 解耦；下游失败不会阻塞源站增量同步。

## 26. 最终推荐架构原则

整套系统以以下十条作为长期约束：

1. **Coverage 与 Fetch 分离**：先证明“应该有什么”，再讨论“抓到了什么”。
2. **Feed-first / HTTP-first / Browser-last**：优先使用低成本结构化信号和静态 HTTP，Browser 只做有证据的升级。
3. **Quality-Gated Enrichment**：是否升级抓取由质量决定，而不是只看 HTTP 状态或字符数。
4. **Partial Result 永不丢失**：批次超时只能影响未完成任务，不能抹掉已成功事实。
5. **Fallback 持久化**：route fallback 是业务状态，不是 try/except 里的隐式副作用。
6. **Snapshot Immutable**：原始事实不可变，Extractor/Quality/Projection 都可以 replay。
7. **Identity 不等于 URL 或标题**：URL、redirect、canonical、内容相似度只是身份 Evidence。
8. **正文升级形成 Version**：Feed summary、HTTP 正文、Browser 正文之间的质量提升必须可追溯。
9. **第三方 Runtime Adapter 化**：Crawl4AI/Playwright/向量库都可替换，业务 Contract 稳定。
10. **PostgreSQL + 对象存储是真相源**：Markdown、搜索、向量和 AI 产物全部可重建。
