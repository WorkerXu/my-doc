# 博客知识库技术方案

## 1. 建设目标

建设一个可长期运行、可横向扩展的技术博客知识库平台。首期约 1000 个技术博客、工程博客和技术文档站，抓取尽可能完整的历史文章并清洗为稳定 Markdown；后续可持续增加站点，并支持增量同步、版本追踪、全文/向量检索、Web 管理、质量审计和离线重处理。

设计原则：

1. 新增普通站点主要通过 Source/Profile 配置完成，不修改核心代码。
2. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`REPROCESS`、`RECHUNK`、`REEMBED`、`REINDEX`。
3. 历史完整性必须由 Coverage Evidence 证明，不能以“队列为空”或达到抓取上限代替。
4. Feed/API/Sitemap 优先作为低成本发现与变化信号，HTTP-first，Browser-last。
5. 包装链接、短链和多级跳转必须保留解析证据，不能只保存 final URL。
6. Snapshot、跳转证据、抽取候选、Canonical IR、Document Version 均作为不可变事实保存。
7. Markdown、Chunk、Embedding、全文索引、BM25 Query View 都是可重建 Projection。
8. Source Sync 与 Embedding/LLM 等下游任务解耦。
9. 所有策略均使用 Release 版本化，支持回放、回滚和比较。
10. Web 管理必须能看到 Source、Coverage、任务、跳转链、抓取路由、质量、版本、索引和审计信息。

## 2. 总体架构

```text
Web / REST API
      |
PostgreSQL Truth Store + Transactional Outbox
      |
Redis Streams / Durable Task Bus
      |
      +--> Discovery / Signal Workers
      +--> Redirect Resolution Worker
      +--> HTTP Fetch Workers
      +--> Browser Workers
      +--> Extraction / Quality / Identity Workers
      +--> Projection / Embedding / Index Workers

事实文件与大对象 --> S3 / MinIO
检索投影         --> OpenSearch + Vector Index
监控             --> Prometheus / Grafana / OpenTelemetry
```

PostgreSQL + 对象存储是真相源；Redis、OpenSearch、向量库和 Markdown 文件均可删除重建。

## 3. 技术选型

### 控制面

- FastAPI + Pydantic：REST/Command API。
- PostgreSQL：Source、Profile Release、Run、Task、Coverage、Redirect、Fetch、Version、Projection State、Audit、Outbox。
- Redis Streams：任务运输、重试和背压，不保存唯一业务事实。
- React/Vue + TypeScript：Web 管理。

### 数据面

- HTTPX/aiohttp：HTTP、Feed、Sitemap、API、跳转解析。
- BeautifulSoup/lxml/CSS/XPath：HTML wrapper、meta refresh、确定性提取。
- Trafilatura/Readability：正文抽取。
- Crawl4AI/Playwright：动态页面和质量失败后的浏览器抓取。
- S3/MinIO：HTTP body、HTML、Rendered DOM、Feed payload、PDF、Canonical IR、Markdown、Assets。
- OpenSearch：全文检索和聚合。
- pgvector/OpenSearch kNN 起步，规模扩大后可替换 Qdrant/Milvus。

## 4. Source 与 Profile

`source` 保存站点级信息：

```text
id
name
entry_url
registrable_domain
allowed_origins
source_type
status
robots_policy
schedule_policy_id
active_profile_release_id
```

`site_profile_release` 保存版本化站点策略：

```text
id
source_id
version
scope_rules
provider_config
url_normalization_rules
redirect_resolution_policy
fetch_route_policy
browser_recipe
extractor_policy
quality_policy
asset_policy
chunk_policy_id
created_at
```

Run 一旦引用某个 Release，该 Release 不再修改，只能创建新版本。

## 5. 新站接入与能力探测

新增站点先执行 `PROBE`：

1. 确定站点作用域和允许来源。
2. 读取 robots 和 Sitemap 信息。
3. 探测 RSS/Atom/JSON Feed。
4. 探测 WordPress REST、Ghost、Substack 等平台 API。
5. 识别 Archive、Category/Tag/Author、Docs Navigation 和分页。
6. 抽样比较 HTTP 与 Browser 的正文质量和成本。
7. 检测 Feed/Search 链接是否存在 wrapper、短链或动态跳转。
8. 自动生成首版 Site Profile Release，并在 Web 中允许人工修正。

## 6. 全量历史发现与 Coverage

所有 Provider 使用统一 Contract：

```text
probe(source, profile) -> capability
discover(cursor, budget) -> candidates + evidence + next_cursor
exhaustion() -> explicit_reason
checkpoint() -> durable_cursor
```

推荐发现顺序：

1. 官方/CMS API。
2. Sitemap Index / Sitemap。
3. 历史 Feed。
4. Archive 年/月分页。
5. Category/Tag/Author。
6. Docs Navigation/TOC。
7. Next/Prev 和普通内链。
8. 站内搜索只作为补充。

每个 Provider 独立记录 candidate、cursor、Evidence、失败和 exhaustion reason。达到预算只能标记 known gap，不能冒充完整。

`coverage_provider_run` 至少保存：

```text
run_id
provider_type
cursor_before
cursor_after
status
exhaustion_reason
candidate_count
completed_count
failed_count
known_gap
blocker
```

## 7. Redirect Resolution Plane

在 Discovery 与 Fetch 之间增加独立 URL Resolution 阶段：

```text
Candidate URL
  -> HTTP Redirect
  -> HTML Meta Refresh / Wrapper Unwrap
  -> Browser Navigation Fallback
  -> Resolved Fetch Target
```

### HTTP Redirect

记录每一跳的 from、to、HTTP 状态和时间，限制最大跳数并检测循环。临时跳转与永久跳转使用不同缓存周期。

### HTML Wrapper

Profile 可以声明 `LinkUnwrapAdapter`：

```text
host_patterns
query_param_rules
css/xpath_rules
meta_refresh_enabled
js_blob_patterns
confidence
```

它只产生解析证据，不直接决定 Document Identity。

### Browser Fallback

只有 HTTP/HTML 无法得到可信目标、且页面确实依赖脚本导航时才进入 Browser。Browser 解析与正文 Browser Fetch 分开记录成本。

### 数据模型

`redirect_resolution_attempt`：

```text
id
task_id
source_id
input_url_id
resolver_release_id
method
state
outcome_code
hop_count
resolved_url_id
confidence
started_at
finished_at
```

`redirect_edge`：

```text
id
resolution_attempt_id
seq
from_url_id
to_url_id
evidence_type
http_status
snapshot_id
observed_at
```

`evidence_type` 可为 `HTTP_LOCATION`、`META_REFRESH`、`WRAPPER_PARAM`、`HTML_TARGET_LINK`、`BROWSER_NAVIGATION`。

需要严格区分 Redirect 与 Canonical：final URL 表示网络导航终点；rel=canonical、og:url、平台 ID 等进入 Identity Evidence，不作为跳转直接跟随。

## 8. 增量同步

按成本从低到高：

```text
Feed/API cursor
 -> Sitemap lastmod
 -> Conditional GET
 -> Metadata Probe
 -> Full HTTP
 -> Browser
```

### Feed-first

保存 Feed payload 和 Feed Item Observation；比较 GUID、link、updated、content hash。新链接先经过 Redirect Resolution，再进入 Fetch。

### Conditional Fetch

保存 ETag、Last-Modified 和上次 body hash。304 直接结束，不创建新 Document Version，也不重复 Projection。

### Reconcile

即便站点已有 Feed，仍周期性执行 Sitemap/Archive 差异检查，发现 Feed 窗口外遗漏、URL 迁移和文章删除。

单次 404 不立即删除知识库文档；多次观察或 authoritative API 明确下线后记录 Tombstone，历史版本继续保留。

## 9. Fetch Route Policy

默认路由：

```text
HTTP_STATIC
 -> HTTP_ALT_HEADERS
 -> HTTP_PLATFORM_API
 -> BROWSER_CRAWL4AI
 -> BROWSER_PLAYWRIGHT_RECIPE
 -> SITE_CUSTOM_EXTRACTOR
```

路由由 Profile 和历史统计共同决定。每次 attempt 记录成功率、延迟、response bytes、browser seconds、质量分和下一跳建议。

HTTP Worker 负责普通页面和条件请求；Browser Worker 独立部署，只处理动态页面或质量失败后的 fallback。

## 10. Snapshot、正文抽取与 Canonical IR

每次网络事实先保存不可变 Snapshot，再做抽取。一个 Snapshot 可以产生多个 `content_candidate`：

- 平台 API / Site Selector。
- JSON-LD。
- Trafilatura。
- Readability。
- Crawl4AI cleaned output。
- Rendered DOM。
- Feed Content。
- PDF Text。

建议先形成 Canonical IR，再生成 Markdown：

```text
DocumentIR
  metadata
  title
  authors[]
  published_at
  updated_at
  canonical_url
  blocks[]
  assets[]
  source_evidence[]
```

`blocks[]` 支持 heading、paragraph、list、quote、code、table、image、figure 等。

Canonical Clean Markdown 必须确定性：去除导航/广告/分享/评论噪声，保留标题层级、代码块、列表、表格、链接和必要图片引用。Markdown 只是 Projection，未来可以从 IR 重建。

## 11. BM25 Query View 的边界

BM25 适合从页面或 Chunk 中选择与查询相关的片段，但不能作为唯一正文清洗策略。

正确链路：

```text
Canonical IR
 -> Clean Markdown
 -> Chunk
 -> BM25 Query View / Search Snippet
```

禁止：

```text
HTML -> BM25 Filter -> 唯一 Markdown 真相
```

因为 query 变化会改变过滤结果，可能丢掉原文中暂时“不相关”的重要段落。

## 12. 文档身份、版本与去重

Document Identity 综合：

1. 平台稳定 ID。
2. rel=canonical / og:url。
3. Redirect Evidence。
4. 规范化 URL。
5. 标题 + 发布时间。
6. exact content hash / near-duplicate signal。

`document_alias` 保存 redirect、canonical、AMP、print、migrated、mirror 等关系。

`document_version` 保存：

```text
document_id
source_url_id
snapshot_id
content_candidate_id
canonical_ir_hash
metadata_hash
version_no
published_at
updated_at
accepted_at
version_reason
status
```

只有 Canonical IR 或需要追踪的元数据发生变化时才创建新版本。

## 13. Projection 与检索

Projection 类型：

```text
CLEAN_MARKDOWN
PLAIN_TEXT
CHUNK
BM25_QUERY_VIEW
EMBEDDING
FULLTEXT_INDEX
SUMMARY
```

`projection_record` 使用 `input_hash` 去重：事实输入和策略 Release 完全相同则复用已有产物。

Chunk 使用 heading-aware 结构切分，代码块和表格尽量保持完整，并保存 heading path 和 block range。

默认 Hybrid Search：

```text
OpenSearch BM25
 + Vector Search
 -> fusion
 -> metadata filter
 -> optional rerank
 -> Context Assembly
```

所有检索结果都能回溯到 Document Version、Source、Canonical URL、Chunk Range 和 Retrieval Release。

Embedding 模型或检索 schema 升级时创建新的 `index_generation`，后台构建、校验后原子切换，旧 generation 保留回滚窗口。

## 14. 任务编排与可靠性

Task 状态：

```text
PENDING
LEASED
RUNNING
SUCCEEDED
RETRY_WAIT
FAILED_TERMINAL
CANCELLED
```

Worker 使用 lease；进程异常后 lease 到期自动恢复。

关键幂等键示例：

```text
DISCOVER: source + provider_release + cursor
RESOLVE: url + resolver_release
FETCH: url + route_release + fetch_epoch
EXTRACT: snapshot + extractor_release
PROJECTION: document_version + projection_release
```

批量任务逐项提交结果。100 个 URL 中 90 个成功、10 个失败时，90 个成功事实立即落库，不整批回滚。

## 15. 限流、背压与成本

并发分层控制：

```text
global
 -> worker pool
 -> registrable domain
 -> source
 -> route type
```

HTTP 并发较高，Browser 并发较低且独立扩容。对每个 Run 记录 requests、bytes、browser seconds、accepted documents、changed documents、embedding tokens 等。

重点指标：

- `cost_per_accepted_document`
- `cost_per_changed_document`
- browser fallback rate
- redirect resolution success rate
- projection reuse rate

## 16. Web 管理

### Source

- 新增/停用站点。
- PROBE 结果。
- Feed/Sitemap/API 能力。
- Profile Release 历史和 diff。
- 调度与预算。

### Coverage

- Provider 状态。
- candidate 数。
- exhaustion reason。
- known gap。
- unresolved candidate。

### URL / Redirect Chain

可视化展示：

```text
feed/search url
  --302--> tracking
  --wrapper--> target
  --browser navigation--> article
```

并展示 resolver release、每跳证据、最终 fetch target、canonical identity 以及失败位置。

### Run / Task

查看 Run DAG、队列 backlog、retry、partial failure、Browser 使用量，并支持安全的定向重跑。

### Quality / Version

查看 Snapshot、Extraction Candidate 对比、quality breakdown、Document Version diff、alias/canonical graph 和 quarantine。

### Projection / Search

查看 Markdown/Chunk、Projection Release、Index Generation，并提供 Hybrid Search 调试台。

所有管理操作进入 Audit Log。

## 17. 安全与合规

- 遵守站点 robots 和访问策略。
- 不绕过登录、付费内容、验证码或明确访问控制。
- 起始 URL 和每次跳转都执行统一 URL/网络出口校验。
- Browser Worker 独立隔离，使用受控网络和资源上限。
- API token、代理凭据和对象存储密钥放 Secret Manager，不进入 Profile 和普通日志。

## 18. 可观测性

关键指标：

```text
coverage_candidates_total
coverage_known_gap_total
fetch_success_rate
accepted_document_rate
changed_document_rate
redirect_resolution_success_rate
browser_fallback_rate
quality_reject_rate
projection_reuse_rate
index_lag
```

统一 `trace_id` 贯穿：

```text
Run
 -> Provider Evidence
 -> Redirect Resolution
 -> Fetch Attempt
 -> Snapshot
 -> Extraction Candidate
 -> Document Version
 -> Projection
 -> Index
```

重点告警：站点错误率激增、redirect loop 激增、Browser fallback 激增、Feed/Sitemap 长时间无变化、Projection backlog、Index Generation 数量不一致。

## 19. 部署与扩容

生产 Worker 按功能池拆分：

```text
api
scheduler
outbox-relay
discovery/signal
redirect-resolution
http-fetch
browser
extract/quality/identity
projection
embedding/index
```

HTTP、Browser、Extraction、Embedding 可独立横向扩容。大 body 和 IR 放对象存储，PostgreSQL 只保存 metadata/ref/hash。高增长 Observation/Task/Audit 表按时间或 source hash 分区。

## 20. 实施阶段

### Phase 1：20 个代表性站点

覆盖 WordPress、Ghost、Substack、静态博客、Docs、JS 站、Feed wrapper 和短链。完成 Source/Profile、Coverage、Redirect Resolution、HTTP/Browser、Snapshot/IR/Markdown、增量和基础 Web。

### Phase 2：100 个站点

验证配置复用率、域名限流、失败分类、Coverage gap、Identity 去重和 Browser 成本。

### Phase 3：1000 个站点

完成 Worker 横向扩容、PostgreSQL 分区、对象存储生命周期、全链路监控、批量运营和索引 Generation 管理。

### Phase 4：检索优化

完善 Hybrid Search、Retrieval Release、Context Assembly、BM25 Query View、rerank 和检索回归集。

## 21. 验收标准

### Coverage

- 每个 Source 有可解释的 Provider。
- FULL_BACKFILL 有明确 exhaustion evidence。
- known gap 可枚举、可重跑。

### 增量

- Feed/API/Sitemap 新文章在目标 SLA 内进入系统。
- 304 不重复抽取和建版。
- 未变化 Projection 不重复计算。
- Reconcile 能发现 Feed 窗口外遗漏。

### Redirect / Identity

- wrapper/短链保留完整解析证据。
- 跳转循环可检测。
- final URL 与 canonical identity 分离。
- 同一文章多入口不会因包装 URL 大量产生重复文档。

### Markdown

抽样检查标题、作者、时间、正文完整性、导航噪声、代码块、列表、表格和链接；Markdown 可由 Canonical IR 确定性重建。

### 可靠性

- Worker 异常可恢复。
- fan-out 保留部分成功。
- Embedding/Index 故障不阻塞 Source Sync。
- 所有重要管理操作可审计。

## 22. 最终原则

1. Coverage First：先证明“应该有哪些 URL”，再看抓取成功率。
2. Evidence First：发现、跳转、抓取、身份和版本都保存证据。
3. HTTP First, Browser Last：Browser 是 fallback，不是默认路径。
4. Snapshot First, Replay Later：保存不可变原始事实，处理规则升级优先离线重放。
5. IR First, Markdown as Projection：Markdown 可重建，不是唯一真相。
6. Canonical Content 与 Query View 分离：BM25/LLM 过滤不能破坏完整正文。
7. Release Everything：Profile、Resolver、Extractor、Quality、Chunk、Embedding、Retrieval 都版本化。
8. Idempotent + Recoverable：任务可重复、可恢复、可观察。
9. Source Sync 与 AI 解耦：源站事实不被下游模型服务绑架。
10. Web 管理是平台能力：异常、证据、版本和成本都必须能被运营人员看到并安全重放。
