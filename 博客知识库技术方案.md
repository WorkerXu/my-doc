# 博客知识库技术方案

适用范围：从约 1000 个技术博客、工程博客、技术文档站、Newsletter、CMS 与公开文档站抓取尽可能完整的历史文章，清洗为稳定 Markdown 建设长期知识库；后续可持续增加网站，并支持全量回灌、增量同步、Web 管理、版本留存、离线重处理、全文/向量检索、质量审计、图片附件归档，以及 Dify/n8n/Agent/Newsletter/TTS 等下游工作流消费。整体按百万级文档、数百万到千万级 URL、站点高度异构和多年持续运行设计。

## 1. 建设目标

1. 支持站点根地址、单 URL、批量 URL、RSS/Atom/JSON Feed、Sitemap/Sitemap Index、OPML、CSV、JSON、Markdown/TXT 等来源导入。
2. 自动探测 Sitemap、Feed、公开 API、Archive、Category/Tag/Author、Docs Navigation、普通内链、`llms.txt`/`llms-full.txt` 和平台 API 等发现入口。
3. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`PROBE`、`METADATA_PROBE`、`FOCUSED_DISCOVERY`、`REPROCESS`、`ASSET_REPAIR`、`REINDEX`、`DERIVED_BUILD`、`DELIVERY_RETRY` 等运行类型。
4. 历史完整性由 Provider 级 Coverage Evidence 证明，不能以“队列空了”“达到 max_depth/max_pages/max_urls”代替完整性证明。
5. HTTP-first；只有动态渲染、正文缺失、挑战页、选择器失效或质量失败有证据时才升级 Browser/Crawl4AI/Playwright。
6. 支持站点能力学习，用真实运行的正文接受率、质量、延迟、成本和 blocker 优化抓取 route，但 Capability 不能替代 Coverage。
7. Source/Profile/Discovery/Fetch/Extractor/PostProcess/Quality/Asset/Chunker/Search/Reranker/Embedding/AI/TTS/Workflow/Delivery 全部采用不可变 Release。
8. 使用 Sitemap `lastmod`、Feed GUID/updated、API cursor、ETag、Last-Modified、body hash、Canonical IR hash、重叠窗口与周期 Reconcile 驱动增量同步。
9. 最终知识对象保存标题、作者、发布时间、更新时间、标签、canonical、正文块、代码块、表格、图片、附件、链接关系、Snapshot、字段 provenance 和完整 lineage。
10. 支持 durable frontier、断点续跑、幂等、lease、heartbeat、重试、限流、熔断、公平调度、backpressure 和 dead-letter。
11. 保存不可变网络 Snapshot、Rendered DOM、Extraction Candidate、Canonical IR、Markdown Projection 和质量结果；规则升级优先离线 replay。
12. Web 管理端覆盖 Source Intake、Site Profile、Provider Coverage、Focused Discovery、Capability & Route、Run/Task、Targeted Fetch、Extraction Preview、Snapshot Replay、Version Diff、Quality/Drift、Assets、Search/RAG、AI Artifact、Workflow/Delivery、成本和审计。
13. Markdown、全文索引、Embedding、摘要、标签、Topic、FAQ、Digest、Newsletter、TTS 和报告均是 Accepted Document Version 的异步 Projection/Derived Artifact，失败不能阻塞 Source Sync。
14. append-only 历史真相层之上提供可重建 Current Projection。
15. Canonical IR hash 在 Chunk/Embedding/AI/TTS 等昂贵下游之前计算；正文未变化只写 Freshness Observation。
16. Change-Signal Plane 优先通过 Feed/Sitemap/API 元数据判断是否可能变化，再决定是否抓正文。
17. “日报/Newsletter”必须先冻结 Selection Manifest，再执行摘要/TTS，保证补跑和重放结果可解释。
18. Dify/n8n 的 Code/Plugin/Tool 节点视为独立不可信执行边界，必须使用版本化沙箱策略、默认拒绝出网、最小权限 Secret、资源配额和节点级执行证据；抓取任意 URL 必须通过 Integration Gateway/`TARGETED_FETCH`。
19. Discovery 中的主题相关度、URL pattern、SEO/head metadata、链接位置和人工主题只生成可解释的 Priority Hint；在 `FULL_BACKFILL`/`RECONCILE` 中不得因低分或未命中而删除合法 Coverage Candidate。
20. 候选页 `<head>`/有限字节探测必须建模为显式 `METADATA_PROBE` 任务，受缓存、QPS、字节、deadline 和总预算约束；禁止把会联网的过滤器伪装成无成本本地 Predicate。
21. 第三方 crawler/scorer/priority queue 的原生 `score` 不能直接成为业务调度优先级；必须通过版本化 Priority Adapter 归一化，并用实际 pinned runtime 做顺序契约测试。
22. Web 控件、API Command 与持久 Run Type 必须强类型一一映射；系统同时保存 requested/effective run type 与 config，禁止隐藏字符串布尔值、前端默认字段或 truthy 判断静默改变核心运行模式。

## 2. 非目标与边界

- 不绕过登录、付费墙、验证码、WAF 或明确访问控制。
- 不承诺未知站点仅靠自动探测达到 100% 历史完整；必须展示 coverage confidence、known gap 与 blocker。
- 不用 LLM、Reranker、SEO 分数、URL 关键词或抓取成功率决定 FULL_BACKFILL 中合法历史文章是否存在。
- Markdown 不是唯一真相；Canonical IR 是内部内容真相，Markdown 是确定性投影。
- 不锁死单一爬虫库、浏览器库、向量库、搜索引擎、消息队列、工作流引擎或模型供应商。
- HTTP 200、Browser success、协程正常返回不等于业务内容成功。
- 不把全局 reload/scroll/wait 脚本作为所有站点默认策略；浏览器动作必须进入 Site Profile Recipe。
- 不把多个 Document Version 的全文拼成一个无结构大字符串作为默认 AI/TTS 输入。
- 不在 Web/Dify 请求线程同步执行长期 Browser、LLM、OCR 或 TTS。
- 不允许 Workflow Code/Plugin 节点拥有不受控的互联网/内网访问、数据库/对象存储真相层凭据或云 metadata 权限。
- 不相信第三方 API 名称暗示的排序方向；`BestFirst`、`score`、`priority` 等必须通过运行时契约验证。
- 不在已经取得父页面链接集合后，因剩余抓取页数预算按 DOM 顺序预先裁掉未评分候选。
- 不把模型/API Secret 放进客户端 cookie/session、普通日志、trace 或前端可回显配置。

## 3. 核心原则

1. **PostgreSQL 是业务状态真相源；S3/MinIO 是不可变大对象真相源。**
2. Redis Streams 只做任务运输；决定性状态先写 PostgreSQL，再由 Transactional Outbox 投递。
3. Source Intake、Change Signal、Discovery、Admission、Route Decision、Fetch、Extraction、PostProcess、Quality、Identity、Publish、Asset、Search、AI、TTS、Delivery 分层。
4. Discovery 只产生 URL Candidate 与 Evidence；列表页、Feed 摘要和搜索结果不能直接成为最终正文。
5. URL Identity、Document Identity 与 Freshness 分离。
6. Discovery Fetch Route 与 Article Fetch Route 分离，能力统计按 operation type 单独维护。
7. HTTP、Browser、PDF/OCR、Platform API、Fast Path 通过 Adapter 隔离，第三方原始返回 shape 不泄漏到业务真相层。
8. 所有 Stage 使用固定 Typed Outcome Contract。
9. `document_version` append-only；Current/Markdown/Search/Embedding/AI/TTS/Delivery Projection 可重建。
10. `max_depth/max_pages/max_runtime/max_urls/top_k` 是预算，不是 Coverage Evidence。
11. 进程内 semaphore/`asyncio.gather` 只做 Worker 本地资源保护，不承担持久调度。
12. Config 使用不可变 Release；Secret 只保存 Secret Manager 引用。
13. 代码缩进、表格、列表层级、引用、公式和换行都是内容数据，不能在通用 cleaner 中被压平。
14. 多个 Extractor/Strategy 和 Link Discovery 优先消费同一 Snapshot；网络访问与内容解释解耦。
15. Coverage Candidate 与 Priority Hint 是两个不同事实：前者决定“应该被枚举与审计哪些 URL”，后者只决定“预算内先处理谁”；任何相关性策略都不得隐式改变历史全集。
16. Discovery Filter 必须声明 `LOCAL_PREDICATE` 或 `NETWORK_PROBE` 副作用类型；会发起网络请求的 Filter/Scorer 必须转化为持久 Probe Task，并继承 Admission、限流、Snapshot、deadline、审计与成本治理。
17. 第三方 `raw_score` 与平台 `normalized_priority` 分离；平台统一定义 **normalized_priority 越大越先处理**，实际队列 key 由 Scheduler Adapter 明确转换。
18. 已抓父页面产生的 observable links 先形成 Candidate/Evidence，再做评分和 fetch budget admission；候选观察事实不能被执行预算提前抹掉。
19. UI 请求参数、Command、Run 的 requested/effective config 都是可审计事实；“用户点了什么”和“系统实际运行了什么”必须可对账。

## 4. 推荐技术栈

### 4.1 控制面

- Web：Next.js + TypeScript。
- API：FastAPI + Pydantic，所有业务 Command 使用 Enum/Discriminated Union Schema。
- PostgreSQL：Source、Profile、Provider、Run、Task、Document、Version、Coverage、Capability、RouteDecision、PriorityEvidence、SelectionManifest、DerivedArtifact、WorkflowExecution、DeliveryJob、Audit。
- Redis Streams：任务运输和 Consumer Group。
- Transactional Outbox：保证数据库状态与消息投递一致。
- Redis/Valkey：robots、Capability Projection、短期 probe 和 UI 热数据缓存，不作为唯一真相源。
- Secret Manager：Vault、云 Secret Manager 或 Kubernetes Secret 引用。
- Integration Gateway：面向 Dify/n8n/Agent/Webhook 提供稳定 Job/Artifact API。
- Workflow Preflight：DSL Parser/Linter + target-engine import test + dependency resolver。
- Workflow Sandbox：Kubernetes Job/隔离 Worker + seccomp/AppArmor/NetworkPolicy/egress proxy，默认拒绝网络与宿主资源。

### 4.2 数据面

- HTTP：`httpx.AsyncClient` 长生命周期连接池。
- HTML：selectolax/lxml/BeautifulSoup。
- 主体抽取：Trafilatura/Readability/Crawl4AI/CSS/XPath/JSON-LD/OpenGraph 组合 Adapter。
- Browser：Playwright/Crawl4AI Browser Runtime Pool。
- PDF：PyMuPDF/pypdf + OCR fallback。
- 对象存储：S3/MinIO。
- 全文检索：OpenSearch。
- 向量：pgvector 起步，规模扩大后可替换专用 Vector Store。
- AI/TTS：独立 Adapter + Worker Pool。
- 可观测：OpenTelemetry + Prometheus/Grafana + 日志聚合。

## 5. 总体架构

```text
Source Registry
  -> Coverage Providers
  -> Durable URL Candidate/Evidence
  -> Change Signal
  -> Admission & Security
  -> Metadata Probe / Priority Hint (optional)
  -> Priority Adapter -> normalized priority
  -> Durable Frontier
  -> Route Decision
  -> HTTP / Browser / Fast Path / PDF
  -> Immutable Snapshot
  -> Multi Extractor Candidates
  -> Canonical IR + Quality
  -> Identity + Append-only Version
  -> Markdown / Assets / Search / Embedding
  -> Selection Manifest
  -> Summary / Digest / TTS Derived Artifacts
  -> Workflow Preflight + Sandboxed Execution
  -> Delivery Jobs
  -> Dify / n8n / Agent / Newsletter / Webhook

                   ^
                   |
         Capability Observation
                   |
         Route Capability Projection
```

控制面只创建和管理持久事实，不直接执行长期 I/O/GPU 任务；数据面由独立 Worker 消费队列。Change-Signal/Discovery 优先产生候选和覆盖证据；专题探索可创建 `FOCUSED_DISCOVERY`，但 Best-First 只改变候选优先级。需要读取页面 head 的评分统一交给 `METADATA_PROBE` Worker，禁止 Discovery Worker 在过滤函数里产生不可见网络 I/O。

第三方 crawler 内部 frontier 只能作为单 Task/单 Worker 的实现细节，不能成为平台唯一的 Candidate、Coverage 或调度真相。只要页面已经被获取，其 observable links 必须尽可能写回平台 Candidate/Evidence 层，再由平台预算、Priority Policy 和公平调度决定后续 fetch。

## 6. Release Registry 与可重复运行

所有会改变结果的行为都必须版本化：

```text
command_schema_release
config_resolver_release
site_profile_release
discovery_provider_release
discovery_priority_policy_release
priority_adapter_release
metadata_probe_policy_release
fetch_engine_release
representation_provider_release
route_policy_release
extractor_release
postprocess_release
quality_policy_release
asset_policy_release
chunker_release
search_provider_release
reranker_release
embedding_release
ai_model_release
ai_recipe_release
tts_model_release
tts_recipe_release
selection_policy_release
workflow_recipe_release
workflow_sandbox_policy_release
workflow_adapter_release
delivery_recipe_release
destination_adapter_release
runtime_release
```

每个 Release 至少记录 release id/version、source commit、config hash、dependency lock hash、container image digest、SBOM、作者/时间、Contract/Golden/shadow test、predecessor/rollback target。

`runtime_release` 额外记录 Python/OS、Browser/Playwright/Crawl4AI revision、parser/Trafilatura、模型/tokenizer、device/dtype/quantization 和外部 API 模型版本。不能只记录“用了 Crawl4AI”，必须记录能确定实际运行语义的精确版本、镜像 digest 和依赖锁。

`priority_adapter_release` 保存第三方 scorer 的原生范围、方向、归一化函数、queue comparator/heap 映射和 tie-break 规则。任何版本升级都要在 Golden Candidate 集上验证高/低优先级顺序、candidate admission、max_pages/max_depth 边界和取消语义。

Workflow DSL/Graph 本身独立版本化。发布前必须完成 parse/import/export round-trip、graph/node/variable 引用校验、Code 节点直接依赖校验、Model/Tool/Plugin 可用性检查、Secret scope、Endpoint、安全网络策略、输入输出大小、资源上限和 Sandbox Policy Preflight；关键工作流还要用 Golden Input 在隔离环境运行。

## 7. Source Registry 与 Site Profile

`source` 保存 root URL、canonical host、source type、启用状态、sync policy、active profile release、compliance policy。

Site Profile Release 至少包含：

- allowed/denied host/path；
- article URL pattern；
- Feed/Sitemap/API/Archive provider；
- canonical 规则；
- metadata selectors；
- include/remove selectors；
- Browser action recipe；
- content-type policy；
- asset policy；
- rate limit/politeness；
- Golden URL；
- fast-path policy；
- route hard constraints。

站点专用 CSS/XPath 必须进入 Profile/Extractor Release；脆弱规则必须通过 Snapshot Replay 验证。Site Profile Studio 支持草稿、测试、shadow、发布和回滚。

Profile Preflight 还必须检测恒真/恒假 pattern、include/exclude 覆盖、通配符吞掉规则、正则异常成本，以及规则在 Golden URLs 上的 recall/precision。未知站点默认 soft scoring，只有经过 Golden/Replay 验证的规则才允许变成 hard include/exclude。

## 8. Discovery、Coverage 与历史完整性

### 8.1 Provider 优先级

默认按权威性和可枚举性排序：官方 API/Export → Sitemap/Sitemap Index → RSS/Atom/JSON Feed → Archive/年月分页/Category/Tag/Author → Docs Navigation → DOM 内链/BFS → 外部搜索 Gap Discovery。

### 8.2 Discovery Evidence

每条 Evidence 保存 source/provider/release、discovered URL、时间、provider cursor/item id、lastmod/updated、referrer、depth、anchor/link context、evidence snapshot。相同 URL 来自多个 Provider 时 Evidence 全保留，Candidate/Task 层再去重。

如果使用优先级策略，额外保存：

- `priority_policy_release_id`；
- `raw_score`；
- `raw_score_semantics`；
- `priority_adapter_release_id`；
- `normalized_priority`；
- `priority_score_components`；
- `metadata_probe_snapshot_id`；
- tie-break evidence。

### 8.3 Coverage Evidence

每个 Provider 维护 enumeration boundary、oldest/newest observed time、pages/items enumerated、cursor exhausted、pagination continuity、expected/discovered count、known gaps、blocker、confidence。`max_pages`、`max_depth`、当前页没有更多链接、前 N 条抓完都不能作为 coverage complete。

`RECONCILE` 周期重新枚举高价值 Provider，将新 manifest 与历史 manifest 做集合差异，发现漏抓、删除、URL 迁移和旧文章更新。

### 8.4 Focused / Prioritized Discovery

`FOCUSED_DISCOVERY` 用于新站接入、专题研究、人工排错和 Golden URL 寻找。它可以使用 Best-First、URL 关键词、path pattern、anchor/link context、head metadata 和人工主题形成 Priority Hint，在固定预算内优先处理高价值候选。

推荐内部优先级契约：

```text
third-party/local raw score
 -> priority_adapter_release
 -> normalized_priority in [0,1]
    1.0 = 最应优先
    0.0 = 最低优先
 -> scheduler_key
    (-normalized_priority, next_due, depth, discovered_at, canonical_url)
```

禁止把第三方 `PriorityQueue` 的 min-heap/max-heap 方向、正负 score、默认 tie-break 当作隐式业务规则。

约束：

- Priority Hint 只改变 frontier 的 `priority`/`next_due`，不从 `FULL_BACKFILL`、`RECONCILE` 的 Coverage Candidate 集合中删除低分 URL。
- `max_pages`、`max_depth`、`top_k`、score threshold 只代表预算或交互停止条件，不能产生 `coverage_complete`。
- URL 关键词命中只能标记为 URL-level relevance，不能冒充正文语义相关度；正文语义评分必须消费已有 Snapshot/IR，不能为了打分重复访问源站。
- 未知站点默认 soft scoring；只有经过 Golden Corpus/Replay 验证的 Profile 才允许 hard include/exclude。
- Profile Preflight 必须检测恒真/恒假 pattern、规则互相覆盖和通配符导致的过滤失效。
- 每次评分保存 policy release、raw score、normalized priority、score components 与 evidence refs，保证排序可解释、可重放、可 shadow 对比。
- Focused Run 仍受 source/host token bucket、backlog age、Coverage 优先级和公平调度约束，不能长期饿死历史回灌或增量同步。
- 已抓父页面上的 observable links 应先持久化 Candidate/Evidence，再评分和决定本次 fetch budget；禁止先按 DOM 顺序用 `remaining_capacity` 裁掉未评分候选。
- 第三方 deep crawler 如果内部会预先裁候选，只能作为 Worker 局部优化；平台层必须尽可能从父页面 Snapshot/Link Discovery 恢复完整 observable link evidence。

### 8.5 Metadata Probe

当优先级、路由或站点接入需要 title、canonical、robots、description、schema.org、真实 Content-Type 等轻量信号时，创建显式 `METADATA_PROBE`：

```text
URL Candidate
-> Admission / robots / rate limit
-> METADATA_PROBE Task
-> HEAD 或受限 GET/range
-> metadata_probe_snapshot
-> Metadata Candidate
-> Priority Hint / Route Hint / Profile Draft
```

`metadata_probe_snapshot` 至少记录 request/final URL、headers、实际读取字节、head hash/object ref、title/canonical/robots/schema 摘要、content-type、started/finished、policy release、trace。Probe 必须复用连接池与缓存，支持 ETag/Last-Modified，限制 per-site QPS、response bytes、redirect、connect/read/total deadline 和单 Run 总预算。

SEO/metadata 质量只能用于排序、路由和站点诊断；老旧博客缺少规范 meta/schema 时仍可能是合法历史文章，不能因此在 FULL_BACKFILL 中被排除。相同 Probe Snapshot 可被不同 scorer、Profile Studio 和 Route Decision 离线复用，不重复访问源站。

## 9. 增量同步

优先使用 Feed GUID/updated、Sitemap lastmod、API update cursor、ETag/Last-Modified、conditional GET、body hash、Canonical IR hash。增量采用重叠窗口，抵抗乱序、迟到更新和时间戳不可靠。

```text
Provider signal unchanged -> 可跳过正文 fetch
changed/unknown          -> conditional fetch
Network representation unchanged -> Freshness Observation
Canonical IR unchanged   -> Freshness Observation，不建新 Version
Canonical IR changed     -> 新 Version + 异步刷新 Projection
```

抓取库自己的 cache/bypass_cache 只是工具行为，不是业务 freshness 判定。

Provider cursor、last successful window、overlap window、reconcile cadence 都保存为业务状态，不把“上次 Worker 内存里抓到哪里”当唯一断点。

## 10. URL Admission、安全与合规

- 只允许 `http/https`，标准化 host/port/path。
- 拒绝 localhost、私网、link-local、multicast、reserved 和云 metadata。
- 限制 redirect 次数，每次 redirect 重新校验 URL/DNS。
- 限制 response bytes、Content-Type、解压膨胀比；connect/read/total deadline 分层。
- Browser/Crawl4AI 独立 Worker 必须通过 NetworkPolicy/egress proxy/防火墙默认拒绝内网和 metadata，不能只依赖应用层预解析。
- robots、站点条款和内部合规策略作为独立 Gate；阻断记为 `BLOCKED_POLICY/BLOCKED_ROBOTS`，不能当普通网络失败。
- Code/Plugin/第三方 Tool 默认 `deny-all` egress，只允许 Integration Gateway、批准的模型/TTS Provider、必要对象代理和明确 Destination。
- Workflow Code/Plugin 禁止直连 PostgreSQL、Redis、S3 管理端、Kubernetes API、宿主 Docker socket、云 metadata；Secret 使用最小权限 scope + 短期 token。
- Web 输入的模型/API Credential 只生成/绑定 Secret Manager reference，不把明文放入客户端 session/cookie；日志、trace、error detail 统一 Secret Redaction。
- 生产环境禁止默认固定 Web Secret、debug server/debugger；所有关键 Secret 必须外部注入并支持轮换。

## 11. Capability Learning 与 Route Decision

每次抓取写 append-only Capability Observation，至少包含 site/path/operation type、route/release、transport outcome、content outcome、quality、latency/bytes/cost、HTTP status、blocker/error、render_required、snapshot/trace。

异步聚合 Route Capability Projection：sample_count、confidence、EWMA acceptance/quality、p50/p95 latency、cost、last success/failure、cooldown、next probe。PostgreSQL 保存真相，Redis 只缓存。

Route 类型可包括：`PLATFORM_API`、`FEED_FULL_CONTENT`、`LLMS_FULL_TEXT`、`HTTP_STRUCTURED`、`HTTP_STATIC`、`CRAWL4AI`、`PLAYWRIGHT_PROFILE`、`PDF_NATIVE`、`PDF_OCR`、`ARCHIVE_FETCH`。

执行顺序：Hard Gate → Fast Path → 通用 Route Candidate → Site Profile → Operator Override → Capability Projection → Ranking → 持久化 Route Decision → 执行 → Quality → 合法升级。冷启动不过早永久禁用 route；使用时间衰减、exploration/canary，并按 host/path/operation 分层。

## 12. Durable Command、Run/Task 与调度

### 12.1 Command 与配置解析

所有 Web/API/Agent 操作先形成 Typed Command。Command 至少保存：

- actor/tenant；
- command type；
- requested run type；
- requested config；
- command schema release；
- idempotency key；
- submitted_at。

Config Resolver 输出：

- effective run type；
- effective config；
- config resolver release；
- default/profile/policy/override provenance；
- validation warnings/errors。

禁止通过普通字符串 truthiness、隐藏字段默认值、按钮未使用参数等方式决定核心模式。

### 12.2 Run

`run` 保存 id、source、requested/effective run type、status、requested config、effective config、config provenance、release set、started/finished、counters、deadline、parent command id。

Web 必须能展示 requested/effective diff，便于回答“用户点的是 Deep/Focused，为什么实际跑成 Single/Targeted”之类问题。

### 12.3 Task

`task` 保存 id/run、stage、subject key、status/attempt、lease owner/until、heartbeat、next retry、parent task、input/output artifact ref、route decision、error class。

Task 使用稳定幂等键、lease、heartbeat、分层 deadline 和 cancellation propagation；side effect 用事务/唯一键保证幂等。全局、站点、host 三层 token bucket/并发上限，按 normalized priority、next_due、backlog age、politeness 做公平调度。Scheduler 只物化持久 Run/Task/Selection Manifest，不直接做网络、LLM 或 TTS。

## 13. Fetch Snapshot 与 Runtime Pool

`fetch_snapshot` 保存 request/final URL、route/release、时间、status/response headers/content type、body object/hash、rendered DOM、可选 screenshot、resolved IP attestation、runtime attestation。

同一 Snapshot 供 Link Discovery、JSON-LD/OpenGraph、Trafilatura、Readability、CSS/XPath、Crawl4AI Candidate 和 Quality 共享，禁止为找链接、比较 Extractor 或重投影 Markdown 再访问源站。

Link Discovery 从 Snapshot 产生的链接先写 Candidate/Evidence，再交给 Priority/Admission/Scheduler。这样即使第三方 crawler 的内部 frontier 因版本 bug、budget 或进程退出丢失，平台仍保留“父页面当时观察到了哪些链接”的事实。

HTTP Session、Browser Context、Model Runtime Handle 使用 Runtime Pool，具备 max_total/per-site hard cap、idle timeout、周期 sweep、in-flight/busy pin、最大寿命/页面数/内存阈值、finally 释放和 pool 指标。每篇文章创建全新 Browser/Crawler 生命周期只适合 Demo，不作为规模化默认方案。

## 14. Extraction、Canonical IR 与质量

一个 Snapshot 可产生 Trafilatura、Readability、Crawl4AI、CSS/XPath、JSON-LD、平台 API 等多个 Candidate。每个 Candidate 保存 extractor release、输入 snapshot、字段 provenance 和结构统计。

Canonical IR 采用块结构：title、authors、published/updated、canonical URL、tags、language、blocks；Block 类型至少覆盖 heading、paragraph、list、code、table、quote、image、math、html，并保存 source locator。

正文清洗使用 DOM 级移除 nav/footer/sidebar/广告；禁止全局压空白破坏代码；有损规则产生 Evidence 并设置高删除率 Guardrail。

Quality Gate 至少检测 title、正文长度、结构、text-to-DOM、boilerplate、重复块、challenge/login 特征、metadata provenance、历史异常变化。Outcome：`ACCEPTED`、`REJECTED_EXPECTED`、`REJECTED_QUALITY`、`UNEXPECTED_EMPTY`、`BLOCKED_POLICY`、`BLOCKED_ROBOTS`、`NEEDS_BROWSER`、`NEEDS_PROFILE_REVIEW`。`UNEXPECTED_EMPTY` 不能覆盖已有正常版本。

Transport/Tool success 与 Content success 分开。第三方库 `result.success=true` 只能说明其运行层成功，不足以发布知识库版本。

## 15. Identity、版本与去重

URL normalize 只做安全确定性规则，并保留原始 URL/Evidence。Document Identity 优先组合 canonical、平台稳定 ID、Feed GUID、结构化数据 ID、可信 mapping 和人工 merge。

`document_version` append-only，保存 document_id、accepted_at、canonical_ir object/hash、source_snapshot、extractor/postprocess/quality release、metadata provenance、predecessor_version_id。Canonical IR hash 不变时只写 Freshness Observation。

SimHash/MinHash/embedding 相似只形成 Similarity Evidence/Duplicate Cluster，不自动删除不同来源文档。

## 16. Markdown Projection 与 Asset Pipeline

Markdown 是 Canonical IR 的确定性投影：front matter/正文规则版本化；代码 fence、列表、表格稳定；图片 URL 由 Asset Projection 重写；不把抓取时间戳写入正文。推荐路径：`markdown/{source_id}/{document_id}/{version_id}.md`。

图片/附件独立流程：URL Admission → 下载限制 → MIME sniff → hash → 安全策略 → 对象存储 → version-asset relation → Markdown rewrite。Asset 失败不阻塞原文 Version 接受，可通过 `ASSET_REPAIR` 独立修复。

## 17. Search 与 RAG

建立 OpenSearch BM25/字段索引 + Embedding + metadata filter + 可选 reranker。技术博客需要专门处理代码 token、错误码、版本号、命令、配置键和路径。

```text
query
-> lexical + vector recall
-> RRF/merge
-> metadata/security filter
-> rerank
-> context assembly
```

Chunk 按 IR block、heading boundary、代码块完整性切分，保存 `document_version_id + block range + chunker_release_id`。

## 18. AI Derived Artifact

AI 只消费 Accepted Version 的显式 Projection，任何摘要/标签/FAQ/报告都不写回 Canonical IR。长文采用 block-aware Map/Reduce：Document Version → AI Input Projection → Chunk Plan → Map Artifact × N → Reduce Artifact → Final Derived Artifact。

Derived Artifact 保存 type、input version ids/set hash、input projection hash、parent artifacts、recipe/model/workflow/runtime release、object/content hash、status、tokens、cost、latency。单篇 Summary/Digest Item 至少包含 `document_version_id`、title、canonical URL、published_at、summary、key points、source block refs。

Prompt、temperature、语言、输出 JSON Schema、token budget 和截断策略都属于 AI Recipe Release；同输入版本集合 + 同 Recipe/Model/Runtime 使用稳定幂等键和缓存。

禁止默认把多篇完整 Markdown 用 `"\n\n".join(...)` 拼成一个无边界大 Context。交互式少量问答也应至少保存 Selection/Input Manifest、版本边界、token budget 与截断 evidence。

## 19. Selection Manifest：日报、Newsletter 与批量 AI 的可重放输入

不能使用“当前搜索结果前 N 条”作为日报事实。生成前必须冻结本次选择：trigger type、timezone、window start/end、as_of、scope、selection policy release、ordered document version ids、ranking evidence、duplicate suppression evidence、manifest hash。

典型流程：Scheduler → Selection Manifest → 单篇 Summary Artifact（可复用）→ Digest Reduce → Digest Artifact → TTS(optional) → Delivery。失败重试必须复用同一 Manifest；需要重新选文时显式创建新 Manifest。

对于 Focused Discovery 后的“立即总结/问答”，也应先把本次 Accepted Version 集合冻结成 Selection/Input Manifest，再运行 AI，避免爬取结果到达顺序或后续更新改变同一次请求的事实输入。

## 20. Workflow / Dify / n8n Integration

长期 Source Sync 与外部工作流分离。推荐链路：Accepted Version → Selection Manifest → Derived Job → Workflow Execution(optional) → Summary/Digest/TTS Artifact → Delivery Job → Dify/n8n/Agent/Webhook。

Integration API 至少提供：创建 derived job、创建 selection manifest、查询 job、获取 artifact、search、targeted fetch。Web/Dify 请求线程不得等待 Browser、LLM 或 TTS 全链路完成。

外部工作流只接收稳定 Version/Artifact Ref；任意 URL 抓取必须走 `TARGETED_FETCH` Admission；DSL/Graph 改动生成新的 Workflow Recipe Release；每次生产执行保存 Workflow Execution/Node Execution，记录实际 Code/Tool/Plugin/Model、输入输出 Artifact hash、Secret scope、network policy decision、latency/cost、Outcome 和 trace，不保存 Secret 明文。

## 21. TTS 与 Delivery

TTS 默认消费 Summary/Digest，而不是多篇完整正文；限制输入字符/token、目标音频时长、单任务 deadline、模型/音色 Release、GPU 配额、失败重试和成本预算。Audio Artifact 使用标准 typed metadata、对象存储 key/hash、duration、mime、runtime attestation。

Delivery Job 保存 trigger、selection manifest、input artifact、destination adapter release、delivery recipe release、idempotency key、status/attempt、next retry、response fingerprint 和 error class。Webhook 支持 HMAC 签名、timestamp + replay window、deadline、response byte limit、指数退避 + jitter、最大重试次数、dead-letter 和审计。

## 22. Web 管理端

### 22.1 Source Intake

单 URL、批量站点、OPML、CSV/JSON、粘贴文本导入；先进入 Intake/Probe，不直接写生产 Profile。

### 22.2 Typed Command Console

所有按钮、表单和批量动作提交显式 command type/run type enum，例如：

```text
START_FULL_BACKFILL
START_INCREMENTAL
START_RECONCILE
START_FOCUSED_DISCOVERY
START_TARGETED_FETCH
START_REPROCESS
CANCEL_RUN
RETRY_TASK
```

前端不能通过隐藏 `simple=1`、字符串 `"false"`、按钮文字或未被后端读取的 `action` 参数决定业务模式。

提交成功后 UI 展示：

- requested run type/config；
- effective run type/config；
- 默认值/Profile/Policy/Override 来源；
- config resolver release；
- Run ID；
- validation warnings。

### 22.3 Site Profile Studio

编辑 URL/Provider/Selector/metadata/remove rule、HTTP/Browser route constraint、Golden URL；同一 Snapshot 多策略预览 Candidate/IR/Markdown；支持草稿、测试、shadow、发布、回滚。

### 22.4 Provider Coverage

显示 Provider、枚举边界、oldest/newest、cursor、expected/discovered、known gap、blocker、confidence，并可触发 Reconcile。

### 22.5 Focused Discovery / Onboarding Explorer

输入 root URL + 可选主题 + 页面/时间预算后只创建持久 `FOCUSED_DISCOVERY` Run。Web 通过 SSE/WebSocket/轮询渐进展示 discovered、probe、scored、scheduled、fetched、accepted 数量，以及候选 URL 的 provider、depth、raw score、normalized priority、score components、probe evidence、route 和 Quality Outcome。

管理员可从探索结果选择 Golden URL、建议 article path pattern、Archive/Feed/Sitemap 入口或 Browser Recipe，写入 **Site Profile Draft**；必须经过 Snapshot Replay/Golden 验证后才能发布。UI 必须明确显示“Focused 结果不是 Coverage 完整率”，并允许切换到 Provider Coverage 查看历史全集证据。

UI 还应区分：

- observed candidates；
- budget admitted；
- fetched；
- quality accepted；

避免把“未在本次 budget 中抓”误显示成“没有发现”。

### 22.6 Capability & Route

展示 transport/content success、acceptance/quality、p50/p95 latency、cost、Browser escalation、preferred route、cooldown/exploration、Route Decision 时间线和人工 Override TTL。

### 22.7 Run/Task 与 Replay

查看 stage backlog、lease、retry、dead-letter、requested/effective config、route decision、deadline、语义计数和 trace。任意 Snapshot 不重新联网即可运行多个 Extractor/PostProcess/Quality/Priority Release，比较 Candidate/IR/Markdown/priority order。

### 22.8 Quality/Drift、Search/RAG/AI、Delivery

展示 unexpected empty、reject、selector miss、正文长度漂移、Browser 升级率、Golden Regression；支持检索调试、chunk/rerank trace、AI lineage、token/cost/truncation；支持 Recipe/Selection/Delivery Studio、Workflow Preflight、Node trace、Destination 测试和 Delivery retry/dead-letter。

## 23. Observability、Audit 与 Health

全链路 Trace：Command → Run → Task → PriorityEvidence → RouteDecision → Fetch → Snapshot → Extraction → Quality → Identity → Version → Projection → SelectionManifest → DerivedJob → WorkflowExecution/NodeExecution → DerivedArtifact → AudioArtifact → DeliveryJob。

技术指标：queue lag、latency、timeout、retry、browser crash、DB/Redis/S3 error、response-too-large、cancellation、pool utilization/eviction/in-flight。

业务指标：discovered URL、candidate evidence、budget admitted、fetched、new version/unchanged、unexpected empty、quality reject、coverage gap、sync lag、asset missing；Focused Discovery 额外观察 first-accepted latency、raw/normalized priority 分布、candidate-to-fetch ratio、Coverage backlog starvation；Metadata Probe 观察 request count、cache hit、bytes、p95、budget exhausted。

Profile/Release/Override/Manual Merge/Targeted Fetch/Focused Discovery/Metadata Probe/Replay/Quality override/Selection Build/Derived Build/Workflow Preflight/Sandbox Override/Delivery Retry/Command Config Resolution 都写 Audit，记录 actor、before/after、reason、time。Priority Policy 评分证据和 Metadata Probe 预算/网络结果关联 trace。

Health 区分 liveness、readiness、dependency_degraded、critical_dependency_unavailable。缓存/搜索/AI/TTS/Destination 故障可降级；PostgreSQL/S3 等任务所需真相源不可用时 fail-closed。

## 24. 缓存策略

可缓存 robots、Provider manifest、Capability Projection、Metadata Probe 结果/conditional metadata、短期 Fetch dedupe、Search result、Derived Artifact lookup。缓存必须有严格 timeout、有限重试；错误退化到 PG/S3/实时执行；cache key 带必要 Release/Input Version/Manifest 维度。Redis、搜索索引、向量库都不能成为不可变内容唯一真相源。

Priority 计算若依赖 Metadata Probe Snapshot，可缓存 probe 事实，但 `normalized_priority` 仍要带 Priority Policy/Adapter Release 维度，避免规则升级后复用旧排序值。

## 25. 失败分类与升级

统一错误分类至少覆盖 DNS/连接/读取超时、deadline、response too large、HTTP 4xx/5xx、rate limit、robots/policy/SSRF block、WAF challenge、JS shell、empty content、selector miss、quality low、unsupported type、browser crash、parser/storage/model/workflow/TTS/delivery error、cancelled、command/config validation error、priority contract violation。

只有适合升级的错误才进入下一 route，例如 `JS_SHELL` 可 HTTP → Browser；`ROBOTS_BLOCKED/POLICY_BLOCKED` 不升级；`RATE_LIMITED` 延迟重试；Workflow/AI/TTS/Delivery 错误不反向标记 Source Sync 失败。Sandbox/Egress violation 默认 fail-closed。

若运行时自检发现 Priority Contract 违反（例如高 normalized priority 实际被第三方队列后执行），该 runtime/adapter release 应停止用于 Focused Priority，回退到平台 Durable Frontier 或上一稳定 release，而不是静默继续。

## 26. 测试、Replay 与发布门禁

1. **Contract Test**：Adapter 输入输出 schema、错误分类、timeout、取消传播、request/response 字节边界、provenance。
2. **Golden Corpus**：覆盖静态博客、JS SPA、WordPress、Ghost、Substack、Docs、代码重页面、表格、图片、PDF、挑战页、多语言和 selector drift。
3. **Shadow Replay**：新 Extractor/PostProcess/Quality/Route/Priority Policy 在历史 Snapshot/Observation 上离线运行，比较 accepted/rejected、Markdown hash、Browser 比例、latency/cost。
4. **Supply Chain Gate**：校验直接依赖、lockfile、镜像 digest、SBOM、漏洞扫描、Browser revision、模型/tokenizer/plugin/DSL hash。
5. **Resource Boundary Test**：注入超大响应、慢响应、取消、连接悬挂、Browser/Session 泄漏、长任务，验证不会 OOM，deadline/cancellation 能释放连接和 lease。
6. **Workflow Preflight/Sandbox Test**：阻断缺失 import、无效变量/节点、不可用 Model/Tool/Plugin、硬编码 localhost/私网/metadata、越权源站访问、Secret/PG/S3/Redis/Kubernetes 越权、DNS 重绑定、资源超限。
7. **Discovery Priority / Metadata Probe Test**：使用同一 Provider manifest 构造高分、低分、无关键词、旧式无 meta、恒真通配 pattern 和需要 head probe 的候选集，验证 `FULL_BACKFILL` Coverage Candidate 集合不随 Priority Policy 改变；不同 Priority Release 只改变处理顺序；`max_pages/max_depth/top_k` 不产生完整性结论；会联网的 scorer 必须生成 `METADATA_PROBE` Task；Probe 的 cache/QPS/bytes/deadline/总预算生效；同一 Probe Snapshot 可离线重放不同评分策略且不再次访问源站。
8. **Priority Semantic Conformance Test**：对每个 pinned crawler/scorer/runtime 构造明确的 high-score/low-score URL，验证经过 `priority_adapter_release` 后 high normalized priority 确实先被平台调度；检查 min-heap/max-heap、正负号、tie-break、batch 顺序和 stream 顺序，禁止仅根据类名判断。
9. **Candidate-before-budget Test**：父页面包含大量候选且最高分 URL 位于 DOM 尾部，验证 observable links 全部先形成 Candidate/Evidence，评分后再应用 fetch budget；第三方内部 `remaining_capacity` 不能让平台候选事实丢失。
10. **Web Command Contract Test**：端到端操作每个按钮/表单，断言 UI action → Command Schema → requested run type → effective run type → durable Run 一致；覆盖隐藏字段、字符串 `"0"/"1"/"false"`、默认值、缺字段、重复提交和旧前端版本。
11. **Secret Boundary Test**：日志、trace、session/cookie、error response、Audit、job payload 均不得出现模型/API Secret 明文；只允许 Secret reference/scope。

## 27. 部署与横向扩展

起步部署：API ×2、Integration Gateway ×2、Scheduler/Outbox Relay ×2（leader/lease）、Signal/Discovery Worker、Metadata Probe Worker、HTTP Worker 多实例、Browser Worker 独立节点、Extraction/Quality Worker、Asset Worker、Search/Embedding Worker、AI Worker、TTS Worker、Delivery Worker、Workflow Preflight/隔离 Sandbox Worker、Capability Projector、PostgreSQL、Redis、MinIO/S3、OpenSearch。

按队列 backlog 与资源类型独立扩容。Browser/GPU/TTS/Workflow Sandbox 不与普通 HTTP Worker 混部。1000 站点的主要瓶颈通常来自礼貌限速、Provider 枚举和页面复杂度，而不是单机协程数量。

第三方 deep crawler 可在 Worker 内并发/批处理，但跨进程、跨重启、跨版本的 frontier 由平台 durable task/candidate 层承接。即使 Worker 内部 crawler 不支持可靠 resume，也不能影响平台任务可恢复性。

## 28. 容量与成本控制

容量规划至少观察 URL Candidate/Evidence、frontier、Snapshot bytes、Accepted Version、Browser/GPU/TTS 工时、Search/Embedding index、Derived Artifact、Workflow Node 执行量和音频对象存储。

关键降本手段：Change-Signal 先行、HTTP-first、平台 API/Feed/llms-full Fast Path、Capability-based route ranking、Canonical IR hash 早去重、Snapshot replay 避免重抓、unchanged 不重复 Embedding/AI、单篇 Summary 复用于多个 Digest、Selection Manifest + input set hash 保证重跑不重算、TTS 默认仅对 Digest、Browser/GPU/TTS/Sandbox 独立预算、Workflow 节点传 Artifact Ref 而非复制全文、S3 lifecycle 热冷分层。

Metadata Probe 与 Priority 也有独立预算。对已抓页面的链接枚举/持久化属于低成本事实采集，不应为了节省后续 fetch budget 而提前丢弃 Candidate；真正昂贵的是后续联网、Browser、Embedding、LLM、TTS。

## 29. 分阶段实施

### Phase 1：可用主链路

Source Registry、Typed Command/Run Contract、Sitemap/Feed/Archive Discovery、durable Candidate/Run/Task、HTTP-first、Snapshot、Trafilatura/CSS Extractor、Canonical IR、Markdown、PostgreSQL + MinIO、基础 Web、统一 deadline/响应边界、Secret Redaction。

### Phase 2：规模化与增量

Provider Coverage、conditional fetch、Reconcile、Metadata Probe、Browser Runtime Pool、fairness/backpressure、Quality/Drift、Asset Pipeline、OpenSearch。

### Phase 3：Capability、Focused Discovery 与自适应路由

Capability Observation、Route Projection、Route Decision、Fast Path、Focused/Prioritized Discovery、Discovery Priority Policy、Priority Adapter、Candidate-before-budget、exploration/cooldown/probe、Onboarding Explorer、Priority/Route Policy shadow replay。

### Phase 4：知识库与 AI

Chunk/Embedding、Hybrid Search/Reranker、AI Input Projection、Map/Reduce、AI Recipe/Model/Runtime Registry、Targeted Fetch、Replay/Diff、成本面板。

### Phase 5：Workflow / Digest / Delivery

Selection Policy/Manifest、Workflow Recipe Release、Workflow DSL Preflight、Workflow Sandbox Policy/Node Execution Evidence、Integration Gateway、Derived Artifact、结构化 Digest、Dify/n8n Adapter、Delivery Job、Webhook 签名/重试/dead-letter。

### Phase 6：TTS 与高级治理

TTS Worker、Typed Audio Artifact、Recipe/Selection/Delivery Studio、Dataset/Training Release、自托管模型评估、多租户/RBAC 强化和自动 Drift 修复建议。

## 30. 验收标准

1. 新增普通技术博客不改核心代码，仅新增配置/Adapter 即可接入。
2. 1000 站点可独立限流、暂停、重跑和查看 Coverage。
3. Worker 重启后任务继续，不因进程内状态丢失。
4. 同一 Snapshot 可离线重跑新 Extractor/IR/Markdown，不重新访问源站。
5. 未变化正文不会重复创建 Version、Embedding、摘要或 TTS。
6. Markdown 保留代码缩进、列表、表格和段落结构。
7. Selector 漂移和异常空正文能明确识别，不会静默成功。
8. Targeted Fetch 与批量同步共享 Admission/Snapshot/Quality/Identity 主链路。
9. 每次抓取可回答 route 选择依据、Profile、Capability Projection 和 Override。
10. Coverage 与 Capability 语义分离，route 成功率不能显示成历史完整率。
11. SSRF 同时有 URL/DNS 校验和 Browser/Fetcher 网络层 egress 隔离。
12. 任意 AI Artifact 可回答使用的 Version、Recipe、Model、Runtime、token/truncation 和 source block refs。
13. 任意日报/Newsletter 可回答 timezone、时间窗、Selection Policy、ordered Version 集合和 manifest hash。
14. 同一 Selection Manifest 重试不会因查询结果变化而改变输入集合。
15. 外部工作流创建长任务时 API 立即返回 job/run id，不同步等待 Browser/LLM/TTS。
16. Workflow Preflight 能在发布前阻断硬编码 localhost/私网、缺失依赖、未解析节点/变量、不可用 Model/Tool/Plugin 和不合规 Sandbox Policy。
17. Workflow Code/Plugin 节点默认无法直连源站、PG/S3/Redis、metadata；抓取 URL 必须通过 Integration Gateway/`TARGETED_FETCH`。
18. 同一 Provider manifest 在不同 Discovery Priority Policy 下，`FULL_BACKFILL`/`RECONCILE` 的 Coverage Candidate 集合保持一致，只允许处理顺序变化。
19. URL keyword、SEO/head metadata、Best-First raw score 均有明确语义和 score components，不能显示为正文语义相关度或历史完整率。
20. 所有需要联网的 Discovery Filter/Scorer 都物化为 `METADATA_PROBE` Task，受 Admission、cache、QPS、bytes、deadline、总预算与审计约束；相同 Probe Snapshot 可离线重放不同评分策略。
21. Web Onboarding Explorer 能渐进展示 Focused Discovery 进度和评分证据，并将候选 Golden URL/pattern/provider 写入 Profile Draft；UI 明确提示 Focused 结果不是 Coverage 结论。
22. `博客知识库技术方案.md` 只维护当前最终方案，不记录调研过程。
23. 对任意第三方 crawler/scorer pinned runtime，平台能证明 `normalized_priority` 越高实际越早被调度；若库原生 score 方向相反，由 `priority_adapter_release` 正确映射。
24. 已获取父页面产生的 observable links 在应用 fetch budget 前先形成 Candidate/Evidence；高价值候选不会因为位于 DOM 后部而在评分前被 `remaining_capacity` 静默裁掉。
25. 任意 Web 控件都能端到端证明其 Command、requested/effective run type 与实际持久 Run 一致；隐藏字段/string truthiness 不会改变业务模式。
26. 模型/API Secret 不出现在客户端 session/cookie、日志、trace、error response 或 Audit 明文中。
27. Web 可以同时展示 observed candidates、budget admitted、fetched、accepted，避免把预算未抓误判为未发现。

## 31. 推荐最终形态

最终系统不是“一台 Crawl4AI 把 1000 个站点爬下来”，也不是“Dify 每次需要内容时临时抓网页”，而是长期运行的数据同步与派生平台。

Coverage 回答“应该有哪些文章”，Candidate/Evidence 回答“我们实际观察到了哪些 URL 及来源”，Priority Hint 回答“预算内先处理谁”，Priority Adapter 回答“第三方原生 score 如何转换成平台一致的调度语义”，Capability 回答“怎样抓更可靠、更便宜”，Metadata Probe 回答“在完整正文抓取前有哪些低成本可复用信号”，Snapshot/IR/Version 回答“当时抓到了什么、如何解释”，Selection Manifest 回答“本次批量消费到底选了哪些不可变版本”，Derived Artifact 回答“如何生成摘要/语音”，Workflow Recipe/Preflight/Sandbox/Node Execution 回答“编排逻辑是什么、依赖什么、实际运行了什么且是否越权”，Delivery 回答“如何可靠地交给外部系统”，Typed Command/Run Contract 回答“用户请求的操作与系统实际执行是否一致”。

对 1000 站点长期知识库，核心边界始终保持：**源站抓取是持久同步任务；Coverage Candidate 决定历史全集而 Priority Hint 只能改变处理顺序；已经观察到的链接先成为可审计 Evidence，再应用评分和 fetch budget；第三方 score 必须经过版本化 Priority Adapter 和实际 runtime 契约测试；任何联网评分都进入受预算和审计约束的 Metadata Probe；Accepted Document Version 是事实输入，Markdown/搜索/AI/TTS 是可重建派生结果，Selection Manifest 固化每次批量消费输入，Dify/n8n/Agent 是受控消费者与编排器而不是抓取真相源；任何可执行 Workflow 扩展都必须在可预检、可沙箱、可审计、可重放的边界内运行；Web 控件到 durable Run 必须强类型、可对账，Secret 永不进入客户端 session/cookie 或日志明文。**
