# 博客知识库技术方案

## 1. 建设目标与边界

建设一个长期运行、可横向扩展、可审计、可回放的技术博客知识库平台。首期接入约 1000 个技术博客、工程博客、技术文档站和公开 Newsletter，尽可能抓取完整历史文章并清洗为稳定 Markdown；后续持续增加网站，并低成本完成增量同步、版本管理、搜索/RAG、Web 管理、导出和离线重处理。

目标规模：

- 1000+ Source，可扩展到数千站点；
- 百万级 Document / Document Version；
- 数百万到千万级 URL Observation；
- 千万级 Chunk / Embedding；
- HTML、Markdown、TXT、JSON/API、RSS/Atom/JSON Feed 为主要输入；PDF 作为可选证据输入；
- 中英日韩等多语言；
- HTTP 为默认抓取路线，Browser 为受控 fallback；
- CMS API、Sitemap、Feed、Archive、Category/Tag、Docs TOC、Common Crawl、URL Seeder、受控 Deep Crawl 等多入口历史发现；
- PostgreSQL + Object Storage 保存事实；Markdown、全文索引、向量索引、Graph、AI 派生、Export、Dashboard Read Model 均为可重建 Projection；
- Web UI、n8n、MCP、CI、Agent、外部系统只能通过受控 API / Event 边界驱动平台。

平台必须能够回答：

1. 一个站点理论上有哪些历史文章，为什么认为历史已经基本覆盖；
2. 一个 URL 从哪里被发现、如何规范化、为何被过滤或放行、经过哪些跳转、最终抓到了什么；
3. 为什么某 URL 走 HTTP、Crawl4AI Browser 或特殊 Playwright Recipe；
4. 一个 Markdown 来自哪个 Snapshot、Extractor Release 和 Document Version；
5. 源站变化后哪些内容真正变化，哪些派生物需要重建；
6. Worker 崩溃、批任务部分失败或外部自动化重启后如何恢复且不丢成功项；
7. 新增第 1001 个站点时，能否主要依靠配置、Probe 与审核接入，而不是增加站点专用代码；
8. 全量历史抓取是否有 durable checkpoint，能否从断点继续；
9. 百万 URL 批处理是否保持有界内存，并支持逐 URL 提交；
10. Browser 是否被复用且安全隔离，何时回收，是否因资源压力自动降并发；
11. Crawl4AI 等第三方 crawler 升级后，是否只修改 Adapter/Runtime Release 而不污染平台数据契约；
12. Markdown 导出到本地/S3/Git 后，是否仍能稳定映射回平台内部 Document Version。

非目标：

- 不把“互联网搜索发现的任意 URL”自动算入正式博客源的历史 Coverage；
- 不以模型输出替代原始 Snapshot、Canonical IR 或人工可验证证据；
- 不把 Redis、Crawler 内部队列、Deep Crawl frontier、n8n 执行记录当业务真相；
- 不默认绕过登录墙、付费墙、CAPTCHA 或源站明确的访问控制。

---

## 2. 核心设计原则

1. **Config First**：普通站点靠 Source / Site Profile / Release 接入，不修改核心代码。
2. **Coverage First**：历史完整性由 Provider Evidence、cursor、exhaustion reason 和 known gap 证明，队列为空不等于完整。
3. **Authoritative First**：CMS/API、Sitemap、Feed、Archive 优先，Common Crawl/URL Seeder/Deep Crawl 用于补洞和交叉验证。
4. **Discovery Before Fetch**：先发现、Observation、规范化、过滤、分类、去重，再抓正文。
5. **Durable Streaming Frontier**：大站 URL 发现必须流式、小批持久化，禁止把全站 URL 一次性物化在内存。
6. **HTTP First, Browser Last**：静态 HTML、Feed、API、Sitemap 优先 HTTP；Browser 只处理动态正文、交互和质量 fallback。
7. **Adapter Is Boundary**：HTTPX、Crawl4AI、Playwright 只输出平台内部稳定契约，第三方 DTO 和版本变化不得进入事实层。
8. **Platform Permit Before Local Dispatcher**：平台 global/domain/source 配额是并发上限；Crawl4AI MemoryAdaptiveDispatcher/Semaphore 只负责 Worker 内二级资源控制。
9. **Bounded Window + Stream + Commit Per URL**：Worker 只 lease 有界窗口，流式返回结果，每个 URL 成功立即落库，不等待整批完成。
10. **Snapshot First**：先保存不可变网络事实，再做抽取、清洗、Markdown、Chunk、索引和 AI。
11. **IR First, Markdown as Projection**：Canonical IR / Document Version 是内容事实层，Markdown 可确定性重建。
12. **Canonical Before Fit**：Pruning/BM25/LLM 等有损过滤只能产生 FIT/ENHANCED Projection，绝不能覆盖 Canonical Markdown。
13. **Reuse Browser, Isolate Session**：复用 Browser 进程降低成本，但 Cookie、LocalStorage、认证态、代理身份按 Source/Profile/安全域隔离。
14. **Recipe, Not Site Code**：交互、路由、Filter、Schema、Selector、Extractor 均声明式、版本化、可回放。
15. **Release Everything**：影响结果的 Profile、Adapter、Crawler Runtime、Recipe、Extractor、IR、Markdown、Chunk、Embedding、Metric、Export 均版本化。
16. **Idempotent + Recoverable**：任务有幂等键、lease、fencing token、checkpoint、retry、partial success、dead-letter。
17. **One Durable Task System**：PostgreSQL durable state + Transactional Outbox + Redis Streams 是唯一业务任务体系。
18. **Projection Is Rebuildable**：全文、向量、Graph、AI、Export、Dashboard 都可从事实层离线重建。
19. **Security on Every Hop**：起始 URL、Redirect、Browser navigation、Webhook endpoint 每跳重新做 SSRF/Egress/Scope 校验。
20. **Source Friendly**：robots、站点政策、分层限流、Retry-After 和退避优先于吞吐。
21. **Benchmark Before Switch**：Crawler Runtime、Extractor、Markdown、Embedding 等升级先固定回归，再激活。

---

## 3. 总体架构与真相边界

```text
                     Web / REST / MCP / n8n / CI
                               │
                   API + RBAC + Idempotency
                               │
                 PostgreSQL Truth Store
   Source/Profile/Release/Run/Task/Observation/Coverage
       Snapshot/Document/Version/Audit/Outbox/Export
                  │                    │
               Outbox               Query
                  │                    │
            Redis Streams        Read Models
                  │             Search/Dashboard
        ┌─────────┴──────────────┐
        │                        │
 Discovery Workers          Fetch Workers
 CMS/Sitemap/Feed/         HTTP Pool / Browser Pool
 Archive/Seeder/CC/        Crawl4AI / Playwright
 Deep Crawl                    │
        │                      │
 URL Observation -> Durable Frontier -> Snapshot
                                      │
                              Extract / Canonical IR
                                      │
                ┌─────────────────────┼─────────────────┐
                │                     │                 │
       Canonical Markdown       Chunk/Search       Export Sink
                │                     │
                │                Vector/RAG
                └────────────── Derived Projections
```

真相边界：

```text
PostgreSQL + Object Storage = Truth
Redis Streams              = Transport
Crawler internal frontier  = Execution state only
Crawler dispatcher         = Worker-local execution control only
Browser/session cache      = Ephemeral execution state
OpenSearch / Vector Store  = Rebuildable Projection
Canonical Markdown         = Rebuildable Projection from IR
Fit/Enhanced Markdown      = Rebuildable Derived Projection
Dashboard Read Model       = Rebuildable Projection
n8n/MCP/Agent state        = External automation state only
```

任何派生系统故障都不得把已经成功保存的 `Snapshot` 或 `Document Version READY` 回退成失败。

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST/Command API、Schema 校验、Webhook Receiver、内部服务接口；
- **PostgreSQL**：Source、Profile、Run、Task、Coverage、Observation、Snapshot metadata、Document、Version、Release、Audit、Outbox；
- **Transactional Outbox**：保证数据库状态与异步消息发布一致；
- **Redis Streams**：consumer group、retry transport、backpressure，只做任务运输；
- **React/Next.js 或 Vue + TypeScript**：Web 管理台；
- **Docker/Kubernetes**：按 Discovery / HTTP Fetch / Browser / Extract / Projection Worker 独立扩缩容。

### 4.2 抓取与解析

- **HTTPX/aiohttp**：静态 HTML、Feed、Sitemap、API、Conditional GET、Range GET；
- **Crawl4AI**：异步多 URL 抓取、Browser、URL Seeder、Deep Crawl、Markdown candidate、Worker 内 Dispatcher；
- **Playwright**：复杂 JS、滚动/点击、多步骤交互等特殊 Recipe fallback；
- **lxml / BeautifulSoup / trafilatura / readability 类算法**：正文候选抽取；
- **PyMuPDF / pdftotext**：PDF 原生文本解析；扫描型 PDF 才进入 OCR fallback；
- **RSS/Atom/JSON Feed parser、XML Sitemap parser**：权威发现入口。

### 4.3 存储与检索

- **S3/MinIO**：原始响应、HTML/PDF、Snapshot blob、Canonical IR、导出包；
- **OpenSearch**：BM25、字段过滤、聚合、短语/代码/错误信息检索；
- **pgvector**：首期向量检索；规模扩大时通过 Vector Adapter 替换 Qdrant/Milvus；
- **PostgreSQL JSONB**：配置、evidence、quality metrics，小型结构化数据。

---

## 5. Crawler Adapter 与 Runtime Release

Crawler 不直接暴露给业务层，统一通过稳定接口：

```text
CrawlerAdapter
  probe(url, profile) -> ProbeResult
  fetch_one(task, profile) -> FetchResult
  fetch_many(tasks, profile) -> AsyncIterator[FetchResult]
  discover(seed, profile) -> AsyncIterator[DiscoveryObservation]
  capabilities() -> CapabilitySet
```

`CapabilitySet` 至少描述：

```text
HTTP_FETCH
BROWSER_RENDER
JS_INTERACTION
STREAMING_BATCH
DEEP_CRAWL
SITEMAP_SEEDING
COMMON_CRAWL_SEEDING
MARKDOWN_CANDIDATE
STRUCTURED_EXTRACTION
PROXY
SESSION_REUSE
```

Crawl4AI 版本升级必须进入 `crawler_runtime_release`：

```text
crawler_runtime_release
  id
  adapter_name
  adapter_version
  crawl4ai_version
  playwright_version
  chromium_version
  docker_image_digest
  config_schema_version
  benchmark_result_json
  status              DRAFT | TESTED | ACTIVE | RETIRED
  activated_at
```

禁止生产环境无审核 `pip install -U`。新 Runtime 先对 Golden Corpus 比较抓取成功率、Markdown diff、代码/表格保留率、Browser crash、内存峰值、429/重试，再激活。

---

## 6. 核心数据模型

### 6.1 Source 与配置

```text
source
  id
  name
  base_url
  source_type        BLOG | DOCS | NEWSLETTER | OTHER
  status             PROBING | ACTIVE | PAUSED | DISABLED
  profile_release_id
  sync_policy_id
  created_at / updated_at

source_profile_release
  id
  source_id
  version
  config_json
  fingerprint
  status             DRAFT | TESTED | ACTIVE | RETIRED
  created_by
```

`config_json` 包含允许域、路径 allow/deny、canonical 规则、Feed/Sitemap/CMS 入口、Archive pattern、正文 selector、发布日期/作者规则、Browser recipe、robots 策略、速率上限、语言、历史截止时间、FetchMode 等。

### 6.2 发现与 Coverage

```text
discovery_run
  id
  source_id
  run_type           BACKFILL | INCREMENTAL | VERIFY
  provider           CMS | SITEMAP | FEED | ARCHIVE | TOC | SEEDER | CC | DEEP
  release_id
  cursor_json
  status
  exhausted_reason
  started_at / finished_at

url_observation
  id
  source_id
  discovered_url
  normalized_url
  provider
  provider_item_id
  provider_timestamp
  evidence_json
  filter_decision
  filter_reason
  observed_at

coverage_state
  source_id
  provider
  release_id
  checkpoint_json
  is_exhausted
  known_gap_json
  evidence_summary
  verified_at
```

Provider 每读取一页/一小批就持久化 Observation 和 checkpoint，不能把几十万 URL 留在 Python 内存。

### 6.3 Task 与 Snapshot

```text
fetch_task
  id
  source_id
  normalized_url
  task_kind
  priority
  state
  attempt
  lease_owner
  lease_until
  fencing_token
  idempotency_key
  next_run_at

fetch_attempt
  id
  task_id
  adapter_release_id
  route               HTTP | CRAWL4AI_BROWSER | PLAYWRIGHT_RECIPE
  started_at / finished_at
  status_code
  failure_class
  dispatcher_wait_ms
  dispatcher_active_peak
  process_rss_peak_mb
  browser_pages_peak
  metrics_json

snapshot
  id
  source_id
  requested_url
  final_url
  fetched_at
  status_code
  headers_json
  redirect_chain_json
  mime_type
  body_object_key
  body_sha256
  byte_size
  fetch_release_id
  network_evidence_json
```

Snapshot 不可变。解析失败不能删除 Snapshot；Extractor/Markdown 升级可直接离线重放。

### 6.4 Document、Version 与 IR

```text
document
  id
  source_id
  canonical_identity
  canonical_url
  first_seen_at
  latest_version_id
  lifecycle          ACTIVE | SUSPECT_MISSING | MISSING | RETIRED

document_version
  id
  document_id
  snapshot_id
  canonical_content_sha256
  extractor_release_id
  ir_release_id
  status             READY | QUARANTINED
  title / author / published_at
  created_at

ir_block
  id
  document_version_id
  block_index
  block_type          HEADING | PARAGRAPH | CODE | LIST | TABLE | QUOTE | IMAGE
  text
  text_sha256
  locator_json
  attributes_json
```

URL 是定位器，不是 Document 身份。requested/final/canonical URL 分开保存，redirect 和 canonical 都只是身份判断证据。

---

## 7. 新站点接入与 Probe

新增 Source 不直接全站开爬，先执行 `PROBE`：

1. DNS/TLS/robots/redirect/egress 安全检查；
2. 探测 `sitemap.xml`、robots 中 sitemap、RSS/Atom、JSON Feed；
3. 识别 WordPress、Ghost、Substack、自建 CMS、静态站、文档框架等特征；
4. 探测公开 CMS API、分页方式、Archive/Category/Tag；
5. 抽样 10～30 个正文 URL，比较 HTTP 抽取与 Browser 抽取质量；
6. 判断是否需要 JS、滚动、点击或特殊 Recipe；
7. 形成 Source Profile 草稿；
8. 运行 dry-run，只产生 Observation/Preview；
9. Web UI 审核后激活 Profile Release。

典型配置：

```yaml
source:
  allowed_hosts: [example.com, www.example.com]
  include_paths: [/blog/, /posts/]
  exclude_paths: [/tag/, /author/, /search/, /login/]

discovery:
  providers:
    - {type: cms_api, priority: 10, required: true}
    - {type: sitemap, priority: 20, required: true}
    - {type: feed, priority: 30}
    - {type: archive, priority: 40}
    - {type: common_crawl, priority: 80, mode: gap_only}
    - {type: deep_crawl, priority: 90, mode: gap_only}

fetch:
  mode: AUTO
  requests_per_second: 0.5
  max_concurrency: 2
  browser_fallback: true

extract:
  preferred_selectors: [article, main]
  remove_selectors: [nav, footer, .newsletter-signup]
```

所有规则属于版本化 Release，不能静默修改生产行为。

---

## 8. 全量历史发现与 Coverage 证明

### 8.1 Provider 优先级

按权威性和历史覆盖排序：

1. CMS/API；
2. Sitemap/Sitemap Index；
3. Feed；
4. Archive/年月页/分页列表；
5. Category/Tag、Docs TOC；
6. Crawl4AI URL Seeder / Common Crawl 历史索引补洞；
7. 受控 Deep Crawl 最后补洞。

### 8.2 两阶段：Discover -> Fetch

```text
Discover Stage
 CMS/Sitemap/Feed/Archive/Seeder/CC/Deep
 -> URL Observation
 -> normalize/scope/filter
 -> dedupe
 -> durable frontier

Fetch Stage
 eligible URL
 -> HTTP First
 -> Browser fallback
 -> Snapshot
 -> Extract/IR/Markdown
```

Deep Crawl 和 URL Seeder 只能产生 Observation，不能绕过平台 Filter/Scope 直接把任意站内链接当正文抓取。

### 8.3 Durable Streaming Frontier

```text
fetch provider page
 -> parse items
 -> upsert observation in small batch
 -> persist provider cursor/checkpoint
 -> enqueue eligible URL task
 -> commit
 -> next page
```

### 8.4 Coverage 完成条件

`BACKFILL COMPLETE` 至少满足：

- required authoritative provider 明确耗尽；
- provider cursor/checkpoint 已持久化；
- known gap 为空或有人工接受理由；
- discovered/allowed/fetched/filtered/failed 数量可核对；
- 对年份、月份、分页边界做交叉验证；
- Common Crawl/Deep Crawl 补洞不会发现大量新正文；
- robots/policy/长期失败 URL 被单独计为可解释缺口。

“Deep Crawl 没有新链接”或“队列为空”都不能单独证明历史完整。

---

## 9. URL 规范化与过滤

默认规则：

- scheme/host 小写；
- 去 fragment；
- 规范默认端口；
- 删除明确追踪参数 `utm_*`、`gclid` 等；
- query 排序/删除按 Source Profile 配置，避免全局激进处理；
- 保留 requested URL、final URL、canonical URL；
- `<link rel=canonical>` 仅是证据之一，跨域 canonical 必须审核。

Filter：

```text
HARD           永不抓：登录、搜索结果、管理页
SOFT           默认不抓，可规则/人工覆盖
DEPRIORITIZE   可抓但低优先级：Tag 聚合页
ALLOW          正常正文抓取
```

每次决定保存 `filter_release_id + reason + evidence`，未来规则变化可对历史 Observation 重判。

---

## 10. Durable Task、幂等与恢复

PostgreSQL 保存 durable Task state；Transactional Outbox 在同一事务写事件；Publisher 投递 Redis Streams；Worker 通过 consumer group 消费。Redis 丢失可以从 PostgreSQL 重建运输队列。

Task 状态：

```text
PENDING -> LEASED -> RUNNING -> SUCCEEDED
                    |          -> PARTIAL
                    -> RETRY_WAIT -> PENDING
                    -> QUARANTINED
                    -> DEAD
```

Worker 领取任务获得 `lease_until + fencing_token`。超时后新 Worker 可重新 lease；旧 Worker 即使恢复，也不能用过期 token 覆盖新结果。

幂等键示例：

```text
DISCOVER: source + provider + release + cursor
FETCH: source + normalized_url + sync_run
EXTRACT: snapshot + extractor_release
MARKDOWN: version + markdown_release + projection_type
EMBED: chunk + embedding_release
EXPORT: export_spec_hash + release
```

每阶段都必须允许安全重放。

---

## 11. 抓取路由与 Crawl4AI 执行模型

### 11.1 FetchMode

```text
AUTO
HTTP_ONLY
BROWSER_REQUIRED
RECIPE_REQUIRED
```

AUTO 路由：

```text
CMS/API/Feed/Sitemap -> HTTP
static HTML          -> HTTP -> extraction quality gate
PDF/file             -> HTTP stream -> Snapshot -> parser
JS shell/low quality -> Crawl4AI Browser
special interaction  -> Playwright/Crawl4AI Recipe
```

判定 Browser fallback 的证据包括：可见正文长度、DOM/文本密度、JS shell 特征、正文 selector 命中、抽取质量、Profile 配置。

### 11.2 有界多 URL 流式执行

Crawl4AI `fetch_many/arun_many` 只处理 Worker 已 lease 的有界窗口，例如 20～100 条，不接收全站几十万 URL。

```text
lease bounded tasks
 -> acquire global/domain/source permits
 -> Crawl4AI streaming batch
 -> each result immediately persist Snapshot or retry
 -> release permits
```

成功结果逐 URL 提交，长尾或失败 URL 不拖住已完成项。

### 11.3 分层 Permit

```text
global concurrency
 -> domain concurrency/rate
 -> source concurrency/rate
 -> worker-local MemoryAdaptiveDispatcher/Semaphore
```

MemoryAdaptiveDispatcher 解决本机内存压力，不替代全局/domain/source 限流。多个 Worker 对同一域名的总请求速率必须由平台控制。

### 11.4 Browser 复用与隔离

```text
Browser process scope = WORKER
Context scope         = SOURCE | SECURITY_DOMAIN
Page scope            = TASK
Session scope         = RECIPE_INSTANCE
```

- Browser 进程长期复用；
- Context 按 Source/安全域隔离；
- Page 默认每任务短生命周期；
- session 只用于明确的顺序交互，不用于并行任务共享；
- Cookie/LocalStorage/认证态/代理身份不得跨 Source；
- page 数、RSS、任务数、运行时间到阈值主动 recycle；
- Browser 下载、弹窗和外部导航受 egress policy 限制。

### 11.5 运行指标

`fetch_attempt` 持久化：

```text
dispatcher_wait_ms
dispatcher_active_peak
process_rss_peak_mb
browser_pages_peak
browser_recycle_count
local_throttle_count
route
adapter_release_id
```

用于 Web 管理和容量规划，但不作为业务真相。

---

## 12. Snapshot First 与内容解析

抓取成功后：

```text
network result
 -> stream body to object storage
 -> sha256
 -> persist snapshot metadata
 -> commit
 -> emit SNAPSHOT_READY
```

Extract Worker 从 Snapshot 读取，不依赖源站再次在线。

正文抽取采用多候选：

```text
site selector candidate
trafilatura candidate
readability candidate
Crawl4AI candidate
DOM density candidate
```

统一质量评分包括：正文长度、标题一致性、链接密度、导航污染、代码块保留率、表格完整性、时间/作者命中、语言异常。选择结果保存 `extraction_evidence`。

Extractor 升级先跑 Golden Corpus：技术文章、大量代码、表格、多页、Newsletter、国际化、动态站、边界失败样本。

---

## 13. Canonical IR 与 Markdown 分层

### 13.1 IR 先于 Markdown

```text
DocumentVersion
  metadata
  blocks[]
    heading(level, text)
    paragraph(text)
    code(language, exact_text)
    list(items)
    table(cells)
    quote(text)
    image(src, alt, caption)
```

Markdown 是 IR 的确定性 Renderer，后续格式变化无需重抓源站。

### 13.2 三种 Markdown Projection

```text
CANONICAL_MARKDOWN
  完整正文，由 Canonical IR 确定性生成；默认展示、导出和事实引用。

FIT_MARKDOWN
  Pruning/BM25/LLM 等有损过滤结果；用于检索、摘要或 query-specific context。

ENHANCED_MARKDOWN
  可叠加内部链接、标注或展示增强；不能替代事实版本。
```

Crawl4AI 返回的 raw/fit Markdown 可作为候选/派生结果，但不得绕过 Snapshot/IR 直接成为不可追溯事实。过滤阈值变化只重建 FIT Projection，不能制造 Document Version。

### 13.3 Canonical Markdown 规则

- 标题层级稳定；
- 代码块保留语言标签和原始空白；
- 表格稳定转换则使用 GFM，否则保留 HTML block 并标记；
- 图片保留绝对原链接、alt/caption，可选独立镜像；
- 相对链接转绝对 canonical URL；
- 删除脚本、导航、Cookie banner、广告、相关推荐，但保留正文引用和脚注；
- 不把 AI 摘要、自动链接写入 Canonical Markdown。

导出结构：

```text
knowledge/
  sources/{source_slug}/{yyyy}/{slug}--{document_id}.md
```

Front Matter：

```yaml
---
document_id: doc_xxx
version_id: ver_xxx
snapshot_id: snap_xxx
source: source_slug
source_url: https://...
canonical_url: https://...
title: ...
author: ...
published_at: ...
fetched_at: ...
content_sha256: ...
---
```

文件名变化不影响内部稳定 ID。

---

## 14. 增量同步、版本与删除检测

### 14.1 发现增量

优先使用：

1. CMS API `updated_at` / cursor；
2. Sitemap `lastmod`；
3. Feed entry id/guid/published/updated；
4. Archive 最近若干页；
5. 定期低频 Coverage Verify / Deep Crawl 补洞。

每个 Provider 单独保存 checkpoint，不能只有一个模糊的 `last_sync_time`。

### 14.2 Conditional Fetch

存在 ETag/Last-Modified 时发送 `If-None-Match/If-Modified-Since`：

```text
304
 -> update observation/fetch evidence
 -> no new Document Version

200 + body hash same
 -> no extraction

200 + body hash changed
 -> extract Canonical IR
 -> canonical content hash same: template/ads/noise change
 -> canonical content hash changed: create new Version
```

### 14.3 自适应调度

Source 记录最近发布间隔、变化率和同步延迟：

```text
high recent change rate -> shorter interval
long no change          -> longer interval
new feed entry          -> immediate incremental run
coverage verify         -> independent slow schedule
```

实际上下限由 Web UI 配置。

### 14.4 删除/失踪

单次 404/5xx 不立即删除：

```text
ACTIVE -> SUSPECT_MISSING -> MISSING
       -> ACTIVE（再次出现）
```

综合 Sitemap/CMS 消失、连续 404、redirect/迁移证据判断；历史 Snapshot 永久按生命周期政策保存。

---

## 15. 去重、转载与关系

分层候选：

1. exact canonical content hash；
2. SimHash/MinHash/shingles；
3. 标题/作者/发布时间；
4. embedding 近邻；
5. LLM 只作为可审计关系判别补充证据。

关系类型：

```text
SAME_WORK
REPUBLISHED
TRANSLATION_OF
RELATED
CONTRASTS
SUPPORTS
```

相似度是证据，不直接删除或不可逆合并 Document。UI 可折叠转载，但底层 URL/Snapshot/Version 保留，可撤销误判。

---

## 16. Search / RAG

索引对象：Document metadata、IR Block/Chunk、Canonical Markdown、可选 FIT Markdown、AI Summary/Insight（单独类型）。

Hybrid Retrieval：

```text
query
 -> metadata/source/date/language filter
 -> BM25 candidates
 -> vector candidates
 -> union/RRF
 -> optional reranker
 -> diversity/relation collapse
 -> source block evidence expansion
```

技术知识不能只依赖向量：版本号、API 名、异常字符串、代码符号、命令参数由 BM25/字段检索更可靠。

所有高可信回答应能回到 `Document Version + IR Block`，AI 摘要只是 CONTEXT/RANKING_SIGNAL，不能成为唯一证据。

---

## 17. Web 管理功能

### 17.1 Source/Profile

- Source 列表、状态、标签、Owner；
- 新增/编辑 Source；
- Probe 结果与 Profile 草稿；
- Feed/Sitemap/API/Archive/JS 探测；
- allow/deny path、canonical、selector、FetchMode、Browser Recipe 编辑；
- Profile Release Diff、测试、激活、回滚。

### 17.2 抓取与 Coverage

- Backfill/Incremental/Verify Run；
- Provider cursor、exhaustion、known gaps；
- discovered/allowed/fetched/failed/filtered 计数；
- 年/月历史覆盖热图；
- Task retry/quarantine/dead-letter；
- 域名限流、429/403、Browser fallback、资源消耗；
- HTTP/Browser 路由比例；
- Dispatcher 等待、峰值内存、Browser recycle。

### 17.3 文档与版本

- Document/Version 列表；
- requested/final/canonical URL；
- Snapshot headers、redirect、hash；
- 原始 HTML 与 Canonical IR/Markdown 对比；
- Canonical/FIT/Enhanced Projection 切换；
- 版本 Diff；
- 重新 Extract/Render/Chunk/Embed，不重新访问网络；
- 单篇/Source/时间段/全库 Markdown 导出。

### 17.4 Runtime/Release

- Crawl4AI/Playwright/Chromium Runtime Release；
- Golden Corpus benchmark；
- Adapter/Extractor/Markdown Release 激活和回滚；
- Worker Pool 版本分布；
- 失败率、内存、Browser crash 对比。

### 17.5 权限与审计

RBAC：Viewer、Operator、Editor、Admin。修改 Profile、重跑、Runtime 激活、任务重试、导出、删除等高风险操作全部写 Audit Log。

---

## 18. API、Webhook 与外部自动化

核心接口：

```text
POST /sources
POST /sources/{id}/probe
POST /sources/{id}/runs/backfill
POST /sources/{id}/runs/incremental
GET  /sources/{id}/coverage
GET  /runs/{id}
POST /tasks/{id}/retry
POST /snapshots/{id}/reextract
GET  /documents/{id}/versions
POST /exports
GET  /crawler-runtimes
POST /crawler-runtimes/{id}/activate
```

写接口支持 `Idempotency-Key`。

Webhook 只发布事实事件：

```text
source.run.completed
document.version.ready
document.version.changed
task.quarantined
coverage.warning
crawler.runtime.activated
```

n8n/MCP/Agent 可以调用 API 或消费 Webhook，但不得直写核心数据库、直接决定 Coverage 或以自己的执行记录代替 Task state。

---

## 19. 安全、合规与源站友好

### 19.1 SSRF/Egress

用户输入 URL、HTTP redirect、Browser navigation、附件 follow link、Webhook callback 每跳都重新解析和校验：阻止 loopback、link-local、RFC1918、metadata endpoint、未允许端口/协议，并防 DNS rebinding。

### 19.2 内容安全

- body 最大尺寸；
- 压缩比限制，防 decompression bomb；
- PDF/附件解析进沙箱 Worker；
- Browser 禁止任意文件系统访问；
- Markdown/HTML 展示做 XSS sanitize；
- Secret 使用 Vault/K8s Secret，不写 Profile 明文。

### 19.3 访问策略

- 尊重 robots 和组织合规策略；
- 明确 User-Agent/联系方式；
- 低速默认、按域名退避；
- 429/Retry-After 优先遵守；
- 403/CAPTCHA 进入 `POLICY_BLOCKED` 或人工审核；
- Proxy 只用于合法出口故障切换、地域访问测试或组织网络策略，不默认用于绕过访问控制；
- 对登录墙、付费墙和明确禁止访问内容不绕过控制。

---

## 20. 可观测性与 SLO

抓取指标：

```text
fetch_success_rate
http_304_rate
429_rate / 403_rate
p50/p95 latency
bytes_downloaded
browser_fallback_rate
browser_crash_rate
retry_rate
queue_lag
dispatcher_wait_ms
process_rss_peak_mb
browser_pages_peak
```

Coverage：

```text
provider_exhausted_ratio
known_gap_count
historical_year_coverage
observation_to_document_ratio
failed_url_count
unresolved_redirect_count
```

内容质量：

```text
extraction_quality_score
boilerplate_ratio
code_block_preservation
table_preservation
metadata_confidence
version_churn_rate
markdown_render_error
```

检索质量：

```text
retrieval_recall@k
source_block_grounding_ratio
projection_lag
embedding_rebuild_lag
```

日志统一带 `trace_id/run_id/task_id/source_id/document_id/snapshot_id`；Prometheus/Grafana 做指标，OpenTelemetry 做 trace。

---

## 21. Projection Generation、升级与回放

全文、向量、FIT Markdown 等派生物使用 generation：

```text
build generation N+1
 -> validate corpus/metrics
 -> compare active N
 -> atomic alias/pointer switch
 -> keep N for rollback window
```

Extractor/Markdown/Crawler Runtime 升级都先跑固定回归集。因为 Snapshot 已保存，Extractor、IR、Markdown、Chunk、Embedding 可以完全离线重建。

Release Registry：

```text
profile_release
crawler_runtime_release
fetch_adapter_release
extractor_release
ir_release
markdown_release
chunk_release
embedding_release
metric_release
export_release
```

---

## 22. 存储、备份与生命周期

PostgreSQL：PITR/WAL 备份；Observation/Task 大表按时间或 source hash 分区；定期检查 FK、孤儿对象、Outbox backlog。

Object Storage key：

```text
snapshots/{source_id}/{yyyy}/{mm}/{snapshot_id}/body
ir/{document_id}/{version_id}/{ir_release}.json
exports/{export_id}/...
```

开启 versioning/生命周期策略。OpenSearch/Vector 都视为 Projection，必须有从 PostgreSQL + Object Storage 重建的作业和演练。

---

## 23. 部署与横向扩展

Worker 分池：

```text
discovery-worker
http-fetch-worker
browser-fetch-worker
extract-worker
projection-worker
embed-worker
export-worker
```

Browser、Embedding、HTTP 抓取的资源模型不同，必须独立扩缩容。Crawl4AI 可以部署为固定镜像的内部执行服务，也可以嵌入 Browser Worker；无论哪种方式都只是 Adapter Runtime，不保存平台业务真相。

Kubernetes HPA 指标：queue lag、CPU、memory、Browser slots、dispatcher pressure，而不只看 CPU。

1000 Source 运行原则：

- 默认每域低并发；
- 横向增加不同域名 Worker，不提高单站攻击性并发；
- Backfill 和 Incremental 使用不同 priority，增量优先；
- Browser Pool 与 HTTP Pool 分开；
- 大站 discovery/fetch 全程有界窗口；
- 每 Source 可独立暂停、限流、重跑和回滚 Profile。

初始容量不要写死，可用如下基线压测后调整：

```text
HTTP fetch slots:     50~200 / cluster
Browser pages:        10~40 / cluster
per-domain HTTP:      1~4
per-domain Browser:   1~2
worker lease window:  20~100 URLs
```

真正的并发上限由源站策略、429、延迟、内存和 Browser crash 数据自动调小。

---

## 24. 两条关键执行链

### 24.1 首次历史回填

```text
Create Source
 -> PROBE
 -> Profile Review/Activate
 -> BACKFILL Run
 -> authoritative provider discovery
 -> streaming Observation + checkpoint
 -> Seeder/CC/Deep gap verification
 -> filter/canonicalize/dedupe
 -> Fetch Task
 -> platform permits
 -> HTTP / Crawl4AI Browser / Recipe
 -> immutable Snapshot
 -> Extract
 -> Canonical IR
 -> Document/Version READY
 -> Canonical Markdown
 -> async FIT/Search/Vector Projection
 -> Coverage Verify
 -> COMPLETE or COMPLETE_WITH_GAPS
```

### 24.2 增量同步

```text
schedule/feed trigger
 -> CMS/Sitemap/Feed/Archive incremental discovery
 -> Observation
 -> new URL: fetch
 -> known URL: conditional fetch
 -> 304: observation only
 -> 200 same body hash: no extract
 -> 200 changed body but same canonical hash: no version
 -> changed canonical hash: new Version
 -> rebuild affected projections only
 -> emit change event
```

---

## 25. 失败模式与降级

- **Redis 不可用**：暂停运输，PostgreSQL Task 保持真相，恢复后重发 Outbox；
- **Object Storage 不可用**：不得把网络抓取标记为持久成功；延迟重试；
- **PostgreSQL 不可用**：Worker 停止领取新任务，避免产生无法记录的事实；
- **Crawl4AI Browser crash**：单 URL/小窗口重试，必要时 recycle Worker，不影响 HTTP Pool；
- **内存压力**：Worker Local Dispatcher 降并发，同时平台根据 telemetry 降低 Browser slots；
- **大量 429/503**：domain/source circuit breaker + Retry-After + jitter backoff；
- **Extractor 质量下降**：Version 进入 Quarantine，保留 Snapshot，可切换旧 Extractor Release 离线重跑；
- **Crawler Runtime 升级回归失败**：保持旧 ACTIVE Release，不影响已保存事实；
- **站点结构变化**：触发 Probe/Profile Warning，而不是自动写站点专用代码；
- **Deep Crawl 爆炸**：max depth/max pages/max wall time/max external domains 立即截断并记录 stop reason。

---

## 26. 实施阶段

### Phase 1：事实层与 20～50 个站点

- Source/Profile/Release；
- Sitemap/Feed/CMS/Archive Discovery；
- PostgreSQL durable Task + Outbox + Redis Streams；
- HTTP Fetch、Snapshot、Object Storage；
- 基础 IR/Canonical Markdown；
- Web Source/Run/Task/Document 页面；
- ETag/Last-Modified 增量；
- Markdown 导出。

### Phase 2：1000 站全量能力

- Durable Streaming Frontier；
- Crawl4AI Adapter + Runtime Release；
- URL Seeder/Common Crawl/Deep Crawl gap workflow；
- Browser Pool/Session 隔离/MemoryAdaptiveDispatcher；
- Coverage Evidence/Gap Dashboard；
- 多候选 Extractor + Golden Corpus；
- Backfill/Incremental 优先级和自动限流；
- Canonical/FIT Markdown 分层；
- 版本 Diff、离线 Reprocess；
- OpenSearch/pgvector。

### Phase 3：知识检索与治理

- Hybrid RAG；
- block-level provenance；
- 转载/翻译关系；
- Projection generation 原子切换；
- Runtime/Extractor benchmark 页面；
- PDF 可选证据通道。

### Phase 4：高可用

- 多副本 API/Worker；
- DB/对象存储备份演练；
- 安全扫描、SSRF/Egress 强化；
- SLO、告警、成本和权限治理完善。

---

## 27. 验收标准

### 全量历史

- 随机不同 CMS/静态站/文档站均能给出 Provider Coverage Evidence；
- Worker 中断后从 checkpoint 继续，不重复制造 Document Version；
- 百万级 URL Discovery 保持有界内存；
- Deep Crawl/Seeder 只能补洞，不能绕过 Scope/Filter。

### 增量

- 新文章按 Source SLA 被发现；
- 304 不创建新 Version；
- 模板变化不制造大量假版本；
- 正文变化只重建受影响派生物。

### Markdown

- 标题、正文、代码块、表格、列表、引用、链接稳定；
- 同一 IR + Markdown Release 输出确定；
- CANONICAL 与 FIT 语义分离；
- 任意导出 Markdown 可回溯到 `document_id/version_id/snapshot_id`。

### 可恢复性

- Redis 清空后可从 DB 恢复待办；
- Worker lease 超时不会让旧 Worker 覆盖新 Worker；
- Snapshot 存在时 Extractor/Markdown/Embedding 可完全离线重跑；
- Crawl4AI/Browser 进程崩溃不丢已成功 Snapshot。

### 扩展性

- 第 1001 个站点主要通过 Probe + Profile 配置接入；
- Crawler 升级通过 Runtime Release/Adapter 隔离；
- Browser Worker 可独立扩缩容；
- 新发现 Provider 可通过 Provider Adapter 加入，而不重写事实层。

### Web 管理

- 能查看 Coverage、失败、版本 Diff、Snapshot、Release、Crawler Runtime 和任务状态；
- 能查看 Dispatcher/Browser 资源压力和路由原因；
- 能重试、暂停、重处理、导出、激活/回滚 Release；
- 所有高风险管理操作可审计。

最终系统应把“抓到网页”升级为一套可证明 Coverage、可重放事实、可稳定增量、可安全扩展的长期数据平台：Crawl4AI 提供高效异步、Browser、Deep Crawl、URL Seeder 和 Markdown 候选能力，但它必须位于版本化 Adapter 和平台调度边界之后；PostgreSQL + Object Storage 保存真相，Durable Frontier 保证全量历史可恢复，Canonical IR 保证 Markdown 可重建，Canonical/FIT Projection 分层保证 RAG 优化不会污染来源事实。