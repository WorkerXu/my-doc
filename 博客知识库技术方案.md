# 博客知识库技术方案

## 1. 建设目标

建设一套面向 **1000+ 技术博客、工程博客、技术文档站和公开 Newsletter** 的长期知识库采集平台，并可继续扩展到数千站点。

平台需要同时解决：

1. **历史尽量抓全**：知道文章有哪些、从哪里发现、为什么认为 Provider 已到边界、还有哪些 Known Gap；
2. **持续增量同步**：及时发现新文章、修改、迁移和删除；
3. **内容稳定可用**：把 HTML、动态页面、PDF 等转换为可追溯的 Canonical IR 和 Markdown；
4. **低成本可扩展**：新增站点主要依赖 Source Profile/Recipe，不为每个站点长期维护一套专用爬虫；
5. **Web 可管理**：支持接入、Probe、审核、调度、回放、失败处理、质量、Coverage、成本和 Release 管理；
6. **结果可审计**：任意 Markdown 都能定位到 Source、URL、Snapshot/Render Artifact、Recipe、Runtime、Extractor、IR、Quality、Version 和 Release；
7. **AI 友好但不牺牲事实完整性**：Canonical 内容与面向 RAG 的 Fit/Filter 投影明确分离。

最终提供：

- 全历史文章 URL Discovery + Backfill；
- Sitemap/RSS/CMS/API/Archive/动态列表/受限 Deep Crawl 等多 Provider；
- 定时 Incremental、Known URL Verify、删除/迁移判定；
- HTTP-first、Browser-last 的正文获取；
- 动态栏目 Infinite Scroll / Load More / Virtual Scroll；
- 模板级 Deterministic Extraction Recipe（CSS/XPath/Regex）与通用正文抽取双轨；
- 原始事实、Canonical IR、Canonical Markdown、AI Fit Markdown、全文和向量索引分层保存；
- Web 管理、RBAC、审计、成本治理、告警、Canary/回滚；
- Markdown/JSONL/IR 等批量与增量导出。

### 1.1 核心 SLO

平台不以 HTTP 200 比例作为核心 SLO，而以业务结果衡量：

- **Coverage**：历史 URL 覆盖、Provider exhaustion evidence、Known Gap；
- **Freshness**：新文章从发布到进入 PASS Canonical Markdown 的延迟；
- **Quality**：正文完整率、噪声率、结构质量、Quarantine 比例；
- **Data Supply**：URL 从首次发现到 `Quality=PASS` 且投影可用的 `time_to_usable_data`；
- **Reliability**：任务成功率、Retry/DLQ、队列年龄、Worker 故障恢复、Browser 资源回收；
- **Cost**：每 Source、每 1000 个 PASS 文档、每个 accepted version 的网络/CPU/Browser/存储/LLM 成本；
- **Traceability**：任意结果可回溯到不可变事实和版本化执行环境。

### 1.2 非目标

- 不默认绕过登录墙、付费墙、CAPTCHA、WAF 或明确访问控制；
- 不用 LLM 主观判断代替 Coverage 证明；
- 不把 Redis、Browser session、Crawler cache/frontier 当业务真相；
- 不允许 Web/API 直接向 Browser 透传任意 JavaScript、Playwright evaluate、代理或系统参数；
- 不把 LLM 摘要/重写作为 Canonical Markdown；
- 不把 BM25/Pruning/Fit Markdown 作为 Canonical 内容事实；
- 不把标题、slug、图片 alt、图片 URL 或展示文本当跨阶段唯一主键；
- 不因为 URL 曾经抓过就永久停止复检；
- 不把 crawler `success=true`、HTTP 200 或“最终 HTML 看起来较长”直接等价为正文成功或 Coverage 完成。

---

## 2. 核心设计原则

1. **Coverage First**：先证明“文章有哪些”，再批量抓正文。
2. **Authoritative First**：CMS/API、Sitemap、Feed、Archive 优先，动态列表/Deep Crawl 用于补洞。
3. **Discovery Before Fetch**：URL 发现、规范化、Scope、分类和去重独立于正文抓取。
4. **Discover Cheap, Fetch Expensive**：低成本 Provider 先跑，Browser 后置。
5. **HTTP First, Browser Last**：静态 HTTP 优先；确定性 Browser fallback；Agent Browser 极少使用。
6. **Deterministic Before Agentic**：主链在没有 LLM 时也能完成 Backfill 与 Incremental。
7. **Snapshot First**：先保存不可变抓取事实，再做 Extract/IR/Markdown。
8. **IR First, Markdown as Projection**：Canonical IR 是内容事实，Markdown 是可重建投影。
9. **Canonical Before Fit**：Pruning/BM25/Fit Markdown 只做 AI/RAG 投影，不改变 Canonical Version。
10. **Quality Is First-Class**：传输成功与内容合格是两套状态。
11. **Cost Is First-Class**：高成本 Browser/LLM 必须可归因、可预算。
12. **Tool Contract, Not Framework Coupling**：Crawl4AI/Playwright/httpx 通过 Adapter 接入。
13. **Recipe, Not Site Code**：selector、pagination、scroll、click、virtual scroll、结构化 extraction 通过版本化 Recipe 表达。
14. **Semantic Recipe, Not Runtime Parameters**：Recipe 表达业务动作语义，不绑定某版本 crawler 参数名。
15. **Capability != Outcome**：Runtime Capability 说明“支持什么”，Runtime Outcome 说明“本次实际执行了什么”。
16. **Business Identity Over Runtime Dedup**：文章 Coverage 以规范化 URL/URL Identity 为准，不依赖 crawler 内部 DOM/文本去重。
17. **Stable Identity**：Source、URL、Artifact、Document、Version 使用稳定不可变 ID 关联。
18. **Idempotent + Recoverable**：Task 有幂等键、lease、fencing token、checkpoint、retry、DLQ。
19. **Fair by Default**：大站 Backfill 不得长期饿死 Incremental 和小站。
20. **Micro-batch Is Execution Optimization**：批量/streaming 只优化 Worker 吞吐，Task 仍是幂等、重试、落库和审计单位。
21. **Detect Drift Online**：Golden Corpus 防平台升级回归，Drift Detector 防站点改版静默退化。
22. **Security on Every Hop**：start URL、redirect、href、iframe、Asset、Browser navigation、proxy destination 每跳重新校验。
23. **Release Everything That Changes Results**：Profile、Recipe、Compiler、Runtime、Extractor、Schema、Renderer、Filter 全版本化。
24. **Projection Is Rebuildable**：Markdown、Fit Markdown、全文索引、向量和其他派生结果可从事实层重建。
25. **Fail Closed on Semantic Incompatibility**：运行时无法保持 Recipe 语义时阻止 Release，不能静默少抓。
26. **No Silent Fallback for Coverage**：请求的动态能力没有真正生效时必须显式降级为 Known Gap/DEGRADED，而不是借 crawler success 宣称抓全。

---

## 3. 总体架构

```text
                         Web Admin / REST API / CLI / MCP
                                      |
                          API + RBAC + Idempotency
                                      |
                            Command / Query Layer
                                      |
                         PostgreSQL Truth Store
                                      |
                           Transactional Outbox
                                      |
                              Redis Streams
                                      |
                 Global Admission + Weighted Fair Scheduler
                                      |
       +----------------------+-------+----------------------+----------------+
       |                      |                              |                |
   Discovery             HTTP Content                 Browser Pools     Extract/Project
    Workers                Workers                         |              Workers
       |                      |                 +----------+----------+      |
       |                      |                 |                     |      |
CMS/API/Sitemap         httpx/aiohttp     Browser Discovery   Browser Content Canonical IR
Feed/Archive            Conditional GET        Worker              Worker     Quality Gate
Prefetch Mapping        Raw Snapshot            |                   |         Canonical MD
Dynamic Listing                              Crawl4AI/Playwright Adapter       AI Fit MD
Bounded Deep Crawl                              |                             Search/Vector
       |                              Recipe Compiler + Runtime Contract        |
       |                                        |                              |
       |                                Runtime Outcome                         |
       +----------------------+-----------------+------------------------------+
                                      |
                   Snapshot / Render Artifact / Execution Trace
                                      |
                      Deterministic + Generic Extract Candidates
                                      |
                                 Canonical IR
                                      |
                               Document Version
                                      |
                      Projection / Search / RAG / Export
```

### 3.1 真相边界

```text
PostgreSQL + Object Storage = 业务真相

URL Observation            = URL 被某 Provider 观察到的事实
Coverage Evidence           = Provider 覆盖/穷尽事实
Snapshot                    = HTTP 响应事实
Render Artifact             = Browser 渲染事实
Execution Trace             = 编译计划与逐动作执行事实
Runtime Outcome             = 请求能力这次是否实际生效的事实
Extraction Result           = 某 Extractor/Recipe 的候选事实
Canonical IR                = 正文结构事实
Document Version            = 语义内容版本事实
Quality Result              = 质量判定事实
Drift Observation           = 模板/执行/产出分布变化事实
Cost Event                  = 执行成本事实
Release                     = 影响结果的软件/配置版本事实

Redis Streams               = 运输层
Redis limiter/semaphore     = 短期 Admission 加速层
Crawler cache/frontier      = 执行优化
Browser session             = 易失执行上下文
Crawler batch/stream        = 执行优化，不是业务任务边界
Crawler merged HTML         = 执行产物，不是 Coverage 主键
Canonical/Fit MD/Search/Vector = 可重建投影
```

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST、Command API、Schema 校验；
- **PostgreSQL**：Source/Profile、Run/Task、Coverage、Observation、Artifact、Extraction Recipe/Result、Document/Version、Projection、Quality、Drift、Cost、Release、Audit、Outbox；
- **Redis Streams**：任务运输、consumer group、backpressure；
- **Redis Lua token bucket / semaphore**：跨 Worker Domain Admission；
- **React/Next.js + TypeScript**：Web 管理台；
- **Prometheus + Grafana + OpenTelemetry**：Metric/Log/Trace；
- **S3/MinIO**：Raw HTML、DOM/MHTML、IR、附件、失败诊断、导出；
- **OpenSearch**：全文/BM25；
- **pgvector**：首期向量检索，通过 Adapter 可替换；
- **Docker Compose**：本地开发与小规模验证；
- **Kubernetes**：生产 Worker Pool 横向扩展与 HPA。

### 4.2 抓取与解析

- **httpx/aiohttp**：HTTP、Sitemap、Feed、CMS API、Conditional GET、附件；
- **Crawl4AI**：动态页面、批量/streaming Browser、URL mapping、Virtual Scroll、CSS/XPath/Regex extraction 等，通过 Adapter 使用；
- **Playwright**：复杂但确定性的交互 Recipe；
- **lxml + trafilatura/readability 类算法**：默认通用正文抽取；
- **JSON-LD/OpenGraph/Microdata**：结构化元数据优先；
- **Pruning/BM25 等 Filter**：仅用于 AI Fit/RAG 投影或辅助候选，不直接决定 Canonical 内容；
- **PyMuPDF/pdftotext**：PDF；扫描件才进入 OCR。

### 4.3 Crawler 的边界

```text
平台负责：
Task / Retry / DLQ / Coverage / Fairness / Global Rate Limit /
Budget / Checkpoint / Release / Quality / Audit / Runtime Outcome /
Extraction Recipe Release / Projection Semantics

Crawler 负责：
单次导航 / 页面动作 / DOM / 链接 / 媒体 / 浏览器资源管理 /
局部 batch/stream / 结构化 extractor 执行
```

生产锁定经过 Golden Corpus 验证的 Crawl4AI、Playwright、Chromium 与镜像 digest。升级必须经过 Capability、Compile、Benchmark、Replay、Canary 与 Approval，禁止生产直接追随 `latest`。

---

## 5. 核心领域模型

### 5.1 Source 与 Source Profile

```text
source
- source_id UUID PK
- name
- base_url
- source_type
- timezone
- freshness_class: HOT | WARM | COLD
- status: PROBING | ACTIVE | PAUSED | DEGRADED | DISABLED
- created_at
- updated_at

source_profile
- profile_id UUID PK
- source_id
- version
- allowed_hosts[]
- include_paths[]
- exclude_paths[]
- discovery_policy JSONB
- fetch_policy JSONB
- extraction_policy JSONB
- projection_policy JSONB
- asset_policy JSONB
- incremental_policy JSONB
- quality_policy JSONB
- budget_policy JSONB
- status: DRAFT | TESTED | ACTIVE | RETIRED
- created_at
```

示例：

```yaml
source:
  base_url: https://example.com
  allowed_hosts: [example.com, www.example.com]
  include_paths: [/blog/, /posts/]
  exclude_paths: [/tag/, /author/, /search/, /login/]

discovery:
  providers:
    - {type: cms_api, priority: 10}
    - {type: sitemap, priority: 20}
    - {type: feed, priority: 30}
    - {type: archive, priority: 40}
    - {type: common_crawl, priority: 80, mode: gap_only}
    - {type: prefetch_map, priority: 85, mode: gap_only, max_depth: 2, max_pages: 1000}
    - {type: dynamic_listing, priority: 87, mode: gap_only, recipe: dynamic_blog_list_v5}
    - {type: deep_crawl, priority: 90, mode: gap_only, max_depth: 3, max_pages: 5000}

incremental:
  discover_interval: 2h
  overlap_seconds: 86400
  verify_known_urls: true
  deletion_grace_runs: 3

fetch:
  mode: AUTO
  requests_per_second: 0.5
  max_concurrency: 2
  browser_fallback: true
  agent_fallback: false

extraction:
  recipe: article_template_v3
  generic_fallback: true

projection:
  canonical_markdown: true
  ai_fit_markdown:
    enabled: true
    filter_profile: rag_default_v2

quality:
  min_overall_score: 0.82
  quarantine_on_large_shrink: true
  template_drift_threshold: 0.75

budget:
  max_browser_seconds_per_day: 3600
  max_llm_calls_per_day: 20
```

### 5.2 URL Identity 与 Observation

```text
url_identity
- url_id UUID PK
- source_id
- normalized_url
- normalized_url_hash
- first_seen_at
- last_seen_at
- state: ACTIVE | MISSING | REDIRECTED | DELETED | OUT_OF_SCOPE
- UNIQUE(source_id, normalized_url_hash)

url_observation
- observation_id UUID PK
- url_id
- provider_type
- provider_run_id
- observed_url
- discovered_at
- lastmod nullable
- published_hint nullable
- title_hint nullable
- parent_url nullable
- discovery_round nullable
- evidence JSONB
```

所有 Provider 的输出统一转换为：

```text
DiscoveryItem[]
 -> resolve URL
 -> normalize
 -> scope/SSRF
 -> classify
 -> dedupe
 -> URL Identity / URL Observation
```

### 5.3 Run / Task

```text
run
- run_id
- source_id
- type: PROBE | BACKFILL | INCREMENTAL | VERIFY | REPROCESS
- profile_id
- release_id
- status
- started_at
- finished_at

crawl_task
- task_id
- run_id
- source_id
- url_id nullable
- task_type
- idempotency_key UNIQUE
- priority
- state: READY | LEASED | RUNNING | RETRY | SUCCESS | FAILED | DLQ
- attempt
- max_attempts
- lease_owner
- lease_until
- fencing_token
- not_before
- checkpoint JSONB
- error_code
- error_detail
```

幂等键示例：

```text
BACKFILL_FETCH:{url_id}:{profile_release}
VERIFY:{url_id}:{time_bucket}
DYNAMIC_DISCOVERY:{source_id}:{recipe_release}:{runtime_release}:{time_bucket}
EXTRACT:{artifact_id}:{extractor_release}:{recipe_release}
MARKDOWN:{document_version_id}:{renderer_release}
FIT_MARKDOWN:{document_version_id}:{filter_release}:{config_hash}
INDEX:{document_version_id}:{index_release}
```

### 5.4 Snapshot / Render Artifact / Execution Trace

```text
snapshot
- snapshot_id
- task_id
- url_id
- purpose: CONTENT | DISCOVERY_PREFETCH | VERIFY
- requested_url
- final_url
- status_code
- response_headers JSONB
- etag
- last_modified
- content_type
- body_sha256
- object_key
- fetched_at
- runtime_release_id

render_artifact
- render_id
- task_id
- url_id nullable
- start_url
- final_url
- dom_object_key
- mhtml_object_key nullable
- screenshot_object_key nullable
- network_summary JSONB
- recipe_release_id
- recipe_compiler_version
- runtime_release_id
- runtime_capabilities_hash
- compiled_plan_hash
- browser_process_id nullable
- rendered_at

execution_trace
- trace_id
- task_id
- compiled_plan_hash
- action_index
- action_type
- started_at
- finished_at
- input_summary JSONB
- output_summary JSONB
- error_code nullable
```

Raw HTML/DOM 使用 gzip/zstd 压缩存对象存储，数据库保留 Hash、元数据和对象键。

### 5.5 Runtime Capability 与 Runtime Outcome

Capability 是 Release 级静态事实：

```text
runtime_capability
- runtime_release_id
- crawler_name
- crawler_version
- playwright_version
- chromium_version
- capability_schema_version
- capabilities JSONB
- capabilities_hash
```

Outcome 是 Task/动作级动态事实：

```text
runtime_outcome
- runtime_outcome_id
- task_id
- recipe_release_id
- runtime_release_id
- compiled_plan_hash
- feature
- feature_requested boolean
- feature_engaged boolean
- container_selector nullable
- container_found nullable
- detected_mode nullable
- actions_attempted
- actions_completed
- waits_satisfied
- harvest_rounds
- raw_candidates_by_round JSONB
- normalized_urls_by_round JSONB
- unique_url_union_count
- runtime_merge_item_count nullable
- batch_id nullable
- browser_recycle_reason nullable
- stop_reason
- warnings JSONB
- created_at
```

原则：

```text
Capability: “这个 Runtime 能做 Virtual Scroll / streamed batch / JSON CSS extraction”
Compiled Plan: “这次准备这样做”
Outcome: “这次实际有没有命中容器、进入对应模式并完成动作”
Coverage Evidence: “业务上发现了哪些 URL、为什么认为到达边界”
```

### 5.6 Document / Version / Canonical IR

```text
document
- document_id
- source_id
- canonical_url_id
- first_published_at
- current_version_id
- state

document_version
- version_id
- document_id
- source_artifact_type: SNAPSHOT | RENDER
- source_artifact_id
- extractor_release_id
- extraction_recipe_release_id nullable
- ir_schema_version
- ir_object_key
- semantic_content_hash
- created_at
```

只有 Canonical IR 的语义正文发生实质变化才创建新 Version。广告、导航、追踪参数、模板微调、Filter 参数变化和 Markdown 格式微调不应造成正文版本膨胀。

### 5.7 Deterministic Extraction Recipe

```text
extraction_recipe
- extraction_recipe_id
- source_id
- version
- match_rule JSONB
- mode: GENERIC | JSON_CSS | JSON_XPATH | REGEX | LLM_FALLBACK
- schema_json JSONB
- required_fields[]
- optional_fields[]
- min_selector_hit_rate
- min_field_completeness
- status: DRAFT | TESTED | ACTIVE | RETIRED
- extractor_release_id
- created_at

extraction_result
- extraction_result_id
- artifact_type
- artifact_id
- extraction_recipe_id nullable
- extractor_release_id
- fields JSONB
- selector_hit_rate nullable
- field_completeness
- confidence JSONB
- warnings JSONB
- created_at
```

CSS/XPath/Regex 是确定性候选，不直接等价于 Canonical IR。模板 selector 命中但正文异常时必须 fallback/Quarantine。

### 5.8 Projection

```text
projection
- projection_id
- version_id
- type: CANONICAL_MD | AI_FIT_MD | FULLTEXT | VECTOR_CHUNKS
- projection_release_id
- config_hash
- object_key
- content_hash
- created_at
```

规则：

```text
CANONICAL_MD = 仅从 Canonical IR 稳定渲染
AI_FIT_MD    = 可使用 Pruning/BM25/领域 Filter，可随 Filter Release 重建
FULLTEXT/VECTOR = 派生投影，可重建
```

Query-dependent Fit 结果永远不能参与 `semantic_content_hash`。

### 5.9 Quality / Coverage / Drift / Cost

```text
document_quality
- quality_id
- version_id
- overall_score
- content_length
- text_density
- boilerplate_ratio
- link_density
- metadata_confidence
- structure_score
- code_block_score
- table_structure_score
- duplicate_score
- decision: PASS | QUARANTINE | FAIL
- reasons JSONB

coverage_evidence
- evidence_id
- source_id
- provider_type
- run_id
- cursor_start
- cursor_end
- discovered_count
- unique_article_count
- min_published_at
- max_published_at
- exhausted boolean
- exhaustion_reason
- expected_count nullable
- runtime_outcome_id nullable
- known_gap JSONB
- evidence_object_key nullable

cost_event
- event_id
- task_id
- source_id
- category: HTTP | BROWSER | PROXY | STORAGE | LLM | INDEX
- amount
- unit
- estimated_cost
- recorded_at
```

---

## 6. 新站点接入：Probe -> Review -> Release

### 6.1 自动 Probe

新增 Source 后先进入 `PROBING`：

1. 读取 robots.txt；
2. 探测 `/sitemap.xml`、Sitemap Index、RSS/Atom；
3. 识别 WordPress/Ghost/Medium/Substack 等常见 CMS/API；
4. 发现 Archive、分页、year/month/category index；
5. 抽取 URL/path 样本，生成 include/exclude 候选；
6. 随机选择 5~10 篇文章样本，并覆盖长文、代码、表格等结构；
7. 比较 HTTP Extractor 与 Browser/Recipe 的正文质量；
8. 检测 JSON-LD、作者、发布时间、代码块、图片、表格；
9. 自动生成/建议 JSON CSS/XPath Extraction Recipe 候选，并和 Generic Extractor 做 diff；
10. 估算历史 URL 数与时间分布；
11. 权威发现不足时做受限 Prefetch Mapping；
12. 若栏目依赖 JavaScript，识别 Pagination/Load More/Append Scroll/Virtual Scroll，生成 Dynamic Listing Recipe 候选；
13. Probe 动态能力时同时记录 Runtime Outcome，确认能力真正生效，而不是只看 crawler success；
14. 评估 Canonical Markdown 与 AI Fit Markdown 的差异，Fit 仅做投影；
15. 估算 Browser 比例、Backfill 和日常增量成本；
16. 生成 DRAFT Source Profile、Discovery Recipe 和 Extraction Recipe。

### 6.2 Web 审核

管理员审核：

- Provider 与优先级；
- URL 样本、include/exclude/path 分类；
- HTTP/Browser URL 产出与正文质量对比；
- Dynamic Listing 每轮 URL 增长、Runtime Outcome、编译执行计划和收敛原因；
- Generic vs Deterministic Extraction 字段/正文差异；
- Raw/IR/Canonical Markdown/AI Fit Markdown 预览；
- selector hit rate、field completeness、Quality 分数；
- robots/Scope；
- 预计文章数、已发现数、Known Gap；
- 预计 Backfill 成本和 Browser 占比。

审核通过后发布 Source Profile/Discovery Recipe/Extraction Recipe Release，再进入 Backfill。

---

## 7. 历史全量 URL 发现与 Coverage

### 7.1 Provider 优先级

推荐：

```text
1. CMS / 官方 API
2. Sitemap / Sitemap Index
3. RSS / Atom
4. Archive / Pagination / Year-Month Index
5. Category/Tag Index（补充）
6. URL Seeder / 公开索引
7. Prefetch Mapping
8. Dynamic Listing Discovery
9. bounded Deep Crawl
10. 人工补充 / CSV 导入
```

Browser Provider 默认不压过可用的权威低成本数据源。

### 7.2 Coverage 不是“队列空了”

Backfill 完成必须有可审计证据：

- CMS/Sitemap cursor 已穷尽或明确不可用；
- Archive 到达最老分页/年份边界；
- Provider 集合交叉后新增 URL 收敛；
- Dynamic Listing 达到可解释的 exhaustion boundary，且 Runtime Outcome 证明请求的交互语义真正生效；
- Prefetch/Deep Crawl 达到 max_pages/max_depth/time budget 时形成 Known Gap；
- expected_count 存在时进行数量对照；
- 历史时间分布异常空洞进入补洞队列。

以下都 **不能** 单独作为 Coverage 完成证据：

```text
queue empty
crawler success=true
HTTP 200
DOM 高度不再变化
最终 HTML 体积较大
某个 selector 一次命中
crawler 内部合并/去重后的 item 数
structured extraction 成功返回 JSON
```

### 7.3 URL 规范化

统一：

- scheme/host 小写；
- 默认端口清理；
- fragment 移除；
- tracking 参数移除；
- query 仅保留 Source allowlist；
- trailing slash 由 Profile 决定；
- relative URL resolve；
- Canonical Link 只作为证据，不无条件信任；
- redirect final URL 重新过 Scope/SSRF；
- 保留原始 observed URL 与 normalized URL。

### 7.4 Prefetch Mapping

```text
Phase A: Map
seed URLs
 -> cheap HTML + links
 -> normalize/scope/filter
 -> URL Observation + parent edge
 -> page/path classifier
 -> 估算站点空间

Phase B: Process
article candidates
 -> 正式 HTTP/Browser Fetch
 -> Snapshot/Render
 -> Extract/IR/Quality
```

必须设置 `max_depth / max_pages / time_budget`，并对日历、搜索、tag、query 无限空间设置 guard。

---

## 8. Dynamic Listing Discovery

JavaScript 栏目、Infinite Scroll、Load More、Virtual Scroll 是独立 Discovery Provider，不是正文 Browser 中的临时脚本。

### 8.1 语义 Recipe IR

```yaml
name: dynamic_blog_list_v5
kind: dynamic_listing
start_urls:
  - https://example.com/blog

ready:
  type: CSS
  selector: ".information-flow-list"

harvest:
  item_selector: ".information-flow-list article"
  link_selector: "a[href]"
  extract:
    url: "a::attr(href)"
    title: "a::text"
    published_hint: "time::attr(datetime)"

interaction:
  mode: APPEND_SCROLL
  action: SCROLL_PAGE
  expected_runtime_mode: APPEND
  wait_after_action:
    type: ITEM_GROWTH
    selector: ".information-flow-list article"
    timeout_ms: 5000

convergence:
  no_growth_rounds: 3
  max_rounds: 40
  max_runtime_seconds: 120
  max_items: 5000
```

支持：

```text
STATIC
PAGINATION
CLICK_MORE
APPEND_SCROLL
VIRTUAL_SCROLL
```

Recipe 只描述业务语义，不直接出现 crawler 私有参数。

### 8.2 Runtime Capability Contract

能力示例：

```json
{
  "supports_js_before_wait": true,
  "supports_js_only_session": true,
  "supports_scan_full_page": true,
  "supports_virtual_scroll": true,
  "supports_streamed_batch": true,
  "supports_json_css_extraction": true,
  "js_execution_order": [
    "navigate",
    "js_before_wait",
    "wait_for",
    "delay",
    "js_after_wait",
    "capture"
  ]
}
```

### 8.3 Recipe Compiler

当前 Crawl4AI 0.9.x Adapter 的典型语义映射：

```text
TRIGGER -> WAIT -> HARVEST
 -> js_code_before_wait + wait_for + capture

CLICK_MORE 多轮交互
 -> session_id + js_only=True + 多次受控 action

APPEND_SCROLL
 -> scan_full_page 或逐轮 scroll + harvest

VIRTUAL_SCROLL
 -> VirtualScrollConfig 或逐轮 harvest + 业务 URL union

PAGINATION
 -> next href 作为新 URL 重新进入 discovery pipeline
```

禁止把旧版本 `js_code/wait_for` 参数文本原样搬到新 Runtime；编译目标是保持语义，而非参数名。

### 8.4 Runtime 升级兼容门禁

```text
Build Runtime Capability
 -> Compile ALL ACTIVE Recipes
 -> Static Compatibility Check
 -> Golden Corpus Replay
 -> Compare Coverage / Quality / Cost / Outcome
 -> Stream cancel / Browser recycle regression
 -> Canary Sources
 -> Approval
 -> Release
```

不兼容时：

```text
RECIPE_RUNTIME_INCOMPATIBLE
UNSUPPORTED_INTERACTION_MODE
WAIT_SEMANTICS_CHANGED
VIRTUAL_SCROLL_CAPABILITY_MISSING
STREAM_CLEANUP_REGRESSION
EXTRACTION_SCHEMA_INCOMPATIBLE
```

### 8.5 文章身份提取

Discovery 主身份必须来自真实链接：

```json
{
  "observed_url": "https://example.com/post/123",
  "title_hint": "文章标题",
  "published_hint": "2026-08-01T10:00:00Z",
  "parent_url": "https://example.com/blog",
  "round": 7,
  "provider": "DYNAMIC_LISTING"
}
```

禁止用图片 alt、图片 URL、卡片文本或 runtime 文本 fingerprint 作为文章唯一身份。

### 8.6 收敛算法

```python
seen = set()
no_growth = 0

for round_no in range(max_rounds):
    candidates = harvest_current_state()
    urls = normalize_scope_filter_classify(candidates)
    new_urls = urls - seen
    seen |= new_urls
    persist_observations(new_urls, round_no)

    if incremental_overlap_reached(new_urls):
        stop("OVERLAP_BOUNDARY")
        break

    no_growth = no_growth + 1 if not new_urls else 0
    if no_growth >= no_growth_rounds:
        stop("NO_GROWTH")
        break

    outcome = perform_next_semantic_action()
    verify_runtime_outcome(outcome)
    wait_for_item_growth_or_timeout()
else:
    stop("MAX_ROUNDS")
```

收敛核心是“规范化且通过 Scope/文章分类后的唯一文章 URL 是否继续增长”。DOM 高度、文本内容和 runtime 合并 item 数只作辅助证据。

### 8.7 Virtual Scroll 的业务正确性

虚拟列表会回收旧 DOM，必须：

- 每轮滚动立即 harvest URL 并持久化；或
- 使用 Runtime 的 virtual-scroll chunk 合并能力，但把合并结果重新进入 URL pipeline。

Crawler 的 Virtual Scroll 合并属于执行优化，不是业务 Coverage 真相。

```text
runtime text/html dedup
 != URL Identity dedup
 != Coverage proof
```

### 8.8 Runtime Outcome Gate

请求动态能力时必须验证“实际执行结果”：

```text
feature_requested
feature_engaged
container_found
detected_mode
actions_attempted
actions_completed
waits_satisfied
harvest_rounds
unique_url_union_count
stop_reason
warnings
```

Gate：

```text
feature_requested && !feature_engaged
 -> Provider DEGRADED
 -> exhausted=false
 -> Known Gap

VIRTUAL_SCROLL && !container_found
 -> VIRTUAL_SCROLL_CONTAINER_NOT_FOUND
 -> exhausted=false

expected_runtime_mode != detected_mode
 -> INTERACTION_MODE_MISMATCH
 -> 保存 Artifact/Trace
 -> Recipe Review

crawler success=true 但 URL 无增长
 -> 进入 convergence 判断
 -> 不可直接判定 Provider exhausted
```

### 8.9 Dynamic Coverage Evidence

至少保存：

```text
recipe_release_id
recipe_compiler_version
runtime_release_id
runtime_capabilities_hash
compiled_plan_hash
interaction_mode
runtime_feature_engaged
container_found
detected_mode
rounds
actions_attempted
actions_completed
observed_items
unique_article_urls
new_urls_by_round[]
duplicate_ratio
selector_hit_rate
first_published_hint
last_published_hint
exhausted
exhaustion_reason
runtime_ms
render_artifact_id
runtime_outcome_id
```

正常边界：

```text
CURSOR_EXHAUSTED
NO_GROWTH（动作 Outcome 正常）
OVERLAP_BOUNDARY（增量）
```

以下只能形成 Known Gap：

```text
MAX_ROUNDS
TIME_BUDGET
MAX_ITEMS
CONTAINER_NOT_FOUND
INTERACTION_MODE_MISMATCH
ACTION_FAILED
RUNTIME_FEATURE_NOT_ENGAGED
```

### 8.10 动作安全白名单

Web/API 只允许：

```text
WAIT_SELECTOR
CLICK_SELECTOR
SCROLL_PAGE
SCROLL_CONTAINER
WAIT_ITEM_GROWTH
EXTRACT_LINKS
```

Adapter 生成受控脚本。生产禁止任意 JavaScript 透传。

---

## 9. 正文抓取与最小成本路由

### 9.1 Acquisition Planner

```text
L0 CMS/API payload
L1 HTTP
L2 Crawl4AI deterministic Browser
L3 Playwright deterministic Recipe
L4 LLM-assisted extraction / Controlled Agent Browser
```

优化目标：

```text
minimize expected_cost / expected_accepted_document_version
subject to Quality SLO and Freshness SLO
```

策略建议与生产 Release 分离，变化必须可审核、可回滚。

### 9.2 HTTP Worker

支持：

- `If-None-Match`；
- `If-Modified-Since`；
- gzip/br；
- connect/read/write 分阶段 timeout；
- redirect 上限；
- per-domain connection pool；
- `Retry-After`；
- 429/503 指数退避 + jitter；
- body size 上限；
- MIME allowlist；
- 原始 header + body hash 保存。

### 9.3 Browser Worker

Browser 进程复用，Context 按安全域隔离，Page 按 Task 短生命周期：

- 默认拦截广告、追踪、字体、视频等非必要资源；
- 默认不截图、不生成 PDF；
- 仅诊断/Golden Corpus/审核时截图；
- wait/scroll/click 来自版本化 Recipe；
- 单 Worker crawler 限流只作第二层保护，全局公平与限流由平台控制；
- Page 完成后强制关闭；
- Context 仅在同一安全域的短 session 内复用；
- Browser 按 RSS/年龄/pages-since-recycle/error-rate 阈值安全回收。

资源池：

```text
Browser Discovery Worker
- 长页面、多轮交互、低并发、强预算

Browser Content Worker
- 单文章、短生命周期、正文完整度优先
```

#### 9.3.1 受控 micro-batching

Browser Content Worker 可在 Adapter 内使用 Crawl4AI `arun_many(..., stream=True)` 或等价能力提升吞吐，但必须保持业务 Task 独立：

```text
Scheduler
 -> claim 8~32 个已通过 Admission 的 Task
 -> 按 runtime/browser_config/resource_class 分组
 -> Adapter streamed micro-batch
 -> 每返回一个 result 就按 task_id 独立落库
```

约束：

- batch_id 仅用于执行追踪，不是幂等键；
- 单 URL 失败不回滚整个 batch；
- 每个 URL 独立 lease/fencing/retry；
- batch 不能绕过 per-domain token bucket；
- 跨安全域不共享持久 Cookie/Profile；
- stream cancel/SIGTERM 必须清理未完成 page/context；
- runtime 升级必须回归 `stream cancel -> active pages 回到基线`。

#### 9.3.2 Browser 生命周期回收

```text
Page          -> 每 Task 关闭
Context       -> session/安全域结束关闭
Browser       -> max pages / RSS / age / crash rate 触发 recycle
Worker Pod    -> 达到长期内存或异常阈值滚动重启
```

业务 checkpoint 永远在 PostgreSQL，不依赖 Browser 进程活着。

### 9.4 Browser Fallback 条件

HTTP 成功但以下条件不满足时才升级 Browser：

- 正文过短；
- text density 低；
- JSON-LD/正文 DOM 明显缺失；
- 仅 hydration shell；
- Challenge/login/cookie 模板命中；
- Source Profile 明确要求 Browser。

Browser 仍失败则 Quarantine，不无限重试。

---

## 10. 正文抽取、Canonical IR 与 Markdown

### 10.1 Extractor Pipeline

```text
Snapshot / Render Artifact
 -> MIME / encoding normalize
 -> metadata candidates(JSON-LD/OpenGraph/Microdata)
 -> DOM sanitize
 -> template route
 -> Deterministic Extraction Recipe candidate
 -> Generic main-content extractor candidate
 -> candidate reconciliation
 -> block reconstruction
 -> Canonical IR
 -> Quality Gate
 -> Document Version
 -> Projection Renderer
```

### 10.2 Deterministic Extraction Recipe

结构稳定的博客模板优先使用低成本、确定性的 CSS/XPath Schema；Regex 仅用于局部字段/校验。

示例语义：

```yaml
name: article_template_v3
match:
  path_regex: "^/blog/.+"
mode: JSON_CSS
base_selector: "article"
fields:
  - {name: title, selector: "h1", type: text, required: true}
  - {name: author, selector: ".author", type: text}
  - {name: published_at, selector: "time", type: attribute, attribute: datetime}
  - {name: body, selector: ".post-content", type: html, required: true}
quality:
  min_field_completeness: 0.8
  min_body_chars: 500
```

候选融合规则示例：

```text
published_at:
JSON-LD(valid) > time[datetime] recipe > OpenGraph > text heuristic

body:
explicit content selector candidate 与 generic extractor 比较
 -> 结构/长度/boilerplate 合格则接受
 -> 异常则 fallback generic + Quarantine/Drift signal
```

不能因为 selector 命中或 JSON 可解析就直接判定正文成功。

### 10.3 Canonical IR

```json
{
  "schema_version": 3,
  "title": "...",
  "canonical_url": "...",
  "author": ["..."],
  "published_at": "...",
  "updated_at": "...",
  "language": "zh-CN",
  "blocks": [
    {"type": "heading", "level": 2, "text": "..."},
    {"type": "paragraph", "text": "..."},
    {"type": "code", "language": "python", "text": "..."},
    {"type": "image", "src": "...", "alt": "..."},
    {"type": "table", "rows": []}
  ],
  "metadata": {}
}
```

IR 的作用：

- Markdown Renderer 可升级后重建；
- 向量切块按 heading/block 结构切分；
- 代码块、表格、图片语义不丢；
- semantic hash 不受 Markdown 格式微调影响；
- 不受 BM25 query 或 Filter 参数变化影响。

### 10.4 Canonical Markdown

统一 Front Matter：

```yaml
---
document_id: ...
source_id: ...
url: ...
canonical_url: ...
title: ...
author: [...]
published_at: ...
updated_at: ...
retrieved_at: ...
version_id: ...
content_hash: ...
---
```

正文规范：

- 保留 heading 层级；
- fenced code block + language；
- 简单表格还原为 GFM；
- 复杂 rowspan/colspan 无法无损转换时保留 HTML block 或 IR 引用；
- 图片使用规范化 URL 或对象存储代理路径；
- 去除导航、相关推荐、Cookie、页脚、分享栏；
- AI 摘要不混入 canonical body。

### 10.5 AI Fit Markdown

Crawl4AI Pruning/BM25 或其他 Filter 可生成更适合 RAG 的投影：

```text
Canonical IR
 -> Filter Release + Filter Profile
 -> AI_FIT_MD Projection
```

必须遵守：

- Fit Markdown 不是 Document Version 的内容事实；
- query-dependent BM25 不能参与 semantic hash；
- Filter 升级只 rebuild Projection；
- 导出默认使用 Canonical Markdown；
- RAG 可按场景选择 Canonical 或 Fit；
- Web 支持 Canonical vs Fit diff。

### 10.6 Asset

```text
REFERENCE_ONLY
DOWNLOAD_REFERENCED
DOWNLOAD_ALL_CONTENT_ASSETS
```

下载资产必须做 Scope/SSRF、MIME、size limit、hash 去重和生命周期管理。

---

## 11. Quality Gate、Golden Corpus 与 Drift

### 11.1 Quality Gate

至少检查：

- 标题存在；
- 正文长度；
- text density；
- boilerplate/link density；
- heading/code/table/list 结构；
- metadata confidence；
- deterministic recipe selector hit rate / field completeness；
- 与历史版本的大幅缩水；
- 与其他页面的高度重复；
- Challenge/login/cookie/error-page 特征；
- Source 特定规则。

结构级指标建议：

```text
table_count_source
table_count_ir
table_cell_recall
gfm_table_valid_rate
code_block_count_delta
heading_path_similarity
list_item_count_delta
```

```text
PASS       -> 可成为 current version
QUARANTINE -> 保存，等待人工/备用 Extractor
FAIL       -> 不进入默认知识库
```

### 11.2 Golden Corpus

每个 Source 保存 3~10 个代表样本：

- 普通文章；
- 长文；
- 代码密集；
- 复杂表格；
- 图片密集；
- 旧模板；
- 新模板；
- 关键动态列表页。

Runtime/Extractor/Recipe/Renderer/Filter 升级自动比较：

```text
URL yield
Runtime Outcome
selector hit rate
field completeness
正文长度
结构 block 数
table/code structure delta
semantic hash diff
canonical markdown diff
fit projection diff
quality score
browser seconds
network requests
active page cleanup
```

Runtime Golden Corpus 额外覆盖：

```text
STREAM_CANCEL_25_PERCENT
STREAM_CANCEL_50_PERCENT
WORKER_SIGTERM_DURING_BATCH
BROWSER_RECYCLE_DURING_LOAD
```

### 11.3 在线 Drift Detector

典型信号：

- selector hit rate 下降；
- required field completeness 下降；
- deterministic vs generic body 差异暴涨；
- container_found rate 下降；
- runtime feature engaged rate 下降；
- detected_mode 分布改变；
- action completion rate 下降；
- Dynamic Listing 每轮唯一 URL yield 骤降；
- HTTP Quality 降低同时 Browser fallback 暴涨；
- 大量页面变成相同 Cookie/Login/Challenge；
- Sitemap/Archive 路径模式变化；
- title/author/time 元数据缺失率上升；
- Source 平均正文长度突然缩水；
- table/code 结构完整率下降。

处置：

```text
Drift Alert
 -> Source DEGRADED / 保护性降速
 -> 保存失败 Artifact + Runtime Outcome + Extraction Result
 -> Golden Corpus + 最近样本回放
 -> 生成 Profile/Recipe 候选
 -> Compare Coverage / Quality / Cost / Outcome
 -> Approval
 -> New Release
```

如果同一 Runtime Release 后多个不同 Source 同时出现 feature 未生效、stream cleanup 异常或 mode 突变，优先判定平台回归。

---

## 12. 增量同步与版本管理

### 12.1 增量发现

按 Source 能力组合：

```text
RSS/Atom
CMS cursor
Sitemap lastmod
Archive 最近分页
Dynamic Listing 顶部窗口
低频 Prefetch/Deep Crawl 补漏
```

使用 overlap window，例如从上次 watermark 往前回退 24 小时，通过 URL 去重吸收重复。

Freshness Class 示例：

```text
HOT  -> 5~30 min
WARM -> 1~6 h
COLD -> 1~7 d
```

最终频率由历史更新率、Provider 能力、429/503、预算、新 URL yield 与业务优先级共同决定。

### 12.2 Dynamic Listing 增量

```text
打开列表顶部
 -> harvest
 -> normalize/dedupe
 -> 继续动作并验证 Runtime Outcome
 -> 遇到 overlap 时间边界
    或连续 K 个已知 URL + N 轮无 unseen URL
 -> 停止
```

Browser session 不作为 checkpoint。PostgreSQL 保存 watermark、Observation、Provider cursor、Recipe/Runtime Release、Runtime Outcome 与 Coverage Evidence。

### 12.3 已知 URL 复检

```text
Conditional GET
 -> 304: 更新时间戳，不创建 Version
 -> 200 + body hash 相同: 不 Extract
 -> body hash 变化: Extract -> IR
 -> semantic hash 相同: 不创建 Version
 -> semantic hash 变化: 新 Document Version -> Quality -> Projection rebuild
```

Extraction Recipe/Filter/Renderer 变化但 Artifact 未变时，可离线 Reprocess/Reproject，不必重新联网。

### 12.4 删除与 URL 迁移

不能把一次 404 当永久删除：

- 404/410 连续 N 次；
- 多个增量 run 不再观察到；
- Sitemap/CMS 明确消失；
- redirect 指向新 canonical；
- grace period 后再标记 DELETED/MOVED。

保留旧 Version 和历史 URL Identity。

### 12.5 周期性深度校验

- 周/月级 Provider cross-check；
- 随机历史 URL Verify；
- Sitemap 全量 diff；
- 低频 Archive/Prefetch gap scan；
- Coverage 时间分布空洞检测；
- Dynamic Runtime Outcome 异常样本复跑；
- Deterministic Extraction Recipe 的历史 Snapshot 抽样回放。

---

## 13. 调度、可靠性与公平性

### 13.1 Transactional Outbox

```text
DB transaction:
  update business state
  insert outbox event
commit

Outbox Relay -> Redis Streams
```

Worker 完成后先写 PostgreSQL 再 ACK，Redis 丢失可从事实重建运输层。

### 13.2 Lease + Fencing Token

Worker claim task：

```text
lease_owner
lease_until
fencing_token++
```

所有最终写检查 fencing token，旧 Worker 恢复后不能覆盖新结果。

### 13.3 Retry 分类

```text
RETRYABLE:
timeout / connection reset / 429 / 503 / browser crash

RECIPE_OR_SOURCE:
selector timeout / required field missing / container not found /
interaction mode mismatch / feature not engaged / template drift /
no article links

RUNTIME:
stream cleanup regression / browser recycle failure / page leak

PERMANENT:
out-of-scope / unsupported MIME / explicit denied
```

指数退避 + jitter，超过 max_attempts 进入 DLQ。

### 13.4 Weighted Fair Scheduler

- Incremental > Verify > Backfill；
- Source 轮转；
- 大站分片；
- per-domain concurrency；
- Browser/HTTP 独立资源池；
- aging 防永久饥饿；
- micro-batch 只能从已被 Scheduler/Admission 接受的 Task 中组装。

### 13.5 Global Domain Admission

```text
Redis token bucket  -> requests/sec
Redis semaphore     -> concurrent requests
DB policy           -> Source truth
```

429/503/Retry-After 自动降速并逐步恢复。Crawler 内部限速只作第二层保护。

### 13.6 Backpressure

监控：

- stream lag；
- oldest task age；
- Browser pool saturation；
- Browser active pages/contexts；
- object storage upload latency；
- Extract/index throughput。

压力高时优先保证 Incremental Freshness，暂停低优先级 Deep Crawl/Backfill；不得靠无限扩大 `arun_many` batch 掩盖背压。

---

## 14. Web 管理功能

### 14.1 Source 管理

- 新增/编辑/暂停/禁用 Source；
- 批量 CSV 导入；
- Probe；
- Profile/Discovery Recipe/Extraction Recipe 版本；
- Freshness Class；
- robots/Scope/预算；
- Release/Canary/回滚。

### 14.2 Coverage 页面

展示：

- Provider 状态；
- Provider 发现数与交集；
- 历史发布时间分布；
- expected vs discovered；
- Dynamic Listing 每轮新增 URL 曲线；
- Runtime Outcome 概览；
- exhaustion reason；
- Known Gap；
- Backfill 完成度；
- Provider 最近增量延迟。

### 14.3 Dynamic Recipe 页面

- 语义 Recipe 编辑器；
- 动作白名单；
- Runtime Capability；
- 编译后的 Execution Plan；
- `recipe_release -> runtime_release` 兼容检查；
- Runtime Outcome：feature requested/engaged、container found、detected mode、动作完成率、逐轮 URL union；
- Probe/Golden Corpus 回放；
- 每轮 item/URL yield；
- Render Artifact/Execution Trace；
- Release diff、Canary、回滚。

管理员应能直接回答：

```text
为什么认为这个 Provider 已到历史边界？
这次请求的 Browser/Virtual Scroll 语义是否真的执行？
哪些 URL 是逐轮实际观察到的？
```

### 14.4 Extraction Recipe 与质量页面

- CSS/XPath/Regex Schema 编辑与校验；
- 页面 DOM 样本与 selector 高亮；
- required/optional 字段预览；
- selector hit rate / field completeness；
- Generic vs Deterministic Extractor diff；
- Raw Response/DOM；
- Extraction Result；
- IR/Canonical Markdown/AI Fit Markdown 四层对比；
- table/code/heading 结构指标；
- Quality 分项；
- 当前/历史 Version diff；
- Quarantine 审核；
- 重新 Extract/Render/Reproject/Index；
- Golden Corpus replay、Canary、回滚。

### 14.5 任务与运维页面

- Run/Task；
- Retry/DLQ；
- lease owner；
- queue age；
- batch_id 与 batch 内 Task 状态；
- per-source/domain 并发；
- Browser pool 使用率；
- active browser/context/page；
- browser recycle reason；
- stream cancel cleanup；
- 手工 retry/cancel/reprocess；
- Audit Log。

### 14.6 成本页面

- Source 日/周/月成本；
- Browser 秒；
- 网络流量；
- Snapshot/Asset 存储；
- LLM 调用；
- 每 1000 个 PASS 文档成本；
- Backfill 剩余成本预测；
- Deterministic/Generic/LLM extractor 成本占比。

---

## 15. 安全与合规

### 15.1 SSRF / Scope

每次外部跳转校验：

- 仅 `http/https`；
- DNS 解析后阻断 loopback/private/link-local/metadata IP；
- redirect 每跳重新解析验证；
- allowed_hosts；
- URL 长度/redirect 次数/body size 上限；
- Asset、iframe 同样处理；
- proxy destination 同样做全球可路由性/allowlist 校验。

### 15.2 Browser 沙箱

- 独立 Pod/容器；
- non-root；
- 只读根文件系统；
- CPU/内存/PID 限制；
- NetworkPolicy；
- Context 隔离；
- Cookie/凭据默认不持久化；
- 禁止任意 JS/代理配置/Chromium extra_args 透传；
- 自托管 Crawler API 仅内网访问并启用认证；
- 生产镜像使用固定 digest。

### 15.3 Recipe 安全

- selector/动作白名单；
- 最大动作数、运行时、滚动轮数；
- 禁止跨 Scope 导航；
- Extraction Schema 只允许声明式 selector/field 类型；
- Recipe 变更版本化、审计、Release；
- Compiler 不拼接用户原始脚本。

### 15.4 RBAC

```text
VIEWER
OPERATOR
EDITOR
RELEASE_MANAGER
ADMIN
```

发布/回滚 Runtime/Discovery Recipe/Extraction Recipe 属于高权限操作。

### 15.5 数据与访问策略

每个 Source 保存：

- robots 决策；
- 抓取频率；
- User-Agent；
- 许可/访问备注；
- disabled reason；
- 审计记录。

---

## 16. 可观测性与成本治理

### 16.1 指标

系统级：

```text
queue_depth
oldest_task_age
worker_busy_ratio
http_latency
browser_seconds
browser_crash_rate
browser_process_rss_bytes
browser_process_age_seconds
active_contexts
active_pages
pages_since_recycle
stream_cancel_cleanup_ms
retry_rate
dlq_count
outbox_lag
object_store_latency
```

Source/Provider/Extractor 级：

```text
new_urls
unique_urls
provider_yield
coverage_gap
freshness_delay
selector_hit_rate
field_completeness
extractor_fallback_rate
container_found_rate
runtime_feature_engaged_rate
runtime_detected_mode_distribution
action_completion_rate
dynamic_new_urls_by_round
browser_fallback_rate
quality_pass_rate
quarantine_rate
content_length_distribution
table_structure_score
code_structure_score
canonical_fit_size_ratio
cost_per_accepted_version
```

### 16.2 Trace

```text
Run
 -> Discovery Observation
 -> Task
 -> Admission
 -> optional Micro-batch
 -> HTTP/Browser
 -> Snapshot/Render
 -> Execution Trace
 -> Runtime Outcome
 -> Extraction Candidates
 -> IR
 -> Quality
 -> Version
 -> Canonical/Fit Projection
 -> Index
```

### 16.3 告警

- HOT Source Freshness 超 SLO；
- Quality PASS 比例突降；
- Extraction required field completeness 突降；
- Dynamic selector/container hit 突降；
- Runtime feature engaged rate 突降；
- detected mode 群体变化；
- Dynamic URL yield 突降；
- 同 Runtime Release 多 Source 同时退化；
- Browser fallback 暴涨；
- active pages/context 持续增长；
- stream cancel 后资源未回基线；
- Browser RSS 长时间超阈值；
- 429/503 增长；
- queue age 持续增长；
- DLQ 堆积；
- Browser/LLM 预算接近上限；
- Outbox lag 异常。

### 16.4 预算控制

```text
per-source daily HTTP request budget
per-source daily browser seconds
per-source proxy budget
per-source LLM budget
global browser concurrency
global monthly cost ceiling
```

超预算：优先保 Freshness，暂停 gap-only Provider，降低历史 Verify，并告警而不是无界继续。

---

## 17. 搜索、RAG 与导出

### 17.1 全文搜索

OpenSearch 索引：

- title；
- body；
- code；
- source；
- author；
- published_at；
- tags；
- canonical_url；
- version_id。

索引是投影，可从 Canonical IR 重建。

### 17.2 AI Fit 与向量切块

RAG 可选择：

```text
Canonical IR -> canonical text -> chunk
Canonical IR -> AI Fit filter -> fit text -> chunk
```

切块：

```text
heading boundary
 -> paragraph/code/table block
 -> token budget
 -> small overlap
```

Chunk metadata：

```text
document_id
version_id
source_id
heading_path
block_range
canonical_url
published_at
projection_release_id
```

### 17.3 导出

- 每 Source Canonical Markdown 目录；
- 可选 AI Fit Markdown 导出；
- 全库 tar/zip；
- JSONL/IR；
- changed-since 增量导出；
- 仅 PASS current version；
- 指定历史 Version 回放。

建议布局：

```text
knowledge-base/
  source-slug/
    2026/
      08/
        stable-document-id.md
```

文件名不依赖标题，避免重命名造成身份变化。

---

## 18. 存储、生命周期与容量

### 18.1 分层保存

```text
Hot metadata: PostgreSQL
Raw/Render/IR/Asset: S3/MinIO
Canonical/Fit Markdown: Object Store / Export
Fulltext: OpenSearch
Vector: pgvector
```

### 18.2 对象键

```text
raw/{sha256}.html.zst
render/{sha256}.mhtml.zst
ir/{sha256}.json.zst
projection/{type}/{sha256}.zst
asset/{sha256}.{ext}
```

数据库记录业务关系，避免同内容重复存储。

### 18.3 生命周期

- 近期 Raw/Render 热存；
- 老 Artifact 转低成本对象存储；
- Canonical IR/Version metadata 长期保留；
- Canonical/Fit Markdown、Index、Vector 可重建；
- screenshot 默认不保存，仅用于失败诊断、Golden Corpus 和人工审核。

### 18.4 容量估算

Backfill 前由 Probe 估算：

```text
estimated_urls
avg_raw_bytes
avg_ir_bytes
avg_projection_bytes
asset_download_ratio
browser_ratio
avg_browser_seconds
```

按 P50/P95 给出网络、存储与 Browser 成本，避免 1000 站点无预算直接开跑。

---

## 19. 部署与扩展

### 19.1 Worker Pool

```text
Discovery Lightweight
HTTP Content
Browser Discovery
Browser Content
Extract/Quality
Projection
Asset
Index/Embedding
Maintenance/Verify
```

各池独立 Deployment/HPA，避免 Browser 内存峰值拖垮 HTTP/Extract Worker。

### 19.2 分片与横向扩展

- Task 以 Source/Domain 做 fairness，不要求固定 Worker 粘性；
- Browser session 只在同一 Task 短期多步操作中使用；
- micro-batch 只在 Worker 内临时组装；
- 业务 checkpoint 在 DB，Pod 可任意重启；
- 大 Source Backfill 按 URL batch 分片；
- PostgreSQL 大表按时间/Source 评估分区；
- Observation/Task/Cost/Projection 表建立 `(source_id, created_at)`、state/lease 等索引。

### 19.3 灾难恢复

- PostgreSQL PITR；
- Object Storage versioning/replication；
- Source Profile/Recipe/Release 备份；
- OpenSearch/pgvector/Markdown Projection 可从事实层重建；
- Redis 丢失时从 PostgreSQL Run/Task/Outbox 重建运输层；
- Browser/stream 中断时依赖 lease/fencing 恢复，不依赖 Runtime cache。

---

## 20. 推荐代码模块

```text
app/
  api/
  domain/
    source/
    discovery/
    coverage/
    document/
    projection/
    quality/
    release/
    runtime_outcome/
  orchestration/
    scheduler/
    admission/
    batching/
    outbox/
    retry/
  adapters/
    http/
    crawl4ai/
    playwright/
    object_store/
    opensearch/
    vector/
  recipes/
    discovery/
    extraction/
    schema/
    compiler/
    runtime_capabilities/
    outcome_contract/
  workers/
    discovery/
    http_content/
    browser_discovery/
    browser_content/
    extract/
    projection/
    index/
  extraction/
    metadata/
    deterministic/
    generic/
    reconcile/
    ir/
  projection/
    canonical_markdown/
    fit_markdown/
    chunking/
  quality/
    gates/
    structure/
    drift/
    golden_corpus/
  web/
```

依赖方向：

```text
Domain <- Orchestration <- Adapters
Domain 不 import Crawl4AI/Playwright
Recipe IR 不 import crawler 参数类型
Coverage 不依赖 crawler 内部文本/DOM 去重主键
Projection 不反向修改 Canonical IR/Version
```

---

## 21. 关键工作流

### 21.1 新 Source Backfill

```text
Create Source
 -> Probe
 -> Draft Profile/Discovery Recipe/Extraction Recipe
 -> Web Review
 -> Release
 -> Discovery Providers
 -> Normalize/Scope/Dedupe
 -> Runtime Outcome（动态 Provider）
 -> Coverage Evidence
 -> Fetch Tasks
 -> HTTP first
 -> Browser fallback if needed
 -> Snapshot/Render
 -> Deterministic + Generic Extract Candidates
 -> Reconcile -> IR
 -> Quality
 -> Version
 -> Canonical Markdown
 -> optional AI Fit / Index
 -> Coverage Review
 -> ACTIVE Incremental
```

### 21.2 Runtime 升级

```text
Build new Runtime image
 -> Capability Contract
 -> Compile all ACTIVE Recipes
 -> Static Compatibility
 -> Golden Corpus
 -> Outcome comparison
 -> streamed batch cancel/recycle regression
 -> Benchmark
 -> Canary Sources
 -> Compare Coverage/Quality/Cost
 -> Approve Release
 -> Gradual rollout
 -> Auto rollback on regression
```

### 21.3 Source 改版

```text
Drift Alert
 -> Source DEGRADED
 -> preserve Artifact/Outcome/Extraction evidence
 -> Probe latest pages
 -> create Discovery/Extraction Recipe candidate
 -> replay Golden Corpus
 -> compare
 -> approve new Release
 -> reprocess affected window
```

### 21.4 Worker/Redis/Browser 故障

```text
Worker crash
 -> lease expires
 -> another Worker claims with new fencing token
 -> resume from DB checkpoint

Stream cancel / Browser crash
 -> cleanup page/context
 -> unfinished Task RETRY
 -> stale Worker cannot write due fencing token

Redis loss
 -> rebuild streams from DB/outbox/incomplete tasks
 -> continue
```

---

## 22. 实施阶段

### Phase 1：可用 MVP

范围：

- Source/Profile；
- Sitemap/RSS/Archive Discovery；
- URL normalize/dedupe；
- HTTP Fetch/Snapshot；
- Generic Extract -> IR -> Canonical Markdown；
- PostgreSQL + S3；
- 基础 FastAPI/Web；
- Run/Task/Retry；
- 手工 Backfill/Incremental。

验收：50 个不同技术博客能全量回填并生成可追溯 Canonical Markdown。

### Phase 2：1000 站点生产化

范围：

- CMS Provider；
- Prefetch Mapping；
- Dynamic Listing；
- Browser Discovery/Content 分池；
- Browser streamed micro-batching；
- Browser lifecycle recycle；
- Deterministic Extraction Recipe（CSS/XPath/Regex）；
- Generic/Deterministic candidate reconciliation；
- Canonical/Fit Projection 分层；
- Runtime Capability Contract；
- Recipe Compiler；
- Runtime Outcome Contract；
- Global Domain Admission；
- Weighted Fair Scheduler；
- Budget/Cost；
- Quality Gate + table/code 结构指标；
- Drift Detector；
- Golden Corpus；
- OpenSearch/pgvector；
- Kubernetes HPA。

验收：1000 个 Source Backfill 与 Incremental 并行运行，Incremental Freshness 不因大站历史回填阻塞；动态 Provider 的 Coverage 必须同时具备 URL 增长证据与 Runtime Outcome；stream cancel/Browser recycle 不产生 page leak 或重复业务写入。

### Phase 3：规模与自治优化

范围：

- Provider 自动选择和成本反馈；
- Coverage gap 自动诊断；
- Profile/Discovery/Extraction Recipe 候选自动生成；
- Runtime Outcome 异常自动聚类；
- Extraction drift 自动聚类；
- Filter/Projection 自动评测；
- Release Canary/自动回滚；
- 更细容量预测和成本优化；
- 多租户/RBAC 增强；
- 对象存储冷热分层。

自动化只生成 Candidate，不直接改变生产抓取语义。

---

## 23. 最终验收标准

### 23.1 Coverage

随机抽取代表 Source，要求：

- Sitemap/CMS/Archive Provider 有 exhaustion evidence；
- 动态列表有逐轮 URL 增长曲线与 exhaustion reason；
- 动态列表请求的交互能力有 `feature_engaged/container_found/detected_mode/actions_completed` 等 Outcome 证据；
- crawler success 但 feature 未生效时不得被判定为 exhausted；
- bounded Provider 达预算时明确 Known Gap；
- Provider 交叉后历史时间分布无无法解释的大空洞；
- crawler 内部文本/HTML 去重不作为文章身份或 Coverage 主键。

### 23.2 Freshness

- HOT Source 新文章在目标时间进入 PASS Canonical Markdown；
- Backfill 期间 Freshness SLO 不显著退化；
- watermark 丢失后可通过 overlap/URL Observation 恢复；
- micro-batch 不得跨越 Scheduler/Admission 破坏 Incremental 优先级。

### 23.3 Quality

- Cookie/Login/Challenge 页面不进入 current version；
- 代码块/标题/发布时间/表格等结构在 Golden Corpus 中稳定；
- Deterministic Recipe required field completeness 达阈值；
- Recipe 失效能 fallback/Quarantine，而不是静默写错；
- Canonical Version 不受 BM25 query/Filter 变化影响；
- Fit Filter 升级只重建 Projection；
- Runtime/Extractor/Renderer 升级无明显 Quality 回归。

### 23.4 Reliability

- Worker 强杀后 lease/fencing 自动恢复；
- Redis 清空后可从 PostgreSQL 重建任务；
- 重复投递不产生重复 Document Version；
- 单 URL 失败不回滚同 micro-batch 其他任务；
- stream cancel/SIGTERM 后 active pages/contexts 回到基线；
- Browser recycle 不造成任务丢失或重复写；
- DLQ 可定位并重放。

### 23.5 Runtime Compatibility & Outcome

- 所有 ACTIVE Discovery/Extraction Recipe 能针对目标 Runtime 编译；
- `trigger -> wait -> harvest` 等语义升级后保持一致；
- 不支持能力时 Release 必须失败，而非静默降级；
- Runtime 声称支持的关键能力在 Golden Corpus 上必须产生符合预期的 Outcome；
- Container 未找到、Mode 不匹配、Feature 未 engaged 时 Provider 不能宣称 Coverage 完成；
- stream cleanup/recycle regression 必须阻止 Runtime Release；
- 任意 Render Artifact 能定位 `recipe_release + compiler_version + runtime_release + capability_hash + compiled_plan_hash + runtime_outcome`。

### 23.6 Cost

- 每 Source/accepted version 成本可归因；
- Browser 使用率、秒数、RSS、recycle 可观察；
- Deterministic/Generic/LLM extractor 成本可区分；
- 预算达到阈值时能降级非关键 Provider，不产生无界成本。

---

## 24. 最终方案结论

平台以 **PostgreSQL + Object Storage 为事实层，Redis Streams 为运输层，配置化 Discovery/Extraction Recipe 为扩展面，HTTP-first/Browser-last 为成本策略，Canonical IR 为内容事实，Canonical/Fit Projection 分层为 AI 使用边界，Quality/Coverage/Release 为正确性护栏**。

对于 1000+ 站点，可扩展性的核心来自四层解耦：

```text
站点发现差异
 -> Source Profile + Discovery Recipe

站点正文模板差异
 -> Deterministic Extraction Recipe + Generic Fallback

工具/版本差异
 -> Runtime Capability Contract + Recipe Compiler + Adapter

单次实际执行差异
 -> Runtime Outcome Contract + URL Observation + Coverage Evidence
```

因此新增第 1001 个站点主要是 Probe、配置、Golden Corpus 和审核；升级 Crawl4AI/Playwright/Chromium 时无需把数百站点 Recipe 按参数名人工重写。动态栏目、Infinite Scroll、Load More 和 Virtual Scroll 统一通过：

```text
Dynamic Listing Provider
 -> Semantic Recipe
 -> Compiled Plan
 -> Runtime Outcome
 -> URL Observation
 -> Coverage Evidence
```

结构稳定的文章模板统一通过：

```text
Artifact
 -> Deterministic Extraction Recipe
 -> Generic Extractor Candidate
 -> Candidate Reconciliation
 -> Canonical IR
```

AI 友好输出统一通过：

```text
Canonical IR
 -> Canonical Markdown
 -> optional AI Fit Markdown / Search / Vector
```

其中 Filter/BM25/Pruning 的变化不会反向污染 Canonical Version。

Browser 批量能力只作为执行优化：

```text
Independent Tasks
 -> Scheduler/Admission
 -> streamed micro-batch
 -> per-task persist
 -> cleanup/recycle
```

必须坚持三个关键边界：

1. **Crawler 的 success、最终 DOM、内部文本去重和 Runtime Capability 都不能单独证明业务 Coverage。**
2. **结构化 selector 命中、JSON 可解析都不能单独证明 Canonical 正文正确。**
3. **AI Fit Markdown 的“更适合模型”不能替代 Canonical 内容完整性。**

最终形成可恢复、可审计、可扩展的知识供应链：

```text
Discover
 -> Prove Runtime Outcome when needed
 -> Prove Coverage
 -> Fetch
 -> Preserve Fact
 -> Extract Candidates
 -> Reconcile to Canonical IR
 -> Quality Gate
 -> Version
 -> Canonical/Fit Projection
 -> Index/RAG
 -> Incremental Verify
 -> Drift/Release Governance
```

这套边界使知识库长期正确性不绑定某个 crawler 的临时参数、DOM 合并逻辑、文本指纹、Filter 算法或批量执行机制，并能在站点数量、页面模板和抓取运行时持续变化时稳定演进。