# 博客知识库技术方案

适用范围：从约 1000 个技术博客网站抓取尽可能完整的历史文章，清洗为稳定 Markdown 形成长期知识库；后续持续新增站点，并支持长期增量同步、版本保留、Web 管理、搜索、审计与外部知识库发布。整体按“百万级文章、数百万到千万级 URL、长期运行、站点异构、可横向扩展、可恢复、可回放、可审计”设计。

## 1. 建设目标

1. 管理员只需录入博客根地址，系统自动探测 Sitemap、Feed、归档页、分类/标签/作者页、平台 API、普通站内链接和动态列表，生成可审核的站点配置草稿。
2. 支持约 1000 个站点历史全量回灌，并持续增加站点；常见站点通过模板、规则和 Adapter 接入，不修改核心状态机。
3. 同一站点允许多个 Discovery Provider，每个 Provider 独立维护 capability、checkpoint、覆盖证据和连续性。
4. 抓取标题、作者、发布时间、更新时间、标签、canonical、正文、代码块、表格、图片和附件，生成稳定 Markdown。
5. 默认 HTTP-first，只有证据表明确实需要 JavaScript、动态分页或交互时才升级 Browser/Crawl4AI。
6. 支持 ETag、Last-Modified、Sitemap lastmod、Feed GUID、API cursor、内容 hash 和重叠时间窗口驱动的增量同步。
7. 支持 durable frontier、断点续跑、幂等、lease、heartbeat、重试、指数退避、限流、熔断、按站公平调度和 backpressure。
8. 保存不可变网络快照、渲染 DOM、清洗 HTML、抽取候选、Article IR、Markdown、规则版本和质量结果；规则升级优先离线 replay。
9. 周期计划、执行实例和后台任务完全分离；计划是持久化业务配置，Scheduler 只是可重建触发器，实际抓取全部由 Worker 执行。
10. Web 管理端支持站点接入、覆盖检查、同步计划、任务管理、质量审核、版本 diff、错误诊断、搜索、成本和发布管理。
11. Crawl4AI、Playwright、Trafilatura、Readability 等均通过 Adapter 接入，不能成为业务状态真相源。

## 2. 核心原则

1. PostgreSQL 是业务状态真相源；S3/MinIO 是不可变大对象真相源。
2. Redis Streams 只做短期任务运输；状态先落 PostgreSQL，再通过 Transactional Outbox 投递。
3. URL 发现、准入、抓取、抽取、质量、索引、发布、计划与执行全部可版本化和可审计。
4. URL 被发现不等于允许抓取，必须经过统一 URL Admission Pipeline；真实连接仍由 Egress Gateway 再次执行 DNS/IP/端口/redirect 安全校验。
5. HTTP、Sitemap、Feed、API、Browser 分层，Browser 是昂贵升级路径。
6. 网络快照不可变；规则、Schema、模型和 Markdown Renderer 升级优先 replay，不重复访问站点。
7. 文章身份与 URL 分离；redirect、canonical、镜像、迁移只是 identity evidence。
8. `article_version` append-only，不覆盖旧版本；`crawl_run` 同样 append-only，不因计划修改而改写历史执行记录。
9. Bloom Filter/RoaringBitmap 只能加速，不能替代数据库唯一约束和 durable frontier。
10. 任何单次执行预算都不能被误判为“历史发现完成”。
11. 重型 Browser、CPU 和 LLM 工作只能运行在独立 Worker 中；控制面和 Scheduler 不执行长任务。
12. 标题、slug、URL path 尾段都只是展示属性，不能承担内部 identity；任何结果必须以稳定 `url_id/article_id/attempt_id/snapshot_id` 关联。
13. 内存 Scheduler、进程内 Semaphore、Redis Consumer ownership 都是可丢失运行时状态，不能取代数据库里的 schedule、run、task、lease 和幂等约束。
14. 通用后台任务表只保存运行时生命周期、轻量结果摘要和业务对象引用；文章、快照、覆盖结果等业务数据仍由各自领域表负责，避免把 task 表变成“万能业务表”。
15. 长期任务 payload 禁止直接持久化 API Key、Cookie、密码、BYOK Token 等秘密；只保存 `secret_ref/secret_version/credential_profile_id`，Worker 运行时从 Vault/Secret Manager/KMS 按最小权限解析。
16. 决定性状态迁移必须与对应 `run_event/task_event` 以及需要派发的 `outbox_event` 在同一 PostgreSQL 事务中提交；禁止“先改状态、后补事件”，SSE/Redis 只能传播已提交事实。

## 3. 总体架构

```mermaid
flowchart LR
    UI[Next.js Web] --> API[FastAPI Control Plane]
    API --> PG[(PostgreSQL)]
    API --> OUT[(Transactional Outbox)]

    SCHED[HA Scheduler / DB Due Claim] --> PG
    SCHED --> OUT
    OUT --> RELAY[Outbox Relay]
    RELAY --> Q[(Redis Streams)]

    Q --> DW[Discovery Workers]
    Q --> HF[HTTP Fetch Workers]
    Q --> BW[Browser/Crawl4AI Workers]
    Q --> EX[Extract Workers]
    Q --> QW[Quality/Normalize Workers]
    Q --> IW[Index Workers]
    Q --> PW[Publish Workers]

    DW --> GATE[URL Admission + Page Type + Priority]
    HF --> OBJ[(S3/MinIO)]
    BW --> OBJ
    EX --> OBJ

    DW --> PG
    HF --> PG
    BW --> PG
    EX --> PG
    QW --> PG
    IW --> PG
    PW --> PG

    IW --> SEARCH[(PostgreSQL FTS / OpenSearch / pgvector)]
    PG --> EVT[Run/Task Event Timeline]
    EVT --> UI
```

控制面管理配置、计划、权限和状态；Scheduler 只创建逻辑执行；数据面长期异步执行。

## 4. Crawl Mode

```text
FULL_BACKFILL   首次历史全量回灌
INCREMENTAL     持续同步新增和更新
RECONCILE       周期性重新枚举，修复断档
TOPIC_EXPANSION 有限预算专题扩展
REPROCESS       不访问网络，基于已有 snapshot 重抽取/清洗/索引/发布
```

约束：

- FULL_BACKFILL 中页面分类和评分只决定顺序，不能因关键词、低分或页面类型静默丢弃合法历史 URL。
- INCREMENTAL/TOPIC_EXPANSION 可使用预算、时间窗口和优先级阈值。
- Crawl4AI deep crawl 的 `max_pages`、Sitemap 单批条目数、Browser 单批页面数都只是 execution slice 预算，不是完成条件。
- 同站点默认只允许一个 active FULL_BACKFILL；INCREMENTAL 与 RECONCILE 是否并行由显式策略控制。

## 5. 新站点接入：Discovery Probe

管理员录入根 URL 后执行：

1. 获取根页面并记录 final URL、Content-Type、编码、响应头和页面结构。
2. 解析 robots.txt 中 Sitemap。
3. 探测常见 Sitemap/Sitemap Index。
4. 探测 RSS/Atom/JSON Feed。
5. 识别平台公开 API、年/月归档、分类/标签/作者分页。
6. 识别普通站内链接和动态列表。
7. 必要时执行 Browser Sample Probe。
8. 抽样 3~10 篇文章，识别正文结构和 metadata。
9. 生成 Provider、Fetch Profile、Filter、Scorer、Page Type Rule、Extraction Pipeline、Action Plan 草稿。
10. 在样本上运行 JSON-LD/meta、CSS/XPath、Trafilatura、Readability、Crawl4AI Markdown 等候选，给出质量证据。
11. Web 端 dry-run/canary/publish 后启动首次 FULL_BACKFILL。
12. 回灌启动后自动创建默认 INCREMENTAL schedule，管理员可修改频率、时区、重叠窗口和启停状态。

Web onboarding 应把“Probe -> 配置审核 -> FULL_BACKFILL -> INCREMENTAL schedule”串成一条可观察工作流。

## 6. Discovery Provider 与覆盖台账

Provider 优先级：

```text
Sitemap/Sitemap Index
> 平台 API
> Feed
> 年/月归档
> 分类/标签/作者分页
> HTML 同域递归
> Browser 动态列表
> 外部公开索引补漏
```

Provider capability：

```text
exhaustive   理论上可枚举完整集合
windowed     只暴露最近窗口
best_effort  尽力发现
```

`provider_coverage` 至少保存：

```text
provider_id
crawl_run_id
crawl_mode
checkpoint_before
checkpoint_after
pages_or_entries_scanned
discovered_count
admitted_count
rejected_count
expected_count
pagination_continuity
new_frontier_count
unresolved_error_count
started_at
finished_at
```

历史回灌完成至少要求：所有 Provider 到达稳定 checkpoint；exhaustive Provider 完整枚举；分页无 continuity gap；durable frontier 无可执行 URL；retry/dead-letter 已处理；新一轮 reconcile 不再产生显著新增；Coverage Ledger 无未知断档。

## 7. Sitemap：持久化、可恢复、流式处理

Sitemap 必须独立建模为持久化任务树，而不是一次递归函数跑完。

`sitemap_task` 至少保存：

```text
sitemap_task_id
site_id
provider_id
crawl_run_id
sitemap_url
parent_sitemap_url
depth
state
body_hash
etag
last_modified
entries_seen
child_sitemaps_seen
checkpoint
lease_owner
lease_until
attempt_count
next_attempt_at
```

执行要求：

1. 每个 Sitemap URL 先走 Admission；真实连接时 Egress 再做 DNS/IP/端口/redirect 校验。
2. 支持 Sitemap Index、普通 Sitemap、gzip、namespace、`lastmod`。
3. 使用安全 XML parser + streaming/iterparse；网络读取、解压、解析、Admission、frontier 持久化形成流式管线，不构造完整 XML DOM 和完整 URL 列表。
4. 同时限制压缩前字节、解压后字节、压缩比，防止 zip bomb。
5. 设置 `max_bytes_per_slice/max_entries_per_slice/max_child_sitemaps_per_slice/max_runtime_per_slice`；预算耗尽保存 checkpoint 后继续排队，不丢历史。
6. `visited sitemap` 持久化并有唯一约束，防止循环和重启后重复。
7. 深度限制只作为安全保护；超深进入异常队列/人工策略，不能静默宣布完成。
8. 子 Sitemap 可并发，但受全局、站点和域名三层配额约束。
9. 404/410、429、5xx、超大文件、解析失败分别分类并记录 coverage gap。
10. 解析到单个 `<url>`/`<sitemap>` 后立即 Admission + 持久化，崩溃时最多重放当前 slice。

## 8. URL Admission、页面分类与 Crawler Trap

Admission Pipeline：

1. URL 规范化；
2. SSRF/Egress 预检查；
3. robots 与站点访问策略；
4. Domain/Subdomain allowlist；
5. path allow/deny；
6. query 规范化；
7. Content-Type/扩展名规则；
8. canonical/redirect identity hints；
9. crawler trap 检测；
10. 页面类型分类；
11. Priority Scorer。

规范化处理 host 大小写、IDN、fragment、默认端口、tracking query、query 顺序、session 参数、trailing slash 和 percent-encoding。

页面类型：

```text
ARTICLE ARCHIVE CATEGORY TAG AUTHOR PAGINATION DOCUMENTATION
PRODUCT PRICING SEARCH ASSET UNKNOWN
```

FULL_BACKFILL 中页面类型和关键词只影响优先级与抽取策略，不作为永久过滤依据。重点防御日历无限翻页、搜索参数组合、tag/filter 笛卡尔积、无界 `?page=`、session/token 参数和多排序 URL。

## 9. 周期计划、执行实例与 HA Scheduler

### 9.1 `sync_schedule` 是持久化配置

至少保存：

```text
schedule_id
site_id
crawl_mode
schedule_revision
cron_expr
interval_seconds
timezone
dst_policy
jitter_seconds
overlap_window
misfire_policy
max_catch_up_runs
policy_release
enabled
next_due_at
last_expected_fire_at
last_scheduled_at
last_success_at
created_at
updated_at
```

数据库统一保存 UTC；`timezone` 使用 IANA 时区，只用于解释用户的本地计划并计算确定的 UTC 触发时刻。每次修改 cron/interval/timezone/misfire 等影响触发语义的字段都递增 `schedule_revision`，历史 run 永远保留当时的 revision。

`misfire_policy` 必须显式配置：

```text
SKIP       跳过停机期间错过的触发
FIRE_ONCE  恢复后合并为一次逻辑同步
CATCH_UP   有上限地逐次补触发
```

INCREMENTAL 默认推荐 `FIRE_ONCE`，结合 overlap window 和后续 RECONCILE 修复停机窗口；如果启用 CATCH_UP，必须设置 `max_catch_up_runs`，超过上限的旧窗口合并为一次 RECONCILE，避免 Scheduler 恢复时形成任务风暴。DST 的不存在时间和重复时间必须由 `dst_policy` 给出确定行为，不能依赖运行库默认值。

### 9.2 Scheduler 是可重建运行时

Scheduler 的内存 job 不是事实来源。启动、故障恢复或 Leader 切换后从 PostgreSQL 读取 enabled schedule 并 reconcile。Web 对计划的 create/update/enable/disable 先提交数据库事务，再由 Scheduler reconcile；修改计划不能改写历史 run。

### 9.3 HA 触发

可采用 Leader Lease，也可由数据库原子 claim 到期 schedule：

```sql
SELECT *
FROM sync_schedule
WHERE enabled = true AND next_due_at <= now()
ORDER BY next_due_at
FOR UPDATE SKIP LOCKED;
```

一次逻辑触发必须在同一事务内：

```text
1. claim 到期 schedule
2. 读取 current schedule_revision 和 expected_fire_at = next_due_at
3. 根据 misfire_policy 决定 skip / fire once / bounded catch-up
4. 创建 append-only crawl_run
5. 生成 run dedupe_key
6. 写 RUN_CREATED run_event
7. 写 outbox_event
8. 推进 next_due_at / last_expected_fire_at / last_scheduled_at
9. COMMIT
```

Scheduler 不执行 HTTP、Browser、抽取、索引或发布。

### 9.4 逻辑触发时间、Misfire 与 DST

必须区分“本来应该触发的时间”和“实际恢复/创建 run 的时间”：

```text
expected_fire_at     逻辑计划时间，参与幂等和窗口计算
actual_triggered_at  Scheduler 实际创建 run 的时间
```

例如 Scheduler 停机 2 小时后恢复，不能把所有补跑都视为“现在”的同一个任务。对于 CATCH_UP，每个允许补跑的窗口保留原 `expected_fire_at`；对于 FIRE_ONCE，创建一条合并 run，并把被合并的逻辑窗口范围记录到 `window_start/window_end` 和事件中。

`next_due_at` 必须由业务调度逻辑计算并持久化，而不是只存在 APScheduler 内存。这样 Web、HA 副本和故障恢复看到的是同一个未来触发事实。修改 schedule 后，旧 revision 未触发的 runtime job 必须被撤销/reconcile，新 revision 从明确基线重新计算 next due。

### 9.5 `crawl_run` 与 schedule 分离

`crawl_run` 至少包含：

```text
crawl_run_id
site_id
schedule_id
schedule_revision
crawl_mode
triggered_by
expected_fire_at
actual_triggered_at
window_start
window_end
policy_release
status
dedupe_key
supersedes_run_id
created_at
started_at
completed_at
```

计划表示“未来何时执行”，run 表示“一次已经发生或正在发生的逻辑执行”。计划禁用、删除或修改不影响旧 run。

### 9.6 Run Now 与重复触发

周期触发建议 dedupe key：

```text
sync:{site_id}:{schedule_id}:{schedule_revision}:{crawl_mode}:{expected_fire_at}
```

手工 Run Now 使用独立的 request id/逻辑窗口幂等键，不能伪装成某个 cron expected fire。PostgreSQL 对 active run 建部分唯一索引，防止 cron 重复、Leader 切换、HTTP 重试、Web 双击或 Outbox 重放制造重复执行。禁止只采用“先查再插”的应用层去重。

- `Run Now`：若同 site/mode 已有 active run，默认返回现有 run，或按显式策略创建独立 manual run。
- `Force New Run`：创建新 run，写 `supersedes_run_id`；旧 run 进入 `cancel_requested`，由 Worker 协作停止。
- FULL_BACKFILL 默认禁止两个 active run 并行。

## 10. 后台任务状态机、Claim、Lease 与恢复

所有长任务持久化为通用 runtime task 或领域 task：

```text
task_id
crawl_run_id
kind
site_id
payload
status
dedupe_key
priority
run_after
attempt_count
max_attempts
worker_id
lease_version
lease_until
claimed_at
heartbeat_at
started_at
completed_at
result_ref
error_class
created_at
updated_at
```

推荐状态：

```text
QUEUED -> CLAIMED -> RUNNING -> COMPLETED
RUNNING -> RETRY_WAIT -> QUEUED
RUNNING -> CANCEL_REQUESTED -> CANCELLED
RUNNING -> FAILED / DEAD_LETTER
```

### 10.1 原子 Claim

Worker 使用 `FOR UPDATE SKIP LOCKED`、`UPDATE ... RETURNING` 或 lease CAS 原子抢占。Redis Streams Consumer Group 只负责运输；最终业务执行权仍由 PostgreSQL claim/lease 决定。

Worker 不应先 claim 大量自己暂时没有 runtime capacity 的任务。Dispatcher 应按 `runtime_class/kind/site` 分 lane 或在 claim 条件中考虑可用配额，避免任务已 CLAIMED 却长时间卡在本地 semaphore，造成 lease 占用和队头阻塞。

### 10.2 Heartbeat 与 Lease

- Worker 定期刷新 `heartbeat_at/lease_until`。
- 状态更新必须匹配 `worker_id + lease_version`。
- lease 过期后旧 Worker 即使恢复，也不能提交成功结果覆盖新 Worker。
- Worker 正常退出时主动释放或缩短 lease。

### 10.3 孤儿任务恢复

Recovery Worker 周期扫描：

- `CLAIMED/RUNNING` 且 heartbeat 超时；
- 进程重启后 heartbeat 为空的 orphaned task；
- 未达 `max_attempts` 重新排队并增加 attempt；
- 达上限进入 DEAD_LETTER 并记录最终错误和恢复原因。

Worker crash 只能造成当前 lease slice 的有限重放，不能产生永久僵尸任务。

### 10.4 Domain Data 与 Runtime Data 分离

`background_task` 只保存生命周期、进度、错误、轻量 result summary 和 `result_ref`。文章版本、抓取快照、Provider Coverage、索引结果等仍存领域表。

Executor 接口建议只暴露：

```text
task
payload
event_writer
heartbeat/is_lease_valid
is_cancel_requested
complete(result_ref, summary)
fail(error)
```

Executor 不直接依赖通用 task 表 schema；这样未来从进程内 Worker 迁移到独立 Worker/Kubernetes Job 时不改业务代码。

### 10.5 Secret Reference

任务 payload 中不得嵌入可直接使用的密钥。保存：

```text
credential_profile_id
secret_ref
secret_version
required_scope
```

Worker 启动 attempt 后按身份和最小权限解析 secret，使用完立即释放；日志、event、error、snapshot 元数据均进行密钥脱敏。

### 10.6 状态、事件与消息的事务原子性

任何会改变 `background_task/crawl_run` 决定性状态的操作，都必须通过统一 transition service 在同一 PostgreSQL 事务中完成：

```text
BEGIN
1. 用 task_id/run_id + expected status + lease_version 做条件更新
2. 写 task_event/run_event，event_id 单调递增
3. 如需唤醒后续阶段，写 outbox_event
4. COMMIT
```

`COMPLETED/FAILED/CANCELLED/RETRY_WAIT/RECOVERED` 等状态不能先 commit，再用另一个事务“补事件”。否则进程恰好在两个事务之间崩溃时，会产生状态与审计时间线不一致。纯采样型 progress event 可独立提交，但它不能改变业务状态，也不能作为唯一成功证据。

SSE、Redis Pub/Sub、Redis Streams 或 LISTEN/NOTIFY 只负责传播已提交事件；消费者根据 `event_id/outbox_id` 幂等处理，不能把内存广播当作事实来源。

## 11. 取消、Supersede 与安全终止

取消是协作状态，不是简单把数据库状态改成 failed：

1. API 写 `CANCEL_REQUESTED`，并在同一事务写 `RUN_CANCEL_REQUESTED/TASK_CANCEL_REQUESTED` event。
2. Worker 在网络请求、分页 slice、Browser action、Artifact Commit 前后检查 cancellation/lease。
3. 能取消的 I/O 主动 abort；Browser context/page 必须清理。
4. 已开始的原子 Artifact Commit 要么完成并留下可引用对象，要么保持 staging 状态由 Cleanup 回收。
5. 最终完成提交必须再次校验 lease 和 cancel 状态。
6. `Force New Run` 使用 supersede 关系，不删除旧事件、不伪装旧任务从未发生。

## 12. Append-only Event Timeline 与 Web 实时进度

除最新 status 外，每个 run/task 追加不可变事件：

```text
RUN_CREATED
DISCOVERY_STARTED
SITEMAP_SLICE_COMPLETED
FRONTIER_GROWTH
FETCH_STARTED
FETCH_THROTTLED
BROWSER_ESCALATED
SNAPSHOT_COMMITTED
EXTRACTION_REPLAYED
QUALITY_REJECTED
RETRY_SCHEDULED
RUN_CANCEL_REQUESTED
TASK_RECOVERED
RUN_COMPLETED
RUN_FAILED
```

事件至少保存：

```text
event_id        # 单调递增游标
run_id
task_id
phase
status
summary
payload
created_at
```

决定性 state-change event 与状态行、必要 outbox 同事务提交；只有 commit 后的 event 才进入 Web 时间线和消息传播。

SSE/WebSocket 不能每次重连把整段历史从头扫一遍。接口应支持：

```text
GET /tasks/{task_id}/events?after_id=123
Last-Event-ID: 123
```

服务端用 `WHERE task_id = ? AND event_id > ? ORDER BY event_id LIMIT ?` 增量读取；先 catch-up，再持续发送新事件，最后发送明确 terminal event。网络重连按 `event_id` 恢复，客户端按 event_id 去重。

高频进度事件应分级：状态变化和关键业务事件长期保留；纯采样型进度可以聚合/降采样，避免百万 URL 任务产生无限事件表。Web 审计不能依赖日志文本推断状态。

## 13. 公平调度、限流与 Backpressure

Dispatcher 使用 weighted fair queue。每域维护最大并发、QPS、最小请求间隔、token bucket、Retry-After、429/403/5xx 退避、HTTP/Browser 独立预算和 circuit breaker。

优先级建议：

```text
NEW_FEED / CHANGED
> INCREMENTAL_REFRESH
> RECONCILE
> HISTORICAL_BACKFILL
> LOW_PRIORITY_REPROCESS
```

并发至少三层：

1. 全局系统配额；
2. site/domain 配额与 politeness；
3. Worker 进程内 semaphore/pool。

下游积压时降低 Discovery、暂停 Browser expansion、优先处理已抓未抽取 snapshot，并保留增量高优先级通道。

## 14. Async Runtime Topology

Adapter 声明：

```text
runtime_class = ASYNC_NATIVE | BLOCKING_IO | CPU_BOUND | BROWSER
```

- `ASYNC_NATIVE`：HTTP/Sitemap/Feed，运行于主 asyncio Worker。
- `BLOCKING_IO`：同步 SDK/网络库，进入有界线程池或专用 Worker。
- `CPU_BOUND`：DOM 解析、diff、MinHash/SimHash、IR normalize、Markdown render，进入进程池/CPU Worker。
- `BROWSER`：Crawl4AI/Playwright，进入 Browser Worker。

HTTP/Discovery Worker 使用最小依赖镜像和长期连接池。Browser Worker 长期维护 Browser/context/page pool、session TTL、内存与 CPU 上限。禁止每 URL 新建 event loop 或 Browser，也禁止对百万 URL 一次性 `asyncio.gather`。

## 15. HTTP-first、分层获取与 Crawl4AI Adapter

HTTP Worker 尽量发送 `If-None-Match/If-Modified-Since`。304 只更新 freshness；200 先保存 raw bytes 再抽取。

获取链：

```text
HTTP 原文
 -> HTTP + 结构化抽取
 -> Browser/Crawl4AI 渲染
 -> 站点专用 API/Adapter
 -> 人工诊断
```

每次 attempt 必须记录：获取引擎、升级原因、fallback 原因、超时、runtime release、最终来源。Browser escalation 原因结构化保存：

```text
EMPTY_ARTICLE_BODY
SPA_SHELL
JS_PAGINATION
LOAD_MORE
INFINITE_SCROLL
DYNAMIC_RENDER_REQUIRED
EXTRACTION_READINESS_FAILED
```

Crawl4AI 可承担 AsyncWebCrawler、JS 渲染、deep crawl、Markdown 生成和 CSS/XPath/Regex extraction，但不能承担 durable frontier、多站公平调度、业务 checkpoint、文章版本、schedule、run 和覆盖完成判定。

Adapter 输出统一为：

```text
adapter_output_schema_version
url_id
attempt_id
final_url
status_code
headers
raw_html_ref
rendered_dom_ref
raw_markdown_ref
fit_markdown_ref
links/media
metrics
source_provenance
fallback_reason
error_class
runtime_bundle_release
```

Browser/Crawl4AI 镜像锁定完整依赖图、Browser revision、adapter version 和 image digest；HTTP-only 模式必须能独立运行。

## 16. Snapshot、Artifact Commit 与 Replay

每次成功网络访问保存不可变 snapshot：

```text
raw response bytes
response headers
observed charset
final_url
redirect chain
resolved_ip_evidence
body sha256
rendered DOM
cleaned HTML
raw Markdown
fit Markdown
links/media references
source provenance
fetch/crawler/runtime release
adapter_output_schema_version
fetched_at
```

大对象使用原子 Artifact Commit：

```text
1. 以 attempt_id/snapshot_id 创建 staging object key
2. 流式上传并计算 sha256/size/media type
3. 校验后得到 content-addressed object key
4. PostgreSQL 事务写 artifact_object + fetch_snapshot + 状态变化 + event + outbox_event
5. COMMIT 后 snapshot=COMPLETE，对象不可变
6. 重试通过 idempotency key/hash 复用已有完整对象
7. Cleanup Worker 回收未被引用的过期 staging object
```

禁止任务先标记成功再异步补写对象或事件。REPROCESS 默认只使用已有 snapshot，不访问网络。

## 17. 抽取链、Article IR 与 Markdown

同一 snapshot 产生多个 candidate：

1. 平台 API/JSON-LD/OpenGraph/meta；
2. 站点 CSS/XPath Contract；
3. Trafilatura；
4. Readability；
5. Crawl4AI raw/fit Markdown；
6. 必要时 LLM Structured Extraction。

HTML 先转换为 Article IR：

```text
heading
paragraph
list
quote
code_block
table
image
link
embed_placeholder
```

Quality Gate 从 candidate 中选择最终字段和正文，任何 extractor 都不能静默覆盖前一结果。

Markdown 路径建议：

```text
knowledge/{site_slug}/{yyyy}/{article_id}.md
```

front matter 至少包含：

```yaml
article_id:
site_id:
source_url:
canonical_url:
title:
authors:
published_at:
updated_at:
fetched_at:
content_hash:
article_version:
extraction_release:
runtime_bundle_release:
crawl_run_id:
```

文件名不依赖标题；正文相对链接和图片统一变成可追踪的绝对或托管地址。

## 18. URL 去重、文章身份与版本

URL 唯一键建议：

```text
(site_id, sha256(normalized_url))
```

exact duplicate 使用 Article IR/content hash；near duplicate 用 MinHash/SimHash 生成候选；redirect/canonical 只作为 identity evidence；多 URL 同文章记录 `article_url_relation`。

正文、关键 metadata、canonical identity 或 Article IR 实质变化时创建新 `article_version`；仅抓取时间或响应头变化且 IR hash 相同时不创建新版本。

## 19. 增量同步与 Reconcile

增量信号优先级：

```text
Sitemap lastmod
> Feed GUID/updated_at
> API cursor
> ETag
> Last-Modified
> normalized content hash
```

windowed source 必须使用重叠时间窗口，不能只保存最后一条 GUID 单向推进。长时间离线后扩大 overlap，并周期性通过 Sitemap/API/Archive RECONCILE 修复断档。

建议默认频率：

- 高频站点：15 分钟~数小时；
- 普通技术博客：数小时~1 天；
- 低频站点：1~7 天；
- RECONCILE：周/月级，按断档风险调整。

自适应调度策略必须版本化并可在 Web 查看。自适应修改频率时生成新的 `schedule_revision`，不能覆盖历史 run 对应的调度语义。

## 20. 幂等、Outbox 与一致性

URL 主状态可表达：

```text
DISCOVERED -> ADMITTED -> QUEUED -> FETCHING -> FETCHED
-> EXTRACTING -> QUALITY_CHECK -> STORED -> INDEXED -> PUBLISHED
```

失败状态：`RETRY_WAIT/DEAD_LETTER/REJECTED`。

幂等 key：

```text
run:     site_id + schedule_id + schedule_revision + crawl_mode + expected_fire_at/logical_window
fetch:   url_id + requested_snapshot_epoch + fetch_profile_release + cache_policy_release
extract: snapshot_id + extraction_pipeline_release_id
index:   article_version_id + index_release_id
publish: article_version_id + destination + publish_release_id
```

每次决定性状态迁移在同一 PostgreSQL 事务中写：

```text
业务状态/领域结果
+ run_event/task_event
+ outbox_event
```

Relay 只投递已提交 outbox_event 到 Redis Streams。重复消息必须安全重放；ack 只能发生在持久化成功之后或遵循明确 inbox/outbox 协议。这样数据库状态、Web 审计时间线和后续消息不会因进程在两个 commit 之间崩溃而分叉。

## 21. 核心数据模型

至少包括：

```text
site
site_source
site_source_release
discovery_provider
discovery_checkpoint
provider_coverage
sync_schedule
crawl_run
run_event
sitemap_task
url_frontier
background_task
task_event
fetch_task
crawl_attempt
artifact_object
fetch_snapshot
browser_session_lease
page_type_rule_release
url_filter_release
url_scorer_release
fetch_profile_release
cache_policy_release
browser_action_plan_release
adapter_schema_release
extraction_pipeline_release
extraction_candidate
quality_evaluation
article
article_url_relation
article_version
index_release
publish_state
runtime_bundle_release
credential_profile
outbox_event
```

`sync_schedule` 管未来触发，`crawl_run` 管一次逻辑执行，`background_task` 管可领取工作单元，三者不得合并成一张表。`run_event/task_event` 是结构化审计事实，决定性事件必须和对应状态迁移事务耦合。

## 22. Quality Gate 与 Drift Detection

至少监控：标题存在率、正文字符数/段落数、链接密度、导航占比、代码块保留率、表格保留率、图片有效率、发布时间证据、boilerplate 比率、Markdown 截断、canonical/final URL 冲突和历史重复度。

平台指标：

```text
extraction_success_rate
body_length_p50/p95
title_missing_rate
code_block_retention
table_retention
browser_fallback_rate
duplicate_ratio
sitemap_gap_count
provider_continuity_gap
queue_lag
schedule_trigger_lag
schedule_misfire_count
schedule_catch_up_count
run_duration_p50/p95
stale_task_recovery_count
dedupe_collision_count
heartbeat_age_p95
browser_pool_active
artifact_commit_failure_rate
staging_object_age_p95
event_stream_lag
event_replay_count
state_event_atomicity_violation_count
cancel_to_stop_latency
```

release 后指标突变时自动暂停扩大范围、重新 Probe 并生成规则草稿；低质量内容不能覆盖高质量旧版本。

## 23. 错误分类与重试

统一分类 DNS、connect/read timeout、TLS、401/403、404/410、429、5xx、robots denied、egress denied、content too large、sitemap malformed、depth anomaly、decompression bomb、browser crash/timeout、extraction failed、quality rejected、runtime mismatch、artifact commit failed、schema mismatch、lease lost、scheduler duplicate trigger、scheduler misfire overflow、secret resolution failed 等。

可重试错误按 `Retry-After + jittered exponential backoff + max attempts + circuit breaker` 处理；配置错误、明确拒绝和确定性校验失败不原样重试。只重放最小必要单元。

## 24. 安全、robots 与 Egress

所有网络访问统一经过 Egress Gateway：

- 只允许 http/https；
- 拒绝 localhost、RFC1918、loopback、link-local、metadata endpoint；
- DNS 解析检查全部 A/AAAA；失败默认 fail-closed；
- 连接使用已验证解析结果，防 DNS rebinding/TOCTOU；
- redirect 每一跳重新校验；
- 限制端口并禁止 file/ftp/gopher 等 scheme；
- 子 Sitemap、文章 URL、Browser 子资源都执行相同策略；
- 保存 redirect chain、DNS/IP 决策和拒绝原因；
- 遵守 robots、Retry-After 和礼貌抓取规则；
- 不绕过验证码、登录、付费墙或明确访问控制。

Web/API 的 URL parser 只负责语法、规范化和用户反馈，不是 SSRF 安全边界。即使 URL 在入口校验中语法合法，真实连接前仍必须由 Egress 重新解析和校验；生产环境不能因为 parser 接受 localhost、直接 IP 或某个 hostname 就放行连接。

Secret 只通过 Vault/云 Secret Manager/KMS 解析，任务 payload、event、日志和 artifact metadata 默认脱敏。

## 25. Web 管理功能

### 25.1 站点接入

- Probe 向导；
- Sitemap/Feed/API/归档/动态列表证据；
- 样本 URL 与 HTTP/Browser 差异；
- 自动规则草稿；
- dry-run/canary/publish；
- 一键启动 FULL_BACKFILL；
- 自动创建默认 INCREMENTAL schedule。

### 25.2 同步计划与运行

- cron/interval、IANA timezone、DST policy、jitter、overlap window；
- schedule revision、misfire policy、max catch-up runs；
- enabled/disabled；
- next_due_at、last_expected_fire_at、last_scheduled_at、last_success_at；
- expected_fire_at 与 actual_triggered_at 的触发延迟；
- misfire/catch-up/reconcile 合并记录；
- Run Now / Force New Run；
- dedupe key、supersede/cancel 关系；
- schedule 修改审计和 revision diff。

### 25.3 覆盖与任务

- Provider capability/checkpoint/continuity gap；
- Sitemap task 树、depth、未完成子任务和异常；
- expected/discovered/admitted/fetched/article count；
- full backfill 完成证据；
- run/task 状态和 append-only event timeline；
- `Last-Event-ID/after_id` 断线续传；
- heartbeat、lease、attempt、worker、recovery 记录；
- 暂停、恢复、取消、重试；
- 单 URL 调试和 reprocess；
- runtime class、队列与 Worker 饱和度。

### 25.4 质量与调试

- raw HTML/rendered DOM/raw Markdown/final Markdown 对比；
- extraction candidates 与最终选择理由；
- 页面类型和 priority score 解释；
- provenance、Browser escalation、runtime bundle、adapter schema；
- redirect/DNS/Egress 决策链；
- artifact object/hash/commit 状态；
- selector drift 和文章版本 diff。

### 25.5 搜索与发布

- 全文搜索；
- 可选向量搜索；
- article/source/version 浏览；
- 外部知识库发布状态；
- 失败重推和版本回滚。

## 26. 搜索与发布

默认 PostgreSQL FTS；查询规模提高后接 OpenSearch。向量检索按需使用 pgvector/独立 Vector DB，不作为抓取主链路强依赖。

发布 Worker 支持文件系统/Git、S3、外部 RAG/知识库、搜索索引和版本化 API；幂等键为：

```text
article_version_id + destination + publish_release_id
```

## 27. 推荐技术栈

| 层 | 推荐 |
|---|---|
| API | Python + FastAPI |
| Web | React / Next.js |
| Scheduler | PostgreSQL 持久计划 + leader lease/DB claim；APScheduler 可作单实例时钟但不是真相源 |
| Worker | Python asyncio + 独立 Blocking-I/O/CPU/Browser Worker |
| 状态库 | PostgreSQL |
| 队列 | Redis Streams + Transactional Outbox |
| 大对象 | S3 / MinIO |
| HTTP | httpx / aiohttp |
| Browser | Playwright + Crawl4AI Adapter |
| Sitemap / Feed | defusedxml/lxml iterparse + feedparser |
| 正文抽取 | CSS/XPath + Trafilatura + Readability + Crawl4AI |
| 文档模型 | Article IR + Markdown Renderer |
| 全文检索 | PostgreSQL FTS，后续 OpenSearch |
| 向量 | pgvector/独立 Vector DB，可选 |
| 观测 | OpenTelemetry + Prometheus + Grafana + Loki |
| Secret | Vault / 云 Secret Manager / KMS |
| 部署 | Docker + Kubernetes |

初期 1000 站不建议直接引入 Kafka。Redis Streams + PostgreSQL Outbox 足够；吞吐、保留周期或跨区域消费出现明确瓶颈时再迁移 Kafka/Pulsar。

## 28. 运行时发布与供应链

每个 `runtime_bundle_release` 固定：

```text
runtime_role
source_commit_sha
built_artifact_hash
container_image_digest
python_lock_hash
crawler_engine_version
playwright_version
browser_revision
adapter_version
adapter_output_schema_version
build_sbom_ref
created_at
```

发布前执行 clean build、dependency lock/resolve、SBOM/依赖审计、单元测试、golden snapshot replay、Browser smoke test、Adapter contract test。

运行时至少拆为：

```text
crawler-http      HTTP/Sitemap/Feed，最小依赖
crawler-blocking  同步 SDK/网络依赖，有界并发
crawler-browser   Crawl4AI/Playwright/Browser 完整依赖
crawler-cpu       DOM/IR/diff/hash/Markdown CPU 任务
```

## 29. 部署与容量规划

推荐最小生产拓扑：

```text
2 x FastAPI Control Plane
2 x Scheduler（leader lease 或 DB due-row claim）
2 x Outbox Relay
N x Discovery Worker
N x HTTP Fetch Worker
可选 Blocking-I/O Worker Pool
独立 Browser/Crawl4AI Worker Pool
N x Extract / Quality Worker
N x Index / Publish Worker
1+ x Recovery/Cleanup Worker
PostgreSQL HA
Redis Streams
S3 / MinIO
Prometheus + Grafana + Loki + OpenTelemetry Collector
Next.js Web
```

扩容依据：queue lag、schedule trigger lag、HTTP connection concurrency、CPU、Browser memory/seconds、domain throttle、artifact throughput、event lag、object storage 和 index size，而不是简单按站点数量扩容。

## 30. 实施阶段

### Phase 1：MVP

- site/source/Probe；
- Sitemap/Feed；
- 持久化 `sitemap_task` + true streaming parse；
- durable frontier；
- FULL_BACKFILL/INCREMENTAL；
- `sync_schedule + crawl_run` 分离；
- schedule revision + expected fire + misfire/catch-up/DST 语义；
- Scheduler DB reconcile + leader/DB claim；
- run dedupe + Web Run Now；
- task claim/lease/heartbeat/recovery；
- 决定性状态 + event + outbox 同事务 transition service；
- task/run event timeline + event cursor；
- HTTP-first；
- runtime class 路由；
- raw snapshot + 原子 Artifact Commit；
- stable ID；
- Trafilatura/Readability；
- Article IR/Markdown；
- PostgreSQL + Redis Streams + Outbox + MinIO；
- Web 站点/计划/任务/文章；
- ETag/Last-Modified；
- Admission + Egress/robots；
- Secret Reference；
- 基础 error taxonomy。

### Phase 2：异构站点与质量闭环

- CSS/XPath Rule；
- Page Type Rule；
- URL Filter/Scorer；
- Browser Action DSL；
- Crawl4AI/Playwright Adapter；
- Browser 全局/site/进程三层配额；
- Quality Gate；
- replay；
- drift detection；
- coverage gap 面板；
- SSE `Last-Event-ID`/断线续传；
- 事件聚合/降采样；
- runtime/provenance 调试面板。

### Phase 3：规模化

- 多 Provider checkpoint；
- Provider Coverage Ledger；
- weighted fairness；
- backpressure；
- 多 Scheduler HA；
- 自适应 schedule；
- RECONCILE；
- Archive Full；
- OpenSearch/pgvector；
- 外部知识库发布；
- 自动回归测试和运行时发布门禁。

## 31. 验收标准

### 31.1 全量回灌

随机选择不同规模和技术栈站点，输出 Provider 级覆盖证据；Worker 重启后可继续；Sitemap slice 预算耗尽后能从 checkpoint 恢复；FULL_BACKFILL 不因关键词、页面分类、单次 max_pages/max_entries 或固定 URL 上限漏掉已发现合法 URL；大 Sitemap 不把完整 XML 和 URL 集合同时常驻内存。

### 31.2 增量同步与调度

新增文章在 SLA 内进入知识库；更新文章产生新版本；304/内容未变不产生无意义版本；Feed 窗口断档可由 reconcile 修复。

关闭任意 Scheduler 实例后，另一实例可从 PostgreSQL 恢复计划；同一 `schedule_revision + expected_fire_at` 在 Scheduler 重启、Leader 切换、HTTP 重试、Outbox 重放和 Web 连续点击下最多产生一个周期 active run。修改 cron/interval/timezone 不改变历史 run。

故障注入必须覆盖 Scheduler 停机后错过 1 次和多次触发，分别验证 SKIP/FIRE_ONCE/CATCH_UP；CATCH_UP 超过上限不会形成任务风暴而会合并为 RECONCILE。对存在 DST 的时区构造春季不存在本地时间和秋季重复本地时间，验证同一 revision 下 `expected_fire_at` 计算确定、可重放、无双重歧义触发。

### 31.3 任务恢复与取消

故障注入覆盖 CLAIMED、RUNNING、写 snapshot 前后、heartbeat 中断、lease 失效和 cancel_requested；stale/orphaned task 能重新排队或进入 DEAD_LETTER；旧 Worker 失去 lease 后不能覆盖新 Worker 结果；Force New Run 能保留 supersede/cancel 审计链。

### 31.4 Event Stream 与状态一致性

SSE 断开后用 `Last-Event-ID/after_id` 从断点继续，不从头重放全部事件；客户端重复连接不会重复展示同一 event_id；终态后发送 terminal event 并关闭；百万级任务的高频进度事件有可验证的聚合/降采样策略。

对每个决定性 transition 在“更新状态后、写 event 前”“写 event 后、写 outbox 前”“事务提交前”注入进程崩溃/数据库异常，最终只能看到全部提交或全部回滚，不能出现 `COMPLETED` 无完成事件、存在完成事件但 task 仍 RUNNING、或状态已推进但下游永远没有 outbox 的分裂状态。

### 31.5 抽取质量

标题、发布时间、正文、代码块、表格按抽样集评估；规则升级可基于 snapshot replay；Markdown 不因文本扁平化破坏技术结构。

### 31.6 可恢复性与安全

Sitemap/Fetch/Browser 任务均能 lease 重放；嵌套 Sitemap、redirect 和最终文章 URL 都执行 Admission/Egress/SSRF 校验；超大 Sitemap、压缩炸弹、循环 Sitemap、429/5xx 不拖垮 Worker。

安全测试覆盖入口公网 URL 302 到 RFC1918/metadata、DNS 多 A/AAAA 混合公网与内网、DNS rebinding、嵌套 Sitemap 指向内网、危险端口和 Browser 子资源越权请求，均在真实网络执行前被阻断并留下审计证据。Web/API parser 即使接受语法合法的 localhost、直接 IP 或 hostname，也不能绕过 Egress 的 fail-closed 策略。

任务 payload、event、日志和 snapshot 元数据中不能出现可直接使用的 API Key/Cookie/密码；只允许出现 secret reference。

### 31.7 可运维性

Web 能定位任一 URL 的发现来源、Sitemap 父子关系、Admission/Egress 决策、页面类型、priority、attempt、snapshot、artifact、Adapter schema、runtime class、抽取 candidate、质量结果、文章版本和发布状态。

Web 能定位任一 schedule 的 revision、timezone/DST policy、misfire/catch-up policy、下次触发、expected/actual trigger、最近 run、dedupe key、supersede/cancel 关系，以及任一任务的 worker、lease、heartbeat、attempt、恢复事件和完整 event timeline。

### 31.8 运行时与供应链

`crawler-http` 在完全不安装 Crawl4AI/Playwright 时仍能完成 Sitemap/Feed/HTTP 抓取与增量判断；`crawler-browser` 使用独立锁定依赖图和镜像 digest；同一 runtime bundle 可重复构建并通过 golden replay/contract test。

### 31.9 身份与持久化原子性

构造 `/blog/setup`、`/docs/setup`、同标题不同 URL、slug 变更、canonical 迁移等碰撞用例，必须保持不同 `url_id`，不能因 path 尾段或标题相同被覆盖。

故障注入覆盖对象上传中断、上传成功但 DB 回滚、DB 提交后 Worker 崩溃、重复消息重放和 Cleanup 并发。COMPLETE snapshot 必须引用完整可校验对象；staging object 可回收；重复 attempt 不制造不可控重复对象。

## 32. 最终方案

最终采用“持久化发现 + Durable Frontier + 持久化同步计划 + Append-only Crawl Run + HA Scheduler 只触发不执行 + Transactional Outbox + 可恢复后台任务 + HTTP-first + Browser 按证据升级 + 不可变 Snapshot + 原子 Artifact Commit + 多候选抽取 + Article IR + append-only 文章版本 + 增量/对账 + Web 管理”的平台架构。

关键工程约束：

- Sitemap 是可恢复任务树；所有大小、条目数、深度和运行时间限制只是 execution slice 安全预算，不能静默截断历史。
- `sync_schedule` 是未来计划，`crawl_run` 是一次逻辑执行，`background_task` 是可领取工作单元，三层职责分离。
- Schedule 触发语义是业务数据：持久化 revision、IANA timezone、DST policy、misfire/catch-up policy、`expected_fire_at` 和 `next_due_at`，不能依赖内存 Scheduler 默认行为。
- Scheduler 到期只创建 run + event + outbox，不直接抓取；相关状态与事件在同一事务提交。
- 数据库级 dedupe/unique 约束保证 cron 重复、Leader 切换、Web 重试和消息重放不制造重复 active run。
- Worker 使用原子 claim、lease、heartbeat、attempt 和孤儿恢复；失去 lease 的 Worker 不允许提交结果。
- 所有决定性 task/run 状态迁移使用“状态 + event + outbox”同事务协议，禁止状态和审计时间线分两次 commit。
- 取消采用 `cancel_requested + cooperative stop`，Force New Run 保留 supersede 审计链。
- 通用 task 只保存 runtime 状态与业务引用，不能吞并领域数据。
- run/task event append-only，并以单调 event_id 支持 SSE 断线续传；高频进度事件可聚合，关键状态事件长期保留。
- task payload 禁止持久化秘密，只保存 Secret Reference。
- URL 页面类型和关键词只用于调度与抽取选择，不用于 FULL_BACKFILL 永久过滤。
- 并发采用全局、站点/域名、进程三层有界控制；任务按 runtime lane 调度，避免 claim 后本地队头阻塞。
- Crawl4AI 是可替换 Adapter，Browser 生命周期长期复用，不能掌握平台状态真相。
- 获取链每次 fallback/升级都保存 provenance 和原因。
- 正文抽取以 Article IR 为中心，不采用简单文本扁平化作为最终知识库格式。
- Web/API URL 格式校验只负责入口体验；Admission 负责准入，Egress 在真实连接时强制执行 DNS/IP/端口/redirect 安全策略。
- 内部关系使用稳定 ID，绝不使用标题、slug 或 URL path 尾段作为更新 key。
- snapshot/artifact 使用内容 hash、schema/release 版本和原子提交协议；规则升级优先 replay。
- 供应链通过 lock、SBOM、artifact hash、image digest 和 contract test 固定。

该方案可从几十个站点平滑扩展到 1000+ 站点，并在持续增加新站点、新发现方式、新抓取引擎和新同步频率时保持核心状态机稳定，同时保证长期增量同步在 Scheduler 重启、停机补触发、DST 边界、Worker 崩溃、消息重放和多副本部署下仍可恢复、可去重、可审计。