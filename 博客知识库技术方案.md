# 博客知识库技术方案

## 1. 建设目标

建设一套可长期运行、可横向扩展、可审计、可回放的技术博客知识库平台。首期接入约 1000 个技术博客、工程博客、技术文档站和公开 Newsletter，后续扩展到数千站点时，普通站点主要通过配置接入，而不是持续编写专用爬虫。

平台需要同时满足：

- 尽可能发现并抓取每个站点的完整历史文章；
- 持续发现新增文章、文章更新、URL 迁移和删除；
- 将正文稳定清洗为 Markdown，适合全文检索、RAG 和离线导出；
- 保留原始响应、浏览器渲染结果、Canonical IR、内容版本和完整数据血缘；
- 新增站点通过 Probe、Source Profile、Discovery Recipe、Extraction Recipe 和 Web 审核完成；
- 支持 Web 管理、调度、失败重试、Coverage、质量审核、成本治理、版本管理、搜索/RAG 和导出；
- Worker、Redis、Browser 或具体 crawler 故障后可以恢复；
- 任意一篇 Markdown 都能追溯到 Source、URL、Snapshot/Render Artifact、Extractor、IR、Version 和 Release。

### 1.1 核心 SLO

平台运营不以“HTTP 成功率”作为唯一指标，而以以下 SLO 为主：

- **Coverage**：历史 URL 覆盖率、Provider 穷尽证据、Known Gap；
- **Freshness**：新文章发现延迟、已知文章复检延迟；
- **Quality**：正文完整率、噪声率、结构质量、Quarantine 比例；
- **Data Supply**：URL 从发现到 Quality PASS 且 Markdown/索引可用的 `time_to_usable_data`；
- **Reliability**：任务成功率、重试率、DLQ、队列年龄、Worker 恢复时间；
- **Cost**：每 Source 成本、每 1000 个有效文档成本、每个 accepted version 成本；
- **Traceability**：任意结果均可追溯到不可变事实和版本化 Release。

### 1.2 非目标

- 不默认绕过登录墙、付费墙、CAPTCHA、WAF 或明确访问控制；
- 不用 LLM/Agent 的主观判断代替 Coverage 证明；
- 不把 Redis、crawler cache、Browser session、Deep Crawl frontier 或 Agent memory 当业务真相；
- 不允许外部用户直接向 Browser/crawler 透传任意 JavaScript、Hook、代理或系统参数；
- 不把 LLM 摘要、重写、翻译作为 Canonical Markdown；
- 不把标题、slug、展示 URL 作为跨阶段唯一主键；
- 不因为 URL 已见过就永久停止检查；
- 不把一次 HTTP 200 或 crawler `success=true` 当作内容质量成功。

---

## 2. 核心设计原则

1. **Coverage First**：先证明有哪些历史文章，再批量抓正文。
2. **Authoritative First**：CMS API、Sitemap、Feed、Archive 优先，Common Crawl、动态列表和 Deep Crawl 只补洞。
3. **Discovery Before Fetch**：URL 发现、规范化、Scope、去重先于正文抓取。
4. **Discover Cheap, Fetch Expensive**：发现阶段优先低成本 Provider，Browser 只在必要时使用。
5. **HTTP First, Browser Last**：正文静态 HTTP 优先，确定性 Browser fallback，Agent Browser 极少使用。
6. **Deterministic Before Agentic**：主链不依赖 LLM 决策也能完成回填和增量。
7. **Snapshot First**：先保存不可变抓取事实，再 Extract、IR、Markdown。
8. **IR First, Markdown as Projection**：Canonical IR 是内容事实，Markdown 是可重建投影。
9. **Quality Is First-Class**：抓取成功与内容合格是两种状态，必须有 Quality Gate。
10. **Cost Is First-Class**：CPU、Browser、网络、存储、代理、LLM 都需要归因。
11. **Tool Contract, Not Framework Coupling**：Crawl4AI、Playwright、HTTP 客户端通过 Adapter 接入。
12. **Recipe, Not Site Code**：selector、wait、scroll、分页、virtual scroll、iframe 等用版本化配置表达。
13. **Stable Identity**：Source、URL、Snapshot、Document、Version 使用不可变 ID 关联。
14. **Idempotent + Recoverable**：Task 有幂等键、lease、fencing token、checkpoint、retry 和 DLQ。
15. **Source Friendly**：robots、Retry-After、per-domain rate/concurrency、指数退避优先。
16. **Fair by Default**：大站 Backfill 不能长期饿死小站和增量任务。
17. **Detect Drift Online**：Golden Corpus 防平台升级回归，在线 Drift Detector 防源站改版静默退化。
18. **Security on Every Hop**：起始 URL、redirect、iframe、Asset URL、Browser navigation 每跳校验。
19. **Release Everything That Changes Results**：Profile、Recipe、Runtime、Extractor、Schema、Renderer 全版本化。
20. **Projection Is Rebuildable**：Markdown、全文索引、向量和派生结果均能从事实层重建。

---

## 3. 总体架构

```text
Web Admin / REST API / CLI / MCP
                |
        API + RBAC + Idempotency
                |
        Command / Query Layer
                |
        PostgreSQL Truth Store
                |
        Transactional Outbox
                |
           Redis Streams
                |
      Global Admission / Fair Scheduler
                |
+---------------+----------------+----------------+----------------+
|               |                |                |                |
Discovery       HTTP Fetch       Browser Fetch    Extract/Quality  Asset/Index
Workers         Workers          Workers          Workers          Workers
|               |                |                |                |
CMS/Sitemap     httpx/aiohttp    Crawl4AI         Canonical IR     S3/MinIO
Feed/Archive    conditional GET  Playwright       Quality Gate    OpenSearch
URL Seeder      raw snapshot     Recipe Engine    Drift Detect    pgvector
Prefetch Map                     Dynamic Listing  Markdown        Export
Deep Crawl                       Virtual Scroll   Quarantine
|               |                |                |                |
+---------------+----------------+----------------+----------------+
                         |
          Snapshot / Render Artifact / Trace
                         |
                     Canonical IR
                         |
                   Document Version
                         |
        Markdown / Search / RAG / Export
```

### 3.1 真相边界

```text
PostgreSQL + Object Storage = 业务真相
URL Observation            = URL 发现事实
Coverage Evidence           = 历史覆盖事实
Snapshot                    = HTTP 抓取事实
Render Artifact             = Browser 渲染事实
Canonical IR                = 内容结构事实
Document Version            = 内容版本事实
Quality Result              = 内容质量事实
Drift Observation           = 在线模板/内容分布变化事实
Cost Event                  = 执行成本事实
Redis Streams               = 运输层
Redis limiter/semaphore     = 短期 Admission 加速层
Crawler cache/frontier      = 执行优化，不是业务真相
Markdown/Search/Vector      = 可重建投影
```

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST、内部 Command API、Schema 校验；
- **PostgreSQL**：Source、Profile、Run、Task、Coverage、Observation、Version、Quality、Drift、Cost、Release、Audit、Outbox；
- **Redis Streams**：队列运输、consumer group、backpressure；
- **Redis Lua token bucket/semaphore**：分布式 Domain Admission 快速闸门；
- **React/Next.js + TypeScript**：Web 管理台；
- **Prometheus + Grafana + OpenTelemetry**：指标、日志、Trace；
- **S3/MinIO**：Raw HTML、Rendered DOM/MHTML、Canonical IR、附件和导出；
- **OpenSearch**：全文/BM25；
- **pgvector**：首期向量检索，通过 Adapter 可替换；
- **Docker Compose**：本地开发、小规模验证；
- **Kubernetes**：生产横向扩展、独立 Worker Pool、HPA。

### 4.2 抓取与解析

- **httpx/aiohttp**：HTTP、Feed、Sitemap、CMS API、Conditional GET、媒体下载；
- **Crawl4AI**：确定性 Browser/render、批量执行、URL Seeder、轻量站点映射、动态列表和受控 Deep Crawl；
- **Playwright**：复杂但稳定的交互 Recipe；
- **lxml + trafilatura/readability 类算法**：默认正文抽取；
- **JSON-LD/OpenGraph/Microdata**：元数据优先；
- **PyMuPDF/pdftotext**：PDF；扫描件才走 OCR。

### 4.3 Crawl4AI 的定位

Crawl4AI 只作为 `CrawlerAdapter / RenderAdapter / DiscoveryAdapter`，不成为平台状态机。

生产锁定经过 Golden Corpus 验证的 Crawl4AI、Chromium、Playwright 和镜像版本；升级必须经过 Benchmark、回归和 Canary。

Crawl4AI 的 Dispatcher、RateLimiter、Browser session 负责单执行器内部资源管理；平台 Task、Coverage、Global Domain Admission、Source Fairness、Retry 和 DLQ 仍由控制面管理。

---

## 5. 核心领域模型

### 5.1 Source 与 Source Profile

```text
source
- source_id UUID PK
- name
- base_url
- status: PROBING | ACTIVE | PAUSED | DEGRADED | DISABLED
- source_type
- timezone
- created_at / updated_at

source_profile
- profile_id UUID PK
- source_id
- version
- allowed_hosts[]
- include_paths[]
- exclude_paths[]
- discovery_policy JSONB
- fetch_policy JSONB
- extraction_policy JSONB
- asset_policy JSONB
- incremental_policy JSONB
- quality_policy JSONB
- budget_policy JSONB
- status: DRAFT | TESTED | ACTIVE | RETIRED
- created_at
```

示例：

```yaml
source:
  base_url: https://example.com
  allowed_hosts: [example.com, www.example.com]
  include_paths: [/blog/, /posts/]
  exclude_paths: [/tag/, /author/, /search/, /login/]

discovery:
  providers:
    - {type: cms_api, priority: 10}
    - {type: sitemap, priority: 20}
    - {type: feed, priority: 30}
    - {type: archive, priority: 40}
    - {type: common_crawl, priority: 80, mode: gap_only}
    - {type: prefetch_map, priority: 85, mode: gap_only, max_depth: 2, max_pages: 1000}
    - {type: dynamic_listing, priority: 87, mode: gap_only, recipe: dynamic_blog_list_v3}
    - {type: deep_crawl, priority: 90, mode: gap_only, max_depth: 3, max_pages: 5000}

incremental:
  discover_interval: 2h
  overlap_seconds: 86400
  verify_known_urls: true
  deletion_grace_runs: 3

fetch:
  mode: AUTO
  requests_per_second: 0.5
  max_concurrency: 2
  browser_fallback: true
  agent_fallback: false

quality:
  min_overall_score: 0.82
  quarantine_on_large_shrink: true
  template_drift_threshold: 0.75

budget:
  max_browser_seconds_per_day: 3600
  max_llm_calls_per_day: 20
```

### 5.2 URL Identity 与 Observation

URL 身份与 URL 发现事实分离。

```text
url_identity
- url_id
- source_id
- normalized_url
- normalized_url_hash
- first_seen_at
- last_seen_at
- state

url_observation
- observation_id
- url_id
- provider_type
- provider_run_id
- observed_url
- discovered_at
- lastmod
- published_hint
- title_hint
- parent_url
- discovery_round nullable
- evidence JSONB
```

所有 Provider 发现的 URL 先写 Observation，再统一 normalize、scope、filter、dedupe。

### 5.3 Run / Task

```text
run
- run_id
- source_id
- type: PROBE | BACKFILL | INCREMENTAL | VERIFY | REPROCESS
- profile_id
- status
- started_at / finished_at

crawl_task
- task_id
- run_id
- source_id
- url_id nullable
- task_type
- idempotency_key UNIQUE
- priority
- state
- attempt
- max_attempts
- lease_owner
- lease_until
- fencing_token
- not_before
- checkpoint JSONB
- error_code
- error_detail
```

幂等键示例：

```text
BACKFILL_FETCH:{url_id}:{profile_release}
VERIFY:{url_id}:{time_bucket}
DYNAMIC_DISCOVERY:{source_id}:{recipe_release}:{time_bucket}
EXTRACT:{snapshot_id}:{extractor_release}
MARKDOWN:{document_version_id}:{renderer_release}
```

### 5.4 Snapshot / Render Artifact

```text
snapshot
- snapshot_id
- task_id
- url_id
- purpose: CONTENT | DISCOVERY_PREFETCH | VERIFY
- requested_url
- final_url
- status_code
- response_headers JSONB
- etag
- last_modified
- content_type
- body_sha256
- object_key
- fetched_at
- runtime_release_id

render_artifact
- render_id
- task_id
- url_id nullable
- start_url
- final_url
- dom_object_key
- mhtml_object_key nullable
- screenshot_object_key nullable
- network_summary JSONB
- recipe_release_id
- runtime_release_id
- rendered_at
```

Raw HTML/DOM 使用 gzip/zstd 压缩存对象存储，数据库只保存 Hash、索引和关键 Header。Discovery 阶段已经获取且满足 freshness/完整性条件的 HTML 可以复用为内容 Snapshot，避免无意义二次抓取。

### 5.5 Document / Version / Canonical IR

```text
document
- document_id
- source_id
- canonical_url_id
- first_published_at
- current_version_id
- state

document_version
- version_id
- document_id
- source_artifact_type
- source_artifact_id
- extractor_release_id
- ir_schema_version
- ir_object_key
- semantic_content_hash
- created_at
```

只有 Canonical IR 发生实质变化时创建新版本，广告、追踪参数、导航变化不应产生正文版本。

### 5.6 Quality / Drift / Cost

```text
document_quality
- quality_id
- version_id
- content_length
- text_density
- boilerplate_ratio
- link_density
- metadata_confidence
- content_completeness_score
- structure_score
- overall_score
- gate_result: PASS | QUARANTINE | FAIL
- reasons JSONB
- quality_release_id

source_drift_observation
- drift_id
- source_id
- profile_id
- layout_fingerprint
- article_container_fingerprint
- listing_selector_hit_rate
- listing_unique_url_yield
- content_length_zscore
- fallback_ratio_window
- quality_pass_ratio_window
- drift_score
- reasons JSONB
- detected_at

cost_event
- cost_event_id
- run_id
- task_id
- source_id
- executor_type
- cpu_ms
- memory_peak_mb
- browser_ms
- network_in_bytes
- network_out_bytes
- storage_bytes
- proxy_bytes
- llm_input_tokens
- llm_output_tokens
- external_request_cost
- estimated_infra_cost
- created_at
```

核心成本指标：

```text
cost_per_accepted_version = 总执行成本 / Quality PASS 的新 DocumentVersion 数
```

---

## 6. 新站点接入

新增站点先 Probe，再进入生产。

### 6.1 自动 Probe

1. URL、DNS、Scope、robots 校验；
2. 检测 Sitemap/Sitemap Index、RSS/Atom；
3. 检测 WordPress、Ghost、Substack 等常见 CMS/API；
4. 抽取首页、Archive、Category 的链接模式；
5. 随机选 5~10 篇文章样本；
6. 比较 HTTP、默认 Extractor、Crawl4AI Browser、Recipe 的正文质量；
7. 检测 JSON-LD、作者、发布时间、代码块、图片；
8. 估算历史 URL 数量；
9. 权威发现不足时执行受限 Prefetch Mapping；
10. 如果列表依赖 JavaScript，识别 append scroll、Load More、pagination 或 virtual scroll，生成 Dynamic Listing Recipe 候选；
11. 估算 Browser 比例、历史回填成本和日常增量成本；
12. 生成 DRAFT Source Profile。

### 6.2 Web 审核

管理员看到：

- Discovery Providers 和优先级；
- URL 样本、include/exclude/path 分类；
- HTTP/Browser 对比；
- Dynamic Listing 每轮 URL 增长和收敛原因；
- Markdown 预览；
- Quality 分数；
- robots；
- 预计文章数、已发现数、Known Gap；
- 预计回填成本；
- 风险项。

审核通过后发布 `TESTED -> ACTIVE` Profile。

### 6.3 可扩展性目标

普通站点接入路径：

```text
输入域名
 -> Probe
 -> 自动生成 Source Profile/Recipe
 -> 样本验收
 -> 发布
 -> Backfill
 -> Incremental
```

只有登录态、极端 JS 或非标准数据源才新增 Adapter 代码。

---

## 7. 历史全量发现与 Coverage

“抓完整历史”最难的是证明 Coverage，而不是提高并发。

### 7.1 Provider 优先级

```text
1. CMS API / 官方内容 API
2. Sitemap / Sitemap Index
3. RSS / Atom
4. Archive / Pagination / Year-Month Index
5. Tag/Category Index（补充）
6. URL Seeder / 可选公开索引
7. Prefetch Mapping
8. Dynamic Listing Discovery
9. bounded Deep Crawl
10. 人工补充/导入
```

顺序可按站点实际成本调整，但 Browser Provider 默认不能压过可用的权威数据源。

### 7.2 Coverage Evidence

```text
coverage_evidence
- evidence_id
- source_id
- provider_type
- run_id
- cursor_start
- cursor_end
- discovered_count
- unique_article_count
- min_published_at
- max_published_at
- exhausted boolean
- exhaustion_reason
- expected_count nullable
- known_gap JSONB
- evidence_object_key nullable
```

Backfill 完成不能以“队列空了”为标准，而要满足：

- 高优先级 Provider 已穷尽或明确不可用；
- Sitemap/CMS cursor 已保存；
- Archive 到达明确分页/年份边界；
- Provider 交叉后的新增 URL 收敛；
- Dynamic Listing 达到可解释的收敛边界；
- Prefetch/Deep Crawl 只用于补洞，并记录 max_pages/max_depth/time budget 截止原因；
- Known Gap 可见且可审计。

### 7.3 URL 规范化

统一执行：

- scheme/host 小写；
- 默认端口清理；
- fragment 移除；
- tracking 参数移除；
- query 参数按 Source allowlist 保留；
- trailing slash 规则由 Source Profile 决定；
- Canonical Link 只作为证据，不无条件信任；
- redirect 后 final URL 再做 Scope/SSRF；
- 保留原始 `observed_url` 和规范化 URL。

### 7.4 Prefetch Mapping

```text
Phase A: Map
seed URLs
 -> cheap HTML + links
 -> normalize/scope/filter
 -> URL Observation + parent edge
 -> page/path classifier
 -> 估算站点空间

Phase B: Process
article candidates
 -> 正式 Fetch/Browser
 -> Snapshot/Render
 -> Extract/IR/Quality
```

Prefetch 必须配置 `max_depth / max_pages / time_budget`，并对日历、搜索、tag、query 无限空间设置 guard。

---

## 8. Dynamic Listing Discovery

JavaScript 栏目页、Infinite Scroll、Load More、虚拟列表必须作为独立 Discovery Provider，而不是散落在正文 Browser 抓取中的脚本。

### 8.1 Recipe

```yaml
name: dynamic_blog_list_v3
start_urls:
  - https://example.com/blog
ready_selector: ".information-flow-list"
list_item_selector: ".information-flow-list article"
link_selector: "a[href]"
interaction_mode: APPEND_SCROLL
max_rounds: 40
max_runtime_seconds: 120
max_items: 5000
no_growth_rounds: 3
wait_after_action_ms: 800
extract:
  url: "a::attr(href)"
  title: "a::text"
  published_hint: "time::attr(datetime)"
```

支持：

```text
STATIC
PAGINATION
CLICK_MORE
APPEND_SCROLL
VIRTUAL_SCROLL
```

### 8.2 执行策略

- `STATIC`：HTTP 或普通 Browser 解析；
- `PAGINATION`：提取 next href，重新进入 URL pipeline；
- `CLICK_MORE`：受控 click + 等待 item 数增长；
- `APPEND_SCROLL`：全页扫描或受控多轮滚动；
- `VIRTUAL_SCROLL`：使用虚拟滚动能力或逐轮 harvest，防止旧节点被 DOM 替换后丢失。

平台 Recipe 不能直接等同于 Crawl4AI 参数。Adapter 将声明式 Recipe 映射到当前版本支持的 `CrawlerRunConfig`、session/js-only、多步交互、全页扫描或 Virtual Scroll 配置。

### 8.3 文章身份抽取

Discovery 的核心字段是实际链接，不使用图片 `alt`、展示标题或图片 URL 作为文章主键。

最小输出：

```json
{
  "observed_url": "https://example.com/post/123",
  "title_hint": "文章标题",
  "published_hint": "2026-08-01T10:00:00Z",
  "parent_url": "https://example.com/blog",
  "round": 7,
  "provider": "DYNAMIC_LISTING"
}
```

优先使用结构化 CSS 提取真实 `<a href>`；selector 不稳定时可以先获取内部链接，再经 Source Scope、路径分类器和规则过滤。

### 8.4 收敛条件

“列表容器出现”或“一次滚到底”不代表历史发现完成。停止条件基于规范化文章 URL 的增长：

```python
seen = set()
no_growth = 0

for round_no in range(max_rounds):
    candidates = harvest_current_dom()
    urls = normalize_scope_filter(candidates)
    new_urls = urls - seen
    seen |= new_urls
    persist_observations(new_urls)

    if incremental_overlap_reached(new_urls):
        stop("OVERLAP_BOUNDARY")
        break

    no_growth = no_growth + 1 if not new_urls else 0
    if no_growth >= no_growth_rounds:
        stop("NO_GROWTH")
        break

    perform_next_action()
    wait_for_growth_or_timeout()
else:
    stop("MAX_ROUNDS")
```

动态列表 Coverage Evidence 保存：

- recipe release；
- interaction mode；
- rounds；
- observed item 数；
- unique article URL 数；
- 每轮新增数；
- duplicate ratio；
- selector hit rate；
- first/last published hint；
- exhausted；
- exhaustion reason；
- runtime；
- Render Artifact ID。

### 8.5 安全边界

Web 管理台只允许声明式动作：

```text
WAIT_SELECTOR
CLICK_SELECTOR
SCROLL_PAGE
SCROLL_CONTAINER
WAIT_ITEM_GROWTH
EXTRACT_LINKS
```

不允许任意 JavaScript 直接进入生产 Browser。每次导航、redirect、iframe 和提取出的 href 都重新进行 Scope/SSRF 校验。

---

## 9. 抓取执行与最小成本路由

### 9.1 Acquisition Planner

Planner 根据 Source Profile、历史成功率、质量和成本生成受约束 FetchPlan：

```text
L0 CMS/API
L1 HTTP
L2 Crawl4AI deterministic Browser
L3 Playwright deterministic Recipe
L4 LLM-assisted extraction / Controlled Agent Browser
```

优化目标：

```text
minimize expected_cost / expected_accepted_document_version
subject to Quality SLO and Freshness SLO
```

执行结果可以生成策略建议，但自动建议与生产发布分离，策略变化必须版本化并可回滚。

### 9.2 HTTP Worker

默认：

- `If-None-Match`；
- `If-Modified-Since`；
- gzip/br；
- 分阶段 timeout；
- redirect 上限；
- per-domain connection pool；
- `Retry-After`；
- 429/503 指数退避 + jitter；
- body size 上限；
- MIME 白名单。

### 9.3 Browser Worker

Browser Worker：

- Browser 进程复用；
- Context 按 Source/安全域隔离；
- Page 按 Task 短生命周期；
- 默认阻断广告、追踪、字体、视频等非必要资源；
- 默认不截图、不生成 PDF；
- 只有 Asset Policy 要求时下载正文图片；
- 所有 wait/scroll/click 进入版本化 Recipe；
- 单 Worker 可使用 crawler 自带资源保护，但全局调度仍由平台控制。

建议拆成两个池：

```text
Browser Discovery Worker
- 长页面、多轮交互、低并发、强预算

Browser Content Worker
- 单文章、短生命周期、正文完整度优先
```

### 9.4 Agent Browser

默认禁用。只有以下条件才允许：

- 站点价值高；
- HTTP/Crawl4AI/确定性 Recipe 全失败；
- 无登录绕过、无 CAPTCHA 绕过；
- 有明确预算；
- 动作可审计且 URL 受 Scope 约束。

Agent 只能产出候选 Artifact 或 Recipe 建议，不能直接覆盖 Canonical 数据。

---

## 10. 正文抽取、Canonical IR 与 Markdown

### 10.1 元数据抽取顺序

```text
结构化官方 API
> JSON-LD / Microdata
> OpenGraph / meta
> 稳定 selector
> DOM heuristic
> LLM fallback
```

每个字段保存 `value + source + confidence`。

### 10.2 Extractor Pipeline

```text
Snapshot/Render
   |
DOM normalize
   |
remove known chrome
   |
article candidates
   |
trafilatura/readability/lxml
   |
block normalization
   |
Canonical IR
   |
Quality Gate
   |
PASS -> DocumentVersion
QUARANTINE -> Web Review / fallback extractor
FAIL -> retry/fallback
```

### 10.3 Canonical IR

IR 至少表达：

- heading；
- paragraph；
- list；
- blockquote；
- code block + language；
- table；
- image/asset reference；
- link；
- horizontal rule；
- math 可选；
- embedded media placeholder。

### 10.4 Canonical Markdown

Markdown 从 IR 确定性生成：

```markdown
---
document_id: doc_xxx
version_id: ver_xxx
source_id: src_xxx
source_url: https://example.com/post
canonical_url: https://example.com/post
published_at: 2026-01-01T10:00:00Z
author: Jane Doe
language: zh-CN
content_hash: sha256:...
---

# 标题

正文……
```

Front Matter 不存易变化的摘要、向量或模型输出。

### 10.5 导出路径

```text
export/
  {source_slug}/
    {yyyy}/
      {document_id}.md
```

另生成：

```text
manifest.jsonl
source_url -> document_id -> current_version_id -> md_path
```

标题变化不会造成文件身份漂移。

---

## 11. Quality Gate 与漂移检测

### 11.1 质量指标

- 正文长度；
- 文本密度；
- Boilerplate 比例；
- Link density；
- 标题一致性；
- 作者/时间置信度；
- 代码块完整率；
- 图片/alt 保留情况；
- 结构完整度；
- 与历史版本长度/结构的异常变化。

示例 Gate：

```text
PASS
- overall_score >= 0.82
- 正文长度达到 Source 基线
- 无登录页/Cookie 页面特征

QUARANTINE
- 新版本正文比上一版本突然减少 > 70%
- title 与正文主标题冲突
- Browser 页面只有 skeleton
- 大量导航文本

FAIL
- 无有效正文
- 非文章页面
- 被登录/CAPTCHA/错误页替代
```

只有 PASS Version 成为默认 current version。

### 11.2 Golden Corpus

每个 Source 保留 3~10 个代表页面，覆盖普通文章、长文、代码密集、图片密集、旧模板和新模板。

Runtime、Extractor、Recipe、Renderer 升级时自动比较：

- 正文覆盖率；
- 噪声率；
- metadata 准确率；
- 代码块完整率；
- Markdown diff；
- Quality Gate 结果。

### 11.3 在线 Drift Detector

滚动基线包括：

```text
layout_fingerprint
article_container_fingerprint
listing_selector_hit_rate
listing_unique_url_yield
listing_duplicate_ratio
content_length_p50/p95
boilerplate_ratio_p50
link_density_p50
http_to_browser_fallback_ratio
quality_pass_ratio
```

典型漂移信号：

- selector 命中率群体性下降；
- Dynamic Listing 每轮唯一 URL 产出骤降；
- HTTP Quality 降低同时 Browser fallback 暴涨；
- 多篇页面出现相同 Cookie/Login/Challenge 内容；
- Sitemap/Archive/文章路径模式突然变化。

处置：

```text
Drift Alert
 -> Source DEGRADED / 保护性降速
 -> 最近样本 + Golden Corpus 回放
 -> 生成 Profile/Recipe 候选
 -> Compare Coverage/Quality/Cost
 -> 审批
 -> 新 Release
```

---

## 12. 增量同步

增量同步拆成“发现增量”和“内容增量”。

### 12.1 发现增量

优先：

- RSS/Atom；
- CMS cursor；
- Sitemap `lastmod`；
- Archive 最近分页；
- Dynamic Listing 顶部窗口；
- 低频 Prefetch/Deep Crawl 补漏。

使用 overlap window，例如每次从上次 watermark 往前回退 24 小时，再通过 URL 去重吸收重复。

### 12.2 Dynamic Listing 增量

动态列表通常按时间倒序：

```text
打开顶部
 -> harvest 第一批 URL
 -> normalize/dedupe
 -> 滚动/Load More
 -> 遇到 overlap 时间边界
    或连续 K 个已知 URL + N 轮无 unseen URL
 -> 停止
```

Browser session 不作为 checkpoint。业务 checkpoint 写 PostgreSQL：watermark、最近 Observation、Provider cursor、Recipe Release 和 Coverage Evidence。

### 12.3 已知 URL 复检

```text
Conditional GET
 -> 304: 更新 check fact，不建 Version
 -> 200: 新 Snapshot
 -> Extract Canonical IR
 -> semantic hash 未变: 不建 Version
 -> hash 变化: Quality Gate
 -> PASS: 新 DocumentVersion
```

### 12.4 自适应刷新

```text
HOT    15~60 分钟发现增量
WARM   2~6 小时
COOL   12~24 小时
COLD   2~7 天
```

文章复检频率根据文章年龄、历史修改概率和站点更新模式逐步衰减。

### 12.5 删除与迁移

```text
ACTIVE
SUSPECT_MISSING
MISSING_CONFIRMED
MOVED
BLOCKED
```

- 一次 404 不立即删除；
- 410 提高删除置信度；
- 连续 N 次缺失后确认；
- 301/308 建 URL Alias/Move Edge；
- Canonical 变化保留证据；
- 历史 Version 不因源站删除而物理删除。

---

## 13. 队列、调度与恢复

### 13.1 Redis Streams 只做 Transport

```text
discovery
fetch-http
fetch-browser-discovery
fetch-browser-content
extract
quality
asset
index
reprocess
```

DB 中 Task 才是业务状态。

### 13.2 Transactional Outbox

在同一个 PostgreSQL 事务中完成：

```text
更新业务状态
+ 插入 outbox_event
```

Outbox Relay 再发布到 Redis Streams，避免“DB 已提交但消息丢失”或“消息已发但 DB 未提交”。

### 13.3 Lease + Fencing Token

Worker 领取任务时获得：

```text
lease_owner
lease_until
fencing_token
```

执行前/写结果时校验 token，防止超时 Worker 在新 Worker 已接管后继续写旧结果。

### 13.4 Retry / DLQ

错误分：

```text
TRANSIENT
- timeout
- 429
- 503
- browser crash

POLICY
- robots denied
- scope rejected
- budget exceeded

CONTENT
- no article body
- login page
- template drift

PERMANENT
- unsupported MIME
- confirmed removed
```

只有 TRANSIENT 自动指数重试；CONTENT 优先 fallback/Quarantine；POLICY 等待配置或人工处理。

### 13.5 Global Domain Admission

所有 HTTP 和 Browser 请求先经过：

```text
Global per-domain token bucket
+ Global per-domain semaphore
+ Source fairness
+ Priority
+ Budget
```

Redis 可以做高速令牌闸门，PostgreSQL 保存策略和审计事实。

### 13.6 Fair Scheduler

队列优先级建议：

```text
P0 用户手工重试/生产事故
P1 Incremental HOT
P2 Incremental WARM
P3 Verify
P4 Backfill
P5 Deep Crawl / gap repair
P6 Reprocess / projection rebuild
```

再使用 Source 级 Weighted Fair Queue，避免单个超大站独占 Worker。

---

## 14. Web 管理功能

### 14.1 Source 管理

- 新增/编辑/暂停 Source；
- Probe；
- Profile/Recipe 版本；
- robots、Scope、预算；
- 发布/回滚 Release。

### 14.2 Coverage 页面

- Provider 状态；
- 每个 Provider 发现数；
- Provider 集合交叉关系；
- 历史时间分布；
- Dynamic Listing 每轮新增 URL 曲线；
- exhaustion reason；
- Known Gap；
- Backfill 完成度。

### 14.3 抓取与质量页面

- Run/Task 状态；
- 当前队列年龄；
- HTTP/Browser route；
- Snapshot/Render 回放；
- 原文与 Markdown 对照；
- Quality Score/Reasons；
- Quarantine 审核；
- 手工 retry/reprocess。

### 14.4 Recipe 编辑器

提供声明式配置，不开放任意 JS：

- ready selector；
- item/link selector；
- click/scroll/virtual-scroll；
- max rounds/runtime/items；
- wait condition；
- extraction schema；
- 样本测试；
- URL 预览；
- 对比旧 Recipe 的 Coverage/Quality/Cost。

### 14.5 运营页面

- Freshness；
- Quality；
- Drift；
- Cost；
- Browser 利用率；
- 429/503；
- DLQ；
- 每 Source 成本与文档产出。

---

## 15. 安全设计

### 15.1 SSRF / Egress

每次请求和导航校验：

- 只允许 HTTP/HTTPS；
- DNS 解析后阻止 loopback/private/link-local/metadata 地址；
- redirect 每跳重验；
- Browser navigation/iframe/new tab 每跳重验；
- Asset URL 重验；
- allowed_hosts + path scope；
- 最大 redirect、URL 长度、body size。

### 15.2 Recipe 安全

- 不接受任意 JavaScript；
- selector 和动作白名单；
- 最大动作数、运行时、滚动轮数；
- 禁止跨 Scope 导航；
- Browser Context 隔离；
- Cookie/凭据默认不持久化；
- 所有 Recipe 变更有审计和 Release。

### 15.3 权限

RBAC：

```text
Viewer
Operator
Reviewer
Admin
```

高风险操作：修改 Scope、启用代理、提高预算、启用 Agent、删除数据、发布 Recipe，必须有更高权限和 Audit Log。

---

## 16. 搜索、RAG 与导出

知识库正文进入：

```text
Canonical IR
 -> Markdown projection
 -> OpenSearch BM25
 -> chunk pipeline
 -> embedding
 -> pgvector
```

Chunk 记录：

- `document_id`；
- `version_id`；
- heading path；
- char/token range；
- source URL；
- content hash；
- embedding model release。

当正文产生新 Version 时，只重建受影响的 chunk 和向量。旧 Version 可以保留用于审计或时间旅行检索。

导出支持：

- 全量 Markdown；
- 按 Source/时间筛选；
- manifest；
- JSONL；
- 对象存储快照；
- 指定 Version 的可重复导出。

---

## 17. 可观测性与告警

### 17.1 指标

```text
crawl_requests_total
crawl_request_latency
provider_discovered_urls
provider_unique_article_urls
listing_unique_url_yield
listing_no_growth_rounds
coverage_gap_count
freshness_lag_seconds
quality_pass_ratio
quarantine_ratio
browser_fallback_ratio
browser_seconds
queue_age_seconds
retry_total
dlq_total
cost_per_accepted_version
```

### 17.2 Trace

每个 Run/Task 使用统一 Trace ID 串起：

```text
API -> Scheduler -> Worker -> HTTP/Browser -> Extract -> Quality -> Index
```

### 17.3 告警

- HOT Source Freshness 超 SLO；
- Quality PASS 比例突降；
- Dynamic Listing selector hit/URL yield 突降；
- Browser fallback 暴涨；
- 429/503 增长；
- 队列年龄持续增长；
- DLQ 堆积；
- Browser/LLM 日预算接近上限；
- Coverage Provider 异常减少。

---

## 18. 部署与容量规划

### 18.1 Worker Pool 独立扩缩容

```text
Discovery HTTP Pool
HTTP Content Pool
Browser Discovery Pool
Browser Content Pool
Extract/Quality Pool
Asset Pool
Index/Embedding Pool
```

Browser Pool 以 CPU/内存/Browser session 数扩容；HTTP Pool 以队列年龄和网络吞吐扩容；Extract/Embedding 按 CPU/GPU 或外部 API 配额扩容。

### 18.2 初始容量策略

1000 个站点不应预设“每站点一个爬虫进程”。应采用任务化共享 Worker：

- 大多数增量 discovery 走 Sitemap/Feed/CMS，成本很低；
- 大多数正文走 HTTP；
- Browser 只覆盖 JS 必需页面；
- Backfill 低优先级填满剩余容量；
- 增量任务拥有优先级保障。

容量模型按实测参数计算：

```text
日请求量
= Source 数 × 平均 discovery 请求
+ 已知 URL verify 请求
+ 新文章正文请求
+ Backfill 预算

Browser 秒数
= Browser Discovery 秒数
+ Browser Content 秒数
```

禁止依据第三方 benchmark 直接制定生产 SLO，必须用自己的 50~100 个代表站 Benchmark。

### 18.3 存储生命周期

- Raw Snapshot/Render Artifact：热存近期，旧数据转低成本对象存储；
- Canonical IR/Version metadata：长期保留；
- Markdown/Index：随时可重建；
- screenshot 默认不保存，只在失败诊断/Golden Corpus/人工审核时保留。

---

## 19. 实施阶段

### Phase 1：核心事实链与 50 站验证

实现：

- Source/Profile；
- PostgreSQL + Outbox + Redis Streams；
- Sitemap/Feed/CMS/Archive；
- HTTP Fetch；
- Snapshot；
- Extract/IR/Markdown；
- Quality Gate；
- Web Source/Run/Document 基础页面。

验收：50 个不同技术站点能回填、增量、重启恢复。

### Phase 2：Browser 与动态列表

实现：

- Crawl4AI Adapter；
- Browser Content Worker；
- Browser Discovery Worker；
- Dynamic Listing Recipe；
- APPEND_SCROLL/CLICK_MORE/VIRTUAL_SCROLL；
- Coverage Evidence；
- Recipe Web 测试与回滚。

验收：JavaScript archive/feed 可稳定发现真实文章 URL，并有可解释收敛证据。

### Phase 3：1000 站扩展

实现：

- Probe 自动化；
- Global Domain Admission；
- Weighted Fair Scheduler；
- Budget/Cost；
- Drift Detector；
- Golden Corpus；
- OpenSearch/pgvector；
- Kubernetes HPA。

验收：1000 个站点增量不因历史 Backfill 堵塞，Freshness/Quality/Cost 可观测。

### Phase 4：高级能力

- Common Crawl/公开索引补洞；
- 受控 Deep Crawl；
- 高价值站点 Agent fallback；
- 自动 Recipe 建议；
- 多租户/团队权限；
- Time-travel RAG；
- 复杂导出与数据 API。

---

## 20. 验收标准

### 20.1 Coverage

- 每个 Source 至少有一种可解释的历史发现 Provider；
- Backfill 结束有 Coverage Evidence，而不是仅“队列为空”；
- 动态列表有 rounds、unique URL、no-growth/cursor exhaustion 记录；
- Known Gap 可见。

### 20.2 Incremental

- 新文章可以在 Source 对应 Freshness SLO 内发现；
- RSS/CMS/Sitemap/Archive/Dynamic Listing 可组合运行；
- Seen URL 会复检；
- 304 不创建 Version；
- 内容实质变化才创建 Version。

### 20.3 Quality

- Markdown 结构稳定；
- 代码块、标题、列表、链接、图片引用可重建；
- 抓取成功与 Quality PASS 分离；
- 异常页面进入 Quarantine，不污染 current version。

### 20.4 Reliability

- Redis 重启不丢业务状态；
- Worker 崩溃后 lease 到期可接管；
- 重复消息不产生重复 Version；
- Browser 崩溃不破坏业务 checkpoint；
- 所有阶段支持 DLQ/reprocess。

### 20.5 Extensibility

- 普通新站点无需写代码；
- Source Profile/Recipe 可 Web 配置、测试、发布、回滚；
- Crawl4AI/Playwright 更换版本不改变领域模型；
- 第 1001 个站点接入仍遵循 Probe -> Recipe/Profile -> 验收 -> 发布。

### 20.6 Security

- 所有网络跳转有 Scope/SSRF 校验；
- Web 不可执行任意 JavaScript；
- robots 和访问策略可审计；
- Agent 默认关闭；
- 高风险操作受 RBAC 和 Audit Log 保护。

---

## 21. 推荐最终形态

最终系统应形成三个稳定层次：

```text
事实层
PostgreSQL + Object Storage
URL Observation / Coverage / Snapshot / Render / IR / Version / Quality

执行层
Redis Streams + Fair Scheduler + Admission
HTTP / Browser Discovery / Browser Content / Extract / Index Workers

管理与消费层
Web Admin / API / Search / RAG / Markdown Export
```

对于 1000+ 博客，最重要的不是选择一个“万能爬虫”，而是把 **历史 Coverage、动态列表 URL 发现、低成本执行路由、Canonical 内容版本、增量同步、质量门禁、全局调度、可回放事实和 Web 配置** 做成平台能力。这样普通站点可以配置化接入，复杂站点也只是在 Provider/Recipe/Adapter 层局部扩展，不会把整个知识库演化成 1000 份无法维护的站点脚本。
