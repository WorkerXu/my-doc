# 博客知识库技术方案

## 1. 目标与范围

建设一个长期运行、可扩展的技术博客知识库平台：首期约 1000 个技术博客/工程博客/技术文档站，抓取尽可能完整的历史文章并清洗为稳定 Markdown；后续可以持续增加站点，并支持增量同步、历史版本、全文/向量检索、Web 管理、质量审计和离线重处理。

设计按百万级文档、数百万到千万级 URL、站点高度异构和多年运行考虑。核心目标不是“某个爬虫能抓页面”，而是形成可证明 Coverage、可恢复任务、可重放处理、可追溯内容版本和可替换抓取/检索组件的平台。

必须满足：

1. 新增普通站点尽量只配置 Source/Profile，不修改代码。
2. 支持 `FULL_BACKFILL`、`INCREMENTAL`、`RECONCILE`、`TARGETED_FETCH`、`REPROCESS`、`RECHUNK`、`REEMBED`、`REINDEX`。
3. 历史完整性由 Provider Coverage Evidence 证明，不能以“队列空”“达到 max_pages/max_depth”代替。
4. Feed/API/Sitemap 作为低成本发现和变化信号，HTTP-first，Browser-last。
5. 所有 fan-out 保留已经成功的部分结果；批次超时不能清空已完成事实。
6. 原始网络事实、抽取候选、Canonical IR、Accepted Version 不可变；规则升级优先离线 replay。
7. Markdown、Chunk、Embedding、全文索引、摘要等都是可重建 Projection，不是真相源。
8. Source Sync 与 Embedding/LLM/TTS/外部工作流解耦，下游失败不得阻塞源站同步。
9. Web 管理覆盖 Source、Coverage、Run/Task、抓取路由、质量、版本、检索和审计。
10. 不绕过登录、付费墙、验证码、WAF 或明确访问控制，统一执行 robots、限流、SSRF/Egress 策略。

## 2. 总体架构

```text
                         ┌──────────────────────────┐
                         │       Web / REST API     │
                         │ Source / Run / Search    │
                         └────────────┬─────────────┘
                                      │ Command
                         ┌────────────▼─────────────┐
                         │ PostgreSQL Truth Store   │
                         │ State / Metadata / Audit │
                         │ Outbox / Version / Run   │
                         └───────┬───────────┬──────┘
                                 │           │
                         Outbox  │           │ Projection Event
                                 ▼           ▼
                       ┌────────────────┐   ┌───────────────────┐
                       │ Redis Streams  │   │ Projection Workers│
                       │ Durable Bus    │   │ Search/Vector/MD  │
                       └───────┬────────┘   └─────────┬─────────┘
                               │                      │
             ┌─────────────────┼─────────────────┐    │
             ▼                 ▼                 ▼    ▼
      Discovery/Signal     HTTP Fetch       Browser Fetch   AI/Delivery
          Worker             Worker             Worker         Worker
             │                 │                 │
             └────────────┬────┴───────┬─────────┘
                          ▼            ▼
                    Extraction      Quality/ID
                          │            │
                          └──────┬─────┘
                                 ▼
                         ┌───────────────┐
                         │ S3 / MinIO    │
                         │ Snapshot / IR │
                         │ Assets / MD   │
                         └───────┬───────┘
                                 ▼
                       OpenSearch / Vector DB
```

### 2.1 真相边界

- **Coverage**：理论上应该有哪些历史文章。
- **Evidence**：某个 Provider 在什么时间观察到某个 URL。
- **Change Signal**：Feed GUID/updated、Sitemap lastmod、ETag、Last-Modified 等变化迹象。
- **Route Attempt**：某次 HTTP/Browser/平台 API 抓取尝试。
- **Snapshot**：源站当时实际返回的不可变内容。
- **Extraction Candidate**：不同 extractor 产生的正文候选。
- **Canonical IR / Document Version**：平台最终接受的内容事实。
- **Markdown / Chunk / Search / Embedding**：可从 Accepted Version 重建的投影。

PostgreSQL + 对象存储是事实真相源；Redis、OpenSearch、向量库、Markdown 文件都可删除重建。

## 3. 技术选型

### 3.1 控制面

- API：FastAPI + Pydantic/JSON Schema。
- PostgreSQL：Source、Run、Task、Coverage、Version、Budget、Audit、Outbox。
- Redis Streams：任务运输与 backpressure，不保存唯一业务状态。
- Transactional Outbox：数据库状态变化与消息发布同事务落库。
- Web：React/Vue + TypeScript；高风险操作走强类型 Command API。

### 3.2 数据面

- S3/MinIO：HTTP body、HTML、Rendered DOM、Feed payload、PDF、截图、Canonical IR、Markdown、Assets。
- HTTPX/aiohttp：静态 HTTP、Feed、Sitemap、API。
- Trafilatura/Readability/lxml/BeautifulSoup/CSS/XPath：确定性正文抽取。
- Crawl4AI/Playwright：动态页面和质量失败后的 Browser Adapter。
- OpenSearch：全文检索、过滤、聚合。
- pgvector/OpenSearch kNN 起步；规模扩大后可替换 Qdrant/Milvus。
- Prometheus + Grafana + OpenTelemetry：指标、日志、trace。

### 3.3 Worker Pool

生产至少拆分：

```text
api
scheduler
outbox-relay
discovery-worker
signal-worker
http-fetch-worker
browser-worker
extract-worker
quality-worker
identity-worker
projection-worker
asset-worker
embedding-worker
```

Browser Worker 独立容器/节点，生产优先 Linux，不与 API 进程共享 Playwright 事件循环。

## 4. 核心数据模型

### 4.1 Source / Profile

`source`

```text
id
name
entry_url
registrable_domain
allowed_origins
source_type
status
robots_policy
schedule_policy_id
active_profile_release_id
created_at / updated_at
```

`site_profile_release`

```text
id
source_id
version
scope_rules
provider_config
url_normalization_rules
fetch_route_policy
browser_recipe
extractor_policy
quality_policy
asset_policy
chunk_policy_id
created_by
created_at
```

被 Run 引用后的 Release 不可修改，只能创建新 Release。Feed 入口、Sitemap、API cursor、站点 selector、页尾裁剪、Browser wait 条件都放在 Profile Release，禁止散落硬编码。

### 4.2 Run / Task / Budget

`run`

```text
id
source_id
run_type
profile_release_id
status
started_at
finished_at
coverage_summary
success_summary
cost_summary
partial_failure_count
```

`task`

```text
id
run_id
stage
task_type
url_id
priority
state
attempt
lease_owner
lease_until
deadline_at
idempotency_key
parent_task_id
created_at / updated_at
```

`budget_reservation`

```text
id
scope_type / scope_id
resource_type
reserved_amount
consumed_amount
expires_at
state
idempotency_key
```

预算资源至少包含 request、response bytes、browser seconds、OCR pages、embedding tokens、LLM tokens 和外部交付调用。

### 4.3 Coverage / Evidence

`coverage_provider_run`

```text
id
run_id
provider_type
provider_release_id
cursor_before
cursor_after
status
exhaustion_reason
candidate_count
completed_count
failed_count
known_gap
blocker
```

`url_candidate`

```text
id
source_id
normalized_url
url_hash
first_seen_at
last_seen_at
state
```

`url_evidence`

```text
id
candidate_id
provider_run_id
parent_url
relation_type
provider_cursor
observed_at
```

同一 URL 可以有多条 Evidence，去重 URL 不得丢失发现来源。

### 4.4 Feed / Change Signal

`feed_item_observation`

```text
id
source_id
provider_run_id
feed_url
guid
entry_url
title
published_at
updated_at
author
categories
summary_hash
content_hash
payload_snapshot_id
observed_at
```

`change_signal_observation`

```text
id
source_id
url_id
signal_type
signal_value
observed_at
changed
```

signal_type 包括 `SITEMAP_LASTMOD`、`FEED_GUID`、`FEED_UPDATED`、`API_CURSOR`、`ETAG`、`LAST_MODIFIED`、`HEAD_HASH`。

### 4.5 Fetch / Snapshot

`fetch_route_attempt`

```text
id
task_id
source_id
url_id
route_type
route_release_id
attempt_no
requested_url
state
outcome_code
request_count
response_bytes
browser_seconds
snapshot_id
next_route_hint
started_at / finished_at
```

route_type 示例：

```text
HTTP_STATIC
HTTP_ALT_HEADERS
HTTP_PLATFORM_API
BROWSER_CRAWL4AI
BROWSER_PLAYWRIGHT_RECIPE
SITE_CUSTOM_EXTRACTOR
```

`fetch_observation`

```text
id
task_id
requested_url
final_url
redirect_chain
status_code
etag
last_modified
content_type
body_hash
snapshot_id
outcome_code
```

任务始终通过 `task_id/observation_id` 关联；final URL 只能作为 redirect/canonical evidence，禁止用 final URL 反查原任务。

### 4.6 Extraction / Version

`content_candidate`

```text
id
source_id
url_id
snapshot_id
feed_item_observation_id
candidate_source
extractor_release_id
canonical_ir_ref
content_hash
quality_score
quality_breakdown
status
created_at
```

candidate_source 包括 Feed Content、JSON-LD、Trafilatura、Readability、Site Selector、Crawl4AI fit/raw、Rendered DOM、平台 API、PDF Text。

`document`

```text
id
source_id
canonical_identity_key
current_version_id
created_at
```

`document_alias` 保存 canonical、redirect、migrated、AMP、print、mirror 等 URL 关系。

`document_version`

```text
id
document_id
source_url_id
snapshot_id
content_candidate_id
canonical_ir_hash
metadata_hash
version_no
published_at
updated_at
accepted_at
version_reason
status
```

version_reason 可为 `NEW_DOCUMENT`、`SOURCE_UPDATE`、`EXTRACTION_UPGRADE`、`METADATA_FIX`、`REPROCESS`。

## 5. Source Intake 与能力探测

新增站点先执行 `PROBE`：

1. 解析 URL，确定 registrable domain 和 allowed origins。
2. 获取 robots.txt 并保存 Policy Evidence。
3. 自动探测 robots 声明的 Sitemap、`/sitemap.xml`、Sitemap Index。
4. 探测 RSS/Atom/JSON Feed、`/feed`、`/rss`、`/atom.xml`、`llms.txt`。
5. 识别 WordPress REST、Ghost、Substack、Dev.to、Medium、Docusaurus、MkDocs、Hugo、Jekyll 等平台能力。
6. 抽样页面比较 HTTP 与 Browser 的正文质量和成本。
7. 识别 Archive、Category/Tag/Author、Docs Navigation、分页和文章 URL pattern。
8. 生成 Site Profile Release、能力置信度和 blocker。

Web 允许人工修正 Profile，但每次修改生成新 Release。

## 6. Provider Contract 与全量 Coverage

所有发现 Provider 使用统一 Contract：

```text
probe(source, profile) -> capability
discover(cursor, budget) -> candidates + evidence + next_cursor
exhaustion() -> explicit_reason
checkpoint() -> durable_cursor
```

推荐发现顺序：

1. 官方/CMS API。
2. Sitemap Index / Sitemap。
3. 历史 Feed。
4. Archive 年/月分页。
5. Category/Tag/Author。
6. Docs Navigation/TOC。
7. 站内分页、Next/Prev。
8. 普通内链图。
9. 搜索页仅作补充。

Provider 可以并行，但各自独立记录 Coverage。exhaustion 必须明确，例如 `API_CURSOR_END`、`SITEMAP_INDEX_EXHAUSTED`、`PAGINATION_END`、`FEED_WINDOW_ONLY`、`ROBOTS_BLOCKED`、`BUDGET_STOPPED`。达到预算只能标记 known gap，不能伪装成完整。

## 7. 增量同步：Change-Signal Plane

按成本从低到高：

```text
Feed/API cursor
  ↓
Sitemap lastmod
  ↓
Conditional GET: If-None-Match / If-Modified-Since
  ↓
HEAD / limited-range METADATA_PROBE
  ↓
Full HTTP body
  ↓
Browser
```

### 7.1 Feed-first 与渐进式 Enrichment

Feed 不只是“发现 URL”，也可以提供正文候选。流程：

1. 保存 Feed payload Snapshot 和 Feed Item Observation。
2. 比较 GUID/link/updated 与历史。
3. `content:encoded`/Atom content/JSON Feed content 生成低成本 `ContentCandidate`。
4. 通过 Quality Gate：完整且质量足够则无需 Browser；若只是摘要或正文不足则抓页面。
5. 页面 HTTP 抽取仍不合格时才升级 Crawl4AI/Playwright。
6. Feed 正文即使已接受，URL 仍参与后续 Reconcile。

这形成 `metadata/discovery → cheap body → full HTTP → browser` 的渐进式成本模型，避免高频增量同步无条件打开浏览器。

### 7.2 重叠窗口与 Reconcile

Feed/API cursor 默认保留最近 N 小时或 N 条重叠窗口，通过 GUID/URL/IR hash 幂等去重，防止乱序、修订和时钟漂移。

每周/月执行 `RECONCILE`：重扫 Sitemap/Archive、抽样 conditional fetch、检查删除/迁移/canonical 变化，并补齐已知 gap。

## 8. 抓取路由、质量门控与容错

### 8.1 HTTP-first / Browser-last

```text
HTTP_STATIC
  ├─ 304 → Freshness Observation
  ├─ 成功 + Quality Accept → 完成
  ├─ 正文不足 → HTTP_ALT / Site Extractor
  ├─ 动态壳页 → Browser
  └─ Challenge / Block → Policy Stop / Manual
```

Quality Policy 不只看字符数，而至少综合：正文长度、文本密度、标题相关度、导航/推荐比例、错误页 marker、代码/表格/图片完整度、发布时间/作者/canonical、Feed 与页面相似度、页面类型、历史 extractor 接受率。

决策：`ACCEPT`、`RETRY_HTTP_ALT`、`ESCALATE_BROWSER`、`SITE_RECIPE`、`MANUAL_REVIEW`、`REJECT_NON_ARTICLE`。

### 8.2 Browser Worker Pool

- 复用 Browser process，避免每 URL 启停 Chromium。
- 限制单 worker page/context 数。
- 按 `max_tasks_per_browser`、RSS、生命周期 recycle。
- Source 可独立 context，避免 cookie/session 污染。
- Crawl4AI 与 Playwright Recipe 统一适配为平台 `FetchResult`。
- 第三方 runtime 版本、配置和输出字段全部通过 Adapter 隔离。

### 8.3 Partial Result Preservation

禁止使用“外层 `wait_for(gather(...))` 超时后返回空集合”的语义。

100 个站点中 90 个完成、10 个超时：90 个立即提交；Run 标记 `PARTIAL_SUCCESS`；只重试 10 个。成功事实永远不因批次 deadline 丢失。

### 8.4 Fallback 持久化

Fallback 是业务状态，不是内存 `try/except`：

```text
RouteAttempt A: HTTP_STATIC
  ↓ quality fail
RouteAttempt B: BROWSER_CRAWL4AI
  ↓ runtime fail
RouteAttempt C: HTTP_ALT_EXTRACTOR
  ↓
RouteAttempt D: SITE_RECIPE / MANUAL
```

每个 Attempt 都有独立预算、Snapshot、错误和状态，重启后可继续。

## 9. 抽取、清洗与 Canonical IR

同一 Snapshot 可以产生多个候选：JSON-LD、`article/main`、Trafilatura、Readability、Site Selector、Feed Content、Crawl4AI fit/raw、Rendered DOM、平台 API。

内部长期内容真相使用结构化 `DocumentIR`：

```text
DocumentIR
  title
  subtitle
  author[]
  published_at / updated_at
  canonical_url
  tags[]
  blocks[]
    heading(level,text)
    paragraph(text)
    code(language,text)
    list(items)
    quote(text)
    table(cells)
    image(src,alt,caption,asset_id)
    embed(type,url)
  outbound_links[]
  metadata
  provenance
```

通用 boilerplate 清理包含 nav/header/footer/aside/form、cookie overlay、share toolbar、newsletter signup、related posts。站点专用尾部 marker 和 selector 必须版本化进入 Profile Release，并能离线 replay。

### 9.1 Markdown 与 Prompt Text 分离

Markdown Projection 保留标题层级、链接、图片、code fence、表格、blockquote、列表和必要 metadata；另建 `prompt_text_projection` 供 LLM 降噪使用。不能把“prompt-friendly 清洗”直接当知识库 Markdown。

建议 Markdown：

```markdown
---
title: ...
author: [...]
published_at: ...
updated_at: ...
canonical_url: ...
source_url: ...
document_id: ...
document_version_id: ...
tags: [...]
---

# 标题

正文……
```

### 9.2 Content Upgrade

首次只得到 Feed summary，后来页面拿到全文时：创建新 Candidate → 重新 Quality → 新 Canonical IR → 创建 `EXTRACTION_UPGRADE` Version；不得原地覆盖历史。只重建受影响的 Markdown/Chunk/Embedding。

## 10. URL、Canonical、去重与版本身份

URL Normalization 分层：语法规范化 → Source Profile 规则 → redirect evidence → `rel=canonical`/OG URL → 内容相似度证据。

不得用标题或 URL hash 直接等价为 Document Identity。区分：

```text
URL Candidate ID
Document ID
Document Version ID
Chunk ID
```

Normalized title、SimHash、MinHash、canonical、redirect、发布日期只生成 `Duplicate Hint`；最终由 Identity Resolver 决定 merge。这样避免“Weekly Update”“Release Notes”等同标题文章被错误删除。

## 11. Chunk、Embedding、Search 与 RAG

### 11.1 稳定 Chunk

优先按 Heading path → block group → token ceiling 切分，仅必要时 overlap。Chunk 保存 document/version、heading path、ordinal、source URL、published_at、tags、text hash。

稳定 Chunk key 示例：

```text
sha256(document_id + document_version_id + heading_path + ordinal + text_hash)
```

新增/变化 Chunk 才重新 Embedding；未变化向量复用；删除 Chunk 写 tombstone 后从索引移除。

### 11.2 Retrieval Policy Release

检索策略必须版本化，不能把权重硬编码在 ContextBuilder。新增 `retrieval_policy_release`：

```text
id
version
candidate_sources
lexical_weight
vector_weight
field_weights
freshness_policy
source_priority_policy
quality_policy
full_body_boost
reranker_config
context_budget
created_at
```

推荐检索链：

```text
Metadata filters
   ↓
BM25 + Vector recall
   ↓
RRF / weighted merge
   ↓
Policy scoring
   ↓
optional reranker
   ↓
Context budget selection
```

可借鉴 LLM_trader 的思想：title/body/category/tag 使用不同字段权重；正文过短降权；完整正文优先；再叠加发布时间、新鲜度、来源优先级和检索相关性。但“新鲜度”必须是可配置策略：技术知识检索通常不能像交易新闻那样把 24 小时前内容快速衰减到零。

RAG 引用必须固定到：`document_id + document_version_id + chunk_id + source_url`。

### 11.3 Retrieval Hot Path 不触发重型同步

查询路径只消费最近成功的 Search/Vector Projection，不同步等待抓站、Browser、全量 index rebuild。若索引过旧，查询返回当前可用结果并异步创建刷新任务；用 `projection_age`、`source_lag` 和 `stale` 标记暴露新鲜度。

这避免“用户每查询一次就尝试刷新外部数据”的热路径耦合，也避免外部站点异常拖垮检索服务。

### 11.4 缓存降级

Source/Provider 暂时失败时，不删除现有 Accepted Version 和索引。检索继续使用上一个有效 Projection，同时显示 freshness。只有明确删除/撤回策略才产生 tombstone。

## 12. Web 管理

### 12.1 Source 列表

显示状态、Entry URL、最近/下次同步、文章数、Coverage Confidence、Known Gaps、同步成功率、Browser 使用率、最近错误和成本。

### 12.2 Source 详情

Tabs：

```text
Overview
Profile Releases
Discovery Providers
Coverage
Feed / Change Signals
Runs / Tasks
Fetch Routes
Extraction Candidates
Quality
Documents / Versions
Assets
Search / Embedding
Audit
```

### 12.3 Extraction Preview / Replay

同一 URL 并排查看 Raw HTML、Rendered DOM、Feed body、Trafilatura、Readability、Crawl4AI fit/raw、Canonical IR、Markdown 和 Quality breakdown。修改规则时创建新 Release，在旧 Snapshot 上 Replay 预览 diff，再决定发布。

### 12.4 实时运维

WebSocket/SSE 推送 Run 进度、worker health、queue depth、日志和告警。控制面支持 Pause/Resume Source、Full Backfill、Incremental、Reconcile、Targeted Fetch、Retry failed only、Reprocess Snapshot、Rebuild Markdown、Rechunk/Reembed。

配置修改必须经过 schema 校验、版本化 Release 和 Audit；不能直接热改无版本的全局 ini 后失去可追溯性。

## 13. 调度与可靠性

Task 真相在 PostgreSQL，Redis 只负责投递：

```text
claim lease
→ admission / budget
→ execute
→ persist result + outbox in transaction
→ ack stream
```

- 长任务 heartbeat，lease 到期自动恢复。
- Task 幂等；同 URL/Run/Operation 使用 idempotency key。
- 429 使用 Retry-After + domain throttle；5xx 限次退避；Quality Fail 升级 route，不盲重试同 route。
- 按 domain + route 建 Circuit Breaker。
- 使用 weighted fair queue，防止一个百万页站点占满全部 worker。
- 限流至少有 Global、Source、Registrable Domain、Route 四层。

## 14. 安全与合规

所有 Feed/HTTP/Browser/Asset 请求统一走 Egress Policy：

1. 只允许 http/https。
2. DNS 解析 A/AAAA 后拒绝 private、loopback、link-local、multicast、metadata ranges。
3. 每次 redirect 重新校验目标。
4. 防 DNS rebinding，连接 IP 必须来自已校验解析结果。
5. 限制端口和 Source allowed origins。
6. Browser 请求经 network interception 执行相同策略。
7. 禁止 `file://` 和未授权内部网络。

Web 使用 RBAC（viewer/operator/admin）、SSO/OIDC、CSRF/CSP/安全响应头、操作审计；Secrets 放 Vault/KMS/Kubernetes Secret。

## 15. 可观测性

核心指标：

```text
coverage_candidates_total
provider_exhausted_total
known_gap_total
source_lag_seconds
fetch_requests_total
http_304_ratio
route_attempts_total
browser_seconds_total
candidate_accept_ratio
zero_body_ratio
browser_escalation_ratio
extractor_accept_ratio
task_queue_depth
lease_expired_total
retry_total
dead_letter_total
partial_success_runs_total
cost_per_source
cost_per_accepted_document
search_projection_age_seconds
rag_retrieval_latency
rag_context_tokens
```

关键告警：Sitemap candidate 数骤降、Feed 全失败、Browser escalation 突增、Extractor 接受率突降、dead-letter 堆积、Source 长时间无成功同步、索引 freshness 超 SLA。

## 16. Snapshot Replay 与规则升级

```text
Existing Snapshot
  ↓
New Extractor Release
  ↓
New Content Candidate
  ↓
New Quality Release
  ↓
Canonical IR compare
  ↓
New/unchanged Document Version
  ↓
Projection diff rebuild
```

如果 Canonical IR 没变，仅更新 processing provenance，不重新抓源站，也不重新 Embedding。

## 17. 数据规模与保留

- PG metadata 长期保留；大 payload 放对象存储。
- `task`、`fetch_observation`、`feed_item_observation` 按时间/source hash 分区。
- `url_candidate` 唯一索引 `(source_id, url_hash)`。
- 对象存储使用 content-addressed key：`sha256/ab/cd/<hash>`。
- Raw HTML/Feed payload 长期或按合规策略压缩归档。
- Screenshot/MHTML 按失败、审计、抽样策略保留。
- Search/Vector 可随时重建。

## 18. 测试策略

### 单元测试

URL normalization、RSS/Atom/JSON Feed、Sitemap、canonical/redirect、Quality、Markdown deterministic projection、SSRF resolver、retrieval scoring。

### Contract Test

对 Crawl4AI、Playwright、Trafilatura、各 Provider Adapter、Retriever Adapter 固定 fixture，确保第三方升级不改变平台 Contract。

### Golden Corpus

维护 50~200 个代表站点 Snapshot：静态博客、SPA、WordPress、Substack、Medium、Docs、代码密集、表格、广告导航、redirect、AMP、404/challenge。Extractor/Quality Release 每次都做离线 golden diff。

### 故障测试

必须覆盖：

- 单个 Feed 超时但其他结果保留。
- 批次 deadline 后 partial success 不丢。
- Worker 崩溃后 lease 恢复。
- Browser crash 只重试受影响 task。
- Redirect 后仍关联 requested URL。
- Crawl4AI success 但正文质量失败时正确升级/回退。
- Feed summary 后续升级全文形成新 Version。
- Source 暂时失败时搜索仍使用上一个有效 Projection。
- Retrieval 查询不会在请求线程触发 Browser/全量抓取。

## 19. 推荐实施阶段

### Phase 1：可用历史知识库

Source/Web 管理、Sitemap/Feed/普通内链 Discovery、PostgreSQL + Redis Streams + MinIO、HTTP Fetch、Trafilatura/Readability、Canonical IR + Markdown、Document Version、Run/Task 页面、ETag/Last-Modified 增量。

### Phase 2：动态站点与质量体系

Browser Worker Pool、Crawl4AI Adapter、Quality-Gated Enrichment、Feed Body Candidate、Partial-result-preserving fanout、Durable Route Fallback、Extraction Preview、Snapshot Replay、Assets、Coverage/Known Gap UI。

### Phase 3：搜索、RAG 与智能运维

OpenSearch、Chunk/Embedding、Retrieval Policy Release、稳定 RAG Citation、Duplicate Hint/Identity Resolver、Capability Learning、Route Cost Optimization、Drift Detection、AI/工作流 Integration Contract。

## 20. 验收标准

1. 新增普通博客不改代码即可接入。
2. 每个 Source 能展示 Coverage Evidence、Known Gap 和 blocker。
3. 服务重启、Worker 崩溃后任务可恢复，不重复大规模抓取。
4. 1000 站并发同步时，部分超时不会导致已成功站点结果丢失。
5. Feed/API 已带完整正文时能跳过 Browser；不足时能质量驱动升级。
6. Redirect 后仍可追溯原 task/requested URL。
7. 同标题不同文章不会被硬去重。
8. 后续获取更完整正文时创建 `EXTRACTION_UPGRADE` Version，而非覆盖历史。
9. Crawl4AI/Extractor 升级后可用历史 Snapshot 离线重放。
10. Markdown、Chunk、Embedding、全文索引均可从事实层重建。
11. RAG Citation 可稳定定位到 Document Version + Chunk。
12. Retrieval Policy 可版本化、回放和 A/B，对查询热路径无源站抓取依赖。
13. 所有网络出口统一执行 SSRF/Egress、robots、限流和审计。
14. Web 能定位失败发生在 Coverage、Signal、Fetch、Extraction、Quality、Identity 还是 Projection。

## 21. 最终架构原则

1. **Coverage 与 Fetch 分离**：先证明“应该有什么”，再讨论“抓到了什么”。
2. **Feed/API-first、HTTP-first、Browser-last**：昂贵 route 只在质量证据需要时升级。
3. **渐进式 Enrichment**：先结构化元数据和已有正文，再按需补全页面。
4. **Partial Result 永不丢失**：批次超时只影响未完成任务。
5. **Fallback 持久化**：抓取 route 是可恢复状态，而非进程内异常控制流。
6. **Snapshot Immutable**：原始事实不可变，处理规则可 replay。
7. **Identity 不等于 URL 或标题**：URL/canonical/标题只是身份 Evidence。
8. **正文升级形成 Version**：内容质量提升必须可追溯。
9. **Retrieval Policy 版本化**：检索字段权重、新鲜度、来源优先级和 rerank 不硬编码。
10. **查询热路径与抓取解耦**：RAG 使用最近有效投影，刷新由后台任务完成。
11. **第三方 Runtime Adapter 化**：Crawl4AI、Playwright、搜索和向量组件可替换。
12. **PostgreSQL + 对象存储是真相源**：Markdown、索引、向量和 AI 产物全部可重建。
