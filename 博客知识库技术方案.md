# 博客知识库技术方案

## 1. 建设目标

建设一套长期运行、可横向扩展、可审计、可回放的技术博客知识库平台，首期接入约 1000 个技术博客、工程博客、技术文档站和公开 Newsletter，后续扩展到数千站点时不需要为普通站点持续编写专用爬虫。

平台必须同时满足：

- 尽可能发现并抓取每个站点的完整历史文章；
- 持续发现新增文章、文章更新、URL 迁移和删除；
- 将正文清洗为稳定、可重建、适合检索/RAG 的 Markdown；
- 保留原始网络响应、渲染结果、内容版本和完整数据血缘；
- 新增网站主要通过 Probe、Source Profile、Recipe、Schema 和 Web 审核完成；
- 支持 Web 管理、调度、失败重试、Coverage、质量审核、成本治理、版本管理、搜索/RAG、导出；
- 在 Worker、Redis、Browser 或具体 crawler 故障后可恢复；
- 任何一篇 Markdown 都能追溯到来源 URL、Snapshot、Extractor、IR、版本和处理 Release。

### 1.1 关键 SLO

平台上线后按以下维度运营，而不是只看“爬虫成功率”：

- **Coverage**：历史 URL 覆盖率、Provider 证据、已知缺口；
- **Freshness**：新文章发现延迟、已知文章复检延迟；
- **Quality**：正文完整率、噪声率、结构质量、Quarantine 比例；
- **Reliability**：任务成功率、重试率、DLQ、队列年龄；
- **Cost**：每 Source 成本、每 1000 个有效文档成本、每个 accepted version 成本；
- **Traceability**：任意结果必须能追溯到不可变事实和 Release。

### 1.2 非目标

- 不默认绕过登录墙、付费墙、CAPTCHA、WAF 或明确访问控制；
- 不以 LLM/Agent 的“感觉抓完了”作为历史 Coverage 证明；
- 不把 Redis、crawler cache、Deep Crawl frontier 或 Agent memory 当业务真相；
- 不让外部用户直接向 Browser/crawler 透传任意 JavaScript、Hook、代理或系统参数；
- 不把 LLM 摘要、重写、翻译作为 Canonical Markdown；
- 不把标题、slug、展示 URL 作为跨阶段主键；
- 不因为 URL 已经见过就永久停止检查它；
- 不把单次 HTTP 200 当作内容质量成功。

---

## 2. 核心设计原则

1. **Coverage First**：先证明“有哪些历史文章”，再谈批量抓正文。
2. **Authoritative First**：CMS API、Sitemap、Feed、Archive 优先；Common Crawl/Deep Crawl 只补洞。
3. **Discovery Before Fetch**：URL 发现、规范化、Scope、去重先于正文抓取。
4. **Discover Cheap, Fetch Expensive**：发现阶段避免 Browser、截图、媒体和 LLM。
5. **HTTP First, Browser Last**：静态 HTTP 优先，确定性 Browser fallback，Agent Browser 极少使用。
6. **Deterministic Before Agentic**：主链不依赖 LLM 决策也能完成回填和增量。
7. **Snapshot First**：先保存不可变网络/渲染事实，再 Extract、IR、Markdown。
8. **IR First, Markdown as Projection**：Canonical IR 是内容事实，Markdown 是可重建投影。
9. **Quality Is First-Class**：抓取成功与内容合格是两个状态，必须有 Quality Gate。
10. **Cost Is First-Class**：自托管不是零成本，CPU、Browser、流量、存储、代理、LLM 都要归因。
11. **Tool Contract, Not Framework Coupling**：Crawl4AI、Playwright、Firecrawl 等通过 Adapter 接入。
12. **Recipe, Not Site Code**：selector、wait、scroll、分页、iframe 等用版本化配置表达。
13. **Stable Identity**：Source、URL、Snapshot、Document、Version 使用不可变 ID 关联。
14. **Idempotent + Recoverable**：Task 有幂等键、lease、fencing token、checkpoint、retry、DLQ。
15. **Source Friendly**：robots、Retry-After、per-domain rate/concurrency、指数退避优先。
16. **Security on Every Hop**：起始 URL、redirect、iframe、Asset URL、Browser navigation 每跳校验。
17. **Release Everything That Changes Results**：Profile、Runtime、Recipe、Extractor、Schema、Renderer 全版本化。
18. **Projection Is Rebuildable**：Markdown、全文索引、向量和派生结果都能从事实层重建。

---

## 3. 总体架构

```text
Web Admin / REST API / CLI / MCP
                |
        API + RBAC + Idempotency
                |
        Command / Query Layer
                |
        PostgreSQL Truth Store
                |
        Transactional Outbox
                |
           Redis Streams
                |
+---------------+----------------+----------------+----------------+
|               |                |                |                |
Discovery       HTTP Fetch       Browser Fetch    Extract/Quality  Asset/Index
Workers         Workers          Workers          Workers          Workers
|               |                |                |                |
CMS/Sitemap     httpx/aiohttp    Crawl4AI         IR              S3/MinIO
Feed/Archive    conditional GET  Playwright       Quality Gate    OpenSearch
UrlSeeder       cheap fetch      Recipe           Markdown        pgvector
Deep Crawl      metadata         limited Agent    Quarantine      Export
|               |                |                |                |
+---------------+----------------+----------------+----------------+
                         |
          Snapshot / Render Artifact / Trace
                         |
                     Canonical IR
                         |
                   Document Version
                         |
        Markdown / Search / RAG / Export
```

### 3.1 真相边界

```text
PostgreSQL + Object Storage = 业务真相
URL Observation            = URL 发现事实
Coverage Evidence           = 历史覆盖事实
Snapshot                    = 网络抓取事实
Render Artifact             = 浏览器渲染事实
Canonical IR                = 内容结构事实
Document Version            = 内容版本事实
Quality Result              = 内容质量事实
Cost Event                  = 执行成本事实
Redis Streams               = 运输层
Crawler cache/frontier      = 执行优化，不是业务真相
Markdown/Search/Vector      = 可重建投影
```

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST、内部 Command API、Schema 校验；
- **PostgreSQL**：Source、Run、Task、Coverage、Observation、Version、Quality、Cost、Release、Audit、Outbox；
- **Redis Streams**：队列运输、consumer group、backpressure；
- **React/Next.js + TypeScript**：Web 管理台；
- **Prometheus + Grafana + OpenTelemetry**：指标、日志、Trace；
- **S3/MinIO**：Raw HTML、MHTML/Rendered DOM、IR、附件、导出；
- **Docker Compose**：开发/小规模；
- **Kubernetes**：生产横向扩展、独立 Worker Pool、HPA。

### 4.2 抓取与解析

- **httpx/aiohttp**：HTTP、Feed、Sitemap、CMS API、Conditional GET、媒体下载；
- **Crawl4AI**：确定性 Browser/render、`arun_many()`、URL Seeder、受控 Deep Crawl；
- **Playwright**：复杂但稳定的交互 Recipe；
- **lxml + trafilatura/readability 类算法**：默认正文抽取；
- **JSON-LD/OpenGraph/Microdata**：元数据优先；
- **PyMuPDF/pdftotext**：PDF；扫描件才走 OCR；
- **OpenSearch**：全文/BM25；
- **pgvector**：首期向量检索，可通过 Adapter 更换。

### 4.3 Crawl4AI 的定位

Crawl4AI 只作为 `CrawlerAdapter/RenderAdapter`，不直接成为平台状态机。

生产锁定测试通过的固定版本、镜像 digest、Chromium 版本和 lockfile；升级必须经过 Golden Corpus、Benchmark 和 Canary。

当前 0.9.x 自托管版本提供认证、严格请求边界和更安全的声明式 Hook 方向，平台仍必须额外实施自己的 SSRF、Egress、Scope、预算和参数白名单。

`arun_many()`、Dispatcher、RateLimiter 只负责单 Worker 内的资源管理；平台 Task、Coverage、限流、Retry 和 DLQ 仍由平台控制。

---

## 5. 核心领域模型

### 5.1 Source 与 Profile

```text
source
- source_id UUID PK
- name
- base_url
- status: PROBING | ACTIVE | PAUSED | DEGRADED | DISABLED
- source_type
- timezone
- created_at / updated_at

source_profile
- profile_id UUID PK
- source_id
- version
- allowed_hosts[]
- include_paths[]
- exclude_paths[]
- discovery_policy JSONB
- fetch_policy JSONB
- extraction_policy JSONB
- asset_policy JSONB
- incremental_policy JSONB
- budget_policy JSONB
- status: DRAFT | TESTED | ACTIVE | RETIRED
- created_at
```

示例 Profile：

```yaml
source:
  base_url: https://example.com
  allowed_hosts: [example.com, www.example.com]
  include_paths: [/blog/, /posts/]
  exclude_paths: [/tag/, /author/, /search/, /login/]

discovery:
  providers:
    - {type: cms_api, priority: 10}
    - {type: sitemap, priority: 20}
    - {type: feed, priority: 30}
    - {type: archive, priority: 40}
    - {type: common_crawl, priority: 80, mode: gap_only}
    - {type: deep_crawl, priority: 90, mode: gap_only, max_depth: 3, max_pages: 5000}

incremental:
  discover_interval: 2h
  overlap_seconds: 86400
  verify_known_urls: true
  deletion_grace_runs: 3

fetch:
  mode: AUTO
  requests_per_second: 0.5
  max_concurrency: 2
  browser_fallback: true
  agent_fallback: false

quality:
  min_overall_score: 0.82
  quarantine_on_large_shrink: true

budget:
  max_browser_seconds_per_day: 3600
  max_llm_calls_per_day: 20
```

### 5.2 URL Observation

URL 身份与 URL 发现事实分离。

```text
url_identity
- url_id
- source_id
- normalized_url
- normalized_url_hash
- first_seen_at
- last_seen_at
- state

url_observation
- observation_id
- url_id
- provider_type
- provider_run_id
- observed_url
- discovered_at
- lastmod
- published_hint
- parent_url
- evidence JSONB
```

所有 Provider 发现的 URL 都先进入 Observation，再统一 normalize、scope、filter、dedupe。

### 5.3 Run / Task

```text
run
- run_id
- source_id
- type: PROBE | BACKFILL | INCREMENTAL | VERIFY | REPROCESS
- profile_id
- status
- started_at / finished_at

crawl_task
- task_id
- run_id
- source_id
- url_id
- task_type
- idempotency_key UNIQUE
- priority
- state
- attempt
- max_attempts
- lease_owner
- lease_until
- fencing_token
- not_before
- error_code
- error_detail
```

幂等键示例：

```text
BACKFILL_FETCH:{url_id}:{profile_release}
VERIFY:{url_id}:{time_bucket}
EXTRACT:{snapshot_id}:{extractor_release}
MARKDOWN:{document_version_id}:{renderer_release}
```

### 5.4 Snapshot / Render Artifact

```text
snapshot
- snapshot_id
- task_id
- url_id
- requested_url
- final_url
- status_code
- request_headers_hash
- response_headers JSONB
- etag
- last_modified
- content_type
- body_sha256
- object_key
- fetched_at
- runtime_release_id

render_artifact
- render_id
- snapshot_id nullable
- url_id
- final_url
- dom_object_key
- mhtml_object_key nullable
- screenshot_object_key nullable
- network_summary JSONB
- recipe_release_id
- runtime_release_id
- rendered_at
```

Raw HTML/DOM 统一 gzip/zstd 压缩存 S3/MinIO，数据库只保留索引、Hash 和关键 Header。

### 5.5 Document / Version / IR

```text
document
- document_id
- source_id
- canonical_url_id
- first_published_at
- current_version_id
- state

document_version
- version_id
- document_id
- source_artifact_type
- source_artifact_id
- extractor_release_id
- ir_schema_version
- ir_object_key
- semantic_content_hash
- created_at
```

版本只有在 Canonical IR 发生实质变化时创建；HTML 广告、追踪参数或导航变化不应产生正文版本。

### 5.6 Quality

```text
document_quality
- quality_id
- version_id
- content_length
- text_density
- boilerplate_ratio
- link_density
- title_confidence
- author_confidence
- published_at_confidence
- content_completeness_score
- structure_score
- overall_score
- gate_result: PASS | QUARANTINE | FAIL
- reasons JSONB
- quality_release_id
```

### 5.7 Cost

```text
cost_event
- cost_event_id
- run_id
- task_id
- source_id
- document_id nullable
- executor_type
- cpu_ms
- memory_peak_mb
- browser_ms
- network_in_bytes
- network_out_bytes
- proxy_bytes
- storage_bytes
- llm_input_tokens
- llm_output_tokens
- external_request_cost
- estimated_infra_cost
- created_at
```

核心成本指标：

```text
cost_per_accepted_version = 总执行成本 / Quality PASS 的新 DocumentVersion 数
```

---

## 6. 新站点接入流程

新增网站不能直接进入生产抓取，先进入 Probe。

### 6.1 自动 Probe

1. 校验 URL、DNS/Scope、robots；
2. 检测 `/sitemap.xml`、Sitemap Index、RSS/Atom；
3. 检测 WordPress/Ghost/Medium/Substack/常见 CMS API；
4. 抽取首页/归档页链接模式；
5. 随机选择 5~10 篇文章样本；
6. 比较 HTTP、Trafilatura、Crawl4AI Browser、Recipe 的正文质量；
7. 检测 JSON-LD、作者、发布时间、代码块、图片；
8. 估算历史 URL 数量；
9. 估算 Browser 比例和回填成本区间；
10. 生成 DRAFT Source Profile。

### 6.2 Web 审核

管理员看到：

- 发现 Provider；
- URL 样本和匹配规则；
- HTTP/Browser 对比；
- Markdown 预览；
- Quality 分数；
- robots；
- 预计文章数；
- 预计回填成本；
- 风险项。

审核通过后发布 `TESTED -> ACTIVE` Profile。

### 6.3 第 1001 个站点的目标

普通博客新增流程只需要：

```text
输入域名 -> Probe -> 调整 Profile/Recipe -> 样本验收 -> 发布 -> Backfill
```

只有特殊登录态、极端 JS 或非标准内容源才需要新增 Adapter 代码。

---

## 7. 历史全量发现

“抓完整历史”最难的部分不是并发，而是 Coverage 证明。

### 7.1 Provider 优先级

```text
1. CMS API / 官方内容 API
2. Sitemap / Sitemap Index
3. RSS / Atom
4. Archive / Pagination / Year-Month Index
5. Tag/Category Index（只作补充）
6. Crawl4AI AsyncUrlSeeder：sitemap + 可选 Common Crawl
7. bounded Deep Crawl
8. 人工补充/导入
```

### 7.2 Coverage Evidence

每个 Provider Run 保存：

```text
coverage_evidence
- evidence_id
- source_id
- provider_type
- run_id
- cursor_start
- cursor_end
- discovered_count
- min_published_at
- max_published_at
- exhausted boolean
- exhaustion_reason
- expected_count nullable
- known_gap JSONB
- evidence_object_key nullable
```

Backfill 完成条件不能是“队列空了”，而应该是：

- 所有高优先级 Provider 已穷尽；
- Sitemap/CMS cursor 已保存；
- Archive 分页达到明确边界；
- Provider 集合交叉后的新增 URL 收敛；
- Deep Crawl 仅用于补洞，并记录 max_pages/max_depth 截止原因；
- Known Gap 明确可见。

### 7.3 URL 规范化

统一执行：

- scheme/host 小写；
- 默认端口清理；
- fragment 移除；
- tracking 参数移除；
- query 参数按站点 allowlist 保留；
- trailing slash 规则由 Source Profile 决定；
- Canonical Link 只作为证据，不能无条件信任；
- redirect 后 final URL 重新过 Scope/SSRF。

保留 `observed_url` 和 `normalized_url`，不能只保存最终规范化结果。

---

## 8. 抓取执行与最小成本路由

### 8.1 Acquisition Planner

Planner 根据 Source Profile、历史成功率、质量和成本生成受约束 FetchPlan。

```text
L0 CMS/API
L1 HTTP
L2 Crawl4AI deterministic Browser
L3 Playwright deterministic Recipe
L4 LLM-assisted extraction / Controlled Agent Browser
```

伪代码：

```python
if api_available and api_quality_pass:
    route = API
elif http_success_rate >= 0.98 and http_quality_p50 >= 0.90:
    route = HTTP
elif source_profile.browser_required:
    route = CRAWL4AI_BROWSER
else:
    route = HTTP_WITH_BROWSER_FALLBACK
```

### 8.2 HTTP Worker

默认使用 HTTPX/aiohttp：

- `If-None-Match`；
- `If-Modified-Since`；
- gzip/br；
- timeout 分阶段配置；
- redirect 次数上限；
- per-domain connection pool；
- `Retry-After`；
- 429/503 指数退避 + jitter；
- body size 上限；
- MIME 白名单。

HTTP 路径不启动 Browser，降低 CPU、内存和延迟。

### 8.3 Browser Worker

Crawl4AI 为默认 Browser Adapter：

- Browser 进程复用；
- Context 按 Source/安全域隔离；
- Page 按 Task 短生命周期；
- 默认阻断广告、追踪、字体、视频等非必要资源；
- 默认不截图、不生成 PDF；
- 只有 Asset Policy 要求时下载正文图片；
- 固定 wait/scroll/click 进入 Recipe；
- `arun_many()` 只处理平台已经授权的一小批 URL；
- Worker 内使用 `MemoryAdaptiveDispatcher` 做内存保护；
- Crawl4AI `RateLimiter` 是第二层保护，第一层仍是平台 domain limiter。

### 8.4 Playwright Recipe

需要稳定交互的站点使用配置化 Recipe：

```yaml
steps:
  - wait_for: "css:article"
  - click_if_exists: "button.load-more"
  - scroll:
      max_rounds: 8
      stable_rounds: 2
  - wait_network_idle:
      timeout_ms: 5000
```

Recipe 必须有最大动作数、最大运行时和导航 Scope。

### 8.5 Agent Browser

默认禁用。只有以下条件才允许进入：

- 站点价值高；
- HTTP/Crawl4AI/Recipe 全失败；
- 无登录绕过、无 CAPTCHA 绕过；
- 有明确预算；
- 动作可审计、URL 受 Scope 约束。

Agent 输出只能生成候选 Artifact 或 Recipe 建议，不能直接覆盖 Canonical 数据。

---

## 9. 正文抽取、Canonical IR 与 Markdown

### 9.1 元数据抽取顺序

标题、作者、时间、Canonical URL 优先级：

```text
结构化官方 API
> JSON-LD / Microdata
> OpenGraph / meta
> 页面稳定 selector
> DOM heuristic
> LLM fallback
```

每个字段保存 `value + source + confidence`。

### 9.2 正文 Extractor Pipeline

```text
Snapshot/Render
   |
DOM normalize
   |
remove known chrome
   |
article candidates
   |
trafilatura/readability/lxml
   |
block normalization
   |
Canonical IR
   |
Quality Gate
   |
PASS -> DocumentVersion
QUARANTINE -> Web Review / fallback extractor
FAIL -> retry/fallback
```

### 9.3 Canonical IR

IR 至少表达：

- heading；
- paragraph；
- list；
- blockquote；
- code block + language；
- table；
- image/asset reference；
- link；
- horizontal rule；
- math 可选；
- embedded media placeholder。

不要把 Markdown 字符串当中间格式，否则二次处理难以稳定识别结构。

### 9.4 Canonical Markdown

Markdown Renderer 从 IR 确定性生成，文件建议：

```markdown
---
document_id: doc_xxx
version_id: ver_xxx
source_id: src_xxx
source_url: https://example.com/post
canonical_url: https://example.com/post
published_at: 2026-01-01T10:00:00Z
author: Jane Doe
language: zh-CN
content_hash: sha256:...
---

# 标题

正文……
```

Front Matter 不应塞入易变化的向量、摘要或模型输出。

### 9.5 文件路径

默认使用稳定 ID，不用标题作为唯一文件名：

```text
export/
  {source_slug}/
    {yyyy}/
      {document_id}.md
```

额外生成 manifest：

```text
manifest.jsonl
source_url -> document_id -> current_version_id -> md_path
```

标题变化不会造成文件身份漂移。

---

## 10. Quality Gate 与回归体系

HTTP 200、`result.success=true` 只表示执行成功，不表示知识库内容可用。

### 10.1 质量指标

- 正文长度；
- 文本密度；
- Boilerplate 比例；
- Link density；
- 标题一致性；
- 作者/时间置信度；
- 代码块数量与闭合完整性；
- 图片/alt 保留情况；
- 结构完整度；
- 与历史版本长度/结构的异常变化。

### 10.2 Gate

示例：

```text
PASS
- overall_score >= 0.82
- 正文长度达到 Source 基线
- 无登录页/Cookie 页面特征

QUARANTINE
- 新版本正文比上一版本突然减少 > 70%
- title 与正文主标题冲突
- Browser 页面只有 skeleton
- 大量导航文本

FAIL
- 无有效正文
- 非文章页面
- 被登录/CAPTCHA/错误页面替代
```

只有 PASS Version 成为默认 current version。

### 10.3 Golden Corpus

每个 Source 保留 3~10 个代表页面作为 Golden Corpus，覆盖：

- 普通文章；
- 长文；
- 代码密集；
- 图片密集；
- 旧模板；
- 新模板。

Runtime、Extractor、Recipe、Renderer 升级时自动回归：

- 正文覆盖率；
- 噪声率；
- metadata 准确率；
- 代码块完整率；
- Markdown diff；
- Quality Gate 结果。

---

## 11. 增量同步

增量同步必须拆成“发现增量”和“内容增量”。

### 11.1 发现增量

优先：

- RSS/Atom；
- CMS cursor；
- Sitemap `lastmod`；
- Archive 最近分页；
- 低频 bounded Deep Crawl 补漏。

使用 overlap window 防止边界漏数据：例如每次从上次 cursor 往前回退 24 小时再去重。

### 11.2 已知 URL 复检

Seen URL 只表示身份已存在，不能停止 freshness 检查。

复检顺序：

1. Conditional GET；
2. 304 -> 更新 check fact，不建新 Version；
3. 200 -> 保存新 Snapshot；
4. 对 Snapshot/IR 做 Hash；
5. Canonical IR 无变化 -> 不建新 Version；
6. IR 变化 -> Quality Gate；
7. PASS -> 新 DocumentVersion。

### 11.3 自适应刷新

Source 按发布频率分层：

```text
HOT    15~60 分钟发现增量
WARM   2~6 小时
COOL   12~24 小时
COLD   2~7 天
```

文章自身复检频率根据文章年龄和历史修改概率逐步衰减。

### 11.4 删除与迁移

状态：

```text
ACTIVE
SUSPECT_MISSING
MISSING_CONFIRMED
MOVED
BLOCKED
```

- 一次 404 不立即删除；
- 410 可提高删除置信度；
- 连续 N 次缺失再确认；
- 301/308 建 URL Alias/Move Edge；
- Canonical 变化保留证据；
- 历史版本永不因源站删除而直接物理删除。

---

## 12. 队列、调度与恢复

### 12.1 Redis Streams 只做 Transport

推荐 Stream：

```text
discovery
fetch-http
fetch-browser
extract
quality
asset
index
reprocess
```

DB 中的 Task 才是业务状态。

### 12.2 Transactional Outbox

创建/修改 Task 与写 Outbox 在同一 PostgreSQL 事务中；独立 relay 将 Outbox 投递 Redis Streams。

这样解决“数据库写成功但消息未发”或“消息已发但数据库没提交”的双写问题。

### 12.3 Lease + Fencing Token

Worker 领取 Task：

```text
lease_owner
lease_until
fencing_token += 1
```

写最终结果时必须携带最新 fencing token，防止旧 Worker 失联恢复后覆盖新 Worker 结果。

### 12.4 Retry / DLQ

错误分层：

```text
TRANSIENT_NETWORK
RATE_LIMITED
SERVER_5XX
ROBOTS_DENIED
SCOPE_DENIED
PARSER_ERROR
QUALITY_FAIL
BROWSER_CRASH
POLICY_BLOCKED
```

不同错误使用不同 retry 策略，Policy/robots 错误不能无限重试。

---

## 13. 限流与资源调度

### 13.1 两层限流

**平台层**按 registrable domain/source 使用 token bucket：

- RPS；
- max concurrency；
- Retry-After；
- circuit breaker。

**执行器层**再使用 Crawl4AI RateLimiter/Dispatcher 做单进程保护。

不能只依赖 Worker 本地 limiter，因为多个 Worker 会同时访问同一域名。

### 13.2 Worker Pool 隔离

```text
discovery-http   高并发、低内存
fetch-http       高并发、低成本
browser-light    Crawl4AI Browser
browser-heavy    Playwright Recipe
extract          CPU 型
llm-extract      预算隔离
asset            低优先级
index            异步投影
```

Browser Pool 单独 HPA，避免浏览器 OOM 拖垮 HTTP 抓取。

### 13.3 优先级

建议：

```text
P0  增量新文章
P1  近期已知文章 freshness
P2  Backfill
P3  Asset
P4  Reprocess / 重建索引
```

Backfill 不能挤占增量同步。

---

## 14. 数据存储与索引

### 14.1 PostgreSQL

建议：

- 大表按时间/Source hash 分区；
- `url_identity(source_id, normalized_url_hash)` 唯一；
- `crawl_task(idempotency_key)` 唯一；
- Observation、Snapshot、Cost Event 使用 append-heavy 设计；
- 热查询建立 Source/State/Time 复合索引；
- 大 JSON/正文不直接塞数据库。

### 14.2 Object Storage

目录示例：

```text
raw/{source_id}/{yyyy}/{mm}/{snapshot_id}.html.zst
render/{source_id}/{render_id}.dom.zst
ir/{document_id}/{version_id}.json.zst
assets/{sha256}
exports/{export_run_id}/...
```

Asset 使用内容寻址去重。

### 14.3 Search / Vector

搜索与向量都是 Projection：

```text
DocumentVersion PASS
    -> chunk
    -> OpenSearch
    -> embedding
    -> pgvector
```

Chunk 必须保存 `document_id + version_id + block range + chunker_release`，不能只存裸文本。

---

## 15. Web 管理台

Web 管理不是“看日志”，而是数据运营控制面。

### 15.1 Dashboard

展示：

- Source 总数/状态；
- Backfill Coverage；
- Incremental lag；
- Queue age；
- HTTP/Browser route 比例；
- Quality PASS/QUARANTINE/FAIL；
- Browser fallback rate；
- 429/5xx/robots；
- 每日 accepted versions；
- 每日/每 Source 成本；
- DLQ。

### 15.2 Source 管理

功能：

- 新增/编辑/暂停 Source；
- Probe；
- Profile diff；
- Release/回滚；
- Provider Coverage；
- 手动 Backfill/Incremental；
- rate/concurrency；
- Browser/LLM budget；
- Golden Corpus；
- Known Gaps；
- robots 状态。

### 15.3 Document 管理

功能：

- URL Observation；
- Snapshot 历史；
- Render Artifact；
- DocumentVersion；
- Version diff；
- Markdown 预览；
- Quality 分数；
- Quarantine 审核；
- 重新抓取；
- 重新 Extract；
- 重新 Render Markdown。

### 15.4 Task 管理

- Run/Task 搜索；
- Retry；
- Cancel；
- DLQ replay；
- 错误聚类；
- Worker lease；
- Trace 链接。

### 15.5 Cost 页面

必须展示：

- 每 Source 月成本；
- 每 Adapter 成本/成功率；
- `cost / accepted version`；
- Browser 秒数；
- 代理流量；
- LLM Token；
- 重试浪费成本；
- 304 命中率；
- 预算告警。

---

## 16. API 设计

关键 API：

```text
POST   /sources
POST   /sources/{id}/probe
POST   /sources/{id}/profiles
POST   /sources/{id}/profiles/{profile_id}/activate
POST   /sources/{id}/backfill
POST   /sources/{id}/incremental
POST   /sources/{id}/pause
GET    /sources/{id}/coverage
GET    /sources/{id}/quality
GET    /sources/{id}/cost

GET    /documents
GET    /documents/{id}
GET    /documents/{id}/versions
GET    /documents/{id}/markdown
POST   /documents/{id}/refetch
POST   /versions/{id}/reprocess

GET    /runs
GET    /tasks
POST   /tasks/{id}/retry
POST   /dlq/{id}/replay
```

所有写 API 支持 Idempotency-Key，并记录 Audit Event。

---

## 17. 成本治理

### 17.1 预算对象

可按 Source/Organization/Runtime 设置：

- 每日 HTTP 请求；
- 每日 Browser 秒；
- 每日代理流量；
- 每日 LLM 调用；
- 月度总成本软/硬阈值。

### 17.2 优化优先级

1. Authoritative API/Sitemap 发现；
2. HTTP 替代 Browser；
3. Conditional GET 提升 304；
4. Browser 阻断非必要资源；
5. Browser 批次和复用；
6. LLM 只处理疑难内容；
7. Asset 延后和内容寻址；
8. Backfill 低优先级运行。

自托管 Crawl4AI 是默认 Browser 能力，但仍保留外部托管抓取 Provider Adapter，作为灾备或个别特殊站点能力，避免反向锁定。

---

## 18. 可观测性

### 18.1 Metrics

至少包括：

```text
source_coverage_ratio
source_known_gap_count
new_url_discovery_lag_seconds
known_url_revalidation_lag_seconds
fetch_success_rate
http_304_ratio
browser_fallback_ratio
quality_pass_ratio
quality_quarantine_ratio
accepted_version_rate
queue_age_seconds
retry_rate
dlq_count
cost_per_accepted_version
browser_seconds_total
llm_tokens_total
```

### 18.2 Trace

Trace 链：

```text
Source Run
 -> Discovery Provider
 -> Observation
 -> FetchPlan
 -> Fetch/Render
 -> Snapshot/Artifact
 -> Extract
 -> Quality
 -> DocumentVersion
 -> Markdown/Index
```

任何异常都可从 Document 反查到执行器和 Release。

### 18.3 Alert

- Source 超过 freshness SLO；
- Sitemap/CMS 突然变空；
- Quality PASS 比例骤降；
- Browser fallback 暴涨；
- 文章正文平均长度突降；
- 429/403 激增；
- 队列 backlog 持续增长；
- 成本异常；
- Worker lease 超时；
- Object Storage 写失败。

---

## 19. 安全与合规

### 19.1 SSRF / Egress

所有 URL：

1. parse/normalize；
2. hostname allow/scope；
3. DNS resolve；
4. 阻断 localhost、link-local、metadata、RFC1918 未授权地址；
5. connect；
6. redirect 后重新执行 1~5。

Browser navigation、iframe、Asset 下载同样执行。

### 19.2 Runtime 隔离

- Crawler 不直接暴露公网；
- 内部 Gateway + service identity；
- 固定镜像 digest；
- 最小容器权限；
- 独立网络策略；
- Secret Manager；
- Runtime 参数白名单；
- 普通用户禁止任意 JS/Hook/proxy/browser args；
- 生产 Runtime 只能使用 TESTED/ACTIVE Release。

### 19.3 来源友好

- 尊重 robots 和站点政策；
- 明确 User-Agent/联系信息；
- 遵守 Retry-After；
- per-domain 限流；
- 不绕过访问控制；
- 对版权内容只保存业务允许范围内的知识库数据，并保留来源信息。

---

## 20. Release、测试与回滚

以下对象全部版本化：

```text
SourceProfileRelease
DiscoveryProviderRelease
PlannerRelease
CrawlerAdapterRelease
RuntimeRelease
BrowserRecipeRelease
ExtractorRelease
QualityRelease
IRSchemaRelease
MarkdownRendererRelease
ChunkerRelease
EmbeddingRelease
```

### 20.1 发布流程

```text
开发
 -> Unit Test
 -> Golden Corpus
 -> Benchmark
 -> Security Scan
 -> Canary Source
 -> Compare Quality/Cost
 -> ACTIVE
```

### 20.2 回滚原则

回滚 Release 不删除已经保存的 Snapshot/Version；只改变后续执行的版本，并可对指定历史 Snapshot 重新处理。

---

## 21. 故障恢复

### Redis 故障

DB Task/Outbox 仍是事实；Redis 恢复后重新投递未确认消息。

### Worker 崩溃

Lease 超时后由其他 Worker 领取，fencing token 阻止旧 Worker 回写。

### Browser 崩溃

Task 重试，Browser Context/Page 不承担持久状态。

### Crawl4AI 升级回归失败

保持旧 RuntimeRelease ACTIVE，新版本 RETIRED/FAILED，不影响已有数据。

### Object Storage 短暂不可用

Snapshot 不标记完成，禁止只写 DB 元数据后丢正文。

### PostgreSQL 故障

不允许 Worker 退化成“只写本地文件”；恢复后从数据库状态继续。

---

## 22. 容量与扩展方式

1000 个网站不需要 1000 个常驻 crawler，应共享 Worker Pool。

容量主要由以下变量决定：

```text
历史 URL 总量
每日新增 URL
平均页面大小
HTTP 与 Browser 比例
平均 Browser 秒数
Asset 下载策略
复检周期
```

估算：

```text
HTTP worker demand ≈ HTTP requests / target throughput
Browser capacity ≈ browser_task_count * avg_browser_seconds / available_window
Storage ≈ raw_snapshot_bytes + render_bytes + IR + assets + versions
```

扩展时优先独立扩容 Browser Worker 和 Extract Worker，不需要复制控制面。

当达到千万级 Observation/Snapshot 后：

- PostgreSQL 分区；
- 历史冷数据归档；
- Object Storage Lifecycle；
- OpenSearch 独立集群；
- Redis Streams 分流；
- Source hash 分片 Worker routing。

---

## 23. 实施阶段

### Phase 1：可运行 MVP

- Source/Profile；
- Sitemap/RSS/Archive Discovery；
- HTTP Fetch；
- Crawl4AI Browser fallback；
- Snapshot + S3；
- Trafilatura/lxml -> IR -> Markdown；
- PostgreSQL Task；
- Redis Streams + Outbox；
- Incremental；
- 基础 Web Source/Run/Document 页面。

### Phase 2：生产可靠性

- Coverage Evidence；
- Quality Gate/Quarantine；
- Golden Corpus；
- Lease/Fencing/DLQ；
- Cost Event/Budget；
- OpenTelemetry；
- Source Probe；
- Profile Release/回滚；
- Version diff。

### Phase 3：规模化

- AsyncUrlSeeder/Common Crawl gap fill；
- bounded Deep Crawl；
- Adaptive Scheduler；
- Browser Pool 独立扩容；
- OpenSearch/pgvector；
- Asset Pipeline；
- 大表分区与生命周期。

### Phase 4：疑难自动化

- LLM-assisted extractor；
- Recipe 自动建议；
- Controlled Agent Browser；
- 异常聚类与自动诊断。

LLM/Agent 永远不是历史全量和增量同步的必需依赖。

---

## 24. 最终推荐

最终采用：

```text
FastAPI + PostgreSQL + Redis Streams + S3/MinIO
+ HTTPX/aiohttp
+ Crawl4AI 自托管 Browser Adapter
+ Playwright Recipe fallback
+ lxml/trafilatura/readability
+ Canonical IR + deterministic Markdown renderer
+ Quality Gate + Golden Corpus
+ Coverage Evidence
+ Incremental conditional revalidation
+ Cost Event/Budget
+ React/Next.js Web Admin
+ OpenTelemetry/Prometheus/Grafana
+ OpenSearch + pgvector（需要检索/RAG时启用）
```

架构的核心不是 Crawl4AI 本身，而是把“发现、抓取、事实保存、内容抽取、质量、版本、成本、增量、审核”拆成独立且可替换的能力。

这样才能同时解决 1000 个站点全量历史抓取、后续不断增加网站、持续增量同步、Markdown 知识库稳定性和 Web 管理问题，并在规模扩大后仍清楚回答：**抓了什么、为什么认为抓全、内容是否可信、多久没更新、花了多少钱，以及每一篇 Markdown 是如何生成的。**
