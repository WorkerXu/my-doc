# 博客知识库技术方案

## 1. 建设目标与边界

建设一个长期运行、可横向扩展、可审计、可回放的技术博客知识库平台。首期接入约 1000 个技术博客、工程博客、技术文档站和公开 Newsletter，尽可能抓取完整历史文章并清洗为稳定 Markdown；后续持续增加网站，并低成本完成增量同步、版本管理、搜索/RAG、知识派生、Web 管理、导出和离线重处理。

目标规模：

- 1000+ Source，可扩展到数千站点；
- 百万级 Document / Document Version；
- 数百万到千万级 URL Observation；
- 千万级 Chunk / Embedding；
- HTML、Markdown、TXT、JSON/API、RSS/Atom/JSON Feed 为主要输入；PDF、公开视频字幕作为可选外部证据输入；
- 中英日韩等多语言；
- HTTP 为默认抓取路线，Browser 为受控 fallback；
- CMS API、Sitemap、Feed、Archive、Category/Tag、Docs TOC、Common Crawl、受控 Deep Crawl 等多入口历史发现；
- PostgreSQL + Object Storage 保存事实；Markdown、全文索引、向量索引、Graph、AI Insight、Relation、Annotation、Export、Dashboard Read Model 均为可重建 Projection；
- Web UI、n8n、MCP、CI、Agent、外部系统只能通过受控 API / Event 边界驱动平台。

平台必须能够回答：

1. 一个站点理论上有哪些历史文章，为什么认为历史已经基本覆盖；
2. 一个 URL 从哪里被发现、如何规范化、为何被过滤或放行、经过哪些跳转、最终抓到了什么；
3. 为什么某 URL 走 HTTP、平台 API、文件流、Crawl4AI Browser 或 Playwright；
4. 一个 Markdown 来自哪个 Snapshot、Extractor Release 和 Document Version；
5. 源站变化后哪些内容真正变化，哪些派生物需要重建；
6. Worker 崩溃、批任务部分失败或外部自动化重启后如何恢复且不丢成功项；
7. 新增第 1001 个站点时，能否主要依靠配置、Probe 与审核接入，而不是增加站点专用代码；
8. 全量历史抓取是否有 durable checkpoint，能否从断点继续；
9. 百万 URL 批处理是否保持有界内存，并支持逐 URL 提交；
10. Browser 是否被复用且安全隔离，何时回收，是否因资源压力自动降并发；
11. 第三方 crawler API 升级后，是否只修改 Adapter 而不污染平台数据契约；
12. Markdown 导出到本地/S3/Git 后，是否仍能稳定映射回平台内部 Document Version；
13. 一条 AI Insight 到底来自哪个原文版本、哪些 block、哪个模型和 Prompt；
14. 一个所谓“引用”到底是逐字原文、清理后的 transcript，还是模型 paraphrase；
15. 两篇内容被判断为“同文转载”“翻译”或“相关主题”时，依据是什么，是否可逆；
16. 自动研究发现的新站点为什么进入知识库、消耗多少预算、是否会影响受管理站点 Coverage；
17. 一个回答是否直接引用 Source Block，还是只在反复总结 AI Summary/Insight；
18. AI 声称“只加链接/格式”时，系统如何证明它没有偷偷改正文。

非目标：

- 不把“互联网搜索发现的任意 URL”自动算入正式博客源的历史 Coverage；
- 不以模型输出替代原始 Snapshot、Canonical IR 或人工可验证证据；
- 不用不可逆的向量聚类直接合并、删除文档；
- 不把 Redis、Crawler 内部队列、n8n 执行记录当业务真相。

---

## 2. 核心设计原则

1. **Config First**：普通站点靠 Source / Site Profile / Release 接入，不修改核心代码。
2. **Coverage First**：历史完整性由 Provider Evidence、cursor、exhaustion reason 和 known gap 证明，队列为空不等于完整。
3. **Authoritative First**：CMS/API、Sitemap、Feed、Archive 优先，Common Crawl/Deep Crawl 用于补洞和交叉验证。
4. **Discovery Before Fetch**：先发现、Observation、规范化、过滤、分类、去重，再抓正文。
5. **Durable Streaming Frontier**：大站 URL 发现必须流式、小批持久化，禁止把全站 URL 一次性物化在内存。
6. **Filter With Evidence**：Filter 可 HARD/SOFT/DEPRIORITIZE，但任何决定都保存规则、原因和证据。
7. **URL Is Locator**：资源类型由响应 MIME、签名、跳转和站点规则判断，不能只看扩展名。
8. **HTTP First, Browser Last**：静态 HTML、Feed、API、Sitemap 优先 HTTP；Browser 只处理动态正文、交互和质量 fallback。
9. **Reuse Browser, Isolate Session**：复用 Browser/Crawler 进程降低成本，但 Cookie、LocalStorage、认证态和代理身份按 Source/Profile/安全域隔离。
10. **Adapter Is Boundary**：Crawl4AI/Playwright/HTTP 客户端只输出平台内部稳定契约，第三方 DTO、字段名和版本变化不得泄漏到事实层。
11. **Platform Permit Before Local Dispatcher**：平台 global/domain/source 配额是并发上限；Crawler Dispatcher/Semaphore 只负责 Worker 内二级自适应控制。
12. **Bounded Window + Commit Per URL**：Worker 只 lease 有界窗口，流式返回结果；每个 URL 成功立即落库，不等待整批完成。
13. **Snapshot First**：先保存不可变网络事实，再做抽取、清洗、Markdown、Chunk、Graph、AI。
14. **IR First, Markdown as Projection**：Canonical IR / Document Version 是内容事实层，Markdown 可确定性重建。
15. **Full Canonical Before Fit Markdown**：Pruning/BM25/LLM 等有损过滤只能产生 FILTERED/ENHANCED Projection，绝不能覆盖 Canonical Markdown。
16. **Recipe, Not Site Code**：交互、路由、Filter、Schema、Selector、Extractor 均声明式、版本化、可回放。
17. **Stable Identity**：URL Alias、Document、Version、Block、Chunk、Embedding、Relation、Knowledge Note、Annotation、Export 身份分离且稳定。
18. **Release Everything**：影响结果的 Profile、Provider、Filter、Adapter、Recipe、Extractor、Markdown、Chunk、Embedding、Graph、Relation、Grounding、Validation、Model、Prompt、Research、Metric、Webhook、Export、Runtime 全部版本化。
19. **Idempotent + Recoverable**：任务有幂等键、lease、fencing token、checkpoint、retry、partial success。
20. **One Durable Task System**：PostgreSQL durable state + Transactional Outbox + Redis Streams 是唯一业务任务体系。
21. **External Automation Is Adapter, Not Truth**：n8n/MCP/CI/Agent 不能成为 Coverage、Task、Snapshot、Version 或 retry 状态真相。
22. **Projection Is Rebuildable**：全文、向量、Graph、AI、Relation、Annotation、Export、Dashboard 都可从事实层离线重建。
23. **Generation Instead of Destructive Rebuild**：索引/图谱/语义关系/AI 派生/Annotation 新版本后台构建、验证、原子切换。
24. **Security on Every Hop**：起始 URL、Redirect、Browser navigation、Webhook endpoint 每跳重新做 SSRF/Egress/Scope 校验。
25. **Source Friendly**：吞吐最大化不是唯一目标，robots、站点策略、分层限流和退避优先于速度。
26. **Benchmark Before Switch**：Adapter、Filter、Extractor、Markdown、Embedding、Graph、Retrieval、Grounding、Metric 升级先固定回归，再激活。
27. **Source Truth != AI Derivation**：摘要、Insight、FAQ、实体、研究问题不能写回 Canonical IR 或 Document Version。
28. **User Note != AI Insight**：用户自己的判断/批注与模型生成内容必须有不同 origin、权限和审计。
29. **Similarity Is Evidence, Not Identity**：向量、标题距离和 LLM 判定只产生关系证据，不能单独触发不可逆 Document 合并。
30. **Collapse Is Projection, Not Destructive Merge**：同文转载可在 UI 中折叠，但事实记录和来源引用保持可恢复。
31. **Research Expansion Is Bounded**：自动研究必须有 max depth/fanout/URL/domain/cost budget 和明确 stop reason。
32. **External Research != Managed Source**：搜索发现的外部 URL 不计入正式 Source Coverage，只有显式 Promote 后才走 PROBE/Backfill。
33. **Hybrid Retrieval First**：代码符号、错误信息、版本号和专有名词必须依靠 BM25/字段检索与 Vector 互补，不能只靠 Embedding。
34. **Provenance to Block Level**：AI Insight、检索片段和知识关系都应回溯到 Document Version + IR block；PDF/字幕还需页码或时间轴定位。
35. **Derived Knowledge Is Context, Not Sole Evidence**：AI Summary/Insight 可用于召回、排序和 query planning，但高可信回答和 quote 必须回到 Source Block。
36. **Ground Before Cite**：任何可引用 Claim 都要绑定 Evidence Span；无法绑定的派生物只能标为 PARTIAL/UNGROUNDED，不能冒充原文。
37. **Validate AI by Invariant**：Schema 正确只是最低门槛；还要验证引用、目标、证据和语义不变量，有限修复后失败即隔离。
38. **Annotation Over Mutation**：内部链接、高亮、关系提示优先作为独立 Annotation Projection，不修改基础原文或 Knowledge Note 内容。
39. **No Recursive Derivation Drift**：跟踪 derivation lineage/depth，禁止“摘要的摘要”长期递归成为唯一证据。
40. **Evidence Role Is Explicit**：每个派生输入明确标为 EVIDENCE / CONTEXT / QUERY_EXPANSION / RANKING_SIGNAL，不能混用。

---

## 3. 总体架构与真相边界

```text
                    ┌─────────────────────────────────────────────┐
                    │ Web / REST / n8n / MCP / CI / Agent       │
                    └───────────────────┬─────────────────────────┘
                                        │ Command / Query
                    ┌───────────────────▼─────────────────────────┐
                    │ API + RBAC + Idempotency + Policy          │
                    └───────────────────┬─────────────────────────┘
                                        │
                    ┌───────────────────▼─────────────────────────┐
                    │ PostgreSQL Truth Store                      │
                    │ Source/Profile/Run/Task/Observation         │
                    │ Coverage/Snapshot/Document/Version          │
                    │ Release/Audit/Outbox/Knowledge/Research     │
                    └──────────────┬────────────────┬─────────────┘
                                   │                │
                                Outbox            Query
                                   │                │
                        ┌──────────▼────────┐  ┌────▼──────────────┐
                        │ Redis Streams     │  │ Read Models       │
                        │ Task Transport    │  │ Search/Dashboard  │
                        └───────┬─────┬─────┘  └───────────────────┘
                                │     │
                     ┌──────────┘     └──────────────┐
                     ▼                               ▼
             Discovery Workers                Fetch Workers
      API/Sitemap/Feed/Archive/          HTTP Pool / Browser Pool
      CC/Deep Crawl/Surface              Crawl4AI / Playwright
                     │                               │
                     ▼                               ▼
            URL Observation ──> Durable Frontier ──> Snapshot
                                                       │
                                                       ▼
                                           Extract / Canonical IR
                                                       │
             ┌─────────────────────┬───────────────────┼────────────────────┐
             ▼                     ▼                   ▼                    ▼
     Canonical Markdown      Chunk/Search       Structured/Graph       Export Sink
                                   │
                                   ▼
                        Derived Knowledge Plane
                 Summary / Insight / FAQ / Entity / Question
                                   │
                                   ▼
                     Evidence Binding / Grounding
                                   │
             ┌─────────────────────┼─────────────────────┐
             ▼                     ▼                     ▼
      Semantic Relation       Annotation          Research Expansion
                                                       │
                                                External Evidence
                                                       │
                                           explicit Promote only
                                                       ▼
                                                Managed Source
```

真相边界：

```text
PostgreSQL + Object Storage = Truth
Redis Streams              = Transport
Crawler internal frontier  = Execution state only
Crawler dispatcher         = Worker-local execution control only
n8n execution              = External automation state only
OpenSearch / Vector Store  = Rebuildable Projection
Graph Store                = Rebuildable Projection
Canonical Markdown         = Rebuildable Projection from IR
Filtered/Enhanced Markdown = Rebuildable Derived Projection
AI Insight                 = Rebuildable Derived Knowledge
Semantic Relation          = Rebuildable/Reviewable Derived Knowledge
Link Annotation            = Rebuildable Presentation Projection
Dashboard Read Model       = Rebuildable Projection
SSE/WebSocket/Webhook      = Notification/Delivery only
Browser/session cache      = Ephemeral execution state
```

任何派生系统故障都不得把已经成功保存的 `Snapshot` 或 `Document Version READY` 回退成失败。

---

## 4. 技术选型

### 4.1 控制面

- **FastAPI + Pydantic**：REST/Command API、Schema 校验、Webhook Receiver、内部服务接口；
- **PostgreSQL**：Source、Profile、Run、Task、Coverage、Observation、Snapshot metadata、Document、Version、Release、Audit、Outbox、Knowledge/Research metadata；
- **Transactional Outbox**：保证业务数据库事务与异步消息发布一致；
- **Redis Streams**：consumer group、retry transport、backpressure，只做任务运输；
- **React/Vue + TypeScript**：Web 管理台；
- **Docker/Kubernetes**：按 Discovery / HTTP Fetch / Browser / Extract / Projection / Derive / Research Worker 独立扩缩容。

### 4.2 抓取与解析

- **HTTPX/aiohttp**：静态 HTML、Feed、Sitemap、API、Conditional GET、Range GET；
- **Crawl4AI**：URL Seeder、多 URL 流式抓取、Browser Interaction、Markdown/Structured Extraction Adapter、Worker 内 Dispatcher；
- **Playwright**：复杂 JS、登录前公开交互、滚动/点击等特殊流程 fallback；
- **lxml / BeautifulSoup / trafilatura / readability 类算法**：正文候选抽取；
- **PyMuPDF / pdftotext**：PDF 原生文本解析；仅扫描型 PDF 再进入 OCR fallback；
- **RSS/Atom/JSON Feed parser、XML Sitemap parser**：权威发现入口。

### 4.3 存储与检索

- **S3/MinIO**：原始响应、PDF、HTML、WARC-like Snapshot blob、Canonical IR、可选导出包；
- **OpenSearch**：BM25、字段过滤、聚合、短语/代码/错误信息检索；
- **pgvector**：首期向量检索与关系候选生成；规模或隔离需求显著增加时可替换 Qdrant/Milvus，但通过 Vector Adapter 屏蔽；
- **PostgreSQL JSONB**：小型结构化 evidence、release config、quality metrics；大文本不直接塞入任务表。

---

## 5. 核心数据模型

### 5.1 Source 与配置

```text
source
  id
  name
  base_url
  source_type            BLOG | DOCS | NEWSLETTER | OTHER
  status                 PROBING | ACTIVE | PAUSED | DISABLED
  profile_release_id
  crawl_policy_id
  sync_policy_id
  created_at / updated_at

source_profile_release
  id
  source_id
  version
  config_json
  fingerprint
  status                 DRAFT | TESTED | ACTIVE | RETIRED
  created_by
```

`config_json` 包含：允许域、路径 allow/deny、canonical 规则、Feed/Sitemap/CMS 入口、archive pattern、正文 selector、发布日期/作者规则、JS recipe、robots 策略、速率上限、语言、历史截止时间等。

### 5.2 发现与 Coverage

```text
discovery_run
  id
  source_id
  run_type               BACKFILL | INCREMENTAL | VERIFY
  provider               CMS | SITEMAP | FEED | ARCHIVE | TOC | CC | DEEP
  release_id
  cursor_json
  status
  exhausted_reason
  started_at / finished_at

url_observation
  id
  source_id
  discovered_url
  normalized_url
  provider
  provider_item_id
  provider_timestamp
  evidence_json
  filter_decision
  filter_reason
  observed_at

coverage_state
  source_id
  provider
  release_id
  checkpoint_json
  is_exhausted
  known_gap_json
  evidence_summary
  verified_at
```

对大规模 Observation 按时间或 source hash 分区；对 `(source_id, normalized_url, provider, provider_item_id)` 建幂等约束。

### 5.3 URL、抓取与 Snapshot

```text
url_alias
  id
  source_id
  observed_url
  normalized_url
  final_url
  canonical_url
  relation               REDIRECT | CANONICAL | TRACKING_VARIANT | ALIAS
  evidence_json

fetch_task
  id
  source_id
  normalized_url
  task_kind
  priority
  state
  attempt
  lease_owner
  lease_until
  fencing_token
  idempotency_key
  next_run_at

snapshot
  id
  source_id
  requested_url
  final_url
  fetched_at
  status_code
  headers_json
  redirect_chain_json
  mime_type
  body_object_key
  body_sha256
  byte_size
  fetch_release_id
  network_evidence_json
```

Snapshot 不可变。解析失败不删除 Snapshot；Extractor 升级可直接离线重放。

### 5.4 Document、Version 与 IR

```text
document
  id
  source_id
  canonical_identity
  canonical_url
  first_seen_at
  latest_version_id
  lifecycle              ACTIVE | MISSING | RETIRED | DELETED_HINT

document_version
  id
  document_id
  snapshot_id
  content_sha256
  semantic_hash
  extractor_release_id
  ir_release_id
  status                 READY | QUARANTINED
  title / author / published_at
  created_at

ir_block
  id
  document_version_id
  block_index
  block_type              HEADING | PARAGRAPH | CODE | LIST | TABLE | QUOTE | IMAGE | TRANSCRIPT
  text
  text_sha256
  locator_json
  attributes_json
```

`locator_json` 采用媒体类型相关定位器：

- HTML：`dom_path/css_hint + source_char_range + normalized_char_range`；
- PDF：`page + bbox + source_text_range`；
- Transcript：`segment_id + start_ms + end_ms + speaker + raw_text_hash`。

即使主业务是博客，也为 PDF、公开视频字幕等外部证据保留稳定锚点，避免以后只能引用“模型整理后的文本”。

### 5.5 派生知识与证据

```text
knowledge_note
  id
  origin                 AI | USER
  note_type               SUMMARY | INSIGHT | FAQ | ENTITY_NOTE | HYPOTHESIS
  source_version_id
  title
  body
  status                  READY | PARTIAL | UNGROUNDED | QUARANTINED
  derivation_release_id
  model_release_id
  prompt_release_id
  lineage_depth

evidence_span
  id
  knowledge_note_id
  document_version_id
  block_id
  start_offset
  end_offset
  evidence_kind           VERBATIM | NORMALIZED_TRANSCRIPT | PARAPHRASE
  anchor_status           BOUND | AMBIGUOUS | UNBOUND
  source_text_hash
  locator_override_json

semantic_relation
  id
  left_object_type / left_object_id
  right_object_type / right_object_id
  relation_type           SAME_WORK | REPUBLISHED | TRANSLATION_OF | RELATED | CONTRASTS | SUPPORTS
  confidence
  evidence_json
  relation_release_id
  review_status
```

关系是证据，不直接改写事实身份。

---

## 6. 新站点接入与 Probe

新增 Source 不直接全站开爬，先执行 `PROBE`：

1. DNS / TLS / robots / redirect / egress 安全检查；
2. 探测 `sitemap.xml`、robots 中 sitemap、RSS/Atom、JSON Feed；
3. 识别 WordPress、Ghost、Substack、自建 CMS、静态站、文档框架等特征；
4. 探测公开 CMS API、分页方式、archive/category/tag 页面；
5. 抽样 10～30 个 URL，比较 HTTP 抽取与 Browser 抽取质量；
6. 形成 Source Profile 草稿；
7. 运行 dry-run，仅产生 Observation/Preview，不进入大规模回填；
8. Web UI 审核后激活 Profile Release。

典型 Profile：

```yaml
source:
  allowed_hosts: [example.com, www.example.com]
  include_paths: [/blog/, /posts/]
  exclude_paths: [/tag/, /author/, /search/, /login/]

discovery:
  providers:
    - type: cms_api
      priority: 10
    - type: sitemap
      priority: 20
    - type: feed
      priority: 30
    - type: archive
      priority: 40

fetch:
  http_first: true
  browser_fallback: true
  requests_per_second: 0.5
  max_concurrency: 2

extract:
  preferred_selectors: [article, main]
  remove_selectors: [nav, footer, .newsletter-signup]
```

所有规则都属于版本化 Release，不能静默修改当前生产行为。

---

## 7. 全量历史发现与 Coverage 证明

### 7.1 Provider 优先级

按“权威性 + 历史覆盖”排序：

1. CMS/API：通常最适合完整分页和更新时间；
2. Sitemap/Sitemap Index：覆盖稳定、URL 规范；
3. Feed：适合近期增量，不应单独承担历史完整性；
4. Archive/年月页/分页列表；
5. Category/Tag、Docs TOC；
6. Common Crawl 等历史索引用于补洞；
7. 受控 Deep Crawl 只作为最后补洞手段。

### 7.2 Durable Streaming Frontier

Provider 每拉一页/一批即：

```text
fetch provider page
 -> parse items
 -> upsert url_observation in small batch
 -> persist provider cursor/checkpoint
 -> enqueue eligible URL tasks
 -> commit
 -> fetch next page
```

不得先把几十万 URL 装进 Python list，再一次入库。

### 7.3 Coverage 完成条件

`BACKFILL COMPLETE` 至少需要：

- 所有配置为 required 的 authoritative provider 明确耗尽；
- provider cursor/checkpoint 已持久化；
- known gap 已为空，或有人工接受理由；
- 发现 URL 数、可抓 URL 数、成功 Snapshot 数、被过滤数、失败数均可核对；
- 抽样比较 archive/sitemap/CMS 的年份、月份、分页边界；
- 对异常历史断层生成 Coverage Warning。

队列为空只能说明“当前没有待执行任务”，不能证明历史完整。

---

## 8. URL 规范化、过滤与身份

### 8.1 规范化

默认安全规则：

- scheme/host 小写；
- 去 fragment；
- 规范默认端口；
- 移除明确追踪参数 `utm_*`、`gclid` 等；
- query 参数是否排序/删除必须按站点 Profile 配置，不能全局激进处理；
- 保留 requested URL、final URL、canonical URL 三个概念；
- `<link rel=canonical>` 只是证据之一，不能盲目信任跨域 canonical。

### 8.2 Filter

过滤结果分：

```text
HARD           永不抓，例如登录、搜索结果、管理页
SOFT           默认不抓，但可人工/规则覆盖
DEPRIORITIZE   可以抓但低优先级，例如 tag 聚合页
ALLOW          正常进入正文抓取
```

每次决定保存 `filter_release_id + reason + evidence`，以后规则变化可重新判断历史 Observation。

---

## 9. Durable Task、幂等与恢复

### 9.1 唯一业务任务体系

PostgreSQL 保存 durable Task state；Transactional Outbox 在同一事务写待发布事件；Publisher 把事件投递 Redis Streams；Worker 用 consumer group 消费。

Redis 丢失可以从 PostgreSQL 重建运输队列，不影响事实状态。

### 9.2 Task 状态

```text
PENDING -> LEASED -> RUNNING -> SUCCEEDED
                    |          |
                    |          -> PARTIAL
                    -> RETRY_WAIT -> PENDING
                    -> QUARANTINED
                    -> DEAD
```

Worker 领取任务时获得 `lease_until + fencing_token`。超时后新 Worker 可重新 lease；旧 Worker 即使恢复，也不能用过期 token 覆盖新结果。

### 9.3 幂等键

示例：

```text
DISCOVER: source_id + provider + release_id + cursor
FETCH: source_id + normalized_url + sync_run_id
EXTRACT: snapshot_id + extractor_release_id
MARKDOWN: version_id + markdown_release_id
EMBED: version_id/chunk_id + embedding_release_id
DERIVE: version_id + derivation_release_id + note_type
```

每阶段都必须允许安全重放。

---

## 10. 抓取路由、限流与 Browser Pool

### 10.1 Resource Probe

正式抓正文前执行轻量 Probe：

- DNS/IP/SSRF 检查；
- HTTP status、Content-Type、Content-Length；
- redirect 每跳重新校验；
- magic bytes / HTML 壳检测；
- 是否需要 JS；
- 是否命中 robots/站点策略；
- 条件请求能力（ETag/Last-Modified）。

### 10.2 路由策略

```text
CMS/API/Feed/Sitemap -> HTTP
static HTML          -> HTTP -> extractor quality gate
PDF/file             -> HTTP stream -> snapshot -> parser
JS HTML              -> HTTP quality fail -> Crawl4AI Browser
special interaction  -> Playwright recipe
```

Browser 绝不作为默认通道，否则 1000 站全量历史抓取的成本和稳定性都会显著恶化。

### 10.3 分层 Permit

同时受以下上限约束：

```text
global concurrency
 -> domain concurrency/rate
 -> source concurrency/rate
 -> worker-local dispatcher/resource monitor
```

收到 429/503、延迟上升、内存压力、Browser crash 时自动降低局部并发，并采用带 jitter 的指数退避。

### 10.4 Browser 复用与隔离

- Browser 进程长期复用；
- Context/Page 短生命周期；
- Cookie/LocalStorage/session 按 Source 或安全域隔离；
- page 数、RSS、任务数、运行时达到阈值主动 recycle；
- 禁止共享认证态跨 Source；
- Browser 下载、弹窗、外部导航均受 egress policy 限制。

---

## 11. Snapshot First 与内容解析

抓取成功后先写 Snapshot：

```text
network result
 -> stream body to object storage
 -> compute sha256
 -> persist snapshot metadata
 -> commit
 -> emit SNAPSHOT_READY
```

之后 Extract Worker 从 Snapshot 读取，不再依赖源站在线。

正文抽取采用多候选而不是单库锁死：

```text
site selector candidate
trafilatura candidate
readability candidate
Crawl4AI candidate
DOM density candidate
```

统一计算质量：正文长度、标题一致性、链接密度、重复导航比例、代码块保留率、表格完整性、时间/作者命中、模板污染、语言异常等。选择结果和评分保存到 `extraction_evidence`。

抽取器升级必须通过固定 Golden Corpus 回归：技术文章、含大量代码文章、表格文章、多页文章、Newsletter、国际化文本、动态站、边界失败样本都要覆盖。

---

## 12. Canonical IR、Markdown 与多模态锚点

### 12.1 IR 先于 Markdown

Canonical IR 是结构化事实：

```text
DocumentVersion
  metadata
  blocks[]
    heading(level, text)
    paragraph(text)
    code(language, text)
    list(items)
    table(cells)
    quote(text)
    image(src, alt, caption)
```

Markdown 只是 IR 的确定性 Renderer。这样后续 Markdown 格式变化时无需重抓源站。

### 12.2 Markdown 规则

- 标题层级稳定，不从正文臆造标题；
- 代码块保留语言标签和原始空白；
- 表格能稳定转换则使用 GFM，否则回退 HTML block 并标记；
- 图片保留绝对原链接及 alt/caption，可选独立镜像；
- 内部相对链接转绝对 canonical URL；
- 删除脚本、导航、Cookie banner、相关推荐、广告，但保留正文引用和脚注；
- 不把 LLM 摘要、Insight、自动内部链接写入 Canonical Markdown。

建议导出：

```text
knowledge/
  sources/{source_slug}/{yyyy}/{slug}--{document_id}.md
```

Front Matter：

```yaml
---
document_id: doc_xxx
version_id: ver_xxx
source: source_slug
source_url: https://...
canonical_url: https://...
title: ...
author: ...
published_at: ...
fetched_at: ...
content_sha256: ...
---
```

路径和标题变化不影响内部 `document_id/version_id`。

### 12.3 PDF 与 Transcript

可选外部证据遵循与 HTML 相同的 Snapshot First 原则：

- PDF：先保存原文件；原生文本 parser 优先；只有扫描页才 OCR；IR block 保存页码/bbox；
- Transcript：优先来源可验证字幕；保留原始 segment、`start_ms/end_ms`、speaker（若有）和 raw text；清理口语产生 normalized transcript 时保存 raw→normalized 对齐映射。

因此“清理后的字幕”永远不会被错误标成逐字引用。

---

## 13. 增量同步、版本与删除检测

### 13.1 调度

每 Source 有同步策略：

- 高频站：15～60 分钟发现 Feed/Sitemap/API 增量；
- 普通博客：数小时到每日；
- 低频站：按历史发布频率自适应；
- 定期低频 Coverage Verify 检查旧内容被补录、迁移或删除。

实际策略在 Web UI 配置，不把固定周期写死在代码。

### 13.2 Conditional Fetch

存在 ETag/Last-Modified 时发送 `If-None-Match/If-Modified-Since`。304 记 Observation，不新建 Document Version。

200 响应按：

```text
raw body hash
 -> extraction
 -> canonical IR/content hash
 -> semantic comparison
```

只有 Canonical 内容变化才创建新 Version。模板、追踪参数、广告变化不应制造正文版本噪声。

### 13.3 删除/失踪

单次 404/5xx 不立即删除。使用状态机：

```text
ACTIVE -> SUSPECT_MISSING -> MISSING
       -> ACTIVE（再次出现）
```

结合 Sitemap/CMS 消失、连续多次 404、站点迁移、redirect 等证据，原历史 Snapshot 永久保留。

---

## 14. 去重、转载、翻译与语义关系

使用分层候选：

1. exact canonical content hash；
2. SimHash/MinHash 或 shingles；
3. 标题/作者/发布时间；
4. embedding 近邻；
5. 必要时 LLM 只做关系判别补充证据。

输出不是“直接合并”，而是 `semantic_relation`：

```text
SAME_WORK
REPUBLISHED
TRANSLATION_OF
RELATED
CONTRASTS
SUPPORTS
```

Web UI 可按关系折叠 SAME_WORK/REPUBLISHED，但底层每个来源、URL、Snapshot、Version 都保留。误判只需撤销 Relation，不需要恢复被删除的数据。

Embedding 阈值、标题距离、LLM Prompt、模型版本都写 `relation_release_id/evidence_json`，关系可重新构建。

---

## 15. Derived Knowledge：Summary、Insight Card 与证据绑定

当 `Document Version READY` 后，通过 Outbox 异步触发：

```text
DERIVE_SUMMARY
DERIVE_INSIGHT
EMBED_BLOCK/CHUNK
RELATE
OPTIONAL_RESEARCH
```

这些任务失败不影响正文 Version READY。

### 15.1 Insight Card

Insight 是独立可检索的派生知识对象，适合把长文章拆成小颗粒观点：

```text
knowledge_note
  note_type = INSIGHT
  title
  body
  source_version_id
  evidence_span_ids[]
  derivation_release_id
  confidence/status
```

生成时要求模型返回“观点 + evidence hint”，随后由程序做 Evidence Binding，而不是直接信任 `snippet`。

### 15.2 Evidence Binding

流程：

```text
LLM claim + evidence hint
 -> exact normalized match
 -> resolve block/offset
 -> transcript alignment when applicable
 -> fuzzy match only creates candidate
 -> persist source_text_hash
 -> grounding policy
```

规则：

- `VERBATIM`：必须精确回到原文 block/span；
- `NORMALIZED_TRANSCRIPT`：允许口语清理，但必须能映射回原字幕时间段；
- `PARAPHRASE`：只能显示为概括，不能加引号伪装原文；
- `UNBOUND`：允许保留为低可信 Insight，但不能进入可引用证据通道。

### 15.3 结构化输出与有限修复

LLM 输出至少经过：

1. Schema 校验：type/required/enum；
2. Referential 校验：version/block/target 必须存在；
3. Grounding 校验：证据能绑定；
4. Semantic Invariant：例如“只加链接”时剥离链接后的正文必须 byte/normalized 等价。

修复循环最多有限次数，仍失败进入 Quarantine，并记录：模型、Prompt、输入版本、错误类型、token/cost、repair 次数。

---

## 16. Annotation：链接与展示增强不改正文

自动内部链接、高亮、主题提示、关系入口都保存为 Annotation：

```text
annotation
  target_version_id / knowledge_note_id
  span
  annotation_type
  target_object_id
  release_id
```

渲染时叠加，不写回 Canonical IR，也尽量不直接修改 AI Knowledge Note。

若某流程必须生成增强文本，至少执行“不改变正文”的不变量验证：剥离所有新增标记后必须与输入文本完全一致；目标 ID 必须来自允许集合。

---

## 17. 自动研究闭环与外部来源治理

Insight、Article、Search 可生成有限数量研究问题，但研究必须与正式 Source Sync 隔离。

```text
knowledge object
 -> generate research question
 -> ResearchRun
 -> external search provider
 -> candidate URLs
 -> policy/filter/ranking
 -> ExternalEvidence ingest
 -> derive/ground
 -> optional follow-up
```

每个 ResearchRun 必须配置：

```text
max_depth
max_queries
max_urls
max_domains
max_new_documents
max_model_tokens
max_search_cost
max_wall_time
```

达到预算即保存 `stop_reason`。

搜索发现的 URL 默认属于 `EXTERNAL_EVIDENCE`，不增加任何 Managed Source Coverage；只有管理员点击 Promote，才创建 Source Candidate，执行 PROBE、Profile、Backfill。

这样既能得到主动知识发现能力，又不会让自动研究无限扩散成失控爬虫。

---

## 18. Search / RAG

### 18.1 索引对象

- Document metadata；
- IR Block / Chunk；
- Canonical Markdown projection；
- AI Summary/Insight 作为单独索引类型；
- User Note 单独权限域。

### 18.2 Hybrid Retrieval

```text
query
 -> metadata/source/date/language filter
 -> BM25 candidates
 -> vector candidates
 -> union/RRF
 -> optional reranker
 -> diversity + relation collapse
 -> evidence expansion to source blocks
```

技术知识库不能只依赖向量：版本号、API 名称、异常字符串、代码符号、命令参数等由 BM25/字段检索更可靠。

### 18.3 RAG Grounding

Answer Planner 可以用 Summary/Insight 做 query expansion/ranking/context，但最终高可信 Claim 必须指向 `Document Version + Block + Evidence Span`。

防止递归漂移：

- AI Summary 的 Summary 不允许无限生成；
- lineage_depth 超过阈值的派生物不能作为唯一 EVIDENCE；
- Derived Knowledge 默认角色是 CONTEXT/RANKING_SIGNAL；
- 需要引用时强制回源。

---

## 19. Web 管理功能

### 19.1 Source 管理

- Source 列表、状态、标签、Owner；
- 新增/编辑 Source；
- Probe 结果与 Profile 草稿；
- Feed/Sitemap/API/Archive 探测；
- allow/deny path、canonical、selector、Browser recipe 编辑；
- Profile Release Diff、测试、激活、回滚。

### 19.2 抓取与 Coverage

- Backfill/Incremental/Verify Run；
- Provider cursor、exhaustion、known gaps；
- discovered / allowed / fetched / failed / filtered 计数；
- 年/月分布热图，快速发现历史断层；
- Task retry/quarantine/dead-letter；
- 域名限流、429、Browser fallback、资源消耗。

### 19.3 文档与版本

- Document/Version 列表；
- requested/final/canonical URL；
- Snapshot headers、redirect、hash；
- 原始 HTML 与 Canonical IR/Markdown 对比；
- 版本 Diff；
- 重新 Extract/Render/Chunk/Embed，不重新访问网络；
- 导出单篇、Source、时间段或全库 Markdown。

### 19.4 派生知识

- Summary/Insight/FAQ/Entity；
- Evidence Span 定位预览；
- Grounding 状态；
- Relation review/撤销；
- Research Run 预算、外部候选、Promote；
- User Note 与 AI Note 清晰分栏。

### 19.5 权限与审计

RBAC 至少包含：Viewer、Operator、Editor、Admin。所有修改 Profile、重跑、Promote、Relation 审核、删除、导出、Webhook 密钥操作写 Audit Log。

---

## 20. API、Webhook、n8n 与 MCP

核心平台提供受控接口：

```text
POST /sources
POST /sources/{id}/probe
POST /sources/{id}/runs/backfill
POST /sources/{id}/runs/incremental
GET  /sources/{id}/coverage
GET  /runs/{id}
POST /tasks/{id}/retry
POST /snapshots/{id}/reextract
GET  /documents/{id}/versions
POST /exports
POST /research/{id}/promote
```

写接口支持 `Idempotency-Key`。

Webhook 只发布事实事件，例如：

```text
source.run.completed
document.version.ready
document.version.changed
task.quarantined
coverage.warning
research.candidate.ready
```

n8n/MCP/Agent 可以调用 API 或消费 Webhook，但不得直写核心数据库、直接 ack Redis 业务状态或自行定义 Coverage。

---

## 21. 安全、合规与源站友好

### 21.1 SSRF/Egress

每一次：

- 用户输入 URL；
- HTTP redirect；
- Browser navigation；
- PDF/full-text follow link；
- Webhook callback；

都重新解析 DNS/IP，阻止 loopback、link-local、RFC1918、metadata endpoint、未允许端口和协议。DNS rebinding 需要连接前后校验。

### 21.2 内容安全

- 响应 body 最大尺寸；
- 压缩比限制，防 decompression bomb；
- PDF/附件解析放沙箱 Worker；
- Browser 禁止任意文件系统访问；
- 渲染 Markdown/HTML 做 XSS sanitize；
- Secret 进入 Vault/K8s Secret，不写 Profile 明文。

### 21.3 爬取策略

- robots 与站点 ToS/政策按组织要求执行；
- 明确 User-Agent 和联系方式；
- 低速默认、按域名退避；
- 429/Retry-After 优先遵守；
- 对登录墙、付费墙和禁止访问内容不绕过控制。

---

## 22. 可观测性与质量指标

### 22.1 抓取指标

```text
fetch_success_rate
http_304_rate
429_rate / 403_rate
p50/p95 latency
bytes_downloaded
browser_fallback_rate
browser_crash_rate
retry_rate
queue_lag
```

### 22.2 Coverage 指标

```text
provider_exhausted_ratio
known_gap_count
historical_year_coverage
observation_to_document_ratio
failed_url_count
unresolved_redirect_count
```

### 22.3 内容质量

```text
extraction_quality_score
boilerplate_ratio
code_block_preservation
metadata_confidence
version_churn_rate
markdown_render_error
```

### 22.4 AI/RAG

```text
grounding_bound_ratio
ungrounded_note_ratio
invalid_quote_ratio
relation_review_reject_rate
retrieval_recall@k
answer_source_coverage
model_token/cost per source/version
```

日志使用 `trace_id/run_id/task_id/source_id/document_id` 串联；Prometheus/Grafana 做指标，OpenTelemetry 做 trace。

---

## 23. Projection Generation、升级与回放

全文索引、向量、Graph、Derived Knowledge、Annotation 都使用 generation：

```text
build generation N+1 in background
 -> validate corpus/metrics
 -> compare with active generation N
 -> atomic alias/pointer switch
 -> keep N for rollback window
```

Extractor/Markdown Release 升级同样先跑固定回归集，再选择全量离线重处理。因为 Snapshot 已保存，不需要重访问 1000 个源站。

Release Registry 至少记录：

```text
profile_release
fetch_adapter_release
extractor_release
ir_release
markdown_release
chunk_release
embedding_release
relation_release
derivation_release
grounding_release
annotation_release
research_release
```

---

## 24. 存储、备份与生命周期

### 24.1 PostgreSQL

- 核心事实表定期 PITR/WAL 备份；
- 大表按时间或 source hash 分区；
- Task/Observation 历史可冷热分层，但幂等键和审计不可随意清除；
- 定期检查 FK、孤儿对象、outbox backlog。

### 24.2 Object Storage

建议 key：

```text
snapshots/{source_id}/{yyyy}/{mm}/{snapshot_id}/body
ir/{document_id}/{version_id}/{ir_release}.json
exports/{export_id}/...
```

开启 versioning/生命周期策略。原始 Snapshot 的保存周期按合规与成本政策决定，但不能因为重新生成 Markdown 而覆盖原始对象。

### 24.3 索引

OpenSearch/Vector 均视为缓存式 Projection；必须有从 PostgreSQL + Object Storage 重建的作业和演练。

---

## 25. 部署与横向扩展

Worker 分池：

```text
discovery-worker
http-fetch-worker
browser-fetch-worker
extract-worker
projection-worker
embed-worker
derive-worker
relation-worker
research-worker
export-worker
```

原因：Browser、Embedding、LLM、网络抓取的资源模型完全不同，不能让 Browser 内存峰值阻塞普通 HTTP 抓取。

Kubernetes HPA 指标：queue lag、CPU、memory、Browser slots、LLM budget，不只看 CPU。

对于 1000 Source：

- 默认每域低并发；
- 横向增加不同域名 Worker，而不是提高单站并发；
- Backfill 与 Incremental 使用不同 priority，增量任务优先，避免历史回填淹没新内容；
- Research 使用独立 budget/queue，绝不抢占 Managed Source 关键同步资源。

---

## 26. 两条关键执行链

### 26.1 首次历史回填

```text
Create Source
 -> PROBE
 -> Profile Review/Activate
 -> BACKFILL Run
 -> authoritative provider discovery
 -> streaming Observation + checkpoint
 -> filter/canonicalize/dedupe
 -> Fetch Task
 -> HTTP/Browser
 -> immutable Snapshot
 -> Extract
 -> Canonical IR
 -> Document/Version READY
 -> Markdown/Search Projection
 -> async Derive/Embed/Relate
 -> Coverage Verify
 -> COMPLETE or COMPLETE_WITH_GAPS
```

### 26.2 增量同步

```text
schedule
 -> Feed/Sitemap/API/Archive incremental discovery
 -> Observation
 -> new URL: fetch
 -> known URL: conditional fetch
 -> 304: observation only
 -> 200 same canonical hash: no version
 -> 200 changed canonical hash: new version
 -> rebuild affected projections only
 -> emit change event
```

---

## 27. 实施阶段

### Phase 1：事实层与 20～50 个站点验证

- Source/Profile/Release；
- Sitemap/Feed/CMS/Archive Discovery；
- PostgreSQL durable Task + Outbox + Redis Streams；
- HTTP Fetch、Snapshot、Object Storage；
- 基础 IR/Markdown；
- Web Source/Run/Task/Document 页面；
- ETag/Last-Modified 增量；
- Markdown 导出。

### Phase 2：1000 站全量能力

- Durable Streaming Frontier；
- Browser Pool/Crawl4AI/Playwright recipe；
- Coverage Evidence/Gap Dashboard；
- 多候选 Extractor + Golden Corpus；
- Backfill/Incremental 优先级和自动限流；
- 版本 Diff、离线 Reprocess；
- OpenSearch/pgvector。

### Phase 3：知识派生与主动研究

- Summary/Insight Card；
- Evidence Binding/Grounding；
- non-destructive Semantic Relation；
- Hybrid RAG；
- bounded Research Expansion；
- External Evidence Promote；
- Annotation Projection；
- PDF/Transcript 可选证据通道。

### Phase 4：高可用与治理

- 多副本 API/Worker；
- DB/对象存储备份演练；
- Projection generation 原子切换；
- 安全扫描、SSRF/Egress 强化；
- 成本预算、SLO、告警、审计与权限完善。

---

## 28. 验收标准

### 全量历史

- 随机选择不同 CMS/静态站/文档站，能给出 Provider Coverage Evidence；
- Worker 中断后从 checkpoint 继续，不重复制造 Document Version；
- 百万级 URL Discovery 保持有界内存。

### 增量

- 新文章按 Source SLA 被发现；
- 304 不创建新 Version；
- 模板变化不制造大量假版本；
- 正文变化只重建受影响派生物。

### Markdown

- 标题、正文、代码块、表格、列表、引用、链接可稳定重建；
- 同一 IR + Markdown Release 输出确定；
- 任意导出 Markdown 可回溯到 `document_id/version_id/snapshot_id`。

### 可恢复性

- Redis 清空后可从 DB 恢复待办；
- Worker lease 超时不会让旧 Worker 覆盖新 Worker；
- Snapshot 存在时 Extractor/Markdown/Embedding 可完全离线重跑。

### 知识与 RAG

- AI Insight 与 User Note、Source Fact 身份分离；
- 可引用 Insight 的 Evidence Span 能回到原文 block；
- Transcript 清理文本不会伪装成 verbatim quote；
- 相似关系误判可撤销，无需恢复被合并数据；
- RAG 的关键 Claim 可回源，不依赖递归摘要。

### Web 管理

- 新 Source 能通过 Probe + 配置接入；
- 能查看 Coverage、失败、版本 Diff、Snapshot、Release 和任务状态；
- 能重试、暂停、重处理、导出、审核关系、Promote 外部来源；
- 所有高风险管理操作可审计。

最终系统应把“抓到网页”升级为一套可证明 Coverage、可重放事实、可稳定增量、可安全派生知识的长期数据平台：抓取层负责可靠保存来源事实，IR/Markdown 层负责可复现清洗，搜索/RAG 与 Insight/Research 层负责可重建的知识增强，所有自动化都不能越过事实与证据边界。