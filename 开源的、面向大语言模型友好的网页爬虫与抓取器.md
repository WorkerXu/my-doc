# 开源的、面向大语言模型友好的网页爬虫与抓取器

## 1. 调研对象与结论

- 编号：15
- 原文：Open-source LLM Friendly Web Crawler & Scraper
- 来源：Substack / Cobus Greyling
- 地址：https://cobusgreyling.substack.com/p/open-source-llm-friendly-web-crawler
- 发布时间：2025-04-15
- 相关项目：Crawl4AI
- 官方项目：https://github.com/unclecode/crawl4ai
- 官方文档：https://docs.crawl4ai.com/

原文的核心判断是：LLM/RAG 的数据交付不能只依赖固定 API，网页抓取需要同时解决实时访问、动态渲染、异步并发、站内发现、正文清洗和 Markdown/JSON 输出。文章以 Crawl4AI 为例，提到了 Playwright/HTTP、异步并发、MemoryAdaptiveDispatcher、Deep Crawl、Docker/FastAPI/JWT、代理、Markdown、Question-based Crawl、PDF/图片/iframe 等能力。

对“1000+ 技术博客全量历史 + 长期增量同步 + Web 管理”的生产系统，本次结论是：

1. Crawl4AI 很适合做 **Crawler Adapter / Worker Runtime**，但不应成为业务控制面或事实数据库；
2. 原文强调的 async、Browser、Deep Crawl、Markdown 能力都能显著降低抓取实现成本，但“是否抓全、是否变化、是否可回放”仍必须由平台自己证明；
3. 当前 Crawl4AI 已进一步提供 Deep Crawl resume/cancel、Prefetch、AdaptiveCrawler、iframe 处理、Shadow DOM flatten、流式多 URL、代理策略等能力，方案可以利用，但必须明确每项能力的业务边界；
4. 本次最重要的新增架构优化不是换 crawler，而是补齐 **Network Snapshot + Rendered DOM 双层事实、动态 hydration 可回放、iframe/Shadow DOM provenance、Adaptive Crawl 与历史 Coverage 分离、Crawler Docker 内网边界**；
5. 原文中“通过模拟人类行为绕过 CAPTCHA”“代理轮换避免限流”这类表述不能直接转化为生产默认策略；平台应优先遵守 robots、Retry-After、访问控制和组织合规策略。

---

## 2. 原文能力拆解

文章可拆成六层能力：

```text
访问层
  HTTP / Browser / JavaScript rendering

执行层
  asyncio / arun_many / Dispatcher / rate limiting

发现层
  Deep Crawl / sub-page exploration / query-guided crawl

内容层
  cleaned HTML / Markdown / JSON / extraction

部署层
  Docker / FastAPI / authentication

LLM 数据层
  chunking / RAG / real-time data delivery
```

这六层中，Crawl4AI 可以覆盖前五层的大部分执行能力，但生产知识库还缺少三个平台级问题：

```text
Coverage truth
  到底抓全了没有？

Data provenance
  这段 Markdown 到底来自哪个网络响应/渲染 DOM？

Long-term lifecycle
  以后 crawler/selector/markdown 算法升级，旧结果还能否解释和重建？
```

因此项目的职责边界应该是：

```text
Crawl4AI
  = execution engine

PostgreSQL + Object Storage
  = truth store

Canonical IR
  = content fact layer

Markdown/Search/Vector
  = rebuildable projections
```

---

## 3. AsyncWebCrawler：为什么适合 1000 个博客

### 3.1 技术原理

网页抓取大部分时间在等待：DNS、TLS、远端响应、网络传输、Browser 页面加载。asyncio 的 event loop 可以让一个任务等待 I/O 时继续推进其他任务，所以跨大量不同域名时，比逐 URL 串行执行的资源利用率高很多。

但 async 不等于无限并发。真正安全的执行链应是：

```text
PostgreSQL FetchTask
 -> lease bounded window
 -> acquire cluster global/domain/source permit
 -> crawler arun_many(stream=True)
 -> each result immediately persist
 -> release permit
```

Worker 一次只处理有界窗口，例如 20～100 URL，而不是把整个站点几十万 URL 一次性传给 crawler。

### 3.2 `stream=True` 的价值

流式结果的关键价值是 **逐 URL 提交事实**：

- URL A 成功后立即保存 Snapshot；
- URL B 超时不会拖住 A；
- URL C 失败只重试自己；
- Worker 崩溃时已经成功的 URL 不需要回滚；
- lease/fencing token 可以把失败恢复粒度控制在单 URL。

因此 `arun_many(stream=True)` 应当映射到平台的 `Bounded Window + Stream + Commit Per URL`，而不是被理解成“给前端展示进度”这么简单。

---

## 4. MemoryAdaptiveDispatcher：只能控制本机，不能替代平台调度

当前 Crawl4AI 的 MemoryAdaptiveDispatcher 会根据本机内存占用控制并发，并可配合 `max_session_permit`、RateLimiter、CrawlerMonitor 等机制。

它解决的是：

```text
这个 Worker 现在还能开多少任务？
```

而平台要解决的是：

```text
整个集群现在允许对 example.com 发多少请求？
这个 Source 的限速是多少？
Backfill 是否应该让位给 Incremental？
```

例如 20 个 Worker，每个本地 Dispatcher 都允许同域并发 5 个任务，那么整个集群仍可能瞬间对一个站点产生 100 并发。因此正确模型是：

```text
平台层
 global concurrency
  -> domain token bucket/concurrency
   -> source quota/policy
    -> worker lease window

Worker 层
 MemoryAdaptiveDispatcher / SemaphoreDispatcher / RateLimiter
```

平台 Permit 是硬边界；Crawler Dispatcher 是本机二级保护。

429/503 时，Crawler 的随机延迟和退避可以保留，但证据应回传平台 domain circuit breaker；`Retry-After` 应优先于本地默认参数。

---

## 5. HTTP First / Browser Last：真实成本模型

原文强调 Crawl4AI 可以用 Browser 访问动态页面，这一点对现代博客很重要，但 1000 个站点不能默认全部走 Chromium。

Browser 成本包括：

- Chromium 常驻内存；
- Context/Page 内存；
- JavaScript CPU；
- 字体、图片、广告、分析脚本的额外网络；
- 弹窗、下载、导航、页面 crash；
- hydration 等待带来的长尾延迟。

推荐路由：

```text
CMS / Feed / Sitemap / API
 -> HTTP

普通 HTML
 -> HTTP
 -> extraction quality gate
 -> pass: Extract
 -> JS shell / quality fail: Browser fallback

必须点击/滚动/复杂交互
 -> versioned Browser Recipe
```

Browser 进程可 Worker 级复用，但 Cookie/LocalStorage/认证态/代理身份不能跨 Source 复用。

---

## 6. 一个关键生产问题：Network HTML 不等于 Browser 最终看到的 DOM

这是原文强调“动态页面处理”后，对知识库方案最重要的架构推论。

典型 SPA 页面首次网络响应可能只有：

```html
<div id="app"></div>
<script src="bundle.js"></script>
```

真正文章正文在 JavaScript 执行、API 请求和 hydration 之后才进入 DOM。如果平台只保存最初网络 HTML，却把 Browser 抽取出的 Markdown 当事实，那么以后离线重跑 Extractor 时根本无法重现当时正文。

因此 Browser 路由必须保存两层事实：

```text
Network Snapshot
  status / headers / redirect / raw body / hash

Render Artifact
  final rendered DOM
  wait strategy
  JS/interactions
  iframe/shadow transform manifest
  browser/runtime release
  optional MHTML/screenshot
```

正确链路：

```text
Browser navigation
 -> save network fact
 -> run versioned wait/interactions
 -> capture final rendered DOM
 -> persist Render Artifact
 -> Extract from stored Render Artifact
 -> Canonical IR
```

这样 Extractor/Markdown 升级后，不访问源站也能重新处理动态正文。

这比单纯保存 `result.markdown` 更重要，因为 Markdown 已经是经过 crawler cleaning/renderer 的派生结果。

---

## 7. Hydration、等待条件与结果稳定性

动态站点的难点并不只是“使用 Playwright”，而是 **什么时候认为页面已经准备好**。

固定 `sleep(3)` 容易导致：

- 网络快时浪费时间；
- 网络慢时抓到半成品；
- 同一 URL 两次正文长度不同；
- 因等待抖动制造假版本。

更合理的优先级：

```text
明确 selector / JS predicate
 -> 应用明确状态或事件
 -> 通用 wait_until
 -> bounded fixed delay fallback
```

Recipe 必须版本化保存：

```text
wait_for
wait_until
delay_before_return_html
js_code / interaction
scroll/load-more
viewport
locale/timezone
process_iframes
flatten_shadow_dom
```

新站点 Probe 应对动态样本重复抓 2～3 次，比较 Rendered DOM/Canonical content 稳定度，再激活 Recipe。

---

## 8. iframe 与 Shadow DOM：内容能抓到，还必须能解释来源

当前 Crawl4AI 支持 `process_iframes=True` 将 iframe 内容并入处理结果，也支持 `flatten_shadow_dom=True` 将 Web Components 的 Shadow DOM 展平到主文档。

### 8.1 iframe

问题在于：把 iframe HTML 直接合并到主 DOM 后，内容来源可能丢失。特别是跨域 iframe，Markdown 最后看起来像主站正文，但事实来自第三方域名。

因此平台应：

```text
main Render Artifact
 -> iframe discovered
 -> egress/scope validation
 -> optional child Snapshot/Render Artifact
 -> embedded_resource relation
 -> IR block provenance points to child source
```

抓不到 child artifact 时，至少保留 iframe URL、同源/跨域、处理结果和失败证据。

### 8.2 Shadow DOM

Shadow DOM 是同一页面内部的封装子树，普通 `page.content()` 可能看不到。flatten 能解决可见内容缺失，但它本身属于 DOM 变换，因此必须记录：

```text
flatten_shadow_dom=true
runtime/browser release
transform manifest
locator/provenance
```

不能让“某版本 crawler 自动展开了 closed shadow roots”成为不可追溯的隐式行为。

---

## 9. Deep Crawl：BFS/DFS/BestFirst 的业务边界

### BFS

按层展开，适合未知站点的广度 gap discovery，容易解释。

### DFS

沿分支深入，适合 archive/category 的深分页，但必须设置 `max_depth/max_pages/max_wall_time`，避免日历、标签和参数页爆炸。

### BestFirst

按 scorer 优先相关 URL，适合预算有限的主题探索，但天然带选择偏差，不能用来证明“全站历史抓全”。

因此：

```text
Authoritative provider exhausted
 + gap providers material-new-doc rate low
 + known gaps explained
 != BestFirst queue stopped
```

Crawler 自带 URLPatternFilter/DomainFilter/Scorer 只做前置执行优化；最终 Scope/Filter 决策仍由平台版本化规则完成。

---

## 10. Deep Crawl resume/cancel/prefetch：如何接平台

### 10.1 Runtime checkpoint

当前 Deep Crawl 支持 crash recovery。可保存 visited/pending/depth/pages 等执行状态并继续。

正确定位：

```text
Deep Crawl runtime state
 = execution checkpoint / acceleration hint

URL Observation + Coverage State
 = platform truth
```

checkpoint 只有在以下 fingerprint 一致时才允许恢复：

```text
source/profile release
crawler runtime release
strategy
start seed
allowed scope
max_depth/max_pages
filter/scorer
prefetch mode
```

指纹不一致就丢弃 runtime state，从 seed 重跑；Observation 幂等吸收重复。

不要每个 URL 同步写完整 frontier JSON。更合理的是 Observation 小批持续提交，runtime state 每 N URL 或 T 秒 coalesce 后持久化。

### 10.2 Cancellation

Web 取消：

```text
Run -> CANCEL_REQUESTED
 -> crawler should_cancel / cancel
 -> finish current safe boundary
 -> flush checkpoint
 -> CANCELLED
```

已提交 Observation/Snapshot 不回滚。

### 10.3 Prefetch

Prefetch 的价值是把“发现 URL”和“生成正文”拆开：

```text
HTML + fast link extraction
 -> Observation
 -> platform scope/filter
 -> eligible URL
 -> full fetch/extract
```

Sitemap/API/Feed 不需要为了统一工具去 Browser Prefetch；Prefetch 主要适合动态链接图和受控 Deep Crawl。

---

## 11. Link Discovery Scope 与 Content Extraction Scope 必须分离

当前 Crawl4AI 的内容选择能力允许只对 `article/main` 等元素做 Markdown/Extraction，同时仍可分析页面链接。这个能力揭示了一个容易被忽略的设计原则：

```text
正文应该尽量窄
发现链接往往应该更宽
```

博客 archive/navigation 中指向历史文章的链接通常不在正文 `article` 内。如果平台把同一个 CSS selector 同时用于正文和发现，就可能得到很干净的 Markdown，却漏掉历史 URL。

因此 Source Profile 应分别配置：

```text
content_scope
link_discovery_scope
```

发现阶段再用独立 allow/deny/filter 控制 URL 图。

---

## 12. AdaptiveCrawler / Question-based Crawl：不能与历史 Coverage 混用

原文提到“question-based crawler”可以根据自然语言问题寻找相关内容。当前 Crawl4AI 的 AdaptiveCrawler 会根据 query 评估：

```text
coverage
consistency
saturation
confidence
```

然后在“信息已经足够”时停止。这种机制非常适合研究和 RAG，但它回答的是：

> 对这个问题，我是否已经收集到足够信息？

它不是：

> 这个博客的全部历史文章是否已经枚举完？

官方文档也明确把 Adaptive Crawling 定位为研究、问答、聚焦知识库等场景，而不是 full site archiving。

因此方案新增独立 `ResearchRun`：

```text
ResearchRun(query, page_budget)
 -> AdaptiveCrawler
 -> selected URL Observation
 -> platform scope/filter
 -> normal Fetch/Snapshot/Render/Extract
 -> store query confidence metrics
 -> never update coverage_state.is_exhausted
```

这能把 AdaptiveCrawler 的价值用于“知识缺口按需补充”，同时不污染历史 Coverage 语义。

---

## 13. Markdown、Fit Markdown 与 Canonical IR

原文认为 Markdown/JSON 很适合直接送给 LLM，这在应用层是对的，但长期知识库不能把第三方 Markdown 当唯一事实。

Crawl4AI 的 Markdown 输出可能受以下因素改变：

- cleaned/raw/fit HTML source；
- markdown generator 版本；
- Pruning threshold；
- BM25 query；
- crawler runtime；
- iframe/shadow handling；
- selector/target elements。

因此事实链保持：

```text
Network Snapshot / Render Artifact
 -> versioned Extractor
 -> Canonical IR
 -> Document Version
 -> deterministic Canonical Markdown
 -> optional FIT / ENHANCED Markdown
```

语义：

```text
CANONICAL_MARKDOWN
  完整正文，默认展示、导出、事实引用

FIT_MARKDOWN
  Pruning/BM25/LLM 有损过滤，供检索/query context

ENHANCED_MARKDOWN
  内部链接、标注、AI 辅助展示
```

Crawl4AI `raw_markdown/fit_markdown` 可作为 extractor candidate、质量对比基线或 Projection 实现，但不能绕过 IR。

---

## 14. Docker/FastAPI/JWT：部署方便，不等于平台控制面

原文提到 Crawl4AI 可以 Docker 部署，并通过 FastAPI/JWT 提供服务。对本项目有价值的是：

- Crawler Runtime 可以标准镜像部署；
- Browser/依赖环境更容易固定；
- Discovery/Browser Worker 可以按资源独立扩缩容；
- Runtime 可以通过内部 REST/streaming 接口被 Adapter 调用。

但不应把 Crawl4AI Server 直接当用户控制面：

```text
User/Web/MCP
 -> Platform API
   RBAC
   idempotency
   audit
   source/domain rate policy
   task/lease/fencing
 -> Internal Crawl Runtime
```

即使 Runtime 自带 JWT，也不能替代多租户 RBAC、审计、Coverage、任务状态和业务幂等。

运行时必须记录：

```text
crawl4ai/playwright/chromium version
container digest
lockfile digest
SBOM
security scan
config schema
benchmark result
```

新 Runtime 先 Golden Corpus + canary，再激活。

---

## 15. Proxy、Anti-bot 与合规边界

原文把代理轮换描述为避免 rate limit 的手段，并提到模拟人类操作应对 CAPTCHA/动态障碍。Crawler 确实可能提供代理、undetected browser、anti-bot retry 等功能，但知识库平台不能把“能做”直接变成“默认应该做”。

推荐策略：

```text
429 / Retry-After
 -> 降速 / 退避 / circuit breaker

403 / CAPTCHA / 明确访问阻断
 -> POLICY_BLOCKED / 人工审核

Proxy
 -> 仅组织批准的出口故障切换、地域测试、隐私网络策略
```

不默认用代理轮换绕过：

- 源站限流；
- IP 封禁；
- 登录墙；
- 付费墙；
- CAPTCHA；
- robots/访问策略。

这也是为什么 Proxy/Anti-bot 只能作为 Runtime capability，不应成为业务默认路线。

---

## 16. 对博客知识库技术方案的最终优化

基于本篇文章与当前 Crawl4AI 实现，现有总体架构继续保留，新增/强化：

1. **Network Snapshot + Render Artifact 双层事实**：Browser 页面同时保存原始网络事实与渲染 DOM；
2. **Deterministic Hydration**：wait selector/JS、滚动/点击、delay 进入版本化 Recipe；
3. **iframe/Shadow DOM provenance**：嵌入内容内联后仍可追溯来源；
4. **Content Scope 与 Link Discovery Scope 分离**：避免正文 selector 造成历史 URL 漏发现；
5. **Adaptive Research 独立于 Coverage**：新建 ResearchRun，不把 query confidence 当全站完整度；
6. **Deep Crawl execution checkpoint**：resume_state 只做运行恢复，不做业务 truth；
7. **Web cancel/resume**：Run 状态与 crawler cancellation 打通；
8. **Prefetch discovery fast path**：Discover Cheap, Fetch Expensive；
9. **Platform Permit > Worker Dispatcher**：集群级 domain/source 控制仍由平台负责；
10. **Crawler Docker 内网化**：平台 API 统一 RBAC/审计/幂等；
11. **Proxy/Anti-bot 合规限制**：不把绕过访问控制设计成默认功能；
12. **Canonical IR + Markdown Projection**：Crawler Markdown 作为候选/派生，非不可变事实；
13. **Runtime Supply Chain**：固定依赖、Browser、镜像、SBOM、扫描和 benchmark。

---

## 17. 推荐最终执行拓扑

```text
Web / REST / MCP
 -> Platform API + RBAC + Idempotency
 -> PostgreSQL Truth
 -> Outbox -> Redis Streams

Discovery Workers
 CMS / Feed / Sitemap / Archive
 URL Seeder / Common Crawl
 Deep Crawl + Prefetch
 -> URL Observation / Coverage

Fetch Workers
 HTTP
 Browser/Crawl4AI
 Playwright Recipe
 -> Network Snapshot
 -> Render Artifact

Extract Workers
 -> Canonical IR
 -> Document Version
 -> Canonical Markdown

Projection Workers
 -> FIT Markdown
 -> OpenSearch
 -> Vector/RAG
 -> Export

Research Workers
 AdaptiveCrawler(query)
 -> ResearchRun metrics
 -> Observation
 -> same normal fact pipeline
 -> never marks historical Coverage complete
```

最终判断：Crawl4AI 的价值不只是“把网页转 Markdown”，而是提供了一个可插拔的访问、发现和内容处理执行层。对于 1000+ 站长期知识库，真正决定可靠性的仍是平台是否把 **Coverage、网络/渲染证据、幂等、版本、增量、审计和恢复** 从 crawler 内部状态中抽离出来。只有这样，Crawler 升级、Browser 行为变化、Adaptive/Deep Crawl 策略变化时，历史知识库仍然可解释、可验证、可重建。

---

## 18. 参考资料

- 原文：https://cobusgreyling.substack.com/p/open-source-llm-friendly-web-crawler
- Crawl4AI 官方项目：https://github.com/unclecode/crawl4ai
- Crawl4AI 文档：https://docs.crawl4ai.com/
- Multi-URL Crawling：https://docs.crawl4ai.com/advanced/multi-url-crawling/
- Deep Crawling：https://docs.crawl4ai.com/core/deep-crawling/
- Adaptive Crawling：https://docs.crawl4ai.com/core/adaptive-crawling/
- URL Seeding：https://docs.crawl4ai.com/core/url-seeding/
- Markdown Generation：https://docs.crawl4ai.com/core/markdown-generation/
- Content Selection / iframe / Shadow DOM：https://docs.crawl4ai.com/core/content-selection/
- Proxy & Security：https://docs.crawl4ai.com/advanced/proxy-security/
- Browser/Crawler Config：https://docs.crawl4ai.com/core/browser-crawler-config/