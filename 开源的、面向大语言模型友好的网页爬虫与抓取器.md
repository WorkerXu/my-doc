# 开源的、面向大语言模型友好的网页爬虫与抓取器

## 1. 调研对象与结论

- 编号：15
- 原文：Open-source LLM Friendly Web Crawler & Scraper
- 来源：Substack / Cobus Greyling
- 地址：https://cobusgreyling.substack.com/p/open-source-llm-friendly-web-crawler
- 发布时间：2025-04-15
- 相关项目：Crawl4AI
- 官方项目：https://github.com/unclecode/crawl4ai
- 官方文档：https://docs.crawl4ai.com/

文章的核心判断是正确的：面向 LLM/RAG 的抓取器不应只是“下载 HTML”，而应同时解决动态页面访问、异步并发、站内发现、正文清洗、Markdown 生成和结构化提取。但对“1000+ 技术博客全量历史 + 长期增量同步”的生产系统而言，Crawl4AI 最合适的位置仍然是 **Crawler Adapter / Worker Runtime**，而不是业务控制面或数据真相层。

本次结合当前 Crawl4AI 官方实现继续下钻后，新增的关键结论有四个：

1. Deep Crawl 已具备 `resume_state` / `on_state_change` 的执行状态恢复能力，可降低长时间站内遍历中断后的重复工作，但该状态只能是运行时 checkpoint，不能替代平台 URL Observation / Coverage；
2. Deep Crawl 支持 `should_cancel` / `cancel()`，应直接接入平台 Run 的取消状态，使 Web 管理台能够安全终止长任务；
3. `CrawlerRunConfig(prefetch=True)` 提供“只取 HTML + 快速抽链接、不生成 Markdown/正文/媒体/LLM extraction”的轻量发现路径，非常适合 Discover -> Fetch 两阶段架构；
4. URL Seeder、Dispatcher、Markdown Generator 都有自己的缓存、过滤、限流或派生语义，这些内部能力可以利用，但不能成为平台唯一状态或事实来源。

因此，对现有博客知识库技术方案最有价值的优化不是更换总体架构，而是在现有架构上补齐 **Deep Crawl 执行 checkpoint、取消语义、prefetch 发现快路径、运行配置指纹和 Runtime 供应链固定**。

---

## 2. 文章观点与当前 Crawl4AI 能力的对应关系

文章将 Crawl4AI 描述为 LLM Friendly Crawler/Scraper，主要价值可以拆成四层：

1. **访问层**：普通 HTTP 与 Playwright Browser 路径，处理静态页面、JavaScript 渲染、滚动、点击等情况；
2. **执行层**：asyncio、多 URL 批量执行、Dispatcher、内存感知并发与限速；
3. **发现层**：Deep Crawl、链接过滤/评分、后续演进出的 URL Seeder；
4. **内容层**：cleaned HTML、Markdown、fit Markdown、结构化 extraction。

文章发布于 2025 年，适合用于理解产品方向，但生产设计不能停留在文章示例。当前官方项目已经把 Deep Crawl 的恢复、取消和 prefetch 等能力明确化，因此架构需要区分两类状态：

```text
平台 durable state
  Source / Profile / Run / Observation / Coverage / Task / Snapshot / Version

Crawler execution state
  deep crawl visited/pending/depth/pages
  dispatcher memory/rate state
  browser/session cache
  seeder cache
```

前者必须在 PostgreSQL + Object Storage 中保持可审计、可恢复；后者可以辅助恢复和提速，但丢失后最多增加重复执行，不能造成业务事实丢失。

---

## 3. AsyncWebCrawler 与多 URL 执行模型

### 3.1 为什么异步适合 1000 个博客

技术博客抓取的主要等待来自 DNS/TLS、服务器响应、网络传输和 Browser 页面加载。对这种 I/O 密集型负载，asyncio 可以让单 Worker 在等待某个请求时继续推进其他请求，因此比逐 URL 串行执行更适合跨大量域名的抓取。

但“异步”只代表执行模型，不代表可以无限并发。真正的生产链路应是：

```text
PostgreSQL FetchTask
  -> lease bounded window
  -> acquire platform global/domain/source permits
  -> Crawl4AI arun_many(stream=True)
  -> each result immediately persist
  -> release permit / ack transport
```

Worker 一次只领取有界窗口，例如 20～100 个任务。不能把几十万 URL 一次性装进 Python list 再交给 `arun_many()`，否则会同时放大内存、失败恢复粒度和长尾等待问题。

### 3.2 stream 的真正价值

`stream=True` 的关键不是“前端实时显示”，而是让结果逐 URL 提交：

- URL A 成功后立刻持久化 Snapshot；
- URL B 超时不阻塞 A 的事实提交；
- URL C 失败只进入自己的 Retry 状态；
- Worker 崩溃时，已经提交的 URL 不需要重新制造事实；
- lease/fencing token 可以把恢复粒度控制到单 URL。

这与平台的 `Bounded Window + Stream + Commit Per URL` 原则完全一致。

---

## 4. Dispatcher：本地资源控制，不是全局调度器

### 4.1 MemoryAdaptiveDispatcher 的工作边界

当前官方 Dispatcher 文档中，`MemoryAdaptiveDispatcher` 可以配置：

- `memory_threshold_percent`：系统内存超过阈值时暂停/降低抓取；
- `check_interval`：内存检查周期；
- `max_session_permit`：本执行器最大并发任务数；
- `memory_wait_timeout`：持续高内存后的超时时间；
- `RateLimiter`：针对 429/503 等状态码进行延迟与指数退避；
- `CrawlerMonitor`：观察本地任务、内存与时延。

这说明它解决的是 **Worker/节点内资源压力**。假设部署 20 个 Browser Worker，每个 Worker 都允许对 `example.com` 并发 5 个页面，那么即使每个 Dispatcher 都“正常”，整个集群仍可能同时对同一域发出 100 个请求。

因此必须保留两级控制：

```text
平台层
  global concurrency
    -> domain token bucket / concurrency
      -> source quota / policy
        -> worker lease window

Worker 层
  MemoryAdaptiveDispatcher / SemaphoreDispatcher
    -> browser/page/session execution
```

平台限流决定“业务允许执行多少”，Dispatcher 决定“机器现在承受得了多少”。二者不可互相替代。

### 4.2 RateLimiter 也不能代替平台限流

Crawl4AI 的 `RateLimiter` 能对单执行器增加随机延迟，并对 429/503 做指数退避。这是有价值的二级保护，但同样没有跨 Worker 的统一视角。

推荐做法：

1. 平台 domain limiter 是强上限；
2. Crawl4AI RateLimiter 是 Worker 内防抖与局部退避；
3. 观察到 429/503 时把证据回传平台，平台 circuit breaker 降低该域总额度；
4. `Retry-After` 优先于本地默认退避参数；
5. Web 管理台展示平台限流原因和 Dispatcher 本地等待原因，避免把两者混成一个指标。

---

## 5. HTTP First / Browser Last 的成本模型

Crawl4AI 的 Browser 能力很强，但 1000 个站点不能默认全部走 Chromium。Browser 的成本包括：

- Chromium 进程常驻内存；
- Context/Page 的额外内存；
- JavaScript 执行 CPU；
- 页面资源、字体、图片、追踪脚本的网络开销；
- 页面崩溃、弹窗、下载和导航带来的更大失败面。

推荐自动路由：

```text
CMS / Feed / Sitemap / API
  -> HTTP

普通 HTML
  -> HTTP Snapshot
  -> extraction quality gate
  -> quality pass: Extract
  -> quality fail / JS shell: Browser fallback

必须点击/滚动/交互
  -> versioned Browser Recipe
```

Browser 进程可以 Worker 级复用，但状态必须隔离：

```text
Browser process scope = WORKER
Context scope         = SOURCE | SECURITY_DOMAIN
Page scope            = TASK
Session scope         = RECIPE_INSTANCE
```

“复用 Browser”与“复用登录态/本地存储”必须严格区分。Cookie、LocalStorage、代理身份和下载目录不得跨 Source 泄漏。

---

## 6. Deep Crawl：BFS/DFS/BestFirst 的真实定位

当前官方 Deep Crawl 支持 BFS、DFS、BestFirst，并提供 `max_depth`、`max_pages`、FilterChain、URL scorer 等控制。

### 6.1 三种策略在博客知识库中的使用边界

**BFS**：优先按层展开，适合站点地图未知时的广度枚举，作为普通 gap discovery 默认策略更容易解释。

**DFS**：沿分支深入，适合探测 archive / category 某个深层分页链，但必须设置 `max_depth/max_pages/max_wall_time`，否则容易陷入日历、标签、参数页面。

**BestFirst**：根据 scorer 优先高相关 URL，适合“优先找某类内容”或预算受限时的探索；不适合拿来证明“全站历史已完整”，因为相关性排序和 `max_pages` 会天然产生选择偏差。

所以 Coverage 规则必须明确：

```text
Authoritative provider exhausted
  + gap providers no material new documents
  + known gaps explained
  != BestFirst queue exhausted
```

### 6.2 FilterChain/Scorer 只能做发现优化

Crawl4AI 可以对 URL pattern、domain、content type、相关度等做过滤或打分，但平台仍要再次执行自己的 Scope/Filter：

```text
crawler discovered link
 -> Adapter normalize
 -> platform SSRF/egress/scope validation
 -> URL Observation
 -> platform filter_release decision
 -> eligible -> durable FetchTask
```

这样即使 crawler 的 Filter 实现或版本变化，也不会绕过平台安全和审计边界。

---

## 7. Deep Crawl crash recovery：如何正确接入平台

### 7.1 官方状态结构

当前官方 Deep Crawl 为 BFS/DFS/BestFirst 提供：

- `resume_state`：从之前保存的状态继续；
- `on_state_change`：处理 URL 后回调当前状态；
- `export_state()`：导出最近状态。

状态是 JSON 可序列化结构，核心包含：

```text
strategy_type
visited[]
pending[]
depths{}
pages_crawled
cancelled
```

这使长时间 Deep Crawl 在进程被终止后不必完全从根 URL 重跑。

### 7.2 为什么它仍然不能成为业务 durable frontier

Crawler state 描述的是某个算法实例的执行位置，而平台需要保存的是业务证据：

- 这个 URL 从哪个 Provider 发现；
- 何时发现；
- 原始 URL 与 normalized URL；
- Filter 决策及版本；
- 是否已经创建 FetchTask；
- Provider 为什么认为已经耗尽；
- 哪些年份/分页存在 known gap。

如果只保存 Crawl4AI 的 `visited/pending`，升级算法、改变 Filter 或切换 Runtime 后，很难解释旧状态是否仍有效。因此正确设计是：

```text
Deep Crawl Runtime State
  = execution checkpoint / acceleration hint

URL Observation + Coverage State
  = platform truth
```

### 7.3 Checkpoint 指纹与恢复条件

建议持久化：

```text
discovery_execution_checkpoint
  discovery_run_id
  provider
  runtime_release_id
  strategy_config_fingerprint
  profile_release_id
  checkpoint_json/object_key
  pages_processed
  updated_at
```

只有以下指纹全部一致时才允许 `resume_state`：

```text
source/profile release
crawler runtime release
strategy type
max_depth/max_pages
filter/scorer config
allowed hosts / path scope
start seed
```

如果指纹变化，旧 runtime checkpoint 失效，重新从 seed 运行即可。因为 URL Observation 使用幂等 upsert，重跑最多增加执行成本，不应重复制造 Document Version。

### 7.4 不要每个 URL 都同步写重型事务

官方 `on_state_change` 可以在 URL 处理后触发。如果每个 URL 都同步把完整 `visited/pending` JSON 写 PostgreSQL，大站可能产生很高写放大。

更合理的策略：

- Observation 仍以小批次持续提交；
- runtime checkpoint 在内存中 coalesce；
- 每 N 个 URL 或每 T 秒持久化一次；
- 优雅停止前强制 flush；
- checkpoint 写失败不回滚已经提交的 Observation；
- 重启后允许重复遍历最近一小段，并依靠幂等消除业务重复。

这比追求“每个内部 frontier 变化都零损失”更符合平台成本模型。

---

## 8. Deep Crawl cancellation：接入 Web 管理控制面

当前官方支持两种取消：

- `should_cancel`：在继续处理 URL 前检查外部状态；
- `cancel()`：同进程直接取消策略。

对平台最有价值的是 `should_cancel`，因为它天然可以映射到 PostgreSQL 中的 `discovery_run.cancel_requested_at/status`：

```text
Web 点击取消
 -> API update discovery_run CANCEL_REQUESTED
 -> Worker should_cancel() 读取/缓存该状态
 -> Deep Crawl 完成当前 URL 后停止
 -> flush runtime checkpoint
 -> run = CANCELLED
 -> 已提交 Observation 保留
```

这带来两个好处：

1. 取消是显式业务操作，可审计，而不是 kill Pod；
2. 取消后可以根据相同指纹恢复，也可以废弃 runtime checkpoint 重新启动，不影响已发现 URL。

需要注意：外部取消检查不能每一步都打数据库。可用短 TTL 本地缓存、Redis 信号或平台控制流，但最终取消事实仍由 PostgreSQL Run 状态保存。

---

## 9. Prefetch：最适合两阶段架构的新增能力

### 9.1 官方 fast path 做了什么

当前官方提供 `CrawlerRunConfig(prefetch=True)`。Prefetch 模式只保留轻量路径：

```text
Fetch HTML            保留
Fast link extraction  保留
Markdown generation   跳过
Content scraping      跳过
Media extraction      跳过
LLM extraction        跳过
```

官方文档将其定位为快速 URL discovery / site mapping / crawl planning，并给出大约 5～10 倍的发现速度收益范围。具体收益取决于页面与网络，但架构意义比具体倍数更重要：**发现链接时没有必要提前支付正文清洗、Markdown 和 LLM 的成本。**

### 9.2 在本项目中的正确使用方式

```text
Deep Crawl / unknown site area
 -> prefetch HTML + links
 -> URL Observation
 -> normalize/scope/filter/dedupe
 -> eligible URL -> durable FetchTask
 -> HTTP First full fetch
 -> Snapshot
 -> Extract / IR / Markdown
```

Prefetch 结果不能直接成为 Document 或 Markdown，因为它有意跳过了重处理阶段；它只是 Discovery evidence。

### 9.3 与普通 HTTP discovery 的关系

并不是所有站点都应该用 Crawl4AI Prefetch：

- Sitemap、Feed、CMS API：直接 HTTP parser 更简单、更权威；
- Archive/Category 静态 HTML：普通 HTTP + lxml 抽链接往往更轻；
- 页面需要 Browser 才能生成链接：Crawl4AI Prefetch 很有价值；
- 未知站点结构的受控 Deep Crawl：Prefetch 是默认候选；
- 需要正文判断下一跳的 content-aware exploration：使用完整 Deep Crawl，但仍不直接成为事实版本。

因此 `prefetch` 应是 Provider/Recipe 配置，而不是全局开关。

---

## 10. AsyncUrlSeeder：Sitemap / Common Crawl 的批量发现

当前官方 `AsyncUrlSeeder` 支持 `sitemap`、`cc`、`sitemap+cc`，并提供 pattern、`extract_head`、`live_check`、`max_urls`、并发、hits/sec、cache TTL、Sitemap lastmod 校验以及 BM25 评分等配置。

### 10.1 为什么适合历史回填

URL Seeder 的优势是“先看 URL 集合，再决定抓什么”：

```text
Sitemap / Sitemap Index
 -> bulk URL discovery
 -> platform filter
 -> fetch only eligible article URLs
```

对于站点 Sitemap 完整的技术博客，这比递归打开每个列表页更便宜。

Common Crawl 可以帮助发现当前 Sitemap 已经删除但历史上存在的 URL，因此适合做 gap provider。

### 10.2 为什么不能直接把 Seeder 结果当 Coverage 真相

- Common Crawl 不是源站权威清单；
- 索引中可能有旧参数 URL、资源文件、站外重定向；
- Sitemap 也可能只保留最近内容；
- `max_urls`、pattern、score threshold 会主动截断；
- Seeder 自己的 cache TTL 是执行缓存，不是业务 checkpoint。

所以 Seeder 的输出仍要转成 `URL Observation`，并在 `coverage_state` 中记录 provider、参数、是否截断、数量与 gap 解释。

### 10.3 `extract_head/live_check` 的成本

这两个选项会把纯索引发现转成更多网络请求。对数十万 URL 的历史站点，不应默认全部开启：

- 先用 URL pattern/path 低成本过滤；
- 只有无法从 URL 判断正文类型时抽样或分批 `extract_head`；
- `live_check` 更适合候选确认/删除检测，而不是全量 Sitemap 的第一遍；
- 所有附加请求仍服从平台 domain rate limit。

---

## 11. Markdown：第三方结果是候选/Projection，不是不可变事实

当前官方 `DefaultMarkdownGenerator` 允许从 `cleaned_html`、`raw_html`、`fit_html` 等不同输入生成 Markdown，并可使用 Pruning/BM25 等内容过滤器产生 `fit_markdown`。

这些能力很适合 RAG，但也恰好说明为什么平台不能把 `result.markdown` 当唯一事实：

1. 输入 HTML source 可以变化；
2. HTML cleaning strategy 可以变化；
3. Markdown options 可以变化；
4. Pruning threshold/BM25 query 可以变化；
5. Crawl4AI Runtime 升级也可能改变输出。

因此事实链必须保持：

```text
Immutable Network Snapshot
 -> versioned Extractor
 -> Canonical IR
 -> Document Version
 -> deterministic Canonical Markdown
 -> optional FIT / ENHANCED Markdown
```

三层语义：

```text
CANONICAL_MARKDOWN
  完整正文，来自 Canonical IR，用于知识库事实展示与默认导出。

FIT_MARKDOWN
  Pruning/BM25/其他有损过滤，用于检索、摘要、query-specific context。

ENHANCED_MARKDOWN
  可加入内部链接、标注、AI 辅助信息，只作为派生展示。
```

Crawl4AI 的 Markdown 可以成为 extractor candidate、质量对比基线或 FIT Projection 实现，但不能绕过 Snapshot/IR。

---

## 12. Runtime Release 与供应链治理

Crawl4AI、Playwright、Chromium 以及依赖库都在持续变化。长期知识库必须做到“升级执行器，不改变历史事实”。

建议 `crawler_runtime_release` 至少记录：

```text
adapter_name
adapter_version
crawl4ai_version
playwright_version
chromium_version
docker_image_digest
lockfile_digest
sbom_digest
image_signature/security_scan
config_schema_version
benchmark_result
status
activated_at
```

生产镜像要求：

- 固定 Python 依赖 lockfile；
- 固定 Chromium/Playwright 版本；
- 固定容器 digest，不使用漂移的 `latest`；
- 生成 SBOM 并跑依赖/镜像漏洞扫描；
- 新 Runtime 在 Golden Corpus 和小流量 canary 通过后再激活；
- 旧 Runtime 保留回滚窗口；
- Runtime 变更只能改变后续执行与可重建 Projection，不能覆盖 Snapshot/Document Version 的来源链。

Golden Corpus 至少覆盖：静态博客、JS shell、长代码块、表格、图片、Newsletter、Sitemap Index、大分页站、429/503、redirect、异常编码和 Browser crash 样本。

---

## 13. 对现有博客知识库技术方案的增量优化

现有方案大方向正确，本次调研不建议替换 PostgreSQL + Object Storage 事实层、Outbox + Redis Streams 任务运输、HTTP First、Canonical IR、Coverage Evidence 等核心设计。应该新增或强化以下内容：

1. **新增 Discover Cheap, Fetch Expensive 原则**：发现阶段优先 Sitemap/API/Feed/HTTP link parser，必要时用 Crawl4AI `prefetch`，不要在尚未判断 URL 是否为正文前生成 Markdown/LLM extraction；
2. **新增 Runtime Checkpoint Is Hint 原则**：Deep Crawl `resume_state` 仅用于执行恢复，不等于业务 frontier/Coverage；
3. **为 Deep Crawl 保存 execution checkpoint**：记录 runtime/profile/strategy fingerprint、pages、checkpoint，允许同配置恢复；
4. **接入 cancellation**：`should_cancel` 对接 Run 的 `CANCEL_REQUESTED`，Web UI 可以安全取消而非 kill Worker；
5. **区分 BFS/DFS/BestFirst 用途**：BFS 用于广度 gap discovery，DFS 只做受控深探，BestFirst 只做优先级探索，不作为完整 Coverage 证明；
6. **将 prefetch 作为 Discovery Provider/Recipe 能力**：Prefetch 只产生 Observation，不直接创建 Document/Markdown；
7. **补充 Web 运行视图**：显示 Deep Crawl strategy、prefetch、pages processed、checkpoint age、stop reason、cancel/resume eligibility；
8. **补充 Runtime 供应链字段**：lockfile/SBOM/image signature/security scan，降低第三方快速迭代带来的生产漂移；
9. **保留平台 Permit > Worker Dispatcher**：Crawl4AI 内置 RateLimiter/MemoryAdaptiveDispatcher 作为二级控制；
10. **保留 Canonical/FIT 分层**：Crawl4AI fit Markdown 是有损派生，不制造 Document Version；
11. **URL Seeder 的 cache/score/max_urls 必须进入 evidence**：否则“少发现 URL”可能被误认为站点没有更多内容；
12. **Checkpoint 写入采用 coalescing**：运行时状态允许小窗口重复，避免每 URL 写完整 frontier 造成数据库写放大。

---

## 14. 推荐的 1000+ 站运行拓扑

```text
Web / REST / MCP
  -> API + RBAC + Idempotency
  -> PostgreSQL Truth
       Source / Profile / Run / Observation / Coverage
       Task / Snapshot / Document / Version / Release
  -> Transactional Outbox
  -> Redis Streams transport

Discovery Workers
  CMS / Feed / Sitemap / Archive
  AsyncUrlSeeder
  HTTP link discovery
  Crawl4AI Deep Crawl + Prefetch
  -> Observation + Coverage checkpoint
  -> optional execution checkpoint

Fetch Workers
  HTTP pool
  Browser/Crawl4AI pool
  Playwright Recipe fallback
  -> immutable Snapshot

Extract Workers
  -> candidate extraction
  -> Canonical IR
  -> Document Version
  -> Canonical Markdown

Projection Workers
  -> FIT Markdown
  -> OpenSearch
  -> Vector/RAG
  -> export
```

核心恢复逻辑：

```text
Redis 丢失
 -> 从 PostgreSQL Task/Outbox 重建运输

Deep Crawl runtime checkpoint 丢失
 -> 从 seed 重新运行
 -> Observation 幂等去重
 -> Coverage 事实不丢

Worker/Browser crash
 -> lease expires
 -> fencing token 阻止旧 Worker 覆盖新结果

Extractor/Markdown 升级
 -> 从 Snapshot / IR 离线重建
 -> 不访问源站也能回放
```

最终判断：Crawl4AI 非常适合作为这套知识库平台的抓取执行组件，尤其是异步批量、Browser、Deep Crawl、URL Seeder、Prefetch、Markdown 与 Dispatcher。但规模达到 1000+ 站后，决定系统是否可靠的不是单个 crawler 功能多少，而是能否把 crawler 的内部状态严格限制在执行边界内，同时由平台自己维护 Coverage、幂等、版本、增量、审计和恢复。

---

## 15. 参考资料

- 原文：https://cobusgreyling.substack.com/p/open-source-llm-friendly-web-crawler
- Crawl4AI：https://github.com/unclecode/crawl4ai
- Deep Crawling：https://docs.crawl4ai.com/core/deep-crawling/
- URL Seeding：https://docs.crawl4ai.com/core/url-seeding/
- Multi-URL Crawling / Dispatchers：https://docs.crawl4ai.com/advanced/multi-url-crawling/
- Markdown Generation：https://docs.crawl4ai.com/core/markdown-generation/
