# 开源的、面向大语言模型友好的网页爬虫与抓取器

## 1. 调研对象

- 编号：15
- 原文：Open-source LLM Friendly Web Crawler & Scraper
- 来源：Substack / Cobus Greyling
- 地址：https://cobusgreyling.substack.com/p/open-source-llm-friendly-web-crawler
- 发布时间：2025-04-15
- 相关项目：Crawl4AI
- 官方文档：https://docs.crawl4ai.com/

本文关注的不是“如何写一个单页爬虫”，而是 Crawl4AI 作为 LLM/RAG 数据入口时的执行模型：异步批量抓取、HTTP/Browser 双路径、Deep Crawl、URL Seeder、Markdown 生成、内存自适应调度，以及如何把这些能力嵌入一个面向 1000+ 技术博客、支持历史回填和增量同步的长期知识库平台。

## 2. 文章核心观点

文章把网页抓取定位成 LLM 数据管道的前置基础设施，而不是传统意义上的 HTML 下载器。其核心价值可以归纳为四层：

1. **访问层**：既能直接请求普通网页，也能借助 Playwright 处理 JavaScript 渲染、滚动、点击和动态内容；
2. **并发层**：异步执行多个 URL，并用内存感知的 Dispatcher 调整并发，避免浏览器任务把节点内存打满；
3. **发现层**：支持 Deep Crawl，并可从站内链接继续发现内容；官方后续能力还包括 Sitemap/Common Crawl URL Seeder；
4. **内容层**：把页面转换成更适合 LLM 的 Markdown，并提供内容过滤、结构化提取等能力。

对本项目最重要的启发不是“直接用 Crawl4AI 一把梭抓 1000 个网站”，而是把它放到一个有明确真相边界、可恢复任务系统和版本化 Adapter 后面。Crawl4AI 擅长的是单机/Worker 内的抓取执行和内容转换；站点 Coverage、全局调度、任务幂等、增量状态、版本事实和 Web 管理仍应由平台自身负责。

## 3. Crawl4AI 的执行模型与技术原理

### 3.1 AsyncWebCrawler：异步 I/O 与浏览器执行统一入口

Crawl4AI 的核心使用方式是异步 crawler。对大量不同域名的技术博客而言，网络等待占比远高于 CPU，因此 asyncio 能显著提高单 Worker 的 I/O 利用率。对静态网页可以使用轻量 HTTP 抓取路径，对需要 JavaScript 的网页再进入 Browser/Playwright 路径。

这意味着平台应当把“抓取 URL”抽象成内部稳定接口，而不是让业务代码直接依赖第三方对象：

```text
FetchTask
  -> CrawlerAdapter.fetch(...)
  -> AdapterResult
       requested_url
       final_url
       status_code
       headers
       raw_body/raw_html
       markdown_candidate
       links
       timing
       failure
  -> Snapshot / Extraction pipeline
```

Crawl4AI 的对象结构或字段即使升级，只需要修改 `Crawl4AIAdapter`；平台的 Snapshot、Document、Version 和 Markdown 数据模型不跟着变。

### 3.2 arun_many + stream：有界批处理而不是一次性全站物化

官方多 URL 抓取接口支持批量并发，并可在流式模式下逐个返回结果。这个特性适合 Worker 领取一个“有界窗口”后执行，例如一次 lease 20～100 个 URL，然后结果一出来就写 Snapshot，而不是等待整批全部成功。

正确的生产模型：

```text
PostgreSQL durable tasks
  -> lease N tasks
  -> acquire platform permits
  -> Crawl4AI arun_many(stream=True)
  -> each result immediately commit Snapshot / retry state
  -> acknowledge corresponding transport message
```

错误模型：

```text
发现 50 万 URL
  -> 全部装入 Python list
  -> 一次 arun_many
  -> 等 50 万个结果结束后统一落库
```

后者会产生四个问题：内存不可控、失败恢复粒度过粗、长尾 URL 拖住整批、成功结果无法尽快持久化。

### 3.3 MemoryAdaptiveDispatcher：只解决 Worker 内资源压力

Crawl4AI 的 MemoryAdaptiveDispatcher 会根据内存阈值动态控制并发，避免 Browser/页面对象数量过多导致 OOM。它解决的是**本进程/本节点执行资源**问题，不等价于业务平台的全局限流。

生产系统应有两层 Permit：

```text
平台层：global -> domain -> source concurrency/rate
Worker 层：MemoryAdaptiveDispatcher / SemaphoreDispatcher
```

平台先决定“这个域名现在最多允许多少个请求”，Worker 再根据本机内存决定“这些允许执行的任务中此刻并行多少个”。如果把 Dispatcher 当成唯一限流器，会出现多个 Worker 同时对同一网站放大请求的问题。

建议持久化以下 Worker 运行指标：

- `dispatcher_wait_ms`
- `dispatcher_active_peak`
- `process_rss_peak_mb`
- `browser_pages_peak`
- `browser_recycle_count`
- `local_throttle_count`

这些指标用于 Web 管理台判断 Browser 任务成本、调整 Worker 规格和站点并发，而不是作为业务事实。

### 3.4 HTTP 与 Browser 双路径

对于 1000 个技术博客，默认 Browser 抓取会把资源成本和失败面放大。更合理的是 HTTP First：

```text
HTTP GET
 -> HTML 有有效正文：进入 Snapshot/Extract
 -> HTML 只是 JS shell、正文质量低或必须交互：Browser fallback
 -> 特殊交互：Playwright/Crawl4AI recipe
```

自动路由可以综合：

- Content-Type；
- HTML 可见文本长度；
- `<script>` 占比；
- 是否存在文章结构；
- 页面是否只显示 loading/root shell；
- HTTP 抽取质量得分；
- 站点 Profile 显式配置。

Browser 是昂贵执行资源，应独立 Worker Pool、独立 HPA 和独立故障隔离。静态 HTML、Feed、Sitemap、CMS API、文件下载都不应占 Browser slot。

### 3.5 Browser 复用与 Session 隔离

Crawl4AI/Playwright 可以复用 Browser 进程，减少频繁启动 Chromium 的成本；顺序多步页面还可以借助 session 复用状态。但“复用进程”不等于“跨站点共享会话”。

建议定义：

```text
BrowserReusePolicy
  process_scope = WORKER
  context_scope = SOURCE | SECURITY_DOMAIN
  page_scope    = TASK
  session_scope = RECIPE_INSTANCE
```

原则：

- Browser 进程可长期复用；
- Context 按 Source/安全域隔离；
- Page 默认每任务新建并及时关闭；
- `session_id` 只用于明确的顺序交互流程，不用于并行任务共享；
- Cookie、LocalStorage、认证态、下载目录、代理身份不能跨 Source 泄漏；
- 达到 RSS、页数、任务数或运行时阈值后主动 recycle。

### 3.6 Deep Crawl：发现工具，不是业务 Frontier

Crawl4AI 支持 BFS/DFS/BestFirst 等 Deep Crawl 策略，并能过滤和评分链接。它适合：

- 没有 Sitemap/CMS API 的小型技术博客；
- 验证站点某个区域是否存在漏链；
- 从入口页发现 archive/category 的边缘 URL；
- 受控地补历史 Coverage 缺口。

但不能把 crawler 内部 frontier 当成平台的 durable frontier。原因是：

1. 平台需要知道 URL 是从 Sitemap、Feed、CMS 还是链接图发现；
2. Worker 崩溃后必须从数据库 checkpoint 精确恢复；
3. 不同 Provider 的 Coverage 证据要分别统计；
4. URL 需要在抓正文前经过 canonical/filter/policy；
5. 多 Worker/多版本 crawler 不能各自拥有互不一致的“全站状态”。

即使 Crawl4AI 提供 Deep Crawl 状态恢复，平台仍应把它视作**一次执行的 checkpoint**，最终 URL Observation、Filter 决策、Fetch Task 和 Coverage 状态仍落 PostgreSQL。

## 4. URL 发现：两阶段流水线

### 4.1 Discovery 与 Fetch 必须解耦

历史回填最容易犯的错误是“边遍历页面边立刻递归抓全部正文”，这会导致站点边界失控，且难以证明历史是否完整。

建议拆成：

```text
Stage A: Discover
  CMS API / Sitemap / Feed / Archive / URL Seeder / Deep Crawl
  -> URL Observation
  -> normalize
  -> scope/filter
  -> dedupe
  -> durable frontier

Stage B: Fetch
  eligible URL
  -> HTTP First
  -> Browser fallback
  -> Snapshot
  -> Extract/IR/Markdown
```

Crawl4AI 的快速 URL 发现、Deep Crawl 和 URL Seeder 都放在 Stage A；`arun_many()` 正文抓取放在 Stage B。这样才能对 1000 个站点分别暂停、补洞、重跑或改变 Filter，而不需要重新遍历整个网络。

### 4.2 AsyncUrlSeeder、Sitemap 与 Common Crawl

官方 URL Seeder 能从 Sitemap、Common Crawl 或组合来源获取 URL。对技术博客历史发现有明显价值：

- Sitemap 是当前站点权威 URL 清单；
- Sitemap Index 可覆盖年份/栏目拆分；
- Common Crawl 可以找出源站当前 Sitemap 已不包含、但历史上存在过的 URL；
- 两者取并集后再由平台 Filter 和 Scope 决定是否进入抓取。

但 Common Crawl 只能作为补洞证据，不应高于 CMS API/Sitemap 的权威级别；历史索引里会包含过期页面、参数变体、站外跳转或非正文资源。

### 4.3 Coverage 的判定不能等于“Deep Crawl 没有新链接”

一个站点“抓完”至少要能解释：

- CMS 分页是否耗尽；
- Sitemap/Sitemap Index 是否全部读取；
- Feed 能覆盖什么时间范围；
- Archive 年/月分页是否到达最早年份；
- Deep Crawl 为什么停止；
- Common Crawl 是否发现 Sitemap 没有的历史 URL；
- 哪些 URL 因 robots/权限/错误被放弃；
- 是否存在年份断层或 known gap。

因此 Coverage 是一个证据集合，而不是队列长度。

## 5. Markdown 生成：raw、canonical 与 fit 必须分层

### 5.1 为什么不能直接把 result.markdown 当最终事实

Crawl4AI 可以直接生成高质量 Markdown，这非常适合快速做 RAG。但长期知识库需要可重放、可验证和升级抽取器，所以最终事实链应是：

```text
Network Snapshot
 -> Canonical IR
 -> Canonical Markdown
 -> Filtered/Fit Markdown
 -> Chunk/Search/RAG
```

第三方 crawler 返回的 Markdown 可以作为候选抽取结果或加速路径，但必须能够回溯到 Snapshot 和 Extractor Release。

### 5.2 raw/canonical 与 fit_markdown 的语义不同

官方 Markdown 生成支持原始 Markdown 和经过内容过滤后的 fit Markdown。Pruning/BM25/LLM Filter 的设计目标是减少噪声、提高下游 LLM 相关性，它本质上是**有损投影**。

因此平台明确三种 Projection：

```text
CANONICAL_MARKDOWN
  完整正文，依据 Canonical IR 确定性渲染；知识库事实展示/导出默认使用。

FIT_MARKDOWN
  经过 Pruning/BM25 等过滤；用于检索、摘要、特定 query 的上下文压缩。

ENHANCED_MARKDOWN
  可叠加链接、标注、AI 辅助信息；只用于展示或派生。
```

任何 Fit/Enhanced 输出都不能覆盖 Canonical Markdown。否则过滤阈值升级后无法区分“源站改了”还是“过滤器改了”。

### 5.3 Canonical IR 的必要性

技术博客包含代码块、表格、引用、图片、脚注和标题层级。仅存 Markdown 会丢失部分原始结构信息，也不利于未来换 Renderer。

Canonical IR 应保存：

- heading level；
- paragraph；
- code language + exact text；
- list nesting；
- table cells；
- quote；
- image src/alt/caption；
- 原 DOM/文本定位 hint；
- block hash。

Markdown Renderer 版本化后，可以完全离线从同一 Version 重建 Markdown。

## 6. 增量同步设计

Crawl4AI 负责“执行某次抓取”，但增量同步需要平台维护跨运行状态。

### 6.1 发现增量

按来源优先使用：

1. CMS API 的 `updated_at`、分页 cursor；
2. Sitemap `lastmod`；
3. Feed 的 entry id/guid/published/updated；
4. Archive 页面最近几页；
5. 定期低频 Deep Crawl/Coverage Verify。

每个 Provider 保存自己的 checkpoint，不共用一个模糊的 `last_sync_time`。

### 6.2 内容增量

已知 URL 优先做 Conditional Fetch：

```http
If-None-Match: <etag>
If-Modified-Since: <last-modified>
```

- 304：只更新 observation/fetch evidence，不产生新 Version；
- 200 且 body hash 相同：不重新派生；
- body hash 不同：重新 Extract；
- Canonical IR/content hash 相同：视为模板/广告/追踪变化；
- Canonical content hash 不同：新建 Document Version。

这样可以避免“每次抓取都制造一个 Markdown 文件版本”。

### 6.3 自适应调度

站点发布频率差异巨大，固定每小时抓 1000 个站会浪费资源。Source 记录最近发布间隔和变化率，调度器在配置上下限内自适应：

```text
recent change rate high -> shorter interval
long time no change      -> longer interval
feed push/new item       -> immediate incremental run
coverage verify          -> independent slow schedule
```

## 7. Crawler Adapter 与版本治理

### 7.1 稳定内部接口

建议所有 crawler 实现统一：

```text
CrawlerAdapter
  probe(url, profile) -> ProbeResult
  fetch_one(task, profile) -> FetchResult
  fetch_many(tasks, profile) -> AsyncIterator[FetchResult]
  discover(seed, profile) -> AsyncIterator[DiscoveryObservation]
  capabilities() -> CapabilitySet
```

`CapabilitySet` 至少标记：

- HTTP fetch
- Browser render
- JS interaction
- streaming batch
- deep crawl
- sitemap seeding
- Common Crawl seeding
- markdown candidate
- structured extraction
- proxy support
- session reuse

Scheduler 依据能力而不是根据“组件名字”写 if/else。

### 7.2 CrawlerRuntimeRelease

Crawl4AI 是快速迭代项目，生产上不能“pip install -U 后全库行为改变”。应记录：

```text
crawler_runtime_release
  adapter_name
  adapter_version
  crawl4ai_version
  playwright_version
  chromium_version
  docker_image_digest
  config_schema_version
  benchmark_result
  activated_at
```

新版本先对 Golden Corpus 跑：

- 抓取成功率；
- Browser crash；
- Markdown diff；
- code/table 保留；
- 内存峰值；
- 429/重试；
- 动态页面正文质量。

通过后再激活，不直接覆盖生产 Release。

## 8. Worker 级参考流程

下面是架构伪代码，重点是边界而非某一 Crawl4AI 版本的精确 API：

```python
async def run_fetch_window(worker, source, lease_size=50):
    tasks = await task_repo.lease(
        source_id=source.id,
        limit=lease_size,
        fencing=True,
    )

    permitted = await platform_limiter.acquire_many(tasks)
    configs = build_crawler_configs(permitted, source.active_profile)

    async for result in crawl4ai_adapter.fetch_many(
        permitted,
        configs=configs,
        stream=True,
        memory_adaptive=True,
    ):
        async with db.transaction():
            assert_fencing_token(result.task_id, result.fencing_token)

            if result.ok:
                snapshot = await snapshot_store.persist(result)
                await task_repo.succeed(result.task_id, snapshot.id)
                await outbox.emit("snapshot.ready", snapshot.id)
            else:
                await retry_policy.transition(result)
```

关键点：

- `lease_size` 有界；
- Platform Limiter 先于 Local Dispatcher；
- 流式逐 URL 提交；
- Snapshot 成功即事实成立；
- Extract/Markdown 后续失败不回滚 Snapshot；
- Worker 重启依赖 DB lease/checkpoint，而不是内存里的 Python task。

## 9. Self-hosted Crawl4AI 的边界

官方提供 Docker/API/JWT 等服务化能力，可作为 Browser/Crawler Worker 的部署方式。推荐拓扑：

```text
Platform Fetch Worker
  -> internal Crawl4AI service
       -> Browser/HTTP execution
       -> normalized AdapterResult
  -> Snapshot store
```

可以使用内部 JWT/mTLS、网络隔离和固定镜像 digest，但不能让 Crawl4AI Server 自己成为：

- Source 配置真相；
- 全局 Fetch Task 真相；
- Coverage 真相；
- Document Version 真相；
- retry/dead-letter 的唯一记录。

它是执行器，不是控制面。

## 10. 代理、反爬与合规边界

Crawl4AI 支持代理配置/轮换，这对合法的出口故障切换、地域访问测试或组织网络策略有用。但知识库平台不应把“绕过 CAPTCHA/付费墙/登录控制”设计成默认能力。

平台策略：

- 尊重 robots 和源站政策；
- 公开页面低速抓取，支持 `Retry-After`；
- 429/503 自动退避；
- 403/CAPTCHA 进入 `POLICY_BLOCKED` 或人工审核，而不是自动无限换代理；
- Proxy 仅从受控池选择，按 Source/Profile 记录使用原因；
- 每次 Redirect/Browser navigation 都重新做 SSRF/Egress 检查。

这比“尽可能突破反爬”更适合长期运行的 1000 站知识库。

## 11. 对现有技术方案的具体优化结论

本调研确认并强化以下架构决策：

1. **保留 HTTP First / Browser Last**，Crawl4AI 不应成为所有 URL 的强制 Browser 通道；
2. **将 Crawl4AI 定位为 Adapter/Worker Runtime**，不承担平台 durable task 和 Coverage 真相；
3. **采用两阶段 Discover -> Fetch**，Deep Crawl/URL Seeder 只产生 Observation，不直接越过 Filter 抓正文；
4. **批抓只允许 bounded window + stream**，结合 `arun_many` 的流式返回逐 URL 落 Snapshot；
5. **MemoryAdaptiveDispatcher 是二级本地调度器**，全局/domain/source Permit 仍由平台控制；
6. **新增 CrawlerRuntimeRelease/CapabilitySet**，隔离 Crawl4AI 版本升级和能力差异；
7. **明确 CANONICAL/FIT/ENHANCED Markdown 三层**，fit markdown 不可覆盖原始事实；
8. **Browser 进程复用但 Context/Session 隔离**，降低资源成本又避免跨源状态污染；
9. **持久化 Dispatcher/Browser 运行指标**，让 Web 管理台能解释为什么任务降并发或切换 Browser；
10. **Deep Crawl 状态恢复只能作为执行优化**，平台的 URL frontier/checkpoint 必须持久化在 PostgreSQL；
11. **Sitemap/Common Crawl Seeder 进入历史 Coverage 补洞体系**，但权威 Provider 仍优先；
12. **增量同步独立于 crawler**，由 ETag/Last-Modified、Provider cursor、内容 hash 和 Source 调度策略共同完成。

## 12. 适配 1000+ 技术博客的推荐运行模型

```text
Web / API
  -> Source/Profile/Release
  -> Scheduler
       -> Discovery Queue
            -> CMS/Sitemap/Feed/Archive/Seeder/Deep workers
            -> URL Observation + Coverage checkpoint
       -> Fetch Queue
            -> HTTP workers
            -> Browser/Crawl4AI workers (fallback)
            -> immutable Snapshot
       -> Extract Queue
            -> Canonical IR
            -> Canonical Markdown
       -> Projection Queue
            -> Fit Markdown / Search / Vector / RAG
```

扩容优先增加“不同域名上的横向并发”，而不是把单站并发拉高。Browser Pool 与 HTTP Pool 分开；Backfill 与 Incremental 分优先级；增量任务应抢占历史回填资源，防止百万历史 URL 让新文章延迟数天。

站点越多，核心难点越不是单个爬虫性能，而是：

- 能不能证明历史 Coverage；
- 能不能在中断后从 durable checkpoint 继续；
- 能不能避免重复版本；
- 能不能用配置而非站点专用代码接第 1001 个站；
- 能不能在 crawler 升级后离线回归并快速回滚；
- 能不能解释每篇 Markdown 来自哪个 Snapshot 和 Extractor；
- 能不能保证 Web 管理台看到的状态与后台事实一致。

Crawl4AI 为执行层提供了很好的异步、Browser、Deep Crawl 和 Markdown 能力，但只有把这些能力纳入上述控制面和事实层后，才真正适合长期规模化知识库。