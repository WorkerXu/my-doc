| 推荐理由 | 链接地址 |
|---|---|
| 最适合把 plan 路径、版本或哈希写进 commit message trailer（如 `Plan-File`、`Plan-Hash`），可被 hook/CI 稳定解析并用于建立“计划文件 ↔ Git 提交”的机器可读关联。 | https://git-scm.com/docs/git-interpret-trailers |
| 适合把 plan 元数据或后续审查结果附着到既有 commit，而不改 commit 本体；便于补充审查证据、追踪计划与实现的对应关系。 | https://git-scm.com/docs/git-notes |
| CodeRabbit 官方 Codex 集成可由 Codex 直接触发独立 review、接收 findings 并逐项修复，适合作为代码生成 Agent 之外的第二审查 Agent。 | https://docs.coderabbit.ai/cli/codex-integration |
| 支持 pre-commit 审查、agent 优化结构化输出和 CI/CD，可把审查前移并把结果继续交给 Codex 或其他编排 Agent 处理。 | https://docs.coderabbit.ai/overview/ide-cli-review |
| Qodo 2.0 明确提供 multi-agent expert review 与 codebase 之外的上下文，适合把架构、逻辑、测试、安全等专家角色并行编排到 PR 审查。 | https://www.qodo.ai/blog/introducing-qodo-2.0-agentic-code-review/ |
| Figma MCP 能向 AI Agent 提供组件、变量、布局和设计系统上下文，可让设计文件成为代码审查依据，而不是只依赖截图或文字描述。 | https://developers.figma.com/docs/figma-mcp-server/ |
| Figma 已支持 Codex 等 MCP 客户端直接使用设计上下文与 skills，适合把设计规范、可访问性和设计系统规则纳入 Codex 自动审查闭环。 | https://www.figma.com/blog/the-figma-canvas-is-now-open-to-agents/ |
| Symphony 是 OpenAI 的 Codex 编排规范，把 Linear 等项目看板作为 coding agents 的 control plane，最贴近“多 Agent 审查 + 项目看板”统一编排。 | https://openai.com/index/open-source-codex-orchestration-symphony/ |
| Codex App 原生支持 projects、并行 agents、worktrees 与 diff review，可作为审查结果回流 Codex 项目线程、继续修复和复审的承载层。 | https://openai.com/index/introducing-the-codex-app/ |
| GitHub Projects 的 auto-add workflow 可按 PR/issue 条件自动入看板，适合把 AI review 产出的 PR、issue 或审查任务自动同步到项目视图。 | https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/adding-items-automatically |
| Codex 的 Sites 与 annotations 适合把自动审查结果沉淀为可分享、可继续批注的文章化成果或项目交付物，补齐“审查结果发布到 Codex”的最后一环。 | https://openai.com/index/codex-for-every-role-tool-workflow/ |
| Codex 官方用例明确可把 `/plan` 输出保存为 `PLAN.md`，并通过 `AGENTS.md` 要求实现持续遵循该计划，适合作为“可版本控制 plan 文件”的标准入口。 | https://developers.openai.com/codex/use-cases/browser-games |
| `prepare-commit-msg` 可在提交信息编辑前自动改写 commit message，且不会被 `--no-verify` 跳过，适合自动注入 `Plan-File`/`Plan-Hash`，把计划文件与提交强绑定。 | https://git-scm.com/docs/githooks |
| `git hash-object` 可直接计算 `PLAN.md` 内容对应的 Git object ID；把该哈希写入提交元数据后，可以精确证明某次提交对应的是哪一版计划内容。 | https://git-scm.com/docs/git-hash-object |
| Codex Subagents 原生支持并行专业 Agent、集中等待与汇总，并给出 PR review 的 explorer/reviewer/docs-researcher 示例；可再配置一个使用 Figma MCP 的 design reviewer 形成多路审查。 | https://developers.openai.com/codex/subagents |
| GitHub Copilot custom agents 可作为独立的第二套多 Agent 编排器，把不同自定义 Agent 作为 subagents 并行运行，适合与 Codex 的 review 结果做交叉验证。 | https://docs.github.com/en/copilot/concepts/agents/copilot-cli/about-custom-agents |
| Copilot code review 已支持 repository agent skills 与 MCP servers，可把 Figma/设计上下文作为 review 输入；非常适合让另一套审查 Agent 按设计文件、路径规则和团队规范复核 Codex 生成代码。 | https://docs.github.com/en/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review |
| Figma Code Connect 会把设计组件属性、实际代码导入、prop 映射和源码路径注入 MCP 上下文，使设计审查从“看图比对”升级为“设计组件 ↔ 真实代码组件”的结构化核对。 | https://developers.figma.com/docs/figma-mcp-server/code-connect-integration/ |
| Storybook Visual Tests/Chromatic 可把每个 story 变成跨浏览器视觉测试并在 PR 上产生 UI Tests 检查，适合在 Figma 语义审查后再做像素级视觉回归验证。 | https://storybook.js.org/docs/writing-tests/visual-testing |
| Codex GitHub Action 可在 CI 中重复执行 Codex review、保存 `output-file`/结构化输出并把结果回贴 PR，适合承接多 Agent 聚合结果并进入后续自动化发布链路。 | https://developers.openai.com/codex/github-action |
| GitHub Checks API 可把审查摘要和逐行 annotations 绑定到指定 commit SHA，适合将设计/代码多 Agent findings 固定到具体提交并提供可追踪的审查证据。 | https://docs.github.com/en/rest/checks/runs |
| GitHub Projects API 可把 PR/issue 或审查摘要草稿直接加入 Project v2 并更新字段，适合把自动审查结果程序化推入项目看板并继续状态流转。 | https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/using-the-api-to-manage-projects |
| Git 数据模型说明 commit 固定引用 tree，而 tree 中每个文件固定引用对应的 blob 对象；把 `PLAN.md` 随代码一起提交，可让某次 commit 天然绑定到该版计划内容。 | https://git-scm.com/docs/gitdatamodel.html |
| `git ls-tree <commit> -- PLAN.md` 可直接取出该提交中的计划文件 blob OID，适合 CI 自动核对 `Plan-Hash` 与 commit 实际携带的 `PLAN.md` 是否一致。 | https://git-scm.com/docs/git-ls-tree |
| Qodo v2 官方文档明确由多个 review agents 共享上下文并应用规则系统，可作为 Codex 外部独立评审层，对 bug、需求缺口和规范问题做多 Agent 聚合审查。 | https://docs.qodo.ai/code-review |
| Claude Code 的 agent teams 可并行运行多个 Agent 并自主协调，官方明确适合 codebase reviews；可作为另一套编排器，与 Codex/Qodo 的结果交叉验证。 | https://www.anthropic.com/news/claude-opus-4-6 |
| Figma MCP Catalog 明确 Claude Code 与 Codex 都是受支持的 MCP 客户端，可让同一设计文件上下文同时进入不同 review Agent，降低单一模型设计审查偏差。 | https://www.figma.com/mcp-catalog/ |
| Linear 的 Codex 集成支持在 issue 中分派或 `@Codex`，Codex 会持续回写进度并附上完成任务链接，适合把聚合审查 findings 变成看板任务并回流 Codex 继续修复。 | https://linear.app/integrations/codex |
| Linear MCP 可让 Codex 查找、创建、更新 issue、project 和 comment，适合由审查编排器把结果程序化推入项目看板，再由 Codex 读取后进入下一轮修复。 | https://linear.app/docs/mcp |
| Linear Agents 可被委派 issue、回复评论并协作 projects/documents，适合把 reviewer、judge、design-reviewer 的结果分角色沉淀到项目或文档，而不是只留在 PR 评论里。 | https://linear.app/docs/agents-in-linear |
| GitHub Artifact Attestations 可对 `PLAN.md` 或计划归档生成带 digest 的签名 provenance；配合 CI 中的 commit SHA 校验，可把“计划文件版本 ↔ 提交 ↔ 审查产物”从普通元数据升级成可验证、可追溯的证据链。 | https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations |
| GitHub Agentic Workflows 用 Markdown 定义带权限、工具和安全输出的智能工作流，并明确支持 PR review、文档维护和报告；适合把 Codex 审查、设计校验和结果汇总拆成可复用的编排节点。 | https://docs.github.com/en/copilot/concepts/agents/about-github-agentic-workflows |
| Agentic Workflows 的 AI engine 层原生支持 Codex、Claude Code、Copilot、Gemini 和 Pi；可为不同审查角色选择不同引擎，再由 GitHub Actions 统一触发与汇总，形成真正的交叉 Agent review。 | https://github.github.com/gh-aw/reference/engines/ |
| Agentic Workflows 的 safe outputs 原生支持逐行 review comment、汇总 review、回复/解决线程和添加 reviewer，并带写入边界；适合把多 Agent findings 安全回贴 PR，再继续进入修复或看板流转。 | https://github.github.com/gh-aw/reference/safe-outputs-pull-requests/ |
| Applitools Eyes MCP 将确定性的视觉检测直接暴露给 AI coding assistant，并能返回结构化视觉结果；适合作为 Figma 语义审查之外的独立视觉验证 Agent，避免“生成代码的模型自己验证自己”。 | https://applitools.com/blog/mcp-ai-coding-assistants-visual-testing/ |
| Chromatic 可把 Figma component 与对应 Storybook story 直接关联并并排查看，设计层级和实现组件能在同一审查界面对照；很适合给 design-reviewer 提供明确的“设计源 ↔ 代码组件”映射。 | https://docs.chromatic.com/docs/figma-in-chromatic/ |
| Chromatic UI Review 会在 PR 上自动生成视觉 changeset、讨论线程、多人审批 checklist 和状态检查；适合把设计审查变成可阻断合并的独立质量门，而不是只让 LLM 输出文字意见。 | https://docs.chromatic.com/docs/review/ |
| Cursor Bugbot 可独立审查 PR、读取既有评论上下文，并通过 API/dry-run 返回包含 `commit_sha` 和 findings 的结构化结果；适合作为 Codex 外部 reviewer，再把结果交给聚合 Agent 或看板。 | https://prod.cursor.com/docs/bugbot |
| Atlassian Rovo MCP 已直接支持 Codex Desktop/CLI，并能搜索、创建和更新 Jira work item 与 Confluence page；适合把聚合后的审查结果推入项目看板或文章，再由 Codex 继续读取和修复。 | https://support.atlassian.com/atlassian-rovo-mcp-server/docs/getting-started-with-the-atlassian-remote-mcp-server/ |
| Jira 已能把 work item 一键打开到 Codex，并通过 Rovo MCP 让 Agent 继续读取上下文和更新任务；适合作为“审查 findings → 看板任务 → Codex 修复”的可操作闭环。 | https://support.atlassian.com/jira-software-cloud/docs/open-a-jira-work-item-in-an-ai-coding-tool/ |
| c-CRAB 代码审查基准显示现有审查 Agent 合起来仍只能解决约四成任务，说明单一 reviewer 可靠性不足；可作为采用 Codex + 独立代码 reviewer + 视觉 reviewer + 人工 gate 的实证依据。 | https://arxiv.org/abs/2603.23448 |
| Git 的 `<commit>:<path>` 修订语法可直接寻址某次提交里的 `PLAN.md` blob，适合 CI 用 commit SHA 精确解析“该提交对应哪一版计划文件”，避免只依赖工作区当前文件。 | https://git-scm.com/docs/revisions |
| `git cat-file` 可验证并读取由 `<commit>:PLAN.md` 指向的对象内容、类型和存在性，适合在自动审查前确认 plan 绑定对象真实存在且内容可复现。 | https://git-scm.com/docs/git-cat-file |
| `git diff-tree` 可直接按 commit/tree 和路径比较 blob 变化，适合 CI 检查代码提交是否同时携带预期的 `PLAN.md` 更新，或阻止 plan 与实现版本脱节。 | https://git-scm.com/docs/git-diff-tree |
| `git verify-commit` 能验证签名提交；当 `PLAN.md` 已包含在该 commit tree 中时，可把“计划版本 + 实现 + 提交者签名”作为同一个可验证审查证据单元。 | https://git-scm.com/docs/git-verify-commit |
| Storybook MCP 可把真实组件、stories、文档和组件/可访问性测试暴露给 Codex 等 Agent；配合 Figma reviewer，可用“设计文件 + 真实设计系统实现 + 自动测试”三层上下文审查 UI 代码。 | https://storybook.js.org/docs/ai/mcp/overview |
| OpenAI Agents SDK 的 orchestration 支持 manager/handoff、结构化输出、并行 Agent 和 evaluator loop，适合在 Codex 外编排 security、architecture、design reviewer 与 judge，再聚合成统一 findings。 | https://openai.github.io/openai-agents-python/multi_agent/ |
| Greptile 会基于完整代码库上下文独立审查 PR，并可把具体 finding 直接发送到 Codex 等 coding agent 修复，适合作为与 Codex 解耦的第二 reviewer 和回流通道。 | https://www.greptile.com/docs/introduction |
| GitHub Agentic Workflows 的 Safe Outputs 除 PR 评论外还可创建/更新 Discussion、Project 和 Project status update；适合把多 Agent 审查摘要安全发布成文章化记录或项目看板状态。 | https://github.github.com/gh-aw/reference/safe-outputs/ |
| GitHub Rulesets 支持用正则限制 commit metadata，并可叠加 required status checks/workflows；可把 `Plan-File`/`Plan-Hash` 从约定升级为 push/merge 门禁，再强制经过多 Agent 审查。 | https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/creating-rulesets-for-a-repository |
| GitHub commit comments API 可按 `commit_sha` 创建评论并定位文件位置；适合把多 Agent 聚合 findings 固定到与 `PLAN.md` 对应的具体提交，形成可回溯审查记录。 | https://docs.github.com/en/rest/commits/comments |
| coding-review-agent-loop 原生支持 plan-first 审查，在写代码前由 reviewer 明确阻断/批准计划，并可让 Codex、Claude、Gemini 分别承担规划、实现、复审角色，直接形成 plan→实现→复审循环。 | https://github.com/wwind123/coding-review-agent-loop/blob/main/README.md |
| Gossipcat 用 3+ Agent 独立审查后互相核验真实 `file:line` finding，再做共识和可靠性评分；很适合作为 Codex 外部的交叉验证层，降低单 reviewer 幻觉。 | https://github.com/gossipcat-ai/gossipcat-ai |
| Agent Switchboard 通过本地 MCP 在 Codex、Claude Code、Gemini 等之间做任务移交、共享压缩上下文和多轮 cross-model debate，可充当独立于 Codex 的多 Agent review 编排总线。 | https://github.com/FutureisinPast/mcp-agent-switchboard |
| 2026 年交叉模型实验显示，在该 116 题受控样本中，Claude 审 Codex 将通过率从 71.6% 提升到 89.7%，且 review 方向存在明显非对称性；可作为“生成 Agent 与审查 Agent 不应同配方”的实证依据。 | https://arxiv.org/abs/2607.21656 |
| 2026-08 的大规模 GitHub 研究覆盖 248,641 个收到 AI review 的 AI-attributed PR，并观察到跨产品 AI review 持续增长；适合作为引入 Codex + 外部 reviewer 闭环的最新生态证据。 | https://arxiv.org/abs/2608.21311 |
| Codex app-server 提供双向 JSON-RPC 的 thread/turn/item API，并支持把 durable thread 关联到 `projectId`；外部审查编排器可把聚合 findings 机器化回流到指定 Codex 项目线程继续修复。 | https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md |
| Figma 官方规则示例要求先取 `get_design_context` 和 `get_screenshot`，最后按 Figma 做 1:1 外观与行为校验；可直接固化成 design-reviewer 的必经审查协议。 | https://developers.figma.com/docs/figma-mcp-server/add-custom-rules/ |
| Figma Code to canvas 支持把运行中的 UI 捕获成可编辑设计层，评审后再把 Figma 决策回传给 Codex 等 Agent 实现，形成“代码 → 设计审查 → 代码”的可重复闭环。 | https://developers.figma.com/docs/figma-mcp-server/code-to-canvas/ |
| `git commit-tree` 直接以给定 tree 创建 commit object；若 `PLAN.md` 与实现代码同在该 tree 中，commit OID 就由包含计划文件的精确目录状态派生，适合做底层可验证的“计划版本 ↔ 提交”绑定。 | https://git-scm.com/docs/git-commit-tree.html |
| `pr-review-mcp` 把 CodeRabbit、Gemini、Copilot、Sourcery、Qodo、Codex、Greptile 等 reviewer 的结果统一规范化，并提供并行调用、等待和工作分片能力，适合作为 Codex 外部多 Agent 审查控制面。 | https://github.com/thebtf/pr-review-mcp |
| Factory Custom Droids 可把 correctness、security、architecture、design 等 reviewer 定义成独立只读 subagent，由父 Agent 分派并汇总 findings，适合作为额外的可编排多 Agent review 层。 | https://docs.factory.ai/harness/subagents |
| Factory 的 MCP 支持把 Figma、Playwright 等外部上下文和工具挂给 reviewer；可让 design-reviewer 同时读取设计文件与运行界面，再与代码 reviewer 结果交叉汇总。 | https://docs.factory.ai/harness/mcp |
| Factory Automated Code Review 可在 PR 事件上自动运行、读取 diff 与既有评论、发布逐行 finding 或 approval；适合把外部 Agent 审查作为独立 CI gate，再将结果交回 Codex 修复。 | https://docs.factory.ai/software-factory/code-review-ci |
| OpenAI 与 Figma 的 Codex 集成支持代码→设计画布→设计决策→代码的双向工作流，可让同一设计文件成为 Codex 实现和独立设计审查的共同事实源。 | https://openai.com/index/figma-partnership/ |
| Figma 官方明确 MCP 负责提供 frames、components、layout、tokens、variables 等结构化设计上下文，而最终实现由 Agent 完成；适合把“设计上下文完整性”设成独立 reviewer 的审查输入与判定边界。 | https://developers.figma.com/docs/figma-mcp-server/mcp-vs-agent/ |
| GitHub Status Checks 可由外部系统把聚合审查结论绑定到 commit，并在受保护分支设为 required；适合把 plan 校验、代码 reviewer、design reviewer 的统一结论变成不可绕过的合并门禁。 | https://docs.github.com/en/pull-requests/reference/status-checks |
| GitHub Merge Queue 会在合并组上等待所有 required checks 通过后再入主干；适合在多 Agent/设计审查都通过后，对最新目标分支状态再次执行最终质量门，避免审查通过后因基线变化失效。 | https://docs.github.com/en/pull-requests/how-tos/merge-and-close-pull-requests/merging-a-pull-request-with-a-merge-queue |
| `git show <commit>:PLAN.md` 可直接读取指定提交中的计划文件内容，适合在审查流水线中把 commit SHA 解析回当时实际生效的 plan，而不是读取工作区当前版本。 | https://git-scm.com/docs/git-show |
| `git rev-parse --verify <commit>:PLAN.md` 可解析并校验某提交中计划文件对应的对象 ID；适合把 plan 绑定检查做成失败即阻断的 CI 前置条件。 | https://git-scm.com/docs/git-rev-parse |
| Claude Code Code Review 会让多个专门 Agent 并行审查 diff 和完整代码库，再执行候选 finding 验证；可作为 Codex 之外的独立多 Agent reviewer，降低同模型自审偏差。 | https://code.claude.com/docs/en/code-review |
| Open Code Review 原生识别 Codex 的 `.codex/` 配置，并由 Tech Lead 编排并行 reviewer、交叉讨论、聚合与最终 synthesis；还支持按 spec/requirements 审查，适合直接接在 Codex plan 后。 | https://github.com/spencermarx/open-code-review |
| Codex Council 是面向 Codex 的多角色评审插件，通过独立意见、匿名互评、rubric 评分、异议保留和 Chairman 汇总，可把实现计划和高风险 diff 先过“评审委员会”再执行。 | https://github.com/ercoledevs/codex-council |
| Playwright MCP 官方支持 Codex，可让 reviewer 在真实浏览器中读取结构化页面状态、截图并执行交互；与 Figma 设计上下文配合时，可验证实现后的行为和可访问性而不只做静态代码审查。 | https://github.com/microsoft/playwright-mcp |
| Argos 已提供面向 AI Agent 的视觉审查流程，Agent 可获取 PR 对应的截图变化、分析视觉差异并提交 build review；适合作为 design-reviewer 的独立视觉回归通道。 | https://argos-ci.com/blog/review-builds-ai-agents |
| Percy 的视觉审批会与 commit/PR 状态同步，并可将视觉检查设置为合并阻断项；适合把设计文件审查后的视觉结论固化为独立质量门。 | https://www.browserstack.com/docs/percy/overview/visual-testing-basics |
| Notion MCP 官方文档明确给出 Codex 配置方式，连接后 Codex 可读写 Notion 工作区；适合把多 Agent 审查摘要直接沉淀成项目页面、技术文章或持续更新的审查记录。 | https://developers.notion.com/guides/mcp/get-started-with-mcp |
| Notion MCP 的工具可组合搜索、创建页面并更新属性/数据库，适合把聚合 findings 转成结构化项目条目、报告和后续任务，而不是只停留在 PR 评论。 | https://developers.notion.com/guides/mcp/mcp-supported-tools |
| GitHub Projects GraphQL 原生提供 `createProjectV2StatusUpdate`、项目视图和状态字段；适合把审查摘要作为项目状态更新推入看板，并保留可机器化追踪的生命周期。 | https://docs.github.com/en/graphql/reference/projects |
| GitHub Discussions GraphQL 可创建和持续更新 discussion 文章；适合把每轮多 Agent 审查结果发布成仓库内文章化记录，再从看板或 Codex 任务链接回该审查文章。 | https://docs.github.com/en/graphql/guides/using-the-graphql-api-for-discussions |
| `git update-ref` 可安全创建自定义 ref（如 `refs/plans/<plan-id>`）指向具体 commit，并可记录 reflog；适合把 plan ID/版本做成独立、可机器查询且可原子更新的“计划 → 提交”索引。 | https://git-scm.com/docs/git-update-ref |
| Codex Hooks 可在 repo 内对 `Write/Edit`、Subagent、Stop 等生命周期事件运行命令或 MCP reviewer，并能阻断/继续流程；适合在计划执行、代码修改和提交前自动校验 `PLAN.md` 绑定及触发外部审查。 | https://developers.openai.com/codex/hooks/ |
| Codex SDK 可在 CI/内部编排器中启动、继续和恢复 Codex thread；聚合 reviewer findings 后可直接作为下一轮输入回流同一线程，适合把外部多 Agent 审查接回 Codex 修复闭环。 | https://developers.openai.com/codex/sdk/ |
| `codex exec` 的 `--json`/`--output-schema` 能把 plan updates、文件修改、MCP 调用和最终 findings 输出成机器可读 JSON/JSONL，并可 `resume` 原会话；适合作为审查编排器与 Codex 之间的结构化回流接口。 | https://developers.openai.com/codex/noninteractive/ |
| OpenAI 官方 Claude Code 插件可让 Claude 工作流调用 Codex 做只读 review/对抗式 review、设置 review gate，并能把 Claude 会话 transfer 成可在 Codex 继续的持久 thread；适合构建 Claude↔Codex 双 Agent 交叉审查与回流。 | https://github.com/openai/codex-plugin-cc |
| OpenAI 的 Codex harness 指南明确支持把 app-server 嵌入现有 dashboard、queue 或 task board，创建 thread、流式事件、处理审批并把结果返回系统记录；适合把聚合审查接入项目看板后继续驱动 Codex。 | https://learn.chatgpt.com/blog/codex-as-a-platform |
| Codex Code Review 可从 `AGENTS.md` 读取作用域化 repository rules 并在 finding 中引用规则；可把 plan 约束、设计系统规则和兼容性要求固化为审查标准，让外部 reviewer 与 Codex 使用同一规则基线。 | https://learn.chatgpt.com/blog/custom-code-review-rules-for-codex |
| Figma Webhooks V2 可在文件版本、评论、library publish 等设计变更发生时触发集成；适合自动启动 design-reviewer，并把最新组件、style、variable 变更与对应 commit/PR 一起重审。 | https://developers.figma.com/docs/rest-api/webhooks/ |
| Figma Dev Resources API 可把 URL 直接附着到具体设计节点并支持双向 linking；可把 commit、PR 或审查文章链接到对应 Figma node，实现“设计节点 ↔ 代码提交 ↔ review 结果”的结构化关联。 | https://developers.figma.com/docs/rest-api/dev-resources/ |
| Google ADK 官方把 Parallel Fan-Out/Gather 直接用于 Automated Code Review：Security、Style、Performance reviewer 并行，再由 synthesizer 汇总；适合做 Codex 之外的独立多 Agent 审查层并加入 design reviewer。 | https://developers.googleblog.com/developers-guide-to-multi-agent-patterns-in-adk/ |
| AutoGen GraphFlow 用有向图精确编排多 Agent，原生支持并行、条件分支和循环；适合把 plan verifier、code reviewer、design reviewer、judge、publisher 固化成可重复且有终止条件的审查图。 | https://microsoft.github.io/autogen/stable/user-guide/agentchat-user-guide/graph-flow.html |
| 2026-08 的 Adversarial Review 研究以 reviewer + critic + coding agent 建立结构化异议协议，在其实验中优于更大的多 Agent baseline；适合作为聚合阶段加入 critic 校验、避免简单多数投票和 false-consensus 的最新依据。 | https://arxiv.org/abs/2608.18167 |
| Git pretty-format 可直接用 `%(trailers:key=Plan-Hash,valueonly)` 从 commit message 提取指定 trailer；适合 CI 在不解析自由文本的情况下把 `PLAN.md` 哈希与提交稳定关联并自动核验。 | https://git-scm.com/docs/pretty-formats.html |
| GitHub 官方 MCP Server 明确支持 Codex，并覆盖仓库、Issue、PR、Actions、Discussions 与项目看板相关能力；适合作为 reviewer/publisher 的统一 GitHub 控制面，把 findings 从代码审查直接送进任务或文章记录。 | https://github.com/github/github-mcp-server |
| GitHub MCP Server 提供 Codex 专用安装指南，可直接写入 `~/.codex/config.toml`，并建议最小权限 PAT；适合把“Codex 审查 → GitHub PR/Issue/看板”链路标准化到官方 MCP 接口。 | https://github.com/github/github-mcp-server/blob/main/docs/installation-guides/install-codex.md |
| GitHub MCP 的 server configuration 支持按 toolset/单工具启用以及 read-only 模式；适合把代码 reviewer 设成只读、publisher 单独授予写权限，降低多 Agent 编排中的越权和误写风险。 | https://github.com/github/github-mcp-server/blob/main/docs/server-configuration.md |
| Figma Variables API 可把颜色、间距、字号等设计 token 以结构化数据拉入 CI/Agent，并能追踪已发布变量版本；适合让 design-reviewer 自动比对实现中的 CSS/组件 token 是否与设计源一致。 | https://developers.figma.com/docs/rest-api/variables/ |
| Figma File endpoints 可按指定 file version 获取节点、组件、样式和 annotations；适合把“被审查的设计版本”固定下来，再与具体 commit/PR 一起审查，避免设计文件持续变化造成基准漂移。 | https://developers.figma.com/docs/rest-api/file-endpoints/ |
| Figma Comments API 支持读取与回写文件评论；可让设计审查 Agent 把与 commit/PR 对应的 finding 直接落到 Figma 节点附近，同时保留代码侧审查与设计侧讨论的双向证据。 | https://developers.figma.com/docs/rest-api/comments-endpoints/ |
| Figma Node 数据包含 `devStatus`、styles、annotations 等机器可读字段；适合让 reviewer 只对 Ready for dev 节点执行实现核验，并把设计注释/固定属性作为自动审查规则输入。 | https://developers.figma.com/docs/rest-api/file-node-types/ |
| ACR 会并行启动多个 Codex/Claude/Antigravity reviewer，再做聚类、去重、false-positive 过滤并可回贴 PR；很适合作为 Codex 之外的编排层，把不同模型的 finding 汇总成单一审查结论。 | https://github.com/richhaase/agentic-code-reviewer |
| Codex 公开 issue 记录了 delegated review 中“需审批的 MCP 工具”可能因 elicitation 无法转发而卡住；设计自动化多 Agent review 时应优先使用只读/免审批工具，并配置超时与降级路径。 | https://github.com/openai/codex/issues/31565 |
| `git commit --trailer` 可在创建提交时直接写入 `Plan-File`、`Plan-Hash` 等结构化元数据，省去事后改写步骤，适合把 Codex 的计划文件版本在提交产生的一刻就绑定到 commit。 | https://git-scm.com/docs/git-commit |
| GitHub 支持把第三方审查结果以 SARIF 上传，并可用 category 在同一 commit 上保留多套分析；适合让 code、security、design reviewer 分别输出结构化 findings，再统一显示到 PR/代码扫描结果。 | https://docs.github.com/en/code-security/how-tos/find-and-fix-code-vulnerabilities/integrate-with-existing-tools/upload-sarif-file |
| GitHub Agent HQ 可在同一仓库工作流中并行使用 Codex、Claude、Copilot 等 Agent，产出保留为 draft PR 和评论；适合作为跨模型审查与 Codex 修复回路的统一协作面。 | https://github.blog/news-insights/company-news/pick-your-agent-use-claude-and-codex-on-agent-hq/ |
| `multiagents` 通过 MCP 编排 Claude Code、Codex CLI、Gemini CLI，并提供 reviewer/designer 等角色、实时通信和 review/approval 状态机；很适合把代码审查与设计审查拆给不同 Agent 后交叉汇总。 | https://github.com/zetbrush/multiagents |
| 该 Codex-centric 编排库把 `PLAN.md`、本地 commits 等作为 durable checkpoints，并由 Codex 对委派 Agent 的产物执行独立复审循环；与“计划文件 ↔ 提交 ↔ 多 Agent 审查”目标高度一致。 | https://github.com/formiat/multi-agent-orchestration |
| Figma Agent Loop 让 Codex 先生成设计、再通过 Figma MCP 读取人工编辑后的 frame，并把经审查的 Figma frame 提升为下一步实现的事实源；适合在写代码前设置明确的设计 gate。 | https://github.com/OlliberK/figma-agent-loop |
| Bastion 把部署预览的设计审查封装成独立 MCP judge，返回结构化 finding、证据和 recheck 结果且自身不改代码；适合让设计 reviewer 与 Codex 实现 Agent 解耦，避免自审偏差。 | https://github.com/apatureai/bastion |
| Layout CLI/MCP 可向 Agent 提供 design-system tokens、组件上下文和实时 preview，并能把代码推回 Figma、再读取设计修改形成闭环；适合给 design-reviewer 同时提供设计规范与实际渲染证据。 | https://github.com/uselayout/cli |
| Figma 官方 Workflow Lab 展示从代码到 canvas、再由 Agent 继续设计与实现的工作流，可作为把 Figma 设计评审插入多 Agent 编排链路的官方参考流程。 | https://help.figma.com/hc/en-us/articles/40219873508247-Workflow-lab-Code-to-canvas |
| agent-taskboard 让 Codex、Claude 等 Agent 直接认领任务、把完成项转到 review，并把 reject 原因写回任务供下一 Agent 处理；适合把聚合审查 findings 推入可操作看板并驱动后续修复。 | https://github.com/52216108/agent-taskboard |
| Annotated tag 可直接指向某个 commit，并支持 tag message、签名及 `--trailer`；可用 `plan/<id>` 标签携带 `Plan-File`/`Plan-Hash`，形成独立于 commit message 的“计划版本 → 提交”可验证绑定。 | https://git-scm.com/docs/git-tag |
| `git for-each-ref` 支持按 ref 命名空间遍历、`--points-at` 反查具体 commit，并格式化 tag/object 字段；适合 CI 查询 `refs/plans/*` 或 plan tags，自动建立 commit 与计划索引。 | https://git-scm.com/docs/git-for-each-ref |
| `git archive` 对 commit/tag 归档时会保存对应 commit ID；可把该提交里的 `PLAN.md`、审查摘要和证据一起导出成可复现归档，便于审查结果与计划/提交长期追溯。 | https://git-scm.com/docs/git-archive |
| Figma 原生 Design Agent 可直接在设计文件内做可用性、视觉层级、可访问性等 critique，并支持并行提示与共享线程；适合作为代码 reviewer 之外的独立 design reviewer，再把结论送入 Codex 修复链路。 | https://www.figma.com/solutions/design-review/ |
| Figma 2026 Workflow Lab 展示把生产代码接入 Figma、在设计侧完成修改与团队评审并一路推进到 PR；适合把“设计审查 → 代码变更 → PR”做成明确的可审计闭环。 | https://www.figma.com/blog/workflow-lab-deploying-designs-directly-with-figma-make/ |
| UX Autopilot 用 PM、UX、设计系统、前端等多 Agent 串联 Brief→设计系统→React→Figma Review→PR，直接展示了“多 Agent 编排 + 设计文件 gate + 代码交付”的完整参考实现。 | https://github.com/chowan2/ux-autopilot |
| Manki 由 planner 按 PR 复杂度动态选择 1/3/5/7 个 reviewer，并行审查后经 dedup 与 judge 过滤，还支持混用 OpenAI/Anthropic/Gemini；适合做 Codex 外部的自适应多 Agent review 层。 | https://github.com/manki-review/manki |
| ReviewCrew 由 orchestrator 并行调度 bugs、security、git history、tests 等专门 reviewer，统一置信度评分、去噪并可回贴 GitHub PR；适合与 Codex 形成“实现 Agent + 独立多角色 reviewer”组合。 | https://github.com/MANOJ21K/agentic-code-review |
| Codex Taskboard 可直接嵌入 Codex，Skill/CLI/API 能创建 project/issue、记录 Codex thread，并把 issue 绑定 branch/worktree；任务描述和评论支持 Markdown，适合把聚合审查结果作为看板文章/任务回流 Codex。 | https://github.com/SoeyMoner/codex-taskboard |
| Agent OS 是面向 Codex 的本地 task board + MCP 控制面，强调跨项目路由、精确 Codex task 关联、PR/source context 与 durable outcomes；适合把自动审查 findings 沉淀成可追踪项目项并继续驱动 Codex。 | https://github.com/andrewgolovanov/agent-os |
| `git write-tree` 会从当前 index 生成精确 tree OID；先确保 `PLAN.md` 与实现都已 staged，再记录/校验该 tree，可在 commit 创建前得到包含“计划 + 代码”的不可变快照指纹。 | https://git-scm.com/docs/git-write-tree |
| `git diff-index --cached` 可按路径比较基准 tree 与 index；适合在 pre-commit/CI 中只检查 `PLAN.md` 是否与代码一起进入暂存快照，阻止“代码已提交、计划仍停留在工作区”的脱节。 | https://git-scm.com/docs/git-diff-index |
| GitHub Git Trees API 可基于 `base_tree` 把 `PLAN.md` 与实现文件的 blob 一次组合成新 tree，再把该 tree 交给 commit；适合服务端自动化构造可精确追溯的“计划文件版本 + 代码版本”提交快照。 | https://docs.github.com/en/rest/git/trees |
| GitHub Git Commits API 创建 commit 时必须显式指定 `tree` SHA 与 parent；可让编排器把已包含 `PLAN.md` 的 tree 固定进 commit，对外形成可验证的计划—实现绑定。 | https://docs.github.com/en/rest/git/commits |
| GitHub Git References API 可创建或更新独立 ref 指向某个 commit；可用 `refs/plans/<plan-id>` 一类命名在远端建立持久的“计划 ID → commit SHA”索引，便于审查器和看板跨机器查询。 | https://docs.github.com/en/rest/git/refs |
| Calimero AI Code Reviewer 会并行运行多个专门 reviewer，对安全、性能、质量等 finding 做共识加权，并支持增量审查与收敛停止；适合作为 Codex 外部的独立多 Agent 复核层。 | https://github.com/calimero-network/ai-code-reviewer |
| morch 专门编排 Cursor、Claude、Codex 的代码审查流水线，保留状态与 provenance，并支持 GitHub issue→branch→PR→review→human gate；适合把 Codex 实现、外部复审和人工放行串成同一可审计流程。 | https://github.com/enginrect/multi-agent-orchestrator |
| Microsoft Agent Framework Workflows 提供 concurrent、group chat、Magentic 等内置多 Agent 编排，以及 checkpoint 与 human-in-the-loop；适合把 plan verifier、code reviewer、design reviewer、judge、publisher 固化成可恢复的审查图。 | https://learn.microsoft.com/en-us/agent-framework/workflows/ |
| Figma 的 agentic framework 指南明确给出“一个 Agent 读 Figma、另一个处理实现、由 orchestrator 排序，并加入 QA/critique Agent”的模式；可直接作为设计文件驱动的多 Agent 代码审查架构参考。 | https://www.figma.com/resource-library/agentic-framework/ |
| Figma Design Agent 在画布内原生处理设计探索与 critique，可让设计侧 Agent 独立检查视觉层级、可用性、可访问性等，再把结论交给代码 reviewer/Codex 修复，降低实现 Agent 自审偏差。 | https://www.figma.com/blog/the-figma-agent-is-here/ |
| Better Codex 把 Task board 与 Agent profiles 直接放进 Codex Desktop，任务可关联会话并在需要 review/decision 时回到看板；适合把聚合 findings 变成 Codex 内的 project/task 并继续沿原线程修复。 | https://github.com/Ericwong5021/better-codex |
| agent-board 为 Codex、Claude Code 等提供共享 Markdown 看板与 CLI，除 tasks 外还持久化 goals、specs、knowledge 和验证证据；适合把审查摘要既作为项目任务，也作为可长期复用的文章化知识记录。 | https://github.com/questpie/agent-board |
| 先把已批准 `PLAN.md` 做成独立 plan commit，再用 `git merge-base --is-ancestor` 强制实现提交必须继承该 plan commit；配合 `Plan-Commit` trailer，可形成比仅记录文件名更稳的“计划提交 → 实现提交”祖先绑定。 | https://git-scm.com/docs/git-merge-base |
| 若用 Git notes 保存 `Plan-Hash`/审查元数据，`notes.rewriteRef` 与 `notes.rewrite.<command>` 可让 notes 在 amend/rebase 重写提交时复制，避免计划绑定因 SHA 改写而丢失。 | https://git-scm.com/docs/git-config |
| `git rev-list` 可遍历提交可达关系并按路径/对象审计计划到实现之间的历史，适合 CI 生成“plan commit → implementation commit”的证据链并检查中间提交是否偏离计划基线。 | https://git-scm.com/docs/git-rev-list |
| CrewAI Flows 支持事件驱动、共享状态、分支/循环以及多个并行起点，可把 plan verifier、代码 reviewer、design reviewer、judge、publisher 编排成可持久化的多 Agent 审查流。 | https://docs.crewai.com/en/concepts/flows |
| LangChain Subagents 让主 Agent 把专家 Agent 包装成工具，并支持独立任务并行与上下文隔离；适合让安全、架构、测试、设计 reviewer 分工执行，再由 supervisor 聚合回流 Codex。 | https://docs.langchain.com/oss/python/langchain/multi-agent/subagents |
| GitHub Actions 的 `needs` 和 matrix 可把不同 reviewer 拆成独立 CI job，并让最终 synthesis/publisher job 等待全部结果，给跨模型审查增加确定性的 fan-out/fan-in 编排层。 | https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/use-jobs |
| Workflow artifacts 可跨 job 保存和传递 reviewer JSON、截图及证据文件，适合让代码/设计 Agent 各自产出证据，再由聚合 job 统一生成可推送到 Codex 或看板的审查摘要。 | https://docs.github.com/en/actions/tutorials/store-and-share-data |
| cross-model-review 把设计文档审查、实现计划审查和最终 diff 审查设成三个自动 checkpoint，并用 Claude↔Codex 对抗式复核，直接覆盖“设计 → plan → 代码 → 交叉 review”链路。 | https://github.com/TimSimpsonJr/cross-model-review |
| Agentic Design System 可直接安装到 Codex，并用 intent、baseline、rubric、rendered evidence、review、revise 的固定 gate 审查真实 UI 输出，适合作为独立设计审查层。 | https://github.com/aa-on-ai/agentic-design-system |
| Figma MCP 的 `get_design_context`、`get_screenshot`、`get_variable_defs`、`get_code_connect_map` 等工具可同时提供结构、视觉、token 与代码映射，适合 design-reviewer 做多证据交叉核验。 | https://developers.figma.com/docs/figma-mcp-server/tools-and-prompts/ |
| Figma 官方建议组件化、Code Connect、Variables、语义命名、Auto Layout、annotations 与 dev resources，可把这些设为自动审查前的“设计文件可机器审查性”门禁，减少 Agent 因设计歧义误判。 | https://developers.figma.com/docs/figma-mcp-server/structure-figma-file/ |
| Asana 官方 V2 MCP 已给出 Codex 专用连接与建任务方式，可把多 Agent 聚合 findings 直接写入 Asana 项目/看板，再由 Codex 从同一 MCP 读取任务继续修复。 | https://developers.asana.com/docs/connecting-mcp-clients-to-asanas-v2-server |
| `git update-index` 可在提交前把指定 `PLAN.md` 精确注册到 index，并配合 `--refresh` 检测暂存快照是否仍与工作区一致；适合把“计划文件必须进入本次提交”做成 pre-commit/CI 的确定性前置校验。 | https://git-scm.com/docs/git-update-index |
| Figma Code Connect 的 CI/CD 指南支持仅在相关组件映射变更时发布，并可用 `--exit-on-unreadable-files` 让流水线失败；适合把“设计组件 ↔ 代码组件”映射完整性作为 design-reviewer 的独立质量门。 | https://developers.figma.com/docs/code-connect/ci-cd/ |
| Code Connect CLI 的 `parse` 可输出 JSON、`preview --inspect/--all` 可枚举组件属性与变体并验证渲染；适合把结构化设计映射直接喂给多 Agent reviewer，而不是只依赖截图。 | https://developers.figma.com/docs/code-connect/cli-reference/ |
| GitHub Copilot Code Review 的定制指南明确支持 `AGENTS.md`、仓库级和路径级 instructions；可把 plan 约束、设计系统规则、安全规范拆成可复用审查策略，让独立 reviewer 与 Codex 共享同一规则基线。 | https://docs.github.com/en/copilot/tutorials/customize-code-review |
| Copilot Code Review 概念页明确支持 repository agent skills 与 MCP servers，并记录 review session 的 MCP/tool 调用；适合把 Figma/项目上下文注入第二套 reviewer，同时保留可审计的外部工具使用证据。 | https://docs.github.com/en/copilot/concepts/agents/code-review |
| Commit Statuses API 可把不同 reviewer 用独立 `context` 绑定到同一 commit SHA，并给每个状态挂 `target_url`；适合聚合 plan、代码、设计审查结论并链接到 Codex/项目文章或看板。 | https://docs.github.com/en/rest/commits/statuses?apiVersion=2026-03-10 |
| GitHub Projects 内置自动化可在 item 加入、PR 合并/关闭等事件后自动更新 Status 或归档；适合让审查 findings 进入项目后自动流转 Todo→In review→Done，减少 publisher Agent 的重复写操作。 | https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/using-the-built-in-automations?apiVersion=2022-11-28 |
| `git log --follow -- PLAN.md` 可按计划文件路径追踪其完整提交历史（含重命名前历史），适合自动生成“计划版本 → 修改提交 → 后续实现提交”的审计索引，并核对 plan 是否在实现过程中被同步更新。 | https://git-scm.com/docs/git-log |
| codex-orchestrator 提供持久化 Codex/Claude Agent、角色团队、监督模式和 git worktree 隔离，并要求每轮返回 commit、测试结果或明确 review 决策，适合把 planner、implementer、reviewer 固化成可审计编排流水线。 | https://github.com/zm2231/codex-orchestrator |
| MCO 可从 Codex 等 coding agent 内启动多个不同 provider 的只读并行 review，再并排比较原始意见；适合作为轻量级交叉模型审查层，减少单一 reviewer 的盲点。 | https://github.com/mco-org/mco |
| adversarial-review 让 Claude 与 Codex 先独立审查，再多轮互相质疑 finding 并形成共识；适合在最终 synthesis 前加入对抗式复核，用来过滤 false positive 和保留真实分歧。 | https://github.com/alecnielsen/adversarial-review |
| UX Critique 可作为 Codex skill 直接审查 Figma flow，并把可编辑 critique cards 写回设计文件且验证覆盖率；适合把设计审查结果留在设计源旁，再由代码 reviewer/Codex 按 finding 修复实现。 | https://github.com/Thecsiz/ux-critique |
| Figma 公开了其 Agent 在代码编写阶段、每个 PR 和存量 monorepo 审计中的统一安全策略实践；适合作为把多 Agent review 统一到同一 policy/rubric、持续扫描而非一次性审查的工程参考。 | https://www.figma.com/blog/how-figma-stays-ahead-of-vulnerabilities-with-agents/ |
| Codex 社区最新 taskboard 提案明确要求把 issue 与 repository、branch、worktree、Codex thread 和 review 状态绑定；可作为“审查 finding → Codex 看板任务 → 原线程继续修复”的字段模型参考。 | https://github.com/openai/codex/discussions/37045 |
| Codex 关于 subagent MCP capability broker 的提案强调子 Agent 默认零权限、按需租赁 MCP 并可追踪释放；适合给 Figma/GitHub publisher 等敏感工具做最小权限隔离，避免多 Agent reviewer 共享全部凭据。 | https://github.com/openai/codex/issues/38353 |
| Open Island Codex Taskboard 会自动汇总活跃 Codex 任务并保留 `codex://threads/<id>` 精确跳回能力；适合把聚合审查结果做成看板任务，同时保持 finding 与原 Codex thread 的可追踪关联。 | https://github.com/jiatong-lab914/open-island-codex-taskboard |
| claude-code-review-council 会并行调用 Codex、Gemini 和多个安全/性能/逻辑/回归 specialist，再由 synthesis 步骤对照真实代码验证并去重 finding；适合作为 Codex 外部高风险变更的交叉模型 review council。 | https://github.com/yeameen/claude-code-review-council |
| Bubo 把 review 拆成 discover→review→filter→post→learn，并支持独立 verification lenses、置信度门槛、AI provenance 和 outcome sync；适合作为 Codex 外部 reviewer/publisher，把高置信 finding 绑定 PR 后持续学习人类处置结果。 | https://mountainowl.github.io/bubo/how-it-works/ |
| FigCC 通过本地桥接把 Figma 插件连接到 Codex App Server/Claude Code，可直接检查、推理并编辑 canvas；适合让 design reviewer 读取真实设计上下文、产出设计侧修订，再把同一会话身份回接代码实现。 | https://github.com/madebypan/FigCC |
| `git status --porcelain=v2` 提供稳定、可解析的 index/worktree 状态；可在 commit 前强制检查 `PLAN.md` 已进入暂存区且工作区没有未暂存漂移，避免提交绑定到旧计划快照。 | https://git-scm.com/docs/git-status |
| `git diff --cached -- PLAN.md` 直接展示下一次 commit 实际会包含的计划文件内容；可与 `Plan-Hash`/预期 blob 哈希做 CI 对照，把“计划文件已暂存”从约定变成可验证门禁。 | https://git-scm.com/docs/git-diff |
| `git worktree` 为同一仓库提供彼此独立的 HEAD/index/工作目录，适合让 implementer、code-reviewer、design-reviewer 在不同 worktree 并行运行，避免多 Agent 审查互相污染暂存状态。 | https://git-scm.com/docs/git-worktree.html |
| `git range-diff` 能比较同一补丁系列的两个版本；当 plan 或 review 后发生 amend/rebase 时，可让最终 reviewer 精确看到“上一轮已审提交 → 修订后提交”发生了什么变化。 | https://git-scm.com/docs/git-range-diff |
| `codex-review` 同时提供 plan 文件审查和 commit 后自动 Codex review：Claude 每次成功 `git commit` 后触发阻断式复审与修复循环，很适合把“计划先审 → 实现提交 → 提交再审”串成同一质量门。 | https://github.com/andreidavid/codex-review |
| `codex-review-loop` 可对计划先启动依赖、架构、安全、测试四个只读 Codex reviewer，再对实现启动 N 路并行 review、合并 findings；可直接把 design reviewer 作为额外 lane 接入。 | https://github.com/dkorobtsov/codex-review-loop |
| `claude-review-loop` 用 Codex multi-agent 并行审查并去重汇总到持久 review 文件，适合作为 Claude/Codex 之间的独立第二审查层，再把聚合结果推到看板或交回实现 Agent 修复。 | https://github.com/hamelsmu/claude-review-loop |
| Claude-et-Codex 会让 Codex 的 explorer 与 architecture reviewer 并行核对实现计划，并输出 APPROVE/CHANGES/BLOCK 与真实 `file:line` 证据；适合作为执行前的多 Agent plan gate。 | https://github.com/Pricing-Logic/Claude-et-Codex |
| HyperClaude 把 research→plan→plan review→implementation→code review→ship 做成带自动 revise loop 的门控流水线，并让 Codex 始终以只读 critic 身份运行，适合与负责实现的 Agent 解耦。 | https://github.com/zeikar/hyperclaude |
| Belmont 的 verification pipeline 把“按 Figma 做 Playwright 视觉对比”的 verification-agent 与独立 code-review-agent 并行执行；是把设计文件审查真正加入代码验收而非只比 diff 的直接参考。 | https://github.com/blake-simpson/belmont |
| Figma 官方 MCP Server Guide 除结构化设计上下文和 Code Connect 外，还提供 `generate-project-plan` workflow skill，可把 PRD/代码库生成 FigJam 项目计划，并用自定义规则固定 `get_design_context + get_screenshot + 1:1` 验证协议。 | https://github.com/figma/mcp-server-guide |
| GitHub 2026-03 更新显示 Figma MCP 已能在 Copilot 中完成“设计上下文→代码→把运行 UI 写回 Figma 可编辑图层”的双向循环；可把独立 design-reviewer 放在写回画布后的人工/Agent 复核点。 | https://github.blog/changelog/2026-03-06-figma-mcp-server-can-now-generate-design-layers-from-vs-code/ |
| `izscc/taskboard` 提供可写 Codex Taskboard、HTTP API/CLI/Skill，issue 可记录 `CODEX_THREAD_ID` 并绑定 branch/worktree；适合把聚合 review findings 直接落成看板 issue，再一键回到原 Codex thread 修复。 | https://github.com/izscc/taskboard |
| Codex 的 `trinity/trinity-ultra` 社区提案把三个独立 reviewer lane 放在“写代码前审 plan”和“写完后按 plan 审 diff”两个 checkpoint；虽是提案，但很适合作为多 provider 共识门禁的字段与流程参考。 | https://github.com/openai/codex/issues/33846 |
| `git ls-files --stage PLAN.md` 能在 commit 生成前直接读取 index 中 `PLAN.md` 的 blob OID；可将这个对象 ID 写入 `Plan-Hash`/检查项，确保绑定的是“本次真正会提交的计划快照”，而不是工作区版本。 | https://git-scm.com/docs/git-ls-files |
| 若采用 `plan/<id>` annotated tag 标记已批准计划，`git describe --tags <commit>` 可从实现提交反查最近可达的计划标签；对 blob 还能生成 `<commit-ish>:<path>` 表示，适合生成可读的 plan→commit 血缘标识。 | https://git-scm.com/docs/git-describe |
| GitHub Git Blobs API 可先把 `PLAN.md` 内容写成独立 blob 并获得 SHA，再把该 SHA 与代码 blob 一起组装进 tree/commit；补齐服务端自动化“计划内容 → blob → tree → commit”的可验证绑定链。 | https://docs.github.com/en/rest/git/blobs?apiVersion=2026-03-10 |
| OpenAI 的 Figma design-to-code skill 把 `get_design_context` 设为先决步骤，并要求优先复用 Code Connect、组件文档、annotations 和 design tokens；可直接当作 design-reviewer 的共同审查基线，避免只看截图或模型自造样式。 | https://github.com/openai/plugins/blob/main/plugins/figma/skills/figma-design-to-code/SKILL.md |
| Multi-MCP 通过 MCP 并行编排 OpenAI、Claude、Gemini 做代码质量、安全和性能审查并形成多 Agent 共识；可把 Figma/设计证据作为额外上下文交给独立 reviewer，再把统一 findings 回流 Codex。 | https://github.com/religa/multi_mcp |
| MA Orchestrator 可在 Codex 中启动隔离 worktree 的多模型 worker，支持文件范围权限、交叉复审、重试和持久 run artifacts；适合把 code-reviewer、design-reviewer、judge 分开运行后由 Codex 汇总。 | https://github.com/1345191768/multiAgents |
| Figma Code Composer 把 Figma MCP 连通性设为硬门禁，并在编排中同时检测技术栈、设计系统、组件路径、Storybook 与测试；可作为设计审查 lane 的前置上下文/验收配置，减少 reviewer 在错误组件体系上比对。 | https://github.com/raveracker/figma-code-composer |
| Agent Kanban 提供 Codex 可直接连接的 MCP 看板，支持 `post_progress`、`request_review`、diff/文本/artifact 进度流和评论；适合把聚合后的审查结论与证据直接推入任务卡并进入 review 状态。 | https://github.com/graywrk/agent-kanban |
| Codex App 的 task-board/cross-thread 讨论明确把 durable thread 视为长期 work item，并给出“一个线程实现、另一个线程审查”的使用模型；适合作为把多 Agent findings 映射到 Codex 看板卡片/线程并做跨线程 handoff 的数据模型参考。 | https://github.com/openai/codex/discussions/26148 |
| Codex 最新多 Agent review issue 暴露“把越界 finding 当成新需求会形成无限阻断循环”的风险；可把“必须映射已批准 plan、由本次 diff 导致、可复现”设成 judge 的三项硬门槛，防止审查器擅自扩 scope。 | https://github.com/openai/codex/issues/38375 |
| codex-workflows 把 Design Doc、Work Plan 与任务 handoff 作为可检查的仓库工件，并按任务执行验证与独立 review、每个任务生成独立提交；适合作为把 plan task/版本稳定关联到 Git 提交并按 governing docs 复审的参考实现。 | https://github.com/shinpr/codex-workflows |
| codex-agent-workflow 为 plan→run→review 保存显式生命周期状态和 Git review baseline，并把历史任务与审查归档；适合借鉴在 plan 元数据里固化基线 SHA、再让后续多 Agent review 始终对同一提交范围工作。 | https://github.com/cgilldev/codex-agent-workflow |
| Pitboss 由 planner 生成 `plan.md`，随后通过分阶段执行器自动跑测试、commit 与 audit loop，并提供 TUI dashboard；很适合参考“计划文件 → 分阶段提交 → 审查/审计”的一体化绑定流程。 | https://github.com/elicpeter/pitboss |
| codex-claude-bridge 同时支持 `review-plan`、pre-commit staged review、diff review 和跨阶段 `session_id` 连续上下文；适合把“先审计划 → 再审对应提交”的证据链交给独立 Codex reviewer。 | https://github.com/AmirShayegh/codex-claude-bridge |
| 该 CI/CD 实践把 PR 中生成的 Codex 计划先发布给人工审核，批准后再执行并自动 commit/push；适合作为“plan 审批状态与最终 Git 提交”绑定的现成 GitHub Actions 流程参考。 | https://smartscope.blog/en/ai-development/automation/codex-plan-mode-ci-cd-implementation/ |
| 这个 Codex `AGENTS.md` 工作流在 `PLAN.md` YAML 中记录 `base_commit`/worktree，并要求提交信息带 `goal_id`；可直接借鉴成“计划记录基线 SHA + 提交反向记录计划 ID”的双向绑定模式。 | https://gist.github.com/robspassky/1eba38588652b934a6a90a179c32769f |
| Figma Review Skill 会自动截取设计、从工程实现角度检查 UI/UX，并把 finding 精确回贴到 Figma 节点位置；适合作为独立 design-reviewer lane，再把结论与代码 reviewer 聚合。 | https://github.com/shomtsm/figma-review-skill |
| figma-mcp-express 面向 Codex/Claude 等 Agent 暴露真实节点、组件、变量、样式和原型，并专门支持多 Agent 分通道活动与多文件路由；适合在并行代码审查中提供隔离的设计证据源。 | https://github.com/sunhome243/figma-mcp-express |
| claude-talk-to-figma-mcp 明确支持多 Agent 并行访问同一 Figma 文件，并用服务端命令队列避免并发把插件打挂；适合让 design、accessibility、component reviewer 同时取证再统一汇总。 | https://github.com/arinspunk/claude-talk-to-figma-mcp |
| fatsecret-workflow 对 UI 功能把 Figma MCP 设为必需输入，并在 review-task 中加入规格核对、UI verification 与 Codex MCP 独立对抗式 debate；很贴近“设计文件 + 外部多 Agent code review”的组合质量门。 | https://github.com/fatsecret-group/fatsecret-workflow |
| agentic-kanban 提供可安装到 Codex 的 agent skill、MCP/CLI 看板与 review/merge gate；适合让 publisher 把聚合 findings 变成卡片并由 Codex 接手修复、复审和合并状态流转。 | https://github.com/p-wegner/agentic-kanban |
| agent-dashboard 同时管理 Codex 与 Claude 会话，统一显示 running/review/PR/merged 状态、捕获 plan/diff，并用 hooks 强制测试与 commit gate；适合作为多 Agent review 的可视化控制面和审查结果承载层。 | https://github.com/bjornjee/agent-dashboard |
| `git push --atomic` 可在远端将实现分支与 `refs/plans/<id>` 等计划引用作为一个事务更新；任一 ref 失败则都不更新，适合避免“代码提交已推送但 plan 绑定 ref 未更新”的半完成状态。 | https://git-scm.com/docs/git-push |
| `git show-ref --verify/--exists` 能精确检查 `refs/plans/<id>` 是否存在并读取其 OID；可作为 CI/reviewer 在审查前后的确定性核验步骤，确认 plan ref 真实指向预期提交。 | https://git-scm.com/docs/git-show-ref |
| Axeyum 的 `PLAN.md` 直接记录 exact pushed checkpoint、基线和多 lane 状态，把计划状态与具体提交 SHA 同时维护；很适合参考“plan 文件内正向记录 commit + 提交侧反向记录 plan ID”的双向证据模型。 | https://github.com/mjbommar/axeyum/blob/main/PLAN.md |
| `agent-md` 将 `memory/plan.md` 与 hooks/artifacts 区分，并提供可选 `.githooks/pre-commit`，强调把可忘记的流程约束移出提示词变成可执行检查；适合强制 `PLAN.md` 绑定校验后才能提交。 | https://github.com/iamfakeguru/agent-md |
| `council` 可并行调用 Claude Code、Codex、OpenCode、Cursor 等不同后端审查同一 staged diff，再由 host synthesis 去重；结果持久化到 `.reviews/`，适合把 design reviewer 的输出作为额外证据一起给最终 judge。 | https://github.com/ktaletsk/council |
| `claude-code-gauntlet` 采用 concern-specialized agents、确定性 verification 与 blind challenge 做对抗式多 Agent review，并持续用基准门禁衡量 recall/noise；适合作为 Codex 外部的高可信复审层。 | https://github.com/liatrio-labs/claude-code-gauntlet |
| `model-council` 把 Claude、GPT/Codex、Gemini 作为只读顾问并行审查，再由 overseer 验证关键主张、保留共识与分歧；很适合在代码 reviewer 与 design-reviewer 之间做跨模型最终裁决。 | https://github.com/OSideMedia/model-council |
| `figbridge` 通过 Figma 插件 + 本地 MCP 让 Agent 读取 screens/tokens/components，还能 audit、把代码页面导回 Figma、比较设计与源码并生成 patch；可作为独立 design-review lane 的双向证据桥。 | https://github.com/rudraptpsingh/figbridge |
| `figma-free-mcp` 可把 `.fig` 提取为本地 bundle，固定参考 PNG 的 SHA-256，并通过 `review_visual_match`/`compare` 输出像素 diff 与固定阈值 gate；适合让 Codex 之外的 reviewer 做可重复、离线的设计一致性验证。 | https://github.com/symonbaikov/figma-free-mcp |
| `open-figma-mcp` 除结构化设计读取外还提供 accessibility audit、设计版本 semantic diff 和 design-token↔repo `audit_drift`；适合把“设计文件变化”和“实现漂移”拆成独立机器可审查 findings。 | https://github.com/Shivam990q/open-figma-mcp |
| CodeLoop 将 build/test 与 `codeloop_visual_review`、`codeloop_design_compare` 组合为结构化 MCP gate，可直接以 Figma URL/导出图做多 viewport 比对，并在设计证据不足时阻断交付；适合作为 LLM reviewer 之外的确定性设计 QA 层。 | https://codeloop.tech/docs/tools |
| `saltbo/agent-kanban` 支持 Codex/Claude/Gemini/Copilot 等 Agent 作为一等成员，自主建任务、委派、互审并在实时看板保留身份/提交/PR 关系；适合把聚合 review findings 变成可追踪的 Codex 修复任务。 | https://github.com/saltbo/agent-kanban |
| `ai-agent-kanban-board` 从 planner 生成 `plan.md/plan.json`，经人工批准后让 coder/reviewer/tester/debugger 在隔离 worktree 执行并产出完成报告；非常贴近“计划 → 多 Agent 实现/审查 → 看板结果”的整条流水线。 | https://github.com/leoli-dev/ai-agent-kanban-board |
| AgenticX 把 `Plan-Id`/`Plan-File` 设为 commit trailer 白名单，并要求实施前把 plan 移到与 trailer 一致的仓库路径再随代码提交；这是现成的“计划文件路径 ↔ Git 提交元数据”双向绑定范式。 | https://github.com/DemonDamon/AgenticX/blob/main/AGENTS.md |
| `boyand/codex-review` 会把当前计划快照成不可漂移的 canonical `artifacts/plan.md`，先由 Codex 审 plan、再按已批准 plan 审实现，并持久化 decisions/findings；适合作为实现 Agent 外的第二审查链。 | https://github.com/boyand/codex-review/blob/main/README.md |
| Pi + Herdr workflow kit 将 planner/coder/reviewer 分成独立角色，保存 `plan.md`、plan-review、code-review、PR description，并按阶段产生 Git commits；适合把计划、实现提交和多 Agent 审查证据串成同一流水线。 | https://github.com/Jackliu-miaozi/pi-herdr-workflow-kit |
| team-loop 用 Claude+Codex 的 12 阶段受监督协议、显式 review gates 与迭代 checkpoint commits 管理 spec→plan→implementation，可作为“每轮提交都经过独立审查”的可审计编排模板。 | https://github.com/aluarius/team-loop |
| figma-design-skills 可在 Codex 中先从 Figma MCP 提取精确 token/尺寸/组件映射，再对运行中的 Web/移动 UI 做数值级 fidelity 验证并分类 DRIFT/HARDCODED 等问题；比单纯截图目测更适合作为 design-reviewer。 | https://github.com/jeltehomminga/figma-design-skills |
| uiMatch 用 Playwright 将实现与 Figma node 比较，输出像素、布局、样式、颜色、文本差异、Design Fidelity Score 和机器可读报告，并能在 CI 失败；适合给多 Agent review 增加确定性的设计质量门。 | https://github.com/kosaki08/uimatch |
| codex-kanban 在 Codex CLI 会话之上直接增加 Kanban board，并持久化 running/needs-attention/waiting-approval/errored 等状态；适合把 reviewer findings 按会话映射为 Codex 内可继续处理的审查工作项。 | https://github.com/duo121/codex-kanban |
| AI Agent Board 可在同一看板委派 Codex、Claude Code、Copilot 等 Agent，按任务建立 branch/worktree、实时回传执行结果，并在完成后进入 review/PR 流；适合作为多 reviewer findings 的项目化回流面。 | https://github.com/DanWahlin/ai-agent-board |
| VibeBoard 通过双向 MCP 让 Codex 等 Agent 与人共用同一 Kanban，卡片进入 In Progress 后可启动 Agent 并把进度持续写回；适合让 publisher Agent 将聚合审查结果落为可跟踪任务而不是只留在聊天。 | https://github.com/zanuartri/vibeboard |
| codex-workflow 会为每次 Codex/Claude 会话生成 Markdown session 文档、更新 `docs/status/board.md`，并用 MCP 写入状态和链接；适合把审查摘要同时沉淀成“文章记录 + 项目看板”并与后续实现关联。 | https://github.com/manastm/codex-workflow |
| coding-agent-toolkit 用 `spec.md`/`plan.md` 作为跨 Agent 合约，并把 plan 拆成 GitHub milestone/issues，随后 branch→PR→review→squash merge；适合把审查结论转成项目项并沿 Git 提交/PR 生命周期闭环。 | https://github.com/stefan-jansen/coding-agent-toolkit |
| claude-harness 的 `close-wave` 会把已完成 wave 的 commit hash 回写到 `plan.md`，并在合并前执行质量门和可选 Codex 复审；可直接借鉴“计划项 ↔ 实际提交 SHA”的双向追踪方式。 | https://github.com/agaleraib/claude-harness |
| optim-plans 会为计划生成稳定的 plan hash 与 execution manifest hash，并固定运行 worktree/branch 后再审批执行；适合把 Codex plan 先做内容指纹，再把后续 commit 绑定到同一执行清单。 | https://github.com/Optim-Agent/optim-plans |
| Codex 的 private live plan file 提案针对 Plan Mode 缺少可持续查看的计划文件，建议维护独立 Markdown 副本；可作为把计划先物化、版本化并计算哈希后再绑定 Git 提交的实现参考。 | https://github.com/openai/codex/issues/19125 |
| codex-research-workflow 把 durable plan、review、score、checkpoint 与独立 reviewer/verifier 都保存成仓库工件，并采用 PLAN→IMPLEMENT→VERIFY→REVIEW→FIX 流程；适合构建可审计的计划与多 Agent 复审链。 | https://github.com/zhao-dongchen/codex-research-workflow/blob/main/README.md |
| codex-review-action 可通过 PR `/codex-review` 命令触发 Codex 审查，保留逐行 review thread、更新统一摘要并串行同一 PR 的重复审查；适合把聚合 findings 安全发布到 GitHub 审查面。 | https://github.com/codemod/codex-review-action |
| orchestra 用 proposer 写代码、独立 read-only reviewer 返回结构化 `approved/blocking_issues`，并循环到通过；适合让 Codex 与 Claude 分离承担实现和复审，减少同一 Agent 自审偏差。 | https://github.com/dingtianding/orchestra |
| AWS 的 sample-aidlc-design-reviewer 可安装到 Codex，并由 orchestrator 自动发现、分类设计工件后执行设计审查；适合把设计文档/Figma 导出等证据作为独立审查 lane 再与代码 findings 汇总。 | https://github.com/aws-samples/sample-aidlc-design-reviewer |
| compound-engineering-amp 同时提供多 reviewer 组合与 Figma design sync 指引，可把 security/performance 等代码 reviewer 和“实现是否匹配 Figma”审查放在同一编排流程。 | https://github.com/nbbaier/compound-engineering-amp |
| frontend-craft 把 code reviewer 与 Figma implementer/design 上下文拆成独立 Agent，并将每轮审查写入时间戳 Markdown report；适合把设计审查、代码审查和后续看板回流之间用可追踪报告衔接。 | https://github.com/bovinphang/frontend-craft |
| Veritas Kanban 的 Codex 集成会针对任务 worktree diff 执行只读 Codex review，并把结构化 finding 映射为看板 review comment；很贴近“多 Agent 审查结果 → Codex/项目看板任务”的发布闭环。 | https://github.com/BradGroux/veritas-kanban/blob/main/docs/CODEX-INTEGRATION.md |
| coghex/kanban 可直接在 GitHub 项目卡上启动 Codex/Claude 的 solve、review、revision，并在同一终端看板追踪这些 job；适合把外部 reviewer 结果变成可继续处理的卡片状态而不是只留在聊天。 | https://github.com/coghex/kanban |
| codex-plan-reviewer 仅在 plan 内容哈希变化后重新触发 Codex 审查，并用 Stop hook 在高严重度 finding 未解决时阻断结束；适合把 plan hash 作为计划版本标识并形成明确的执行前审查 gate。 | https://github.com/smart-byte/codex-plan-reviewer |
| planning-with-files 把 `task_plan.md`、`findings.md`、`progress.md` 作为可恢复的持久计划工件，并明确支持 Codex 等多种 coding agent；适合把计划物化进仓库后再用现有 `Plan-Hash`/trailer 机制与提交绑定，避免计划只存在于会话上下文。 | https://github.com/OthmanAdi/planning-with-files |
| Compound Engineering 的统一 plan artifact 用固定 Markdown contract、readiness 元数据和 Codex `create_goal` handoff，把需求/计划变成可版本控制的长期事实源；适合让实现提交和后续 review 都引用同一计划工件。 | https://github.com/everyinc/compound-engineering-plugin/blob/main/docs/plans/2026-06-18-001-refactor-unified-plan-doc-artifact-plan.md |
| Quorum 将 security、performance、correctness、tests、style 等专门 reviewer 并行运行，再由 synthesizer 去重、排序并给出统一 verdict；适合作为 Codex 外部的轻量多 Agent 复核层。 | https://github.com/skmdroid/quorum |
| MergeWatch 是开源 GitHub App，多专门 Agent 并行审查 PR，orchestrator 统一去重并按 severity/confidence 汇总成单条结果；适合作为与 Codex 解耦的自动 PR review gate。 | https://mergewatch.ai/about |
| Google A2A 的任务/工件生命周期、能力发现和安全的 Agent-to-Agent handoff 可把 Codex、设计 reviewer、代码 reviewer、publisher 做成跨框架独立服务协作，减少单一主 Agent 的上下文污染。 | https://developers.googleblog.com/how-a2a-is-building-a-world-of-collaborative-agents/ |
| kanban-mcp 明确给出 Codex CLI MCP 配置，并持久化 issue/epic/status、进度、文件链接与 git commit 活动时间线；适合把聚合 findings 直接推成看板项，同时保留与提交的可追踪关系。 | https://github.com/multidimensionalcats/kanban-mcp |
| humbleteam/design-review 的 Agent Skill 可直接给 Codex 使用，并把截图/URL/HTML 审查固定成评分、优先问题和 Nielsen/WCAG 依据；适合作为 Figma/视觉证据之外的独立 UX/accessibility reviewer。 | https://github.com/humbleteam/design-review |
| design-review-workflow 支持 Codex 等 repo-capable Agent，能读取只读 Figma 证据、记录截图/工具 provenance 与哈希并输出可校验报告和 CI 阈值；适合作为多 Agent 流程里的独立 design-review lane。 | https://github.com/RNT56/design-review-workflow |
| 该实践明确要求任务状态必须与实际工作在同一个 Git commit 中更新，并把 commit body 作为完成证据；适合借鉴为 Codex `PLAN.md` 与实现提交同提交演进的硬规则，避免事后补写计划造成追踪失真。 | https://github.com/dantte-lp/powerdns-upstream-patches/blob/main/docs/plan.md |
| Codex Multi-Agent Orchestration 把实现、研究和多级独立 review 拆成隔离角色，使用 worktree、源文件指纹和持久 review artifacts；适合把 plan/commit 基线固定后交给不同 reviewer 复核并保留可追溯证据。 | https://github.com/omid511/codex-agent-orchestration |
| Codex-Orchestration 原生区分 Planner、独立 Advisor、Designer 和 Executor，并要求 Planner/Advisor 走不同模型路由；适合在 Codex 内把计划审查、设计交接和实现拆成相互制衡的多 Agent 流程。 | https://github.com/Cjbuilds/Codex-Orchestration |
| Figma MCP Server Guide 的 Fidelity Guard 草案专门检测设计上下文截断、稀疏或不一致，并在高保真实现前要求明确的 context-completeness 状态；虽仍是 draft PR，但很适合作为 design-reviewer 的“证据完整性”前置门禁。 | https://github.com/figma/mcp-server-guide/pull/81 |
| Codex Control Plane MCP 为 Plan Mode、code review、结构化 final report、thread goal 同步和持久任务状态提供统一 MCP 控制面；适合把外部多 Agent 聚合 findings 重新送入指定 Codex 线程/目标并继续修复。 | https://github.com/aresyn/codex-control-plane-mcp |
| Project Home 直接给 Codex++ 项目增加按项目隔离的 Kanban，并通过 MCP 暴露创建、更新、评论和移动 issue 的工具；适合把审查结果直接沉淀为 Codex 项目卡片并保留后续处理状态。 | https://github.com/b-nnett/codex-plusplus-project-home |
| ATC 用任务锁、Git worktree 隔离、冲突检测和实时看板协调 Codex、Claude Code、Gemini CLI、OpenCode；适合把 code/design reviewer 分派到独立工作区并把审查状态统一汇总到项目看板。 | https://github.com/JSCOP/atc-kanban |
| autoeng 把 `plan` 明确做成已提交、可人工编辑的唯一语义事实源，并在其中保留任务状态、review cursor 与 findings ledger；适合让 Codex plan 与实现一起进入 Git 历史，再按提交精确重放审查上下文。 | https://github.com/stass/autoeng/blob/master/DESIGN.md |
| Context Foundry 的 task 完成 hook 会同时暴露 `FOUNDRY_TASK_ID`、状态和 `FOUNDRY_COMMIT_SHA`；可直接把计划/任务标识与实际提交 SHA 发送到外部看板或审查发布器，补齐双向关联。 | https://github.com/context-foundry/context-foundry |
| agent-tasks 原生提供 `spec→plan→implement→test→review` 阶段门禁、artifact versioning、reviewer 角色和实时 Kanban；适合作为 Codex 外部多 Agent 编排层，并把每轮审查文档留在任务卡。 | https://github.com/keshrath/agent-tasks |
| DevFlow MCP 明确支持 Codex，并把 `proposal→specs/design→tasks→Kanban` 做成审批 DAG；适合把设计工件、实现任务和审查结论统一沉淀到可由 Codex 继续读取的看板流程。 | https://github.com/sureshdsk/devflow-mcp |
| Kanbots 同一看板可调度 Codex、Claude 等多种 coding agent，每次运行独立 worktree，保留实时 agent thread，并可把结果提升为正式 commit 或 draft PR；适合作为审查与回流的共享控制面。 | https://github.com/leodavinci1/kanbots |
| Bbasche/design-review 会先提取项目 design system，再抓取多 viewport 实际页面，并支持直接与 Figma spec 比对颜色、字体、间距和布局；适合作为独立于代码生成 Agent 的设计实现复核 lane。 | https://github.com/Bbasche/design-review |
| claude-figma-skills 能直接在 Figma 中执行 review、读取真实画布并把 actionable feedback 固定到具体设计元素；适合让设计侧 reviewer 独立产出证据，再与 Codex 的代码 findings 聚合。 | https://github.com/madebysan/claude-figma-skills |
| design-board 会让多个设计 critic 独立评分、相互 deliberation 后再形成统一 verdict，并能从 Figma MCP 获取设计上下文；适合把设计审查本身也做成多 Agent 共识层，避免单 reviewer 的视觉偏差。 | https://github.com/Chosen9115/design-board |
| Codex Linear Method Toolkit 把 issue 实现放入隔离 worktree，并要求独立 review branch、review 文档与 PR；适合让审查结果成为独立可追踪提交，再与原实现提交、Linear/项目项形成清晰链路。 | https://github.com/rawr-ai/codex-linear-method-toolkit/blob/main/workflows/review-issue.md |
| Codex Coding OS 用不可变 campaign specification 同时绑定 repository/worktree/branch/base commit，并让验证、review 和发布都固定到 exact candidate head；非常适合作为“计划工件 ↔ Git 提交 ↔ 审查证据 ↔ 发布结果”强追溯模型。 | https://github.com/AymanShams/codex-coding-os |
| OpenSpec 默认把 specs/changes 放在代码仓库内并随代码版本化，且原生支持 Codex；适合把计划/规格作为同一 commit tree 的可审查工件，再用 commit SHA 精确锁定实现所依据的计划版本。 | https://openspec.dev/docs/setup |
| codex-spec 会把 specification/requirements/plan/tasks.json 固化到仓库内的 `.codex-specs/`，并用 git diff 更新上下文；适合让 Codex 的 plan 成为可提交、可按 SHA 回溯且可继续执行的标准工件。 | https://github.com/shenli/codex-spec |
| metaswarm 对 Codex 提供完整多 Agent 编排：plan-review 走 3 个对抗 reviewer、design-review 走 5 个并行 reviewer，并可接 Playwright 视觉审查与 git-native BEADS/PR 流程；适合作为 Codex 外部的“计划/设计/代码/PR”统一审查层。 | https://github.com/dsifry/metaswarm |
| Agent_Do 是 Codex 可直接使用的文件型多 Agent 协调层，内置 plan compliance、code/security/design review 和 validation evidence 等严格 gate，并把 handoff/状态写到 `AI_COORDINATION/`；适合把计划、设计审查和代码审查结果统一成可版本控制证据。 | https://github.com/Mekawey0/Agent_Do |
| CX Design Gate 是 Codex 原生视觉验收插件，把设计依据整理成 Visual Fidelity Contract/Evidence Board，再由独立 Designer/Implementer 角色按截图做 hard-fail/score-cap 审查；适合让设计文件真正成为代码验收 gate，而不是只做实现输入。 | https://github.com/ymt23/cx-design-gate |
| Img Review 把截图/设计差异通过框选、箭头、元素变换等方式转成 `annotations.json`、`review.md`、`ai-task.json` 等机器可读任务，再直接送回 Codex；适合把视觉审查 finding 精确回流到修复 Agent。 | https://github.com/z5396120-maker/img-review |
| ai-jury 可让 Claude Code、Codex、Antigravity 和本地模型对同一 diff/PR 并行评审、交叉质询并做 verification 后输出单一 verdict；适合作为 Codex 之外的跨模型 reviewer/judge 层，降低单模型漏报和误报。 | https://berkayturanci.github.io/ai-jury/ |
| Plan Swarm Review 用多样化 reviewer + focused decomposition 分轮审查 PLAN/设计文档，并要求每轮读取更新后的计划；适合在实现前先对 Codex plan 做多 Agent 压测，再把通过的计划版本与随后 commit 绑定。 | https://github.com/AnastasiyaW/codex-claude-code-config/blob/main/skills/architecture/plan-swarm-review/SKILL.md |
| Codex Taskboard 提供可嵌入 Codex 的本地 issue 看板和 `taskctl` API/CLI；Skill 会把任务从 `in_progress` 经验证推进到 `in_review`，最终经人工确认才 `done`，很适合把聚合 review findings 直接写成看板卡片并闭环修复。 | https://github.com/chuspeeism/dashi-taskboard |
| Wallfacer 把 versioned specs、任务看板、per-task worktree、可插入 review step 的多 Agent flow 和 Codex harness 放在同一控制面；适合把审查 finding 转成 board tasks，并保留从 spec 到 commit/差异/结果的审计轨迹。 | https://github.com/changkun/wallfacer |
| Agent Studio 把 Codex/Claude/Copilot/Gemini 放到同一 task board + agent pipeline + project wiki 管理层，并用独立 Remote Review Executor 与 Orchestrator Engine 做 gate/review/judging；适合承载多 Agent 审查结果、证据和后续任务状态。 | https://github.com/agent-orc/agent-studio |
| Stack 的实践把每个功能绑定到独立 worktree、Agent 会话和 PR，并用版本化 skills + adversarial review + 第二模型 review 形成明确 gate；适合参考为 Codex plan/commit 建立可审计的“计划→实现→复审→PR”生命周期。 | https://www.abstract.systems/stack |
| Figma 官方 Codex→Figma 工作流支持把真实运行界面带回 Figma 继续设计，再把设计上下文带回 Codex；适合把设计文件作为 review source-of-truth，并把视觉审查结果继续回流实现 Agent。 | https://www.figma.com/blog/introducing-codex-to-figma/ |
| Agentic Design School 总结了“设计与代码收敛为同一审查循环、设计系统成为机器可读合约”的趋势，可作为把 Figma/设计证据纳入多 Agent code review 的架构参考。 | https://www.agenticdesign.school/articles/where-agentic-design-is-heading |
| Rudder 用 `/plan` DAG、并行 Codex/Claude workers、独立 Review 状态和可交互 Web project board 编排大量 Agent；适合把 reviewer findings 直接送入看板并继续要求修改、复审和合并。 | https://github.com/viraatdas/rudder |
| skill-codex 已实现结构化 APPROVED/WARNING/BLOCKED verdict、bounded fix→re-review loop、Codex/Claude 双插件与 live project board；适合做 Codex 外部第二审查层并把结果持续回流项目控制面。 | https://github.com/Arystos/skill-codex/blob/master/ROADMAP.md |
| prog-decoding-research 的 `PLAN.md` 明确要求每个 phase 一个 intentional commit，并在阶段结束返回文件、测试、命令、结果与验收状态；可直接借鉴为“计划阶段 ↔ Git commit”一对一绑定约束。 | https://github.com/ramigb/prog-decoding-research/blob/main/PLAN.md |
| BranchLab 的 `.codex/PLANS.md` 把 ExecPlan、exact commit、verification evidence、design/visual gates 与最终 commit/push 状态放在同一长期工件里；适合参考双向记录 plan 与 candidate SHA 的证据模型。 | https://github.com/jlov7/branchlab/blob/main/.codex/PLANS.md |
| Krypton 用 `PLAN.md + GOAL.md + EVIDENCE.md` 把目标、执行计划和验收证据拆开，并提供 PR gate；适合在 Codex 计划与具体 commit 之间增加可验证 evidence 层，再交给独立 reviewer 判定。 | https://github.com/jturntdev/krypton |
| OpenAI Cookbook 的 ExecPlans 明确把 `PLANS.md`/ExecPlan 作为从设计到实现的长期设计文档；适合作为将 Codex plan 物化进仓库、随 Git 提交版本化并供后续 reviewer 精确引用的官方模式。 | https://github.com/openai/openai-cookbook/blob/main/articles/codex_exec_plans.md |
| Bottega 把 planning→implementation⇄adversarial review→PR 做成可混用 Codex/Claude/OpenCode 的编排循环，并提供可选 Kanban board；适合参考“计划→多 Agent 审查→看板→PR”的完整控制面。 | https://github.com/vdaubry/bottega |
| ProPR 以 GitHub 为 control plane，支持 Codex、隔离 worktree、PR 只读 review、运行记录和后续评论驱动修复；适合作为 Codex 外部审查编排层并持续保留任务/审查 provenance。 | https://propr.dev/ |
| GitHub 官方给出用 Actions 自动把 PR 加入 Projects 并更新 Status/日期字段的方案；适合把多 Agent/Codex 审查结论映射为“待审查、阻塞、需复审、通过”等看板状态。 | https://docs.github.com/en/issues/planning-and-tracking-with-projects/automating-your-project/automating-projects-using-actions |
| Figma MCP 官方高阶指南明确列出 Codex 为支持客户端，并覆盖设计上下文、截图、变量和规则约束；适合把设计文件作为独立 design reviewer 的 source-of-truth，再与代码 reviewer 交叉汇总。 | https://help.figma.com/hc/en-us/articles/32132100833559-Guide-to-the-Figma-MCP-server |
| `git fsck` 可验证 Git 对象库的连接性、对象有效性和 hash mismatch；在把 `PLAN.md` blob/tree OID 绑定到提交后，可作为 CI 的证据链完整性检查，避免“记录了 Plan-Hash 但对象缺失或损坏”的假绑定。 | https://git-scm.com/docs/git-fsck |
| spsk-dev/code-review 会并行启动 7 个 reviewer（Claude 专门角色 + Codex + Gemini），再做置信度评分和跨模型共识加权；适合作为 Codex 之外的第二审查编排层，只把高置信 finding 回贴 PR 或交给后续修复。 | https://github.com/spsk-dev/code-review |
| Bito AI Code Review Agent 可直接读取仓库中的 `AGENTS.md`、`CLAUDE.md`、`GEMINI.md` 等项目规则并用于 PR 审查；适合让 Codex 与外部 reviewer 共享同一计划、架构和设计规范基线，减少不同 Agent 按不同规则审代码。 | https://docs.bito.ai/ai-code-reviews-in-git/implementing-custom-code-review-rules |
| Playwright 的 `toHaveScreenshot()` 可把实现界面固化为可版本控制的视觉基线并在后续提交自动比较像素差异；适合把 Figma/design reviewer 的语义判断再落为确定性的 CI 视觉 gate，并把结果绑定具体 commit。 | https://playwright.dev/docs/next/test-snapshots |
| Storybook a11y addon 基于 axe-core 对实际渲染组件执行 WCAG 自动审查，并可在 CI 中把 violation 设为失败；适合给 Figma design reviewer 增加独立可访问性 lane，避免只检查布局/像素而漏掉语义与交互可访问性。 | https://storybook.js.org/docs/writing-tests/accessibility-testing |
| DTCG 2025.10 Design Tokens Format 给出跨工具交换设计 token 的稳定、厂商中立文件格式；可把 Figma 变量/设计系统导出的 token 作为机器可读审查合同，让 design reviewer 精确比较代码中的颜色、间距、字体等值是否漂移。 | https://www.w3.org/community/reports/design-tokens/CG-FINAL-format-20251028/ |
| figma-relai 让 Codex、Claude、Cursor 通过 MCP 读取、审计和修改 Figma，并把设计文件自身的 conventions、precedents、no-go zones 当成可继承规则，违规编辑在执行前被拒绝；适合把“设计文件本身”升级为 design-review policy gate。 | https://github.com/syoooo/figma-relai |
| GitHub Pages 自定义 Actions 工作流可把聚合后的 Markdown/静态审查报告作为 artifact 自动部署并输出页面 URL；适合把多 Agent review 结果发布成可长期访问的审查文章，再把该 URL 回填到 Codex 或项目看板卡片。 | https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages |
| 对 36,710 个仓库的实证研究发现可提交的 Agent Plan 很稀少，但保存下来的计划主要包含实施步骤、具体文件位置和验证条件；为把 Codex plan 持久化进 Git 并用提交历史做可审计绑定提供了最新实证依据。 | https://arxiv.org/abs/2608.04661 |
| Codex Planning Kit 把 `PLANS.md`、`AGENTS.md`、issue 与验证约束组织成仓库内工件，适合作为“计划先版本化、再让实现提交引用计划版本”的轻量模板。 | https://github.com/EEstevanell/codex-planning-kit |
| codex-workflow 把 spec→plan→implement 与 bugs、coverage、type-safety、maintainability 等专门 review 技能分开，可让 Codex 实现后再由多角色 reviewer 独立复核并把结果汇总回主流程。 | https://github.com/doodledood/codex-workflow |
| 对终端式 agentic review 轨迹的研究显示高质量审查与更强的前置规划相关，同时探索/验证成本很高；适合让 reviewer 先读取绑定到 commit 的 plan，再限制搜索范围和验证预算。 | https://arxiv.org/abs/2607.16740 |
| SWE-Review 用 generate→review→revise 闭环证明结构化 reviewer feedback 能持续提升 AI 生成 PR；适合作为“Codex 提交→外部 reviewer→findings 回流 Codex 修复”的研究级流程依据。 | https://arxiv.org/abs/2607.06065 |
| 对 31,073 组 Agent review/开发者反馈的实证研究发现 56.3% 建议被拒，多因 false positive、重复、越界或偏离开发意图；适合在多 Agent 汇总后增加 judge/adjudicator，再把高置信 finding 推到 Codex 看板。 | https://arxiv.org/abs/2607.03316 |
| Hydra-Reviewer 通过上下文检索、多个审查维度和后处理协同生成 review comment，适合作为 Codex 外部的多视角 reviewer，并把统一 finding 交给后续 judge/publisher。 | https://github.com/ZJUSig/Hydra-Reviewer |
| Consensus-Engine 用 LangGraph 编排 OpenAI、Anthropic、Gemini 对 GitHub PR 做代码、安全和运行时多路共识审查，适合作为跨模型第二审查层，降低单一 Codex reviewer 的盲点。 | https://github.com/Clownstein/Consensus-Engine |
| multireview 由 orchestrator 预制共享 PR 快照后并行调用多模型 reviewer，统一去重、过滤弱 finding，并可发布 PR 评论或仓库 `REVIEW.md`；很适合把审查结果直接沉淀成文章工件再回流看板。 | https://github.com/rmichelena/multireview |
| Buoy 可把设计系统 token/组件基线转成机器可读 drift finding，并支持 staged/pre-commit/CI 检查与 JSON 输出；适合作为 Figma/设计系统语义 reviewer，和 LLM 代码 reviewer 并行后统一裁决。 | https://github.com/ahoybuoy/buoy |
| drift-guard 用设计快照与 token 约束检测 AI 改动造成的设计漂移，并可放进 pre-commit/CI 阻断违规变更；适合作为 Figma/视觉 reviewer 之外的确定性设计回归门禁。 | https://github.com/Hwani-Net/drift-guard |
| Codex Coordinator 用显式任务边界协调同仓库多个 Codex task，并提供冲突感知与 Mission Control 可视面；适合把 plan-verifier、code-reviewer、design-reviewer、publisher 拆成独立 Codex 任务后回收到同一项目目标。 | https://github.com/eyeinthesky6/codex-coordinator |
| Kun Design Mode 用共享 `DESIGN_SYSTEM.md`、实现时保存设计系统哈希以及双向 design/code drift 标记维护设计→代码 provenance；适合借鉴为“设计版本哈希 ↔ 实现提交 ↔ 审查结果”的绑定模型。 | https://github.com/KunAgent/Kun/blob/master/docs/DESIGN_MODE.md |
| 把已批准 plan 拆成一任务一提交，并在每次 commit 前由独立 orchestrator 审 diff、跑测试 gate；非常适合作为 Codex plan→Git commit 的可审计映射模板。 | https://github.com/cathrynlavery/codex-build |
| 把 spec→plan→tasks 固化成仓库工件，每个 task 明确定义为测试+实现+一个 commit，并用 fresh subagent 做规格符合性和代码质量两阶段审查，适合把计划项、提交和独立 review 一一关联。 | https://github.com/suportly/ai-augmented-developer |
| Codex 官方仓库的 code-review orchestrator 明确一类审查技能对应一个 subagent，并汇总全部带 file:line 的 finding；可直接作为 Codex 原生多 Agent review 编排基线。 | https://github.com/openai/codex/blob/main/.codex/skills/code-review/SKILL.md |
| 用 plan-review→implementation-review→shipped-review 三段式复核，把实现明确对照已批准 plan，并可作为实现 Agent 之外的第二次质量门。 | https://github.com/schuettc/codex-reviewer |
| 专门可靠读取 Codex 在 GitHub PR 上的 inline findings/严重度并驱动 @codex re-review loop，适合把多 Agent 汇总结果准确带回 Codex 复审与修复闭环。 | https://github.com/1XP-AI/codex-review-skill |
| 用 append-only `plan.md` 同时承载契约和 task board，Claude 做协调/QA、Codex workers 分工执行，且 QA 不交给实现 worker；适合作为可审计的外部多 Agent 编排层。 | https://github.com/shuaige121/codex-orchestrator |
| 为 Codex 等 CLI Agent 提供任务卡→独立 worktree→diff review→评论回发→commit/PR 的 Web 看板，并支持多 Agent 并行与依赖链；很适合承载聚合审查结果和后续修复状态。 | https://github.com/cline/kanban |
| OpenAI curated Figma design-system rules 强制先取结构化 `get_design_context`、再取 screenshot，并把 token/组件约束固化成规则；适合作为 design-reviewer 与 Codex 共享的设计审查基线。 | https://github.com/openai/skills/blob/main/skills/.curated/figma-create-design-system-rules/SKILL.md |
| 直接把 Codex 当前工作流与官方 Figma MCP、当前文件/节点/选区连接，并优先用结构化数据校验、截图兜底；适合让设计文件成为独立 reviewer 的实时证据源。 | https://github.com/liaocaoxuezhe/Codex-Design-Figma-in-Codex |
| 通过 MCP→本地桥→Figma Plugin 把 Agent 产物写成真实 Figma 原生图层，可把实现/审查结果落回设计画布供二次 critique，补齐代码↔设计的可验证闭环。 | https://github.com/evidence-codes/figma-design-bridge |
| 把计划逐任务交给 fresh implementer，要求“代码→测试→commit→自审”，随后再由独立 spec/code-quality reviewer 复核，并支持并行 Agent Team；适合把 Codex plan 的任务粒度与 Git commit、复审证据一一绑定。 | https://github.com/Anandsharma1/plan-and-execute/blob/main/SKILL.md |
| Claude Codex Forge 把 Claude 设计、Codex 独立复审、Engineering Council 裁决和并行 code review 固化成 14 阶段流水线，并用 Git worktree 隔离并行工作；适合在 Codex 实现之外增加跨模型 reviewer/judge。 | https://github.com/pablomarin/claude-codex-forge |
| gstack 的 plan-design-review 会把 Design/Eng/Codex 等审查结果写回计划文件，并记录对应 commit、检测 HEAD 漂移；同一套 review tier 还支持 Claude+Codex 对抗审查，适合把“plan→commit→设计/代码复审状态”做成可追踪报告。 | https://github.com/garrytan/gstack/blob/main/plan-design-review/sections/review-sections.md |
| Figma 官方 Codex MCP 配置页专门说明 Codex 可读取组件、变量、布局、FigJam/Make 与 Code Connect，并可把界面写回画布；适合作为 design-reviewer 在 Codex 中获取真实设计文件上下文的标准接入。 | https://help.figma.com/hc/en-us/articles/39888629089175-Codex-and-Figma-Set-up-the-MCP-server |
| visual-diff 用 Figma REST 拉取实时设计、Playwright 截取实现页面，并提供 side-by-side/overlay/difference 三种比对和 AI 可读 skeleton；适合作为多 Agent 设计审查中的确定性视觉证据 lane。 | https://github.com/s1awek/visual-diff |
| oh-my-symphony 以 Codex app-server 为一等后端，同时可混用 Claude/Gemini 等多个 coding agent；每张卡独立 git worktree，并把运行、Git 变更、Review/PR 状态集中到可写 Kanban/Linear/Jira 控制面，适合作为聚合 findings 的回流看板。 | https://github.com/cskwork/oh-my-symphony |
| Open Multi-Agent 让 coordinator 动态生成任务 DAG、并行执行 review team 并保留可审批/回放运行记录；适合把 plan verifier、代码 reviewer、design reviewer、publisher 拆成可重放的多 Agent 审查图。 | https://github.com/open-multi-agent/open-multi-agent |
| Docker cagent PR Review 用 Drafter→Verifier 的多 Agent 复核流水线，把 finding 回贴 PR 并从反馈持续学习；还支持附加 prompt files，可把 Figma 导出的设计规则或证据作为额外审查上下文。 | https://github.com/docker/cagent-action/blob/main/review-pr/README.md |
| 该 LangGraph PR Reviewer 并行运行 security、bug、performance 三个 Agent，结构化聚合并带人工 gate 与 checkpoint；适合作为 Codex 外部 reviewer，再把设计证据加入共享 state 后统一回贴 GitHub。 | https://github.com/shreyas-kapse/ai_pr_reviewer |
| GitHub 官方教程完整演示 Figma MCP→GitHub issues→实施计划→PR→Playwright 验证→回写 issues 的闭环；可迁移为 Codex 的 design-review lane 与 publisher 流程模板。 | https://docs.github.com/en/copilot/tutorials/enhance-agent-mode-with-mcp |
| Codex 社区的 document-based planning 提案把计划物化为可引用的 `PLAN.md`/slice 文件并持续更新状态；适合作为先固化 plan artifact、再计算哈希并进入 Git 提交绑定的文件模型参考。 | https://github.com/openai/codex/discussions/11758 |
| discuss-cli 可把 `plan.md` 与 staged/commit diff 放在同一 PR 式审查会话，保留逐行线程、verdict 与历史归档；适合把“批准的计划文件”和“实际提交差异”放到同一可追溯 review artifact。 | https://github.com/codesoda/discuss-cli/ |
| GitHub Squad 的 repository-native 多 Agent 架构把设计、实现、测试、review 协调留在仓库内并强调可检查状态；适合参考为 Codex 外部 reviewer team 的持久上下文和任务协作模型。 | https://github.blog/ai-and-ml/github-copilot/how-squad-runs-coordinated-ai-agents-inside-your-repository/ |
| ClickUp 官方 MCP 可让 AI Agent 创建/更新任务、搜索 Docs/评论并生成状态报告，适合作为 publisher 把聚合 review findings 同时落成看板任务和长文记录，并由 Codex 通过标准 MCP 接入。 | https://developer.clickup.com/docs/connect-an-ai-assistant-to-clickups-mcp-server |
| GitHub 已把 Codex、Claude、Copilot 等 coding-agent session 直接显示到 Issues 与 Projects 表格/看板，并暴露 queued/working/waiting for review/completed 状态；适合把聚合审查 finding 变成项目项后直接观察 Codex 修复会话进度。 | https://github.blog/changelog/2026-03-26-agent-activity-in-github-issues-and-projects/ |
| GitHub Projects 现可在项目看板侧边栏直接打开 agent session、查看日志并继续 steer；适合把多 Agent 审查结果发布成项目任务后，无需离开看板就继续驱动 Codex 修复和复审。 | https://github.blog/changelog/2026-04-23-view-and-manage-agent-sessions-from-issues-and-projects/ |
| Mastra 已能把 Codex、Claude Code、Cursor 作为一等 subagent 编排，并保留各自文件/代码库推理、handoff 与权限能力；适合让 Codex 实现、独立 code reviewer、design reviewer 由同一外部 orchestrator 并行/串行协作。 | https://mastra.ai/blog/introducing-sdk-subagents |
| Mastra Dynamic Workflows 允许 Agent 在运行时用 JSON 组装由 agents、tools 和控制流构成的工作流且无需重新部署；适合按 PR 风险动态生成 plan-verifier→code-review→design-review→judge→publisher 审查图。 | https://mastra.ai/blog/introducing-dynamic-workflows |
| Mastra 的 code review bot 示例把仓库 coding standards 作为 workspace skill 注入 reviewer 并输出结构化 PR feedback；适合把已绑定 commit 的 plan/规则与 Figma 设计规范一起作为独立 reviewer 的可复用审查基线。 | https://mastra.ai/blog/building-a-code-review-bot |
| Codex 的计划持久化实现提案把 finalized Plan Mode 结果写入 `.codex/PLAN.md`，再让新上下文读取该文件实施；可在此基础上把该计划文件纳入 Git tree/哈希/trailer，稳定建立 plan artifact ↔ commit 绑定。 | https://github.com/openai/codex/issues/15453 |
| Conventional Commits 规范允许机器可读 footer，且 footer 语法明确受 Git trailer 约定启发；可在现有 `feat/fix` 历史中统一加入 `Plan-File`、`Plan-Hash` 或 `Plan-Id`，让计划绑定既兼容团队提交规范又易被 CI 解析。 | https://www.conventionalcommits.org/en/v1.0.0/?embedable=true |
| figma-pilot 仅用少量 MCP 工具让 Codex 等 Agent 通过代码执行读取/批量检查 Figma 真实节点与设计系统，并提供 Codex skill；适合作为独立 design-review lane 的低上下文开销设计证据源。 | https://github.com/youware-labs/figma-pilot |
| Figma Edit MCP 可由 Codex 等支持 SKILL.md 的 Agent 读取/修改设计，并在执行前做引用与破坏性操作安全校验；适合作为可写 design-reviewer 的受约束设计通道，把审查修订留在真实 Figma 文件而非只输出文字。 | https://github.com/neozhehan/figma-edit-mcp |
| Codex Plan Mode 的最新源码调研梳理了 `<proposed_plan>`→`TurnItem::Plan`→同/新上下文实施的运行链路；适合在外部编排器中抓取稳定 plan artifact 后计算哈希并绑定后续 Git commit，而不是依赖普通聊天文本。 | https://github.com/openai/codex/issues/38305 |
| ghist 把任务的已保存 plan 与 commit hash 自动关联，commit 后会提示把 SHA 绑定到当前任务，且 Web UI 可直接追踪；很适合借鉴成 Codex plan/task ↔ Git commit 的自动索引。 | https://github.com/unnecessary-special-projects/ghist |
| deep-implement 按 plan section 执行 TDD→独立 subagent code review→修复→原子 commit，并把 commit hash 回写用于恢复；适合作为“计划段 ↔ 提交 ↔ 审查证据”一一绑定模板。 | https://github.com/piercelamb/deep-implement |
| dwarves-kit 的 `/review-team` 把 infra、performance、advisor、responding-to-review 等专门 reviewer 编排成团队，并用 proof/gate 约束结果；适合作为 Codex 外部的多角色复核层。 | https://github.com/dwarvesf/dwarves-kit |
| c2d2c 的 Figma→code 流程把 plan review、1:1 实现、回归页面、motion review、截图 acceptance gate 与自动 MR 串在一起；适合作为设计文件驱动的独立 design-review lane。 | https://github.com/BIAsia/c2d2c |
| Checklist Design skills 可从 Figma MCP 读取结构化样式、token 绑定和 variants，并结合截图/Playwright 做审查；能发现硬编码颜色、缺失 variant 等单靠代码 diff 难发现的问题，适合给 Codex 增加设计语义 reviewer。 | https://github.com/Checklist-Design/skills |
| work-with-design-systems 明确兼容 Codex，能审计 token 合规、交互 states、WCAG、组件描述，并做 Storybook↔Figma variant parity；适合作为结构化设计系统审查 Agent 并输出 JSON/AI prompt 给后续聚合。 | https://github.com/natdexterra/work-with-design-systems |
| caclawphony 把 Linear 项目看板、Symphony 与 Codex Agent 串成 PR triage→review→merge 多阶段流水线，并在阶段间保留人工 gate；非常适合把聚合 findings 推成看板项后继续驱动 Codex 修复/复审。 | https://github.com/openclaw/caclawphony |
| GitHub Actions 可把 security、logic、design 等 reviewer 封装成可复用 workflow，并用 matrix 并行调用后聚合输出；适合把 Codex 与外部多 Agent 审查固化成可复现的 CI 编排层。 | https://docs.github.com/en/actions/how-tos/reuse-automations/reuse-workflows |
| GitHub Actions matrix 可按 reviewer 类型或模型动态展开并行 job，再把各 lane 的输出汇总到 synthesis/publisher；适合构建确定性的多 Agent fan-out/fan-in 审查流程。 | https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/run-job-variations |
| GitHub Project status update 支持 Markdown 正文并保留完整历史，可把每轮聚合 review 发布成项目侧边栏的文章化状态，同时记录状态和日期。 | https://docs.github.com/en/issues/planning-and-tracking-with-projects/sharing-project-updates |
| Figma 对 27 个设计→代码用例的实测显示 Code Connect 可提升 Agent 代码质量并降低 token 与耗时；适合作为把真实组件映射和设计系统上下文纳入 design reviewer 的最新实证依据。 | https://www.figma.com/blog/the-benefits-of-code-connect-in-mcp/ |
| GitHub Actions 的 `hashFiles('PLAN.md')` 可生成计划文件 SHA-256，并与 `github.sha` 一起写入 check、artifact 或 Project 字段；适合在 CI 层建立可核验的 plan hash ↔ commit SHA 绑定。 | https://docs.github.com/en/actions/reference/workflows-and-actions/expressions |
| Council Forge 把 research、plan、code、verify、decision、status 固化为仓库工件，并用 gate 校验跨 Claude、Gemini、Codex 的状态流转；适合把 plan↔commit↔review 证据做成可恢复的 repository-native 流程。 | https://github.com/arcobaleno64/council-forge |
| master-workflow 用确定性 harness 把 fresh worker 的真实 Git diff 交给另一模型只读复审，未达阈值就把 findings 折回下一轮；适合给 Codex 外挂独立 reviewer/judge，避免只审摘要。 | https://github.com/luckeyfaraday/master-workflow |
| The Council 可并行调用 Codex/Gemini 做只读第二意见并由 host 汇总共识与分歧；采用时应使用包含 OS 级只读约束修复的较新版本，适合作为代码/设计 reviewer 后的跨模型最终裁决层。 | https://github.com/DantesPeak85/the-council |
| `/plan` 把 Workbench 的 plan/todo、独立 reviewer 与 worker commit SHA 放进同一完成契约，并在批准后逐一核验每个 todo 的 commit SHA；可直接借鉴为“计划项 ↔ Git 提交 ↔ 审查证据”的强绑定流程。 | https://github.com/HazAT/pi-config/blob/main/skills/plan/SKILL.md |
| 该多 Agent 编排 skill 要求每个任务完成后立即把 commit SHA 回写到 `plan.md` 并同步 metadata checkpoint；非常适合把 Codex plan 的任务粒度与 Git 提交做可恢复的一一映射。 | https://github.com/Ibrahim-3d/orchestrator-supaconductor/blob/master/skills/conductor-orchestrator/SKILL.md |
| 将计划固化到 `.claude/plan/*.md`，由 Codex 独立审 plan，再在实现后由另一模型/Codex 循环复审并输出结构化 verdict；适合作为 Codex 外部的交叉模型 plan→implementation review 层。 | https://github.com/ching-kuo/claude-codex |
| codex-acp 把 Codex subagent、MCP 与 `/review-commit` 暴露成标准 ACP 能力；外部 orchestrator 可按 commit 启动 Codex reviewer，并同时接入 Figma/设计 MCP 形成统一编排。 | https://github.com/agentclientprotocol/codex-acp |
| Codex Team Orchestrator 内置 planner、implementer、reviewer、tester、researcher、共享 task board、artifact exchange 与 arbitration；适合把代码与设计 reviewer 的 findings 聚合后交给专门 reviewer/publisher 再回流看板。 | https://github.com/ajjucoder/codex-team-orchestrator |
| Kanboard 通过 MCP 让 Codex 读取、创建和移动卡片，并以 `In Review` 作为显式质量门；适合作为 publisher 把聚合审查 findings 落成可追踪任务，再由 Codex 接手修复和复审。 | https://kanboard.io/codex-kanban |
| figma-refine 读取 Figma MCP、项目上下文和 ReviewNote，并用三层截图复核设计修改；可作为独立 design-review lane，把设计文件证据交给代码 reviewer/judge，而不是只靠代码 diff。 | https://github.com/fideguch/figma-refine |
| Argus 是面向 Codex 等 Agent 的 MCP-native、零 LLM 设计规则运行时，用确定性规则审查设计系统/UI；适合与多 Agent LLM code review 并行，提供不受同一模型偏差影响的设计质量证据。 | https://github.com/Dragoon0x/argus |
| multi-agent-workflow 用 `spec.md`/`decision-log.md` 作为 Claude Code 与 Codex 的共享事实源，并要求 designer 产出可审查 mockup 与 visual QA；适合把设计工件、代码审查和跨 Agent handoff 做成同一持久流水线。 | https://github.com/hensonzyw-git/multi-agent-workflow/blob/main/PROJECT_STATE.md |
| Pencil Actions 会为 `.pen` 设计文件的每次 PR 变更生成 before/after 视觉 diff；可把设计文件本身纳入与代码 commit 同一 PR 的确定性审查证据，再与 Codex/多 Agent findings 一起发布。 | https://github.com/RemoteState/pencil-actions |
| `git check-ref-format` 可在创建 `refs/plans/<plan-id>` 前校验引用名是否合法，适合把“计划 ID → commit”索引做成稳定、可由 CI/脚本安全解析的 Git ref 命名约束。 | https://git-scm.com/docs/git-check-ref-format.html |
| Git reflog 会记录分支及其他 refs 的 tip 更新；给 `refs/plans/*` 建 reflog 后，可追踪计划引用从旧 commit 移到新 commit 的完整变更轨迹，补足 plan↔commit 绑定的审计历史。 | https://git-scm.com/docs/git-reflog.html |
| 新版 `git refs` 提供 `list`、`exists`、`verify` 等低层 ref 检查能力，可在自动审查前验证 `refs/plans/*` 是否存在、指向是否可查询及 ref 数据库是否一致。 | https://git-scm.com/docs/git-refs/2.54.0 |
| 该文系统梳理 Codex 从会话内 subagents 到 issue-tracker 驱动常驻编排的多 Agent 层级，并给出并行 review/research 模式；适合作为 Codex 外部 reviewer、judge 与 publisher 的编排参考。 | https://www.firecrawl.dev/blog/codex-multi-agent-orchestration |
| 该工作流明确采用 Codex 执行 build、Claude Code 做独立 review、随后刷新 Kanban/board 状态，直接对应“生成 Agent → 异构审查 Agent → 看板回流”的闭环。 | https://mer.vin/2026/05/hermes-telegram-loop-codex-build-goals-and-claude-code-review-handoffs/ |
| 文章建议让 ChatGPT、Claude、Cursor、Codex 通过 MCP 共用同一项目看板，并把 scope、status、evidence、next action 作为权威状态；适合沉淀多 Agent 审查 findings 并继续驱动 Codex 修复。 | https://agiflow.io/blog/one-project-board-chatgpt-claude-cursor-codex |
| 该 Figma MCP 实战说明 Codex 可读取结构化 layout、tokens、Code Connect 映射并完成 code↔design roundtrip；可让独立 design reviewer 以真实 Figma 设计系统为依据复核实现。 | https://codex.danielvaughan.com/2026/03/27/codex-cli-figma-mcp-design-to-code/ |
| 该指南聚焦 Codex CLI 连接 Figma MCP 的可审查配置、权限边界和精确 frame 约束，适合把设计文件读取能力固定为多 Agent review 流水线中的独立 design-review lane。 | https://codexclimcp.com/guides/figma-mcp-codex-cli/ |
| 这份统一 MCP 架构研究把 Codex/Gemini 等 Agent 接到同一 Kanban MCP，并提供 task、reviewer、dependency 等控制面；适合把代码 reviewer 与设计 reviewer 的结果统一汇总到看板。 | https://github.com/777genius/agent-teams-ai/blob/main/docs/research/unified-mcp-architecture.md |
| Foldo 用与具体 commit/state 绑定的 live app frame 做可视化代码审查，并可把 pinned comment 通过 MCP 重新派发给 Agent 生成新 commit；适合作为设计/实现复核后的可视化反馈与回流面板。 | https://foldo.dev/ |
| PlanBridge 用 stable plan hash 固定已批准计划，再把计划交给 Codex 在隔离 worktree 执行并回传 review diff；适合把“计划内容指纹 → Codex 执行 → Git 状态/审查证据”做成可核验链路。 | https://github.com/jbelnick/planbridge |
| issue-workflow-kit 把已批准 plan 视为不可变工件，并在执行 envelope 同时携带 plan/contract digest、base/current SHA 与实际 commit SHA；很适合作为“plan 版本 ↔ 实现提交”强绑定与审查门禁参考。 | https://github.com/sine-io/issue-workflow-kit/blob/main/WORKFLOW.md |
| OpenAI 的 Product Design `design-qa` skill 要求同时取得 Figma/截图等 source visual 与真实 rendered implementation 才能通过 QA，缺任一证据即 blocked；适合做独立 design-reviewer 的硬门禁。 | https://github.com/openai/role-specific-plugins/blob/main/plugins/product-design/skills/design-qa/SKILL.md |
| figma-suite 可双向同步 design tokens、组件与映射，并执行只读 design-system audit、检测 drift/硬编码/组件与变量问题；适合作为多 Agent code review 中独立的 Figma 结构化审查 lane。 | https://github.com/robukh/figma-suite |
| agentic-board 直接驱动真实 GitHub Projects v2，看板可并发调度 Codex/Claude/Copilot 等 CLI 的隔离 worktree，并以 PR review、CI 与未解决线程作为默认 gate；适合把聚合 findings 直接变成项目项继续闭环。 | https://github.com/CSalcedoDataBI/agentic-board |
| Camelot 用 Kanban 编排 Codex 与 Claude Code：Agent 先写 plan 等待批准，再执行、开 PR 并处理 reviewer 评论/CI；适合把计划、异构 Agent 审查与看板状态串成一条受控流水线。 | https://github.com/T0ha/camelot |
| Luigi 支持多 reviewer/executor 编排、候选迭代与批准后 commit/apply/merge，可让 Codex 与 Claude 分任实现和复审并保留迭代基线；适合作为外部 reviewer/judge 编排层。 | https://github.com/RiccardoRomagnoli/luigi |
| Semantic Kernel Agent Orchestration 原生提供 Concurrent、Sequential、Handoff、Group Chat、Magentic 等模式，可把 plan verifier、code reviewer、design reviewer、judge、publisher 固化为可复用多 Agent 流程。 | https://learn.microsoft.com/en-us/semantic-kernel/frameworks/agent/agent-orchestration/ |
| TRIP 默认用不同模型/线程分别承担 plan、plan review、实现、最终 code review，并明确要求 writer 与 reviewer 不同线程；适合构建 Codex 参与的异构多 Agent 审查链，降低同一 Agent 自审盲点。 | https://github.com/PiLastDigit/TRIP-workflow |
| Codex 的 orchestrated multi-agent PoC 把任务拆成 framing、repo investigation、planning、execution、review 等显式角色，适合参考为 plan verifier、实现 Agent 和 reviewer 建立有边界的编排状态机。 | https://github.com/openai/codex/issues/32100 |
| Git AI 会用 Agent hooks 为代码建立 checkpoint，并在 commit 后用 Git Notes 保存模型、prompt 与代码归因，且能在 rebase/amend 等操作中迁移归因；可借鉴把 Codex plan/review provenance 与具体提交长期绑定。 | https://github.com/git-ai-project/git-ai |
| GN Conductor 的工作流会在每个阶段完成后取得 checkpoint commit SHA、回写 `plan.md`，并把完整验证报告附成 Git Note；这是“计划阶段 ↔ commit SHA ↔ 审查证据”双向绑定的直接工程范例。 | https://gn.googlesource.com/gn.git/%2B/HEAD/conductor/workflow.md |
| Shiftlog 通过 hooks 自动把 coding-agent 会话保存到 `refs/notes/shiftlog` 并随 push/pull 同步，还处理 rebase 等历史改写；适合作为 Codex plan、实现理由与 review 会话绑定 commit 的低侵入 provenance 层。 | https://github.com/re-cinq/shift-log |
| Airship 把真实运行页面放进类似 Figma 的多设备可视画布，能将选中元素映射到源码并让 Codex/Claude/OpenCode修改、查看 diff 和回滚；可作为设计/视觉 reviewer 的运行时证据面，再把结果交给独立代码 reviewer。 | https://github.com/0xnyn/airship |
| Figma Make 已支持直接连接生产代码库，让设计、原型代码与真实代码更靠近同一工作面；适合把设计评审前移到真实代码上下文，再将确认后的视觉/交互决策交给 Codex 与其他 reviewer 复核。 | https://www.figma.com/blog/figma-make-now-on-your-local-code/ |
| mpi-kanban 把 planner/worker/reviewer/verifier、共享任务与文件 claim、并行 batch 和 Kanban 状态放进同一仓库型工作流，并提供 Codex 原生 skill 适配；适合把已版本化 plan 拆成可审查任务，再由 reviewer/verifier 回写看板状态和证据。 | https://github.com/MadPonyInteractive/mpi-kanban/blob/main/PLAN.md |
| agentao-kanban 将 Planner、Worker、只读 Reviewer、Verifier 固化为独立角色，并把看板状态、原始 transcript、版本化 prompt 与人类可读 reports 分层保存；适合把多 Agent 审查 finding 发布为看板事件/报告，同时保留可追溯证据。 | https://github.com/jin-bo/agentao-kanban |
| claw-orchestrator 暴露按 commit SHA 运行的 codex_review 工具，并能通过 Codex app-server 设置长期 goal；适合让编排器把 plan 对应的候选提交精确交给 Codex 复审，再把结构化结果推进到长周期任务或看板。 | https://github.com/Enderfga/claw-orchestrator/blob/main/skills/references/tools.md |
| figma-ds-workflows 提供专门给 Codex 的 Figma design-system audit/extract skills，并直接依赖 Figma MCP；适合把真实设计系统先做独立审计，再把 token/组件规范作为代码 reviewer 的设计侧约束输入。 | https://github.com/chelswcs/figma-ds-workflows |
| CSG Design System 把 Figma 设计系统同步成可版本化的 DESIGN.md，Codex 直接通过 AGENTS.md 消费该规范；适合让设计依据与代码一起进入 Git 历史，并让 reviewer 按同一 token/组件契约核对实现。 | https://github.com/Seeed-Solution/csg-design-system/blob/main/README.md |
| figma-design-sync 同时支持 Codex 与 Figma Console MCP，能用 sync-from-figma 对 Figma 变量和代码 token 做差异审计，并用 --fresh 保存设计快照；适合作为代码审查旁路的可重复设计一致性检查。 | https://github.com/lifesized/figma-design-sync |
| Aura 可直接用 aura loop seed .aura/plans/PLAN.md 把既有计划转换成依赖图，再按 ready 集合调度 Crew 并结合验证/回滚；适合将 Codex plan 物化为可追踪执行 DAG，并把每个完成节点再绑定实际 Git 提交。 | https://github.com/Naridon-Inc/aura |
| Codex #40037 提议用包含 scope contract、ownership chain、验证证据与 diff/artifact 摘要的紧凑 evidence packet 驱动动态多 Agent 升级/回退；适合让 reviewer/judge 只基于可观察证据决定重试、重规划、升级或发布结果，避免无界审查循环。 | https://github.com/openai/codex/issues/40037 |
| Codex #36720 提议维护可持久化 decision ledger，并让每条决定直接关联 files、commits、tests、issues/PR；很适合把“批准的 plan 决策 ↔ 实现 commit ↔ reviewer 证据”固化为可跨线程、多 Agent 读取的 provenance。 | https://github.com/openai/codex/issues/36720 |
| Codex #36718 提议为任务生成“需求→可观察证据”的结构化完成报告，并保留命令结果、失败/跳过检查及 machine-readable 输出；适合把多 Agent reviewer 的验证结果聚合后发布到看板或项目，而不是只留自然语言总结。 | https://github.com/openai/codex/issues/36718 |
| Codex #36219 要求父任务 Review 面聚合所有子 Agent 的最终改动，而不是只显示主 Agent 文件；可作为多 Agent code review 的完整性原则，确保看板/审查报告引用的是任务级真实 diff 而非不完整局部视图。 | https://github.com/openai/codex/issues/36219 |
| Codex #29530 提议给实现线程挂载只读 reviewer sidecar，并维护小型 review board、finding 状态和回传给 worker 的修复提示；与“独立审查 Agent→看板→Codex 修复线程”的闭环非常贴合。 | https://github.com/openai/codex/issues/29530 |
| designreview 会并行调度 PRD、UX、内容、分析、Figma 等 specialist，统一输出 HTML review 并可把结论写回 Figma；适合作为 Codex 代码 reviewer 之外的独立设计意图审查层。 | https://designagent.dev/plugins/designreview |
| kaji 把 design→design review→implement→code review→fix→verify→PR 建模成可恢复、有限重试的工作流，并可按步骤路由给 Codex/其他 coding agent；适合做跨 Agent 的明确审查状态机。 | https://github.com/apokamo/kaji |
| MCP-Codex-Reviewer 明确把 Codex 暴露为 plan reviewer 与 implementation reviewer，并返回 approved、plan_adherence、issues 等结构化字段；适合作为另一实现 Agent 的独立 Codex 复核通道。 | https://github.com/MarcEspuna/MCP-Codex-reviewer |
| compound-engineering 同时包含 design-implementation-reviewer、figma-design-sync 与多类代码 reviewer，可把“Figma 设计一致性 + 代码质量”作为同一编排框架中的并行审查证据。 | https://github.com/aegntic/compound-engineering |
| create-implementation-plan skill 将计划文件定义为可供全新执行会话独立消费的自包含 contract，并固定 `.PLAN.md` 命名；适合进一步加入 plan hash/commit trailer，把计划工件稳定纳入 Git 追溯链。 | https://github.com/eai-org/agent-toolkit/blob/main/skills/create-implementation-plan/SKILL.md |
| Figma 官方团队展示 MCP 在真实设计/开发流程中跨画布与代码使用的案例，可作为设计上下文持续进入 Agent 流程、再与代码审查和交付闭环结合的最新实践参考。 | https://www.figma.com/blog/4-ways-were-using-our-mcp-server-at-figma/ |
| GitHub Spec Kit 的 Git 扩展可在 `after_plan` 直接执行 `speckit.git.commit`，并继续对 tasks/implement 等阶段自动提交；适合把 Codex 计划工件与阶段性 Git commit 建立清晰、可机器追踪的边界。 | https://github.com/github/spec-kit/blob/main/extensions/git/extension.yml |
| Playwright 官方 Test Agents 自带 planner、generator、healer 三角色并支持 `--loop=codex`；可把设计/代码审查后的关键用户流转成独立 Agent 测试与修复回路，为多 Agent review 增加真实浏览器验证证据。 | https://playwright.dev/docs/test-agents |
| Penpot 官方 MCP 可让 Agent 直接读取和修改设计文件的组件、样式、token、页面与图层，并支持 design↔code 双向流程；可作为 Figma 之外的结构化设计源，让 design reviewer 按真实设计文件复核代码。 | https://help.penpot.dev/mcp/ |
| Zeplin 官方 MCP 向 Agent 暴露 screen/component specs、annotations 和 design tokens；适合让独立 design-reviewer 用交付规范、交互说明和 token 对照 Codex 实现，而不是只看截图。 | https://support.zeplin.io/en/articles/11559086-zeplin-mcp-server |
| programmator 可从 `plan.md` 执行并用 `--auto-commit` 管理 Git，同时默认并行 9 个专门 reviewer、自动修复并复审；非常贴合“计划→提交→多 Agent 代码审查”的一体化自动化。 | https://github.com/alexander-akhmetov/programmator |
| coordinated-agent-team 将 planner、coder、reviewer、QA、security、designer 等角色置于确定性 Orchestrator 工作流，并持久化任务与报告；适合把设计约束、代码审查和最终汇总拆成可追踪的多 Agent gate。 | https://github.com/q3ok/coordinated-agent-team |
| ccsetup 的 Codex review 可同时读取指定 plan 文件与 Git changes，按 architecture/compliance/risk 循环复审，还能在 plan 或代码变化时由 hook 提醒触发；适合做 plan-adherence 的外部审查补充。 | https://github.com/MrMarciaOng/ccsetup |
| Plane 官方 MCP 可让 Agent 读写 projects、work items 与 project/workspace pages；适合把聚合 findings 同时发布成看板任务或审查文档，再由 Codex 或其他 MCP 客户端继续处理。 | https://developers.plane.so/dev-tools/mcp-server |
| monday.com Platform MCP 提供 60+ 读写工具，覆盖 boards/items、WorkDocs 与 Agent 管理；适合把多 Agent 审查结论程序化写成项目卡片和审查文章，并作为 Codex 外部的项目控制面。 | https://developer.monday.com/api-reference/docs/platform-mcp-tools |
| Genie 把 `DESIGN.md` 的 digest 绑定审查、plan review、并行 role agents、implementation review 和 `genie board` 串在同一 Codex/Claude 流程；设计/计划文档本身进入 Git，适合做“计划工件→实现→独立复审→看板”的一体化基线。 | https://github.com/automagik-dev/genie |
| Kandev 原生支持 Codex、Kanban/pipeline、worktree 和可混搭 Agent 的多步骤 workflow，可把 planner/implementer/Codex reviewer 分开并在同一界面审 diff；外部 MCP 还能接设计上下文，适合作为审查结果回流看板的统一控制面。 | https://github.com/kdlbs/kandev |
| Workspine 把 `.work/.../PLAN.md`、执行摘要和验证报告都作为 repo-native 工件，并为 Codex 生成只读 `gsdd-plan-checker` 做 fresh-context 对抗式计划审查；将 `.work/` 提交 Git 后，能稳定把 plan 版本、实现和验证证据按提交历史关联。 | https://github.com/PatrickSys/workspine/blob/main/distilled/DESIGN.md |
| look-before-you-leap 会先持久化 `design.md`，强制 Codex 在写 plan 前独立审设计可行性、约束、爆炸半径和失败模式，并让后续计划每步带 `codexVerify`；适合把设计文件审查前置成 plan/commit 流程的硬门。 | https://github.com/miospotdevteam/codex-setup/blob/main/look-before-you-leap/skills/brainstorming/SKILL.md |
| AgentHub 可用 Codex/Claude 组成 MAIN+A/B/C/D 团队，原生有 design→confirm→execute→aggregate 协议、`team review` 并行审文档/代码和 `team board`；适合把设计审查、代码多 Agent 复核和看板状态放进同一可恢复消息流水线。 | https://github.com/Dmatut7/AgentHub |
| team 的 `cross-model-review` 每轮设计审查和代码审查都会并行调用 Codex/其他 vendor reviewer，并要求本地 reviewer 用真实 `file:line` 复核后才采纳；审查处置会持久化到 `cross-model-notes.md` 并带入 PR，适合做可审计的异构多 Agent judge。 | https://github.com/bostonaholic/team/blob/main/docs/skills.md |
| Agent Orchestrator 把 Codex/Claude 等 worker 和独立 reviewer 分开运行在隔离 worktree，并把 CI、review comments、merge conflicts 自动路由回原 worker；其项目/会话/PR/Review 控制面适合承载聚合 findings 并继续驱动 Codex 修复。 | https://github.com/Untrivial-ai/agent-orchestrator |
| AgentDeck 已把 Codex/Claude/Gemini 适配器、Kanban、隔离 worktree、自动 reviewer gate、A/B 并行 attempts 和 diff review 组合起来；review verdict 会直接决定卡片进入 done 还是回 queued，适合把多 Agent 审查结论变成可操作看板状态。 | https://github.com/JeremiahM37/agentdeck |
| Codex #17231 记录了 Codex App 将计划下载为 `PLAN.md` 的现有行为及稳定命名诉求；可据此在下载后立即赋予 plan ID/语义文件名并纳入 Git，再用 commit trailer/hash 建立可追踪绑定。 | https://github.com/openai/codex/issues/17231 |
| Codex #14333 的分支级上下文提案把 `plan.md`、当前 commit、提交摘要与执行历史放进同一持久目录；虽未采纳，但很适合作为“branch-scoped plan ↔ commit ↔ review history”数据模型参考。 | https://github.com/openai/codex/issues/14333 |
| Figma 官方已让 coding agents 通过 MCP 把 FigJam 当作白板生成和读取架构/流程图；可将架构设计文件作为独立 design-reviewer 的共享证据，再与实现 diff、代码 reviewer findings 一起汇总。 | https://www.figma.com/blog/figjam-your-coding-agents-whiteboard/ |
| codex-claude-companion 在 Codex 内提供只读、带证据引用的 Claude review lane，并包含 deep multi-agent、adversarial 和 security review；适合作为 Codex 实现之外的异构第二审查层，再把聚合结论回流项目/看板。 | https://github.com/Kenmege/codex-claude-companion |
| 这篇 2026-08 实践明确建议把 `.codex/plans/` 中计划与实现一起提交，并在 commit message 引用已完成 plan；可直接作为“Codex plan artifact → Git commit”轻量落地范式，与现有 Plan-Hash/trailer 门禁组合。 | https://codex.danielvaughan.com/2026/08/09/agent-plans-task-oriented-artifacts-codex-cli-plan-mode-implementation-steps-file-locations-validation/ |
