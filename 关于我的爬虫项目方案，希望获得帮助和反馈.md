# 关于我的爬虫项目方案，希望获得帮助和反馈

## 1. 调研对象

- 编号：14
- 原始标题：Need help / feedback on my approach to my scraping project
- 来源：https://www.reddit.com/r/webscraping/comments/1nr1b6b/need_help_feedback_on_my_approach_to_my_scraping/
- 调研目标：评估“多来源 URL 发现 + listing page 识别 + 深爬补洞 + LLM/人工校验”是否适合 1000+ 技术博客的全量历史采集、增量同步和 Web 管理，并分析如何融入现有博客知识库方案。

## 2. 原方案的核心思路

原帖提出的流程可以抽象为四层：

1. 使用 Sitemap 与 Common Crawl 作为初始 URL 种子来源；
2. 对 URL 做去跟踪参数、去重、正负关键词过滤，寻找“listing pages”——即持续链接到文章、公告、新闻等叶子内容的页面；
3. 通过 deep crawl 遍历站点结构，寻找第一阶段没有发现的新 URL，并再次识别 listing pages；
4. 下载候选 listing page 的内容，使用 LLM 判断是否确实是内容列表页，再让用户人工确认，以反馈改进识别结果。

原帖后续进一步说明，希望避免无差别抓取所有页面，而是沿站点树找到“会产生多个内容叶子节点的分支”，优先保留高价值的导航/聚合节点。

这个思路对本项目最有价值的地方，不是 LLM，而是把“发现 URL”与“抓正文”分开，并把 listing/archive/category 等页面视为长期可复用的发现入口。

## 3. 与 1000 个技术博客需求的匹配度

### 3.1 高度匹配的部分

对于技术博客知识库，最难的问题通常不是单篇正文抽取，而是：

- 如何证明历史文章尽量没有漏；
- 如何在站点新增文章后低成本发现；
- 如何面对 Sitemap 不完整、Feed 只保留最近几十篇、Archive 分页隐藏、旧 URL 已迁移等情况；
- 如何避免每次增量都重新全站深爬。

原方案的“多来源 URL 联合发现 + listing page 固化”恰好可以补强这些问题。

### 3.2 不能直接照搬的部分

原方案仍存在几个关键风险：

1. **Common Crawl 不能证明完整历史。** Common Crawl 官方明确说明其语料是 Web 的采样，并不保证完整归档某个网站，因此只能作为补充发现证据，不能作为 FULL_BACKFILL 完成条件。
2. **仅靠 URL 关键词过滤容易误杀。** 技术博客经常存在无日期、slug 不规则、query-based permalink、Docs 路由等情况，简单正负关键词规则只能作为候选特征。
3. **“走到最深再回退”的树遍历假设不可靠。** 现代站点并不一定是严格树结构，可能有分页、标签交叉、SPA、无限滚动、同文多入口和循环链接。
4. **LLM 不应进入抓取正确性的关键路径。** LLM 可以辅助识别 listing page 和生成 Site Profile，但不能决定是否丢弃历史 URL，也不应“在线自学习”后自动修改生产规则。
5. **Common Crawl CDX API 不适合 1000 站点大规模高频并发查询。** 官方说明该接口有严格限流，大规模过滤更适合 Columnar/URL Index，通过 Athena、Spark 或 DuckDB 等批处理。

因此正确的落地方式应是：保留“发现面”的思想，但将其工程化、证据化、版本化。

## 4. 技术原理一：把 URL Discovery 与 Content Fetch 分成两个平面

传统小型爬虫常见流程是：

```text
首页 -> 跟链接 -> 下载页面 -> 判断是不是文章 -> 保存
```

它的问题是“为了发现 URL，先支付了页面抓取成本”。在 1000 个站点和百万级文章规模下，这会造成大量浪费。

更合理的流程是：

```text
Discovery Plane
  Sitemap / Feed / CMS API / Common Crawl / Archive / Listing / Deep Crawl
        -> Candidate URLs + Evidence

Candidate Normalization / Classification
        -> Article Candidate / Discovery Surface / Ignore / Unknown

Fetch Plane
        -> 只抓真正需要验证或获取正文的 URL
```

Crawl4AI 当前文档也明确区分 URL Seeding 与 Deep Crawling：URL Seeding 更适合一次发现大量已存在 URL，Deep Crawling 更适合实时探索和未知结构。对于本项目，应把二者作为互补 Provider，而不是二选一。

## 5. 技术原理二：Common Crawl 应作为“历史候选补洞器”，不是权威目录

### 5.1 两种 Common Crawl 使用模式

#### 模式 A：CDX/API 交互式查询

适合：

- PROBE 阶段少量站点；
- 单域名排障；
- 管理后台人工查询；
- 验证某个旧 URL 是否曾被抓到。

优点是简单，能快速返回 URL、抓取时间、状态码、WARC 定位等信息。

缺点是官方接口严格限流，不适合对 1000 个域名做高并发、跨多个 CC-MAIN 历史集合的批量扫描。

#### 模式 B：Columnar / URL Index 批处理

适合：

- 1000+ 域名批量历史 URL 枚举；
- 按 registered domain、crawl batch、status、mime 等条件聚合；
- 定期做历史补洞和差异计算。

建议生产环境增加独立的 `COMMON_CRAWL_BULK` Provider，通过 DuckDB/Athena/Spark 查询 Parquet/Columnar Index，输出标准化 URL Observation，再进入平台自己的去重与分类流程。

### 5.2 Common Crawl 的正确语义

Common Crawl Observation 应记录：

```text
provider = COMMON_CRAWL
crawl_id = CC-MAIN-xxxx-xx
capture_time
url
status
mime
warc_filename
warc_offset
warc_length
content_digest
```

它证明的是：

> “在某次 Common Crawl 中观察到过这个 URL/抓取记录。”

它不证明：

> “这个站点的所有文章都已被 Common Crawl 收录。”

因此 Coverage 逻辑中，Common Crawl 只能增加候选和历史证据，不能单独产生 `exhaustion_reason=COMPLETE`。

### 5.3 历史已下线内容的恢复

如果业务确实要求尽可能恢复已经从源站删除的历史文章，可以设计一个可选的：

```text
ARCHIVE_RECOVERY_COMMON_CRAWL
```

流程：

```text
旧 URL live fetch = 404/410/不可达
 -> 检查 Common Crawl capture
 -> 在策略允许时读取 WARC 历史快照
 -> Snapshot.origin = COMMON_CRAWL_ARCHIVE
 -> 后续抽取、质量、Identity 与普通 Snapshot 共用
```

但必须明确区分“当前源站事实”和“历史归档事实”，不能把 Common Crawl WARC 当成实时抓取结果；同时需要按来源版权、站点政策和知识库用途决定是否启用。

## 6. 技术原理三：listing page 应升级为一等实体 Discovery Surface

原帖中最值得吸收的是 listing page 概念。

对博客站而言，高价值发现面通常包括：

```text
SITEMAP
FEED
CMS_API
ARCHIVE_YEAR
ARCHIVE_MONTH
CATEGORY
TAG
AUTHOR
BLOG_INDEX
DOCS_TOC
PAGINATED_LISTING
SEARCH_RESULT
```

这些页面并不是知识库正文，但它们能够持续产生正文 URL。

建议增加实体：

```text
discovery_surface
  id
  source_id
  surface_type
  url_id
  path_template
  discovery_method
  confidence
  state
  validation_state
  article_link_ratio
  unique_article_count
  pagination_signal
  first_seen_at
  last_seen_at
  last_success_at
  active_release_id
```

以及：

```text
discovery_surface_observation
  surface_id
  run_id
  snapshot_id
  outbound_url_count
  article_candidate_count
  new_article_count
  duplicate_count
  next_page_count
  observed_at
```

这样 listing page 不再只是“识别完就丢掉的中间结果”，而是增量同步的长期资产。

## 7. listing page 的识别算法

不建议一开始就把整页交给 LLM。更稳妥的是三阶段分类。

### 7.1 阶段一：低成本结构特征

对候选页面抓取一次轻量 HTML/HEAD 后计算：

- 页面内同域链接数；
- 指向“文章型 URL 模板”的链接比例；
- 链接目标的 URL path 聚类数量；
- 是否存在重复卡片 DOM 结构；
- 是否存在日期密集分布；
- 是否存在 `next` / `older` / 页码 / cursor；
- 主体文本长度与链接密度；
- 标题中是否出现 archive/blog/news/posts/category/tag；
- 是否存在 Schema.org `ItemList`、Breadcrumb、CollectionPage；
- 是否大量链接到同一路径模板下的叶子页面。

可以形成一个确定性分数：

```text
surface_score =
    w1 * article_link_ratio
  + w2 * repeated_card_score
  + w3 * pagination_score
  + w4 * date_density
  + w5 * template_consistency
  - w6 * external_link_ratio
  - w7 * nav_noise_ratio
```

分数高则自动进入候选 Surface；分数低直接忽略；中间灰区才进入第二阶段。

### 7.2 阶段二：LLM 辅助判断

只给 LLM：

- 页面标题；
- 精简 DOM/cleaned HTML；
- 代表性 20~50 个链接及 anchor；
- 结构特征；
- 预估的文章 URL 模板。

要求结构化输出：

```json
{
  "is_discovery_surface": true,
  "surface_type": "CATEGORY",
  "confidence": 0.91,
  "article_url_patterns": ["/blog/*"],
  "pagination": "?page={n}",
  "evidence": ["..."],
  "risks": ["..."]
}
```

LLM 输出只作为 `classification_evidence`，不能直接修改生产 Profile。

### 7.3 阶段三：人工确认

Web 管理台展示：

- 候选 Surface；
- 代表性文章链接；
- 发现来源；
- 规则分数；
- LLM 判断；
- 预计新增文章数。

管理员执行：接受 / 拒绝 / 修改规则。

接受结果生成新的 `Site Profile Release`，而不是覆盖旧配置。

所谓“让 LLM 学习”应落地为：积累人工标注集、更新 classifier/prompt/release，并通过离线回归验证后发布，而不是在线自修改。

## 8. Deep Crawl 的正确角色

原帖希望通过 deep crawl 找到第一阶段没发现的 listing pages，这个方向正确，但需要边界。

### 8.1 为什么不能用深度或 max_pages 证明完整

站点图通常包含：

- 循环导航；
- 标签交叉；
- 无限日历；
- faceted navigation；
- query 参数组合爆炸；
- SPA 路由；
- 同一文章多入口。

因此：

```text
Deep crawl exhausted != Site history complete
max_pages reached != Coverage complete
```

深爬的角色应是：

```text
发现未知 Surface
发现未知 URL 模板
为已有 Provider 的 coverage gap 提供补充候选
```

### 8.2 建议策略

- PROBE：BFS 1~2 层，快速找到 hub/listing；
- 已识别内容路径：Best-First，根据 article URL pattern、anchor、DOM 特征提高优先级；
- 特定结构站：DFS 可以用于探索单一深层分支，但不作为通用策略；
- 全过程必须限制同域、canonical host、query explosion、calendar trap；
- 使用 checkpoint/resume，避免长任务因进程失败从头开始。

Crawl4AI 当前 Deep Crawling 已支持 BFS、DFS、BestFirst、FilterChain、URL scorer、checkpoint/resume，可以作为实现 Adapter，但平台仍应保存自己的 Task/Coverage 状态，避免把业务事实锁死在框架内部状态里。

## 9. Candidate URL 规范化与去重

原帖提出“remove tracking additions / duplicates”，这一步必要，但必须版本化。

建议增加 `url_normalization_release`，规则包括：

```text
lowercase host
punycode normalization
default port removal
fragment removal
path slash policy
percent-encoding normalization
tracking params removal
query sorting
known session params removal
site-specific semantic params allowlist
```

常见可删除 tracking 参数：

```text
utm_*
gclid
fbclid
mc_cid
mc_eid
```

但不能全局删除所有 query 参数，因为：

```text
?id=123
?p=456
?page=7
?article=abc
```

可能本身就是内容身份或分页语义。

正确做法是：

```text
Observed URL
 -> Normalized Candidate URL
 -> Resolution Plane
 -> Network Final URL
 -> Canonical Identity
```

每一步保存原始值和证据，不能为了去重直接抹掉来源信息。

## 10. 基于 Discovery Surface 的增量同步

一旦 PROBE/全量回填识别出高质量 Surface，后续增量就不需要频繁深爬全站。

建议调度：

```text
Tier 0: Feed / CMS API          1~10 分钟或按需求
Tier 1: Sitemap / Blog Index    10~60 分钟
Tier 2: Category/Archive        1~6 小时
Tier 3: Deep Crawl Reconcile    每周/每月
Tier 4: Common Crawl Reconcile  新 CC-MAIN 发布后或按月
```

每个 Surface 维护收益指标：

```text
new_urls_per_scan
changed_urls_per_scan
duplicate_ratio
latency
failure_rate
bytes_per_new_url
```

调度器可以根据收益动态调整频率：高产 Surface 高频扫描，长期无新增的 Surface 自动降频，但保留周期 Reconcile。

这比“每次增量重新深爬整个站”更符合 1000 站点的成本模型。

## 11. 1000 站点规模下的批量实现

### 11.1 Common Crawl 批处理

不要让每个 Source Worker 独立高频请求 CDX API。

建议：

```text
cc-bulk-discovery-worker
  -> 收集待查询 registered domains
  -> 按 crawl_id 批量查询 Columnar URL Index
  -> 按 domain 分桶输出 Observation
  -> 缓存 crawl_id + domain 结果
```

幂等键：

```text
COMMON_CRAWL_DISCOVER:
  registered_domain + crawl_id + provider_release
```

同一 CC-MAIN 对同域只需跑一次；新的 CC-MAIN 出现后再追加扫描。

### 11.2 Deep Crawl Worker

Deep Crawl 不应和正文 Browser Worker 共池。

建议独立：

```text
discovery-browser-worker
content-browser-worker
```

原因：Discovery 的目标是 URL 图和结构信号，正文抓取的目标是稳定 DOM/Markdown，资源模型与超时策略不同。

### 11.3 Surface 监控

对 1000 个站点，如果平均每站维护 5~20 个有效 Surface，也只是约 5000~20000 个轻量发现任务，远低于全站重爬的成本。

## 12. Web 管理应增加的功能

现有管理台可以新增 `Discovery Surface` 页面：

### 12.1 Source 级视图

展示：

- Sitemap / Feed / CMS / Common Crawl / Deep Crawl 各自发现多少 URL；
- Provider 之间交集和独有 URL；
- 哪些 URL 只在 Common Crawl 中出现；
- 哪些历史时间段存在 gap；
- 已确认的 Surface 列表及最近产出。

### 12.2 Surface 审核队列

展示：

- URL；
- 类型预测；
- 规则分；
- LLM 置信度；
- 代表性文章链接；
- pagination 规则；
- 最近一次新增数。

管理员可以接受/拒绝/编辑，操作生成 Profile Release 和 Audit Log。

### 12.3 URL Provenance

任意候选 URL 都可以看到：

```text
Sitemap observed
Common Crawl observed in CC-MAIN-xxxx-xx
Found from /blog/page/3
Found from category page
Deep crawl parent
Feed observed
```

这对解释 Coverage 和去重非常重要。

## 13. 对现有博客知识库方案的具体优化建议

基于本次调研，现有方案不需要推翻，建议做以下增强：

1. 在 PROBE 中加入 Common Crawl/Domain Mapping 作为补充发现能力，但默认只做元数据发现，不直接把其结果当成完整历史。
2. 在发现 Provider 中增加 `COMMON_CRAWL_CDX`、`COMMON_CRAWL_COLUMNAR`，并明确前者只适合轻量/交互查询，后者用于批量历史扫描。
3. 新增 `Discovery Surface` 一等实体，固化 listing/archive/category/docs TOC 等高价值增量入口。
4. 在 Discovery 与 Resolution 之间新增 Candidate Normalization / Classification 阶段，URL 规则按 Release 管理。
5. Deep Crawl 定位为“未知结构发现与 Coverage gap filler”，不是 FULL_BACKFILL 完成证明。
6. 对 listing page 使用“确定性结构特征 -> LLM 灰区判断 -> 人工确认 -> Profile Release”的三级机制。
7. 新增 Surface 级增量调度与收益指标，降低 1000 站点长期运维成本。
8. Common Crawl 大规模查询采用批处理和缓存，避免对公共 CDX API 造成高压。
9. 可选增加 Archive Recovery，将 Common Crawl WARC 作为历史已下线文章的独立 Snapshot 来源，但需要显式策略开关和合规边界。
10. Web 管理增加 Provider overlap、Surface 审核、URL provenance、Surface yield 和历史 gap 视图。

## 14. 不建议加入的设计

### 14.1 不让 LLM 自动决定“这个 URL 永久忽略”

任何可能导致 Coverage 丢失的决策都应由确定性规则、版本化策略或人工确认承担。

### 14.2 不把 Common Crawl 当成事实源站

Common Crawl 是第三方历史采样。当前内容仍应优先从源站抓取，WARC 恢复必须单独标记来源。

### 14.3 不做无边界全域 Domain Mapping

Crawl4AI 的 DomainMapper 可以同时发现 sitemap、Common Crawl、Wayback、证书透明度、robots、feeds、homepage 等多种来源，适合 PROBE。但技术博客知识库通常只需要明确允许的博客/docs host。发现 staging、admin、API、dashboard 等子域后仍要经过 scope policy，不能自动纳入抓取。

### 14.4 不用 max_pages 冒充完整性

达到预算只能产生 `BUDGET_EXCEEDED` / known gap，不能标记 FULL_BACKFILL Complete。

## 15. 最终结论

这篇讨论对现有方案最大的增量价值是把“listing page”抽象为长期维护的 Discovery Surface，并将 Sitemap/Common Crawl/Deep Crawl 组合为分层发现系统。

对于 1000 个技术博客，推荐的总体模式是：

```text
权威来源优先：CMS/API + Sitemap + Feed
        ↓
历史补洞：Archive + Common Crawl + 可选 Wayback
        ↓
结构补洞：BFS/BestFirst Deep Crawl
        ↓
候选标准化、URL 模板聚类
        ↓
Discovery Surface 识别
        ↓
确定性特征 -> LLM 灰区 -> 人工确认
        ↓
Profile Release
        ↓
高收益 Surface 做长期增量同步
        ↓
周期性 Reconcile 重新发现遗漏
```

这样既保留了原帖“先找高价值入口、避免无差别抓取”的优点，又满足本项目对可证明 Coverage、可扩展、可恢复、可审计和增量同步的工程要求。

## 16. 参考资料

- 原始讨论：https://www.reddit.com/r/webscraping/comments/1nr1b6b/need_help_feedback_on_my_approach_to_my_scraping/
- Crawl4AI URL Seeding：https://docs.crawl4ai.com/core/url-seeding/
- Crawl4AI Deep Crawling：https://docs.crawl4ai.com/core/deep-crawling/
- Crawl4AI Domain Mapping：https://docs.crawl4ai.com/core/domain-mapping/
- Common Crawl Index Server：https://index.commoncrawl.org/
- Common Crawl CDXJ Index：https://commoncrawl.org/cdxj-index
- Common Crawl Get Started：https://commoncrawl.org/get-started
- Common Crawl FAQ：https://commoncrawl.org/faq
