# 面向 AI 流程的网页抓取：2026 年真正有效的方法是什么

## 1. 调研对象

- 编号：42
- 原文：Web Scraping for AI Pipelines: What Actually Works in 2026
- 作者：Pankaj Jaiswal / 47Billion
- 发布时间：2026-06-30
- 原文地址：https://medium.com/47billion/web-scraping-for-ai-pipelines-what-actually-works-in-2026-f468b1119abc
- 关联项目背景：47Billion 的 7Seers AI 教育/就业平台内容管线

本文的价值不在于提供一个“万能爬虫”，而在于把 AI 内容采集拆成多个相互独立的工程问题：发现 URL、判断页面是否需要浏览器、获取完整页面、抽取主内容、把内容变成适合 LLM/RAG 的结构、控制成本、检测静默失败、处理访问限制。这个拆分非常适合 1000 个技术博客的长期知识库场景。

## 2. 原文核心架构

原文从两次失败演进到分层架构。

第一阶段是 HTTP + BeautifulSoup。它在静态页面上工作良好，但对大量 CSR/SPA 页面只能拿到壳 HTML，最危险的问题不是请求报错，而是“请求成功、DOM 也合法、正文为空”。这种静默失败会继续流入 LLM，使下游生成看似正常但实际上缺内容的结果。

第二阶段使用 Playwright 执行 JavaScript，解决动态渲染问题。但浏览器路径引入了明显更高的资源开销、限流和访问挑战，因此不能把所有 URL 无条件送入浏览器。

原文最终采用概念上的决策路由：

```text
URL
 -> Decision Router
    -> Static HTML -> lightweight HTTP parser
    -> Dynamic/JS -> Browser renderer
 -> Extraction
    -> Crawl4AI Markdown/JSON
    -> deterministic selector extraction
    -> LLM extraction for messy pages
 -> Clean Markdown/JSON
 -> LLM/RAG agent
```

这个思想比具体工具更重要：抓取系统必须具备“按证据升级”的能力，而不是选择单一抓取器覆盖所有网站。

## 3. 为什么 AI 知识库抓取与传统爬虫不同

传统爬虫通常以“字段是否抓到”为成功标准；AI/RAG 内容管线的成功标准应变成“模型是否能可靠地理解、引用和复现这个页面的语义”。这带来几个直接变化。

### 3.1 原始 HTML 不是最终知识格式

导航、页脚、推荐文章、Cookie Banner、广告、埋点文本和脚本都可能污染上下文。对 RAG 来说，这些内容不仅浪费 token，还会破坏 chunk 的主题纯度，并造成错误链接归因。

因此生产系统至少要同时保存：

```text
raw response / rendered DOM
cleaned HTML
raw markdown
fit/content-focused markdown
canonical Document IR
final markdown
```

其中 raw/fit Markdown 应视为“抽取候选”，而不是唯一业务真相。最终知识库应由统一 Document IR 渲染，便于未来更换清洗器、Markdown renderer 或 LLM，而无需重新访问源站。

Crawl4AI 官方文档确认其 Markdown 结果可以同时包含 `raw_markdown` 与 `fit_markdown`；fit 版本通过 Pruning/BM25 等内容过滤器减少低价值节点。对于技术博客正文，它很适合作为候选内容，但不能直接把被过滤掉的内容视为不存在，因为代码示例、短 API 说明、脚注、表格等可能被误判为低价值。

官方参考：

- https://docs.crawl4ai.com/core/markdown-generation/
- https://docs.crawl4ai.com/core/fit-markdown/

### 3.2 “空正文但请求成功”必须作为一级故障

原文最值得纳入系统设计的一点是 silent failure。HTTP 200、浏览器加载完成、extractor 没抛异常，都不能证明抓取成功。

生产系统需要独立 Content Fitness Gate，至少计算：

```text
main_text_chars
semantic_block_count
heading_count
paragraph_count
code_block_count
table_count
link_density
boilerplate_ratio
content_density
title_body_similarity
raw_to_clean_ratio
rendered_vs_http_delta
```

并结合 Page Type 采用不同阈值。例如 API reference 只有 80 个字也可能完整；长博客只有 100 个字则高度可疑。因此质量判断不能只依赖固定 `word_count_threshold`。

### 3.3 新鲜度是知识库状态问题，不是 LLM 能力问题

技术博客会修改旧文章、迁移 URL、补充代码、更新依赖版本。模型训练数据即使见过旧页面，也不能代替实时同步。因此 URL、Document 和 Version 必须分离：

```text
URL observation -> document identity evidence -> document_version
```

增量同步应使用 ETag、Last-Modified、Sitemap lastmod、Feed GUID/API cursor、body hash、Document IR hash 和周期性 Reconcile，而不是“Markdown 文件已经存在就跳过”。

## 4. Static/Dynamic Decision Router 的实现

原文提出静态/动态分流，但真正落地不能只通过域名白名单或人工判断。建议做可解释的 Preflight Router。

### 4.1 第一次 HTTP Probe

对 URL 先执行受限 HTTP 请求，并保存：

```text
status
final_url
content_type
content_length
etag
last_modified
raw_html_hash
script_count
noscript_text
main_candidate_text_chars
structured_data_presence
meta_title
canonical
```

然后运行轻量正文提取器（selectolax/lxml + Trafilatura/Readability/Crawl4AI 非浏览器路径）。

如果同时满足：正文密度足够、主标题存在、页面无明显 SPA shell、正文结构完整，就直接走 HTTP 路径。

### 4.2 Browser escalation 证据

只在出现以下证据时升级：

```text
SPA_SHELL
EMPTY_ARTICLE_BODY
CLIENT_RENDER_MARKER
JS_PAGINATION
LOAD_MORE
INFINITE_SCROLL
SHADOW_DOM_REQUIRED
HTTP_EXTRACT_QUALITY_FAILED
NAVIGATION_NOT_VISIBLE
```

升级原因必须写入 attempt，不能只是一个布尔 `use_browser=true`。这样 Web 管理端才能统计每个站的浏览器升级率并逐步优化成本。

### 4.3 Browser readiness 不以 networkidle 为默认

浏览器抓取常见错误是把 `networkidle` 当成统一“页面已加载”信号。持续埋点、WebSocket、长轮询会让它长期不能满足，而有些页面 DOMContentLoaded 后正文已经完整。

Playwright 官方 API 明确把 `networkidle` 标为 discouraged，因此建议版本化 Readiness Contract：

```text
wait_until: domcontentloaded | load
wait_for_css
wait_for_js
min_main_text_chars
delay_before_snapshot
scroll_plan
max_scroll_rounds
timeout
```

优先顺序：

```text
HTTP content complete
 -> domcontentloaded
 -> domcontentloaded + stable content selector
 -> explicit JS readiness condition
 -> explicit bounded interaction plan
```

参考：https://playwright.dev/docs/api/class-page

## 5. Crawl4AI 在方案中的合理位置

原文把 Crawl4AI 放在 extraction layer，这个定位是合理的，但对于 1000 站知识库不能让它同时承担业务调度、完整性证明和永久状态。

推荐把 Crawl4AI 封装成 Browser/Extraction Adapter：

```text
System Task
 -> Crawl4AI Adapter
 -> CrawlResult
 -> Immutable Artifact
 -> Typed Domain Writer
 -> Quality Validator
```

系统自身负责 durable frontier、lease、retries、run context、coverage、document identity 和 publish state。

### 5.1 `arun_many()` 的正确使用方式

Crawl4AI 官方支持 `arun_many()`、streaming、MemoryAdaptiveDispatcher 和 SemaphoreDispatcher。它非常适合一个 Browser Worker 内部的小批量并发，但不应该把百万 URL 一次性交给单个进程。

推荐：

```text
DB claim 20~100 tasks
 -> group by domain/profile
 -> arun_many(stream=True)
 -> each result immediately persisted
 -> claim next batch
```

MemoryAdaptiveDispatcher 解决单 worker 内的内存自适应；跨 worker 的 domain concurrency、全局礼貌限流和公平性仍必须由系统调度层控制。

参考：

- https://docs.crawl4ai.com/api/arun_many/
- https://docs.crawl4ai.com/advanced/multi-url-crawling/

### 5.2 raw Markdown 与 fit Markdown 应同时保留

建议 Artifact 中同时保留：

```text
RAW_MARKDOWN
FIT_MARKDOWN
FIT_HTML
CLEANED_HTML
```

然后由独立 Validator 比较：

- fit 是否把代码块删掉；
- 表格是否丢列；
- 标题层级是否异常；
- 引用和链接是否丢失；
- 正文长度是否相对 raw 异常骤降；
- 页面更新后 fit 结果是否发生突变。

最终 accepted version 不应直接等于 `fit_markdown`，而是通过 Document IR 生成。

## 6. Selector、通用提取器和 LLM Extraction 的分层

原文指出 selector 容易随着页面结构变化失效，这对 1000 站特别重要。最佳策略不是“全部用 LLM”，而是按成本和稳定性分层。

### Tier 1：结构化元数据

优先读取 JSON-LD、OpenGraph、HTML meta、Feed/API。标题、作者、发布时间、canonical 等尽可能从标准元数据获得。

### Tier 2：通用正文算法

Trafilatura、Readability、Crawl4AI content filtering 适用于大多数文章页，成本低、无需模型调用。

### Tier 3：站点规则

对于结构稳定且价值高的站，使用版本化 CSS/XPath Contract。规则必须带样本测试和 drift detection，不能直接散落在 worker 代码中。

### Tier 4：LLM Structured Extraction

只对结构混乱、传统方法质量不达标、且业务价值足够高的页面使用。输入应是已压缩的 cleaned HTML/fit HTML，而不是原始整页 HTML；输出必须受 JSON Schema 约束并做 deterministic validation。

这能把 LLM 成本从“每个页面固定开销”变成“少数失败页面的升级成本”。

## 7. 访问限制与合规边界

原文包含住宅代理、浏览器指纹与行为模拟等绕过检测的经验描述。对于长期知识库系统，这些能力不应成为默认抓取策略，原因包括：

1. 可能与 robots.txt、服务条款、访问控制意图冲突；
2. 使成功率变成不可预测的对抗过程；
3. 代理、指纹和浏览器补丁增加供应链和安全风险；
4. 网站防护策略变化会造成高维护成本；
5. 对企业知识库而言，可审计和可持续通常比“强行抓到”更重要。

更适合的生产策略是把挑战页面建模为明确状态：

```text
ROBOTS_DENIED
AUTH_REQUIRED
PAYWALL
WAF_CHALLENGE
CAPTCHA_CHALLENGE
RATE_LIMITED
TERMS_REVIEW_REQUIRED
```

然后按策略转向：官方 API、RSS/Feed、授权数据源、站点导出、人工授权或停止抓取。系统不应自动绕过验证码、登录、付费墙或明确访问控制。

## 8. 对 1000 站历史全量抓取的关键启示

原文主要讨论“单 URL 获取与抽取”，但用户目标更大：需要证明历史文章抓全。因此必须在原文架构上再增加 Discovery/Coverage 层。

每个站至少并行维护这些 Provider：

```text
Sitemap/Sitemap Index
Feed/RSS/Atom/JSON Feed
Platform API
Archive by year/month
Category/Tag/Author pagination
Documentation navigation tree
HTML recursive frontier
Browser dynamic listing
```

每个 URL 保存 discovery evidence。FULL_BACKFILL 完成条件不能是“队列空了”或“抓了 5000 页”，而必须满足主要 Provider 完成、durable frontier 无待处理、dead-letter 已处理、Reconcile 不再产生显著新增、Coverage Ledger 没有未知断档。

## 9. 建议新增到总体技术方案的能力

结合本文，现有方案最值得强化的不是反爬对抗，而是以下五项。

### 9.1 Preflight Fetch Decision Router

把 HTTP-first 从原则升级为可观测决策器，保存每次升级浏览器的证据，并在站点级统计：

```text
http_success_rate
browser_escalation_rate
http_vs_rendered_content_delta
browser_cost_per_document
```

### 9.2 Content Fitness Gate

在抓取完成和 extraction 之间增加独立质量门，专门阻断“HTTP 200 但正文为空”“Browser 成功但内容未加载”“fit_markdown 过度裁剪”等 silent failure。

### 9.3 Extraction Fallback Contract

把提取降级路径版本化为：

```text
metadata/structured data
 -> general extractor
 -> Crawl4AI raw/fit
 -> site selector contract
 -> LLM structured extraction
 -> manual review
```

具体顺序可按站点 release 调整，但 worker 不允许无限自由 fallback。

### 9.4 Browser Cost Model

Browser worker 持续记录 runtime/page 的峰值内存、耗时、失败率、站点升级率，调度层按实时资源和 domain quota 控制并发。Crawl4AI `dispatch_result` 可作为 adapter telemetry，但最终资源决策由系统指标完成。

### 9.5 Access Challenge Policy

显式区分“技术渲染失败”和“网站明确阻止自动访问”。前者可以升级技术路径，后者进入 policy workflow，不自动进入对抗性绕过流程。

## 10. 推荐状态机

```text
DISCOVERED
 -> ADMITTED
 -> HTTP_FETCHED
 -> PREFLIGHT_VALIDATED
    -> EXTRACT_HTTP
    -> BROWSER_REQUIRED
         -> BROWSER_FETCHED
 -> CONTENT_FITNESS_CHECK
 -> EXTRACTION_CANDIDATE
 -> QUALITY_VALIDATED
 -> DOCUMENT_VERSION_ACCEPTED
 -> INDEXED
 -> PUBLISHED
```

异常分支：

```text
ACCESS_CHALLENGE
RETRYABLE_FAILURE
PERMANENT_FAILURE
QUALITY_REJECTED
MANUAL_REVIEW
```

每个状态转移都由持久化领域事实驱动，而不是协程结束或 Redis ack 驱动。

## 11. 关键实现伪代码

### 11.1 Router

```python
async def route_fetch(task, context):
    http = await http_fetch(task.url, context.http_profile)
    snapshot_id = await commit_raw_snapshot(http)

    if http.status == 304:
        return NotModified(snapshot_id)

    route = sniff_content_type(http)
    if route != "HTML":
        return route_non_html(route, snapshot_id)

    candidate = extract_lightweight(http.body)
    fitness = evaluate_content_fitness(candidate, page_type=task.page_type)

    if fitness.passed and not detect_dynamic_shell(http.body):
        return HttpExtractionCandidate(snapshot_id, candidate, fitness)

    return BrowserRequired(
        snapshot_id=snapshot_id,
        reason=fitness.reason or "DYNAMIC_SHELL"
    )
```

### 11.2 Browser Worker

```python
async def browser_loop(worker):
    crawler = await worker.open_long_lived_crawler()
    try:
        while True:
            tasks = await claim_tasks(limit=worker.batch_size)
            if not tasks:
                await worker.idle()
                continue

            async for result in crawler.arun_many(
                build_requests(tasks),
                config=worker.run_config(stream=True),
                dispatcher=worker.dispatcher,
            ):
                await persist_attempt_and_artifacts(result)
                await heartbeat_worker()
    finally:
        await crawler.close()
```

注意：真实生产实现还需要 lease epoch、cancel、domain quota、artifact reservation、outbox 和 finalizer；上面只表达层次关系。

## 12. 结论

这篇文章最重要的工程结论可以概括为：AI 系统的上限经常由内容管线决定，而不是由模型决定；动态页面、正文清洗、静默失败和资源成本必须被当作独立问题建模。

对于 1000 个技术博客知识库，应该吸收其“HTTP/Browser 决策路由、Crawl4AI 内容清洗、按成本分层提取、浏览器资源受控、质量优先”的思想，但不能只停留在单页抓取。生产方案还必须补足 durable discovery、历史覆盖证明、文档版本、增量同步、不可变 Artifact、独立质量门禁、访问挑战策略、Web 管理和可恢复状态机。

因此本轮对总体方案的实际优化方向是：新增可解释 Preflight Fetch Decision Router、Content Fitness Gate、版本化 Extraction Fallback Contract、Browser Cost Model 和 Access Challenge Policy，并继续保持“HTTP-first、Browser 仅按证据升级、不得绕过明确访问控制”的原则。
