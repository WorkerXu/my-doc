# 奢侈品识别方案

## 1. 目标与边界

系统目标不是“找最像的商品”，而是把雷小安、奢当家、腕表之家、小迈步、爱回收等来源的记录归一到**同一个官方商品变体（Official Variant）**。

第一阶段：腕表 + 箱包；历史数据约 100 万–1000 万条。腕表已有部分品牌/系列/型号标准库，箱包主档不完整，需要同时支持 Entity Linking 与 Entity Discovery。

核心原则：

> **Precision >> Recall。宁可输出 UNKNOWN，也不能把不同官方变体错合并。**

只有会定义官方变体的关键属性兼容时，才允许自动认定 `SAME_VARIANT`。系统不做真假鉴定；成色、价格、库存、卖家、单件序列号、保卡状态等属于 Offer / 单件属性，不定义官方商品变体。

---

## 2. 核心实体与身份模型

```text
Brand
  └── Product Family / Series
       └── Product Model
            └── Canonical Variant
                 └── Source Product / Offer
```

关系至少包括：

```text
SAME_VARIANT
VARIANT_OF
SAME_MODEL
SAME_SERIES
VISUALLY_SIMILAR
UNKNOWN
```

只有 `SAME_VARIANT` 可生成自动 Recognition。

Canonical Variant 状态：

```text
ACTIVE      已验证，可自动挂接
PROPOSED    新发现，等待证据/治理
RETIRED     历史实体
MERGED      已治理合并
```

新发现实体默认 `PROPOSED`，不得因为若干来源名称或图片相似就直接进入 `ACTIVE`。

### 2.1 官方标识符与字段语义

统一采集：

```text
reference_full / style_code / mpn
gtin / ean / upc
merchant_sku
product_group_id / item_group_id
variant_dimension_keys / variant_dimension_values
barcode_symbology / payload / checksum
market
identifier_namespace / identifier_owner / identifier_scope
identifier_source / source_field_path / confidence
```

标识符必须按“**谁分配、在哪个来源字段出现、作用于 Product Group 还是 Variant、在哪个市场有效**”解释，不能只看字符串是否像型号。

规则：

- 高可信完整 Reference / Style Code 精确一致：最强 Identity Anchor 之一。
- 官方品牌来源中的 `mpn`/manufacturer part number，经品牌规则和格式验证后可作为 Manufacturer-grade Identifier；Google Product 语义中 `mpn` 表示制造商部件号并在制造商范围内唯一标识产品。
- `sku` 是 merchant-specific identifier。第三方商家页面的 `sku` 默认只属于该商家命名空间，**不得直接升级为官方 Reference / Style Code**；即使出现在品牌官网，也要先确认该品牌/CMS 的字段语义和历史唯一性，再决定是否升级。
- `productGroupID`、`item_group_id`、`inProductGroupWithID` 等是**父商品组/变体组关系标识**，不能作为 Variant Identity Anchor，也不能因为两个 Offer 的 group id 相同就判定 `SAME_VARIANT`。
- Schema.org / Google 的 `ProductGroup.hasVariant`、`Product.isVariantOf`、`variesBy`，以及 Merchant 数据中的 `variant_option`，都属于高价值的**变体边界与关系证据**。它们用于回答“哪些属性会产生独立 Variant”，不能替代具体 Variant 的唯一标识和值。
- 高可信、校验通过的 GTIN/EAN/UPC：Strong Positive + Exact Retrieval Key。GS1 要求不同尺寸/颜色等产品变化使用各自 GTIN，因此同一有效 GTIN 对身份非常有价值。
- **GTIN 证据是非对称的**：不同 GTIN 默认 `POSSIBLE_CONFLICT`，而不是无条件 Hard。GS1 GTIN Management 允许某些包装、品牌、规格、促销或供应链层面的变化触发新 GTIN，这些变化未必都等价于本系统定义的新 Official Variant；只有品牌/市场规则证明该场景中 GTIN 与 Variant 1:1 时才升级 Hard。
- Vintage 必须考虑旧编码、市场差异、包装变化和历史规则。
- Canonical Variant 不依赖任何单一标识符存在。

维护 `identifier_semantics_policy`，最小粒度为：

```text
brand × market × source_group × field_path × identifier_type
```

策略记录该字段是 `MANUFACTURER_ID / MERCHANT_ID / GROUP_ID / INSTANCE_ID / UNKNOWN`，以及是否允许 Exact Retrieval、Strong Positive、Hard Conflict、Auto Activation。

---

## 3. 标准 Schema 与 Hard Constraint

每个属性事实保存：

```text
raw_value
normalized_value
canonical_value_id
extractor / extractor_version
source_type / source_id / source_url
evidence_span / image_bbox
confidence / validation_status
effective_from / effective_to
observed_at / content_hash
```

`observed_at` 与有效期分离；新证据不覆盖旧事实，而用 version / supersedes 表达变化。只有双方属性都达到对应可信度门槛时才形成 Hard Conflict；低置信 OCR/VLM 冲突只能触发 `POSSIBLE_CONFLICT → UNKNOWN`。

### 3.1 腕表

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| series / family | Blocking + Strong；品牌规则可 Hard |
| subseries / model | Strong |
| official_reference_full | Hard；最强身份字段之一 |
| reference_base | Retrieval / Strong，默认不能代替 full |
| gtin/ean/upc | Strong；条件 Hard |
| case_diameter_mm | Hard |
| case_material | Hard |
| dial_color | Hard |
| dial_type | 定义独立变体时 Hard |
| bezel | 定义独立变体时 Hard |
| bracelet_or_strap | 条件 Hard；需识别后换件 |
| caliber / movement | 代际/变体区分时 Hard，否则 Strong |
| functions | 变体区分时 Hard |
| edition / generation | Hard |
| serial_number | 单件属性 |
| condition / price | Offer 属性 |

Reference 保存 `raw / normalized / base / full / parts / brand_rule_version`。默认以 `reference_full` 为准；只有品牌规则明确证明后缀不定义 Variant 时才允许 base reference 参与自动合并。

### 3.2 箱包

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| family / series | Blocking + Strong |
| model / style | Strong |
| official_style_code | Hard；最强身份字段之一 |
| gtin/ean/upc | Strong；条件 Hard |
| official_size | Hard |
| material | Hard |
| official_color | Hard |
| hardware | Hard |
| pattern / print | Hard |
| edition / collaboration | Hard |
| season / year / generation | 官方由其区分时 Hard |
| serial / date_code | 单件/生产证据 |
| condition / price | Offer 属性 |

图片颜色不直接成为 Hard Constraint；灯光、白平衡、老化、修图会造成偏差，视觉颜色只做 Soft Evidence。

### 3.3 两级标准化

```text
material_group = ROSE_GOLD
material_brand_value = ROLEX_EVEROSE_GOLD

color_group = BLACK
color_brand_value = CHANEL_NOIR
```

通用层用于跨平台召回，品牌精确层用于 Variant 判定，避免把品牌专有语义压平。

---

## 4. Evidence、来源独立性与时间版本

来源等级：

```text
T0  品牌官网、官方目录、官方标识体系
T1  已验证内部标准库、授权/专业数据库
T2  高质量专业平台、可信二手平台
T3  普通电商、聚合站、搜索网页
```

可信度按“来源 × 属性”定义，而不是整站一刀切。

### 4.1 来源独立性

维护：

```text
source_group_id
syndication_group_id
content_fingerprint
image_hash_group_id
upstream_hint
```

多个 URL 不等于多个独立来源。复制同一 feed、相同图片、相同异常文案、相同字段错误不能增加 `independent_source_count`。

### 4.2 Evidence Family：避免重复计票

至少定义：

```text
IDENTIFIER        Reference / Style Code / MPN / GTIN
STRUCTURED_ATTR   size / material / color / hardware / movement
VARIANT_GRAPH     ProductGroup / isVariantOf / variesBy / variant_option
LEXICAL_TEXT      BM25 / token overlap
SEMANTIC_TEXT     text embedding / text reranker
IMAGE_VISUAL      image embedding / dense feature / perceptual hash
OCR_TEXT          image → OCR → identifier/attribute
EXTERNAL_WEB      外部网页证据
TEMPORAL          year / generation / effective period
```

同一原始观测派生出的多个分数不是多个独立证据：

- 同一标题的 BM25 + Text Embedding 不算两票。
- 同一张图的 pHash + SigLIP/DINO + VLM 观察不算三票。
- 同一网页中的 JSON-LD、DOM 和其搜索摘要默认属于同一 `source_group/evidence origin`，不能当三个独立来源。
- OCR 从图片读出的 Reference 可升级为 Identifier，但必须保留 image/OCR provenance。
- Student Matcher 可使用所有特征学习联合分布，但 `CERTAIN_MATCH` 的风险解释层不得把相关特征机械求和。

### 4.3 时间版本

Canonical 属性保存 `valid_from / valid_to / version / evidence_ids`，用于同名跨年份改款、Reference 规则变化、新旧机芯/五金/尺寸变化、Vintage 回溯等。没有明确代际证据时，不跨年代自动合并。

---

## 5. 属性抽取与标准化

```text
Raw Source Product
      ↓
Schema Mapping
      ↓
Deterministic Parsing / Dictionary
      ↓
Barcode Decode
      ↓
Text Attribute Extraction
      ↓
Scene OCR
      ↓
Document-like OCR/VLM（按需）
      ↓
General VLM Fallback（少量困难样本）
      ↓
Normalization / Ontology Mapping
      ↓
Attribute Validation / Quality Check
      ↓
Normalized Product Profile
```

优先级：规则/主档 → 条码 → 文本 → Scene OCR → 文档 OCR/VLM → General VLM → Quality Check。能用确定性方法解决的问题不交给大模型。

### 5.1 条码与 OCR

条码先用 zxing-cpp 等确定性 decoder 解析 EAN/UPC/Code128/Data Matrix/QR，经过 check digit、格式、品牌/市场校验后进入 Exact Retrieval。

PoC Scene OCR 基线：PP-OCRv6。OCR 后必须分类为：

```text
reference / style_code
gtin/ean/upc
serial_number
date_code
size
material_text
other_text
```

密集文档、保卡整页、历史目录页、复杂表格可用 PaddleOCR-VL-1.6 / PP-Structure 类能力作为 document-like fallback。

### 5.2 General VLM

PoC 困难样本结构化抽取基线：Qwen3.5-4B。职责是产生**可验证候选证据**，不是最终身份判断。

输出严格 JSON：

```json
{
  "attributes": [],
  "observations": [],
  "conflicts": [],
  "unknown_fields": [],
  "evidence_refs": []
}
```

VLM 结果必须经过 ontology、品牌规则、主档、OCR/文本交叉验证；无权直接写 ACTIVE Canonical 或直接输出 `CERTAIN_MATCH`。

---

## 6. 缺模态与证据可用性路由

这是在线识别的**固定策略层**。不要先用生成模型“补齐”缺失图片/文本，再假装输入完整。

对每条 Source Product 计算 `EvidenceProfile`：

```text
identifier_quality = NONE / WEAK / STRONG
text_quality       = NONE / WEAK / STRONG
image_quality      = NONE / WEAK / STRONG
hard_attr_coverage = 0..1
conflict_state     = NONE / POSSIBLE / HARD
catalog_mode       = LINK / DISCOVERY
synthetic_evidence = true / false
```

### 6.1 路由矩阵

| 场景 | 主要召回 | 图片角色 | 最终判定上限 |
|---|---|---|---|
| 强 Reference/Style Code/GTIN + Hard Attr 无冲突 | Exact Identifier + 属性过滤 | 可选交叉验证 | 可进入 CERTAIN_MATCH |
| 文本强、无图片 | Exact/BM25/Text ANN + 属性召回 | 不需要；不得因无图降为错误 | 可进入 CERTAIN_MATCH，但必须满足 Identity/Evidence Policy |
| 图片强、文本弱、无可靠型号 | Image ANN + OCR + 视觉属性 + 少量 VLM | 主要候选发现 | **默认不能仅凭视觉进入 CERTAIN_MATCH**；需 Identifier/结构化/独立来源补证据 |
| 文本强 + 图片强 | 多路召回 + Rerank | 补充和冲突检查 | 可进入 CERTAIN_MATCH |
| 型号疑似错误 | raw/normalized identifier 双路 + 非 identifier 召回 | 用于发现文本错误 | Identifier 与可信 Hard Attr 冲突时 UNKNOWN/NON_MATCH，不做平均分抵消 |
| 文本与图片冲突 | 两种模态分开保留证据 | 触发冲突定位 | `POSSIBLE_CONFLICT → UNKNOWN`；必要时 Web Evidence |
| 无图 + 无可靠型号，但结构化属性较完整 | BM25/Text ANN/Attribute Retrieval | 无 | 仅在关键 Hard Attr 组合足够区分且认证集达标时自动，否则 UNKNOWN |
| 只有泛化标题、缺 Hard Attr | Text ANN/BM25 | 无 | UNKNOWN；不得靠语义相似自动合并 |
| Vintage / 新款无 Canonical | Source Neighbor + Web/Official Evidence | 候选发现 | 进入 `PROPOSED`，不自动创建 ACTIVE |
| 同图被多个 SKU/来源复用 | pHash/dup detection | 识别复制/来源依赖 | 图像相同不构成 SAME_VARIANT |

### 6.2 合成/补全信息的硬规则

2026 的商品 Missing-Modality benchmark 表明，MLLM 能生成高层语义，但在细粒度词级、像素/patch 对齐上仍不可靠。因此：

1. **不得把模型生成的缺失图片/描述当成真实商品事实。**
2. `synthetic_evidence=true` 的内容不能形成 Hard Constraint、Identity Anchor、官方颜色/尺寸/材质事实。
3. 图→文本补全可用于：query expansion、候选召回、属性候选提示。
4. 文本→图像生成默认不进入识别链路；生成图不用于 Image ANN 身份确认。
5. Catalog 内部相似商品 Pattern/RAG 可用于预测缺失属性候选，但预测值必须经真实来源或审核确认后才能升级为 Canonical Fact。
6. 缺模态本身不是负证据：没有图片不能降低一个强 Reference 匹配为 non-match；没有文本也不能让视觉近似自动升级为 match。

### 6.3 低质量图片

图片质量在 Image ANN 前独立评分：

```text
blur
resolution
subject_area_ratio
occlusion
background_complexity
watermark
crop_type
color_cast
```

低质量图降低 `IMAGE_VISUAL` 权重并减少视觉候选配额；局部细节图可用于局部视觉/属性判断，但不能与完整商品正面图等权。所有图片独立向量化，多图不简单平均。

---

## 7. Candidate Retrieval：Blocking + Hybrid Search

100 万–1000 万记录禁止 N²。

召回通道：

```text
Exact Full Reference / Style Code / Manufacturer MPN
Exact GTIN/EAN/UPC
Brand + normalized model/reference
Brand + series/model + Hard Attributes
BM25
Text ANN
Image ANN
Image exact/perceptual hash
Historical Canonical Alias
Unmatched Entity-Discovery Neighbors
```

默认按 `category`，通常再按 `brand_id` 过滤。

PoC 初始 K：Exact ≤20、BM25 30、Text ANN 50、Image ANN 每图 30/总≤60、Attribute 30；union 后≤150，Cheap Rerank≈30，Expensive Matcher 10–20。最终由域内 Recall@K 调参。

Retrieval Gate：

```text
Recall@100 after fusion/truncation >= 99.5%
Exact-candidate survival rate = 100%
```

### 7.1 多路融合

不同通道 raw score 不直接相加。

- Exact/Identity 候选单独保留，不能被 soft fusion 截断。
- 零/少标签阶段使用 RRF 作为 Soft Retrieval 基线。
- 截断前保留每通道 minimum quota，避免单一路径吞掉候选池。
- Exact ∪ RRF 后再 Cheap Rerank。
- 有足够 judgment 后再比较 RRF、归一化 weighted fusion、learned fusion。
- 融合必须版本化，并监控 `Recall before/after fusion`、`Unique-positive contribution by channel`、候选数与延迟。

### 7.2 搜索引擎

生产基线：OpenSearch 3.8，统一承载 Exact/filter、BM25、filtered k-NN、hybrid retrieval、RRF/score normalization、on-disk vector、quantization/rescore、alias/versioned index。

Milvus 只在 10M 真实压测证明 OpenSearch 的 Recall/QPS/资源成本不满足时再拆分。pgvector 适合小规模 PoC/内部工具，不作为唯一的千万级 Hybrid ANN 引擎。

索引至少：

```text
canonical_variant_search_vN
source_product_search_vN
product_image_search_vN
identifier_lookup_vN
```

模型升级采用 shadow index，离线回填验证后切 alias，禁止在线混用不同 embedding 版本。

### 7.3 Open-world Candidate Guard：NIL / None-of-the-Candidates

Candidate Retrieval 必须允许一种独立状态：**正确 Canonical Variant 根本不在当前 Catalog 或候选集合中**。系统不能因为检索总能返回 Top-1，就默认 Top-1 一定属于正确实体。

每次 Linking 在最终 Decision 前生成 `CandidateSetAssessment`：

```text
source_product_id
best_candidate_id / best_score_calibrated
second_candidate_id / second_score_calibrated
score_margin
near_tie_count
candidate_set_state
none_of_candidates_score (optional)
nil_reason
retrieval_health_snapshot
policy_version
```

`best_score_calibrated` / `second_score_calibrated` 指 Pair Matcher 的校准后分数，不使用 ANN/BM25 raw score 直接做身份判断。

候选集状态固定为：

```text
KNOWN_CANDIDATE       存在满足身份策略的候选
AMBIGUOUS_KNOWN       多个已知 Variant 近似且证据不足以唯一确定
POSSIBLE_NIL          当前 Catalog 很可能不存在该 Variant
RETRIEVAL_FAILURE     搜索/索引/路由异常，无法判断是否 NIL
```

策略：

- **禁止强制 Top-1**。没有 Strong Identity Anchor 时，自动匹配同时要求 Top-1 达到 segment 校准阈值，并通过 Top-1 / Top-2 歧义守门；具体 margin 按品牌/品类/EvidenceProfile 认证，不设全局固定值。
- Top-1 与 Top-2 同属近邻 SKU、分数接近且关键 Variant 属性缺失时，输出 `UNKNOWN_AMBIGUOUS`；优先补做局部视觉/OCR/Web Evidence，而不是扩大模型权重。
- 候选全部低于有效性门槛，且 Retrieval Health 正常、输入证据足够时，输出 `UNKNOWN_POSSIBLE_NIL`，进入 Web Evidence + Entity Discovery。
- 候选为空或异常但索引、路由、embedding 版本、搜索服务不健康时，输出 `UNKNOWN_SEARCH_FAILURE`，**不得**把基础设施失败误判成新品。
- `POSSIBLE_NIL` 只代表开放世界判断，不等于自动创建新 Canonical；新实体仍必须走 `PROPOSED → 治理/证据确认 → ACTIVE`。

---

## 8. Embedding 与视觉检索

### 8.1 Text

PoC：Qwen3-Embedding-0.6B；先测 512d，再比较 256/384d。文本 embedding 同时保留：

1. 结构化稳定模板；
2. 原始标题/描述自由文本。

Text Reranker：Qwen3-Reranker-0.6B，仅对 Top-N 候选使用。

### 8.2 Image

语义/跨模态基线：SigLIP2-B/16 384；细粒度视觉 challenger：DINOv3 ViT-B/16。DINOv3 使用自定义许可证，生产前必须 Legal/Compliance Gate；不能接受时保留 SigLIP2 + DINOv2/其他可商用方案。

每张图独立保存向量。聚合使用 `max_similarity / topk_mean / front/detail similarity / image_quality_weight`。完全相同或近重复图片也不能单独证明 `SAME_VARIANT`。

Qwen3-VL-Embedding-2B / Reranker-2B 作为多模态 challenger；只有显著提升 `Coverage @ 99.9% Precision` 且成本可接受时才进入困难路径。

### 8.3 局部 / Patch 级视觉验证

全局 SigLIP/DINO 向量负责**高召回候选发现**，不能承担近邻 SKU 的最终视觉区分。腕表盘面刻度/表圈/局部刻字、箱包五金/走线/图案局部等差异可能只占少量区域，全局向量会把它们淹没。

仅在以下情况触发局部视觉验证：

- 无可靠 Identifier，但图片质量较高。
- Top-1 / Top-2 属于同系列近邻 Variant 且接近歧义阈值。
- 全局视觉相似度高，但结构化属性不足以唯一确定。
- 有盘面、表圈、五金、铭牌、刻字、纹理等关键局部图或可稳定裁剪区域。

路径：

```text
Global Image ANN Top-K
→ Object / View / Region Selection
→ Patch / Local Descriptor Extraction
→ Shortlist Local Re-rank / Correspondence
→ local_visual_score / region_consistency / patch_match_coverage
→ Pair Decider + Candidate Ambiguity Guard
```

PoC 优先利用冻结视觉基础模型的 patch tokens / local descriptors 做 Top-10～20 shortlist rerank；DINOv3 受 Legal Gate 约束，无法采用时测试 DINOv2 或其他可商用局部特征方案。LOCORE 类 list-wise local-descriptor reranker 可作为 challenger。

局部视觉只是一组 `IMAGE_VISUAL` 证据，**不能单独升级为 Identity Anchor 或绕过 Hard Conflict**。其价值应由“近邻 SKU Hard Negative 上的 Precision/Coverage 提升 ÷ 额外 p95/GPU 成本”决定。

---

## 9. Pair Matching / Decider

```text
Candidate Pair
   ↓
Pre-Conflict Check
   ↓
Cheap Deterministic Features
   ↓
Structured / Text Matcher
   ↓
Multimodal Difficult-Pair Matcher（少量）
   ↓
Local Visual Verify（仅近邻歧义）
   ↓
Post-Conflict Check
   ↓
Calibration
   ↓
Evidence-Family Risk Policy
   ↓
Candidate-Set / NIL / Ambiguity Guard
   ↓
Decision
```

特征包括：Identifier、结构化属性、BM25/Text Embedding/Cross Encoder、图片全局与局部相似/OCR、来源可信度与独立来源数。

### 9.1 CERTAIN_MATCH Policy

至少满足：

```text
NO HARD_CONFLICT
AND CandidateSetState == KNOWN_CANDIDATE
AND
(
  Strong Identity Anchor
  OR
  Calibrated Matcher 达到高阈值
  + 至少两个非重复 Evidence Family 一致支持
  + 通过 Top-1 / Top-2 Ambiguity Guard
)
```

Strong Identity Anchor：高可信 full Reference / official Style Code / Manufacturer-grade MPN exact，或高可信 GTIN exact + Hard Attributes 无冲突。

没有 Strong Identity Anchor 时，`top1 >= tau_segment` 只是必要条件；还要满足 `top1 - top2 >= margin_segment`，或者第二候选存在可靠 Hard Conflict 等可证明唯一性的条件。`tau_segment` 与 `margin_segment` 必须基于品牌/品类/来源/EvidenceProfile 的独立认证集调节，不能使用 ANN raw score 或全局固定阈值。

“两个 Evidence Family”是初始风险策略，不是永恒业务规则；可由独立认证集调节。Student Matcher 不被替代，但风险策略必须防止相关特征共同失效。

### 9.2 冷启动到监督模型

0 labels：高置信 Identifier + Hard Constraint + ontology + pretrained embedding + 自动 Hard Negative。

约 10K 审核 pairs：Logistic/LightGBM + calibration + brand/category/source 阈值。

更多数据：compact Cross Encoder / domain contrastive encoder。

100K+ 且 hard-negative 足够：multimodal student、active learning、learned fusion。

Ditto 保留为离线 Entity Matching baseline。

LLM/VLM Difficult Pair 仅处理高价值 UNKNOWN、新款/Vintage、型号疑似错误、文本/图片冲突、品牌专有别名/材质、复杂目录证据；PoC expensive-path 先控制在 <5%。

---

## 10. Conflict Engine

独立安全墙，Matcher 前后都执行：

```text
HARD_CONFLICT
POSSIBLE_CONFLICT
NO_CONFLICT
```

典型 Hard/Possible：

- Rolex vs Omega → Hard。
- 36mm vs 41mm，双方官方高可信 → Hard。
- Mini vs Medium，双方官方尺寸 → Hard。
- 图片黑 vs 深蓝、无官方颜色 → Possible。
- 官方钢链 vs 疑似后换皮带 → Possible。
- validated GTIN 不同 → 默认 Possible；品牌规则确认 1:1 才 Hard。
- `productGroupID/item_group_id` 相同但 Variant 属性不同 → 明确不是 SAME_VARIANT，group relation 不得覆盖属性冲突。
- 第三方 merchant `sku` 与官方 Reference 不同 → 先视为不同命名空间，不直接形成 Hard Conflict；只有已建立映射规则后才比较。

长期维护 Hard Negative：同系列不同表径、同 Reference 家族不同盘面/材质、同包型不同尺寸/颜色/皮质/五金、图片极像但 Style Code 不同、同名跨年改款、同图多 SKU、Identifier 互相冲突。

任何模型高分都不能覆盖 Hard Conflict。

---

## 11. Confidence、Abstention 与风险认证

输出：

```text
CERTAIN_MATCH
LIKELY_MATCH
UNKNOWN
CERTAIN_NON_MATCH
```

只有 CERTAIN_MATCH 创建 Recognition；LIKELY/UNKNOWN 不自动挂接。

`UNKNOWN` 必须保存 reason code，至少包括：

```text
UNKNOWN_AMBIGUOUS             多个已知候选近似，无法唯一确定
UNKNOWN_POSSIBLE_NIL          当前 Catalog 可能不存在正确 Variant
UNKNOWN_INSUFFICIENT_EVIDENCE 证据覆盖不足
UNKNOWN_SEARCH_FAILURE        搜索/索引/路由不健康
UNKNOWN_CONFLICT              存在未解决 Possible Conflict
```

记录：`match_score / evidence_score / conflict_score / source_trust_score / confidence_level / decision / unknown_reason / policy_version / model_version`。

### 11.1 Calibration 与 Risk Certification 分开

- 调参集与最终认证集分离。
- 对 Auto-Match 使用单侧二项置信上界监控 False Merge Rate。
- 若目标错合并率 <0.1%，0 错误时 95% 单侧置信水平约需 2995 个独立审核样本把上界压到约 0.1%。
- 主要品类/来源/品牌必须 slice-level 认证，不能只看全局平均。
- `KNOWN_CANDIDATE / AMBIGUOUS_KNOWN / POSSIBLE_NIL` 必须分别评估；只在 closed-world 候选中校准出来的阈值不能直接用于新品流量。
- 分布漂移后旧阈值的认证结论自动失效，需重校准/重认证。

Conformal/Selective Risk Control 作为 challenger；time-shift、新品、来源变化会破坏经典 exchangeability 假设，因此首版仍以可解释阈值 + 单侧风险上界为主。

---

## 12. Entity Linking、Discovery 与 Graph Resolution

### 12.1 Linking

增量商品优先检索 `ACTIVE Canonical Variant`：

```text
Source
→ Hybrid Retrieval
→ Pair / Conflict / Calibration
→ CandidateSetAssessment
→ CERTAIN_MATCH or reason-coded UNKNOWN
→ Recognition（仅 CERTAIN_MATCH）
```

`UNKNOWN_AMBIGUOUS` 继续补证据或等待审核，不因为“最接近某个旧 SKU”就自动挂接；`UNKNOWN_SEARCH_FAILURE` 进入重试/降级，不进入 Discovery。

### 12.2 Discovery

只有 `UNKNOWN_POSSIBLE_NIL`、确实无可链接 Canonical 且输入证据达到最低质量要求的商品进入发现池：

```text
POSSIBLE_NIL / Unmatched Products
→ Web / Official Evidence（按需）
→ Strict Blocking
→ High-confidence Pair Edges
→ Constraint-aware Clustering
→ PROPOSED Canonical
```

PROPOSED 升 ACTIVE 必须通过 `catalog_activation_policy`：

- 首选 T0 品牌官方来源的 Manufacturer-grade Identifier + 关键 Variant 属性。
- 官方 Variant Graph 必须明确区分父组与具体 Variant，不能用 group id 代替 Variant id。
- 所有要求的 Hard Attributes 已知或有品牌规则证明不是该 Variant 的区分字段。
- 无 Hard/Possible Conflict，且网页结构化数据、可见页面、历史快照之间不存在未解释不一致。
- 只有品牌/source rule 已在离线认证集上证明可靠时才允许 `AUTO_ACTIVATE`；否则保持 PROPOSED 等待治理/审核。
- T1/T2/T3 单一来源不能独立把新实体升级 ACTIVE；需要真正独立来源交叉支持或治理策略确认。

### 12.3 传递冲突

禁止 `Connected Component = Canonical Variant`。

使用：

```text
Union-Find / Cluster Graph
+ Cannot-Link
+ Cluster Signature
+ Anchor Validation
```

合并 cluster 前检查 cannot-link、Hard Attributes、时间版本、来源互斥/同源 duplicate、anchor 兼容；高风险小 cluster 可做 complete-link/关键 pair 检查。有冲突立即写 cannot-link，阻断 A=B、B=C 推导 A=C 的错误桥接。

---

## 13. Web Evidence Agent 与 Official Web Catalog Ingestion

仅触发：内部无候选、`POSSIBLE_NIL`、新款、Vintage/下架、型号疑似错误、关键属性冲突、Identifier 需外部验证、Top-1/Top-2 近邻歧义且缺少官方属性。

检索优先级：

```text
品牌官网 → 官方目录/经销资料 → 专业数据库 → 高质量二手平台 → 通用搜索
```

Agent 只能输出 Evidence：URL、source tier/group、content hash、retrieved/effective date、extracted attrs、snippet、image refs。禁止 Agent 直接修改 Canonical、直接创建 CERTAIN_MATCH、把搜索摘要当事实、用多个复制网页伪造多源一致。

### 13.1 官方站优先使用结构化 Variant Graph

如果品牌/官方经销站提供 Schema.org / Google-compatible Product structured data，优先从结构化层解析，再用可见 DOM 和图片/OCR 交叉验证：

```text
Sitemap / Internal Links / Search Discovery
        ↓
Fetch Raw HTML + 必要时 Rendered DOM
        ↓
JSON-LD ProductGroup / Product Parse
        ↓
DOM / Embedded State / Identifier Parse
        ↓
External Variant Graph
        ↓
Identifier Semantics Validation
        ↓
Variant URL + Visible Attribute Consistency Check
        ↓
Versioned Evidence Snapshot
        ↓
PROPOSED / Canonical Fact Candidate
```

Google Product Variant 结构明确区分 `ProductGroup` 与具体 `Product` Variant，并使用 `productGroupID / hasVariant / isVariantOf / inProductGroupWithID / variesBy` 描述父子关系；每个 Variant 应有独立 ID（如 sku/gtin）和可直接定位该 Variant 的 URL。该结构非常适合做**关系和变体维度证据**，但不能把任意网页 markup 无条件当成 Canonical 真值。

### 13.2 Web 字段语义映射

| 外部字段 | 内部语义 | 默认可否做 Identity Anchor |
|---|---|---|
| `ProductGroup.productGroupID` / `item_group_id` | Product Group / Parent SKU | 否 |
| `hasVariant` / `isVariantOf` / `inProductGroupWithID` | Group↔Variant relation | 否 |
| `Product.mpn` | Manufacturer Part Number candidate | T0 + 校验后可以 |
| `Product.sku` | Merchant-specific SKU | 默认否；品牌/source policy 明确后才可 |
| `gtin/gtin8/gtin12/gtin13/gtin14` | Global Trade Item Identifier candidate | 校验 + 来源可靠后可以 |
| `variesBy` | 声明 Variant 区分维度 | 否；用于 Hard-Constraint policy |
| Merchant `variant_option` | 自定义 Variant 维度/取值 | 否；用于 Variant boundary evidence |
| `color/size/material/pattern/additionalProperty` | Variant 属性候选 | 达到字段证据等级后可 Hard |
| variant URL / canonical URL | 页面/Variant 路由证据 | 否 |

Google Merchant Center 自 2026 年提供 `variant_option` 来显式声明自定义 Variant-identifying dimensions。若品牌官方 feed/API/页面状态可合法获得等价数据，可用于盘面、五金、宽度、配置等非标准维度；但不能假设所有公开官网都暴露 Merchant feed，也不能把搜索引擎生成的理解反写成官方事实。

### 13.3 结构化网页不是天然真值

每条官方网页证据至少执行：

1. **Source Gate**：域名/子域、品牌归属、地区站点、历史质量在 allowlist 内。
2. **Field Semantics Gate**：按 `identifier_semantics_policy` 判断 `mpn/sku/group id` 的真实作用域。
3. **Variant Graph Gate**：group id、variant id、`hasVariant/isVariantOf` 关系无自相矛盾。
4. **Landing-page Consistency Gate**：Variant URL 打开后，页面可见的颜色/尺寸/材质/盘面/五金等与结构化值一致；若 URL 只指向父页且不能稳定预选 Variant，则降低证据等级。
5. **Collision Gate**：同一 manufacturer-grade identifier 不应在同一品牌/市场同时映射到互斥 Hard Attributes；出现碰撞直接进入冲突治理。
6. **Temporal Gate**：同 URL/同 external id 的字段变化生成新 snapshot/version，不覆盖历史事实。
7. **Freshness Gate**：下架页、重定向、区域站点差异、缓存页必须保留 fetched time 和有效期，不用“当前网页没有”反推历史 Variant 不存在。

页面 JSON-LD 与 DOM 冲突时，默认 `POSSIBLE_CONFLICT`；不能因为来源是官网就自动选择其中一边。

### 13.4 Canonical Activation Policy

允许自动把 Web Evidence 升级为 ACTIVE Canonical 的最小默认条件：

```text
T0 official source
AND source/field semantics policy 已认证
AND explicit manufacturer-grade variant identifier
AND required Hard Attributes 足够完整
AND Variant Graph / landing-page consistency 通过
AND no HARD/POSSIBLE_CONFLICT
AND temporal snapshot 可重放
```

不满足其中任一条件：只生成/更新 `PROPOSED`，不直接进入可自动挂接的 ACTIVE 主档。

以下信息**永远不能单独激活 Variant**：

- `productGroupID/item_group_id`。
- 第三方 merchant SKU。
- 单张图片或 Visual Search 命中。
- 搜索摘要/LLM 摘要。
- 只有营销名称、无具体 Variant ID 的官网页面。
- 多个互相转载的二手/聚合页面。

### 13.5 Web Evidence Snapshot

每次抓取至少保存：

```text
source_domain / source_group_id
page_canonical_url / requested_variant_url
http_status / redirect_chain
extraction_surface = JSON_LD / DOM / EMBEDDED_STATE / OCR
jsonld_entity_id / field_path
external_group_id / external_variant_id
raw_snapshot_ref
page_hash / structured_data_hash / image_hashes
retrieved_at
valid_from / valid_to (if known)
parser_version / ontology_version
```

网页统一作为可版本化 Evidence 输入；Canonical DB 只保存验证后的事实和指向这些 snapshot 的 provenance。

---

## 14. 数据库设计

关系数据库为事实源：PostgreSQL 16+ 或已有 MySQL；搜索/向量由 OpenSearch 承担。

核心表：

```text
brand
product_family
product_model
canonical_variant
canonical_variant_version
canonical_relation
source
source_group
source_product
source_product_image
product_identifier
identifier_semantics_policy
extracted_attribute
normalized_attribute
attribute_fact
external_evidence
external_variant_relation
candidate_pair
candidate_retrieval_evidence
candidate_set_assessment
match_evidence
conflict_record
cannot_link
cluster_membership
recognition_record
review_record
model_version
policy_version
ontology_version
source_trust_policy
catalog_activation_policy
retrieval_fusion_version
route_policy_version
```

`source_product` 额外保存：

```text
evidence_profile
identifier_quality
text_quality
image_quality
hard_attr_coverage
conflict_state
route_policy_version
```

`candidate_set_assessment` 至少保存：

```text
source_product_id
best_candidate_id
best_score_calibrated
second_candidate_id
second_score_calibrated
score_margin
near_tie_count
candidate_set_state
none_of_candidates_score
nil_reason
retrieval_health_snapshot
policy_version
```

`attribute_fact` 增加：

```text
synthetic_evidence boolean
synthetic_method
synthetic_parent_evidence_id
```

`external_evidence` 至少保存：

```text
source_group_id
page_canonical_url
requested_variant_url
extraction_surface
field_path
external_group_id
external_variant_id
raw_snapshot_ref
page_hash
structured_data_hash
retrieved_at
valid_from / valid_to
parser_version
```

`external_variant_relation` 保存外部 ProductGroup↔Variant 图、关系来源和 snapshot/version，禁止把外部 group id 直接写入 Canonical Variant identity 字段。

Recognition 保存 evidence/model/policy/ontology/fusion/route 版本和 snapshot，必须可重放。

关键索引：source+source_product_key unique、brand/category/reference、brand/category/style_code、identifier type+normalized value+market+namespace、normalized attributes、external source+external variant id、candidate pair unique、candidate set state、conflict、source group/content hash。

---

## 15. 离线 Batch 与每日增量

历史初始化：

```text
Raw Import
→ Normalize / Extract / QC
→ Barcode/OCR
→ EvidenceProfile / Route
→ Batch Text/Image Embedding（按需要）
→ Bulk OpenSearch Index
→ Link to Existing Canonical
→ CandidateSetAssessment
→ Recognition / reason-coded UNKNOWN
→ POSSIBLE_NIL Discovery
```

按 `category → brand → series/model bucket` 分区，禁止全库 Pair。

每日增量：

```text
New/Changed Source
→ Normalize / Identifier / EvidenceProfile
→ Route Policy
→ 所需 Embedding/检索通道
→ Search ACTIVE Canonical
→ Fusion
→ Pair / Conflict / Calibration
→ CandidateSetAssessment
→ Recognition or reason-coded UNKNOWN
→ POSSIBLE_NIL Discovery（必要时）
```

Official Web Catalog 增量使用 `sitemap / known product URLs / changed page hashes / external variant relations` 做差分抓取；同 URL 内容变化产生新 Evidence Snapshot。页面下架或 404 不直接删除 Canonical Variant，只更新外部证据有效性并进入时间版本治理。

关键优化：**没有图片就不计算 image embedding；强 Identifier 已足够确定时也不强制等待图片/VLM。** 局部视觉只运行在近邻歧义 shortlist；资源消耗由 Route Policy 控制，而不是所有商品走完整流水线。

模型/ontology/retrieval/route/parser/catalog-activation 升级均使用 version + shadow benchmark；向量和网页事实都不原地不可追溯覆盖。

---

## 16. 生产架构

```text
Source Connectors
      ↓
Raw / Object Storage
      ↓
Normalize + EvidenceProfile + Route Policy
      ↓
┌──────────┬────────────┬──────────────┐
↓          ↓            ↓              ↓
Exact    Text/BM25   Barcode/OCR     Image/VLM(按需)
└──────────┴────────────┴──────────────┘
      ↓
Catalog DB / Evidence Store
      ↓
OpenSearch Multi-channel Retrieval
      ↓
Exact Preserve + Versioned Fusion
      ↓
Reranker / Pair Matcher
      ↓
Local Visual Verify（近邻歧义才触发）
      ↓
Conflict Engine
      ↓
Calibration + Evidence-Family Risk Policy
      ↓
Candidate-set / NIL / Ambiguity Guard
      ↓
NON_MATCH / reason-coded UNKNOWN / CERTAIN_MATCH
                                      ↓
                               Graph Resolution
                                      ↓
                               Recognition Record
                                      ↓
                                Pending Review
                                      ↓
                                  Label Store
```

离线慢路径增加：

```text
Official/Web Discovery
→ Structured Product/Variant Parse
→ Snapshot + Variant Graph
→ Semantics / Consistency / Temporal Validation
→ PROPOSED / Canonical Fact Candidate
→ Re-index affected Canonical
```

组件：Catalog DB PostgreSQL/MySQL；OpenSearch 3.8；S3-compatible object storage；Kafka/Pulsar/企业 MQ；Python/Ray 或已有 Spark；Encoder serving 用 Triton/自建 PyTorch；VLM serving 用 vLLM/SGLang 按兼容性验证。

---

## 17. 容量、成本与性能

成本原则：Exact/规则最便宜；Embedding 入库批算；Reranker 只看小候选；局部视觉只看近邻 shortlist；VLM/Web 只走困难样本；不持久化全量 Pair；历史回填 GPU 与在线 GPU 分开。

### 17.1 10M 向量容量

假设：10M 商品、平均 3 张可用图片、10M text vectors + 30M image vectors、512d。

模型导出 FP16 数组约：

```text
40M × 512 × 2B = 40.96GB
```

但 OpenSearch `on_disk` 完整 `float` 向量仍需落盘，按 FP32：

```text
Text   10M × 512 × 4B = 20.48GB
Images 30M × 512 × 4B = 61.44GB
完整向量磁盘基线 ≈ 81.92GB / active vector set
```

该数字不含 Lucene segment、HNSW/量化索引、倒排、doc values、metadata、translog、merge 临时空间、副本、shadow index。

HNSW 量化内存参考公式：

```text
HNSWBytes ≈ 1.1 × (dimension × bits / 8 + 8 × m) × vector_count
```

512d、m=16、40M vectors 时：32x/1-bit 约 8.45GB，16x/2-bit 约 11.26GB（仅 HNSW 图量级，不是进程总 RAM）。

### 17.2 资源档位

PoC（1–2M 子集）：1×L4 24GB、1×8 vCPU/64GB OpenSearch、0.5TB SSD/block storage；关系库与对象存储尽量复用现有基础设施。

10M 生产压测起点：

```text
3 × 8 vCPU / 64GB OpenSearch data/search nodes
3 × 1TB usable storage, replica=1, 可容纳 active+shadow
2 × L4 24GB online GPU（要求 HA 时）
历史 backfill 临时扩 4–8 × L4/同级 GPU，可重试任务优先 Spot
```

按常见公有云当前量级，PoC 24×7 core envelope 约 $1k/月、10M 生产核心 compute+block storage 起点约 $3k/月；该数字仅用于立项量级，不含 DB、对象存储原图、快照、日志、网络、备份/跨区，并应在采购时按实际区域重新报价。

### 17.3 成本公式

先实测：

```text
TextEmbeddingItemsPerSec
ImageEmbeddingImagesPerSec
LocalVisualPairsPerSec
RerankPairsPerSec
VLMCasePerSec
OfficialWebPagesPerSec
```

再算历史 GPU-hours 与 BatchCost。在线：

```text
MonthlyOnlineCost = Search + GPU + DB + ObjectStorage + Observability + Network
CostPerRecognizedItem = MonthlyOnlineCost / confirmed_auto_recognitions
```

Web enrichment 单独统计 `pages fetched / structured parse rate / rendered-page rate / bytes stored / cost per PROPOSED or activated Variant`，避免慢路径成本被在线平均值掩盖。

`cost per recognized item` 必须与 `Coverage @ 99.9% Precision` 联合优化。

### 17.4 性能 Gate

普通增量 p95 <500ms（不含异步 Web/VLM）；expensive-model rate 初始 <5%；局部视觉触发率、额外 p95、GPU 开销单独监控；10M 实测 filtered ANN 的 Recall/QPS/p95/p99/build/recovery/RAM/disk/quantization loss；16x/32x 压缩必须分别跑域内 Recall；shadow index 期间验证磁盘 watermark。

Official Web 慢路径不进入普通增量 p95；单独监控抓取成功率、结构化 Variant parse coverage、snapshot storage、站点限流/失败率和 PROPOSED→ACTIVE 的 false promotion rate。

---

## 18. Benchmark 与测试集

首版 Gold Set 5K–10K pairs，主动提高 Hard Negative 比例。

分层：腕表/箱包、source/brand、seen/unseen SKU、current/vintage、with/no image、with/missing/dirty model、clean/conflicting、with/no identifier、**EvidenceProfile route**、known/NIL/ambiguous candidate state。

必须有 time-based holdout，测试后续出现的新 SKU。

### 18.1 Leakage-safe Split

最终风险认证**禁止简单 pair-level random split**。Pair 随机拆分只能作为开发期 sanity check；如果同一 Canonical、转载 feed、重复图片或高度重复文本跨 train/dev/test，模型可能记忆 SKU 或图像而得到虚高 Precision。

最终保留三套独立评测轨道：

```text
A. Seen / IID
   允许训练期见过该 Canonical，但具体 Source Offer 不重复

B. Strict Unseen Canonical
   canonical_variant_id 在 train / dev / test 之间互斥

C. Time-shift / New SKU
   按真实时间截断，测试截断后首次出现的 Variant / 新来源 / 新命名
```

Split Builder 在 pair 生成前先按以下 group 做隔离与去重审计：

```text
canonical_variant_id
source_group_id / syndication_group_id
image_hash_group_id
content_fingerprint
```

要求：

- 同一 Source Offer 只能位于一个 split。
- Strict Unseen 轨道中，同一 Canonical Variant 不得跨 split。
- 同一库存图/近重复图、同一 syndication/feed、近乎相同的复制文本不得跨 split 形成泄漏捷径。
- 调参集与最终风险认证集必须独立；认证集不能用于阈值搜索。
- 99.9% Precision 必须分别报告 Seen、Strict Unseen、Time-shift，不得只给混合平均值。

Corner Cases：

- 腕表：同系列不同尺寸、同型号不同盘面/材质、Reference 后缀/缺失/错误、同图不同文本、同文不同图、限量/改款、新旧款、后换表带、GTIN 与 Reference 冲突。
- 箱包：Mini/Small/Medium、不同皮质/颜色/五金/印花、季节限定、同名不同年份、官方命名变化、卖家俗称、局部图、无图、date code 被误当 Style Code、GTIN/UPC 与 Style Code 冲突。
- 缺模态：无图强型号、无型号强图、无图无型号但强属性、泛化标题、低质图片、文本图像冲突、合成属性误导。
- Open-world：真实新 SKU 但旧 Catalog 中存在极相似 SKU、Top-1/Top-2 near-tie、正确实体被人工从 candidate set 移除、搜索服务异常导致空候选。
- Official Web：父 `productGroupID` 被误当 Variant id、第三方 merchant `sku` 被误当官方 Reference、同一官网 JSON-LD 与可见 DOM 冲突、同 external id 跨地区站点属性不同、页面历史改版/下架导致 snapshot 变化、多个 Variant 共用同一库存图。
- 局部视觉：同系列外观几乎相同但盘面刻度/表圈/刻字/五金/走线/图案局部不同，包含局部图、遮挡、复杂背景和视角变化。

指标：

```text
Retrieval:
Recall@20/@50/@100 before/after fusion
Exact survival
Unique-positive contribution/channel
candidate_count/latency

Pair:
Precision / Recall / PR-AUC / Calibration Error
Coverage @ target precision
Evidence-family ablation
Top1-Top2 ambiguity distribution

Open-world:
NIL Precision / Recall
False Known-Link Rate on true new SKU
Forced-link rate
Near-tie auto-match violation
UNKNOWN reason distribution

Official Web:
Structured Product/Variant parse coverage
Variant group→variant relation accuracy
Manufacturer-ID semantic accuracy
JSON-LD↔DOM consistency rate
PROPOSED→ACTIVE false promotion rate
stale/redirect snapshot conflict rate
group-id-as-identity violation = 0
third-party-sku-as-official-id violation = 0

Visual:
Local rerank gain on variant hard negatives
Local visual trigger rate / p95 / GPU cost

Routing:
Coverage / False Merge / UNKNOWN / Cost by EvidenceProfile
Image-only auto-match rate
Synthetic-evidence promotion violation = 0

Leakage:
cross_split_variant_leakage = 0
cross_split_image_hash_leakage = 0
cross_split_syndication_leakage = 0

Cluster:
Purity / False Merge / Split / Constraint Violation / Bridge Merge

Production:
Coverage @ 99.9% Precision
False Merge per Million
UNKNOWN Rate
VLM/Web Rate
cost per recognized item
risk upper bound
```

---

## 19. MVP → Production

### Phase 0：数据审计与 Gold Set

交付字段完整度、Alias/Ontology、Hard Conflict Matrix、Identifier/条码可用率、来源独立性、Evidence Family、EvidenceProfile 分布、Leakage-safe Split Builder、包含真实 NIL/near-tie 的 5K–10K benchmark。

### Phase 1：确定性腕表 Linking

Reference/GTIN lookup、Exact Retrieval、Hard Conflict、Recognition/Review；先证明 99.9% Precision 闭环。

### Phase 2：Hybrid Text + Route Policy + Pair Matcher

BM25、Qwen3-Embedding-0.6B、RRF baseline、Qwen3-Reranker-0.6B、箱包主路径、Calibration、Candidate-set/NIL/Ambiguity Guard；明确无图/缺型号路由，禁止强制 Top-1。

### Phase 3：Image / Barcode / OCR / Multimodal

zxing-cpp、PP-OCRv6、PaddleOCR-VL document fallback、SigLIP2、DINOv3 challenger（Legal Gate）、局部/patch shortlist rerank、Qwen3.5-4B difficult attribute extraction、Qwen3-VL multimodal challenger。重点提升“弱文本/缺型号”和 near-tie SKU 覆盖，不允许视觉单独绕过 Identity/Conflict Policy。

### Phase 4：Open-world Discovery + Official Web Evidence

POSSIBLE_NIL、unmatched graph、constraint-aware clustering、source independence、temporal versioning、PROPOSED Canonical、Vintage/新款 Evidence Agent；实现 ProductGroup/Product structured parse、Identifier Semantics Policy、External Variant Graph、Snapshot、Catalog Activation Gate，明确区分真正新品、父商品组、merchant SKU 与 Retrieval Failure。

### Phase 5：Supervised Student + Continuous Learning

review labels、hard negative mining、NIL/near-tie mining、active learning、domain encoder/matcher、learned retrieval fusion、selective risk control、shadow/A-B/rollback。

---

## 20. 正式开启自动识别 Gate

全部满足才扩大 Auto-Match：

1. 主要品类/来源 `Precision@Auto-Match >= 99.9%`。
2. `Recall@100 after fusion/truncation >= 99.5%`。
3. Exact/官方标识符候选 survival rate = 100%。
4. 有足够独立认证样本支撑目标错误率上界。
5. 所有 Hard Conflict 回归用例 0 自动错合并。
6. Seen/Strict Unseen/Time-shift、Vintage、缺图、缺型号、脏型号、低质图、文本图像冲突分别达标。
7. **Image-only 路径不得产生未经额外身份/结构证据支持的 CERTAIN_MATCH。**
8. **synthetic_evidence 不得成为 Hard Constraint / Identity Anchor，违规数必须为 0。**
9. `AMBIGUOUS_KNOWN / POSSIBLE_NIL / RETRIEVAL_FAILURE` 不得绕过 Candidate-set Guard 自动 Recognition。
10. 指定新 SKU challenge set 中 forced Known-Link 与 near-tie 错误自动挂接均为 0；正式风险仍按独立认证样本计算置信上界。
11. 最终认证集 `cross_split_variant/image_hash/syndication leakage = 0`。
12. 每个品牌/来源/品类/EvidenceProfile/CandidateSetState 有阈值或 kill switch。
13. 模型、规则、ontology、search index、retrieval fusion、route policy、candidate-set policy 均可回滚。
14. Recognition 可重放完整证据与 CandidateSetAssessment。
15. Source/model/route/retrieval drift 超阈值时自动降级 UNKNOWN 或关闭 Auto-Match。
16. `productGroupID/item_group_id` 被提升为 Variant Identity Anchor 的违规数 = 0。
17. 未经认证的第三方 merchant `sku` 被提升为官方 Reference/Style Code 的违规数 = 0。
18. Official Web JSON-LD/DOM 存在未解决冲突时，PROPOSED→ACTIVE 自动升级数 = 0。
19. Official Web parser / identifier semantics / catalog activation policy 均版本化、可回放、可 kill-switch。

任何指标退化时，优先降低 Coverage，而不是放宽 Hard Constraint、歧义守门、NIL Guard 或 Web Catalog Activation Gate。

---

## 21. PoC 实验矩阵

| 问题 | Baseline | Challenger | 决策指标 |
|---|---|---|---|
| Missing-modality routing | 统一全链路 | EvidenceProfile 动态路由 | false merge / coverage / cost |
| Missing text/image | 不生成真值 | MLLM completion 仅 query expansion | Recall gain / hallucination / downstream precision |
| Text Embedding | Qwen3-Embedding-0.6B 512d | 256/384d / 4B | Recall@100 / RAM / latency |
| Retrieval Fusion | RRF + per-channel quota | normalized/learned fusion | Recall@K / exact survival / latency |
| Open-world / NIL | 强制从候选中选 Top-1 | CandidateSetAssessment + none-of-candidates guard | false-known-link / NIL P-R / coverage |
| Candidate ambiguity | 仅绝对 Top-1 阈值 | calibrated Top-1 + Top-1/Top-2 margin | near-tie false merge / coverage |
| Text Rerank | Qwen3-Reranker-0.6B | structured GBDT | Precision / latency |
| Image Semantic | SigLIP2-B/16 | 其他可商用 encoder | hard-case Recall@K |
| Fine-grained Vision | 全局 SigLIP/DINO | patch/local descriptor shortlist rerank | hard-negative precision / coverage / p95 / GPU |
| Multimodal Retrieval | text+image late fusion | Qwen3-VL-Embedding-2B | Coverage@99.9 / GPU |
| Difficult Pair | structured + text | Qwen3.5 / Qwen3-VL reranker | hard-negative precision |
| Evidence Policy | correlated-feature naive sum | Evidence-Family policy | false merge / coverage |
| OCR | PP-OCRv6 | PaddleOCR-VL | identifier extraction F1 / cost |
| Official Web Catalog | DOM/text-only extraction | Structured ProductGroup/Product + Variant Graph + Snapshot | variant relation accuracy / false promotion / coverage / cost |
| Identifier semantics | string-pattern guessing | source×field-path semantics policy | official-id precision / violation=0 |
| Vector Store | OpenSearch 3.8 on-disk | Milvus | Recall/QPS/RAM/disk/ops |
| Benchmark Split | pair-level random | Canonical/source/image-grouped + time-shift | leakage=0 / unseen risk |
| Cluster | constrained union + anchor | naive connected component | false merge / violations |
| Risk Policy | calibrated threshold | CRC/selective CRC | certified coverage / false merge |

只有内部 benchmark 证明能提高 `Coverage @ 99.9% Precision`、降低成本或改善 Unseen/Vintage/缺字段场景的复杂度才进入生产。

---

## 22. 主要技术依据

- Amazon Science — VARM：Variant Relationship 与普通 Entity Resolution 分开建模，并显式识别 variation attributes。  
  https://www.amazon.science/publications/learning-variant-product-relationship-and-variation-attributes-from-e-commerce-website-structures
- Walmart Global Tech：Catalog 属性抽取与 Quality Check 分阶段。  
  https://tech.walmart.com/content/walmart-global-tech/en_us/news/articles/how-walmart-uses-llms-to-manage-its-massive-product-catalogs.html
- Microsoft Research：Unstructured Offer → Structured Product 与 Product Synthesis。  
  https://www.microsoft.com/en-us/research/publication/matching-unstructured-product-offers-to-structured-product-descriptions/  
  https://www.microsoft.com/en-us/research/publication/synthesizing-products-for-online-catalogs/
- WDC Products：现实电商 Entity Matching、corner cases、unseen entities；评测应显式隔离 unseen entity，并防止 offer 跨 split 泄漏。  
  https://github.com/wbsg-uni-mannheim/wdcproducts  
  https://webdatacommons.org/largescaleproductcorpus/wdc-products/index.html
- GenDecider（NAACL 2024）：Entity Linking Rerank 必须识别“正确实体不在候选集合”的 none-of-the-candidates 场景。  
  https://aclanthology.org/2024.naacl-short.22/
- Learn to Not Link（ACL Findings 2023）：NIL Prediction 是不完整知识库下的独立问题，训练/评测需要显式 NIL 样本。  
  https://aclanthology.org/2023.findings-acl.690/
- RAED（EMNLP 2025）：静态实体库难以覆盖 Emerging Entities；外部检索证据适合用于新实体发现与事实 grounding，而不是强迫链接到既有实体。  
  https://aclanthology.org/2025.emnlp-main.1746/
- End-to-end multi-modal product matching in fashion e-commerce：多模态、分布变化、高 Precision 工业匹配。  
  https://arxiv.org/abs/2403.11593
- Benchmarking Multimodal Large Language Models for Missing Modality Completion in Product Catalogues / MMPCBench：MLLM 对缺模态商品补全能捕获高层语义，但细粒度词级/像素级对齐仍弱，因此生成模态不能作为身份真值。  
  https://arxiv.org/abs/2601.19750
- Leveraging Product Catalog Patterns for Multilingual E-commerce Product Attribute Prediction / PatternRAG：内部 Catalog pattern 可提高缺失属性预测，但预测结果仍需质量/事实验证。  
  https://aclanthology.org/2025.emnlp-industry.18/
- AMELI — Enhancing Multimodal Entity Linking with Fine-Grained Attributes：细粒度结构化属性对多模态 entity linking 至关重要。  
  https://aclanthology.org/2024.eacl-long.172/
- Multimodal Joint Attribute Prediction and Value Extraction for E-commerce Product：视觉信息应选择性参与属性预测，不应默认所有视觉信号等权。  
  https://aclanthology.org/2020.emnlp-main.166/
- LOCORE（CVPR 2025）：全局召回之后使用 local descriptors 对 shortlist 做细粒度 rerank，可在产品/时尚等实例检索任务提升排序质量。  
  https://openaccess.thecvf.com/content/CVPR2025/html/Xiao_LOCORE_Image_Re-ranking_with_Long-Context_Sequence_Modeling_CVPR_2025_paper.html
- ILIAS（CVPR 2025）：大规模 instance-level retrieval 评测显示 local descriptors 在 reranking、尤其复杂背景下仍是关键组件。  
  https://openaccess.thecvf.com/content/CVPR2025/html/Kordopatis-Zilos_ILIAS_Instance-Level_Image_retrieval_At_Scale_CVPR_2025_paper.html
- Efficient Fine-grained Image Retrieval with Vision Foundation Models for Industrial Objects（CVPRW 2026）：DINOv2/DINOv3/SigLIP2 的细粒度实例检索中，patch-token 表征对视角/背景变化下的区分具有重要价值。  
  https://openaccess.thecvf.com/content/CVPR2026W/FGVC13/html/Liu_Efficient_Fine-grained_Image_Retrieval_with_Vision_Foundation_Models_for_Industrial_CVPRW_2026_paper.html
- Google Research — RRF、Hybrid lexical+semantic retrieval、Nearly-independent feature families。  
  https://research.google/pubs/reciprocal-rank-fusion-outperforms-condorcet-and-individual-rank-learning-methods/  
  https://research.google/pubs/leveraging-semantic-and-lexical-matching-to-improve-the-recall-of-retrieval-systems-a-hybrid-approach/  
  https://research.google/pubs/on-using-nearly-independent-feature-families-for-high-precision-and-confidence/
- Google Search Central — Product Variant Structured Data：使用 ProductGroup / Product、productGroupID、hasVariant、isVariantOf、inProductGroupWithID、variesBy 描述父组与 Variant，并要求 Variant 可由独立 ID/URL 定位。  
  https://developers.google.com/search/docs/appearance/structured-data/product-variants
- Google Search Central — Merchant Listing Product：`mpn` 是 manufacturer part number，`sku` 是 merchant-specific identifier。  
  https://developers.google.com/search/docs/appearance/structured-data/merchant-listing
- Google Merchant Center — item_group_id / variant_option：item group 表示同一商品组，variant_option 可声明自定义 Variant-identifying dimensions。  
  https://support.google.com/merchants/answer/6324507  
  https://support.google.com/merchants/answer/17085214
- OpenSearch 3.8 / Hybrid Search / disk-based vector search。  
  https://github.com/opensearch-project/OpenSearch/releases/tag/3.8.0  
  https://docs.opensearch.org/latest/vector-search/ai-search/hybrid-search/index/  
  https://docs.opensearch.org/latest/vector-search/optimizing-storage/disk-based-vector-search/
- GS1 GTIN Management：GTIN 是 trade item 标识；不同产品 Variant 需要唯一 GTIN，但 GTIN 变化还可能由包装/品牌/其他供应链变化触发，因此 GTIN 冲突需结合品牌/市场语境解释。  
  https://www.gs1.org/1/gtinrules/en/  
  https://support.gs1.org/support/solutions/articles/43000734083-how-many-gs1-gtins-do-i-need-when-i-have-a-product-with-many-sizes-and-colours-
- zxing-cpp。  
  https://github.com/zxing-cpp/zxing-cpp
- PaddleOCR。  
  https://github.com/PaddlePaddle/PaddleOCR
- Qwen3 Embedding / Reranker。  
  https://github.com/QwenLM/Qwen3-Embedding
- Qwen3.5-4B。  
  https://huggingface.co/Qwen/Qwen3.5-4B
- Qwen3-VL Embedding / Reranker。  
  https://github.com/QwenLM/Qwen3-VL-Embedding
- SigLIP2 / DINOv3。  
  https://github.com/google-research/big_vision  
  https://github.com/facebookresearch/dinov3
- Conformal Risk Control / Selective Conformal Risk Control。  
  https://arxiv.org/abs/2208.02814  
  https://arxiv.org/abs/2512.12844

---

## 23. 最终生产原则

```text
官方知识 / 标准化 / 确定性标识
          ↓
EvidenceProfile / Route Policy
          ↓
只运行当前真实证据所需的检索与模型
          ↓
Exact Preserve + 可版本化 Fusion
          ↓
结构化 + 文本 + 图片多证据
          ↓
必要时局部 / Patch 视觉复核
          ↓
Hard Conflict 安全墙
          ↓
校准 + Evidence-Family 风险策略
          ↓
Candidate-set / NIL / Ambiguity Guard
          ↓
约束图实体解析
          ↓
仅 CERTAIN_MATCH 自动 Recognition
          ↓
审核反馈形成训练与认证数据
```

固定原则：

1. **Identity first，Similarity second。**
2. **Hard Conflict 优先于任何模型高分。**
3. **UNKNOWN 是合法且必要的生产输出，并必须带可操作 reason code。**
4. **缺模态不是负证据，也不能靠生成模态伪装成完整证据。**
5. **合成/补全内容只用于候选扩展或提示，不能形成 Hard Fact / Identity Anchor。**
6. **仅视觉相似不能自动判 SAME_VARIANT。**
7. **全局图片 Embedding 主要负责召回；局部/patch 视觉只在近邻歧义 shortlist 做验证，且不能单独成为身份锚点。**
8. **Best Candidate 不代表正确实体一定存在于当前 Catalog；必须显式允许 NIL / None-of-the-Candidates。**
9. **Top-1/Top-2 近似且缺关键身份属性时必须 Abstain，禁止强制 Top-1。**
10. **所有证据可追溯、可版本化、可重放。**
11. **多个 URL 不等于多个独立来源；同一原始证据的多个分数不等于多个独立证据。**
12. **多路召回 raw score 不直接相加；Exact 候选永不被 soft fusion 截断。**
13. **新 SKU / Vintage / time-shift / missing-modality / NIL 必须单独评测。**
14. **最终风险认证 split 必须隔离 Canonical、重复图片与 syndication/feed 泄漏。**
15. **模型、Fusion、Route Policy、Candidate-set Policy 只在 shadow benchmark 证明增益后上线。**
16. **自动匹配阈值由目标 False Merge Risk 决定，不使用固定 score。**
17. **任何复杂度只有在提高 Coverage@99.9% Precision 或降低总成本时才有价值。**
18. **Product Group ID 是关系标识，不是 Variant 身份标识。**
19. **Merchant SKU 与 Manufacturer Identifier 必须分命名空间管理，未经验证不得互换。**
20. **官网结构化数据优先作为可解析证据，但仍需字段语义、Variant Graph、页面一致性和时间版本验证后才能进入 Canonical Fact。**