# 奢侈品识别方案

## 1. 目标与边界

本系统的目标不是“找视觉上最像的商品”，而是把雷小安、奢当家、腕表之家、小迈步、爱回收及其他来源的商品记录，归一到**同一个官方商品变体（Official Variant）**。

第一阶段覆盖：

- 腕表
- 箱包
- 历史存量约 100 万–1000 万条
- 腕表已有部分品牌 / 系列 / 型号标准库
- 箱包主档不完整，需要同时支持 Entity Linking 与 Entity Discovery

核心风险偏好：

> **Precision >> Recall。宁可输出 UNKNOWN，也不能把不同官方变体错合并。**

只有尺寸、材质、颜色、盘面、五金、Reference / Style Code、版本等会定义官方变体的关键属性兼容时，才允许自动认定 `SAME_VARIANT`。

系统不做真假鉴定。成色、价格、库存、卖家、单件序列号、保卡状态等属于 Offer / 单件属性，不定义官方商品变体。

---

## 2. 核心业务实体与身份模型

### 2.1 实体层级

统一使用：

```text
Brand
  └── Product Family / Series
       └── Product Model
            └── Canonical Variant
                 └── Source Product / Offer
```

- **Brand**：品牌。
- **Product Family / Series**：系列/家族。
- **Product Model**：款式/型号层，可包含多个官方变体。
- **Canonical Variant**：系统识别的最小官方商品实体。
- **Source Product / Offer**：某个平台的一条原始商品记录。

关系类型至少包括：

```text
SAME_VARIANT
VARIANT_OF
SAME_MODEL
SAME_SERIES
VISUALLY_SIMILAR
UNKNOWN
```

只有 `SAME_VARIANT` 可生成自动 Recognition。

### 2.2 Canonical Variant 状态

```text
ACTIVE
PROPOSED
RETIRED
MERGED
```

新发现实体默认 `PROPOSED`。没有高可信主档锚点或多源独立证据时，不允许直接进入 `ACTIVE`。

### 2.3 官方标识符与条码

除品牌 Reference / Style Code 外，统一采集：

```text
gtin
ean
upc
barcode_symbology
barcode_payload
barcode_checksum_valid
market
identifier_source
identifier_confidence
```

GTIN/EAN/UPC 是高价值确定性证据，但不能机械等同于 Canonical Variant ID：

- 相同、校验通过且来源可信的 GTIN/EAN/UPC：Strong Positive + Exact Retrieval Key。
- 不同 GTIN：默认 `POSSIBLE_CONFLICT`；只有品牌/市场规则确认“一个官方变体只能对应一个贸易项目标识”时才升级 Hard。
- 不同颜色/尺寸通常应有不同贸易项目标识，因此 GTIN 对箱包等变体区分价值很高。
- Vintage 数据必须考虑旧编码、市场差异、包装变化和历史规则，不能用 GTIN 单独覆盖其他 Hard Attribute。
- 可解析的 GS1 Application Identifier 等细粒度信息作为额外结构化证据保存。

Canonical Variant 不依赖某一种标识符存在；最终身份由官方标识符、Hard Attributes 与证据策略共同确定。

---

## 3. 标准 Schema 与 Hard Constraint

### 3.1 属性事实模型

每个抽取/标准化属性必须保存：

```text
raw_value
normalized_value
canonical_value_id
extractor
extractor_version
source_type
source_id / source_url
evidence_span / image_bbox
confidence
validation_status
effective_from
effective_to
observed_at
content_hash
```

原则：

1. `observed_at` 与 `effective_from/effective_to` 分离，避免把抓取时间误当属性生效时间。
2. 新证据不直接覆盖旧事实；用 `supersedes` / version 关系表达变化。
3. Hard Conflict 只有在双方属性都达到相应可信度门槛时成立。
4. OCR/VLM 低置信冲突只触发 `POSSIBLE_CONFLICT → UNKNOWN`。

### 3.2 腕表 Schema

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| series / family | Blocking + Strong；品牌规则可 Hard |
| subseries / model | Strong |
| official_reference_full | Hard；最强身份字段之一 |
| reference_base | Retrieval / Strong，默认不能替代 full |
| gtin/ean/upc | Strong；条件 Hard |
| case_diameter_mm | Hard |
| case_material | Hard |
| dial_color | Hard |
| dial_type | 能定义独立变体时 Hard |
| bezel | 能定义独立变体时 Hard |
| bracelet_or_strap | 条件 Hard；需识别后换件 |
| caliber / movement | 代际/变体区分时 Hard，否则 Strong |
| functions | 变体区分时 Hard |
| edition / generation | Hard |
| serial_number | 单件属性，不得作为 Variant ID |
| condition / price | Offer 属性 |

Reference 必须保留：

```text
reference_raw
reference_normalized
reference_base
reference_full
reference_parts
brand_rule_version
```

默认以 `reference_full` 为准。只有品牌规则明确确认后缀不定义官方变体时，才允许 base reference 参与自动合并。

### 3.3 箱包 Schema

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| family / series | Blocking + Strong |
| model / style | Strong |
| official_style_code | Hard；最强身份字段之一 |
| gtin/ean/upc | Strong；条件 Hard |
| official_size | Hard |
| material | Hard |
| official_color | Hard |
| hardware | Hard |
| pattern / print | Hard |
| edition / collaboration | Hard |
| season / year / generation | 官方由其区分时 Hard |
| serial / date_code | 单件/生产证据，不直接作为 Variant ID |
| condition / price | Offer 属性 |

图片颜色不能直接成为 Hard Constraint。灯光、白平衡、皮革老化、修图会造成偏差；图片颜色只做 Soft Evidence。

### 3.4 两级标准化

颜色、材质、尺寸同时保留：

```text
generic_group
brand_specific_value
raw_value
```

例如：

```text
material_group = ROSE_GOLD
material_brand_value = ROLEX_EVEROSE_GOLD

color_group = BLACK
color_brand_value = CHANEL_NOIR
```

这样既能跨平台召回，又不会把品牌专有语义压平。

---

## 4. Evidence、来源独立性与时间版本

### 4.1 来源等级

```text
T0  品牌官网、官方目录、官方标识体系
T1  已验证内部标准库、授权/专业数据库
T2  高质量专业平台、可信二手平台
T3  普通电商、聚合站、搜索网页
```

来源可信度按“属性”而不是整站一刀切。例如专业腕表数据库对 Reference/机芯很强，但卖家文本对原配表带未必可靠。

### 4.2 独立来源不能按 URL 数量计算

维护：

```text
source_group_id
syndication_group_id
content_fingerprint
image_hash_group_id
upstream_hint
```

判定“多个独立来源共同支持”时，要求来源不属于同一已知 syndication group，且文本/图片指纹不显示明显复制关系。

相同图片、近重复图片、相同异常文案、相同字段错误都可作为“来源不独立”的线索。

### 4.3 Evidence Family：避免重复计票

模型层面必须进一步区分**来源独立性**和**特征独立性**。同一原始观测派生出的多个分数不能当成多个独立证据。

至少定义：

```text
IDENTIFIER        Reference / Style Code / GTIN
STRUCTURED_ATTR   size / material / color / hardware / movement
LEXICAL_TEXT      BM25 / token overlap
SEMANTIC_TEXT     text embedding / text reranker
IMAGE_VISUAL      image embedding / dense feature / perceptual hash
OCR_TEXT          image → OCR → identifier/attribute
EXTERNAL_WEB      外部网页证据
TEMPORAL          year / generation / effective period
```

相关性规则：

- BM25 与 Text Embedding 都来自同一标题时，不视为两个独立“投票”。
- 同一张图的 perceptual hash、SigLIP/DINO 特征和 VLM 观察属于同一视觉证据族，除非它们提取的是可独立验证的结构化事实。
- OCR 从图片读出的 Reference 若经校验，可升级为 `IDENTIFIER` 证据，但必须保留其 `image_id` 与 OCR provenance。
- 同一上游 feed 被多个网站转载时，不增加 `independent_source_count`。

Student Matcher 可以使用所有特征学习联合分布，但 `evidence_score`、Risk Policy 和 `CERTAIN_MATCH` 的解释层不得简单把相关特征求和当成独立支持。

### 4.4 时间版本

Canonical 属性支持有效期：

```text
attribute_value
valid_from
valid_to
evidence_ids
version
```

用于解决同名款跨年份改款、Reference/Style Code 规则变化、新旧机芯/五金/尺寸变化、官网下架后的 Vintage 回溯和同一营销名称对应多个世代。没有明确代际证据时，不跨年代自动合并。

---

## 5. 属性抽取与标准化流水线

```text
Raw Source Product
      ↓
Schema Mapping
      ↓
Deterministic Parsing / Dictionary
      ↓
Barcode Decode
      ↓
Text Attribute Extraction
      ↓
Scene OCR
      ↓
Document-like OCR/VLM（按需）
      ↓
General VLM Fallback（少量困难样本）
      ↓
Normalization / Ontology Mapping
      ↓
Attribute Validation / Quality Check
      ↓
Normalized Product Profile
```

### 5.1 优先级

1. 规则、品牌字典、官方主档。
2. 条码解析。
3. 文本抽取。
4. Scene OCR。
5. Document-like OCR/VLM。
6. General VLM。
7. Quality Check。

便宜、确定性的能力优先；大模型只补困难缺口。

### 5.2 条码解析

商品图、吊牌、包装、保卡存在 1D/2D Code 时，先用 **zxing-cpp** 等确定性 decoder 解析 EAN-13、UPC-A/E、Code128、Data Matrix、QR 等。

保存：

```text
image_id
symbology
payload
checksum_valid
decoder_version
bbox
quality
```

条码 payload 必须经过格式校验、GTIN check digit、品牌/市场映射，再进入 Exact Retrieval。

### 5.3 OCR

PoC 默认 Scene OCR：**PP-OCRv6**。

适用表背/吊牌文字、包装标签、保卡局部字段、Style Code / Reference、尺寸与材质文本。OCR 后必须分类为：

```text
reference / style_code
gtin/ean/upc
serial_number
date_code
size
material_text
other_text
```

不能“看到数字就当型号”。对于密集文档、保卡整页、历史目录页、复杂表格等，使用 **PaddleOCR-VL-1.6** 或 PP-Structure 类能力作为 document-like fallback，不让其替代普通商品图上的轻量 OCR。

### 5.4 General VLM

PoC 默认困难样本结构化抽取模型：**Qwen3.5-4B**。它用于多图 + 文本联合观察、属性候选和冲突解释，不承担全量 Pair Matching。

输出严格 JSON：

```json
{
  "attributes": [],
  "observations": [],
  "conflicts": [],
  "unknown_fields": [],
  "evidence_refs": []
}
```

VLM 结果必须经过 ontology、品牌规则、主档、OCR/文本交叉验证。VLM 无权直接写 ACTIVE Canonical 或直接输出 `CERTAIN_MATCH`。

---

## 6. Candidate Retrieval：Blocking + Hybrid Search

100 万–1000 万记录禁止 N²。所有识别先召回小候选集。

### 6.1 召回通道

```text
Exact Full Reference / Style Code
Exact GTIN/EAN/UPC
Brand + normalized model/reference
Brand + series/model + Hard Attributes
BM25
Text ANN
Image ANN
Image exact/perceptual hash
Historical Canonical Alias
Unmatched Entity-Discovery Neighbors
```

默认先按 `category`，通常再按 `brand_id` 过滤。

### 6.2 初始 Top-K

| 通道 | PoC 初始 K |
|---|---:|
| Exact Identifier | ≤20 |
| BM25 | 30 |
| Text ANN | 50 |
| Image ANN | 每图 30，总上限 60 |
| Attribute Combination | 30 |
| 合并去重候选池 | ≤150 |
| Cheap Rerank | ≈30 |
| Expensive Pair Matcher | 10–20 |

最终 K 由内部 `Recall@K` 决定。PoC Retrieval Gate：

```text
Recall@100 >= 99.5%
```

按品类、来源、Seen/Unseen SKU 分桶验证。召回不达标时先修 Blocking/Embedding，不能靠降低最终 Matcher 阈值补救。

### 6.3 多路召回融合与截断策略

不同召回通道的原始分数不可直接比较。生产规则：

1. **Exact/Identity 候选单独保留**：Reference / Style Code / GTIN 等高可信精确命中不得因为融合排名较低而被截掉。
2. **零标签/少标签阶段使用 RRF 作为 Soft Retrieval 基线**：对 BM25、Text ANN、Image ANN、Attribute Retrieval 的 rank 做 Reciprocal Rank Fusion。
3. **候选截断前保留通道配额**：防止单个高召回通道吞掉全部候选。
4. Exact 候选与 RRF 候选求并集后再 Cheap Rerank；RRF 只负责 candidate ordering，不具有身份判定权。
5. 有足够 judgment 后，对比 RRF、标准化 weighted score fusion、轻量 learned fusion。
6. 最终以域内 `Recall@K + latency + downstream Coverage@99.9% Precision` 决定融合方法。

每个候选记录：

```text
retrieval_channels
channel_rank
channel_raw_score
channel_normalized_score
fusion_method
fusion_score
fusion_version
```

必须分别统计：

```text
Recall@K before fusion
Recall@K after fusion/truncation
Exact-candidate survival rate
Unique-positive contribution by channel
candidate_count / latency
```

### 6.4 搜索引擎

生产基线：**OpenSearch 3.8**，统一承载 Exact/filter、BM25、filtered k-NN、hybrid retrieval、RRF / score normalization pipeline、on-disk vector、quantization/rescore、alias/versioned index。

OpenSearch `on_disk` 用于降低向量索引常驻内存，不等于把完整向量本身按 FP16 存储。当前 `on_disk` 模式使用 `float` 向量，并通过量化索引 + full-precision rescore 平衡内存与 Recall；因此容量预算必须区分“完整向量磁盘”和“量化 HNSW 常驻内存”。

Milvus 只在 1000 万级真实压测证明 OpenSearch 在 Recall/QPS/资源成本上不满足要求时再拆分。pgvector 适合小规模 PoC/内部工具，不作为唯一的千万级 Hybrid ANN 引擎。

### 6.5 索引组织

至少：

```text
canonical_variant_search_vN
source_product_search_vN
product_image_search_vN
identifier_lookup_vN
```

模型升级创建 shadow index，离线回填验证后切 alias；禁止在线混用不同 embedding version。

---

## 7. Embedding 与视觉检索

### 7.1 Text Embedding

PoC 默认：**Qwen3-Embedding-0.6B**。官方模型为 0.6B 参数、最大 1024 维并支持 MRL；PoC 先测试 512 维，再比较 256/384 维在域内 hard-case Recall@K 上是否有显著损失。

标准化模板示例：

```text
brand=Rolex | series=Datejust | reference=126334 |
size=41mm | case=steel | bezel=white_gold_fluted |
dial=blue | bracelet=oyster
```

同时保留原始标题/描述的自由文本向量，以覆盖脏数据、俗称和缺字段场景。

### 7.2 Text Reranker

PoC 默认：**Qwen3-Reranker-0.6B**，只对 Top-N 候选使用。

### 7.3 Image Embedding

默认语义/跨模态基线：**SigLIP2-B/16 384**。细粒度视觉 challenger：**DINOv3 ViT-B/16**。

DINOv3 适合验证表盘/表圈/材质局部细节、包型轮廓与细粒度纹理、dense patch feature / 局部匹配、弱文本情况下的视觉召回。DINOv3 使用自定义 License，生产使用必须经过 Legal/Compliance Review；若组织不能接受，则保留 SigLIP2 + DINOv2/其他可商用视觉模型作为替代。

每张图独立保存向量，不把多图简单平均。聚合特征：

```text
max_similarity
topk_mean
front_image_similarity
detail_image_similarity
image_quality_weight
```

图片预处理：

```text
quality check
→ exact hash / perceptual hash
→ duplicate removal
→ subject crop
→ image type classification
→ embedding
```

完全相同或近重复图片也不能单独证明 `SAME_VARIANT`；image hash 主要作为候选召回、来源复制识别和 Soft Evidence。

### 7.4 多模态 Retrieval/Rerank

**Qwen3-VL-Embedding-2B / Qwen3-VL-Reranker-2B** 保留为 challenger。只有内部难例集证明其显著提升 `Coverage @ 99.9% Precision` 且增量 GPU 成本可接受时，才进入在线困难路径。

---

## 8. Pair Matching / Decider

### 8.1 分层判定

```text
Candidate Pair
   ↓
Pre-Conflict Check
   ↓
Cheap Deterministic Features
   ↓
Structured / Text Matcher
   ↓
Multimodal Difficult-Pair Matcher（少量）
   ↓
Post-Conflict Check
   ↓
Calibration
   ↓
Evidence-Family Risk Policy
   ↓
Decision
```

### 8.2 Pair Features

结构化：

```text
brand_exact
reference_full_exact
reference_base_exact
style_code_exact
gtin_exact
gtin_conflict
series_exact
model_exact
size_exact
material_exact
color_exact
hardware_exact
edition_exact
attribute_missing_count
```

文本：`bm25_score`、`text_embedding_cosine`、`cross_encoder_score`、series/model/attribute similarity。

图片：`exact_image_hash_match`、`perceptual_hash_distance`、`image_max_cosine`、`image_topk_mean`、`dense_feature_match`、visual attribute / OCR identifier agreement。

来源：`source_trust`、`independent_source_count`、`official_source_hit`、`syndication_risk`、`attribute_provenance_quality`。

### 8.3 Evidence-Family Risk Policy

`CERTAIN_MATCH` 至少满足：

```text
NO HARD_CONFLICT
AND
(
  Strong Identity Anchor
  OR
  Calibrated Matcher 达到高阈值
  + 至少两个非重复 Evidence Family 提供一致支持
)
```

Strong Identity Anchor 包括高可信 full Reference exact、高可信 official Style Code exact、高可信 GTIN exact + 关键 Hard Attributes 无冲突。

“两个 Evidence Family”是 Precision-first 默认风险策略，不是永久业务真理。BM25 + Text Embedding（同标题）、Image Embedding + perceptual hash（同图片）、多个站点复制同一 feed、OCR Reference + 同图 VLM 观察都不得机械计成“两票”。

### 8.4 零标签 Matcher

零标签阶段不训练“万能二分类器”。

腕表高置信正例：

```text
brand exact
+ official_reference_full exact
+ 所有已知 Hard Attributes 无冲突
+ identifier/evidence source 达高可信等级
```

箱包高置信正例：

```text
brand exact
+ official_style_code exact
+ size/material/color/hardware 无可信冲突
+ 高可信主档或多个真正独立来源支持
```

### 8.5 有标签后的 Student

```text
~10K reviewed pairs:
Logistic / LightGBM + calibration

更多数据:
compact Cross Encoder / domain contrastive encoder

100K+ 且 hard-negative 足够:
multimodal student
```

Ditto 保留为离线 Entity Matching baseline，不作为默认生产依赖。

### 8.6 LLM/VLM Difficult Pair

仅处理高价值 UNKNOWN、新款/Vintage 无主档、型号疑似错误、文本与图片冲突、品牌专有材质/别名解释、复杂网页/目录证据整合。PoC 目标：昂贵模型路径先控制在总商品/候选的 5% 以下。

---

## 9. Conflict Engine

Conflict Engine 是独立安全墙，Matcher 前后都执行：

```text
HARD_CONFLICT
POSSIBLE_CONFLICT
NO_CONFLICT
```

典型规则：品牌不同、官方表径 36mm vs 41mm、官方包尺寸 Mini vs Medium → `HARD_CONFLICT`；仅图片颜色黑 vs 深蓝、疑似后换表带、不同 GTIN 但品牌 1:1 规则尚未确认 → `POSSIBLE_CONFLICT`。

Hard Negative 长期维护：同系列不同表径、同 Reference 家族不同盘面/材质、同包型不同尺寸/颜色/皮质/五金、图片极像但 Style Code 不同、同名跨年份改款、同图被多个 SKU 错误复用、GTIN/Style Code/Reference 相互冲突。

---

## 10. Confidence、Abstention 与统计风险控制

### 10.1 输出状态

```text
CERTAIN_MATCH
LIKELY_MATCH
UNKNOWN
CERTAIN_NON_MATCH
```

只有 `CERTAIN_MATCH` 自动建立 Recognition；`LIKELY_MATCH/UNKNOWN` 不自动挂接；`CERTAIN_NON_MATCH` 排除当前候选。

### 10.2 不使用单一 score 阈值

记录：

```text
match_score
evidence_score
conflict_score
source_trust_score
confidence_level
decision
policy_version
model_version
```

最终判定由 Hard Constraints + calibrated model score + evidence-family policy + source policy + abstention policy 共同决定。

### 10.3 Calibration 与 Risk Certification 分开

1. 阈值调参集、最终认证集分离。
2. 对 `CERTAIN_MATCH` 使用单侧二项置信上界监控 False Merge Rate。
3. 若目标错合并率 <0.1%，且认证样本 0 错误，95% 单侧置信水平下约需 2995 个独立审核样本才能把上界压到约 0.1%。
4. 品类、主要来源、重点品牌做 slice-level 最小样本约束。
5. 分布漂移时旧阈值统计结论失效，必须重校准/重认证。

### 10.4 Selective / Conformal Risk Control

PoC 可评估 Conformal Risk Control / Selective Conformal Risk Control，在目标风险上限下学习哪些样本允许回答、哪些必须 abstain。首版生产仍以可解释阈值 + 单侧错误率上界为主；CRC 作为提升 Coverage 的 challenger，并配合 time-based holdout、drift detection 和定期重认证。

---

## 11. Entity Linking 与 Entity Discovery

### 11.1 Entity Linking

增量商品首先只对 `ACTIVE Canonical Variant`：

```text
Source Product
→ Hybrid Retrieval
→ Pair Match
→ Conflict
→ CERTAIN_MATCH
→ Recognition
```

### 11.2 Entity Discovery

未挂接商品进入独立发现池：

```text
Unmatched Products
→ Strict Blocking
→ High-confidence Pair Edges
→ Constraint-aware Clustering
→ PROPOSED Canonical
```

PROPOSED 升 ACTIVE 至少满足：官方/高可信主档锚点，或至少两个真正独立来源共同支持关键变体属性；同时无 Hard Conflict，并通过主档治理/审核策略。复制同一 feed 的多个站点不能算多个独立证据。

---

## 12. Graph Resolution 与传递冲突

禁止 `Connected Component = Canonical Variant`。推荐：

```text
Union-Find / Cluster Graph
+ Cannot-Link
+ Cluster Signature
+ Anchor Validation
```

每个 cluster 保存 brand、official identifiers、size/material/color/hardware、edition/generation、valid_time_range、provenance groups、cannot_link ids、anchor_entity。

合并前必须：检查 Cannot-Link；检查全部高可信 Hard Attributes；检查时间版本兼容；检查来源互斥；新成员同时通过 cluster signature 与 anchor；必要时做 complete-link/关键 pair 检查。有冲突即写 cannot-link，阻断后续桥接。

---

## 13. 商品数据库与 Web Evidence Agent

Web Agent 仅触发：内部 Canonical 无候选、新款刚发布、Vintage/已下架、型号疑似错误、关键属性冲突、GTIN/Style Code/Reference 需要第三方验证。

检索优先级：

```text
品牌官网
→ 官方目录/官方经销资料
→ 专业数据库
→ 高质量二手平台
→ 通用搜索
```

Agent 只能输出 Evidence：

```text
candidate_url
source_tier
source_group_id
retrieved_at
content_hash
effective_date
extracted_attributes
evidence_snippets
image_refs
```

禁止 Agent 直接修改 Canonical Variant、直接创建 CERTAIN_MATCH、把搜索摘要当事实、用复制页面伪造“多源一致”。网页输入必须经过字段白名单、来源策略、时间版本和 Conflict Engine。

---

## 14. 冷启动与数据飞轮

### 0 Labels

- 官方 Reference / Style Code / GTIN 精确规则
- Hard Constraints
- ontology / alias
- Text/Image pretrained embedding
- 高可信规则生成 Positive
- 自动构造 Hard Negative

### ~10K 审核数据

- Logistic / LightGBM
- calibration
- brand/category/source-specific thresholds
- hard-negative mining
- active sampling
- retrieval fusion judgment set

### 100K+

- domain contrastive encoder
- compact cross encoder
- multimodal student
- active learning
- selective risk control
- learned retrieval fusion（仅在域内证明收益后）

LLM/VLM Teacher 只对困难样本生成结构化解释/候选标签；Teacher 标签必须与规则、主档、审核结果交叉验证后，才能进入训练集。

```text
Recognition
→ Review
→ Confirmed / Rejected
→ Label Store
→ Training / Calibration / Regression Set
```

---

## 15. 数据库设计

关系数据库为事实源，推荐 PostgreSQL；已有 MySQL 基础设施也可沿用。搜索/向量交给 OpenSearch。

核心表：

```text
brand
product_family
product_model
canonical_variant
canonical_variant_version
canonical_relation
source
source_group
source_product
source_product_image
product_identifier
extracted_attribute
normalized_attribute
attribute_fact
external_evidence
candidate_pair
candidate_retrieval_evidence
match_evidence
conflict_record
cannot_link
cluster_membership
recognition_record
review_record
model_version
policy_version
ontology_version
source_trust_policy
retrieval_fusion_version
```

`product_identifier` 保存 entity_type/entity_id、identifier_type/value、market、checksum_valid、source_tier、confidence、valid_from/to。

`canonical_variant` 保存 brand/category/family/model、canonical_reference_or_style、variant_signature/version、status、current_version_id。

`recognition_record` 保存 source_product_id、canonical_variant_id、decision、match/evidence/conflict score、policy/model/ontology/retrieval_fusion version、evidence_snapshot、review_status。

`candidate_retrieval_evidence` 保存 channel、rank、raw/normalized/fusion score、fusion method/version。

关键索引：

- `(source_id, source_product_key)` unique
- `(brand_id, category_id, normalized_reference)`
- `(brand_id, category_id, normalized_style_code)`
- `(identifier_type, normalized_value, market)`
- `(entity_type, entity_id, attribute_name)`
- `(source_product_id, review_status)`
- `(min_product_id, max_product_id)` unique
- `(left_id, right_id, conflict_type)`
- `(content_hash)`
- `(source_group_id)`

---

## 16. 离线 Batch 与增量架构

### 历史初始化

```text
Raw Import
→ Normalize / Extract / QC
→ Barcode/OCR
→ Batch Text/Image Embedding
→ Bulk OpenSearch Index
→ Link to Existing Canonical
→ Unmatched Discovery
→ Recognition / PROPOSED
```

按 `category → brand → series/model bucket` 分区运行，禁止全库 Pair。

### 每日增量

```text
New/Changed Source Product
→ Normalization
→ Identifier Lookup
→ Embedding
→ Search ACTIVE Canonical
→ Multi-channel Fusion
→ Pair/Conflict
→ Recognition or UNKNOWN
→ unmatched neighbor discovery（必要时）
```

### 模型升级

- 向量不原地覆盖。
- 建 `*_vN+1` shadow index。
- 回填固定 benchmark。
- 通过 Gate 后切 alias。
- Retrieval Fusion、Matcher、Policy、Ontology 分别版本化。
- Recognition 保存当时 evidence/model/policy/fusion snapshot，支持重放和回滚。

---

## 17. 生产架构

```text
Source Connectors
      ↓
Raw/Object Storage
      ↓
Normalize & Extract
 ┌────┼───────────────┐
 ↓    ↓               ↓
Text  Barcode/OCR   Image Prep
 └────┼───────────────┘
      ↓
Catalog DB / Evidence Store
      ↓
OpenSearch 3.8 Multi-channel Retrieval
      ↓
Exact Preserve + RRF/Score Fusion
      ↓
Candidate Pool
      ↓
Reranker / Pair Matcher
      ↓
Conflict Engine
      ↓
Calibration + Evidence-Family Risk Policy
      ↓
┌──────────────┬──────────────┐
↓              ↓              ↓
NON_MATCH    UNKNOWN      CERTAIN_MATCH
                              ↓
                       Graph Resolution
                              ↓
                      Recognition Record
                              ↓
                       Pending Review
                              ↓
                         Label Store
```

建议组件：

- Catalog DB：PostgreSQL 16+ / 现有 MySQL
- Hybrid Search：OpenSearch 3.8
- Object Storage：S3 compatible
- Queue：Kafka / Pulsar / 企业 MQ
- Batch：Python/Ray；已有 Spark 则用 Spark
- Encoder Serving：Triton / 自建 PyTorch 服务
- VLM Serving：vLLM / SGLang，按模型支持情况验证
- Cache：Redis（可选）

---

## 18. 容量、成本与性能

### 18.1 成本原则

1. Exact/规则最便宜，优先。
2. Embedding 入库时批算，查询复用。
3. 多路召回先 fusion，再让昂贵 Reranker 看小候选集。
4. Reranker 只看几十个候选。
5. VLM/Web Agent 只走少量困难样本。
6. 不持久化全量 Pair，只保存必要候选证据和审计信息。
7. 原图放对象存储，搜索引擎只留必要元数据/向量。
8. 历史回填 GPU 与日常在线 GPU 分开预算；一次性 Batch 优先临时扩容/Spot，而不是长期常驻。

### 18.2 千万级向量容量：正确的 OpenSearch 口径

容量假设：

```text
Source/Canonical records = 10M
平均可用图片 = 3 张/商品
Text vectors = 10M
Image vectors = 30M
Dimension = 512
Total vectors = 40M
```

如果只计算模型导出的 FP16 数组：

```text
40M × 512 × 2B = 40.96 GB
```

但**不能把这个数字直接当成 OpenSearch `on_disk` 的生产磁盘量**。OpenSearch 当前 `on_disk` 模式使用 `float` 向量，完整向量仍需要落盘，量化主要降低 ANN 图/常驻内存，并通过 rescore 读取完整向量恢复排序精度。因此按 512d FP32 计算：

```text
Text:   10M × 512 × 4B = 20.48 GB
Images: 30M × 512 × 4B = 61.44 GB
完整向量磁盘基线 ≈ 81.92 GB / active vector set
```

这个 81.92GB **仍不包含**：Lucene segment、HNSW/量化索引、倒排字段、doc values、metadata、translog、segment merge 临时空间、副本和 shadow index。

OpenSearch 文档给出的 HNSW 量化内存估算为：

```text
HNSWBytes ≈ 1.1 × (dimension × bits / 8 + 8 × m) × vector_count
```

以 `dimension=512`、`m=16`、40M vectors 为例：

```text
32x / 1-bit quantization  ≈ 8.45 GB HNSW memory（全索引合计）
16x / 2-bit quantization  ≈ 11.26 GB HNSW memory（全索引合计）
```

这些数字用于估算向量图，不代表 OpenSearch 进程总内存。生产还需要 JVM heap、文件缓存、BM25/过滤字段、查询并发和恢复空间。

### 18.3 第一版资源档位

#### PoC：1–2M 商品/子集

建议起步：

```text
1 × NVIDIA L4 24GB GPU
1 × 8 vCPU / 64GB RAM OpenSearch node
0.5TB balanced SSD/block storage
关系库与对象存储可复用现有基础设施
```

一张 24GB L4 足以承载 0.6B embedding/reranker 基线；Qwen3.5-4B 官方权重约 9.32GB，单卡可以做低并发困难样本 PoC，但实际可用 batch/concurrency 仍由 KV cache、图像尺寸和 serving framework 决定，必须实测。

以 Google Cloud `us-central1` 2026-08-14 公开按需价格仅作**可重算参考单价**：

```text
g2-standard-8（1×L4, 8 vCPU, 32GiB）≈ $0.8536/h
n4-highmem-8（8 vCPU, 64GiB）       ≈ $0.4761/h
Balanced Persistent Disk             ≈ $0.10/GB-month
```

若 24×7 常驻且使用 500GB 磁盘，以上三项合计约：

```text
≈ $1,021 / month
```

PoC 实际应在空闲时关停 GPU/临时 Batch 节点，因此通常低于 24×7 上限。

#### 10M 生产起步规格

建议作为**压测起点而非最终购买规格**：

```text
OpenSearch:
3 × 8 vCPU / 64GB RAM search-data nodes
3 × 1TB usable block storage
replica = 1
保留 active + shadow index 共存空间

GPU online:
2 × NVIDIA L4 24GB nodes（HA）
Embedding / Reranker 常驻
VLM difficult-path 限流

Batch backfill:
临时扩 4–8 × L4 或同级 GPU
任务可重试时优先 Spot/Preemptible
```

按同一 GCP Iowa 公开按需单价做参考：

```text
3 × n4-highmem-8            ≈ $1,043/month
2 × g2-standard-16（L4）    ≈ $1,675/month
3TB Balanced PD             ≈ $300/month
Core compute + block storage ≈ $3,018/month
```

该数值**不含**关系数据库、对象存储原图、快照、日志监控、网络流量、备份以及额外跨 AZ/Region 成本。立项预算应在 core envelope 之外预留至少 30%–50% 运维/数据面余量，并以实际云区域或自建 GPU/TCO 重新报价。

如果业务 QPS 很低且 VLM 完全异步，可以从 1 个在线 GPU worker 起步；如果要求在线 HA，则保持 2 个。OpenSearch 是否需要升到 3×128GB、增加节点或更高性能磁盘，必须由 10M filtered ANN + hybrid query 压测决定，而不是仅由向量字节数决定。

### 18.4 历史回填成本不写死吞吐

公开模型参数只能判断“能否装入某类 GPU”，不能可靠推出本业务图片尺寸、文本长度、batch size 下的 items/s。因此历史回填必须先测四个吞吐指标：

```text
TextEmbeddingItemsPerSec
ImageEmbeddingImagesPerSec
RerankPairsPerSec
VLMCasePerSec
```

然后计算：

```text
TextGPUHours = 10M / TextEmbeddingItemsPerSec / 3600
ImageGPUHours = 30M / ImageEmbeddingImagesPerSec / 3600
RerankGPUHours = PairCount / RerankPairsPerSec / 3600
VLMGPUHours = DifficultCaseCount / VLMCasePerSec / 3600

BatchCost = Σ(GPUHours × provider_gpu_hour_price)
```

历史 Batch 是一次性成本，不能用“每月常驻 GPU 数 × 月价”替代。

### 18.5 在线成本公式

```text
DailyEmbeddingGPU = changed_items × avg_images × encoder_cost
PairInference = daily_items × avg_candidates_after_rerank
VLMInference = daily_items × difficult_case_rate
MonthlyOnlineCost = SearchCluster + GPUWorkers + DB + ObjectStorage + Observability + Network
CostPerRecognizedItem = MonthlyOnlineCost / confirmed_auto_recognitions
```

`cost per recognized item` 必须和 `Coverage @ 99.9% Precision` 一起优化，不能单独追求模型调用便宜。

### 18.6 性能 Gate

- 普通增量路径 p95 <500ms，不含异步 Web/VLM。
- expensive-model rate 初始 <5%。
- 1000 万规模实测 filtered ANN：Recall@K、QPS、p95/p99、build/rebuild time、RAM/disk、recovery time、quantization loss。
- Fusion pipeline 单独记录 p50/p95/p99 和 candidate amplification。
- OpenSearch 16x/32x 压缩均必须跑域内 Recall；不能因默认 32x 更省内存就直接上线。
- Shadow index 构建期间必须验证 active + shadow 双份容量不会触发磁盘 watermark/恢复风险。

---

## 19. Benchmark 与测试集

### 19.1 Gold Set

首版 5K–10K pair，主动提高 Hard Negative 比例。

分层：

```text
watch / bag
source
brand
seen SKU / unseen SKU
current / vintage
with image / no image
with model / missing model / dirty model
clean / conflicting
with identifier / no identifier
```

必须有 time-based holdout：规则/训练只看较早数据，测试用后续出现的新 SKU。

### 19.2 Corner Cases

腕表：同系列不同尺寸、同型号不同盘面/材质、Reference 标点/后缀变化、Reference 部分缺失、型号写错、同图不同文本、同文不同图、限量版、改款/新旧款、后换表带、GTIN 与 Reference 冲突。

箱包：Mini/Small/Medium、不同皮质/颜色/五金/印花、季节限定、同名不同年份、官方命名变化、卖家俗称、局部图、无图、date code 被误当 Style Code、GTIN/UPC 与 Style Code 冲突。

### 19.3 指标

Retrieval：

```text
Recall@20/@50/@100 before fusion
Recall@20/@50/@100 after fusion/truncation
MRR
candidate_count
latency
Exact-candidate survival rate
Unique-positive contribution by channel
Per-channel overlap / redundancy
```

Pair：Precision、Recall、PR-AUC、Calibration Error、Coverage @ target precision、Evidence-family ablation。

Cluster：Cluster Purity、False Merge Rate、Split Rate、Constraint Violation、Bridge Merge Count。

Production：Coverage @ 99.9% Precision、False Merge per Million、UNKNOWN Rate、VLM/Web Rate、cost per recognized item、risk upper bound。

---

## 20. MVP → Production

### Phase 0：数据审计与 Gold Set

- 字段完整度
- Alias/Ontology
- Hard Conflict Matrix
- Identifier 可用率/条码可读率
- 来源可信度与 syndication group
- Evidence Family 定义
- 5K–10K benchmark
- PoC 各模型真实吞吐、GPU-hours、OpenSearch 容量/延迟基线

### Phase 1：确定性腕表 Linking

- Reference 解析
- GTIN/EAN/UPC lookup（有则用）
- Exact Retrieval
- Hard Conflict
- Recognition/Review

先证明 99.9% Precision 闭环。

### Phase 2：Hybrid Text + Retrieval Fusion + Pair Matcher

- BM25
- Qwen3-Embedding-0.6B
- RRF baseline
- Qwen3-Reranker-0.6B
- 箱包主路径
- Calibration

### Phase 3：Image / Barcode / OCR / Multimodal

- zxing-cpp
- PP-OCRv6
- PaddleOCR-VL-1.6 document fallback
- SigLIP2
- DINOv3 challenger（Legal Gate）
- Qwen3.5-4B difficult attribute extraction
- Qwen3-VL multimodal retrieval challenger

### Phase 4：Entity Discovery + Web Evidence

- unmatched graph
- constraint-aware clustering
- source-independence
- temporal versioning
- PROPOSED Canonical
- Vintage / 新款 Evidence Agent

### Phase 5：Supervised Student + Continuous Learning

- review labels
- hard negative mining
- active learning
- domain encoder/matcher
- tuned/learned retrieval fusion
- selective risk control
- shadow/A-B/rollback

---

## 21. 正式开启自动识别的 Gate

所有条件同时满足：

1. 主要品类/来源 `Precision@Auto-Match >= 99.9%`。
2. `Recall@100 after fusion/truncation >= 99.5%`。
3. Exact/官方标识符候选在融合截断中的 survival rate = 100%。
4. 有足够独立认证样本支撑目标错误率上界。
5. 所有 Hard Conflict 回归用例 0 自动错合并。
6. Seen/Unseen、Vintage、缺图、缺型号、脏型号分别达标。
7. 新品/time-shift holdout 达标。
8. 每个品牌/来源/品类有阈值或 kill switch。
9. 模型/规则/ontology/search index/retrieval fusion 均可回滚。
10. Recognition 可重放完整证据。
11. Source drift / model drift 超阈值时自动降级为 UNKNOWN 或关闭 Auto-Match。
12. 10M 容量压测验证 active + replica + shadow 的磁盘/内存和恢复余量。
13. 历史回填和月度在线成本已用真实吞吐回填预算模型，且 `cost per recognized item` 可接受。

任何指标退化时，优先降低 Coverage，而不是放宽 Hard Constraint。

---

## 22. PoC 实验矩阵

| 问题 | Baseline | Challenger | 决策指标 |
|---|---|---|---|
| Text Embedding | Qwen3-Embedding-0.6B 512d | 256/384d / 4B | Recall@100 / RAM / latency / GPU-hours |
| Retrieval Fusion | RRF + per-channel quota | weighted / learned fusion | Recall@K / exact survival / latency |
| Text Rerank | Qwen3-Reranker-0.6B | structured GBDT | Precision / latency / pairs/s |
| Image Semantic | SigLIP2-B/16 | 其他可商用 encoder | hard-case Recall@K / images/s |
| Fine-grained Vision | SigLIP2 | DINOv3 ViT-B/16 | variant hard-negative Recall/Precision |
| Multimodal Retrieval | text+image late fusion | Qwen3-VL-Embedding-2B | Coverage@99.9 / GPU cost |
| Difficult Pair | structured + text | Qwen3.5 / Qwen3-VL reranker | hard-negative precision / cases/s |
| Evidence Policy | correlated-feature naive sum | Evidence-Family aware | false merge / coverage |
| OCR | PP-OCRv6 | PaddleOCR-VL-1.6 | identifier extraction F1 / cost |
| Vector Store | OpenSearch 3.8 on-disk | Milvus | Recall/QPS/RAM/disk/ops |
| Compression | OpenSearch 16x | 32x | Recall loss / RAM / p95 / disk IO |
| Cluster | constrained union + anchor | naive connected component | false merge / violations |
| Risk Policy | calibrated threshold | CRC/selective CRC | certified coverage / false merge |

复杂模型只有在内部 benchmark 证明提高 `Coverage @ 99.9% Precision`、降低成本或改善 Unseen/Vintage/缺字段场景时才进入生产。

---

## 23. 主要技术依据

- Amazon Science / VARM：Variant Relationship 与普通 Entity Resolution 分开建模，并显式识别 variation attributes。  
  https://www.amazon.science/publications/learning-variant-product-relationship-and-variation-attributes-from-e-commerce-website-structures

- Walmart Global Tech：Catalog 属性抽取与 Quality Check 分阶段。  
  https://tech.walmart.com/content/walmart-global-tech/en_us/news/articles/how-walmart-uses-llms-to-manage-its-massive-product-catalogs.html

- Microsoft Research：Unstructured Offer → Structured Product 与 Product Synthesis。  
  https://www.microsoft.com/en-us/research/publication/matching-unstructured-product-offers-to-structured-product-descriptions/  
  https://www.microsoft.com/en-us/research/publication/synthesizing-products-for-online-catalogs/

- WDC Products：现实电商 Entity Matching、corner cases、unseen entities。  
  https://github.com/wbsg-uni-mannheim/wdcproducts

- End-to-end multi-modal product matching in fashion e-commerce：生产商品匹配中的多模态、分布变化与高 Precision。  
  https://arxiv.org/abs/2403.11593

- Google Research — Reciprocal Rank Fusion。  
  https://research.google/pubs/reciprocal-rank-fusion-outperforms-condorcet-and-individual-rank-learning-methods/

- Google Research — Hybrid lexical + semantic retrieval。  
  https://research.google/pubs/leveraging-semantic-and-lexical-matching-to-improve-the-recall-of-retrieval-systems-a-hybrid-approach/

- Google Research — Nearly-independent feature families for high precision。  
  https://research.google/pubs/on-using-nearly-independent-feature-families-for-high-precision-and-confidence/

- OpenSearch Hybrid Search / RRF。  
  https://docs.opensearch.org/latest/vector-search/ai-search/hybrid-search/index/  
  https://opensearch.org/blog/introducing-reciprocal-rank-fusion-hybrid-search/

- OpenSearch 3.8 / disk-based vector search / memory-optimized vectors：`on_disk`、16x/32x 压缩、full-precision rescore 与 HNSW 内存估算。  
  https://github.com/opensearch-project/OpenSearch/releases/tag/3.8.0  
  https://docs.opensearch.org/latest/vector-search/optimizing-storage/disk-based-vector-search/  
  https://docs.opensearch.org/latest/mappings/supported-field-types/knn-memory-optimized/

- GS1 GTIN Management。  
  https://www.gs1.org/1/gtinrules/en/

- zxing-cpp。  
  https://github.com/zxing-cpp/zxing-cpp

- PaddleOCR：PP-OCRv6 与 PaddleOCR-VL-1.6。  
  https://github.com/PaddlePaddle/PaddleOCR

- Qwen3 Embedding / Reranker：0.6B embedding 支持 32–1024 自定义维度/MRL。  
  https://huggingface.co/Qwen/Qwen3-Embedding-0.6B  
  https://github.com/QwenLM/Qwen3-Embedding

- Qwen3.5-4B：Apache-2.0 原生多模态模型，官方权重约 9.32GB。  
  https://huggingface.co/Qwen/Qwen3.5-4B

- Qwen3-VL Embedding / Reranker。  
  https://github.com/QwenLM/Qwen3-VL-Embedding

- Google SigLIP2。  
  https://github.com/google-research/big_vision

- Meta DINOv3：细粒度视觉 challenger，使用自定义 License。  
  https://github.com/facebookresearch/dinov3  
  https://github.com/facebookresearch/dinov3/blob/main/LICENSE.md

- Conformal Risk Control / Selective Conformal Risk Control。  
  https://arxiv.org/abs/2208.02814  
  https://arxiv.org/abs/2512.12844

- Google Cloud Compute 公开价格：仅作为当前资源档位的可重算参考，不绑定生产云厂商。  
  https://cloud.google.com/products/compute/pricing/accelerator-optimized  
  https://cloud.google.com/products/compute/pricing/general-purpose  
  https://cloud.google.com/products/block-storage

---

## 24. 最终生产原则

```text
官方知识 / 标准化 / 确定性标识
          ↓
高召回 Multi-channel Retrieval
          ↓
Exact Preserve + 可版本化 Fusion
          ↓
结构化 + 文本 + 图片多证据
          ↓
Hard Conflict 安全墙
          ↓
校准 + Evidence-Family + Abstention 风险策略
          ↓
约束图实体解析
          ↓
仅 CERTAIN_MATCH 自动 Recognition
          ↓
审核反馈形成训练与认证数据
```

核心原则：

1. **Identity first，Similarity second**：相似不等于相同。
2. **Hard Conflict 优先于任何模型高分**。
3. **UNKNOWN 是合法且必要的生产输出**。
4. **所有证据必须可追溯、可版本化、可重放**。
5. **多个 URL 不等于多个独立来源**。
6. **同一原始证据派生出的多个分数不等于多个独立证据**。
7. **多路召回 raw score 不直接相加；融合方法必须版本化并做 Recall Gate**。
8. **Exact/官方标识符候选不得在 soft fusion 中被截断**。
9. **新 SKU / Vintage / time-shift 必须单独评测**。
10. **模型升级必须在 shadow benchmark 证明增益**。
11. **自动匹配阈值由目标 False Merge Risk 决定，而不是固定 score**。
12. **能用规则、标识符、条码、OCR 解决的问题，不交给大模型**。
13. **成本必须拆成一次性历史回填与月度在线 TCO；所有 GPU 预算必须由真实 items/s 回填**。
14. **任何复杂度只在提高 Coverage@99.9% Precision 或降低总成本时才有价值**。
