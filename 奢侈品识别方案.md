# 奢侈品识别方案

## 1. 目标与边界

系统目标不是“找最像的商品”，而是把雷小安、奢当家、腕表之家、小迈步、爱回收等来源的记录归一到**同一个官方商品变体（Official Variant）**。

第一阶段：腕表 + 箱包；历史数据约 100 万–1000 万条。腕表已有部分品牌/系列/型号标准库，箱包主档不完整，需要同时支持 Entity Linking 与 Entity Discovery。

核心原则：

> **Precision >> Recall。宁可输出 UNKNOWN，也不能把不同官方变体错合并。**

只有会定义官方变体的关键属性兼容时，才允许自动认定 `SAME_VARIANT`。系统不做真假鉴定；成色、价格、库存、卖家、单件序列号、保卡状态等属于 Offer / 单件属性，不定义官方商品变体。

---

## 2. 核心实体与身份模型

```text
Brand
  └── Product Family / Series
       └── Product Model
            └── Canonical Variant
                 └── Source Product / Offer
```

关系至少包括：

```text
SAME_VARIANT
VARIANT_OF
SAME_MODEL
SAME_SERIES
VISUALLY_SIMILAR
UNKNOWN
```

只有 `SAME_VARIANT` 可生成自动 Recognition。

Canonical Variant 状态：

```text
ACTIVE      已验证，可自动挂接
PROPOSED    新发现，等待证据/治理
RETIRED     历史实体
MERGED      已治理合并
```

新发现实体默认 `PROPOSED`，不得因为若干来源名称或图片相似就直接进入 `ACTIVE`。

### 2.1 官方标识符与字段语义

统一采集：

```text
reference_full / style_code / mpn
gtin / ean / upc
merchant_sku
product_group_id / item_group_id
variant_dimension_keys / variant_dimension_values
barcode_symbology / payload / checksum
market
identifier_namespace / identifier_owner / identifier_scope
identifier_source / source_field_path / confidence
```

标识符必须按“谁分配、在哪个来源字段出现、作用于 Product Group 还是 Variant、在哪个市场有效”解释，不能只看字符串是否像型号。

规则：

- 高可信完整 Reference / Style Code 精确一致：最强 Identity Anchor 之一。
- 官方品牌来源中的 `mpn`，经品牌规则和格式验证后可作为 Manufacturer-grade Identifier。
- 第三方商家 `sku` 默认属于 merchant namespace，不能直接升级为官方 Reference / Style Code。
- `productGroupID / item_group_id / inProductGroupWithID` 是父商品组/变体组关系标识，不是 Variant Identity Anchor。
- `ProductGroup.hasVariant / Product.isVariantOf / variesBy / variant_option` 是高价值变体边界证据，用于解释哪些属性产生独立 Variant。
- 高可信、校验通过的 GTIN/EAN/UPC：Strong Positive + Exact Retrieval Key。
- 不同 GTIN 默认 `POSSIBLE_CONFLICT`；只有品牌/市场规则证明 GTIN 与本系统 Variant 1:1 时才升级 Hard。
- Vintage 必须考虑旧编码、市场差异、包装变化和历史规则。

维护 `identifier_semantics_policy`，最小粒度：

```text
brand × market × source_group × field_path × identifier_type
```

策略声明 `MANUFACTURER_ID / MERCHANT_ID / GROUP_ID / INSTANCE_ID / UNKNOWN`，以及是否允许 Exact Retrieval、Strong Positive、Hard Conflict、Auto Activation。

---

## 3. 标准 Schema 与 Hard Constraint

每个属性事实保存：

```text
raw_value
normalized_value
canonical_value_id
extractor / extractor_version
source_type / source_id / source_url
evidence_span / image_bbox
confidence / validation_status
effective_from / effective_to
observed_at / content_hash
```

`observed_at` 与有效期分离；新证据不覆盖旧事实，而用 version / supersedes 表达变化。只有双方属性都达到对应可信度门槛时才形成 Hard Conflict；低置信 OCR/VLM 冲突只能触发 `POSSIBLE_CONFLICT → UNKNOWN`。

### 3.1 腕表

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| series / family | Blocking + Strong；品牌规则可 Hard |
| subseries / model | Strong |
| official_reference_full | Hard；最强身份字段之一 |
| reference_base | Retrieval / Strong，默认不能代替 full |
| gtin/ean/upc | Strong；条件 Hard |
| case_diameter_mm | Hard |
| case_material | Hard |
| dial_color | Hard |
| dial_type | 定义独立变体时 Hard |
| bezel | 定义独立变体时 Hard |
| bracelet_or_strap | 条件 Hard；需识别后换件 |
| caliber / movement | 代际/变体区分时 Hard，否则 Strong |
| functions | 变体区分时 Hard |
| edition / generation | Hard |
| serial_number | 单件属性 |
| condition / price | Offer 属性 |

Reference 保存 `raw / normalized / base / full / parts / brand_rule_version`。默认以 `reference_full` 为准；只有品牌规则明确证明后缀不定义 Variant 时才允许 base reference 参与自动合并。

### 3.2 箱包

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| family / series | Blocking + Strong |
| model / style | Strong |
| official_style_code | Hard；最强身份字段之一 |
| gtin/ean/upc | Strong；条件 Hard |
| official_size | Hard |
| material | Hard |
| official_color | Hard |
| hardware | Hard |
| pattern / print | Hard |
| edition / collaboration | Hard |
| season / year / generation | 官方由其区分时 Hard |
| serial / date_code | 单件/生产证据 |
| condition / price | Offer 属性 |

图片颜色不直接成为 Hard Constraint；灯光、白平衡、老化、修图会造成偏差，视觉颜色只做 Soft Evidence。

### 3.3 两级标准化

```text
material_group = ROSE_GOLD
material_brand_value = ROLEX_EVEROSE_GOLD

color_group = BLACK
color_brand_value = CHANEL_NOIR
```

通用层用于跨平台召回，品牌精确层用于 Variant 判定，避免把品牌专有语义压平。

---

## 4. Evidence、来源独立性与时间版本

来源等级：

```text
T0  品牌官网、官方目录、官方标识体系
T1  已验证内部标准库、授权/专业数据库
T2  高质量专业平台、可信二手平台
T3  普通电商、聚合站、搜索网页
```

可信度按“来源 × 属性”定义，而不是整站一刀切。

### 4.1 来源独立性

维护：

```text
source_group_id
syndication_group_id
content_fingerprint
image_hash_group_id
upstream_hint
```

多个 URL 不等于多个独立来源。复制同一 feed、相同图片、相同异常文案、相同字段错误不能增加 `independent_source_count`。

### 4.2 Evidence Family：避免重复计票

```text
IDENTIFIER        Reference / Style Code / MPN / GTIN
STRUCTURED_ATTR   size / material / color / hardware / movement
VARIANT_GRAPH     ProductGroup / isVariantOf / variesBy / variant_option
LEXICAL_TEXT      BM25 / token overlap
SEMANTIC_TEXT     text embedding / text reranker
IMAGE_VISUAL      image embedding / dense feature / perceptual hash
OCR_TEXT          image → OCR → identifier/attribute
EXTERNAL_WEB      外部网页证据
TEMPORAL          year / generation / effective period
```

同一原始观测派生出的多个分数不是多个独立证据：

- 同一标题的 BM25 + Text Embedding 不算两票。
- 同一张图的 pHash + SigLIP/DINO + VLM 观察不算三票。
- 同一网页中的 JSON-LD、DOM、搜索摘要默认属于同一 evidence origin。
- OCR 读出的 Reference 可升级为 Identifier，但必须保留 image/OCR provenance。
- Student Matcher 可使用所有特征，但 `CERTAIN_MATCH` 风险解释层不得把相关特征机械求和。

### 4.3 时间版本

Canonical 属性保存 `valid_from / valid_to / version / evidence_ids`，用于同名跨年份改款、Reference 规则变化、新旧机芯/五金/尺寸变化、Vintage 回溯等。没有明确代际证据时，不跨年代自动合并。

### 4.4 反馈闭环隔离与 Canonical Write Barrier

`Recognition`、Matcher 分数、Discovery cluster、模型生成属性和伪标签都是**派生 Artifact**，不是新的独立现实观测。系统必须阻止“模型先猜一次 → 把猜测写回主档/别名 → 下一次 Exact 命中更确信 → 再拿结果训练自己”的自增强闭环。

统一标记 artifact origin：

```text
OBSERVED        原始平台/官方/审核可追溯观测
DERIVED         模型、规则、聚类、Recognition 推导结果
REVIEWED        人工审核确认的关系/标签
GOVERNED        通过主档治理流程晋升的 Canonical Fact / Alias / Identifier
```

硬规则：

1. `recognition_record` 只表示 `Source Product → Canonical Variant` 的派生关系，**不得被重新计作独立 Evidence Family、独立来源或 Canonical Fact**。
2. 自动 `CERTAIN_MATCH` 可以生成 `PSEUDO_POSITIVE` 供离线实验，但默认进入隔离的 pseudo-label pool；未经审核/治理不得升级为 `REVIEWED_GOLD`，不得直接修改 Canonical 属性、官方 alias、identifier semantics 或 ontology。
3. 从自动 Recognition 学到的标题 alias / Reference alias / 图像近邻只能进入 `CANDIDATE_ALIAS`；只有 T0/T1 证据或 Governance Review 通过 `fact_promotion_policy` 后，才能进入 Exact Retrieval / Strong Identity 路径。
4. `PROPOSED Canonical`、Web Fact Candidate、cluster 推导属性同样受 Write Barrier；不能因为多个派生对象互相支持就伪装成多个独立来源。
5. 每个派生 artifact 必须记录 `upstream_artifact_ids / derivation_type / producer_version / promotion_status / derivation_depth`。任何人工纠错、来源撤回、parser bug、模型 bug 都能沿 lineage 正向找到所有受影响 alias、伪标签、训练数据集、索引与模型版本。
6. 上游 artifact 被判定错误时，先标记 `TAINTED`，立即从高等级 Evidence/Exact Retrieval/新训练集构建中排除；历史已训练模型不假定自动“消毒”，而是标记 impacted，进入重训/重认证队列。
7. 训练集构建必须区分 `REVIEWED_GOLD / GOVERNED_FACT / PSEUDO_LABEL`；同一模型/策略生成的 pseudo label 不得在没有独立标签信号时作为该版本的“真值”评测自身。
8. 所有 Canonical mutation、Alias promotion、label promotion 都写不可变 promotion ledger，保存 before/after、upstream evidence、review/policy/version；回滚不是删除历史，而是生成 superseding event 并重建受影响索引。

这条 Write Barrier 的目标不是禁止弱监督，而是把弱监督限制在**可追溯、可降权、可撤销、不能自证**的位置。

---

## 5. 属性抽取与标准化

```text
Raw Source Product
      ↓
Schema Mapping
      ↓
Deterministic Parsing / Dictionary
      ↓
Barcode Decode
      ↓
Text Attribute Extraction
      ↓
Scene OCR
      ↓
Document-like OCR/VLM（按需）
      ↓
General VLM Fallback（少量困难样本）
      ↓
Normalization / Ontology Mapping
      ↓
Attribute Validation / Quality Check
      ↓
Normalized Product Profile
```

优先级：规则/主档 → 条码 → 文本 → Scene OCR → 文档 OCR/VLM → General VLM → Quality Check。能用确定性方法解决的问题不交给大模型。

### 5.1 条码与 OCR

条码先用 zxing-cpp 等确定性 decoder 解析 EAN/UPC/Code128/Data Matrix/QR，经过 check digit、格式、品牌/市场校验后进入 Exact Retrieval。

PoC Scene OCR 基线：PP-OCRv6。OCR 后必须分类：

```text
reference / style_code
gtin/ean/upc
serial_number
date_code
size
material_text
other_text
```

密集文档、保卡整页、历史目录页、复杂表格可用 PaddleOCR-VL-1.6 / PP-Structure 类能力作为 document-like fallback。

### 5.2 General VLM

PoC 困难样本结构化抽取基线：Qwen3.5-4B。职责是产生**可验证候选证据**，不是最终身份判断。VLM 结果必须经过 ontology、品牌规则、主档、OCR/文本交叉验证；无权直接写 ACTIVE Canonical 或直接输出 `CERTAIN_MATCH`。

---

## 6. 缺模态与证据可用性路由

在线识别使用固定 `EvidenceProfile + Route Policy`，不先用生成模型“补齐”缺失模态再假装输入完整。

```text
identifier_quality = NONE / WEAK / STRONG
text_quality       = NONE / WEAK / STRONG
image_quality      = NONE / WEAK / STRONG
hard_attr_coverage = 0..1
conflict_state     = NONE / POSSIBLE / HARD
catalog_mode       = LINK / DISCOVERY
synthetic_evidence = true / false
```

### 6.1 路由矩阵

| 场景 | 主要召回 | 图片角色 | 自动判定上限 |
|---|---|---|---|
| 强 Reference/Style Code/GTIN + Hard Attr 无冲突 | Exact + 属性过滤 | 可选验证 | 可进入 CERTAIN_MATCH |
| 文本强、无图片 | Exact/BM25/Text ANN + 属性 | 不需要 | 可进入 CERTAIN_MATCH，但必须满足 Risk Policy |
| 图片强、文本弱、无可靠型号 | Image ANN + OCR + 少量 VLM | 候选发现 | 默认不能仅凭视觉 CERTAIN_MATCH |
| 文本强 + 图片强 | 多路召回 + Rerank | 补充/冲突检查 | 可进入 CERTAIN_MATCH |
| 型号疑似错误 | raw/normalized identifier 双路 + 非 identifier 召回 | 发现文本错误 | 冲突时 UNKNOWN/NON_MATCH |
| 文本与图片冲突 | 分开保留证据 | 冲突定位 | POSSIBLE_CONFLICT → UNKNOWN |
| 无图无可靠型号但属性较完整 | BM25/Text ANN/Attribute | 无 | 仅认证后允许自动，否则 UNKNOWN |
| 只有泛化标题、缺 Hard Attr | BM25/Text ANN | 无 | UNKNOWN |
| Vintage / 新款无 Canonical | Neighbor + Web/Official Evidence | 候选发现 | PROPOSED，不直接 ACTIVE |
| 同图多 SKU/来源复用 | pHash/dup | 识别复制 | 图像相同不构成 SAME_VARIANT |

### 6.2 合成信息硬规则

- 生成的缺失图片/描述不能当真实商品事实。
- `synthetic_evidence=true` 不能形成 Hard Constraint、Identity Anchor、官方颜色/尺寸/材质事实。
- 图→文本只用于 query expansion、候选召回、属性候选。
- 文本→图像默认不进入识别链路。
- Catalog Pattern/RAG 可预测缺失属性候选，但需真实来源/审核确认后才能升级 Canonical Fact。
- 缺模态本身不是负证据。

### 6.3 低质量图片

在 Image ANN 前独立评分：blur、resolution、subject_area_ratio、occlusion、background_complexity、watermark、crop_type、color_cast。低质量图降低视觉权重和候选配额；局部图不能与完整正面图等权。

---

## 7. Candidate Retrieval：Blocking + Hybrid Search

100 万–1000 万记录禁止 N²。

召回通道：

```text
Exact Full Reference / Style Code / Manufacturer MPN
Exact GTIN/EAN/UPC
Brand + normalized model/reference
Brand + series/model + Hard Attributes
BM25
Text ANN
Image ANN
Image exact/perceptual hash
Historical Canonical Alias
Unmatched Entity-Discovery Neighbors
```

默认按 `category`，通常再按 `brand_id` 过滤。

PoC 初始 K：Exact ≤20、BM25 30、Text ANN 50、Image ANN 每图 30/总≤60、Attribute 30；union 后≤150，Cheap Rerank≈30，Expensive Matcher 10–20。最终由域内 Recall@K 调参。

Retrieval Gate：

```text
Recall@100 after fusion/truncation >= 99.5%
Exact-candidate survival rate = 100%
```

### 7.1 多路融合

- 不同通道 raw score 不直接相加。
- Exact/Identity 候选单独保留，不能被 soft fusion 截断。
- 零/少标签阶段用 RRF 作为 Soft Retrieval 基线。
- 截断前保留每通道 minimum quota。
- Exact ∪ RRF 后再 Cheap Rerank。
- 有 judgment 后比较 RRF、归一化 weighted fusion、learned fusion。
- 融合必须版本化并监控 Recall before/after fusion、Unique-positive contribution、候选数与延迟。

### 7.2 搜索引擎

生产基线：OpenSearch 3.8，统一承载 Exact/filter、BM25、filtered k-NN、hybrid retrieval、RRF/score normalization、on-disk vector、quantization/rescore、alias/versioned index。

Milvus 只在 10M 真实压测证明 OpenSearch 的 Recall/QPS/资源成本不满足时再拆分。pgvector 适合小规模 PoC/内部工具。

索引至少：

```text
canonical_variant_search_vN
source_product_search_vN
product_image_search_vN
identifier_lookup_vN
```

每个 Canonical/Identifier 搜索文档必须携带 `canonical_version / catalog_event_version / canonical_status / search_generation`。OpenSearch 是 near-real-time 搜索投影，写入/删除在 refresh 前不保证对 Search 可见；因此在线链路不得把“已写 DB”自动等同于“搜索已同步”。增量索引使用数据库单调版本作为 external version，拒绝乱序旧事件覆盖新状态；breaking schema/embedding 变更使用 shadow index + 原子 alias 切换。

### 7.3 Open-world Candidate Guard：NIL / None-of-the-Candidates

系统不能因为检索总能返回 Top-1，就默认 Top-1 一定属于正确实体。

候选集状态：

```text
KNOWN_CANDIDATE
AMBIGUOUS_KNOWN
POSSIBLE_NIL
RETRIEVAL_FAILURE
```

每次 Linking 保存 `best/second calibrated score、score_margin、near_tie_count、candidate_set_state、nil_reason、retrieval_health_snapshot`。

策略：

- 禁止强制 Top-1。
- 无 Strong Identity Anchor 时，Top-1 必须达到 segment 阈值且通过 Top-1/Top-2 歧义守门。
- 近邻 SKU near-tie 且缺 Variant 属性时输出 `UNKNOWN_AMBIGUOUS`。
- 候选都低且 Retrieval 健康时输出 `UNKNOWN_POSSIBLE_NIL`，进入 Web Evidence + Discovery。
- 搜索/索引/embedding 版本不健康时输出 `UNKNOWN_SEARCH_FAILURE`，不能误判成新品。
- Catalog/Search publication watermark 不一致、候选来自未发布 generation、或关键撤销事件尚未在搜索投影可见时输出 `UNKNOWN_PUBLICATION_INCONSISTENT`；同样不得进入 Discovery。
- `POSSIBLE_NIL` 不等于自动创建新 Canonical。

---

## 8. Embedding 与视觉检索

### 8.1 Text

PoC：Qwen3-Embedding-0.6B；先测 512d，再比较 256/384d。文本 embedding 同时保留结构化模板和原始标题/描述自由文本。

Text Reranker：Qwen3-Reranker-0.6B，仅对 Top-N 使用。

### 8.2 Image

语义/跨模态基线：SigLIP2-B/16 384；细粒度视觉 challenger：DINOv3 ViT-B/16。DINOv3 使用自定义许可证，生产前必须 Legal/Compliance Gate。

每张图独立保存向量；多图聚合使用 max_similarity、topk_mean、front/detail similarity、image_quality_weight。完全相同或近重复图片也不能单独证明 `SAME_VARIANT`。

Qwen3-VL-Embedding-2B / Reranker-2B 作为多模态 challenger；只有显著提升 `Coverage @ 99.9% Precision` 且成本可接受时才进入困难路径。

### 8.3 局部 / Patch 级视觉验证

全局向量负责高召回候选，不承担近邻 SKU 最终视觉区分。只在无可靠 Identifier、Top-1/Top-2 near-tie、结构化属性不足且有可用关键局部区域时触发：

```text
Global Image ANN Top-K
→ Region Selection
→ Patch / Local Descriptor
→ Shortlist Local Re-rank / Correspondence
→ local_visual_score / patch_match_coverage
→ Pair Decider + Ambiguity Guard
```

PoC 使用冻结基础模型 patch tokens / local descriptors 做 Top-10～20 rerank；LOCORE 类 list-wise local reranker 作为 challenger。局部视觉仍属于 `IMAGE_VISUAL`，不能成为 Identity Anchor。

---

## 9. Pair Matching / Decider

```text
Candidate Pair
   ↓
Pre-Conflict Check
   ↓
Cheap Deterministic Features
   ↓
Structured / Text Matcher
   ↓
Multimodal Difficult-Pair Matcher（少量）
   ↓
Local Visual Verify（近邻歧义）
   ↓
Post-Conflict Check
   ↓
Calibration
   ↓
Evidence-Family Risk Policy
   ↓
Candidate-set / NIL / Ambiguity Guard
   ↓
Decision
```

### 9.1 CERTAIN_MATCH Policy

至少满足：

```text
NO HARD_CONFLICT
AND CandidateSetState == KNOWN_CANDIDATE
AND
(
  Strong Identity Anchor
  OR
  Calibrated Matcher 达到高阈值
  + 至少两个非重复 Evidence Family 一致支持
  + 通过 Top-1 / Top-2 Ambiguity Guard
)
```

Strong Identity Anchor：高可信 full Reference / official Style Code / Manufacturer-grade MPN exact，或高可信 GTIN exact + Hard Attributes 无冲突。

没有 Anchor 时，`top1 >= tau_segment` 只是必要条件；还要满足 `top1 - top2 >= margin_segment` 或第二候选存在可靠 Hard Conflict。阈值必须按品牌/品类/来源/EvidenceProfile 在独立认证集上确定。

### 9.2 冷启动到监督模型

- 0 labels：Identifier + Hard Constraint + ontology + pretrained embedding + Hard Negative。
- ~10K reviewed pairs：Logistic/LightGBM + calibration + segment threshold。
- 更多数据：compact Cross Encoder / domain contrastive encoder。
- 100K+：multimodal student、active learning、learned fusion。

自动规则、Teacher、历史 `CERTAIN_MATCH` 产生的数据统一按 `PSEUDO_LABEL` 管理；可以用于弱监督实验或降权训练，但不能与 `REVIEWED_GOLD` 混成无来源的同一标签池。若伪标签生成器与 Student/Matcher 同源，还必须做 held-out reviewed set 验证，防止 confirmation bias 被训练指标掩盖。

LLM/VLM Difficult Pair 只处理高价值 UNKNOWN、新款/Vintage、型号疑似错误、文本/图片冲突、复杂目录证据；expensive path 初始 <5%。

---

## 10. Conflict Engine

Matcher 前后都执行：

```text
HARD_CONFLICT
POSSIBLE_CONFLICT
NO_CONFLICT
```

典型：

- 品牌不同 → Hard。
- 36mm vs 41mm 且双方官方高可信 → Hard。
- Mini vs Medium 且双方官方尺寸 → Hard。
- 图片黑 vs 深蓝、无官方颜色 → Possible。
- 官方钢链 vs 疑似后换皮带 → Possible。
- validated GTIN 不同 → 默认 Possible，规则确认 1:1 才 Hard。
- Product Group 相同但 Variant 属性不同 → 不是 SAME_VARIANT。
- Merchant SKU 与官方 Reference 不同 → 不同 namespace，先不形成 Hard。

任何模型高分都不能覆盖 Hard Conflict。

---

## 11. Confidence、Abstention 与风险认证

输出：

```text
CERTAIN_MATCH
LIKELY_MATCH
UNKNOWN
CERTAIN_NON_MATCH
```

只有 CERTAIN_MATCH 创建 Recognition。

`UNKNOWN` reason code 至少：

```text
UNKNOWN_AMBIGUOUS
UNKNOWN_POSSIBLE_NIL
UNKNOWN_INSUFFICIENT_EVIDENCE
UNKNOWN_SEARCH_FAILURE
UNKNOWN_DEPENDENCY_FAILURE
UNKNOWN_PUBLICATION_INCONSISTENT
UNKNOWN_CONFLICT
```

记录 `match/evidence/conflict/source_trust score、confidence_level、decision、unknown_reason、policy/model version、route_execution_signature、publication_id`。

### 11.1 Calibration 与 Risk Certification 分开

- 调参集与最终认证集分离。
- 对 Auto-Match 使用单侧二项置信上界。
- 若目标错合并率 <0.1%，0 错误时 95% 单侧置信水平约需 2995 个独立审核样本把上界压到约 0.1%。
- 主要品类/来源/品牌做 slice-level 认证。
- KNOWN / AMBIGUOUS / NIL 分别评估；closed-world 阈值不能直接用于新品流量。
- 分布漂移后重新校准/认证。

CRC / Selective CRC 作为 challenger，不作为首版安全依赖。

### 11.2 训练审核、风险审计、治理审核分流

```text
TRAINING_REVIEW
  Active Learning / Hard Negative / LIKELY / UNKNOWN / rare slices

CERTIFICATION_AUDIT
  从真实最终 Decision 流按预定义 probability sampling 抽样

GOVERNANCE_REVIEW
  PROPOSED Canonical / Web 冲突 / Identifier semantics / 主档治理
```

规则：

1. 风险认证单位是最终 `recognition_record / Source Product Decision`，不是 candidate pair。
2. Certification 首版采用简单随机或预先固定的分层概率抽样，每条记录 `inclusion_probability / stratum_key`。
3. 全局风险按真实流量 stratum 权重汇总，不能把各 slice 审核样本简单等权平均。
4. Active Learning / Hard Negative 默认只用于训练/诊断；若使用 Active Testing 降标注成本，必须记录非零 inclusion probability 并采用校正估计器。
5. Certification set 不参与同一版本 threshold/model/policy 选择；释放进后续训练后，下一版本需新 holdout。
6. Review 标签至少：SAME_VARIANT / DIFFERENT_VARIANT / INSUFFICIENT_EVIDENCE / SOURCE_DATA_ERROR。
7. 风险指标同时报告整体和关键 slice。
8. 缺 sampling/model/policy/index/route/publication provenance 的 review 不进入正式认证。
9. 自动 Recognition 不自动等价于审核标签；只有显式 `TRAINING_REVIEW`/治理晋升后才能进入 reviewed/gold dataset membership。

---

## 12. Entity Linking、Discovery 与 Graph Resolution

### 12.1 Linking

```text
Source
→ Hybrid Retrieval
→ Pair / Conflict / Calibration
→ CandidateSetAssessment
→ CERTAIN_MATCH or reason-coded UNKNOWN
→ Recognition（仅 CERTAIN_MATCH）
```

### 12.2 Discovery

只有 `UNKNOWN_POSSIBLE_NIL` 且输入证据达到最低质量要求的商品进入发现池：

```text
POSSIBLE_NIL
→ Web / Official Evidence（按需）
→ Strict Blocking
→ High-confidence Pair Edges
→ Constraint-aware Clustering
→ PROPOSED Canonical
```

PROPOSED 升 ACTIVE：优先 T0 官方 Identifier + Variant 属性；T1/T2/T3 单一来源不能独立激活；必须无 Hard/Possible Conflict，并通过 catalog_activation_policy。任何由 cluster / Recognition 推导出来的属性仍是 `DERIVED`，不能绕过 4.4 的 Canonical Write Barrier。

### 12.3 传递冲突

禁止 `Connected Component = Canonical Variant`。

使用：

```text
Union-Find / Cluster Graph
+ Cannot-Link
+ Cluster Signature
+ Anchor Validation
```

合并前检查 cannot-link、Hard Attributes、时间版本、来源互斥、anchor 兼容；有冲突即写 cannot-link，阻断 A=B、B=C 推导 A=C。

---

## 13. Web Evidence Agent 与 Official Web Catalog

仅触发：内部无候选、POSSIBLE_NIL、新款、Vintage/下架、型号疑似错误、关键属性冲突、Identifier 外部验证、near-tie 缺官方属性。

检索优先级：

```text
品牌官网 → 官方目录/经销资料 → 专业数据库 → 高质量二手平台 → 通用搜索
```

Agent 只能输出 Evidence；禁止直接修改 Canonical、直接 CERTAIN_MATCH、把搜索摘要当事实、用复制网页伪造多源一致。

### 13.1 官方站优先解析 Variant Graph

```text
Sitemap / Internal Links
→ Raw HTML + 必要 Rendered DOM
→ JSON-LD ProductGroup / Product
→ DOM / Embedded State / Identifier
→ External Variant Graph
→ Identifier Semantics Validation
→ Variant URL + Visible Attr Consistency
→ Versioned Snapshot
→ PROPOSED / Canonical Fact Candidate
```

ProductGroup/Product 结构适合做关系和变体维度证据，但任何 markup 都必须经过 source/field semantics、可见页面一致性和时间版本验证。

永远不能单独激活 Variant：Product Group ID、第三方 merchant SKU、单张图片/Visual Search、搜索摘要/LLM 摘要、只有营销名无具体 Variant ID 的官网页、互相转载的多个二手页。

每次抓取保存 source/domain/group、canonical/requested URL、redirect、extraction surface、field path、external group/variant id、raw snapshot、hashes、retrieved/effective time、parser/ontology version。

---

## 14. 数据库设计

关系数据库为事实源：PostgreSQL 16+ 或已有 MySQL；搜索/向量由 OpenSearch。

核心表：

```text
brand
product_family
product_model
canonical_variant
canonical_variant_version
canonical_relation
source
source_group
source_product
source_product_image
product_identifier
identifier_semantics_policy
extracted_attribute
normalized_attribute
attribute_fact
external_evidence
external_variant_relation
candidate_pair
candidate_retrieval_evidence
candidate_set_assessment
match_evidence
conflict_record
cannot_link
cluster_membership
recognition_record
review_record
review_sampling_policy
audit_sample
label_dataset_membership
pseudo_label_record
artifact_lineage_edge
artifact_taint
fact_promotion_event
canonical_mutation_log
catalog_outbox_event
search_apply_checkpoint
catalog_publication
model_version
policy_version
ontology_version
source_trust_policy
catalog_activation_policy
fact_promotion_policy
retrieval_fusion_version
route_policy_version
runtime_reliability_policy
```

`source_product` 保存 EvidenceProfile 与质量字段；`candidate_set_assessment` 保存 top1/top2/margin/NIL/retrieval health；`attribute_fact` 保存 synthetic provenance；`review_record/audit_sample` 保存 review purpose、sampling policy、inclusion probability、stratum、decision snapshot；Recognition 保存所有 model/policy/ontology/fusion/route/runtime/publication 版本及快照，必须可重放。

反馈闭环相关结构：

```text
pseudo_label_record:
  label / label_origin / teacher_or_policy_version / source_decision_id
  confidence / eligible_for_training / promotion_status

artifact_lineage_edge:
  upstream_artifact_id / downstream_artifact_id / derivation_type / producer_version

artifact_taint:
  artifact_id / taint_reason / source_event_id / detected_at / resolved_by

fact_promotion_event:
  artifact_id / from_status / to_status / policy_version / reviewer_or_governance_ref

canonical_mutation_log:
  canonical_id / field / before / after / upstream_evidence_ids / supersedes_event_id

catalog_outbox_event:
  event_id / aggregate_id / mutation_version / event_type / payload_ref / created_at

search_apply_checkpoint:
  projection_name / search_generation / applied_mutation_watermark / refreshed_watermark / updated_at

catalog_publication:
  publication_id / canonical_snapshot_version / mutation_watermark
  search_generation / search_alias / refreshed_watermark
  ontology_version / identifier_semantics_policy_version / fact_promotion_policy_version
  status / certified_at / activated_at
```

任何 Exact Alias、Canonical Fact、Reviewed Gold label 的来源必须能沿 `artifact_lineage_edge` 回溯到真实 observed/reviewed/governed artifact；只有 DERIVED→DERIVED 的闭环不能完成晋升。

---

## 15. 离线 Batch 与每日增量

历史初始化：

```text
Raw Import
→ Normalize / Extract / QC
→ Barcode/OCR
→ EvidenceProfile / Route
→ Batch Text/Image Embedding（按需要）
→ Build Versioned OpenSearch Generation
→ Publication Parity Validation
→ Atomic Alias Publish
→ Link to Existing Canonical
→ CandidateSetAssessment
→ Recognition / UNKNOWN
→ POSSIBLE_NIL Discovery
```

每日增量：

```text
New/Changed Source or Governed Canonical Mutation
→ DB Transaction: mutation + catalog_outbox_event
→ CDC / Outbox Relay
→ Idempotent Projection Consumer
→ External-versioned OpenSearch Upsert / Tombstone
→ Refresh Visibility + Advance Search Checkpoint
→ Advance Catalog Publication Watermark
→ Normalize / Identifier / EvidenceProfile
→ Route Policy
→ Search ACTIVE Canonical under pinned publication_id
→ Fusion / Pair / Conflict / Calibration
→ CandidateSetAssessment
→ Recognition or UNKNOWN
→ POSSIBLE_NIL Discovery（必要时）
```

规则：

- Canonical/Alias/Identifier promotion、撤销、RETIRED/MERGED/TAINTED 状态变化与 outbox event 必须在同一关系数据库事务中提交，避免“DB 已改但事件没发”或相反。
- Indexer 以 `event_id` 幂等去重，并用 `mutation_version` 作为 OpenSearch external version；重复/乱序事件不得把旧状态覆盖新状态。
- 普通增量更新在 `search_apply_checkpoint` 已追平并确认 refresh 可见后，才允许 `catalog_publication` 的 serving watermark 前移。
- breaking mapping/embedding/大规模回填使用新 generation，完成 Exact/状态/Hard Attribute parity + Recall benchmark 后，通过 OpenSearch 原子 alias 操作发布；旧 generation 保留到 rollback window 结束。
- 删除/撤销必须生成 tombstone/状态事件；不能只在 DB 删除再等待搜索自然消失。
- 没有图片就不计算 image embedding；强 Identifier 足够时不强制等待图片/VLM；局部视觉只运行 near-tie shortlist。

所有模型/ontology/retrieval/route/parser/catalog-activation/fact-promotion/runtime/publication policy 升级均使用 version + shadow benchmark。

---

## 16. 生产架构

```text
Source Connectors
      ↓
Raw / Object Storage
      ↓
Normalize + EvidenceProfile + Route Policy
      ↓
┌──────────┬────────────┬──────────────┐
↓          ↓            ↓              ↓
Exact    Text/BM25   Barcode/OCR     Image/VLM(按需)
└──────────┴────────────┴──────────────┘
      ↓
Catalog DB / Evidence Store
      ↓
Catalog Publication Barrier
      ↓
OpenSearch Multi-channel Retrieval
      ↓
Exact Preserve + Versioned Fusion
      ↓
Reranker / Pair Matcher
      ↓
Local Visual Verify（近邻歧义才触发）
      ↓
Conflict Engine
      ↓
Calibration + Evidence-Family Risk Policy
      ↓
Candidate-set / NIL / Ambiguity Guard
      ↓
NON_MATCH / reason-coded UNKNOWN / CERTAIN_MATCH
                                      ↓
                               Graph Resolution
                                      ↓
                               Recognition Record
                                      ↓
                                Pending Review
                                      ↓
                                  Label Store
```

主档/训练反馈路径必须与在线 Recognition 路径隔离：

```text
Recognition / Discovery / Model Output
→ DERIVED Artifact Store
→ Review / Governance / Fact Promotion Gate
→ REVIEWED_GOLD / GOVERNED_FACT / APPROVED_ALIAS
→ Canonical Mutation + Transactional Outbox
→ Versioned Search Projection / Training Dataset
```

未经 Promotion Gate 的派生结果不能直接进入 Exact Identity 索引或 gold training set。

慢路径：Official/Web Discovery → Structured Variant Parse → Snapshot/Variant Graph → Semantics/Consistency/Temporal Validation → PROPOSED/Fact Candidate → Fact Promotion → Publication。

组件：PostgreSQL/MySQL、OpenSearch 3.8、S3-compatible object storage、Kafka/Pulsar/企业 MQ、Debezium/等价 CDC Outbox、Python/Ray/Spark、Triton/自建 PyTorch、vLLM/SGLang。

### 16.1 Catalog / Search Publication Barrier

关系数据库是 Canonical Fact 的唯一事实源；OpenSearch 是**异步、near-real-time 的检索投影**。Auto-Match 不能读取“数据库当前 head + 任意时刻搜索 head”的混合状态，而必须在请求入口 pin 一个已 `ACTIVE` 的 `publication_id`。

`catalog_publication` 至少绑定：

```text
publication_id
canonical_snapshot_version / mutation_watermark
search_generation / search_alias / refreshed_watermark
ontology_version
identifier_semantics_policy_version
fact_promotion_policy_version
published_at / status
```

发布流程：

```text
Canonical Mutation + Outbox（同 DB transaction）
→ CDC / Event Stream
→ Idempotent Search Projector
→ external-versioned Upsert/Delete/Tombstone
→ Refresh / Visibility Confirmation
→ Projection Parity Check
→ Advance Publication Watermark or Atomic Alias Switch
→ ACTIVE publication_id
```

在线规则：

1. Recognition 从入口到最终写记录都保存同一 `publication_id`；同一次请求禁止中途切换 Catalog/Search generation。
2. Search hit 返回 `canonical_id + canonical_version + catalog_event_version + canonical_status`；进入 Pair/Conflict 前必须对照 pinned publication 做版本/状态校验。
3. `RETIRED / MERGED / TAINTED`、撤销 Identifier/Alias、Hard Attribute 修订属于**身份安全敏感 mutation**。即使旧搜索文档仍被召回，最终 gate 也必须从权威状态/缓存校验其在当前 publication 是否仍可用；不满足立即丢弃，不能 Auto-Match。
4. `search_apply_checkpoint < publication.mutation_watermark`、refresh 未确认、projection parity 失败、alias 指向未认证 generation、或请求观察到混合 publication 时，返回 `UNKNOWN_PUBLICATION_INCONSISTENT`；不能把空候选解释为 NIL，也不能降级到旧 Top-1。
5. 大版本切换使用新 index generation 完整回填与验证，再通过 OpenSearch `_aliases` 原子切换；增量更新则通过 outbox watermark 前移，不为每条 mutation 重建整索引。
6. 所有 Search projector 必须支持 idempotent replay；消息系统的“exactly once”不能被当成 DB→OpenSearch 原子提交保证，目标是**事务性发出 + 幂等投影 + 单调版本 + 可验证 publication**。

---

## 17. 容量、成本、性能与运行时可靠性

### 17.1 10M 向量容量

假设 10M 商品、平均 3 图、10M text + 30M image vectors、512d。

模型导出 FP16 数组约 40.96GB；OpenSearch on-disk 完整 float 向量按 FP32 约：

```text
Text   20.48GB
Images 61.44GB
合计完整向量磁盘基线 ≈ 81.92GB / active vector set
```

不含 Lucene segment、HNSW/量化索引、倒排、doc values、metadata、translog、merge 临时空间、副本、shadow index。

HNSW 量化内存参考：

```text
HNSWBytes ≈ 1.1 × (dimension × bits / 8 + 8 × m) × vector_count
```

512d、m=16、40M vectors：32x/1-bit 约 8.45GB，16x/2-bit 约 11.26GB（仅 HNSW 图量级）。

### 17.2 资源档位

PoC（1–2M）：1×L4 24GB、1×8 vCPU/64GB OpenSearch、0.5TB SSD；关系库/对象存储复用现有设施。

10M 生产压测起点：

```text
3 × 8 vCPU / 64GB OpenSearch data/search nodes
3 × 1TB usable storage, replica=1, 可容纳 active+shadow
2 × L4 24GB online GPU（HA 时）
历史 backfill 临时扩 4–8 × L4/同级 GPU
```

公有云核心 compute+block storage 量级约 PoC $1k/月、10M 起点约 $3k/月；仅作立项量级，采购时按实际区域重算，另计 DB、原图、快照、日志、网络、备份。

### 17.3 成本公式

先实测：TextEmbeddingItemsPerSec、ImageEmbeddingImagesPerSec、LocalVisualPairsPerSec、RerankPairsPerSec、VLMCasePerSec、OfficialWebPagesPerSec。

```text
MonthlyOnlineCost = Search + GPU + DB + ObjectStorage + Observability + Network
CostPerRecognizedItem = MonthlyOnlineCost / confirmed_auto_recognitions
```

与 `Coverage @ 99.9% Precision` 联合优化。

### 17.4 Precision-first Fail-closed

普通增量 p95 <500ms（不含异步 Web/VLM）；expensive-model rate 初始 <5%。10M 必须压测 filtered ANN Recall/QPS/p95/p99/build/recovery/RAM/disk/quantization loss；shadow index 期间验证磁盘 watermark。

Route Policy 为每个 EvidenceProfile 定义 mandatory/optional stage。任一 mandatory stage 被跳过、timeout、熔断时都会改变 `route_execution_signature`；**未经该 degraded route 独立 benchmark + risk certification，不允许 CERTAIN_MATCH，只能 `UNKNOWN_DEPENDENCY_FAILURE` 或异步重试。**

禁止因为 Reranker / Local Visual / VLM 不可用而静默退化成 ANN Top-1/BM25 Top-1 自动挂接。

Catalog/Search publication 不一致属于 identity-critical dependency failure，优先级高于模型分数；`UNKNOWN_PUBLICATION_INCONSISTENT` 不得被 fallback route 转回 CERTAIN_MATCH。

### 17.5 End-to-end Deadline Budget

每条在线 Recognition 请求在入口生成**绝对 deadline**，沿 OpenSearch、Reranker、OCR、GPU/VLM 等调用链传播，不允许各服务重新发明更长 timeout。

规则：

1. 每个 route 在 `runtime_reliability_policy` 中配置 `total_deadline_ms`、各 stage 预算、mandatory/optional、最小剩余预算。
2. 进入 expensive stage 前检查剩余 deadline；若无法在其 p99 + safety margin 内完成，不再启动该 stage。
3. mandatory stage 因 deadline 不足未执行 → `UNKNOWN_DEPENDENCY_FAILURE`；optional stage 只有在对应简化 route 已独立认证时才允许省略。
4. 客户端取消/上游 deadline 到期时，向下传播 cancellation，停止排队和 GPU 推理，避免“结果已经没人要但仍烧资源”。
5. Web/VLM 慢路径使用独立异步 SLA，不把几秒级工作塞入 <500ms 普通路径。

### 17.6 Retry Ownership 与 Retry Budget

重试可能把局部故障放大为级联故障，因此：

- 每个依赖明确**唯一 retry owner**；同一次逻辑请求禁止 API Gateway、业务服务、SDK、下游同时多层重试。
- 只重试明确 transient/retriable 错误；输入非法、规则冲突、4xx 业务错误、Hard Conflict 不重试。
- 重试使用指数退避 + jitter，限制每请求最大 attempts。
- 每个依赖配置进程/实例级 `retry_budget`；超过预算立即 fail fast，而不是继续占用 OpenSearch/GPU。
- 重试也受原始 end-to-end deadline 约束；剩余时间不足时禁止 retry。
- 记录 `retry_count / retry_reason / retry_budget_exhausted` 到 route execution snapshot。

PoC 不预设统一重试次数；根据依赖 p99、错误恢复时间和压测结果确定。首要目标不是“多重试提高成功率”，而是**不让失败流量吞掉健康流量容量**。

### 17.7 Bounded Queue、Backpressure 与 Circuit Breaker

对 OpenSearch、Reranker、OCR、GPU/VLM、Web Agent 分别设置：

```text
max_inflight
max_pending
queue_deadline
circuit_open_threshold
half_open_probe_budget
retry_budget
```

原则：

- 队列必须有界；请求在队列中等待到已经没有足够 deadline 时直接丢弃/UNKNOWN，不进入实际计算。
- Circuit Breaker 根据 error/timeout/inflight/pending/resource pressure 打开；打开后 fail fast，不把流量继续推向不健康依赖。
- 健康状态区分 `HEALTHY / DEGRADED / UNAVAILABLE`，并写入 `dependency_health_snapshot`。
- OpenSearch 搜索健康和“当前请求候选为空”是两个不同概念；只有前者健康且 publication 一致时才允许将空候选解释为 POSSIBLE_NIL。

### 17.8 Load Shedding 优先级

过载时按业务优先级减负，不能随机砍关键链路：

```text
P0  已有 Strong Identifier 的在线 Linking
P1  普通在线 Hybrid Linking
P2  Near-tie Local Visual / Difficult Pair
P3  Entity Discovery / Web Evidence enrichment
P4  历史 backfill / refresh / optional re-embedding
```

优先停止 P4→P3→P2；P0/P1 只有在容量不足时才 admission control。任何被降级的 route 若没有独立 Precision 认证，只能 UNKNOWN，不能降低 Matcher 阈值“保 Coverage”。

### 17.9 Recovery 与故障演练

- 定期做依赖 timeout、OpenSearch partial failure、GPU unavailable、queue overload、retry storm、CDC lag、乱序/重复事件、refresh lag、撤销 alias 仍被旧索引召回、alias 发布/回滚中断的故障注入。
- 演练目标不仅是 availability，还要验证 `degraded-route auto-match violation = 0` 和 `mixed-publication auto-match violation = 0`。
- 恢复时逐步放量，避免 cold cache / 重建连接后瞬间打满后端。
- 监控：per-stage p50/p95/p99、inflight/pending、retry rate/budget exhaustion、circuit state、deadline-exceeded、cancellation、UNKNOWN reason、route signature、catalog→search lag、applied/refreshed watermark gap、stale-candidate rejection、Precision/coverage by degraded route/publication。

---

## 18. Benchmark 与测试集

首版保留两类互补评测资产：

1. **Pair Gold Set：5K–10K pairs**，提高 Hard Negative 比例，用于 Retriever/Matcher/Conflict/局部视觉开发。
2. **End-to-end Recognition / Production Audit Set**：以 Source Product 最终 Decision 为单位，用于验证完整链路真实风险。

必须有 Seen/IID、Strict Unseen Canonical、Time-shift/New SKU 三轨；split 前按 canonical_variant_id、source/syndication group、image hash、content fingerprint 隔离，最终认证禁止 pair-level random leakage。

Corner Cases 额外覆盖：NIL/near-tie、Official Web group-id 误用、merchant SKU 误用、JSON-LD/DOM 冲突、局部视觉近邻、低质图、合成证据、运行时 timeout/queue overload/K 收缩/retry storm/circuit open、CDC duplicate/out-of-order、search refresh lag、Canonical RETIRED/MERGED/TAINTED 后旧候选仍命中、publication alias 切换/rollback，以及“错误 Recognition 被写成 Alias/伪标签后再次命中”的反馈闭环污染用例。

关键指标：

```text
Retrieval:
Recall@20/@50/@100 before/after fusion
Exact survival / candidate_count / latency

Pair:
Precision / Recall / PR-AUC / Calibration Error
Coverage @ target precision / Evidence-family ablation

Open-world:
NIL Precision/Recall
False Known-Link on true new SKU
Forced-link / near-tie violation

Routing:
Coverage / False Merge / UNKNOWN / Cost by EvidenceProfile
Synthetic-evidence promotion violation = 0

Publication Consistency:
mixed_publication_auto_match_violation = 0
retracted_or_tainted_candidate_auto_match = 0
out_of_order_projection_regression = 0
catalog_to_search_lag_ms
search_apply_watermark_gap
stale_candidate_rejection_rate
publication_rollback_recovery_time

Feedback Integrity:
derived_evidence_reentry_violation = 0
auto_recognition_to_reviewed_gold_violation = 0
canonical_write_without_promotion = 0
unapproved_alias_in_exact_retrieval = 0
taint_propagation_completion_rate = 100%
lineage_traceability_rate = 100%

Runtime Reliability:
degraded-route auto-match violation = 0
retry amplification factor
retry budget exhaustion rate
deadline-exceeded rate
queue timeout / shed rate
circuit-open duration
cancellation propagation completion rate
Precision / Coverage / UNKNOWN by route_execution_signature

Leakage:
cross_split_variant/image/syndication leakage = 0
certification_label_used_for_same-version_tuning = 0

Production:
Coverage @ 99.9% Precision
False Merge per Million
UNKNOWN Rate
cost per recognized item
risk upper bound
```

---

## 19. MVP → Production

### Phase 0：数据审计与 Gold Set

字段完整度、Alias/Ontology、Hard Conflict Matrix、Identifier/条码可用率、来源独立性、Evidence Family、EvidenceProfile 分布、Leakage-safe Split、真实 NIL/near-tie benchmark；建立 TRAINING/CERTIFICATION/GOVERNANCE 三轨标签治理，并定义 OBSERVED/DERIVED/REVIEWED/GOVERNED lineage 与 promotion policy。

### Phase 1：确定性腕表 Linking

Reference/GTIN lookup、Exact Retrieval、Hard Conflict、Recognition/Review；同时建立 Canonical mutation + Transactional Outbox、Search Projection checkpoint、`publication_id` pinning 与撤销/tombstone 回归用例，先用真实 Decision 的概率审计证明 99.9% Precision。

### Phase 2：Hybrid Text + Route Policy + Pair Matcher

BM25、Qwen3-Embedding-0.6B、RRF、Qwen3-Reranker-0.6B、箱包、Calibration、Candidate-set/NIL/Ambiguity Guard；禁止强制 Top-1。

### Phase 3：Image / Barcode / OCR / Multimodal

zxing-cpp、PP-OCRv6、PaddleOCR-VL、SigLIP2、DINOv3 challenger、局部 patch rerank、Qwen3.5-4B、Qwen3-VL challenger；视觉不能绕过 Identity/Conflict。

### Phase 4：Open-world Discovery + Official Web Evidence

POSSIBLE_NIL、constraint-aware clustering、ProductGroup/Product parse、Identifier Semantics、External Variant Graph、Snapshot、Catalog Activation Gate。

### Phase 5：Supervised Student + Continuous Learning

review labels、hard negative、NIL/near-tie mining、active learning、domain encoder、learned fusion、selective risk control、shadow/A-B/rollback；pseudo labels 与 reviewed gold 分层保存，Canonical/Alias/Identifier 的任何自动建议必须经过 Fact Promotion Gate，持续验证反馈闭环污染率为 0。

### 全阶段 Runtime Reliability / Publication Gate

任何会产生 Auto-Match 的 route / fallback / degraded execution signature：

1. 必须有 end-to-end deadline 与 stage budget。
2. retry ownership/budget、bounded queue、circuit breaker 配置完整。
3. 未认证 degraded route 的 CERTAIN_MATCH 必须为 0。
4. load shedding 顺序固定且经过演练。
5. failure injection 下不允许出现 retry amplification 导致级联过载。
6. 每条 Recognition 必须 pin 已 ACTIVE 的 `publication_id`，且 Search applied/refreshed watermark 满足该 publication。
7. publication mismatch、projection lag 超认证阈值或撤销状态未同步时必须 fail closed。

---

## 20. 正式开启自动识别 Gate

全部满足才扩大 Auto-Match：

1. 主要品类/来源 `Precision@Auto-Match >= 99.9%`。
2. `Recall@100 after fusion/truncation >= 99.5%`。
3. Exact/官方标识符候选 survival = 100%。
4. 有足够独立认证样本支撑目标错误率上界。
5. 所有 Hard Conflict 回归 0 自动错合并。
6. Seen/Strict Unseen/Time-shift、Vintage、缺图、缺型号、脏型号、低质图、文本图像冲突分别达标。
7. Image-only 路径不得无额外身份/结构证据 CERTAIN_MATCH。
8. synthetic evidence 不能成为 Hard / Identity Anchor，违规=0。
9. AMBIGUOUS/NIL/RETRIEVAL_FAILURE/PUBLICATION_INCONSISTENT 不得绕过 Candidate Guard。
10. 新 SKU challenge set forced Known-Link/near-tie 错误挂接=0。
11. final certification leakage=0。
12. 每个品牌/来源/品类/EvidenceProfile/CandidateSetState 有阈值或 kill switch。
13. 所有模型/规则/ontology/index/fusion/route/candidate/runtime/publication policy 可回滚。
14. Recognition 可重放完整证据、CandidateSetAssessment、dependency health、retry/deadline、publication snapshot。
15. Source/model/route/retrieval drift 超阈值自动 UNKNOWN/关闭 Auto-Match。
16. Product Group ID 作为 Variant Identity Anchor 违规=0。
17. 未认证 merchant SKU 作为官方 Reference 违规=0。
18. Official Web JSON-LD/DOM 未解决冲突时自动 ACTIVE=0。
19. Web parser/identifier semantics/catalog activation versioned + kill switch。
20. 正式 Precision 证明来自冻结版本真实 CERTAIN_MATCH 流的概率审计。
21. certification sampling provenance 完整率=100%。
22. certification label 同版本调参复用违规=0。
23. mandatory stage timeout/熔断/跳过且 degraded route 未认证时，CERTAIN_MATCH=0。
24. deadline 未传播、无限/多层重试、无界队列任一违规=0。
25. retry budget exhaustion 后仍继续向故障依赖发起自动重试的违规=0。
26. 负载/故障演练中 degraded-route auto-match violation=0。
27. `Recognition / Matcher / Cluster` 派生结果重新作为独立 Evidence Family 或独立来源计票的违规=0。
28. 自动 Recognition 未经 review/promotion 直接进入 REVIEWED_GOLD 或修改 Canonical/官方 alias/identifier semantics 的违规=0。
29. ACTIVE Exact Retrieval alias 必须 100% 可回溯到 OBSERVED/REVIEWED/GOVERNED 上游和 promotion event。
30. 已 taint 上游产生的 downstream artifact 查找与隔离完整率=100%，受影响索引/训练集/模型均有重建或重认证状态。
31. `recognition_record.publication_id` 完整率=100%，同一 Recognition 混用多个 Catalog/Search publication 的违规=0。
32. `RETIRED/MERGED/TAINTED` 或已撤销 Identifier/Alias 因搜索投影滞后仍产生 CERTAIN_MATCH 的违规=0。
33. duplicate/out-of-order/replayed outbox event 导致 Search 文档版本倒退的违规=0。
34. search projection 未追平 publication watermark 或 refresh 未确认时继续 Auto-Match 的违规=0。

任何指标退化时，优先降低 Coverage，不放宽 Identity Safety。

---

## 21. PoC 实验矩阵

| 问题 | Baseline | Challenger | 决策指标 |
|---|---|---|---|
| Missing-modality routing | 统一全链路 | EvidenceProfile route | false merge / coverage / cost |
| Missing completion | 不生成真值 | MLLM 只 query expansion | Recall gain / hallucination / precision |
| Text Embedding | Qwen3-Embedding-0.6B 512d | 256/384d / 4B | Recall@100 / RAM / latency |
| Retrieval Fusion | RRF + quota | normalized/learned fusion | Recall / exact survival / latency |
| Open-world / NIL | 强制 Top-1 | CandidateSetAssessment | false-known-link / NIL P-R |
| Candidate ambiguity | 绝对阈值 | top1 + top2 margin | near-tie false merge |
| Text Rerank | Qwen3-Reranker-0.6B | structured GBDT | Precision / latency |
| Image Semantic | SigLIP2 | 其他商用 encoder | hard-case Recall |
| Fine-grained Vision | global feature | patch/local rerank | hard-negative precision / cost |
| Multimodal Retrieval | late fusion | Qwen3-VL-Embedding-2B | Coverage@99.9 / GPU |
| Difficult Pair | structured+text | Qwen3.5/Qwen3-VL | hard-negative precision |
| Evidence Policy | naive sum | Evidence-Family | false merge / coverage |
| OCR | PP-OCRv6 | PaddleOCR-VL | identifier F1 / cost |
| Official Web | DOM/text | ProductGroup/Product graph | relation accuracy / false promotion |
| Identifier semantics | string guess | source×field policy | official-id precision |
| Vector Store | OpenSearch | Milvus | Recall/QPS/RAM/disk/ops |
| Catalog/Search consistency | DB + OpenSearch 双写/最终一致 | Transactional Outbox + idempotent external-version projector + publication watermark/atomic alias | stale-match violation / lag / rollback |
| Benchmark Split | pair random | grouped + time-shift | leakage / unseen risk |
| Cluster | connected component | constrained union | false merge / violation |
| Risk Policy | calibrated threshold | CRC/selective CRC | certified coverage |
| Audit Sampling | training review 直接算 Precision | probability audit / Active Testing challenger | bias / CI / label cost |
| Feedback Loop | auto Recognition 直接回写 alias/训练集 | lineage + pseudo-label quarantine + Fact Promotion Gate | feedback false merge / taint recovery / traceability |
| Runtime Degradation | timeout 后静默跳 stage | certified route or fail-closed UNKNOWN | degraded false merge / availability |
| Retry Strategy | 多层固定重试 | single-owner + jitter + retry budget | amplification / recovery / p99 |
| Queue/Overload | unbounded queue | bounded queue + priority shedding | latency / drop / cascade resistance |

---

## 22. 主要技术依据

- Amazon Science — VARM：Variant Relationship 与普通 Entity Resolution 分开建模。  
  https://www.amazon.science/publications/learning-variant-product-relationship-and-variation-attributes-from-e-commerce-website-structures
- Walmart Global Tech：Catalog 属性抽取与 Quality Check 分阶段。  
  https://tech.walmart.com/content/walmart-global-tech/en_us/news/articles/how-walmart-uses-llms-to-manage-its-massive-product-catalogs.html
- Microsoft Research：Unstructured Offer → Structured Product / Product Synthesis。  
  https://www.microsoft.com/en-us/research/publication/matching-unstructured-product-offers-to-structured-product-descriptions/  
  https://www.microsoft.com/en-us/research/publication/synthesizing-products-for-online-catalogs/
- WDC Products：现实商品 Entity Matching、corner cases、unseen entities。  
  https://github.com/wbsg-uni-mannheim/wdcproducts
- GenDecider / Learn to Not Link：明确处理 none-of-the-candidates / NIL。  
  https://aclanthology.org/2024.naacl-short.22/  
  https://aclanthology.org/2023.findings-acl.690/
- End-to-end multi-modal product matching in fashion e-commerce。  
  https://arxiv.org/abs/2403.11593
- MMPCBench：缺模态生成高层语义可用，但不能充当细粒度身份真值。  
  https://arxiv.org/abs/2601.19750
- AMELI / Multimodal Joint Attribute Prediction：细粒度结构化属性与选择性视觉使用。  
  https://aclanthology.org/2024.eacl-long.172/  
  https://aclanthology.org/2020.emnlp-main.166/
- LOCORE / ILIAS：局部 descriptor shortlist rerank 对实例级细粒度检索有价值。  
  https://openaccess.thecvf.com/content/CVPR2025/html/Xiao_LOCORE_Image_Re-ranking_with_Long-Context_Sequence_Modeling_CVPR_2025_paper.html  
  https://openaccess.thecvf.com/content/CVPR2025/html/Kordopatis-Zilos_ILIAS_Instance-Level_Image_retrieval_At_Scale_CVPR_2025_paper.html
- Google Search Central — Product Variant Structured Data / Merchant Listing。  
  https://developers.google.com/search/docs/appearance/structured-data/product-variants  
  https://developers.google.com/search/docs/appearance/structured-data/merchant-listing
- GS1 GTIN Management。  
  https://www.gs1.org/1/gtinrules/en/
- Google Research — RRF / Hybrid Retrieval / Nearly-independent feature families。  
  https://research.google/pubs/reciprocal-rank-fusion-outperforms-condorcet-and-individual-rank-learning-methods/  
  https://research.google/pubs/leveraging-semantic-and-lexical-matching-to-improve-the-recall-of-retrieval-systems-a-hybrid-approach/  
  https://research.google/pubs/on-using-nearly-independent-feature-families-for-high-precision-and-confidence/
- OpenSearch 3.8 / Hybrid / disk-based vector。  
  https://github.com/opensearch-project/OpenSearch/releases/tag/3.8.0  
  https://docs.opensearch.org/latest/vector-search/ai-search/hybrid-search/index/  
  https://docs.opensearch.org/latest/vector-search/optimizing-storage/disk-based-vector-search/
- OpenSearch Document/Refresh、External Versioning 与 Alias：Search 可见性依赖 refresh；external version 可防止外部同步时旧版本覆盖新版本；Manage Aliases 支持原子切换 generation。  
  https://docs.opensearch.org/latest/api-reference/document-apis/index/  
  https://docs.opensearch.org/latest/api-reference/document-apis/bulk/  
  https://docs.opensearch.org/latest/api-reference/alias/aliases-api/
- Debezium Outbox Event Router：Transactional Outbox 用于避免业务数据库状态与下游事件之间的不一致，并为事件提供唯一 ID/aggregate key，适合驱动幂等 Search Projection。  
  https://debezium.io/documentation/reference/stable/transformations/outbox-event-router.html
- Apache Kafka Design：Kafka topic 内可借助事务/idempotence 获得 exactly-once 处理，但写入 Kafka 之外的目标系统仍需目标系统协作；因此 DB→OpenSearch 不依赖“消息 exactly once”口号，而使用 Outbox + 幂等投影 + version/watermark。  
  https://kafka.apache.org/41/design/design/
- Qwen3 Embedding/Reranker、Qwen3.5、Qwen3-VL Embedding；SigLIP2、DINOv3；PaddleOCR、zxing-cpp。  
  https://github.com/QwenLM/Qwen3-Embedding  
  https://huggingface.co/Qwen/Qwen3.5-4B  
  https://github.com/QwenLM/Qwen3-VL-Embedding  
  https://github.com/google-research/big_vision  
  https://github.com/facebookresearch/dinov3  
  https://github.com/PaddlePaddle/PaddleOCR  
  https://github.com/zxing-cpp/zxing-cpp
- Conformal Risk Control / Selective CRC。  
  https://arxiv.org/abs/2208.02814  
  https://arxiv.org/abs/2512.12844
- Active Testing（ICML 2021）：主动测试可降低标注成本，但选择过程会引入 bias，必须使用校正估计器。  
  https://proceedings.mlr.press/v139/kossen21a.html
- Google SRE — Addressing Cascading Failures：load shedding、bounded queue、deadline propagation、retry budget、随机指数退避以及故障演练。  
  https://sre.google/sre-book/addressing-cascading-failures/
- gRPC Deadlines：客户端显式 deadline、服务端取消、deadline propagation。  
  https://grpc.io/docs/guides/deadlines/
- Envoy Circuit Breaking / Overload Manager：max requests/pending/retries、retry budget、资源压力触发的 overload protection。  
  https://www.envoyproxy.io/docs/envoy/latest/configuration/upstream/cluster_manager/cluster_circuit_breakers.html  
  https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/operations/overload_manager
- Approximately Bayes-optimal pseudo-label selection（UAI/PMLR 2023）：自训练早期的过拟合或错误 pseudo-label 会被后续迭代传播，必须显式控制 pseudo-label selection，而不能把模型自身高置信输出等同真值。  
  https://proceedings.mlr.press/v216/rodemann23a.html
- PUER（EMNLP 2025）：低资源 Entity Resolution 的迭代方案专门强调 pseudo-label 的鲁棒性与质量控制，说明伪标签不能无治理地进入下一轮匹配学习。  
  https://aclanthology.org/2025.findings-emnlp.1336/
- TensorFlow TFX ML Metadata：用 Artifact / Execution / Event 记录生产 ML pipeline 的上下游 lineage，可从异常数据或模型向下游/上游追踪受影响 artifact，适合作为本系统 fact/label/model lineage 的实现参考。  
  https://www.tensorflow.org/tfx/guide/mlmd

---

## 23. 最终生产原则

```text
官方知识 / 标准化 / 确定性标识
          ↓
EvidenceProfile / Route Policy
          ↓
只运行真实证据所需检索与模型
          ↓
Catalog Publication Barrier
          ↓
Exact Preserve + Versioned Fusion
          ↓
结构化 + 文本 + 图片多证据
          ↓
必要时局部视觉复核
          ↓
Hard Conflict
          ↓
Calibration + Evidence-Family Risk
          ↓
Candidate-set / NIL / Ambiguity Guard
          ↓
Constraint-aware Graph Resolution
          ↓
仅 CERTAIN_MATCH Recognition
          ↓
Derived Artifact Quarantine / Fact Promotion Gate
          ↓
Review Feedback + Independent Risk Audit
```

固定原则：

1. **Identity first，Similarity second。**
2. **Hard Conflict 优先于任何模型高分。**
3. **UNKNOWN 是合法且必要的生产输出，并必须有 reason code。**
4. **缺模态不能靠生成模态伪装成完整证据。**
5. **Synthetic evidence 不能形成 Hard Fact / Identity Anchor。**
6. **仅视觉相似不能自动 SAME_VARIANT。**
7. **全局视觉负责召回，局部视觉只验证 near-tie shortlist。**
8. **必须显式允许 NIL / None-of-the-Candidates。**
9. **Top-1/Top-2 near-tie 时必须 Abstain。**
10. **所有证据、路由、运行时依赖状态可版本化、可重放。**
11. **多个 URL 不等于多个独立来源；同一原始证据的多个分数不等于多个独立证据。**
12. **Exact 候选永不被 soft fusion 截断。**
13. **新 SKU / Vintage / time-shift / missing-modality / NIL 单独评测。**
14. **风险认证 split 隔离 Canonical、重复图片、syndication/feed 泄漏。**
15. **训练 review 与风险 certification 分流。**
16. **自动阈值由目标 False Merge Risk 决定。**
17. **Product Group ID 是关系标识，不是 Variant 身份。**
18. **Merchant SKU 与 Manufacturer Identifier 分命名空间。**
19. **官网结构化数据是高价值 Evidence，不是无条件 Canonical Truth。**
20. **任何 timeout/过载/依赖故障形成的新 route_execution_signature 未认证时 fail closed 为 UNKNOWN。**
21. **Deadline 必须贯穿调用链；过期/取消请求不得继续占用搜索/GPU。**
22. **Retry 必须单层所有权、指数退避+jitter、受 retry budget 和原 deadline 约束。**
23. **队列必须有界；Circuit Breaker 与 load shedding 优先保护健康核心 Linking 流量。**
24. **Recognition / Matcher / Cluster 输出是派生关系，不是新的独立现实证据。**
25. **Pseudo label、Candidate Alias、Derived Fact 默认隔离；只有显式 Review/Governance Promotion 才能进入 Gold/Canonical/Exact Identity。**
26. **任何 Canonical/Label/Index/Model artifact 必须可沿 lineage 回溯；坏上游必须能 taint 并隔离所有受影响下游。**
27. **Canonical DB 是身份事实源，Search 是可重建投影；每次 Auto-Match 必须绑定一致的 Catalog/Search publication，投影延迟不能被误解释为 NIL 或身份事实。**
28. **Search Projection 必须事务性发出、幂等消费、单调版本更新；撤销/TAINT/RETIRED 状态在最终 Identity Gate 必须优先于任何陈旧 Search hit。**
29. **任何复杂度只有在提高 Coverage@99.9% Precision、提升可靠性或降低总成本时才有价值。**