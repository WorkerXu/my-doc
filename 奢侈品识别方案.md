# 奢侈品识别方案

## 1. 目标与边界

本系统的目标不是“找视觉上最像的商品”，而是把雷小安、奢当家、腕表之家、小迈步、爱回收及其他来源的商品记录，归一到**同一个官方商品变体（Official Variant）**。

第一阶段覆盖：

- 腕表
- 箱包
- 历史存量约 100 万–1000 万条
- 腕表已有部分品牌 / 系列 / 型号标准库
- 箱包主档不完整，需要同时支持 Entity Linking 与 Entity Discovery

核心风险偏好：

> **Precision >> Recall。宁可输出 UNKNOWN，也不能把不同官方变体错合并。**

只有尺寸、材质、颜色、盘面、五金、Reference / Style Code、版本等会定义官方变体的关键属性兼容时，才允许自动认定 `SAME_VARIANT`。

系统不做真假鉴定。成色、价格、库存、卖家、单件序列号、保卡状态等属于 Offer / 单件属性，不定义官方商品变体。

---

## 2. 核心业务实体与身份模型

### 2.1 实体层级

统一使用：

```text
Brand
  └── Product Family / Series
       └── Product Model
            └── Canonical Variant
                 └── Source Product / Offer
```

- **Brand**：品牌。
- **Product Family / Series**：系列/家族。
- **Product Model**：款式/型号层，可包含多个官方变体。
- **Canonical Variant**：系统识别的最小官方商品实体。
- **Source Product / Offer**：某个平台的一条原始商品记录。

关系类型至少包括：

```text
SAME_VARIANT
VARIANT_OF
SAME_MODEL
SAME_SERIES
VISUALLY_SIMILAR
UNKNOWN
```

只有 `SAME_VARIANT` 可生成自动 Recognition。

### 2.2 Canonical Variant 状态

```text
ACTIVE
PROPOSED
RETIRED
MERGED
```

新发现实体默认 `PROPOSED`。没有高可信主档锚点或多源独立证据时，不允许直接进入 `ACTIVE`。

### 2.3 官方标识符与条码

除品牌 Reference / Style Code 外，统一采集：

```text
gtin
ean
upc
barcode_symbology
barcode_payload
barcode_checksum_valid
market
identifier_source
identifier_confidence
```

GTIN/EAN/UPC 是高价值确定性证据，但不能机械等同于 Canonical Variant ID：

- 相同、校验通过且来源可信的 GTIN/EAN/UPC：Strong Positive + Exact Retrieval Key。
- 不同 GTIN：默认 `POSSIBLE_CONFLICT`；只有品牌/市场规则确认“一个官方变体只能对应一个贸易项目标识”时才升级 Hard。
- 不同颜色/尺寸通常应有不同贸易项目标识，因此 GTIN 对箱包等变体区分非常有价值。
- 历史/Vintage 数据必须考虑旧编码、市场差异、包装变化及早期标识复用，不能用 GTIN 单独覆盖其他 Hard Attribute。
- 如能解析 GS1 Application Identifier 等更细粒度 variant 信息，作为额外结构化证据保存。

Canonical Variant 不依赖某一种标识符存在；最终身份由官方标识符、Hard Attributes 与证据策略共同确定。

---

## 3. 标准 Schema 与 Hard Constraint

### 3.1 属性事实模型

每个抽取/标准化属性必须保存：

```text
raw_value
normalized_value
canonical_value_id
extractor
extractor_version
source_type
source_id / source_url
evidence_span / image_bbox
confidence
validation_status
effective_from
effective_to
observed_at
content_hash
```

重要原则：

1. `observed_at` 与 `effective_from/effective_to` 分离，避免把“什么时候抓到”误当成“什么时候有效”。
2. 新证据不直接覆盖旧事实；用 `supersedes` / version 关系表达变更。
3. Hard Conflict 只有在双方属性都达到相应可信度门槛时成立。
4. OCR/VLM 低置信冲突只触发 `POSSIBLE_CONFLICT → UNKNOWN`。

### 3.2 腕表 Schema

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| series / family | Blocking + Strong；品牌规则可 Hard |
| subseries / model | Strong |
| official_reference_full | Hard；最强身份字段之一 |
| reference_base | Retrieval / Strong，默认不能替代 full |
| gtin/ean/upc | Strong；条件 Hard |
| case_diameter_mm | Hard |
| case_material | Hard |
| dial_color | Hard |
| dial_type | 能定义独立变体时 Hard |
| bezel | 能定义独立变体时 Hard |
| bracelet_or_strap | 条件 Hard；需识别后换件 |
| caliber / movement | 代际/变体区分时 Hard，否则 Strong |
| functions | 变体区分时 Hard |
| edition / generation | Hard |
| serial_number | 单件属性，不得作为 Variant ID |
| condition / price | Offer 属性 |

Reference 必须保留：

```text
reference_raw
reference_normalized
reference_base
reference_full
reference_parts
brand_rule_version
```

默认以 `reference_full` 为准。只有品牌规则明确确认后缀不定义官方变体时，才允许 base reference 参与自动合并。

### 3.3 箱包 Schema

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| family / series | Blocking + Strong |
| model / style | Strong |
| official_style_code | Hard；最强身份字段之一 |
| gtin/ean/upc | Strong；条件 Hard |
| official_size | Hard |
| material | Hard |
| official_color | Hard |
| hardware | Hard |
| pattern / print | Hard |
| edition / collaboration | Hard |
| season / year / generation | 官方由其区分时 Hard |
| serial / date_code | 单件/生产证据，不直接作为 Variant ID |
| condition / price | Offer 属性 |

图片颜色不能直接成为 Hard Constraint。灯光、白平衡、皮革老化、修图会造成偏差；图片颜色只做 Soft Evidence。

### 3.4 两级标准化

颜色、材质、尺寸同时保留：

```text
generic_group
brand_specific_value
raw_value
```

例如：

```text
material_group = ROSE_GOLD
material_brand_value = ROLEX_EVEROSE_GOLD

color_group = BLACK
color_brand_value = CHANEL_NOIR
```

这样既能跨平台召回，又不会把品牌专有语义压平。

---

## 4. Evidence、来源独立性与时间版本

### 4.1 来源等级

```text
T0  品牌官网、官方目录、官方标识体系
T1  已验证内部标准库、授权/专业数据库
T2  高质量专业平台、可信二手平台
T3  普通电商、聚合站、搜索网页
```

来源可信度按“属性”而不是整站一刀切。例如专业腕表数据库对 Reference/机芯很强，但卖家文本对原配表带可能不可靠。

### 4.2 独立证据不能按 URL 数量计算

大量电商页会复制同一个上游 feed。系统必须维护：

```text
source_group_id
syndication_group_id
content_fingerprint
image_hash_group_id
upstream_hint
```

判定“两个独立来源共同支持”时，至少要求它们不属于同一已知 syndication group，且文本/图片指纹不能显示明显复制关系。

相同图片 SHA-256、感知哈希近重复、相同异常文案、相同字段错误都可作为“来源不独立”的线索。

### 4.3 时间版本

Canonical 属性必须支持有效期：

```text
attribute_value
valid_from
valid_to
evidence_ids
version
```

用于解决：

- 同名款跨年份改款
- Reference/Style Code 规则变化
- 新旧机芯/五金/尺寸变化
- 官网下架后 Vintage 资料回溯
- 同一营销名称对应多个世代

没有明确代际证据时，不跨年代自动合并。

---

## 5. 属性抽取与标准化流水线

```text
Raw Source Product
      ↓
Schema Mapping
      ↓
Deterministic Parsing / Dictionary
      ↓
Barcode Decode
      ↓
Text Attribute Extraction
      ↓
Scene OCR
      ↓
Document-like OCR/VLM（按需）
      ↓
General VLM Fallback（少量困难样本）
      ↓
Normalization / Ontology Mapping
      ↓
Attribute Validation / Quality Check
      ↓
Normalized Product Profile
```

### 5.1 优先级

1. 规则、品牌字典、官方主档。
2. 条码解析。
3. 文本抽取。
4. Scene OCR。
5. Document-like OCR/VLM。
6. General VLM。
7. Quality Check。

便宜、确定性的能力优先；大模型只补困难缺口。

### 5.2 条码解析

商品图、吊牌、包装、保卡存在 1D/2D Code 时，先用 **zxing-cpp** 等确定性 decoder 解析 EAN-13、UPC-A/E、Code128、Data Matrix、QR 等。

保存：

```text
image_id
symbology
payload
checksum_valid
decoder_version
bbox
quality
```

条码 payload 必须经过格式校验、GTIN check digit、品牌/市场映射，再进入 Exact Retrieval。

### 5.3 OCR

PoC 默认 Scene OCR：**PP-OCRv6**。

适用：

- 表背/吊牌文字
- 包装标签
- 保卡局部字段
- Style Code / Reference
- 尺寸与材质文本

OCR 后必须分类：

```text
reference / style_code
gtin/ean/upc
serial_number
date_code
size
material_text
other_text
```

不能“看到数字就当型号”。

对于密集文档、保卡整页、历史目录页、复杂表格等，使用 **PaddleOCR-VL-1.6** 或 PP-Structure 类能力作为 document-like fallback，不让其替代普通商品图上的轻量 OCR。

### 5.4 General VLM

PoC 默认困难样本结构化抽取模型：**Qwen3.5-4B**。

选择原因：

- 当前 4B 级开源多模态模型，适合成本受控的本地/私有部署 PoC。
- 用于多图 + 文本联合观察、属性候选、冲突解释。
- 不承担全量 Pair Matching。

输出严格 JSON：

```json
{
  "attributes": [],
  "observations": [],
  "conflicts": [],
  "unknown_fields": [],
  "evidence_refs": []
}
```

VLM 结果必须经过 ontology、品牌规则、主档、OCR/文本交叉验证。VLM 无权直接写 ACTIVE Canonical 或直接输出 `CERTAIN_MATCH`。

---

## 6. Candidate Retrieval：Blocking + Hybrid Search

100 万–1000 万记录禁止 N²。所有识别先召回小候选集。

### 6.1 召回通道

```text
Exact Full Reference / Style Code
Exact GTIN/EAN/UPC
Brand + normalized model/reference
Brand + series/model + Hard Attributes
BM25
Text ANN
Image ANN
Image exact/perceptual hash
Historical Canonical Alias
Unmatched Entity-Discovery Neighbors
```

默认先按 `category`，通常再按 `brand_id` 过滤。

### 6.2 初始 Top-K

| 通道 | PoC 初始 K |
|---|---:|
| Exact Identifier | ≤20 |
| BM25 | 30 |
| Text ANN | 50 |
| Image ANN | 每图 30，总上限 60 |
| Attribute Combination | 30 |
| 合并去重候选池 | ≤150 |
| Cheap Rerank | ≈30 |
| Expensive Pair Matcher | 10–20 |

最终 K 由内部 `Recall@K` 决定。

PoC Retrieval Gate：

```text
Recall@100 >= 99.5%
```

按品类、来源、Seen/Unseen SKU 分桶验证。召回不达标时先修 Blocking/Embedding，不能靠降低最终 Matcher 阈值补救。

### 6.3 搜索引擎

生产基线：**OpenSearch 3.8**。

职责统一承载：

- Exact/filter
- BM25
- filtered k-NN
- hybrid retrieval
- on-disk vector
- quantization/rescore
- alias/versioned index

OpenSearch 3.7 已支持更高效的 vector `docvalue_fields` retrieval；3.6+ 的磁盘向量、标量/二值量化能力可显著降低内存压力。生产必须用真实奢侈品向量验证量化后的 Recall，而不是直接采用最大压缩率。

Milvus 只在 1000 万级真实压测证明 OpenSearch 在 Recall/QPS/资源成本上不满足要求时再拆分。pgvector 适合小规模 PoC/内部工具，不作为唯一的千万级 Hybrid ANN 引擎。

### 6.4 索引组织

至少：

```text
canonical_variant_search_vN
source_product_search_vN
product_image_search_vN
identifier_lookup_vN
```

模型升级创建 shadow index，离线回填验证后切 alias；禁止在线混用不同 embedding version。

---

## 7. Embedding 与视觉检索

### 7.1 Text Embedding

PoC 默认：**Qwen3-Embedding-0.6B**。

先测试 512 维，再比较 256/384 维在域内 hard-case Recall@K 上是否有显著损失。

标准化模板示例：

```text
brand=Rolex | series=Datejust | reference=126334 |
size=41mm | case=steel | bezel=white_gold_fluted |
dial=blue | bracelet=oyster
```

同时保留原始标题/描述的自由文本向量，以覆盖脏数据、俗称和缺字段场景。

### 7.2 Text Reranker

PoC 默认：**Qwen3-Reranker-0.6B**，只对 Top-N 候选使用。

### 7.3 Image Embedding

默认语义/跨模态基线：**SigLIP2-B/16 384**。

细粒度视觉 challenger：**DINOv3 ViT-B/16**。

DINOv3 适合验证：

- 表盘/表圈/材质等局部细节
- 包型轮廓与细粒度纹理
- dense patch feature / 局部匹配
- 弱文本情况下的视觉候选召回

但 DINOv3 使用独立许可证，模型获取也存在地域/合规限制。因此它不能无条件成为生产默认：必须先通过 License/Legal/Deployment Gate；若不可用则保留 SigLIP2 + DINOv2/其他可商用视觉模型作为替代。

每张图独立保存向量，不把多图简单平均成一个向量。聚合特征：

```text
max_similarity
topk_mean
front_image_similarity
detail_image_similarity
image_quality_weight
```

图片预处理：

```text
quality check
→ exact hash / perceptual hash
→ duplicate removal
→ subject crop
→ image type classification
→ embedding
```

### 7.4 多模态 Retrieval/Rerank

**Qwen3-VL-Embedding-2B / Qwen3-VL-Reranker-2B** 保留为 challenger。

只有内部难例集证明其显著提升：

```text
Coverage @ 99.9% Precision
```

且增量 GPU 成本可接受时，才进入在线困难路径。

---

## 8. Pair Matching / Decider

### 8.1 分层判定

```text
Candidate Pair
   ↓
Pre-Conflict Check
   ↓
Cheap Deterministic Features
   ↓
Structured / Text Matcher
   ↓
Multimodal Difficult-Pair Matcher（少量）
   ↓
Post-Conflict Check
   ↓
Calibration
   ↓
Risk Policy
   ↓
Decision
```

### 8.2 Pair Features

结构化：

```text
brand_exact
reference_full_exact
reference_base_exact
style_code_exact
gtin_exact
gtin_conflict
series_exact
model_exact
size_exact
material_exact
color_exact
hardware_exact
edition_exact
attribute_missing_count
```

文本：

```text
bm25_score
text_embedding_cosine
cross_encoder_score
series_similarity
model_similarity
attribute_text_similarity
```

图片：

```text
exact_image_hash_match
perceptual_hash_distance
image_max_cosine
image_topk_mean
dense_feature_match
visual_attribute_agreement
ocr_identifier_agreement
```

来源：

```text
source_trust
independent_source_count
official_source_hit
syndication_risk
attribute_provenance_quality
```

### 8.3 零标签 Matcher

零标签阶段不训练“万能二分类器”。

腕表高置信正例：

```text
brand exact
+ official_reference_full exact
+ 所有已知 Hard Attributes 无冲突
+ identifier/evidence source 达高可信等级
```

箱包高置信正例：

```text
brand exact
+ official_style_code exact
+ size/material/color/hardware 无可信冲突
+ 高可信主档或多个真正独立来源支持
```

### 8.4 有标签后的 Student

建议演进：

```text
~10K reviewed pairs:
Logistic / LightGBM + calibration

更多数据:
compact Cross Encoder / domain contrastive encoder

100K+ 且 hard-negative 足够:
multimodal student
```

Ditto 保留为离线 Entity Matching baseline，不作为默认生产依赖。

### 8.5 LLM/VLM Difficult Pair

仅处理：

- 高价值 UNKNOWN
- 新款/Vintage 无主档
- 型号疑似错误
- 文本与图片冲突
- 品牌专有材质/别名解释
- 复杂网页/目录证据整合

PoC 目标：昂贵模型路径先控制在总商品/候选的 5% 以下。

---

## 9. Conflict Engine

Conflict Engine 是独立安全墙，Matcher 前后都执行。

```text
HARD_CONFLICT
POSSIBLE_CONFLICT
NO_CONFLICT
```

示例：

```text
brand: Rolex vs Omega
→ HARD_CONFLICT

case_diameter: 36mm vs 41mm
且双方均为高可信官方规格
→ HARD_CONFLICT

bag_size: Mini vs Medium
且双方为官方标准尺寸
→ HARD_CONFLICT

image color: black vs navy
但无可靠官方颜色
→ POSSIBLE_CONFLICT

original bracelet vs suspected aftermarket strap
→ POSSIBLE_CONFLICT

validated GTIN differs
→ 默认 POSSIBLE_CONFLICT
→ 品牌规则确认 1:1 variant mapping 时才 HARD
```

Hard Negative 长期维护：

- 同系列不同表径
- 同 Reference 家族不同盘面/材质
- 同包型不同尺寸
- 同包型不同颜色/皮质/五金
- 图片极像但 Style Code 不同
- 同名跨年份改款
- 同图被多个不同 SKU 错误复用
- GTIN/Style Code/Reference 相互冲突

---

## 10. Confidence、Abstention 与统计风险控制

### 10.1 输出状态

```text
CERTAIN_MATCH
LIKELY_MATCH
UNKNOWN
CERTAIN_NON_MATCH
```

策略：

```text
CERTAIN_MATCH
→ Recognition
→ Machine Recognized
→ Pending Review

LIKELY_MATCH
→ 不自动挂接

UNKNOWN
→ 不处理 / 补证据

CERTAIN_NON_MATCH
→ 排除当前候选
```

### 10.2 不使用单一 score 阈值

记录：

```text
match_score
evidence_score
conflict_score
source_trust_score
confidence_level
decision
policy_version
model_version
```

最终判定由：

```text
Hard Constraints
+ calibrated model score
+ evidence policy
+ source policy
+ abstention policy
```

共同决定。

### 10.3 Calibration 与上线风险认证分开

**Calibration** 解决“模型分数是否可解释”；**Risk Certification** 解决“允许自动识别的流量，真实错合并率是否低于业务上限”。

要求：

1. 阈值调参集、最终认证集分离，避免在同一 Gold Set 上边调阈值边宣称上线达标。
2. 对 `CERTAIN_MATCH` 使用单侧二项置信上界监控 False Merge Rate。
3. 若目标错合并率 <0.1%，且认证样本 0 错误，95% 单侧置信水平下约需 2995 个独立审核样本才能把上界压到约 0.1%。
4. 品类、主要来源、重点品牌至少做 slice-level 最小样本约束；不能只看全局平均。
5. 线上分布发生漂移时，旧阈值的统计结论自动失效，必须重校准/重认证。

### 10.4 Selective / Conformal Risk Control

作为 PoC 的高级策略，评估 **Conformal Risk Control / Selective Conformal Risk Control**：

目标不是让模型“更像概率”，而是直接学习：

> 在目标风险上限下，哪些样本可以回答，哪些必须 abstain。

适合本项目的 `Precision-first + UNKNOWN 合法` 业务语义。

但该能力不是万能保证：

- 经典结论依赖 exchangeability 或相应假设。
- time-shift、品牌新品、来源结构变化会破坏保证。
- 必须配合 time-based holdout、drift detection、定期重校准。
- 首版生产仍以可解释阈值 + 单侧错误率上界为主；CRC 作为提升 Coverage 的 challenger。

---

## 11. Entity Linking 与 Entity Discovery

### 11.1 Entity Linking

增量商品首先只对 `ACTIVE Canonical Variant`：

```text
Source Product
→ Hybrid Retrieval
→ Pair Match
→ Conflict
→ CERTAIN_MATCH
→ Recognition
```

适用于已有腕表主档。

### 11.2 Entity Discovery

未挂接商品进入独立发现池：

```text
Unmatched Products
→ Strict Blocking
→ High-confidence Pair Edges
→ Constraint-aware Clustering
→ PROPOSED Canonical
```

PROPOSED 升 ACTIVE 至少满足：

1. 官方/高可信主档锚点；或
2. 至少两个真正独立来源共同支持关键变体属性；
3. 无 Hard Conflict；
4. 通过主档治理/审核策略。

复制同一 feed 的多个站点不能算多个独立证据。

---

## 12. Graph Resolution 与传递冲突

禁止：

```text
Connected Component = Canonical Variant
```

因为会出现：

```text
A = B
B = C
A != C
```

推荐：

```text
Union-Find / Cluster Graph
+ Cannot-Link
+ Cluster Signature
+ Anchor Validation
```

每个 cluster 保存：

```text
brand
official identifiers
size
material
color/dial
hardware/bezel
edition/generation
valid_time_range
provenance groups
cannot_link ids
anchor_entity
```

合并前：

1. 检查 Cannot-Link；
2. 检查所有高可信 Hard Attributes；
3. 检查时间版本兼容；
4. 检查来源互斥/同一 source duplicate；
5. 新成员必须同时通过 cluster signature 与 anchor；
6. 对小 cluster 的高风险场景可补 complete-link/关键 pair 检查；
7. 有冲突即写 cannot-link，阻断后续桥接。

Pair score 只提供“合并证据”；Cluster Constraint 决定“能否合并”。

---

## 13. 商品数据库与 Web Evidence Agent

Web Agent 仅触发：

- 内部 Canonical 无候选
- 新款刚发布
- Vintage/已下架
- 型号疑似错误
- 关键属性冲突
- GTIN/Style Code/Reference 需要第三方验证

检索优先级：

```text
品牌官网
→ 官方目录/官方经销资料
→ 专业数据库
→ 高质量二手平台
→ 通用搜索
```

Agent 只能输出 Evidence：

```text
candidate_url
source_tier
source_group_id
retrieved_at
content_hash
effective_date
extracted_attributes
evidence_snippets
image_refs
```

禁止 Agent：

- 直接修改 Canonical Variant
- 直接创建 CERTAIN_MATCH
- 把搜索摘要当成事实
- 用多个复制页面伪造“多源一致”

网页是非可信输入，必须经过白名单字段、来源策略、时间版本、Conflict Engine。

---

## 14. 冷启动与数据飞轮

### 0 Labels

- 官方 Reference / Style Code / GTIN 精确规则
- Hard Constraints
- ontology / alias
- Text/Image pretrained embedding
- 高可信规则生成 Positive
- 自动构造 Hard Negative

### ~10K 审核数据

- Logistic / LightGBM
- calibration
- brand/category/source-specific thresholds
- hard-negative mining
- active sampling

### 100K+

- domain contrastive encoder
- compact cross encoder
- multimodal student
- active learning
- selective risk control

### LLM/VLM Teacher

Teacher 只对困难样本生成结构化解释/候选标签。Teacher 标签必须与规则、主档、审核结果交叉验证后，才能进入训练集。

审核流程形成：

```text
Recognition
→ Review
→ Confirmed / Rejected
→ Label Store
→ Training / Calibration / Regression Set
```

---

## 15. 数据库设计

关系数据库为事实源，推荐 PostgreSQL；已有 MySQL 基础设施也可沿用。搜索/向量交给 OpenSearch。

核心表：

```text
brand
product_family
product_model
canonical_variant
canonical_variant_version
canonical_relation

source
source_group
source_product
source_product_image

product_identifier
extracted_attribute
normalized_attribute
attribute_fact
external_evidence

candidate_pair
match_evidence
conflict_record
cannot_link
cluster_membership

recognition_record
review_record

model_version
policy_version
ontology_version
source_trust_policy
```

关键字段：

`product_identifier`

```text
entity_type
entity_id
identifier_type
identifier_value
normalized_value
market
checksum_valid
source_tier
confidence
valid_from
valid_to
```

`canonical_variant`

```text
id
brand_id
category_id
family_id
model_id
canonical_reference_or_style
variant_signature
variant_signature_version
status
current_version_id
```

`recognition_record`

```text
id
source_product_id
canonical_variant_id
decision
match_score
evidence_score
conflict_score
policy_version
model_version
ontology_version
evidence_snapshot
created_at
review_status
```

`external_evidence`

```text
id
url
source_tier
source_group_id
content_hash
retrieved_at
effective_from
effective_to
supersedes_id
payload
```

关键索引：

- `(source_id, source_product_key)` unique
- `(brand_id, category_id, normalized_reference)`
- `(brand_id, category_id, normalized_style_code)`
- `(identifier_type, normalized_value, market)`
- `(entity_type, entity_id, attribute_name)`
- `(source_product_id, review_status)`
- `(min_product_id, max_product_id)` unique
- `(left_id, right_id, conflict_type)`
- `(content_hash)`
- `(source_group_id)`

---

## 16. 离线 Batch 与增量架构

### 历史初始化

```text
Raw Import
→ Normalize / Extract / QC
→ Barcode/OCR
→ Batch Text/Image Embedding
→ Bulk OpenSearch Index
→ Link to Existing Canonical
→ Unmatched Discovery
→ Recognition / PROPOSED
```

按：

```text
category → brand → series/model bucket
```

分区运行，禁止全库 Pair。

### 每日增量

```text
New/Changed Source Product
→ Normalization
→ Identifier Lookup
→ Embedding
→ Search ACTIVE Canonical
→ Pair/Conflict
→ Recognition or UNKNOWN
→ unmatched neighbor discovery（必要时）
```

### 模型升级

- 向量不原地覆盖。
- 建 `*_vN+1` shadow index。
- 回填固定 benchmark。
- 通过 Gate 后切 alias。
- Recognition 保存当时 evidence/model/policy snapshot，支持重放和回滚。

---

## 17. 生产架构

```text
Source Connectors
      ↓
Raw/Object Storage
      ↓
Normalize & Extract
 ┌────┼───────────────┐
 ↓    ↓               ↓
Text  Barcode/OCR   Image Prep
 └────┼───────────────┘
      ↓
Catalog DB / Evidence Store
      ↓
OpenSearch 3.8 Hybrid Retrieval
      ↓
Candidate Pool
      ↓
Reranker / Pair Matcher
      ↓
Conflict Engine
      ↓
Calibration + Risk Policy
      ↓
┌──────────────┬──────────────┐
↓              ↓              ↓
NON_MATCH    UNKNOWN      CERTAIN_MATCH
                              ↓
                       Graph Resolution
                              ↓
                      Recognition Record
                              ↓
                       Pending Review
                              ↓
                         Label Store
```

建议组件：

- Catalog DB：PostgreSQL 16+ / 现有 MySQL
- Hybrid Search：OpenSearch 3.8
- Object Storage：S3 compatible
- Queue：Kafka / Pulsar / 企业 MQ
- Batch：Python/Ray；已有 Spark 则用 Spark
- Encoder Serving：Triton / 自建 PyTorch 服务
- VLM Serving：vLLM / SGLang，按模型支持情况验证
- Cache：Redis（可选）

---

## 18. 成本与性能

### 18.1 成本原则

1. Exact/规则最便宜，优先。
2. Embedding 入库时批算，查询复用。
3. Reranker 只看几十个候选。
4. VLM/Web Agent 只走少量困难样本。
5. 不持久化全量 Pair。
6. 原图放对象存储，搜索引擎只留元数据/向量。

### 18.2 向量容量

公式：

```text
RawVectorBytes =
N × dim × bytes_per_element × vector_families
```

参考量级：

```text
10M × 512 × FP16
= 10.24 GB / 每个单向量族
```

若 10M 商品平均保留 3 张可用图片，并把图片向量统一投影/存成 512d FP16：

```text
Text:   10M × 512 × 2B = 10.24 GB
Images: 30M × 512 × 2B = 30.72 GB
合计原始向量 ≈ 40.96 GB
```

这**不含** ANN graph、量化索引、metadata、segment overhead、replica。副本数为 1 时仅原始向量数据就约翻倍。OpenSearch on-disk/quantization 主要用于降低内存压力，磁盘仍需为全精度/量化数据和索引留足空间。

### 18.3 在线推理公式

```text
DailyEmbeddingGPU =
changed_items × avg_images × encoder_cost

PairInference =
daily_items × avg_candidates_after_rerank

VLMInference =
daily_items × difficult_case_rate
```

### 18.4 PoC 性能目标

- 普通增量路径 p95 <500ms，不含异步 Web/VLM。
- expensive-model rate 初始 <5%。
- 1000 万规模必须实测 filtered ANN：
  - Recall@K
  - QPS
  - p95/p99
  - build/rebuild time
  - RAM/disk
  - recovery time
  - quantization loss

---

## 19. Benchmark 与测试集

### 19.1 Gold Set

首版 5K–10K pair，主动提高 Hard Negative 比例。

分层：

```text
watch / bag
source
brand
seen SKU / unseen SKU
current / vintage
with image / no image
with model / missing model / dirty model
clean / conflicting
with identifier / no identifier
```

### 19.2 时间切分

必须有 time-based holdout：规则/训练只看较早数据，测试用后续出现的新 SKU。

### 19.3 腕表 Corner Cases

- 同系列不同尺寸
- 同型号不同盘面
- 同型号不同材质
- Reference 标点/后缀变化
- Reference 部分缺失
- 型号写错
- 同图不同文本
- 同文不同图
- 限量版
- 改款/老款/新款
- 后换表带
- GTIN 与 Reference 冲突

### 19.4 箱包 Corner Cases

- Mini / Small / Medium
- 不同皮质
- 不同颜色
- 不同五金
- 不同印花
- 季节限定
- 同名不同年份
- 官方命名变化
- 卖家俗称
- 局部图
- 无图
- date code 被误当 Style Code
- GTIN/UPC 与 Style Code 冲突

### 19.5 指标

Retrieval：

```text
Recall@20/@50/@100
MRR
candidate_count
latency
```

Pair：

```text
Precision
Recall
PR-AUC
Calibration Error
Coverage @ target precision
```

Cluster：

```text
Cluster Purity
False Merge Rate
Split Rate
Constraint Violation
Bridge Merge Count
```

Production：

```text
Coverage @ 99.9% Precision
False Merge per Million
UNKNOWN Rate
VLM/Web Rate
cost per recognized item
risk upper bound
```

---

## 20. MVP → Production

### Phase 0：数据审计与 Gold Set

交付：

- 字段完整度
- Alias/Ontology
- Hard Conflict Matrix
- Identifier 可用率/条码可读率
- 来源可信度与 syndication group
- 5K–10K benchmark

### Phase 1：确定性腕表 Linking

- Reference 解析
- GTIN/EAN/UPC lookup（有则用）
- Exact Retrieval
- Hard Conflict
- Recognition/Review

先证明 99.9% Precision 闭环。

### Phase 2：Hybrid Text + Pair Matcher

- BM25
- Qwen3-Embedding-0.6B
- Qwen3-Reranker-0.6B
- 箱包主路径
- Calibration

### Phase 3：Image / Barcode / OCR / Multimodal

- zxing-cpp
- PP-OCRv6
- PaddleOCR-VL-1.6 document fallback
- SigLIP2
- DINOv3 challenger
- Qwen3.5-4B difficult attribute extraction
- Qwen3-VL multimodal retrieval challenger

### Phase 4：Entity Discovery + Web Evidence

- unmatched graph
- constraint-aware clustering
- source-independence
- temporal versioning
- PROPOSED Canonical
- Vintage / 新款 Evidence Agent

### Phase 5：Supervised Student + Continuous Learning

- review labels
- hard negative mining
- active learning
- domain encoder/matcher
- selective risk control
- shadow/A-B/rollback

---

## 21. 正式开启自动识别的 Gate

所有条件同时满足：

1. 主要品类/来源 `Precision@Auto-Match >= 99.9%`。
2. `Recall@100 >= 99.5%`。
3. 有足够独立认证样本支撑目标错误率上界。
4. 所有 Hard Conflict 回归用例 0 自动错合并。
5. Seen/Unseen、Vintage、缺图、缺型号、脏型号分别达标。
6. 新品/time-shift holdout 达标。
7. 每个品牌/来源/品类有阈值或 kill switch。
8. 模型/规则/ontology/search index 均可回滚。
9. Recognition 可重放完整证据。
10. Source drift / model drift 超阈值时自动降级为 UNKNOWN 或关闭 Auto-Match。

任何指标退化时，优先降低 Coverage，而不是放宽 Hard Constraint。

---

## 22. PoC 实验矩阵

| 问题 | Baseline | Challenger | 决策指标 |
|---|---|---|---|
| Text Embedding | Qwen3-Embedding-0.6B 512d | 256/384d / 4B | Recall@100 / RAM / latency |
| Text Rerank | Qwen3-Reranker-0.6B | structured GBDT | Precision / latency |
| Image Semantic | SigLIP2-B/16 | 其他可商用 VLM encoder | hard-case Recall@K |
| Fine-grained Vision | SigLIP2 | DINOv3 ViT-B/16 | variant hard-negative Recall/Precision |
| Multimodal Retrieval | text+image late fusion | Qwen3-VL-Embedding-2B | Coverage@99.9 / GPU |
| Difficult Pair | structured + text | Qwen3.5 / Qwen3-VL reranker | hard-negative precision |
| OCR | PP-OCRv6 | PaddleOCR-VL-1.6 | identifier extraction F1 / cost |
| Vector Store | OpenSearch 3.8 on-disk | Milvus | Recall/QPS/RAM/disk/ops |
| Cluster | constrained union + anchor | naive connected component | false merge / violations |
| Risk Policy | calibrated threshold | CRC/selective CRC | certified coverage / false merge |

复杂模型只有在内部 benchmark 证明提高 `Coverage @ 99.9% Precision`、降低成本或改善 Unseen/Vintage/缺字段场景时才进入生产。

---

## 23. 主要技术依据

- Amazon Science / VARM：Variant Relationship 与普通 Entity Resolution 必须分开建模，并显式识别 variation attributes。  
  https://www.amazon.science/publications/learning-variant-product-relationship-and-variation-attributes-from-e-commerce-website-structures

- Walmart Global Tech：Catalog 属性抽取与 Quality Check 分阶段。  
  https://tech.walmart.com/content/walmart-global-tech/en_us/news/articles/how-walmart-uses-llms-to-manage-its-massive-product-catalogs.html

- Microsoft Research：Unstructured Offer → Structured Product 与 Product Synthesis。  
  https://www.microsoft.com/en-us/research/publication/matching-unstructured-product-offers-to-structured-product-descriptions/  
  https://www.microsoft.com/en-us/research/publication/synthesizing-products-for-online-catalogs/

- WDC Products：现实电商 Entity Matching、corner cases、unseen entities。  
  https://github.com/wbsg-uni-mannheim/wdcproducts

- End-to-end multi-modal product matching in fashion e-commerce：生产商品匹配中多模态与分布变化。  
  https://arxiv.org/abs/2403.11593

- GS1 GTIN Management：不同尺寸/颜色等贸易项目变体的唯一标识原则，以及 GTIN 非复用规则。  
  https://www.gs1.org/1/gtinrules/en/  
  https://support.gs1.org/support/solutions/articles/43000734083-how-many-gs1-gtins-i-need-when-i-have-a-product-with-many-sizes-and-colours-  
  https://support.gs1.org/support/solutions/articles/43000734390-can-a-gtin-be-reused-

- zxing-cpp：EAN/UPC、Code128、Data Matrix、QR 等条码读取。  
  https://github.com/zxing-cpp/zxing-cpp

- PaddleOCR：PP-OCRv6 与 PaddleOCR-VL-1.6。  
  https://github.com/PaddlePaddle/PaddleOCR

- Qwen3 Embedding / Reranker。  
  https://github.com/QwenLM/Qwen3-Embedding

- Qwen3.5：统一多模态基础模型；4B 用于成本受控的困难属性抽取 PoC。  
  https://github.com/QwenLM/Qwen3.6

- Qwen3-VL Embedding / Reranker。  
  https://github.com/QwenLM/Qwen3-VL-Embedding

- Google SigLIP2。  
  https://github.com/google-research/big_vision

- Meta DINOv3。  
  https://github.com/facebookresearch/dinov3

- OpenSearch 3.8 / Vector Search。  
  https://github.com/opensearch-project/OpenSearch/releases/tag/3.8.0  
  https://docs.opensearch.org/latest/vector-search/optimizing-storage/disk-based-vector-search/

- Conformal Risk Control。  
  https://arxiv.org/abs/2208.02814

- Selective Conformal Risk Control。  
  https://arxiv.org/abs/2512.12844

---

## 24. 最终生产原则

生产系统固定遵循：

```text
官方知识 / 标准化 / 确定性标识
          ↓
高召回 Hybrid Retrieval
          ↓
结构化 + 文本 + 图片多证据
          ↓
Hard Conflict 安全墙
          ↓
校准 + Abstention + 风险策略
          ↓
约束图实体解析
          ↓
仅 CERTAIN_MATCH 自动 Recognition
          ↓
审核反馈形成训练与认证数据
```

核心原则：

1. **Identity first，Similarity second**：相似不等于相同。
2. **Hard Conflict 优先于任何模型高分**。
3. **UNKNOWN 是合法且必要的生产输出**。
4. **所有证据必须可追溯、可版本化、可重放**。
5. **多个 URL 不等于多个独立来源**。
6. **新 SKU / Vintage / time-shift 必须单独评测**。
7. **模型升级必须在 shadow benchmark 证明增益**。
8. **自动匹配阈值由目标 False Merge Risk 决定，而不是固定 score**。
9. **能用规则、标识符、条码、OCR 解决的问题，不交给大模型**。
10. **任何复杂度只在提高 Coverage@99.9% Precision 或降低总成本时才有价值**。
