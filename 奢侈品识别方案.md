# 奢侈品识别方案

## 1. 目标与边界

系统目标不是“找最像的商品”，而是把雷小安、奢当家、腕表之家、小迈步、爱回收等来源的记录归一到**同一个官方商品变体（Official Variant）**。

第一阶段覆盖腕表与箱包，历史数据约 100 万–1000 万条。腕表已有部分品牌/系列/型号标准库；箱包主档不完整，因此系统必须同时支持 **Entity Linking** 与 **Entity Discovery**。

核心原则：

> **Precision >> Recall。宁可输出 UNKNOWN，也不能把不同官方变体错合并。**

只有定义官方变体的关键属性兼容时，才允许自动认定 `SAME_VARIANT`。系统不做真假鉴定；成色、价格、库存、卖家、单件序列号、保卡状态等属于 Offer / 单件属性，不定义官方商品变体。

机器成功识别后必须形成一致业务结果：

```text
Source Product 当前状态
+ Source → Canonical 当前识别关系
+ Immutable Recognition Record
+ Pending Review 任务/事件
```

这四个副作用必须原子、幂等、可重放，不能出现“模型判断正确但业务状态只写了一半”。

---

## 2. 核心实体、关系与标识符语义

```text
Brand
  └── Product Family / Series
       └── Product Model
            └── Canonical Variant
                 └── Source Product / Offer
```

关系至少包括：

```text
SAME_VARIANT
VARIANT_OF
SAME_MODEL
SAME_SERIES
VISUALLY_SIMILAR
UNKNOWN
```

只有 `SAME_VARIANT` 可以生成自动 Recognition。

Canonical Variant 状态：

```text
ACTIVE         已验证，可参与自动挂接
PROPOSED       新发现，等待证据/治理
SPLIT_PENDING  疑似一个 Canonical 混入多个 Variant，冻结新自动挂接
MERGE_PENDING  疑似多个 Canonical 实为同一 Variant，冻结相关实体新自动挂接
RETIRED        历史实体，不再接受新自动挂接
MERGED         已治理合并到唯一 surviving Canonical
SPLIT          已确认 one-to-many 拆分，仅保留历史身份与 lineage
TAINTED        关键事实污染，暂停自动挂接
```

`MERGED` 是 many-to-one，可 redirect 到唯一 survivor；`SPLIT` 是 one-to-many，旧 Canonical 不能自动 redirect 到任一 child，必须重新解析。

### 2.1 Identifier Namespace

统一采集：

```text
reference_full / style_code / mpn
gtin / ean / upc
merchant_sku
product_group_id / item_group_id
variant_dimension_keys / variant_dimension_values
barcode_symbology / payload / checksum
market
identifier_namespace / identifier_owner / identifier_scope
identifier_source / source_field_path / confidence
```

规则：

- 高可信完整 Reference / Style Code 精确一致：最强 Identity Anchor 之一。
- 官方品牌来源中的 `mpn` 经品牌规则/格式验证后可作为 Manufacturer-grade Identifier。
- 第三方商家 `sku` 默认属于 merchant namespace，不得直接升级为官方 Reference / Style Code。
- `productGroupID / item_group_id / inProductGroupWithID` 是父商品组/变体组关系标识，不是 Variant Identity Anchor。
- `ProductGroup.hasVariant / Product.isVariantOf / variesBy / variant_option` 是高价值 Variant Boundary Evidence。
- 高可信、校验通过的 GTIN/EAN/UPC：Strong Positive + Exact Retrieval Key。
- 不同 GTIN 默认 `POSSIBLE_CONFLICT`；只有品牌/市场规则证明该语境下 GTIN 与本系统 Variant 1:1 时才升级 Hard。
- Vintage 必须考虑旧编码、地区站点差异、包装变化与历史规则。

维护 `identifier_semantics_policy`，最小粒度：

```text
brand × market × source_group × field_path × identifier_type
```

策略声明 `MANUFACTURER_ID / MERCHANT_ID / GROUP_ID / INSTANCE_ID / UNKNOWN`，并声明是否允许 Exact Retrieval、Strong Positive、Hard Conflict、Auto Activation。

---

## 3. 标准 Schema 与 Hard Constraint

每个属性事实保存：

```text
raw_value
normalized_value
canonical_value_id
extractor / extractor_version
source_type / source_id / source_url
evidence_span / image_bbox
confidence / validation_status
effective_from / effective_to
observed_at / content_hash
market_scope / region_scope
```

`observed_at` 与有效期分离；新证据不覆盖旧事实，而用 version / supersedes 表达变化。只有双方属性都达到对应可信度门槛时才形成 Hard Conflict；低置信 OCR/VLM 冲突只能触发 `POSSIBLE_CONFLICT → UNKNOWN`。

### 3.1 腕表

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| series / family | Blocking + Strong；品牌规则可 Hard |
| subseries / model | Strong |
| official_reference_full | Hard；最强身份字段之一 |
| reference_base | Retrieval / Strong，默认不能代替 full |
| gtin/ean/upc | Strong；条件 Hard |
| case_diameter_mm | Hard |
| case_material | Hard |
| dial_color | Hard |
| dial_type | 定义独立变体时 Hard |
| bezel | 定义独立变体时 Hard |
| bracelet_or_strap | 条件 Hard；需识别后换件 |
| caliber / movement | 代际/变体区分时 Hard，否则 Strong |
| functions | 变体区分时 Hard |
| edition / generation | Hard |
| serial_number | 单件属性 |
| condition / price | Offer 属性 |

Reference 保存 `raw / normalized / base / full / parts / brand_rule_version`。默认以 `reference_full` 为准；只有品牌规则明确证明后缀不定义 Variant 时才允许 base reference 参与自动合并。

### 3.2 箱包

| 字段 | 默认策略 |
|---|---|
| brand | Hard |
| family / series | Blocking + Strong |
| model / style | Strong |
| official_style_code | Hard；最强身份字段之一 |
| gtin/ean/upc | Strong；条件 Hard |
| official_size | Hard |
| material | Hard |
| official_color | Hard |
| hardware | Hard |
| pattern / print | Hard |
| edition / collaboration | Hard |
| season / year / generation | 官方由其区分时 Hard |
| serial / date_code | 单件/生产证据 |
| condition / price | Offer 属性 |

图片颜色不直接成为 Hard Constraint；灯光、白平衡、老化、修图会造成偏差，视觉颜色只做 Soft Evidence。

### 3.3 两级标准化

```text
material_group = ROSE_GOLD
material_brand_value = ROLEX_EVEROSE_GOLD

color_group = BLACK
color_brand_value = CHANEL_NOIR
```

通用层用于跨平台召回，品牌精确层用于 Variant 判定，避免把品牌专有语义压平。

---

## 4. Evidence、来源独立性、事实状态与时间版本

来源等级：

```text
T0  品牌官网、官方目录、官方标识体系
T1  已验证内部标准库、授权/专业数据库
T2  高质量专业平台、可信二手平台
T3  普通电商、聚合站、搜索网页
```

可信度按“来源 × 属性 × 市场/时间”定义，而不是整站一刀切。

### 4.1 来源独立性

维护：

```text
source_group_id
syndication_group_id
content_fingerprint
image_hash_group_id
upstream_hint
```

多个 URL 不等于多个独立来源。复制同一 feed、相同/近重复图片、相同异常文案、相同字段错误不能增加 `independent_source_count`。

### 4.2 Evidence Family

```text
IDENTIFIER        Reference / Style Code / MPN / GTIN
STRUCTURED_ATTR   size / material / color / hardware / movement
VARIANT_GRAPH     ProductGroup / isVariantOf / variesBy
LEXICAL_TEXT      BM25 / token overlap
SEMANTIC_TEXT     text embedding / reranker
IMAGE_VISUAL      image embedding / patch feature / perceptual hash
OCR_TEXT          image → OCR → identifier/attribute
EXTERNAL_WEB      外部网页证据
TEMPORAL          year / generation / effective period
```

同一原始观测派生出的多个分数不是多个独立证据：同一标题的 BM25+Embedding 不算两票；同一图片的 pHash/SigLIP/DINO/VLM 不算多票；同一网页的 JSON-LD/DOM/摘要默认属于同一 evidence origin。

Student Matcher 可使用全部特征学习联合分布，但 `CERTAIN_MATCH` 风险策略不得把相关特征机械相加解释为多个独立支持。

### 4.3 Fact Promotion Barrier

```text
OBSERVED      原始可追溯观测
DERIVED       规则/模型/聚类推导
REVIEWED_GOLD 人工确认标签
GOVERNED_FACT 主档治理确认事实
APPROVED_ALIAS 已批准别名/标识符映射
TAINTED       已知被污染
```

`Recognition`、Matcher 分数、Cluster membership、模型抽取/伪标签都是 DERIVED Artifact。未经 Review/Governance Promotion，不能直接进入 Exact Identity 索引、Hard Constraint 主档、官方 Alias、Identifier Semantics 或 Gold Training Set。

任何 Exact Alias、Canonical Fact、Reviewed Gold Label 必须能沿 lineage 回溯到 OBSERVED/REVIEWED/GOVERNED 上游；DERIVED→DERIVED 不能完成事实晋升。

### 4.4 时间版本

Canonical 属性保存 `valid_from / valid_to / version / evidence_ids`。用于同名跨年份改款、Reference 规则变化、新旧机芯/五金/尺寸变化和 Vintage 回溯。没有明确代际证据时，不跨年代自动合并。

### 4.5 Canonical Attribute Fact Arbitration

`attribute_fact` 是**事实断言集合**，不是“当前主档值”本身。系统必须显式区分：

```text
Attribute Assertion
  某个来源在某个时间/市场对属性作出的观测或治理断言

Canonical Attribute Resolution
  在明确 scope 下，经规则仲裁后可供 Matching/Search/Hard Conflict 使用的 Current Fact Projection
```

仲裁主键至少为：

```text
canonical_variant_id
× attribute_id
× market_scope
× valid_time_scope
× variant_generation
```

不能把不同市场、不同年代、不同代际的值直接放进一个投票池。

每个属性定义 `attribute_semantics_policy`：

```text
cardinality = SINGLE_VALUE / SET_VALUE / ORDERED_MULTI / RANGE
identity_role = HARD / STRONG / SOFT / OFFER_ONLY
normalization_level = GENERIC / BRAND_PRECISE
allows_regional_variation = true/false
allows_temporal_change = true/false
specificity_order / unit_policy / comparison_policy
```

Resolution 状态：

```text
RESOLVED      当前 scope 有唯一可服务值
MULTI_VALID   属性语义允许多个值，且多值均被验证
CONFLICTED    同 scope 存在无法解释的高可信互斥值
UNKNOWN       没有足够证据形成 Current Fact
STALE         原 Current Fact 的支撑证据已失效/撤回，等待重算
```

固定规则：

1. **不采用简单 latest-write-wins。** 新观测时间更晚，不代表事实一定更正确；`observed_at` 只说明何时看到，`valid_from/valid_to` 才描述事实何时有效。
2. **不采用简单 majority vote。** 多个转载/同 feed 来源按 `source_group/syndication_group/content_fingerprint` 去重；同源复制不能压过一个独立官方来源。
3. **Authority 按字段与 scope 定义。** T0 官方来源通常优先，但只有在品牌、市场、Variant、有效期和字段语义均匹配时才可提升权威；官网 JSON-LD 与可见 DOM 冲突仍是 Conflict，不因“官网”自动选一边。
4. **高可信冲突不做平均。** 若同一 scope 的 SINGLE_VALUE Hard Attribute 出现两个经过治理、互斥的高可信值，Resolution=`CONFLICTED`；Canonical 立即停止依赖该字段的 Auto-Match。若冲突可能说明 cluster over-merge，触发 `SPLIT_PENDING`/repair candidate；若更像事实污染，进入 `TAINTED`/fact conflict governance。模型或频率都不能自动裁决。
5. **低可信冲突不能覆盖高可信 Current Fact。** T2/T3、OCR/VLM/模型抽取与已治理 T0/T1 冲突时，只触发 `revalidation_required` / Evidence review；除非上游事实被撤回或治理确认，不直接替换 Current Fact。
6. **保留粒度，不向更粗值退化。** 例如 `Rolex Everose Gold` 与通用 `Rose Gold` 同属一个 material group 时，可以同时保留 Generic + Brand-Precise 两层；更常见的粗粒度值不能自动覆盖已验证的更精确品牌语义。
7. **缺失不是反证。** 某来源没有颜色/尺寸/材质字段，只表示 missing；不能把 Current Fact 清空，也不能形成 DIFFERENT。
8. **多值属性按语义比较。** `functions`、组合材质等若允许 SET_VALUE，按集合规则做兼容/包含/冲突；不能复用 SINGLE_VALUE 的“值不同即冲突”。
9. **撤回与失效可重算。** 支撑 Current Fact 的 Evidence 被撤回、过期、TAINT 或 parser bug 影响时，Resolution 进入 `STALE` 并重新仲裁；旧 Resolution 不删除，写 `superseded_by_resolution_id`。
10. **Current Fact 是版本化投影。** 每次仲裁保存 `resolution_policy_version / input_assertion_ids / source_independence_snapshot / resolution_reason / confidence_class / effective scope`，可完全重放。
11. **Search 与 Matching 只消费 Resolution。** OpenSearch 的 Hard Attribute、Exact/filter 字段、Conflict Engine 的 Canonical 值，默认来自 `RESOLVED/MULTI_VALID` 的 Canonical Attribute Resolution；未经仲裁的原始 assertion 只能作为 Evidence，不得直接进入 Current Canonical Projection。
12. **Publication Barrier 覆盖 Fact Resolution。** Identity-critical Resolution 变化必须生成 Canonical mutation/outbox，并与对应 Search projection 在同一 `publication_id` 下生效，避免 DB Current Fact 与 Search Hard Filter 使用不同版本。

高风险治理路径：

```text
New/Changed Attribute Assertions
→ Normalize + Validate + Source Independence
→ Attribute Arbitration
→ RESOLVED / MULTI_VALID / UNKNOWN
             or
           CONFLICTED
              ↓
   Fact Conflict Governance
      ↙              ↘
Fact correction     Cluster repair candidate
      ↓              ↓
TAINT/Supersede   SPLIT_PENDING / review
      └──────→ Re-arbitrate → Publication
```

这一步的目标是保证：**主档中的“当前值”是可解释的仲裁结果，而不是抓取顺序、来源数量或某个模型输出的偶然产物。**

---

## 5. 属性抽取、OCR 与 VLM

```text
Raw Source Product
→ Schema Mapping
→ Deterministic Parsing / Dictionary
→ Barcode Decode
→ Text Attribute Extraction
→ Scene OCR
→ Document-like OCR/VLM（按需）
→ General VLM Fallback（少量困难样本）
→ Normalization / Ontology Mapping
→ Attribute Validation / Quality Check
→ Attribute Assertion
→ Canonical Attribute Arbitration（仅 Canonical Fact 路径）
→ Normalized Product Profile / Current Fact Projection
```

优先级：规则/主档 → 条码 → 文本 → Scene OCR → 文档 OCR/VLM → General VLM → Quality Check。能用确定性方法解决的问题不交给大模型。

### 5.1 Barcode / OCR

条码先用 zxing-cpp 等确定性 decoder 解析 EAN/UPC/Code128/Data Matrix/QR，经 check digit、格式、品牌/市场校验后进入 Exact Retrieval。

PoC Scene OCR 基线：PP-OCRv6。OCR 输出分类：

```text
reference / style_code
gtin/ean/upc
serial_number
date_code
size
material_text
other_text
```

保卡整页、历史目录、复杂表格等 document-like 场景使用 PaddleOCR-VL-1.6 / PP-Structure 类能力作为 fallback。

### 5.2 General VLM

PoC 困难样本结构化抽取基线：Qwen3.5-4B。职责是产生**可验证候选证据**，不是最终身份判断。VLM 输出必须经过 ontology、品牌规则、主档、OCR/文本交叉验证；无权直接写 ACTIVE Canonical、直接形成 Current Fact 或直接输出 `CERTAIN_MATCH`。

---

## 6. EvidenceProfile、Router 与缺模态 Route Policy

每条 Source Product 先计算：

```text
identifier_quality = NONE / WEAK / STRONG
text_quality       = NONE / WEAK / STRONG
image_quality      = NONE / WEAK / STRONG
hard_attr_coverage = 0..1
conflict_state     = NONE / POSSIBLE / HARD
catalog_mode       = LINK / DISCOVERY
synthetic_evidence = true / false

brand_route_state    = AUTHORITATIVE / MULTI_CANDIDATE / UNKNOWN / CONFLICTING
category_route_state = AUTHORITATIVE / MULTI_CANDIDATE / UNKNOWN / CONFLICTING
routing_scope        = LEAF / ANCESTOR / MULTI_ROUTE / BROAD_FALLBACK
```

| 场景 | 主要召回 | 自动判定上限 |
|---|---|---|
| 强 Reference/Style/GTIN + Hard Attr 无冲突 | Exact + 属性过滤 | 可 CERTAIN_MATCH |
| 文本强、无图片 | Exact/BM25/Text ANN/属性 | 可 CERTAIN_MATCH；无图不是负证据 |
| 图片强、文本弱、无可靠型号 | Image ANN + OCR + 视觉属性 + 少量 VLM | 默认不能仅视觉 CERTAIN_MATCH |
| 文本强 + 图片强 | 多路召回 + Rerank | 可 CERTAIN_MATCH |
| 型号疑似错误 | raw/normalized identifier 双路 + 非 identifier 召回 | 冲突时 UNKNOWN/NON_MATCH |
| 文本与图片冲突 | 模态独立保留 | POSSIBLE_CONFLICT → UNKNOWN |
| 无图无可靠型号、结构化属性较全 | BM25/Text ANN/Attribute | 只有经认证的高区分组合才自动，否则 UNKNOWN |
| 只有泛化标题、缺 Hard Attr | BM25/Text ANN | UNKNOWN |
| Vintage / 新款无 Canonical | Neighbor + Official/Web Evidence | PROPOSED |

Synthetic Evidence 不能形成 Hard Constraint、Identity Anchor 或官方事实；图→文本只可做 query expansion/候选提示；文本→图像默认不进入身份链路。

低质量图片在 Image ANN 前独立评估 blur、resolution、subject_area_ratio、occlusion、background_complexity、watermark、crop_type、color_cast，并降低视觉权重/候选配额。

### 6.1 Router / Filter Safety

Brand/Category Router 的作用是**缩小候选空间和控制成本**，不是身份事实源。路由错误若被当作 hard filter，会在 BM25/ANN 前删除正确 Canonical，随后被误解释为 NIL 或新品，因此路由必须分级：

```text
HARD_FILTER     只有权威、可验证路由证据
MULTI_ROUTE     多个 plausible brand/category route 各保留 quota
ANCESTOR_ROUTE  leaf 不确定时退到 taxonomy ancestor
BROAD_FALLBACK  路由未知/冲突时在更宽 scope 召回
```

固定规则：

1. Exact Identifier Retrieval 永远绕过 Router；Manufacturer-grade Reference/Style/MPN/GTIN lookup 不因预测 brand/category 不一致而被提前过滤。
2. 只有 T0/T1 或经过历史质量认证的结构化 brand/category、或已验证 Identifier→Brand/Category 映射，才允许作为 `HARD_FILTER`。
3. 模型分类、VLM 推断、OCR 猜测、低可信 merchant category、标题推断只能做 `MULTI_ROUTE/ANCESTOR_ROUTE/BROAD_FALLBACK`。
4. Hierarchical category leaf 不稳定时优先退到 ancestor；多个高概率 sibling route 各保留 minimum quota。
5. Brand/category 多证据冲突时保留各 plausible route，并把冲突作为 Pair/Conflict 特征。
6. 图片可补充文本分类，但视觉分类只改变 route candidate 权重，不自动升级为权威品牌/品类事实。
7. 每次查询保存 `routing_decision`：候选 path、confidence、evidence source、filter mode、router/model version、pre/post-filter candidate counts、fallback reason。
8. OpenSearch efficient k-NN filtering 只能解决“给定 filter 正确时”的检索效率，不能修复错误 filter 语义；Router semantic recall 与 Engine filtered-ANN recall 分开评测。

---

## 7. Candidate Retrieval、Fusion 与 Open-world Guard

100 万–1000 万记录禁止 N²。

召回通道：

```text
Exact Full Reference / Style Code / Manufacturer MPN
Exact GTIN/EAN/UPC
Brand + normalized model/reference
Brand + series/model + Hard Attributes
BM25
Text ANN
Image ANN
Image exact/perceptual hash
Historical Canonical Alias
Unmatched Entity-Discovery Neighbors
```

PoC 初始 K：Exact≤20、BM25 30、Text ANN 50、Image ANN 每图30/总≤60、Attribute 30；union≤150，Cheap Rerank≈30，Expensive Matcher 10–20。最终由域内 benchmark 调参。

Retrieval Gate：

```text
Recall@100 after fusion/truncation >= 99.5%
Exact-candidate survival rate = 100%
router_pre_filter_positive_recall >= 99.9%
router_post_filter_positive_recall >= 99.5%
```

### 7.1 Fusion

- 不同通道 raw score 不直接相加。
- Exact/Identity 候选永不被 Soft Fusion 截断。
- 零/少标签阶段用 RRF 作为 Soft Retrieval 基线。
- 截断前保留各通道 minimum quota。
- Exact ∪ RRF 后再 Cheap Rerank。
- 有足够 judgment 后再比较 normalized weighted fusion / learned fusion。
- Fusion、quota、index、embedding、routing policy 全部版本化。

### 7.2 Search Engine

生产基线：OpenSearch 3.8，统一承载 Exact/filter、BM25、filtered k-NN、hybrid retrieval、RRF/score normalization、on-disk vector、quantization/rescore、versioned index/alias。

Milvus 只在 10M 真实压测证明 OpenSearch Recall/QPS/资源成本不满足时拆分；pgvector 适合小规模 PoC/内部工具。

过滤策略必须按 `filter_selectivity × K × latency × recall` 基准选择；post-filter 可能返回显著少于 K 的结果，不能将“返回少”误解释为 Catalog 没有候选。

Search 中用于 Variant identity 的 canonical Hard Attribute 字段必须来自当前 publication 下 `RESOLVED/MULTI_VALID` 的 Canonical Attribute Resolution；原始未仲裁 assertion 不进入 identity-critical filter。

### 7.3 NIL / None-of-the-Candidates

检索 Top-1 不等于正确 Canonical 一定存在。

`CandidateSetAssessment` 保存：

```text
best_candidate_id / best_score_calibrated
second_candidate_id / second_score_calibrated
score_margin / near_tie_count
candidate_set_state
none_of_candidates_score
nil_reason
retrieval_health_snapshot
routing_decision_id / route_bypass_assessment_id
policy_version
```

状态：

```text
KNOWN_CANDIDATE
AMBIGUOUS_KNOWN
POSSIBLE_NIL
RETRIEVAL_FAILURE
ROUTING_AMBIGUOUS
```

near-tie 且关键 Variant 属性缺失 → `UNKNOWN_AMBIGUOUS`；候选整体低且 Retrieval 健康 → 先进入 Route/Filter Recovery；搜索/索引/route 不健康 → `UNKNOWN_SEARCH_FAILURE`，不得误判新品。

### 7.4 Route/Filter Recovery Before NIL

任何会进入 `POSSIBLE_NIL → Discovery` 的请求，只要缺少 Strong Identifier 或 Router 不是 `AUTHORITATIVE`，必须先做一次 `route_bypass_assessment`：

```text
Current routed candidate set
→ Remove disputed model-inferred hard filters
→ Search taxonomy ancestor / plausible sibling routes
→ Broad BM25/Text ANN quota
→ Image ANN / exact hash when available
→ Compare recovered candidates against original set
```

规则：

1. broad/ancestor/multi-route 搜索恢复出可安全匹配的已有 Canonical，重新进入 Pair/Conflict，不得创建新 Canonical。
2. 恢复出高风险 near-tie 但证据不足，输出 `UNKNOWN_ROUTING_AMBIGUOUS`。
3. 只有 Router、Search、Publication 均健康，且 bypass 后仍无安全已有候选时，才允许 `UNKNOWN_POSSIBLE_NIL`。
4. `canonical_novelty_assessment` 必须包含 route-bypass 结果。
5. 生产监控区分 `false_nil_due_to_router`、`candidate_recovered_by_bypass`、`hard_filter_without_authoritative_evidence`。

---

## 8. Embedding、Visual Search 与局部复核

### 8.1 Text

PoC：Qwen3-Embedding-0.6B，先测 512d，再比较 256/384d。文本同时保存结构化稳定模板与原始标题/描述表示。

Text Reranker：Qwen3-Reranker-0.6B，仅对 Top-N 使用。

### 8.2 Image

语义/跨模态基线：SigLIP2-B/16 384；细粒度 challenger：DINOv3 ViT-B/16。DINOv3 使用自定义许可证，生产前必须 Legal/Compliance Gate；无法接受时测试 DINOv2/其他可商用方案。

每图独立保存向量，多图聚合使用 `max_similarity / topk_mean / front/detail similarity / image_quality_weight`。完全相同/近重复图片不能单独证明 `SAME_VARIANT`。

Qwen3-VL-Embedding-2B / Reranker-2B 作为多模态 challenger；只有显著提高 `Coverage @ 99.9% Precision` 且成本可接受才进入困难路径。

### 8.3 Local/Patch Visual Verify

全局向量负责高召回；盘面刻度、表圈、刻字、五金、走线、印花等近邻差异在 Top-10～20 shortlist 做局部复核：

```text
Global Image ANN
→ Region Selection
→ Patch / Local Descriptor
→ Local Rerank / Correspondence
→ local_visual_score / patch_match_coverage
→ Pair Decider + Ambiguity Guard
```

局部视觉仍属于 `IMAGE_VISUAL`，不得成为 Identity Anchor，也不能覆盖 Hard Conflict。

---

## 9. Pair Matcher、Conflict Engine 与 Decision Policy

```text
Candidate Pair
→ Pre-Conflict Check
→ Cheap Deterministic Features
→ Structured / Text Matcher
→ Multimodal Difficult-Pair Matcher（少量）
→ Local Visual Verify（near-tie）
→ Post-Conflict Check
→ Calibration
→ Evidence-Family Risk Policy
→ Candidate-set / NIL / Ambiguity Guard
→ Decision
```

### 9.1 CERTAIN_MATCH

至少满足：

```text
NO HARD_CONFLICT
AND CandidateSetState == KNOWN_CANDIDATE
AND Canonical Identity-critical Attribute Resolution != CONFLICTED/STALE
AND
(
  Strong Identity Anchor
  OR
  Calibrated Matcher >= tau_segment
  + >=2 个非重复 Evidence Family 一致支持
  + Ambiguity Guard 通过
)
```

Strong Identity Anchor：高可信 full Reference / official Style Code / Manufacturer-grade MPN exact，或高可信 GTIN exact + Hard Attributes 无冲突。

`tau_segment`、`margin_segment` 按品牌/品类/来源/EvidenceProfile 独立认证，不使用 ANN raw score 或全局固定阈值。

### 9.2 Conflict Engine

Matcher 前后都执行：`HARD_CONFLICT / POSSIBLE_CONFLICT / NO_CONFLICT`。品牌不同；官方 36mm vs 41mm；Mini vs Medium；官方材质/颜色/五金冲突等属于典型 Hard。Validated GTIN 不同默认 Possible，只有品牌规则证明 1:1 才 Hard。任何模型高分都不能覆盖 Hard Conflict。

Canonical 自身的 identity-critical Current Fact 若 `CONFLICTED/STALE`，视为 Catalog-side identity risk；在 Resolution 恢复前不得用该字段支撑 CERTAIN_MATCH。

### 9.3 UNKNOWN Reason Code

至少包括：

```text
UNKNOWN_AMBIGUOUS
UNKNOWN_POSSIBLE_NIL
UNKNOWN_ROUTING_AMBIGUOUS
UNKNOWN_INSUFFICIENT_EVIDENCE
UNKNOWN_SEARCH_FAILURE
UNKNOWN_DEPENDENCY_FAILURE
UNKNOWN_PUBLICATION_INCONSISTENT
UNKNOWN_CONFLICT
UNKNOWN_CATALOG_REPAIR
UNKNOWN_STALE_SOURCE_VERSION
UNKNOWN_COMMIT_CONFLICT
UNKNOWN_CANONICAL_FACT_CONFLICT
```

---

## 10. Entity Linking、Discovery 与 Canonical Health

### 10.1 Linking

```text
Source Snapshot
→ Route Policy / Filter Safety
→ Hybrid Retrieval
→ Pair / Conflict / Calibration
→ CandidateSetAssessment
→ CERTAIN_MATCH or reason-coded UNKNOWN
→ Recognition Commit Barrier（仅 CERTAIN_MATCH）
→ Current Source→Canonical Link + Immutable Recognition
→ Pending Review
```

最终身份判断与业务写入分层：推理结果只是待提交 Decision，不等于业务状态已经生效。

### 10.2 Discovery 与 Canonical Novelty / Uniqueness Gate

```text
POSSIBLE_NIL
→ Route/Filter Recovery
→ Official/Web Evidence
→ Strict Blocking
→ High-confidence Pair Edges
→ Constraint-aware Clustering
→ PROPOSED Canonical
→ Canonical Novelty / Uniqueness Gate
→ Attribute Fact Arbitration / Required Hard Fact Check
→ ACTIVE or remain PROPOSED
```

`POSSIBLE_NIL` 只表示没有安全链接到已有 Canonical，不等于已经证明是新品。

任何 `PROPOSED → ACTIVE` 必须生成 `canonical_novelty_assessment`，在当前 pinned publication 上对 ACTIVE、PROPOSED、MERGED historical aliases、official external variant ids、governed Manufacturer IDs 做高召回去重。

Novelty 状态：`UNIQUE / DUPLICATE_SUSPECT / AMBIGUOUS_EXISTING / CONFLICTING_EXISTING / RETRIEVAL_UNHEALTHY`。只有 `UNIQUE` 可自动激活。无强 ID 且与已有 Canonical 高度近似、又缺少可区分 Hard Attribute 时保持 PROPOSED。

为防并发 Discovery 创建同一新品，对 variant-unique Manufacturer Identifier / official external Variant ID 建 `canonical_identity_claim` 唯一所有权；无强 ID Proposal 激活前必须在同一事务/锁语境重新执行 novelty check。

激活前所有 required identity-critical Hard Attribute 必须至少为 `RESOLVED/MULTI_VALID/UNKNOWN-with-policy-allowance`；若 `CONFLICTED/STALE`，保持 PROPOSED 或 TAINTED，不能进入 ACTIVE。

### 10.3 传递冲突

禁止 `Connected Component = Canonical Variant`。使用 `Union-Find / Cluster Graph + Cannot-Link + Cluster Signature + Anchor Validation`。合并前检查 cannot-link、Hard Attributes、时间版本、来源互斥、anchor；有冲突立即阻断 A=B、B=C 推导 A=C。

### 10.4 ACTIVE Cluster Health / Split Repair

ACTIVE Canonical 持续做周期性与事件驱动一致性检查：

```text
high_trust_hard_conflict_count
cannot_link_inside_cluster_count
manufacturer_identifier_partition_count
variant_attribute_mode_count
review_disagreement_rate
new_evidence_conflict_rate
temporal_generation_conflict
canonical_fact_conflict_count
cluster_partition_score(optional)
```

治理级 Hard Conflict、不可兼容 Manufacturer ID 分区、confirmed cannot-link、identity-critical Canonical Fact Conflict 穿过 cluster 时，立即冻结相关 Auto-Match。GNN/graph classifier 只能检测/排序 repair candidate，不得直接修改 Canonical。

确认 Split 后：创建 N 个新 Canonical；所有 Source Product 从 ORIGINAL OBSERVED Evidence 重新解析；旧 Recognition 保留并由 superseding/invalidation event 更新 current pointer；跨 child Alias/Identifier 降级；旧 pseudo positive/训练 artifact 沿 lineage taint；每个 child 重新执行 Attribute Arbitration；repair 通过原子 publication 发布；旧 Canonical=`SPLIT`。

### 10.5 Duplicate Canonical / Merge Repair

系统同时防 under-merge：同一 Official Variant 被建成多个 Canonical。

Identity-critical collision（governed Manufacturer ID、official external Variant ID、variant-unique GTIN、reviewed SAME_VARIANT bridge）→ `MERGE_PENDING` 并冻结新挂接；Image/Text 高相似只生成治理候选。治理确认后选择唯一 survivor，旧 Canonical=`MERGED`。

Merge 前不能简单“选 survivor 的字段覆盖另一边”。两边所有 eligible Attribute Assertion 合并进入新的 arbitration scope，重算 Current Fact；若 Hard Attribute 形成 `CONFLICTED`，Merge 不得发布为 ACTIVE survivor，必须回到 Governance/可能的错误 Merge 判断。

---

## 11. Web Evidence Agent 与 Official Web Catalog

仅触发：内部无候选、POSSIBLE_NIL、新款、Vintage/下架、型号疑似错误、关键属性冲突、Identifier 验证、near-tie 缺官方属性、Canonical Fact Conflict 外部复核。

优先级：品牌官网 → 官方目录/经销资料 → 专业数据库 → 高质量二手平台 → 通用搜索。

Agent 只能产出 Evidence；禁止直接修改 Canonical、直接产生 CERTAIN_MATCH、直接裁决 Current Fact、把搜索摘要当事实、用复制网页伪造多源一致。

官方站优先解析：

```text
Sitemap/Internal Links
→ Raw HTML + 必要 Rendered DOM
→ JSON-LD ProductGroup/Product
→ DOM / Embedded State / Identifier
→ External Variant Graph
→ Identifier Semantics Validation
→ Variant URL + Visible Attr Consistency
→ Versioned Snapshot
→ Attribute Assertion / PROPOSED Fact Candidate
```

Product Group ID、第三方 merchant SKU、单张图片/Visual Search、搜索/LLM 摘要、只有营销名无具体 Variant ID 的官网页、互相转载的多个二手页都不能单独激活 Variant 或形成 Current Hard Fact。页面 404/下架只更新 Evidence 有效性，不直接删除 Canonical。

---

## 12. 冷启动、Pseudo-label 与训练数据飞轮

### 12.1 0 Labels

使用高可信 Reference/Style/GTIN、Hard Constraint、alias/ontology、预训练 Text/Image Embedding、确定性 Positive、同系列不同尺寸/颜色/材质等 Hard Negative。

### 12.2 Pseudo-label Confirmation-bias Guard

`pseudo_label_record` 保存 label origin、teacher/policy version、source decision、EvidenceProfile/CandidateState、confidence/uncertainty、selection policy、upstream artifacts、training eligibility、promotion status。

Pseudo-label 永远不能进入 Certification Gold、Canonical Fact 或 Attribute Arbitration eligible set。Teacher 高置信不是独立 Evidence；按 brand/category/source/EvidenceProfile/Canonical 分桶限额；Hard Negative 优先；Teacher/Student 必须在 sealed human-reviewed/time-shift set 上评测。

### 12.3 有标签后的演进

```text
~10K reviewed pairs: Logistic / LightGBM + calibration
更多数据: compact Cross Encoder / domain contrastive encoder
100K+ 且 Hard Negative 足够: multimodal student / active learning / learned fusion
```

LLM/VLM Teacher 只处理困难样本并产出结构化解释/候选标签；重要标签需规则/主档/人工交叉验证。

---

## 13. Review、风险认证与 Reviewer Quality

### 13.1 三轨 Review

```text
TRAINING_REVIEW      Active Learning / Hard Negative / LIKELY / UNKNOWN / rare slices
CERTIFICATION_AUDIT 从真实最终 Recognition Decision 流概率抽样
GOVERNANCE_REVIEW   PROPOSED/Novelty/Merge/Split/Web/Identifier/Fact Conflict/主档治理
```

风险认证单位是最终 Recognition Decision，不是 candidate pair。Certification 样本记录 `inclusion_probability / stratum_key`；全局风险按真实生产流量权重汇总。Active Learning/Hard Negative 只能用于训练和诊断。

### 13.2 Calibration 与 Cluster-aware Certification

- 调参集与最终认证集分离。
- Record-level Auto-Match 计算单侧二项风险上界作为 i.i.d. sanity bound，但原始审核条数不等于有效独立样本数。
- 审计 dependency keys 至少保存 source/feed、Canonical、parser/extractor/identifier/fact-arbitration policy、model/calibrator/route、publication、ingestion batch/time bucket。
- 维护版本化 `audit_cluster_policy`，预先声明 source/feed、canonical、model+parser+policy、publication/time 等 cluster definitions。
- 正式报告给出 design-weighted error、i.i.d. upper bound、cluster-aware upper bound、`n_raw / n_distinct_clusters / design_effect / effective_sample_size`。
- Release 使用预先声明定义中的最保守上界；相关性信息不足则 `CERTIFICATION_INSUFFICIENT`。
- 任一确认 False Merge 都按 dependency keys 展开 sibling scan；系统性问题先 kill-switch/UNKNOWN，再修复与重认证。

```text
release_risk_upper_bound = max(
  iid_record_upper_bound,
  cluster_aware_upper_bounds,
  critical_slice_upper_bounds
)
```

### 13.3 Certification Label Quality

正式 `CERTIFICATION_AUDIT` 默认双人独立盲审，首轮不展示模型 score、Teacher/VLM 建议或另一 reviewer 标签。

标签：`SAME_VARIANT / DIFFERENT_VARIANT / INSUFFICIENT_EVIDENCE / SOURCE_DATA_ERROR`。分歧进入高级 reviewer/专家 adjudication；仍无法裁决的 `AUDIT_UNRESOLVED` 按潜在 false merge 保守处理或关闭对应 slice。

Reviewer 维护 brand/category qualification，定期插入 hidden control items。Guideline、UI、Evidence Snapshot、Qualification 全部版本化。Certification set 不参与同一版本调参。

---

## 14. 数据库、Business Lineage 与 ML Reproducibility

关系数据库是事实源；搜索/向量由 OpenSearch 承担。

核心业务表至少：

```text
brand / product_family / product_model / canonical_variant / canonical_variant_version
canonical_relation / canonical_health_snapshot / canonical_repair_case / canonical_repair_member
canonical_novelty_assessment / canonical_identity_claim / canonical_merge_case / canonical_merge_member
source / source_group / source_product / source_product_image / source_canonical_link
product_identifier / identifier_semantics_policy
extracted_attribute / normalized_attribute / attribute_fact
attribute_semantics_policy / attribute_arbitration_policy
canonical_attribute_resolution / canonical_fact_conflict_case
external_evidence / external_variant_relation
routing_decision / route_bypass_assessment / routing_policy_version
candidate_pair / candidate_retrieval_evidence / candidate_set_assessment
match_evidence / conflict_record / cannot_link / cluster_membership
recognition_commit_intent / recognition_record / recognition_outbox_event
review_task / review_record / review_assignment / review_adjudication
review_sampling_policy / audit_sample / reviewer_qualification / review_control_item / review_guideline_version
audit_cluster_policy / audit_dependency_key / certification_risk_estimate
label_dataset_membership / pseudo_label_record
fact_promotion_event / canonical_mutation_log
catalog_outbox_event / search_apply_checkpoint / catalog_publication
artifact_lineage_edge / artifact_taint
```

`canonical_attribute_resolution` 至少保存：

```text
resolution_id
canonical_variant_id / canonical_version
attribute_id
market_scope / valid_from / valid_to / variant_generation
semantics_policy_version / arbitration_policy_version
resolution_state
resolved_value_ids / generic_value_ids / brand_precise_value_ids
input_assertion_ids
source_independence_snapshot_id
resolution_reason / confidence_class
supersedes_resolution_id
created_at / published_in_publication_id
```

`canonical_fact_conflict_case` 保存冲突 assertion、identity role、scope、冻结动作、治理结论、是否升级 Split/Merge/TAINT、re-arbitration result。

`routing_decision` 保存 source/version、brand/category candidates、route state/scope/filter mode、evidence、router/policy version、pre/post candidate counts、fallback reason。

`source_product` 至少保存 `source_version / content_hash / recognition_epoch / recognition_state / current_recognition_id`。

`recognition_record` 为不可变历史，保存 idempotency key、source version/hash、canonical/version、publication、candidate assessment、model/calibrator/policy/ontology/fusion/route/routing/fact-resolution versions、route signature、decision fingerprint、status/supersedes relation。

### 14.1 Business / ML Lineage

维护 `artifact_lineage_edge / artifact_taint / fact_promotion_event`。Model/Calibrator/Embedding/Index/Policy/Attribute Resolution 都是一等可版本化 Artifact；`model_version` 绑定 weight hash、dataset manifest、feature schema、ontology、identifier/fact/routing policy、calibrator、parent model、serving runtime、代码 commit/container digest。

Recognition 必须能反查训练数据 membership/hash、label guideline、sampling policy、代码、模型权重、calibrator、index、publication、routing policy 和使用的 Canonical Attribute Resolution。MLMD 可作为实现参考；不引入 MLMD 时也实现等价 Artifact–Execution–Event lineage。

---

## 15. Incremental、CDC、Catalog Publication 与 Recognition Commit

### 15.1 Catalog → Search Publication Barrier

历史初始化：

```text
Raw Import
→ Normalize / Extract / QC
→ Barcode/OCR
→ EvidenceProfile / Router
→ Batch Text/Image Embedding
→ Build Initial Attribute Resolutions
→ Build Versioned OpenSearch Generation
→ Publication Parity Validation
→ Atomic Alias Publish
→ Linking / Recognition
```

增量 Canonical mutation：

```text
New/Changed Governed Assertion / Canonical mutation
→ Attribute Re-arbitration when affected
→ DB Transaction: Canonical/Resolution mutation + catalog_outbox_event
→ CDC / Outbox Relay
→ Idempotent Projection Consumer
→ External-versioned OpenSearch Upsert/Tombstone
→ Refresh Visibility + Search Checkpoint
→ Advance Catalog Publication Watermark
```

Canonical/Alias/Identifier/Attribute Resolution promotion、撤销、RETIRED/MERGED/SPLIT_PENDING/SPLIT/MERGE_PENDING/TAINTED 与 outbox event 在同一 DB transaction 提交。Indexer 用 `event_id` 幂等去重，用数据库单调 `mutation_version` 做 OpenSearch external version。删除/撤销生成 tombstone。

Auto-Match 请求入口 pin `ACTIVE publication_id`。Search hit 进入 Pair/Conflict 前校验 pinned publication；修复/撤销/Fact Conflict 状态即使被旧索引召回，也必须在 Identity Gate 丢弃。

`search_apply_checkpoint < publication.mutation_watermark`、refresh 未确认、projection parity 失败、混合 publication → `UNKNOWN_PUBLICATION_INCONSISTENT`。

### 15.2 Recognition Commit Barrier

推理、OCR、检索、Rerank 不持有数据库长事务。在线链路先在事务外形成不可变 `RecognitionDecisionSnapshot`，绑定 idempotency key、Source Version、candidate Canonical/version、publication、模型/策略/route/routing、identity-critical fact-resolution ids 与 decision fingerprint。

只有 `CERTAIN_MATCH` 进入短 ACID transaction：

```text
BEGIN
  Insert/lookup recognition_commit_intent(idempotency_key)
  Lock/CAS source_product current row
  Validate source_version/content_hash/recognition_epoch unchanged
  Validate target Canonical still linkable under authoritative state/publication
  Validate identity-critical fact-resolution ids still active in pinned publication
  Ensure no conflicting current Source→Canonical link
  Insert immutable recognition_record
  Upsert current source_canonical_link
  Update source_product recognition state/current_recognition_id
  Insert exactly one Pending Review task or recognition_outbox_event
COMMIT
```

相同 key + 相同 intent 返回第一次结果；相同 key + 不同语义参数拒绝。Source 变化 → `UNKNOWN_STALE_SOURCE_VERSION`；目标 Canonical 冻结/修复/撤销、Fact Resolution 被 supersede/conflicted 或 publication 不一致 → 不提交。跨系统副作用用 Outbox + idempotent consumer。

Source 内容更新生成新的 `source_version / recognition_epoch`，旧 Recognition 保留历史，current state 转 `REPROCESS_REQUIRED`。Review Reject/Correction、Split/Merge/Reprocess 通过 superseding/invalidation event 改 current pointer，不原地修改历史 Recognition。

---

## 16. Production Runtime Reliability

### 16.1 Fail-closed

Route Policy 定义 mandatory/optional stage。任一 mandatory stage 被跳过、timeout、熔断都会改变 `route_execution_signature`；未经该 degraded route 独立 benchmark + risk certification，不允许 CERTAIN_MATCH，只能 UNKNOWN 或异步重试。

### 16.2 Deadline / Retry / Queue / Circuit Breaker

- 每条请求入口生成绝对 deadline 并沿 OpenSearch、Reranker、OCR、GPU/VLM 传播。
- Expensive stage 开始前检查剩余预算；mandatory stage 无法完成 → `UNKNOWN_DEPENDENCY_FAILURE`。
- 每个依赖只有一个 retry owner；只重试 transient error；指数退避+jitter+max attempts+retry budget，受原 deadline 约束。
- OpenSearch、Reranker、OCR、GPU/VLM、Web Agent 分别配置 `max_inflight / max_pending / queue_deadline / circuit threshold / half-open budget`。
- 队列有界；没有足够 deadline 的请求直接 shed/UNKNOWN。

### 16.3 Load Shedding

```text
P0 Strong Identifier 在线 Linking
P1 普通 Hybrid Linking
P2 Near-tie Local Visual / Difficult Pair
P3 Route-bypass / Entity Discovery / Web Evidence / Novelty / Fact Conflict enrichment
P4 历史 backfill / re-embedding / periodic Canonical dedupe/repair / bulk re-arbitration
```

过载优先停止 P4→P3→P2。未认证简化 route 只能降低 Coverage，不能降低 Identity Safety。Route-bypass 若因过载无法执行，则不能把原结果升级为 POSSIBLE_NIL/新品；identity-critical Fact Conflict 未完成仲裁也不能临时选值继续 Auto-Match。

定期故障注入：timeout、OpenSearch partial failure、GPU unavailable、queue overload、retry storm、CDC lag、重复/乱序事件、refresh lag、publication rollback、Split/Merge partial publish、Recognition commit crash/response loss/concurrent retry、错误 Brand/Category Router，以及 Attribute Resolution supersede/撤回/冲突在 Search 未同步时的 stale match。

---

## 17. 容量、成本与性能

10M、平均3张可用图、10M text + 30M image vectors、512d 时，模型导出 FP16 数组约 40.96GB；OpenSearch on-disk 完整 FP32 向量约 81.92GB，不含 Lucene/HNSW、倒排、doc values、metadata、translog、merge 临时空间、副本、shadow index。

PoC（1–2M）：1×L4 24GB、1×8 vCPU/64GB OpenSearch、0.5TB SSD/block storage。

10M 生产压测起点：3×8 vCPU/64GB OpenSearch data/search nodes、3×1TB usable storage、2×L4 24GB online GPU（HA 时）；历史 backfill 临时扩 4–8×L4/同级 GPU。

成本与 `Coverage @ 99.9% Precision`、`Cost per Confirmed Recognition` 联合优化。Router broad fallback/bypass 与 Fact Re-arbitration/Conflict Governance 单独报告 QPS、候选数、P95、GPU/人工成本。

---

## 18. Benchmark、Leakage 与 End-to-End Audit

保留：

1. **Pair Gold Set：5K–10K pairs**，提高 Hard Negative 比例。
2. **End-to-end Recognition / Production Audit Set**，以 Source Product 最终 Decision 为单位。

必须有 `Seen/IID / Strict Unseen Canonical / Time-shift-New SKU` 三轨，Split 前按 `canonical_variant_id / source_group / syndication_group / image_hash_group / content_fingerprint` 隔离。

Corner Cases 至少覆盖：腕表/箱包近邻 Variant、缺模态/低质图、NIL/near-tie、Web Group ID/merchant SKU 误用、Split/Merge repair、Runtime/Publication、Recognition Commit、成簇 False Merge、Router/filter false-NIL，以及 Attribute Fact Arbitration：

- 同 Canonical 同 market/time 的两个治理级 Hard Attribute 值冲突。
- 官网不同地区站点值不同，但实际属于合法 regional scope。
- JSON-LD 与 visible DOM 对同一字段冲突。
- 三个转载站支持错误值，一个独立 T0 支持正确值。
- T2 新值与旧 T0 Current Fact 冲突。
- 更粗通用值与更精确 brand-specific 值同时出现。
- SET_VALUE 属性顺序不同但集合相同。
- 支撑 Current Fact 的网页被撤回/TAINT 后重新仲裁。
- Attribute Resolution 已 supersede，但旧 Search 文档仍被召回。
- Merge 两个 Canonical 后属性 assertion 合并产生 Hard Conflict。

关键指标：

```text
Attribute Fact Arbitration:
identity_hard_fact_conflict_auto_match_violation = 0
current_fact_without_resolution_provenance = 0
raw_assertion_in_identity_search_projection = 0
syndicated_source_double_count_in_arbitration = 0
latest_write_wins_hard_fact_violation = 0
unexplained_t0_t0_conflict_auto_resolved = 0
stale_resolution_auto_match_violation = 0
fact_retraction_rearbitration_completion_rate = 100%
regional_scope_false_conflict_rate
attribute_resolution_precision / unresolved_rate
fact_conflict_to_freeze_latency
resolution_publication_consistency_violation = 0

Routing / Filter Safety:
router_pre_filter_positive_recall
router_post_filter_positive_recall
routing_loss_rate
false_nil_due_to_router
candidate_recovered_by_bypass
hard_filter_without_authoritative_evidence = 0
exact_identifier_blocked_by_router = 0
proposed_created_without_required_route_bypass = 0

Retrieval:
Recall@20/@50/@100 before/after fusion
Exact survival / candidate_count / latency

Open-world / Novelty:
NIL Precision/Recall
False Known-Link on true new SKU
new_canonical_false_novel_rate
canonical_fragmentation_rate
variant_unique_identifier_multi_canonical_collision = 0

Cluster / Repair:
inconsistent_cluster_detection_recall / precision
false_split_rate / false_merge_repair_rate
partial_repair_publication_auto_match_violation = 0

Recognition Commit Integrity:
duplicate_recognition_side_effect = 0
multiple_current_link_per_source = 0
partial_recognition_commit_violation = 0
stale_source_version_commit = 0
non_linkable_canonical_commit = 0
idempotency_same_key_different_intent_commit = 0
post_commit_retry_duplicate_review_task = 0
recognition_commit_lineage_completeness = 100%

Certification Risk:
audit_raw_sample_count / audit_distinct_dependency_cluster_count
audit_design_effect / audit_effective_sample_size
record_level_risk_upper_bound / cluster_aware_risk_upper_bound
release_risk_upper_bound

Runtime:
degraded-route auto-match violation = 0
retry amplification / deadline-exceeded / shed rate

Leakage:
cross_split_variant/image/syndication leakage = 0
certification_label_used_for_same-version_tuning = 0

ML Reproducibility:
model_lineage_completeness = 100%
training_dataset_manifest_completeness = 100%
model_weight_hash_match = 100%
tainted_artifact_downstream_discovery_recall = 100%

Production:
Coverage @ 99.9% Precision
False Merge per Million
UNKNOWN Rate
cost per recognized item
risk upper bound
```

---

## 19. MVP → Production

### Phase 0：数据审计与 Gold Set

建立字段完整度、Alias/Ontology、Hard Conflict Matrix、Identifier 可用率、来源独立性、EvidenceProfile、Leakage-safe Split、真实 NIL/near-tie benchmark、三轨 Review、Reviewer Qualification、sealed certification holdout；同时：

- 审计 Brand/Category Router 可用性并生成 routing hard negatives。
- 对已有腕表标准库做 Over-merge / Under-merge Audit。
- 为腕表/箱包关键属性建立 `attribute_semantics_policy` 与首版 `attribute_arbitration_policy`。
- 抽样审计当前主档 Hard Attribute 的来源、市场/时间 scope、冲突率与“最新值覆盖/多数票”遗留问题。

### Phase 1：确定性腕表 Linking + Recognition Commit + Hard Fact Arbitration

Reference/GTIN lookup、Exact Retrieval、Hard Conflict、Recognition/Review；实现 Decision Snapshot、idempotency、Source Version CAS、单一 current pointer、Immutable Recognition + Pending Review 原子 transaction、outbox/idempotent consumer。

同步为腕表身份关键字段上线 Attribute Arbitration：`reference_full / case_diameter / case_material / dial / bezel / bracelet / caliber-generation`，先保证 Current Fact provenance、Conflict Freeze 与 Search publication 一致，再扩大自动识别。

### Phase 2：Hybrid Text + Router/Filter Safety + Pair Matcher

上线 BM25、Qwen3-Embedding-0.6B、RRF、Qwen3-Reranker-0.6B、箱包、Calibration、Candidate-set/NIL/Ambiguity Guard，并实现 authoritative vs inferred route、多路/ancestor fallback、Exact router bypass、route-bypass verification 和 Routing Recall/False-NIL benchmark。

箱包同时扩展 `official_size / material / color / hardware / pattern / edition` 的 Attribute Arbitration。

### Phase 3：Image / Barcode / OCR / Multimodal

zxing-cpp、PP-OCRv6、PaddleOCR-VL、SigLIP2、DINOv3 challenger、局部 patch rerank、Qwen3.5-4B、Qwen3-VL challenger。图片可改善候选/属性 extraction，但视觉输出必须先成为 assertion，不能直接覆盖 Canonical Current Fact。

### Phase 4：Open-world Discovery + Official Web Evidence + Canonical Repair

POSSIBLE_NIL、constraint-aware clustering、ProductGroup/Product parse、Identifier Semantics、External Variant Graph、Catalog Activation Gate；上线 Canonical Novelty Scan、Cluster Health、Canonical-to-Canonical duplicate scan、Governed Split/Merge Repair。Novelty/Repair 都消费同一 Attribute Arbitration 结果。

### Phase 5：Supervised Student + Controlled Continuous Learning

Review labels、Hard Negative、NIL/near-tie mining、active learning、domain encoder、learned fusion、shadow/A-B/rollback；上线前具备完整 ML lineage 与 taint propagation。Router/Taxonomy/Fact Arbitration policy 都作为版本化 Artifact 单独认证和漂移监控。

---

## 20. 正式开启自动识别 Gate

全部满足才扩大 Auto-Match：

1. 主要品类/来源 `Precision@Auto-Match >= 99.9%`。
2. `Recall@100 after fusion/truncation >= 99.5%`，Exact/官方标识符 survival=100%。
3. 正式 Certification Audit 满足概率抽样 provenance、distinct dependency cluster coverage，且 `release_risk_upper_bound <= target_false_merge_risk`。
4. 所有 Hard Conflict 回归 0 自动错合并；Seen/Strict Unseen/Time-shift、Vintage、缺图、缺型号、低质图分别达标。
5. Image-only 路径不得无额外身份/结构证据产生 CERTAIN_MATCH；Synthetic/Pseudo evidence 不得成为 Hard Fact / Identity Anchor / Certification Gold。
6. AMBIGUOUS/NIL/ROUTING_AMBIGUOUS/RETRIEVAL_FAILURE/PUBLICATION_INCONSISTENT/CATALOG_REPAIR/CANONICAL_FACT_CONFLICT 不得绕过 Guard。
7. Product Group ID 作为 Variant Identity Anchor 违规=0；未认证 merchant SKU 作为官方 Reference 违规=0；Official Web JSON-LD/DOM 未解决冲突时自动 ACTIVE=0。
8. 所有 ACTIVE 模型/Router/Policy lineage 完整率=100%；tainted upstream 在 SLA 内触发 kill-switch/re-certification。
9. `SPLIT_PENDING / SPLIT / MERGE_PENDING / TAINTED` Canonical 新增 Auto-Match=0；Split/Merge partial publication 下 CERTAIN_MATCH=0。
10. `PROPOSED → ACTIVE` 必须有同 publication 的 `canonical_novelty_assessment` 且 `novelty_state=UNIQUE`；variant-unique ID 不得双 ACTIVE。
11. 一个 committed Recognition intent 只能产生一次业务副作用；partial commit、stale source、多 current link、duplicate review task 均为 0。
12. Exact Identifier 被 Router/Category Filter 提前删除的违规=0；模型/VLM/OCR/低可信 merchant 推断的 brand/category 单独作为 hard filter 的违规=0。
13. Router 非 AUTHORITATIVE 且请求将进入 NIL/Discovery 时，缺少 `route_bypass_assessment` 的违规=0；bypass 恢复安全已有 Canonical 后仍创建新主档的违规=0。
14. Brand/category 冲突被强制单一路由、未保留 plausible route quota 的违规=0；routing policy/taxonomy/router model provenance 完整率=100%。
15. `CERTIFICATION_INSUFFICIENT` 不得当作 PASS；任一认证 False Merge 若显示系统性 dependency-cluster 问题，相关 slice 立即 kill-switch/UNKNOWN 并重新认证。
16. **所有参与 Identity/Hard Conflict/Search Filter 的 Canonical 属性必须来自当前 publication 的 `RESOLVED/MULTI_VALID canonical_attribute_resolution`；raw assertion 直接进入 identity projection 的违规=0。**
17. **同一 market/time/generation scope 的 SINGLE_VALUE identity-critical Hard Attribute 存在治理级高可信冲突时，自动选择某一值继续 Auto-Match 的违规=0。**
18. **Identity-critical Resolution=`CONFLICTED/STALE` 时产生 CERTAIN_MATCH 的违规=0。**
19. **syndicated/复制来源在 Attribute Arbitration 中被重复计票的违规=0；Hard Fact 使用 latest-write-wins 或简单 majority vote 的违规=0。**
20. **Canonical Attribute Resolution provenance（assertions/source-independence/policy/scope/supersedes/publication）完整率=100%。**
21. **支撑 Current Fact 的上游 Evidence 被撤回/TAINT 后，必须在 SLA 内完成 STALE→重仲裁/冻结；旧 Resolution 继续支撑 Auto-Match 的违规=0。**
22. **Attribute Resolution 与 Search publication 不一致、旧 Hard Fact 仍被用于 Exact/filter/Conflict 的违规=0。**

任何指标退化时，优先降低 Coverage，不放宽 Identity Safety，也不允许通过更激进的 Router、抓取顺序、来源多数票或模型输出“自动解决”事实冲突。

---

## 21. 主要技术依据

- Amazon Science — VARM：Variant Relationship 与普通 Entity Resolution 分开建模。  
  https://www.amazon.science/publications/learning-variant-product-relationship-and-variation-attributes-from-e-commerce-website-structures
- Amazon Science — Hierarchical Query Classification in E-commerce Search：电商 taxonomy 是层级结构，类别不平衡与输入歧义会显著增加分类难度。  
  https://www.amazon.science/publications/hierarchical-query-classification-in-e-commerce-search
- Microsoft Research — Improving Product Classification Using Images：Product Classification 会影响 offer→catalog matching；图像与文本融合可提高分类质量。  
  https://www.microsoft.com/en-us/research/publication/improving-product-classification-using-images/
- Microsoft Research — Matching Unstructured Product Offers to Structured Product Descriptions：Bing Shopping 的匹配显式使用 attribute match、mismatch 与 missing 作为核心信号，说明主档属性的正确性直接影响实体链接安全。  
  https://www.microsoft.com/en-us/research/publication/matching-unstructured-product-offers-to-structured-product-descriptions/
- Amazon Science — Automatic Validation of Textual Attribute Values in E-commerce Catalog：商家自报 Catalog 属性天然含噪，需要对 Attribute Value 做自动验证并显式处理不确定性。  
  https://www.amazon.science/publications/automatic-validation-of-textual-attribute-values-in-e-commerce-catalog-by-learning-with-limited-labeled-data
- Amazon Science — Using LLMs to Improve Amazon Product Listings：Catalog quality control 需要结合 product-type/attribute statistics、语义粒度和异常检测；频率高的粗粒度值不能无条件覆盖更精确值。  
  https://www.amazon.science/blog/using-llms-to-improve-amazon-product-listings
- Bayesian Truth Discovery / Data Fusion：冲突多源事实不能只靠多数票，source quality 与多值属性语义需要显式建模。  
  https://arxiv.org/abs/1203.0058
- Amazon Science — Graph Neural Networks for Inconsistent Cluster Detection / SuperPart：成熟 Catalog 要持续做 Cluster Health 与受约束修复。  
  https://www.amazon.science/publications/graph-neural-networks-for-inconsistent-cluster-detection-in-incremental-entity-resolution  
  https://www.amazon.science/publications/superpart-supervised-graph-partitioning-for-record-linkage
- Microsoft Research / PVLDB — Synthesizing Products：Offer→已有 Product Linking 与新品识别是不同风险层。  
  https://www.microsoft.com/en-us/research/publication/synthesizing-products-for-online-catalogs/
- Walmart Global Tech：Catalog Attribute Extraction 与 Quality Check 分阶段。  
  https://tech.walmart.com/content/walmart-global-tech/en_us/news/articles/how-walmart-uses-llms-to-manage-its-massive-product-catalogs.html
- WDC Products：真实电商 Entity Matching、unseen entity、corner cases 与 leakage-safe benchmark。  
  https://github.com/wbsg-uni-mannheim/wdcproducts
- GenDecider / Learn to Not Link：显式处理 None-of-the-Candidates / NIL。  
  https://aclanthology.org/2024.naacl-short.22/  
  https://aclanthology.org/2023.findings-acl.690/
- End-to-end Multi-modal Product Matching / MMPCBench / AMELI。  
  https://arxiv.org/abs/2403.11593  
  https://arxiv.org/abs/2601.19750  
  https://aclanthology.org/2024.eacl-long.172/
- Google Search Central / GS1：ProductGroup/Variant 与 GTIN 语义。  
  https://developers.google.com/search/docs/appearance/structured-data/product-variants  
  https://www.gs1.org/1/gtinrules/en/
- OpenSearch 3.8：Hybrid、disk-based vector、versioned document/alias。  
  https://github.com/opensearch-project/OpenSearch/releases/tag/3.8.0  
  https://docs.opensearch.org/latest/vector-search/ai-search/hybrid-search/index/  
  https://docs.opensearch.org/latest/vector-search/optimizing-storage/disk-based-vector-search/
- Debezium Outbox / Kafka：事务性发出、唯一 event id、幂等投影。  
  https://debezium.io/documentation/reference/stable/transformations/outbox-event-router.html  
  https://kafka.apache.org/41/design/design/
- AWS Builders’ Library / PostgreSQL：Recognition Commit 的显式幂等与 ACID 语义。  
  https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/  
  https://www.postgresql.org/docs/current/sql-insert.html  
  https://www.postgresql.org/docs/current/explicit-locking.html
- TensorFlow ML Metadata：Artifact / Execution / Event / Context lineage。  
  https://www.tensorflow.org/tfx/guide/mlmd
- Google SRE / gRPC / Envoy：deadline、bounded queue、retry budget、circuit breaker、load shedding 与 fail-closed runtime。  
  https://sre.google/sre-book/addressing-cascading-failures/  
  https://grpc.io/docs/guides/deadlines/  
  https://www.envoyproxy.io/docs/envoy/latest/configuration/upstream/cluster_manager/cluster_circuit_breakers.html
- zxing-cpp / PaddleOCR / Qwen3 Embedding / Qwen3-VL / SigLIP2 / DINOv3。  
  https://github.com/zxing-cpp/zxing-cpp  
  https://github.com/PaddlePaddle/PaddleOCR  
  https://github.com/QwenLM/Qwen3-Embedding  
  https://github.com/QwenLM/Qwen3-VL-Embedding  
  https://github.com/google-research/big_vision  
  https://github.com/facebookresearch/dinov3

---

## 22. 最终生产原则

```text
官方知识 / 标准化 / 确定性标识
          ↓
Evidence Assertions + Source Independence
          ↓
Canonical Attribute Fact Arbitration
          ↓
EvidenceProfile + Router/Filter Safety
          ↓
Hybrid Retrieval + Exact Preserve
          ↓
结构化 + 文本 + 图片多证据
          ↓
Hard Conflict + Calibration
          ↓
Candidate-set / NIL / Ambiguity Guard
          ↓
Route-bypass Verification before NIL/Discovery
          ↓
Constraint-aware Graph Resolution
          ↓
CERTAIN_MATCH Decision
          ↓
Recognition Commit Barrier
          ↓
Current Link + Immutable Recognition + Pending Review
          ↓
Canonical Novelty / Health / Repair
          ↓
Independent + Cluster-aware Risk Audit
          ↓
Controlled Training + Immutable ML Lineage
```

固定原则：

1. **Identity first，Similarity second。**
2. **Hard Conflict 优先于任何模型高分。**
3. **UNKNOWN 是合法且必要的生产输出。**
4. **缺模态不能靠生成模态伪装成完整证据；Synthetic/Pseudo evidence 不能形成 Hard Fact / Identity Anchor / Certification Gold。**
5. **仅视觉相似不能自动 SAME_VARIANT；全局视觉负责召回，局部视觉只验证 near-tie。**
6. **必须显式允许 NIL；Top-1/Top-2 near-tie 必须 Abstain。**
7. **Exact Identifier 永远绕过非权威 Router，Exact 候选永不被 Soft Fusion 截断。**
8. **Brand/Category Router 是候选空间优化器，不是身份事实源；模型/VLM/OCR/低可信 merchant 分类不得单独 hard-prune。**
9. **路由不确定时使用 multi-route / ancestor / broad fallback；NIL/Discovery 前必须做 route-bypass verification。**
10. **Router semantic recall 与 Search engine filtered-ANN recall 分开评测。**
11. **训练 Review 与风险 Certification 分流；正式风险用 cluster-aware 上界，不用 raw audit count 冒充 effective sample size。**
12. **Product Group ID 是关系标识，不是 Variant Identity；Merchant SKU 与 Manufacturer ID 分命名空间。**
13. **官网结构化数据是高价值 Evidence，不是无条件 Canonical Truth。**
14. **Canonical Current Fact 不是“最近抓到的值”或“最多来源支持的值”，而是按 Attribute Semantics、Source Authority、Independence、Market 与 Valid-Time 仲裁出的版本化 Resolution。**
15. **同 scope 的治理级 Hard Attribute 冲突必须 `CONFLICTED + freeze/governance`，不能用多数票、LLM 或相似度自动选边。**
16. **Search/Hard Conflict/Identity Filter 只消费已发布的 Canonical Attribute Resolution，不消费原始未仲裁 assertion。**
17. **DB→Search 使用 Transactional Outbox + Idempotent Projection + Monotonic Version + Publication Barrier，Attribute Resolution 变化也属于 Identity-critical publication。**
18. **ACTIVE Canonical 不是永久真值：持续检查 over-merge、under-merge 与 Fact Conflict。**
19. **Entity Discovery 是新品证明问题；`POSSIBLE_NIL` 不能直接推出新 Canonical，任何 `PROPOSED → ACTIVE` 必须通过 Novelty/Uniqueness Gate 与 Required Fact Check。**
20. **Cluster Detector/Repair Classifier/Graph Partitioning 只能检测、排序、提问和生成候选，不能直接修改 Canonical。**
21. **每一个 ACTIVE 模型、Router、Taxonomy、Fact Arbitration Policy 和训练数据集都是不可变、可哈希、可追溯 Artifact。**
22. **timeout/过载/依赖故障形成的新 route 未认证时 fail closed 为 UNKNOWN。**
23. **模型的 CERTAIN_MATCH 只是待提交 Decision；只有通过 Recognition Commit Barrier 后业务关系才生效。**
24. **Recognition Commit 必须满足显式 idempotency intent、Source Version CAS、Canonical/Fact Resolution 再验证、单一 current link、Immutable Recognition、Pending Review/Outbox 原子提交。**
25. **Source 更新、Review Reject、Split/Merge、Reprocess 通过 superseding/invalidation event 改 current pointer，不修改历史 Recognition。**
26. **任何认证 False Merge 都必须按 dependency cluster 做 sibling scan/incident triage；系统性错误先 kill-switch/UNKNOWN，再修复和重认证。**
27. **任何复杂度只有在提高 Coverage@99.9% Precision、提高事实/事务/运行可靠性、降低 Router False-NIL、降低 Catalog fragmentation、降低治理成本或降低总成本时才有价值。**
