# 奢侈品识别方案

## 1. 目标与边界

本系统的目标不是“找视觉上最像的商品”，而是把多个来源的商品记录归一到**同一个官方商品变体（Official Variant）**。自动识别必须精确到官方变体级；只要尺寸、材质、颜色、盘面、五金、官方 Reference / Style Code 等会产生不同官方变体的关键属性发生可信冲突，就不得自动合并。

第一阶段覆盖：

- 腕表
- 箱包
- 历史存量约 100 万–1000 万条商品记录
- 腕表已有部分品牌/系列/型号标准库
- 箱包标准主档不完整，需要同时支持已有主档挂接与新主档发现

核心风险偏好：

> **Precision 优先于 Recall。宁可输出 UNKNOWN，也不能把不同官方变体错合并。**

系统不做真假鉴定；成色、售价、库存、卖家、序列号等属于 Offer / 单件商品属性，不作为“同一官方变体”的直接定义。

---

## 2. 核心业务实体

### 2.1 实体层级

统一采用以下层级：

```text
Brand
  └── Product Family / Series
       └── Product Model
            └── Canonical Variant
                 └── Source Product / Offer
```

其中：

- **Brand**：品牌。
- **Product Family / Series**：系列、家族，例如 Rolex Datejust、Chanel Classic。
- **Product Model**：模型/款式层，允许包含多个官方变体。
- **Canonical Variant**：本系统自动识别最终要挂接的最小官方商品实体；尺寸、材质、颜色、盘面、五金、版本等变体属性已确定。
- **Source Product / Offer**：雷小安、奢当家、腕表之家、小迈步、爱回收或其他来源的一条原始商品记录。

### 2.2 关系类型

禁止把所有关系压缩成一个 similarity score。至少使用：

```text
SAME_VARIANT       同一官方变体，可建立 Recognition
VARIANT_OF         同型号/款式的不同官方变体
SAME_MODEL         同一 Model，但变体不同或不确定
SAME_SERIES        同系列
VISUALLY_SIMILAR   仅视觉近似
UNKNOWN            证据不足
```

只有 `SAME_VARIANT` 才允许生成自动识别关系。

### 2.3 Canonical Variant 状态

```text
ACTIVE      已验证、可用于自动挂接
PROPOSED    系统新发现，证据尚不足或等待审核
RETIRED     历史实体，不接受新自动挂接
MERGED      主档治理后已合并到另一 Canonical Variant
```

新发现的实体默认进入 `PROPOSED`，不能因为若干来源名称相似就直接污染 ACTIVE 主档。

---

## 3. 标准 Schema 与 Hard Constraint

### 3.1 通用字段

每个标准化属性必须同时保存：

```text
raw_value
normalized_value
ontology_id / canonical_value_id
extractor
extractor_version
source_type
source_url / source_record_id
evidence_span / image_bbox
confidence
validation_status
observed_at
```

属性冲突只有在**双方属性均已标准化、通过校验且达到相应可信度**时才升级为 `HARD_CONFLICT`。低质量 OCR、卖家自由文本或低置信 VLM 产生的表面不一致，只能作为疑似冲突，决策应退回 `UNKNOWN`，不能直接否决或直接合并。

### 3.2 腕表 Schema

| 字段 | 角色 | 默认约束策略 |
|---|---|---|
| brand | 品牌 | Hard |
| series / family | 系列 | Blocking + Soft/Hard（品牌规则决定） |
| subseries / model | 子系列/型号 | Strong Evidence |
| official_reference | 官方完整 Reference | Hard；精确命中是最强正证据之一 |
| case_diameter_mm | 表径 | Hard，前提是官方规格或高可信标准化 |
| case_material | 表壳材质 | Hard |
| dial_color | 盘面颜色 | Hard |
| dial_type | 盘面类型/特殊盘 | Hard（能定义独立变体时） |
| bezel | 表圈材质/类型 | Hard（能定义独立变体时） |
| bracelet_or_strap | 官方原配表链/表带 | 条件 Hard；二手替换件不能直接否决 |
| caliber / movement | 机芯 | Generation/Variant 区分时 Hard，否则 Strong Evidence |
| functions | 功能 | Variant 区分时 Hard |
| edition / generation | 限量版、代际、特殊版本 | Hard |
| serial_number | 单件序列号 | **不得作为官方变体 ID** |
| condition / price | 成色/价格 | Offer 属性，不参与同变体定义 |

对腕表 Reference 必须保留：

- 原始字符串
- 去空格/连字符/全半角后的标准化形式
- 品牌特定的前后缀、材质/盘面编码拆解结果
- `reference_base` 与 `reference_full`

只有品牌规则确认“后缀不影响官方变体”时才允许用 base reference 合并；默认以 `reference_full` 为准。

### 3.3 箱包 Schema

| 字段 | 角色 | 默认约束策略 |
|---|---|---|
| brand | 品牌 | Hard |
| family / series | 系列 | Blocking + Strong Evidence |
| model / style | 款式 | Strong Evidence |
| official_style_code | 官方款号/Style Code | Hard；高可信精确命中是最强正证据之一 |
| official_size | Mini/Small/Medium 等官方尺寸及数值尺寸 | Hard |
| material | 皮质/帆布/特殊材质 | Hard |
| official_color | 官方颜色 | Hard |
| hardware | 金/银/黑等五金颜色与类型 | Hard |
| pattern / print | 图案、印花、Monogram 版本 | Hard |
| edition / collaboration | 限量/联名/特别版 | Hard |
| season / year / generation | 年代/季节/代际 | 仅在官方变体由此区分时 Hard |
| serial / date_code | 单件/批次/生产时间证据 | **不得直接作为官方变体 ID** |
| condition / price | 成色/价格 | Offer 属性 |

颜色不直接使用图片像素作为 Hard Constraint。灯光、白平衡、皮革老化和修图会造成明显偏差；图片颜色只提供 Soft Evidence，官方颜色名或高可信标准化文本优先。

### 3.4 两级标准化

颜色、材质等同时保留“通用语义”和“品牌精确语义”：

```text
material_group = ROSE_GOLD
material_brand_value = ROLEX_EVEREOSE_GOLD

color_group = BLACK
color_brand_value = CHANEL_NOIR
```

这样既能跨平台召回，又不会把品牌专有材质/版本错误压平。

---

## 4. 属性抽取与标准化流水线

```text
Raw Source Product
      ↓
Schema Mapping
      ↓
Deterministic Parsing / Dictionary
      ↓
Text Attribute Extraction
      ↓
OCR（有图片文字时）
      ↓
VLM Fallback（仅困难/缺字段样本）
      ↓
Normalization / Ontology Mapping
      ↓
Attribute Validation / Quality Check
      ↓
Normalized Product Profile
```

### 4.1 优先级

1. **规则、品牌字典、标准库**：品牌、Reference、尺寸单位、常见系列/别名优先确定性处理。
2. **文本模型**：从标题、描述、结构化字段抽取系列/型号/颜色/材质/尺寸等。
3. **OCR**：只针对吊牌、表背、保卡、包装、商品细节图中的文字。
4. **VLM**：用于文本缺失、图片包含结构化线索或多个证据冲突的少量样本。
5. **Quality Check**：任何模型抽取结果在进入 Canonical Evidence 前都必须校验。

### 4.2 OCR

首选 **PP-OCRv5** 作为轻量 OCR 基线。OCR 后必须把字符串分类为：

```text
reference / style_code
serial_number
date_code
size
material_text
other_text
```

不能看到数字就当作型号。序列号、日期码、价格或库存编号不得进入 Reference 精确匹配通道。

### 4.3 VLM

首选 **Qwen3-VL-4B-Instruct** 作为 PoC 的结构化属性提取模型；输入多张商品图与已知文本，输出严格 JSON：

```json
{
  "attributes": [],
  "observations": [],
  "conflicts": [],
  "unknown_fields": []
}
```

VLM 的职责是“产生可验证证据”，不是直接输出最终 `SAME_VARIANT`。任何 VLM 属性都必须经过：

- 枚举/ontology 校验
- 品牌规则校验
- 与文本/OCR/官方主档交叉校验
- 置信度与来源记录

VLM 无权直接写 Canonical 主档。

---

## 5. 候选召回：Blocking + Hybrid Retrieval

100 万–1000 万记录禁止 N² 比较。识别必须先召回小候选集，再精排/判定。

### 5.1 召回通道

对每条 Source Product 并行执行：

1. **Exact Reference / Style Code**
2. **Brand + normalized model/reference**
3. **Brand + series/model +关键结构化属性**
4. **BM25**：标准化标题、系列、型号、关键属性文本
5. **Text ANN**：整体语义与脏文本召回
6. **Image ANN**：缺型号、弱文本、局部别名场景
7. **历史 Canonical / Alias**
8. **Entity Discovery 邻居**：仅未挂接 Source Product 之间使用

默认先按 `category`、通常按 `brand_id` 做过滤，避免跨品牌视觉相似污染候选。

### 5.2 初始 Top-K

以下是 PoC 初始值，最终必须由 `Recall@K` 调参，而不是固定业务规则：

| 通道 | 初始 K |
|---|---:|
| Exact Reference/Style | ≤20 |
| BM25 | 30 |
| Text ANN | 50 |
| Image ANN | 每图 30，总上限 60 |
| Attribute Combination | 30 |
| 合并去重后候选池 | 上限约 150 |
| Cheap Rerank 后 | 约 30 |
| Expensive Pair Matcher | Top 10–20 |

候选召回阶段可以追求高 Recall，因为它本身不做自动合并。PoC 的目标应至少验证：

```text
Recall@100 >= 99.5%  （在内部已确认正样本上，按品类/来源分别统计）
```

如果这个门槛无法达到，不能通过降低最终 Matcher 阈值来补救，应先改善 Blocking/索引/Embedding。

### 5.3 搜索引擎

生产首选：

- **OpenSearch 3.x 稳定版本**：Exact/BM25/过滤式 k-NN/混合检索统一承载；选择支持 filtered k-NN、on-disk vector 与量化的版本。
- **Milvus**：只有当 1000 万级真实压测证明 OpenSearch 的向量吞吐、召回或资源成本不满足要求时再拆出独立向量服务。
- **pgvector**：适合小规模 PoC/内部工具，不作为 1000 万级过滤式 Hybrid ANN 的首选唯一引擎。

这样 MVP 不需要从第一天维护“关系库 + OpenSearch + Milvus”三套在线数据面。

### 5.4 索引组织

至少维护：

```text
canonical_variant_search_vN
source_product_search_vN
product_image_search_vN
```

腕表和箱包通过 category/alias 分离；优先用字段过滤而不是为每个品牌创建独立索引。模型升级时创建新版本索引，完成回填和验证后再切 alias，避免在线混用不同 embedding 版本。

---

## 6. Embedding 与视觉检索

### 6.1 Text Embedding

PoC 默认：**Qwen3-Embedding-0.6B**。

理由：中文/英文多语言能力强，支持指令化 embedding 和 Matryoshka Representation Learning，0.6B 版本更适合百万到千万级批处理与在线召回。

建议先存 **512 维**向量进行域内 Recall@K 测试；如果 256/384 维在腕表与箱包 hard case 上无显著损失，可进一步压缩。不要因为公开 benchmark 表现直接固定维度。

文本内容不是直接拼原始 JSON，而是生成稳定模板，例如：

```text
brand=Rolex | series=Datejust | reference=126334 | size=41mm |
case=steel | bezel=white_gold_fluted | dial=blue | bracelet=oyster
```

同时保留标题/描述的自由文本表示，用于脏数据和别名召回。

### 6.2 Text Reranker

PoC 默认：**Qwen3-Reranker-0.6B**，只对候选池 Top-N 使用，不对全库使用。

### 6.3 Image Embedding

PoC 默认：**SigLIP2-B/16 384**；同时以 **DINOv2** 作为对照基线。

每张图片独立保存向量，禁止把所有图片简单平均成一个向量。多图相似度采用：

- `max_similarity`
- top-k mean
- 图片类型加权（正面/背面/细节/吊牌）

图片预处理：

```text
质量检测 → 去重复/近重复 → 主体检测/裁剪 → Embedding
```

对于局部图、背景复杂、商品占比过小的图片，降低 image evidence 权重。

**Qwen3-VL-Embedding-2B / Qwen3-VL-Reranker-2B** 作为多模态 challenger，仅在内部奢侈品难例集上显著提升 `Coverage @ 99.9% Precision` 时进入在线困难样本路径。

### 6.4 向量存储量级

仅计算原始向量：

```text
10M × 512 × FP32 = 20.48 GB
10M × 256 × FP32 = 10.24 GB
10M × 512 × FP16 = 10.24 GB
10M × 256 × FP16 = 5.12 GB
```

这不包含 ANN 图、索引元数据、副本和多图向量。文本 + 图片同时存储时必须分别预算。生产优先使用 on-disk/量化能力，并以真实 Recall/Latency 压测决定 128/256/512 维和量化等级。

---

## 7. Pair Matching / Decider

### 7.1 分层判定

```text
Candidate Pair
    ↓
Stage 0: Hard Conflict Engine
    ↓
Stage 1: Deterministic / Cheap Feature Scoring
    ↓
Stage 2: Text Reranker / Compact Matcher
    ↓
Stage 3: Multimodal Difficult-Pair Matcher（仅少量）
    ↓
Confidence Calibration
    ↓
Decision
```

### 7.2 Pair Features

结构化规则特征：

```text
brand_exact
reference_full_exact
reference_base_exact
series_exact
model_exact
size_exact
material_exact
color_exact
hardware_exact
edition_exact
attribute_missing_count
```

文本特征：

```text
title_bm25
title_embedding_cosine
series_similarity
model_similarity
attribute_text_similarity
cross_encoder_score
```

图片特征：

```text
image_max_cosine
image_topk_mean
image_quality
visual_attribute_agreement
ocr_reference_agreement
```

来源特征：

```text
source_trust
attribute_provenance
independent_source_count
official_source_hit
```

### 7.3 冷启动 Matcher

零标签阶段不要训练“万能二分类模型”。先使用可解释 evidence bundle：

**腕表高置信正例示例**：

```text
brand exact
+ official full reference exact
+ 所有已知 Hard Attribute 无冲突
+ Reference 来源达到高可信等级
```

**箱包高置信正例示例**：

```text
brand exact
+ official style code exact
+ official size/material/color/hardware 无冲突
+ 至少一个高可信来源或两条独立可信证据
```

模型得分高但存在任何可信 Hard Conflict 时仍然 `CERTAIN_NON_MATCH`。

### 7.4 有标签后的 Student Matcher

累计审核数据后分阶段升级：

- 约 10K 级：LightGBM / Logistic Regression 等结构化、可解释基线。
- 数据继续增长：compact Cross Encoder / domain contrastive encoder。
- 100K+ 且 hard-negative 覆盖充分后，再评估更复杂的多模态 Student。

所有概率型 Matcher 必须做 calibration，并且阈值按“自动匹配 Precision”设定，不能使用固定 `score > 0.8`。

Ditto 作为离线 Entity Matching benchmark 基线保留，不作为默认生产依赖。

### 7.5 LLM/VLM Difficult Pair

LLM/VLM 只处理：

- 高价值 `UNKNOWN` 候选
- 新款/Vintage 无主档
- 文本和图片冲突
- 型号疑似错误
- 需要解释品牌专有材质/系列别名的样本

目标是让昂贵模型最终只覆盖少量疑难流量（PoC 先以 <5% 为成本控制目标），并把其经审核确认的结果用于 Student 蒸馏/训练。

---

## 8. Conflict Engine

Conflict Engine 是自动识别的独立安全墙，必须在最终 Matcher 之前和之后都执行一次。

### 8.1 冲突等级

```text
HARD_CONFLICT       可信、明确，禁止 SAME_VARIANT
POSSIBLE_CONFLICT   来源/抽取质量不足，输出 UNKNOWN
NO_CONFLICT         未发现冲突；不代表一定相同
```

### 8.2 示例

```text
brand: Rolex vs Omega
→ HARD_CONFLICT

case_diameter: 36mm vs 41mm
且双方来自官方/高可信规格
→ HARD_CONFLICT

bag_size: Mini vs Medium
且为官方标准尺寸
→ HARD_CONFLICT

color: 图片视觉黑 vs 深蓝
但无官方颜色文字
→ POSSIBLE_CONFLICT，不做 Hard Reject

strap: 官方钢链 vs 二手平台当前皮带
且疑似后换表带
→ POSSIBLE_CONFLICT，不直接认为不同官方变体
```

### 8.3 Hard Negative 资产

自动构造并长期维护：

- 同系列不同表径
- 同 Reference 家族不同盘面/材质后缀
- 同包型不同 Mini/Small/Medium
- 同包型不同皮质/颜色/五金
- 图片极像但官方 Style Code 不同
- 同名跨年份改款

这些样本优先进入训练与回归集，而不是使用大量随机负样本稀释难度。

---

## 9. Confidence 与自动决策

### 9.1 输出状态

```text
CERTAIN_MATCH
LIKELY_MATCH
UNKNOWN
CERTAIN_NON_MATCH
```

策略：

```text
CERTAIN_MATCH
  → 建 Recognition Record
  → 标记机器已识别
  → Pending Review

LIKELY_MATCH
  → 不自动挂接，可进入困难样本/审核候选

UNKNOWN
  → 不处理，可触发补充证据

CERTAIN_NON_MATCH
  → 当前候选排除
```

### 9.2 置信度不是单分数

保存：

```text
match_score
evidence_score
conflict_score
source_trust_score
confidence_level
decision
policy_version
model_version
```

最终决策由**类别化规则 + 校准模型 + 冲突约束**共同决定。

### 9.3 自动上线 KPI

核心 KPI：

```text
Coverage @ 99.9% Precision
```

同时观察：

- Precision@Auto-Match
- Recall / PR Curve
- Abstain Rate
- False Merge per Million
- 各 Hard Negative 子集 Precision
- Seen SKU / Unseen SKU
- 每来源、品牌、品类分桶指标

如果目标是用“零观测错误”的审核样本证明错误率上界低于 0.1%，95% 单侧置信水平下约需要 **2995 个独立已审核自动匹配样本**；正式上线应按主要品类/来源分桶验证，而不是只看全局平均。

---

## 10. Entity Linking 与 Entity Discovery

### 10.1 Entity Linking

增量商品首先只对 `ACTIVE Canonical Variant` 检索：

```text
Source Product
   ↓
Hybrid Retrieval on Canonical
   ↓
Pair Matching + Conflict
   ↓
CERTAIN_MATCH → Recognition
```

这是已有腕表标准库的主路径。

### 10.2 Entity Discovery

未能挂接的记录进入独立发现池：

```text
Unmatched Source Products
   ↓
Strict Blocking
   ↓
High-confidence Pair Edges
   ↓
Constraint-aware Clustering
   ↓
PROPOSED Canonical Variant
```

一个 `PROPOSED` Variant 至少满足以下之一才允许升级 ACTIVE：

1. 有品牌官方/高可信标准主档锚点；或
2. 有至少两个**相互独立**的高可信来源共同支持关键变体属性，且无 Hard Conflict；并通过审核/主档治理策略。

多个网站复制同一个上游 feed 不算独立证据。

---

## 11. Graph Resolution 与传递冲突

禁止使用：

```text
Connected Component = 一个 Canonical Variant
```

因为可能出现：

```text
A = B
B = C
A != C
```

### 11.1 推荐实现

使用 **Union-Find / Cluster Graph + Cannot-Link Constraints + Cluster Signature**：

每个 cluster 保存：

```text
brand
official_reference/style_code set
size set
material set
color/dial set
hardware/bezel set
edition/generation set
source provenance
cannot_link cluster ids
```

合并两个 cluster 前：

1. 检查已有 cannot-link；
2. 对两边所有高可信 Hard Attributes 做交叉冲突检查；
3. 检查是否会引入同一 Source Product 的互斥关系；
4. 只有无冲突且 merge evidence 达到阈值才 union；
5. 一旦发现可信冲突，写入 `cannot_link`，阻断后续传递合并。

Pair score 只决定“是否有合并证据”，Cluster Constraint 决定“是否允许合并”。

---

## 12. 商品数据库与外部 Web Evidence

### 12.1 来源等级

```text
T0  品牌官网、官方目录、官方 Reference/Style Code 文档
T1  已验证内部标准库、授权/高质量品牌专业数据库
T2  高质量奢侈品/腕表专业平台与可信二手平台
T3  普通电商、聚合站、搜索网页
```

不同属性可以有不同来源可信度。例如：专业腕表库对 Reference/机芯可能很强，但二手卖家描述对原配表带未必可靠。

### 12.2 Web Search Agent

Web Agent 只在以下情况下触发：

- 内部 Canonical 无候选
- 新款刚发布
- Vintage / 已下架款
- 型号文本疑似错误
- 关键属性冲突需要第三方证据

检索优先级：

```text
官方域名 → 品牌/专业资料库 → 高质量二手平台 → 通用搜索
```

Agent 输出只能是：

```text
candidate_url
source_tier
extracted_attributes
evidence_snippets
image_refs
retrieved_at
content_hash
```

**Agent 无权直接写 Canonical Variant 或生成 CERTAIN_MATCH。** 外部网页内容视为不可信输入，必须经过解析、字段白名单、来源策略和 Conflict Engine。

### 12.3 Vintage / 新款

- 新款：优先品牌官网增量同步，快速生成 PROPOSED Canonical。
- Vintage：允许没有当前官网页面，依赖品牌档案、历史目录、专业数据库和多个独立可信来源。
- 同一个营销名称跨年份可能发生尺寸、材质、机芯、五金或版型变化；没有代际证据时默认不跨年代自动合并。

---

## 13. 冷启动与数据飞轮

### 13.1 0 Labels

使用：

- 官方 Reference / Style Code 精确规则
- Hard Constraint
- 品牌/系列/颜色/材质 ontology
- 预训练 Text/Image Embedding
- 高可信规则生成 Positive
- 结构化 Hard Negative 自动生成

### 13.2 10K 级审核数据

训练：

- GBDT / Logistic Pair Matcher
- threshold calibration
- source/brand/category-specific policy

重点采样边界样本和 hard negative，而不是随机样本。

### 13.3 100K+ 数据

评估：

- domain contrastive text/image encoder
- compact cross encoder
- multimodal student
- active learning

### 13.4 LLM Teacher

LLM/VLM 可以给困难 pair 输出结构化理由和候选标签，但 Teacher 标签必须和：

- 高置信规则
- 主档证据
- 审核结果

进行校验；确认后的样本用于训练更小 Student，避免大型模型承担千万级全量在线 Pair Matching。

---

## 14. 数据库设计

关系数据库作为事实源，推荐 PostgreSQL；如果现有基础设施以 MySQL 为主，也可使用 MySQL，搜索/向量能力仍由 OpenSearch 承担。

### 14.1 核心表

```text
brand
product_family
product_model
canonical_variant
canonical_relation

source
source_product
source_product_image

extracted_attribute
normalized_attribute
external_evidence

candidate_pair
match_evidence
conflict_record
recognition_record
review_record

model_version
policy_version
source_trust_policy
```

### 14.2 关键字段

`source_product`

```text
id
source_id
source_product_key
raw_payload_hash
brand_id
category_id
normalized_title
normalized_reference
normalized_style_code
status
first_seen_at
last_seen_at
```

`canonical_variant`

```text
id
brand_id
category_id
family_id
model_id
canonical_reference / style_code
status
variant_signature
created_from
confidence
```

`recognition_record`

```text
id
source_product_id
canonical_variant_id
decision
match_score
evidence_score
conflict_score
policy_version
model_version
evidence_snapshot
created_at
review_status
```

`match_evidence`

```text
candidate_pair_id
evidence_type
attribute_name
left_value
right_value
score
source_tier
provenance
extractor_version
```

### 14.3 关键索引

- `source_product(source_id, source_product_key)` unique
- `source_product(brand_id, category_id, normalized_reference)`
- `source_product(brand_id, category_id, normalized_style_code)`
- `canonical_variant(brand_id, category_id, canonical_reference)`
- `canonical_variant(brand_id, category_id, style_code)`
- `normalized_attribute(entity_type, entity_id, attribute_name)`
- `recognition_record(source_product_id, review_status)`
- `candidate_pair(min_product_id, max_product_id)` unique
- `conflict_record(left_id, right_id, conflict_type)`

Reference/Style Code 的唯一约束要考虑品牌后缀规则和历史复用，不应简单全局 unique。

---

## 15. 离线 Batch 与增量架构

### 15.1 历史初始化

```text
Raw Import
  ↓
Schema Mapping
  ↓
Attribute Extract/Normalize/QC
  ↓
Batch Text/Image Embedding
  ↓
Bulk OpenSearch Index
  ↓
Entity Linking to Existing Canonical
  ↓
Unmatched Entity Discovery
  ↓
Recognition / Proposed Canonical
```

处理单位按 `category → brand → series/model bucket` 分区，避免一次构造全库 Pair。

### 15.2 每日增量

```text
New / Changed Source Product
  ↓
Normalization + Embedding
  ↓
Search ACTIVE Canonical
  ↓
Search small unmatched-neighbor pool if needed
  ↓
Pair Match / Conflict
  ↓
Recognition or UNKNOWN/PROPOSED
```

增量绝不重新全库两两计算。

### 15.3 模型升级

- embedding/model_version 不覆盖旧向量。
- 建 shadow index `*_vN+1`。
- 离线回填并跑固定 benchmark。
- 达标后切 alias。
- Recognition 保留当时 model/policy/evidence snapshot，便于审计与回滚。

---

## 16. 生产架构

```text
                ┌──────────────────┐
                │ Source Connectors │
                └────────┬─────────┘
                         ↓
               Raw / Object Storage
                         ↓
             Normalize & Extract Workers
             ┌───────────┼────────────┐
             ↓           ↓            ↓
          Text/OCR     Image Prep     VLM Fallback
             └───────────┼────────────┘
                         ↓
                 Relational Catalog DB
                         ↓
            ┌────────────┴─────────────┐
            ↓                          ↓
      OpenSearch Hybrid          External Evidence
  Exact/BM25/Text ANN/Image ANN     Web Agent
            └────────────┬─────────────┘
                         ↓
                   Candidate Pool
                         ↓
                Reranker / Matcher
                         ↓
                  Conflict Engine
                         ↓
               Confidence / Policy
                         ↓
          ┌──────────────┴──────────────┐
          ↓                             ↓
      UNKNOWN                    CERTAIN_MATCH
                                        ↓
                               Entity Resolution
                                        ↓
                               Recognition Record
                                        ↓
                                  Pending Review
                                        ↓
                                   Label Store
```

建议组件：

- Catalog DB：PostgreSQL 16+ 或现有 MySQL
- Hybrid Search：OpenSearch 3.x
- Object Storage：S3 兼容对象存储
- Queue：Kafka / Pulsar / 现有企业 MQ
- Batch：Python/Ray 起步；已有 Spark 基础设施时可直接用 Spark
- Encoder Serving：Triton / TorchServe 类 GPU 服务
- VLM Serving：vLLM
- Cache：Redis（alias/ontology/热点 catalog，可选）

---

## 17. 成本与性能控制

### 17.1 成本原则

1. 规则/字典/精确索引最便宜，优先使用。
2. Embedding 在入库时批量计算一次，在线查询复用。
3. Reranker 只看几十个候选。
4. VLM 只看少量困难样本。
5. 不持久化所有通道产生的全部 Pair，只持久化最终候选和审计必要证据。
6. 图片原图存对象存储，搜索系统只保存 URL/元数据/向量。

### 17.2 资源预算公式

```text
VectorRawBytes = N × Dimension × BytesPerElement × VectorFamilies

DailyEmbeddingGPU = NewOrChangedItems × AvgImages × EncoderCost

PairInference = DailyItems × AvgCandidateAfterRerank

VLMInference = DailyItems × DifficultCaseRate
```

云成本必须以实际采购区域、实例、QPS、图片数和副本数代入，方案阶段不写伪精确金额。

### 17.3 PoC 性能目标

- 标准增量识别路径：p95 目标 <500ms（不含异步 VLM/Web Agent）。
- VLM/Web Search：异步秒级路径，不阻塞普通自动匹配。
- expensive-model rate：先控制在候选/商品的 5% 以下，再按 Coverage 收益决定是否扩大。
- 1000 万 Canonical/Source 规模下必须做真实过滤式 ANN、索引构建时间、内存、磁盘和恢复压测。

---

## 18. Benchmark 与测试集

### 18.1 Gold Set

虽然线上判定不能依赖人工，但上线前必须建立人工确认 Gold Set。建议首版 5K–10K pair，并且**刻意提高 hard negative 比例**。

分层：

```text
watch / bag
source
brand
seen SKU / unseen SKU
current / vintage
with image / without image
with model / missing model / dirty model
clean attributes / conflicting attributes
```

### 18.2 时间切分

至少做一份 time-based holdout：训练/规则制定只看较早数据，测试使用后续出现的新 SKU，防止 benchmark 只证明“记住历史商品”。

### 18.3 腕表 Corner Cases

- 同系列不同尺寸
- 同型号不同盘面
- 同型号不同材质
- Reference 标点/空格/后缀变化
- Reference 部分缺失
- 卖家写错型号
- 同图不同文本
- 同文不同图
- 限量版
- 改款/老款/新款
- 后换表带

### 18.4 箱包 Corner Cases

- Mini / Small / Medium
- 不同皮质
- 不同颜色
- 不同五金
- 不同印花
- 季节限定
- 同名不同年份/代际
- 官方命名变化
- 卖家俗称
- 图片只拍局部
- 无图片
- 序列号/日期码被误当款号

### 18.5 分阶段指标

Retrieval：

```text
Recall@20 / @50 / @100
MRR
candidate_count
latency
```

Pair Matching：

```text
Precision
Recall
PR-AUC
Calibration Error
Coverage @ target precision
```

Cluster：

```text
Cluster Purity
False Merge Rate
Split Rate
Constraint Violation Count
```

生产：

```text
Coverage @ 99.9% Precision
False Merge per Million
UNKNOWN Rate
VLM/Web Agent Rate
cost per recognized item
```

---

## 19. MVP → Production 路线

### Phase 0：数据审计与 Gold Set

交付：

- 腕表/箱包字段完整度统计
- 品牌/系列/型号 alias 字典
- 5K–10K 高质量 benchmark
- Hard Conflict Matrix
- 来源可信度策略

没有这一步，不训练任何专用 Matcher。

### Phase 1：确定性腕表 Canonical Linking

上线能力：

- Schema Normalization
- Reference 解析
- Exact / Brand+Model Retrieval
- Hard Conflict Engine
- Recognition / Review 数据结构

只自动覆盖最确定的腕表样本，先验证 99.9% Precision 闭环。

### Phase 2：Hybrid Text Retrieval + Pair Matcher

加入：

- BM25
- Qwen3-Embedding-0.6B
- Qwen3-Reranker-0.6B
- 箱包主路径
- Calibration

扩大无完整型号但文本较强的覆盖率。

### Phase 3：Image / OCR / Multimodal

加入：

- PP-OCRv5
- SigLIP2
- DINOv2 对照
- 多图聚合
- Qwen3-VL difficult-case 属性抽取

重点提升缺型号、图片强、文本弱场景，而不是替换规则/文本路径。

### Phase 4：Entity Discovery + Web Evidence

加入：

- unmatched source graph
- constraint-aware clustering
- PROPOSED Canonical
- 官方/专业来源 Web Agent
- Vintage / 新款策略

### Phase 5：监督 Student + Continuous Learning

加入：

- review labels
- hard negative mining
- active learning
- domain fine-tuned encoder/matcher
- shadow model / A-B / rollback

---

## 20. 正式开启自动识别的 Gate

所有条件同时满足才允许扩大自动匹配：

1. Gold Set 的主要品类/来源分桶 `Precision@Auto-Match >= 99.9%`。
2. `Recall@100 >= 99.5%`，否则先修 Retrieval。
3. 统计样本量足以支撑目标错误率；不能只凭几十/几百条“零错误”。
4. 所有已定义 Hard Conflict 用例必须保持 0 自动错合并。
5. Seen / Unseen SKU、Vintage、缺图、缺型号、脏型号分别达标，不能只看整体平均。
6. 每个品牌/品类/来源均有独立阈值或 kill switch。
7. 模型/规则/ontology 版本均可回滚。
8. Recognition Record 能重放当时所有关键证据与决策。

任何指标退化时，优先降低 Coverage、提高 UNKNOWN，而不是放宽 Hard Constraint。

---

## 21. PoC 实验矩阵

PoC 不追求模型数量，而验证会改变架构的关键选择：

| 问题 | A | B | 决策指标 |
|---|---|---|---|
| Text Embedding | Qwen3-Embedding-0.6B 512d | 256/384d 或 4B | Recall@100 / RAM / latency |
| Image Embedding | SigLIP2-B/16 | DINOv2 | hard-case Recall@K |
| Multimodal Retrieval | 单独 text+image ANN 融合 | Qwen3-VL-Embedding-2B | Coverage@99.9 / GPU cost |
| Rerank | Qwen3-Reranker-0.6B | structured GBDT | Precision / latency |
| Difficult Pair | structured + text | Qwen3-VL-Reranker/VLM | hard-negative precision |
| Vector Store | OpenSearch on-disk/quantized | Milvus | recall/QPS/RAM/disk/ops |
| Cluster | constrained union | naive connected component | constraint violation / false merge |

只有域内 benchmark 显著证明收益的复杂模型才进入生产链路。

---

## 22. 主要技术依据

- Amazon Science — Learning variant product relationship and variation attributes from e-commerce website structures（VARM）：强调 Variant Relationship 与普通 Entity Resolution 应分开建模，并识别具体 variation attributes。  
  https://www.amazon.science/publications/learning-variant-product-relationship-and-variation-attributes-from-e-commerce-website-structures

- Walmart Global Tech — How Walmart uses LLMs to manage its massive product catalogs：把 Attribute Extraction 与 Quality Check 拆为独立阶段，并保留属性级质量要求。  
  https://tech.walmart.com/content/walmart-global-tech/en_us/news/articles/how-walmart-uses-llms-to-manage-its-massive-product-catalogs.html

- Microsoft Research — Matching Unstructured Product Offers to Structured Product Descriptions：跨 merchant offer 到结构化 catalog 的 schema/attribute matching，并显式处理 missing/mismatch。  
  https://www.microsoft.com/en-us/research/publication/matching-unstructured-product-offers-to-structured-product-descriptions/

- Microsoft Research — Synthesizing Products for Online Catalogs：同时处理已有 Catalog 匹配与 unmatched offers 的新产品合成。  
  https://www.microsoft.com/en-us/research/publication/synthesizing-products-for-online-catalogs/

- WDC Products Benchmark / Corpus：现实电商 Entity Matching、corner cases、unseen entities 与训练数据规模研究。  
  https://github.com/wbsg-uni-mannheim/wdcproducts

- Ditto：开源 Entity Matching 基线，采用 Blocking → Matching 两阶段结构。  
  https://github.com/megagonlabs/ditto

- Qwen3 Embedding / Reranker：多语言 text embedding、MRL 与 reranker。  
  https://github.com/QwenLM/Qwen3-Embedding

- Qwen3-VL：多模态 OCR、结构化视觉理解与商品相关视觉能力。  
  https://github.com/QwenLM/Qwen3-VL

- Qwen3-VL-Embedding：多模态 dual-encoder retrieval 与 cross-attention reranker。  
  https://github.com/QwenLM/Qwen3-VL-Embedding

- Google Research SigLIP2：多语言 vision-language encoder。  
  https://github.com/google-research/big_vision

- Meta DINOv2：通用自监督视觉特征基线。  
  https://github.com/facebookresearch/dinov2

- PaddleOCR / PP-OCRv5：轻量多语言 OCR。  
  https://github.com/PaddlePaddle/PaddleOCR

- OpenSearch k-NN：过滤式向量搜索、on-disk vector 与量化能力，用于 Hybrid Retrieval。  
  https://docs.opensearch.org/latest/vector-search/filter-search-knn/

- Milvus：当独立向量集群确有必要时的规模化 ANN 备选。  
  https://milvus.io/docs/diskann.md

- End-to-end multi-modal product matching in fashion e-commerce（2024）：生产分布变化下，多模态文本/图像融合优于单模态并兼顾工程成本。  
  https://arxiv.org/abs/2404.09825

- Optimizing Product Deduplication in E-Commerce with Multimodal Embeddings（2025）：两阶段检索/判定与紧凑向量在超大规模商品去重中的工程案例。  
  https://arxiv.org/abs/2504.17654

---

## 23. 最终方案原则

生产系统固定遵循以下优先级：

```text
官方知识 / 标准化
        ↓
高召回候选检索
        ↓
结构化 + 文本 + 图片多证据判断
        ↓
Hard Conflict 安全墙
        ↓
约束图实体解析
        ↓
仅高置信结果自动 Recognition
        ↓
审核反馈形成训练数据
```

任何新模型、新向量库、新 Agent 或外部搜索服务，都只能在内部 benchmark 上证明其能提高 **Coverage @ 99.9% Precision**、降低成本或改善 unseen/Vintage/缺字段场景后，才允许替换现有组件。

系统永远允许 `UNKNOWN`。在证据不足、关键属性冲突或新 SKU 无法可靠归一时，**不自动识别就是正确结果**。
