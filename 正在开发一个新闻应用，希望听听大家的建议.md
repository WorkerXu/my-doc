# 正在开发一个新闻应用，希望听听大家的建议

## 1. 调研对象

- 编号：35
- 原始标题：Working on a news app, looking for some input from y'all
- 中文名称：正在开发一个新闻应用，希望听听大家的建议
- 来源：https://www.reddit.com/r/AppIdeas/comments/1o1krfe/working_on_a_news_app_looking_for_some_input_from/
- 调研目标：分析该新闻聚合应用关于主题订阅、排序、去重、信号融合和“摘要尚未完成时先返回首屏结果”的设计，对 1000+ 技术博客全历史抓取、增量同步、Markdown 知识库和 Web 管理方案的可复用价值。

> 说明：Reddit 正文当前对自动抓取有限制，本次以搜索索引中可验证的原帖标题、描述和问题片段为事实边界，不补造原帖未公开的实现代码或技术栈。可验证信息包括：应用受 Google News 启发、用户选择主题；作者重点询问 ranking/dedup 是否合理；候选排序信号包含 recency、domain authority、social proof、embeddings；并询问在摘要仍异步生成时如何尽快呈现首批结果。

## 2. 原帖透露的核心问题

原帖本质上不是在讨论“怎么爬一个网页”，而是在讨论内容聚合系统进入规模化后一定会遇到的四个问题：

1. **候选太多时，什么先展示、什么先处理**；
2. **多个来源报道同一件事，如何去重但不误删不同文章**；
3. **新鲜度、来源质量、社交热度、语义相关性如何组合**；
4. **摘要、Embedding 等慢任务尚未完成时，如何让用户先看到可用结果**。

这四点对博客知识库同样重要，但必须重新定义语义。新闻 App 可以为了用户体验只展示若干代表性内容；历史知识库则首先承担“保存事实、保证 Coverage、可回放”的职责。因此不能把新闻排序/去重逻辑直接搬进抓取主链。

## 3. 最重要的架构边界：采集完整性与排序必须解耦

### 3.1 两个平面解决不同问题

建议明确拆成：

```text
Collection Truth Plane
  Discovery -> URL Inventory -> Fetch -> Artifact -> Extract -> Quality
  -> Document Identity -> Canonical IR -> Document Version -> Markdown

Serving / Priority Plane
  Candidate/Document Version
  -> ranking features
  -> queue priority / search ranking / UI ordering
```

第一条链回答：

> “这个站点有哪些文章？抓到了什么？正文是什么？版本怎么变化？”

第二条链回答：

> “面对已经保存的候选，用户先看什么？昂贵的增强任务先处理什么？”

任何 `recency/domain_authority/social_proof/embedding_relevance` 都只能改变排序或资源优先级，不能变成“永久不抓”“永久丢弃”的 Coverage 决策。

### 3.2 为什么这条边界很关键

如果把排序分数用于采集准入，会产生不可恢复的偏差：

- 老文章天然输给新文章，历史回填永远不完整；
- 小众技术博客天然输给高权威域，长尾知识消失；
- 社交热度低的深度文章可能被过滤；
- Embedding 与当前主题不相似的文章无法进入未来的新主题检索；
- 排序模型升级后，过去被丢弃的候选无法重算。

正确做法是：**先保留可解释的候选和事实，再在下游做可重建排序。**

## 4. 去重不能只有一个概念：Document Identity 与 Story Cluster 必须分层

原帖关注 ranking/dedup。对于知识库，“去重”至少包含两个完全不同的语义。

### 4.1 Document Identity：它是不是同一篇逻辑文章

用于避免同一文章因为 URL 变体、redirect、canonical 或重复发现而生成多个 `document_id`。

优先证据：

```text
normalized_url
-> redirect final URL
-> rel=canonical
-> CMS stable id / GUID
-> exact artifact hash
-> normalized canonical semantic hash
```

这一层可以产生强约束式合并，但仍需保存 Observation 与原始 URL provenance。

### 4.2 Story Cluster：它们是不是在讲同一主题/同一事件

两个不同博客可能：

- 完整转载同一篇文章；
- 引用并扩写同一篇文章；
- 独立报道同一发布事件；
- 标题高度相似但技术观点完全不同。

这些内容适合“聚类”，不适合直接合并 `document_id`。

建议关系类型：

```text
SAME_DOCUMENT
SYNDICATED_COPY
SAME_STORY
RELATED
DISTINCT
```

`SAME_STORY` 只影响搜索结果折叠、推荐、摘要聚合，不删除成员文档，也不覆盖各自作者、时间、来源和版本。

### 4.3 分层判定流水线

```text
Stage 0: URL/Canonical strong identity
Stage 1: exact semantic hash
Stage 2: SimHash/MinHash near-copy candidate
Stage 3: title + publication time + outbound canonical/reference features
Stage 4: embedding similarity candidate
Stage 5: pairwise evidence gate
Stage 6: ambiguous -> review / keep separate
```

Embedding 只能用于“召回可能相似的候选对”，不能单独作为合并事实。原因是语义向量容易把“同一技术主题的不同文章”误判成重复。

### 4.4 建议数据模型

```text
document_relation
- left_document_id
- right_document_id
- relation_type
- confidence
- evidence_release_id
- evidence_ref
- decided_by
- decided_at

story_cluster
- cluster_id
- cluster_key
- cluster_release_id
- representative_document_id
- first_seen_at
- last_seen_at
- state

cluster_membership
- cluster_id
- document_id
- membership_type
- score
- evidence_ref
- release_id
```

这样可以实现“展示层折叠”，同时保持知识库事实完整。

## 5. 排序信号的正确工程化

原帖列出的 `recency/domain authority/social proof/embeddings` 可以保留，但需要拆成独立特征并版本化。

### 5.1 Recency

新闻应用中可使用指数衰减：

```text
freshness = exp(-age_hours / tau)
```

知识库中它适合：

- 增量任务优先级；
- 搜索结果排序；
- 最新内容首屏；

不适合：历史文章采集过滤。

### 5.2 Domain Authority / Source Quality

不要使用一个不可解释“权威分”控制是否保存。建议分解为：

```text
source_quality_features
- extraction_success_rate
- historical_content_quality
- author/source metadata completeness
- update reliability
- canonical consistency
- spam/error rate
- administrator trust override
```

它可以帮助排序、质量诊断或减少噪声，但不能把低分站点从已批准 Source 的 Backfill 中静默排除。

### 5.3 Social Proof

点赞、分享、评论数具有平台偏差和时间偏差，而且很多技术博客根本没有这些字段。因此：

- 只能作为 optional feature；
- 缺失值不能等价于 0 质量；
- 必须记录来源和采集时间；
- 不进入 Canonical 内容事实。

### 5.4 Embedding Relevance

Embedding 适合：

- 主题相关度排序；
- Story Cluster 候选召回；
- 语义检索；
- 丰富的多样性/去相似排序。

但必须绑定 `embedding_release`。模型升级时重建 Projection，而不是修改原始文档身份。

### 5.5 排序模型建议

优先使用可解释的两阶段排序：

```text
Stage A: hard eligibility
- source scope
- permissions
- current-version
- quality state

Stage B: soft ranking
score = w_f * freshness
      + w_r * semantic_relevance
      + w_q * source_quality
      + w_s * social_signal
      + w_d * diversity_boost
      - w_dup * near_duplicate_penalty
```

权重属于 `serving_rank_release`，通过离线 Golden Query / 在线 Canary 更新。

## 6. “摘要没完成也先出结果”：Progressive Enrichment

这是本次调研对现有博客知识库方案最直接的增强点。

### 6.1 Canonical Markdown 不应等待 LLM

推荐状态链：

```text
DISCOVERED
-> FETCHED
-> EXTRACTED
-> QUALITY_PASS
-> CANONICAL_READY
-> MARKDOWN_READY        # 用户已经可读、可导出
-> ENRICHMENT_PENDING
-> EMBEDDING_READY
-> SUMMARY_READY
-> FULLY_READY
```

只要 Canonical IR 通过 Quality Gate，就立即生成 Markdown 并进入基础关键词检索。摘要、标签、Embedding、实体抽取作为异步 Projection/Enrichment 逐步补齐。

这样能同时实现：

- 首批文章更快进入知识库；
- LLM/Embedding 限流不会阻塞采集；
- 主链即使外部模型服务故障仍然稳定；
- 可按资源预算让新文档优先增强、历史文档后台补齐。

### 6.2 Enrichment Job 必须幂等

```text
content_enrichment_job
- version_id
- enrichment_type
- release_id
- priority
- state
- attempt
- input_hash
- output_ref
- queued_at
- started_at
- finished_at
- error_type
```

幂等键：

```text
ENRICH:{version_id}:{enrichment_type}:{release_id}
```

如果同一 `semantic_hash` 已产生相同 Release 的结果，可安全复用。

### 6.3 两个用户体验 SLO

建议新增：

```text
Time To First Markdown (TTFM)
= discovered_at -> markdown_ready_at

Time To Fully Ready (TTFR)
= discovered_at -> required_enrichment_complete_at
```

对于知识库，TTFM 应优先于 TTFR。不能为了等摘要把已经合格的 Markdown 卡在队列里。

## 7. 用排序优化后台资源，而不是改变事实

1000 站历史回填时，真正昂贵的是 Browser、Repair、Embedding 和 LLM。排序思想可用于“先处理谁”。

建议 Backlog Priority：

```text
P0  新发现的增量正文、Freshness SLO 临近任务
P1  已检测到正文变更的现有文档
P2  新历史文档的 Canonical 主链
P3  可恢复的 Browser/Repair
P4  新文档 Embedding/轻量增强
P5  历史文档摘要/重嵌入/低优先增强
```

每层再叠加 aging，避免低优先任务永久饥饿。

关键不变量：

> priority 决定“什么时候处理”，不决定“是否存在”。

## 8. Story Cluster 对知识库检索的价值

如果 1000 个技术博客同时报道某个发布事件，直接检索可能出现一屏相似结果。可在 Search Projection 层进行：

```text
BM25 / vector candidate
-> current version filter
-> story cluster aware rerank
-> same cluster representative first
-> diversity budget
-> user expands cluster to see all sources
```

代表文档不能固定为“最大域名”。可以结合：

- 与 Query 的相关度；
- 内容完整性；
- 原始发布时间；
- 是否为官方/第一方来源；
- 用户 Source 偏好；
- 文档质量；

动态选代表结果。

RAG 场景也不应把整个 Cluster 永久压缩为一篇，因为不同博客可能提供互补代码、性能数字或反例。正确做法是在 Context Builder 中先做 cluster-aware diversity，再保留必要的多来源证据。

## 9. Web 管理需要新增的能力

### 9.1 Identity / Cluster 诊断

展示：

- Document Identity 合并链；
- canonical/redirect/hash 证据；
- Story Cluster 成员；
- SimHash/MinHash/Embedding 相似度；
- 自动判定 Release；
- 人工 `merge identity / split identity / merge cluster / split cluster`；
- 任何人工动作写 Audit Log。

### 9.2 Enrichment Pipeline

展示：

- Markdown Ready 数；
- Enrichment Pending 数；
- Embedding/Summary/Entity 各自 Queue Age；
- TTFM/TTFR p50/p95；
- 失败原因、Retry、预算；
- 按 Source/Release 重跑某种 Enrichment。

### 9.3 Ranking Diagnostics

任一结果可展开：

```text
freshness contribution
semantic relevance
source quality contribution
social signal
cluster duplicate penalty
diversity boost
final score
serving_rank_release
```

这使“为什么排在这里”可以审计，而不是只有一个黑盒分数。

## 10. 容易踩的坑

### 10.1 Embedding 相似就直接删除

错误。语义相似不等于同一文档。只能先进入 cluster candidate。

### 10.2 用热度决定历史抓取

错误。会永久损害 Coverage。

### 10.3 为等摘要延迟 Markdown

错误。LLM 是慢、贵、可失败依赖，不应成为 Canonical 主链发布条件。

### 10.4 Story Cluster 覆盖原始 Document

错误。Cluster 是 Projection/Relationship，不是替代事实。

### 10.5 缺失 Social Proof 当作低质量

错误。不同来源可观测字段不同，缺失和 0 必须区分。

### 10.6 在线模型直接改变 Identity

高风险。Identity/Cluster/Ranking Release 必须版本化，模糊结果保守分离，并允许回放和人工纠正。

## 11. 对博客知识库完整方案的具体优化

基于本次调研，现有方案无需推翻，但建议补强：

1. 明确 **Collection Truth 与 Serving/Priority Plane 分离**，任何排序特征都不得直接降低 Coverage；
2. 把“多层去重”拆为 **Document Identity Dedup** 与 **Story Cluster**，前者决定逻辑文章身份，后者只做近重复/同事件聚类；
3. 新增 `document_relation/story_cluster/cluster_membership`，保存版本化聚类证据；
4. Embedding 只做近重复候选召回，不作为唯一 merge 条件；
5. 新增 `serving_rank_release`，将 recency、source quality、social proof、semantic relevance、diversity 全部作为可解释软排序信号；
6. 主链在 Quality PASS 后立即生成 Canonical Markdown，不等待 LLM；
7. 新增 `content_enrichment_job`，Summary/Embedding/Entity 等异步、幂等、可单独重放；
8. 新增 `TTFM/TTFR` 两级延迟 SLO，优先保证首个可用 Markdown；
9. Fair Scheduler 在 Browser/Repair/LLM/Embedding backlog 中加入版本化 priority，但使用 aging 防止历史任务饿死；
10. Search/RAG 加入 cluster-aware dedup/diversity，而不是删除聚类成员；
11. Web Admin 增加 Identity/Cluster 审核、Enrichment 队列与 Ranking Diagnostics；
12. 验收标准增加“排序不影响 Coverage、模糊近重复不误合并、Markdown 可在增强未完成时先发布、聚类可拆分和回放”。

## 12. 最终结论

原帖对本项目最有价值的不是某个新闻排序公式，而是提出了规模化内容聚合的三个核心工程矛盾：**重复内容、排序信号、慢增强延迟**。

把这些能力用于 1000+ 技术博客时，推荐最终边界是：

```text
先完整采集、保存事实
        ↓
强证据解决 Document Identity
        ↓
弱/语义证据形成 Story Cluster，不删除成员
        ↓
Quality PASS 即发布 Canonical Markdown
        ↓
Summary / Embedding / Entity 异步增强
        ↓
Recency / Quality / Social / Semantic 只参与软排序与资源优先级
        ↓
Web 管理提供 Cluster、Enrichment、Ranking 可解释诊断
```

这能同时满足全历史 Coverage、增量 Freshness、快速首个可用结果、检索去重、多来源保真和长期可扩展性。

## 13. 参考资料

- 原始讨论：https://www.reddit.com/r/AppIdeas/comments/1o1krfe/working_on_a_news_app_looking_for_some_input_from/
- 调研索引：https://incorporated-jail-assigned-figure.trycloudflare.com/
