# 正在开发一个新闻应用，希望听听大家的建议

## 1. 调研对象

- 编号：35
- 原始标题：Working on a news app, looking for some input from y'all
- 中文名称：正在开发一个新闻应用，希望听听大家的建议
- 地址：https://www.reddit.com/r/AppIdeas/comments/1o1krfe/working_on_a_news_app_looking_for_some_input_from/
- 发布时点：Reddit 页面显示约 2025 年 10 月
- 调研目标：分析其「元搜索 + 抓取 + GPT + 用户反馈 + 自带来源」思路，对 1000+ 技术博客全历史抓取、增量同步、Markdown 知识库和 Web 管理平台的可复用价值。

本文严格区分两类内容：

1. **原帖明确公开的信息**：作者自己描述的实现与计划；
2. **本文工程化推导**：针对博客知识库场景给出的平台设计，不冒充原作者已经实现。

---

## 2. 原帖明确透露了什么

原帖的核心不是单纯的「新闻爬虫」，而是一个面向个人信息摄取的闭环聚合系统。

作者明确说明：

- 产品受 Google News 启发，用户按 topic/keyword 订阅；
- 初始内容发现采用 **meta-search**；
- 正文通过 scraper 抓取；
- GPT 用于内容处理；
- 用户可以对每天的一批结果进行 rating/comment；
- 系统会根据这些反馈重写下一次搜索 prompt，以逐步匹配用户偏好；
- 后续计划支持 **Bring Your Own Sources**，候选格式包括 RSS、Atom、JSON API、email inbox/newsletter；
- 作者重点征求 ranking/dedup、recency/domain authority/social proof/embedding 等排序信号，以及「摘要尚未生成时如何先展示首批结果」的建议。

在评论中，作者进一步明确了技术栈：

```text
SearXNG -> 聚合搜索得到初始文章候选
Crawl4AI -> 抓取文章内容
GPT      -> 内容处理/摘要等智能能力
Tailwind -> 当前基础 UI
```

另有评论者分享过一个失败案例：其 LLM 总结可以生成内容摘要，但文章链接不可靠甚至被生成错误 URL。这个案例不是对某个模型的一般性结论，却非常有价值地提醒我们：**来源 URL 和内容 provenance 不能由生成模型创造。**

---

## 3. 原型的实际技术链路

按原帖信息，可以抽象为：

```text
Topic / Keyword
      |
      v
SearXNG Meta Search
      |
      v
Candidate URL Batch
      |
      v
Crawl4AI Fetch / Render / Extract
      |
      +------> 原始 URL / 页面事实
      |
      v
GPT Summary / Processing
      |
      v
Rank / Dedup / Daily Batch
      |
      v
User Rating + Comment
      |
      v
Preference Feedback
      |
      v
Rewrite Next Search Prompt
```

它的优点是从第一天就形成了「发现 -> 抓取 -> 智能处理 -> 用户反馈 -> 再发现」闭环，而不是只做一次性爬虫。

但这条链路直接用于「1000 个已批准技术博客的全历史知识库」会有明显问题：

- 搜索引擎召回不是站点历史 Coverage 证明；
- 个性化反馈会天然压制长尾和旧文章；
- 搜索排序变化会导致候选集合不可复现；
- LLM 改写搜索 prompt 不适合直接修改全局采集规则；
- 新闻去重倾向于只留代表结果，而知识库需要保留独立来源事实；
- 摘要或生成链接不能成为 Canonical 内容或来源地址。

因此应该借鉴它的**消费层闭环**，而不是把它的搜索逻辑替换成采集真相。

---

## 4. SearXNG 的技术原理与正确定位

SearXNG 是元搜索引擎，会把查询分发给多个搜索引擎/数据源适配器，再汇总结果。官方还提供 `/search` 等 HTTP API，并可配置 JSON、CSV、RSS 等输出格式。

对博客知识库而言，它适合两个场景。

### 4.1 Coverage Gap Discovery

当 Sitemap、CMS API、Archive、RSS 等权威 Provider 已跑完，但仍怀疑有缺口时，可用：

```text
source:example.com + year/month/path/topic query
  -> SearXNG
  -> URL normalize
  -> 与 URL Inventory diff
  -> 新候选进入验证
```

搜索结果只能形成 `Gap Evidence`，不能得出「全历史完整」。原因包括：

- 搜索引擎索引并非全量；
- 深层旧页面可能未收录；
- 搜索结果有 cap；
- 同一查询在时间上可能漂移；
- 搜索引擎排序目标与知识库 Coverage 不一致。

### 4.2 Topic Watch / User Discovery

原帖最值得保留的是另一种用途：为用户临时主题订阅发现**已批准 Source 之外**的潜在新来源。

```text
User Watch Query
 -> Query Plan Release
 -> SearXNG/Search Adapters
 -> Candidate URL
 -> Policy/Scope/Safety Gate
 -> Fetch + Quality
 -> Temporary Discovery Result
 -> 可选 Promote 成正式 Source
```

这条链必须与正式 Source Backfill 分开。它回答「用户最近想看什么」，不回答「知识库应该永久保存什么」。

---

## 5. Crawl4AI 的技术原理与正确定位

Crawl4AI 官方能力包括异步爬取、浏览器渲染、动态页面操作、HTML 到 Markdown、CSS/XPath/LLM 结构化抽取等。`AsyncWebCrawler` 与 `CrawlerRunConfig` 允许把 Browser 行为、缓存、等待条件和抽取策略配置化。

因此它很适合：

- JavaScript Render Only；
- 简单 Load More / Scroll；
- 动态链接发现；
- 站点接入 Probe；
- 生成一个 Markdown/正文候选用于诊断和 Candidate Agreement。

但 **Crawl4AI 直接输出的 Markdown 不应成为知识库 Canonical Truth**。原因不是 Crawl4AI 能力不足，而是生产知识库需要：

- 可比较的多 Extractor Candidate；
- 独立 Quality Gate；
- 版本化 Canonical IR；
- 可重放 Renderer；
- 对代码块、表格、列表、图片、链接的统一规则；
- Extractor 升级后可从不可变 Artifact 离线重算。

所以正确关系是：

```text
Crawl4AI = Fetch/Render/Discovery/Extraction Candidate
Canonical IR = 结构化内容真相
Markdown = Canonical IR 的可重建 Projection
```

复杂多步、有循环/分支/每步提交/Checkpoint 的交互仍应交给直接 Playwright Stateful Engine，而不是无限堆叠 `js_code`。

---

## 6. 最重要的边界：Collection Truth 与 Interest/Serving Plane 分离

原帖的反馈闭环很有价值，但必须放在正确层次。

推荐拆成三个平面：

```text
A. Collection Truth Plane
Discovery -> URL Inventory -> Fetch -> Artifact -> Extract -> Quality
-> Document Identity -> Canonical IR -> Document Version -> Markdown

B. Discovery/Interest Plane
Topic Watch -> Query Plan -> Search Adapters -> New Candidate Sources/URLs
-> Safety/Policy Gate -> Optional Source Promotion

C. Serving/Personalization Plane
Canonical Documents -> Retrieval -> Rank -> Cluster Diversity
-> User Feedback -> Preference Model -> 下一次排序/Query Plan
```

不变量：

> 用户反馈可以改变「下一次搜什么、先看什么」，不能改变「已批准 Source 的历史文章是否应该被完整保存」。

否则会出现不可恢复的偏差：

- 老文章因 freshness 低被永久漏掉；
- 小众博客因 domain authority 低被排除；
- 用户短期兴趣把未来可能重要的主题从知识库抹掉；
- 排序模型升级后无法重建曾经被过滤掉的历史事实。

---

## 7. 不要直接“让 LLM 重写爬虫 Prompt”：使用版本化 Query Plan

原帖做法是根据 rating/comment 重写下一次搜索 prompt。原型阶段很好用，但生产平台不能把一段自由文本直接当不可审计的调度规则。

建议把反馈转成结构化偏好，再生成版本化 Query Plan。

### 7.1 Preference Event

```text
preference_event
- event_id
- principal_id
- watch_id
- document_id / cluster_id
- event_type      # like/dislike/save/hide/rate/comment/open
- value
- comment_text_ref
- created_at
```

原始反馈作为事实永久保存，后续模型更新不能覆盖。

### 7.2 Preference Feature

由 deterministic/LLM extractor 将反馈转为可解释特征：

```text
preference_feature
- principal_id
- watch_id
- topic
- entity
- source_affinity
- novelty_preference
- depth_preference
- language
- positive_weight
- negative_weight
- evidence_event_ids
- preference_release_id
```

### 7.3 Query Plan Release

```text
query_plan_release
- query_plan_id
- watch_id
- parent_release_id
- generated_by
- generator_release
- structured_constraints
- query_variants
- negative_terms
- source_hints
- exploration_budget
- valid_from
- state
```

执行时只使用已发布 Query Plan，而不是让 LLM 在 Worker 内临时自由改写。

这样能够：

- 回放「为什么那天搜到了这些结果」；
- 对比新旧偏好模型；
- 回滚错误偏好；
- 做 Canary；
- 防止一条异常反馈让整个主题搜索发生灾难性漂移。

---

## 8. 反馈闭环要防“兴趣坍缩”

纯粹根据点击/喜欢反馈不断强化，会快速形成 filter bubble：系统只搜索用户最近喜欢的内容，探索空间越来越小。

建议最终排序：

```text
score = relevance
      + source_affinity
      + freshness
      + quality
      + explicit_feedback
      + novelty
      + diversity
      - duplicate_penalty
```

并设置：

- `exploration_floor`：固定比例结果来自低置信新主题/新来源；
- preference time decay：旧偏好逐步衰减，但用户显式长期偏好可锁定；
- cluster diversity：同一 Story Cluster 不占满首屏；
- source diversity：避免同一站点霸屏；
- negative feedback scope：区分「不喜欢这篇」「不喜欢这个来源」「不喜欢这个主题」；
- reset/inspect：用户和管理员能看到并重置偏好。

个性化模型同样属于 Projection，必须可重建，不能修改 Canonical Document。

---

## 9. BYO Sources：把“增加网站”抽象成 Source Adapter

原帖计划支持 RSS/API/newsletter，正好可以补强本项目的可扩展接入。

不应写成：

```text
if wordpress ...
if substack ...
if rss ...
if json_api ...
```

而应建立统一 `Source Input / Provider Adapter` 接口。

### 9.1 推荐 Source Input 类型

```text
WEB_DOMAIN
RSS
ATOM
SITEMAP
CMS_API
JSON_API
NEWSLETTER_EMAIL   # 可选扩展
MANUAL_URL_LIST
SEARCH_WATCH       # 只做候选发现
```

### 9.2 Adapter 统一契约

```text
probe(input)
  -> capabilities
  -> auth requirements
  -> provider type
  -> sample observations

discover(cursor/range)
  -> observations[]
  -> next_cursor
  -> exhaustion evidence
verify(item)
  -> state / redirect / changed hint
```

所有 Adapter 最终都必须归一化成：

```text
url_observation
- source_id
- provider_type
- provider_item_id
- discovered_url
- canonical_hint
- published_hint
- updated_hint
- observed_at
- provenance_ref
```

这让以后新增 GraphQL、Webhook、平台 API、Newsletter Inbox 等输入时不需要改正文处理主链。

### 9.3 Provider 与 Source 必须分开

一个 Source 可以同时有：

```text
Source: example.com
Providers:
- sitemap
- rss
- wordpress api
- archive page
- search gap
```

Provider 只是发现视角；Source 是长期治理对象。多个 Provider 发现同一 URL 必须保留 provenance，但归并到同一个 URL Identity。

---

## 10. Newsletter / Email 输入如何安全并入

原帖把 email inbox/newsletter 列为候选 BYO 格式。它可以作为可选能力，但应与网页抓取区分。

推荐：

```text
Dedicated Inbound Mailbox
 -> MIME parser
 -> sender/domain allowlist
 -> HTML/Text body Artifact
 -> extract canonical/source links
 -> link fetch with normal egress policy
 -> canonical webpage exists ?
      yes -> webpage成为 Canonical Source Artifact
      no  -> email artifact作为独立 document provenance
```

关键点：

- 邮件内链接仍执行 SSRF/redirect/域名策略；
- `List-Unsubscribe`、tracking URL 不应误认为正文 canonical；
- 不能让邮件正文中的 prompt/instruction 改变 Agent 工具权限；
- 凭据必须进 Secret Store，不写 Source Profile 明文；
- email-only 内容需明确版权与导出策略；
- 邮件只是另一种 Provider，不应绕过 Quality/Identity/Version。

---

## 11. Provenance Link：LLM 永远不能“生成来源 URL”

原帖评论里「摘要有了但链接不可靠」的失败经历，对知识库非常重要。

系统必须坚持：

```text
Discover URL
 -> URL Observation
 -> Fetch Artifact
 -> Redirect Chain
 -> Canonical Candidate
 -> Document Identity
 -> Canonical URL
```

URL 必须来自网络观察事实。LLM 可以：

- 选择已有 URL；
- 判断某 URL 可能是正文/引用；
- 对链接作用分类。

LLM 不可以：

- 根据标题拼 URL；
- 根据摘要“猜”来源地址；
- 生成不存在的 citation；
- 用生成 URL 覆盖已有 canonical/redirect 证据。

建议所有用户可见引用都携带：

```text
provenance_link
- document_id
- version_id
- url_id
- observed_url
- redirect_final_url
- canonical_url
- artifact_id
- observed_at
- relation_type
```

Query/RAG 层只允许引用这里已经存在的链接。

---

## 12. Ranking / Dedup：原帖信号如何工程化

### 12.1 Document Identity 与 Story Cluster 分开

强证据用于「是不是同一篇文章」：

```text
normalized URL
-> redirect final URL
-> rel=canonical
-> CMS stable id / GUID
-> exact content hash
-> normalized semantic hash
```

弱/语义证据用于「是不是同一主题/事件」：

```text
SimHash/MinHash
+ title/time/reference
+ embedding ANN
-> pairwise evidence gate
-> SAME_STORY / RELATED / SYNDICATED_COPY
```

Embedding 只作为候选召回，不应单独触发 Document merge。

### 12.2 Serving Rank

原帖提出的 recency/domain authority/social proof/embeddings 都应是软特征：

```text
hard eligibility:
- auth/scope
- quality state
- current version
- policy

soft rank:
- query relevance
- recency
- source quality
- user preference
- optional social signal
- novelty/diversity
- cluster duplicate penalty
```

所有特征保存 contribution 与 release，Web Admin 可解释「为什么排在这里」。

---

## 13. “摘要没完成也先出结果”：Progressive Delivery

原帖询问怎样让首批结果更快出现。对知识库的正确做法不是「先生成一个半成品摘要」，而是让确定性主链先完成。

状态建议：

```text
DISCOVERED
-> FETCHED
-> EXTRACTED
-> QUALITY_PASS
-> CANONICAL_READY
-> MARKDOWN_READY
-> INDEX_TEXT_READY
-> ENRICHMENT_PENDING
   -> EMBEDDING_READY
   -> SUMMARY_READY
   -> ENTITY_READY
-> FULLY_READY
```

只要 `MARKDOWN_READY`，Web UI 就可以展示：

- title；
- source；
- published_at；
- canonical URL；
- Canonical Markdown；
- 「摘要生成中」状态。

之后通过 SSE/WebSocket/轮询刷新 Enrichment 状态即可。

两个独立 SLO：

```text
TTFM = discovered_at -> markdown_ready_at
TTFR = discovered_at -> required_enrichment_ready_at
```

主链不能等待 GPT/Embedding。LLM 限流或故障时仍然能持续建库。

---

## 14. 对 Web 管理后台的新增设计

本次调研建议在现有后台增加以下页面。

### 14.1 Source Inputs

- Source 主体；
- Provider/Adapter 列表；
- RSS/Atom/Sitemap/CMS API/JSON API 配置；
- Credential Secret 引用；
- Probe 结果；
- Provider Coverage 差异；
- Promote/Demote Provider；
- BYO Source 接入向导。

### 14.2 Topic Watch / Discovery

- watch query；
- Query Plan Release；
- SearXNG/Search Adapter 返回；
- 新域名候选；
- 与现有 Source 的重合；
- 一键 Promote 为 Source，但必须重新 Probe/Approval；
- Search 结果不得直接标记 Coverage complete。

### 14.3 Preference Diagnostics

- rating/comment/open/save/hide 事件；
- 当前结构化偏好；
- source/topic affinity；
- Query Plan Diff；
- ranking contribution；
- exploration ratio；
- reset/rollback preference release。

### 14.4 Provenance Diagnostics

- observed URL；
- redirect chain；
- canonical link；
- Provider provenance；
- Artifact；
- 用户看到的 citation；
- 禁止显示无法追溯的生成链接。

---

## 15. 安全边界

BYO Source 与 Search Watch 会把可访问 URL 的范围扩大，因此必须额外强化安全。

### 15.1 SSRF / Egress

每个输入 URL、API base URL、Redirect 和 Browser 子请求都执行：

- http/https scheme allowlist；
- DNS resolve 后阻断 loopback/private/link-local/metadata IP；
- DNS rebinding 二次校验；
- 端口策略；
- 最大 redirect；
- 下载大小和 MIME 限制；
- domain/source scoped concurrency。

### 15.2 Secret

API token、mailbox credential 不写入 Source JSON，保存为 Secret Reference；Worker 按最小权限临时读取。

### 15.3 Prompt Injection

网页、邮件、评论、文章正文一律是 Untrusted Data。它们不能：

- 改写 Tool Allowlist；
- 发起任意外网请求；
- 修改 Source Release；
- 改变采集 Scope；
- 绕过 robots/policy；
- 自动 Promote 新 Source。

LLM 只能产生 Candidate Query Plan/Rule，发布仍经过 Schema、Policy、Budget 与必要 Approval。

---

## 16. 建议新增领域模型

在现有模型上补充：

```text
source_input
source_provider_binding
subscription_watch
preference_event
preference_feature
preference_release
query_plan_release
query_execution
ranking_feedback
provenance_link
source_candidate
source_promotion_run
```

其中：

- `source_input`：用户给出的 domain/feed/api/mailbox 等接入入口；
- `source_provider_binding`：Source 与多个 Provider 的绑定；
- `subscription_watch`：用户主题订阅；
- `preference_event`：不可变用户反馈事实；
- `preference_release`：从反馈推导出的可重建偏好；
- `query_plan_release`：版本化搜索计划；
- `source_candidate`：搜索发现但尚未成为正式 Source 的域；
- `provenance_link`：用户可见链接的事实证据。

这些模型不应替代现有 Source/URL/Artifact/Document/Version，只是扩展 Discovery/Serving 能力。

---

## 17. 对现有博客知识库方案的最终优化结论

现有总体方案的核心边界是正确的，不需要推翻。尤其是：

- Authoritative First；
- Search Is Gap Filling；
- Collection Truth 与 Serving 分离；
- Document Identity 与 Story Cluster 分离；
- Progressive Enrichment；
- TTFM/TTFR；
- Crawl4AI/Playwright 分级；
- Canonical IR + 可重建 Markdown。

本次调研新增的实质性优化是：

1. **增加统一 Source Adapter / Source Input 层**，把 RSS、Atom、Sitemap、CMS/JSON API、Newsletter、手工 URL 等统一成 Provider Observation；
2. **增加 Topic Watch / Search Discovery Plane**，允许 SearXNG 发现新内容/新来源，但与正式 Coverage 分离；
3. **增加 Preference Event + Preference Release**，用户 rating/comment 不直接改爬虫；
4. **用版本化 Query Plan 取代“运行时自由重写 Prompt”**，支持审计、回滚和 Canary；
5. **Serving Rank 增加 user preference、novelty、exploration/diversity**，同时设置 exploration floor，避免兴趣坍缩；
6. **增加 Source Candidate -> Probe -> Approval -> Promote 流程**，让用户发现的新域名可安全进入长期知识库；
7. **强制 Provenance Link**，任何 citation URL 都必须来自真实 URL Observation/Artifact/redirect/canonical 证据，禁止 LLM 生成来源链接；
8. **BYO API/Email 强化 SSRF、Secret 与 Prompt Injection 边界**；
9. **Web Admin 增加 Source Inputs、Topic Watch、Preference Diagnostics、Provenance Diagnostics**；
10. 保持 **Markdown Ready 先于 Summary/Embedding**，把「首屏快」落实为 Progressive Delivery，而不是让 LLM 进入主链。

---

## 18. 推荐最终数据流

```text
                         +----------------------+
                         |   Web Admin / User   |
                         +----------+-----------+
                                    |
              +---------------------+----------------------+
              |                                            |
       Formal Source Input                            Topic Watch
 Domain/RSS/Atom/API/...                         + User Preference
              |                                            |
       Provider Adapters                           Query Plan Release
              |                                            |
 Authoritative Discovery                            SearXNG/Search
              |                                            |
       URL Inventory <---------- Source Candidate ----------+
              |
     Fetch / Crawl4AI / Playwright
              |
       Immutable Artifact
              |
      Extraction Portfolio
              |
        Quality Gate
              |
         Canonical IR
              |
      Document Version
              |
       Markdown Ready -----------------------> User First Result
              |                                      |
              |                              Rating/Comment Event
              |                                      |
       Async Enrichment                       Preference Release
 Summary / Embedding / Entity                        |
              |                                Next Query Plan
       Search Projection                              |
              +---------------- Serving Rank <--------+
                                    |
                         Cluster-aware Results
                                    |
                         Provenance-backed Links
```

关键原则可以浓缩为一句话：

> **搜索和反馈负责“更快找到用户想看的东西”，Authoritative Discovery 与 Canonical Pipeline 负责“完整、可证据化地保存知识事实”；两者可以互相提供候选，但不能共享删除权。**

---

## 19. 验收标准补充

新增能力至少满足：

- 新增 RSS/Atom/JSON API Provider 无需修改正文处理主链；
- 一个 Source 可绑定多个 Provider，且 Provider provenance 可追溯；
- SearXNG 返回结果不会直接使 Coverage 标记 complete；
- Search Watch 新域名必须经 Probe/Policy/Approval 才能成为正式 Source；
- 用户 dislike 某篇文章不会导致该 Source 历史 Backfill 丢文章；
- Preference/Query Plan 都有 Release，可回滚和重放；
- 个性化排序存在 exploration/diversity 预算；
- 任何用户可见 citation 可追溯到真实 observed URL / Artifact；
- LLM 不能生成或覆盖 canonical URL；
- Summary/Embedding 故障时 Markdown 仍持续产出；
- RSS/API/Email 的 URL 和 redirect 都执行统一 SSRF/egress policy；
- API/mailbox 凭据只通过 Secret Reference 使用。

---

## 20. 参考资料

- 原始 Reddit 讨论：https://www.reddit.com/r/AppIdeas/comments/1o1krfe/working_on_a_news_app_looking_for_some_input_from/
- SearXNG 官方文档：https://docs.searxng.org/
- SearXNG Search API：https://docs.searxng.org/dev/search_api.html
- Crawl4AI 官方文档：https://docs.crawl4ai.com/
- Crawl4AI AsyncWebCrawler：https://docs.crawl4ai.com/api/async-webcrawler/
- Crawl4AI Markdown Generation：https://docs.crawl4ai.com/core/markdown-generation/
