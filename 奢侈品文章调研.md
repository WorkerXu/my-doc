| 推荐理由 | 链接地址 |
|---|---|
| 专门研究用 LLM 从商品标题/描述抽取属性值，项目示例可直接抽出 `Model Number`，并在提示中要求保持原始表面字符串、缺失时返回 `n/a`；很适合把腕表 reference 当作目标属性做零/少样本抽取，再用格式与字典做严格校验，避免生成式结果直接参与同款判定。 | https://github.com/wbsg-uni-mannheim/ExtractGPT |
| 将商品属性值抽取改造成多标签分类，并通过同一属性内的负标签采样强化“相近值之间”的区分；可把同品牌、同前缀但实际不同的 reference 作为 hard negatives，针对最危险的近似型号误匹配做专门训练。 | https://arxiv.org/abs/2310.07137 |
| AdaTag 为不同商品属性动态生成属性特定解码器，同时共享跨属性知识；可以把 `reference/model number` 作为高优先级独立属性，在字段稀疏、来源 schema 不一致时仍保持型号抽取的专属性，而不必为每个平台维护完全独立模型。 | https://aclanthology.org/2021.acl-long.362/ |
| AVEQA 把属性抽取建模成问答，并显式训练 `no-answer` 分类器；可以直接询问商品的 reference，当标题里不存在可靠型号时输出“无答案”而不是猜测，天然契合“宁可漏匹配、绝不误匹配”的策略。 | https://doi.org/10.1145/3394486.3403047 |
| 面向数百万商品目录做文本型表格属性错误检测，重点包含属性值验证；适合放在 reference 抽取之后作为独立校验层，识别卖家自报字段、抓取字段或模型抽取出的异常型号，避免脏 reference 被 exact-match 规则放大成错误合并。 | https://aclanthology.org/2023.acl-industry.18/ |
| 专门解决新商品、新属性值不断进入电商系统时的少样本属性抽取，并学习动态阈值；与“只标几百对黄金样本、reference 长尾且持续新增”的条件高度一致，可用于新品牌/新系列的快速适配。 | https://arxiv.org/abs/2308.08413 |
| 研究多个 LLM 在电商属性值抽取上的集成，并已在大规模真实电商数据上验证；在本场景可把多模型对 reference 的一致结果作为保守通过条件，模型间不一致则直接弃权或进入人工复核，而不是平均分数后强行给出匹配。 | https://arxiv.org/abs/2403.00863 |
| 明确针对电商商品与属性持续增长的 lifelong attribute value extraction，兼顾噪声、灾难性遗忘和参数效率；适合三个来源长期增量抓取时持续维护 reference 抽取器，而不必每次新增品牌/系列都全量重训。 | https://aclanthology.org/2024.findings-acl.510/ |
| 针对弱监督信息抽取中的噪声训练样本做无监督、可扩展过滤，在电商属性值抽取上可显著提高 precision 且只轻微牺牲 recall；适合先用品牌字典、reference 正则等自动生成大量弱标签，再过滤后训练高精度型号抽取模型。 | https://aclanthology.org/2022.emnlp-industry.47/ |
| 面向零售中“外观极其相似、类别持续变化”的细粒度商品识别，用视觉 RAG 在少样本条件下识别具体商品 ID，新增类别可通过加入少量参考图适配；适合作为 reference 文本确认后的图像二次验证或冲突否决信号，而不是覆盖型号硬规则。 | https://openaccess.thecvf.com/content/CVPR2025W/FGVC/html/Lamm_A_Visual_RAG_Pipeline_for_Few-Shot_Fine-Grained_Product_Classification_CVPRW_2025_paper.html |
| FashionCLIP 在大规模时尚商品图文对上做领域化训练，可生成商品图片与标题的联合向量并支持检索；对二奢场景可用于比较跨来源图片/标题是否明显冲突，作为同 reference 候选的辅助否决器，尤其适合外观差异明显的材质、盘面和配色变体。 | https://github.com/patrickjohncyh/fashion-clip |
| 文中给出“适用于 iPhone 15 Pro 的保护壳却把 iPhone 15 Pro 误抽成商品自身型号”的真实电商 badcase，和二奢中配件、适配款、兼容型号导致的 reference 误归属问题高度同构；可直接借鉴其评测驱动思路，增加“型号角色/所属主体”校验与 badcase 回归集。 | https://zhuanlan.zhihu.com/p/2068102678136280151 |