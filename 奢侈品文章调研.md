| 推荐理由 | 链接地址 |
|---|---|
| 直接针对实体匹配的类别极不平衡问题，在指定 precision 下尽量提高 recall，并用主动采样降低标注量；非常适合只有几百对黄金标签、且误匹配容忍度极低的场景。 | https://ai.meta.com/research/publications/active-sampling-for-entity-matching/ |
| Selective Classification 允许模型拒识，并显式寻找“假阳性很少”的最大决策集合；可作为最终自动放行层，只接受极高置信度的同 reference number 候选。 | https://proceedings.mlr.press/v130/gangrade21a.html |
| 用 LLM 作为 teacher 自动标注实体匹配训练对，再训练高速小模型；把昂贵推理放到离线训练阶段，适合百万到千万级持续增量数据的线上匹配。 | https://arxiv.org/abs/2606.28823 |
| 在零人工标签下学习 match/unmatch 相似度分布，并利用实体匹配的传递性提升效果；适合作为冷启动与辅助候选判别基线，最终仍由 reference number 严格规则放行。 | https://arxiv.org/abs/1908.06049 |
| 提供 LLM 实体匹配的代码、数据与大量 prompt 示例，覆盖 zero/few-shot、规则生成和错误分析；可快速验证“品牌 + reference number”显式规则与少样本组合方案。 | https://github.com/wbsg-uni-mannheim/MatchGPT |
| 生成式模型既能做显式商品属性抽取，也能处理隐式属性，并在极少 in-context 示例下保持较好数据效率；适合从字段稀疏的腕表标题/描述里抽取型号候选。 | https://aclanthology.org/2023.emnlp-industry.55/ |
| 将商品属性抽取拆成候选生成与 Verify 两步，专门过滤阅读理解式抽取造成的 false positives；很适合 reference number 抽取中“宁可漏掉也不能抽错”的要求。 | https://aclanthology.org/2022.emnlp-industry.9/ |
| 将图片补充到文本属性抽取，并针对弱监督偏差、背景干扰和图文松散配对做校正；可把表盘、底盖等视觉证据作为型号文本不足时的辅助确认信号。 | https://aclanthology.org/2023.findings-acl.127/ |
| 面向电商文本属性的大规模规范化，用自监督双塔与度量学习将不同表述映射到 canonical value；可借鉴处理 reference number 的大小写、空格、连字符等形式差异，但应禁止语义近似合并不同型号。 | https://arxiv.org/abs/2106.09493 |
| 将迁移学习与主动学习结合解决低资源实体解析，并专门采样可能的 false positives/false negatives；适合用很少人工标注快速建立高精度匹配器并持续补强难例。 | https://aclanthology.org/P19-1586/ |