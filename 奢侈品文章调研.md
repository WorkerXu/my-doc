| 推荐理由 | 链接地址 |
|---|---|
| SIGMOD 2021 电商实体匹配项目直接从商品标题中按品牌规则抽取 model number，并把 `brand + cpu + model + ram` 用作 blocking key；与“腕表 reference number 可能埋在标题”高度同构，最适合借鉴“先抽取型号、再以精确型号作硬门槛”的思路。 | https://github.com/vinitra/entity-matcher |
| pyJedAI 是面向 Product Entity Resolution 的开源 Python 工具，覆盖 filtering、verification、blocking、similarity join 和 nearest-neighbor workflow；适合作为三源商品匹配工程骨架，再把 reference number 等值规则放在最终确认层。 | https://github.com/INFORMSJoC/2023.0410 |
| Sparkly 专做实体匹配 blocking，基于 BM25/TF-IDF 与 Spark/Lucene，可扩展到数千万乃至上亿条记录；很适合 100万–1000万级数据先生成小候选集，随后再用 reference number 硬规则做零容忍误匹配确认。 | https://github.com/anhaidgroup/sparkly |
| SC-Block 用监督对比学习做候选 blocking，并在 WDC-Block 这种最大 2000 亿笛卡尔积的多商店商品数据上验证可显著压缩候选集且不牺牲整体 F1；适合高规模场景的候选召回层，但不应代替 reference number 最终判定。 | https://arxiv.org/abs/2303.03132 |
| GSM 把 supervised meta-blocking 形式化为分类任务，并明确只需约 50 个训练样本即可工作；和“可人工标注几百对”的约束非常吻合，可用于学习高精度候选剪枝规则，把有限标注集中到最难的近似型号上。 | https://www.sciencedirect.com/science/article/pii/S0306437923001436 |
| Block-SCL 专门面向 product matching，利用 blocking 产生的 hard negatives 做监督对比学习，并可仅凭短商品标题学习细粒度区分；很适合拿“同品牌、外观很像但 reference number 不同”的商品作为强负例训练判别器。 | https://arxiv.org/abs/2207.02008 |
| 该论文研究当数据或匹配规则持续变化时如何复用已物化的 ER 结果做增量更新，而不是每次全量重跑；正好对应三来源持续增量抓取，可把“已确认 reference number 实体”作为历史状态进行局部更新。 | https://link.springer.com/article/10.1007/s00778-013-0315-0 |
| NLSHBlock 用神经化 LSH 支持复杂、任务定制的相似规则，并在半监督实体匹配场景中验证；适合把品牌、型号字符模式等领域约束融入大规模 blocking，但仍只用于候选生成而非最终身份确认。 | https://www.amazon.science/publications/neural-locality-sensitive-hashing-for-entity-blocking |
| 该多模态商品检索方案把商品文本与图片映射到统一空间，目标就是识别不同描述/图片下的同款商品；适合在 reference number 缺失时用图文共同找候选或做反证，最终仍应回到可验证的型号/参考号规则。 | https://arxiv.org/abs/2302.05093 |
| 这篇中文业务文章强调跨平台“同款”应优先认准 SKU/型号编码，并指出外观相近但渠道型号不同可能并非同一商品；可作为业务规则层的直观依据，支持本需求宁可漏配也不能把近似型号误合并。 | https://zhuanlan.zhihu.com/p/2058488672123671953 |